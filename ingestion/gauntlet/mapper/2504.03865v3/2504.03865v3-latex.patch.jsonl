# 2504.03865v3 — per-paper curated errata (LaTeX-oracle lane). See src/latex-ingest.ps1 :: PER-PAPER PATCH LANE. Re-applied on every latex_convert; guarded (fails loudly if stale).
{"op":"define_macro","name":"\\Parallelograml","body":"\\text{▱}","expect_uses":10,"class":"pgf_drawn_symbol","reason":"\\Parallelograml is a pgfpicture-DRAWN symbol (a left-slanted parallelogram, mapper-loss-main.tex:173) used as an operator mark (L_{▱} losses, \\Parallelograml_\\varphi(S,T) interleaving parallelograms). pgf drawing can never render in KaTeX (the internals guard rightly refuses to expand it), so the erratum substitutes the Unicode parallelogram U+25B1 in a \\text bridge — renders in KaTeX (font-metric warning only) and on GitHub. LIMITATION, flagged for review: Unicode has one parallelogram glyph, so the l/r slant distinction between \\Parallelograml and \\Parallelogramr is carried by context (their \\varphi vs \\psi subscripts), not by the glyph.","source_ref":"mapper-loss-main.tex:166-181 (pgf defs), 27 uses","authored_by":"latex-oracle session (flagged for human review)","authored_utc":"2026-07-06T08:20:00Z"}
{"op":"define_macro","name":"\\Parallelogramr","body":"\\text{▱}","expect_uses":3,"class":"pgf_drawn_symbol","reason":"Right-slanted twin of \\Parallelograml (mapper-loss-main.tex:166) — same pgf-drawn-symbol class, same Unicode substitution, same slant-distinction limitation (context/subscripts disambiguate).","source_ref":"mapper-loss-main.tex:166-181, 5 uses","authored_by":"latex-oracle session (flagged for human review)","authored_utc":"2026-07-06T08:20:00Z"}
