[Page 1]

# Gromov-Wasserstein Bound between Reeb and Mapper Graphs

Ziyad Oulhaj 1 , Mathieu Carri`ere 2 , and Bertrand Michel 1

1 Nantes Universite´, ´ Ecole Centrale Nantes, Laboratoire de Mathe´matiques Jean

Leray, CNRS UMR 6629, Nantes, France

2

DataShape, Centre Inria d’Universit´e Cˆote d’Azur, Sophia Antipolis, France

# Abstract

Since its introduction as a computable approximation of the Reeb graph, the Mapper graph has become one of the most popular tools from topological data analysis for performing data visualization and inference. However, finding an appropriate metric (that is, a tractable metric with theoretical guarantees) for comparing Reeb and Mapper graphs, in order to, e.g., quantify the rate of convergence of the Mapper graph to the Reeb graph, is a difficult problem. While several metrics have been proposed in the literature, none is able to incorporate measure information, when data points are sampled according to an underlying probability measure. The resulting Reeb and Mapper graphs are therefore purely deterministic and combinatorial, and substantial effort is thus required to ensure their statistical validity.

In this article, we handle this issue by treating Reeb and Mapper graphs as metric measure spaces. This allows us to use Gromov-Wasserstein metrics to compare these graphs directly in order to better incorporate the probability measures that data points are sampled from. Then, we describe the geometry that arises from this perspective, and we derive rates of convergence of the Mapper graph to the Reeb graph in this context. Finally, we showcase the usefulness of such metrics for Reeb and Mapper graphs in a few numerical experiments.

# 1 Introduction

The Mapper algorithm is a popular method from topological data analysis (TDA) for data visualization and inference. It provides a synthetic representation of a given dataset based on the topological variations of a continuous function defined on the data, often referred to as filter function . This representation typically takes the form of a graph, which facilitates data visualization and exploration. Indeed, the topological features of the Mapper graph (connected components, branches, loops, etc) are representatives of the topological features of the dataset, and can be used to identify its structures and subpopulations of interest. See Figure 2 for an illustation. As such, Mapper graphs have been successfully used in numerous applications, including, but not limited to, 3D meshes [Wan20; Ros+18], single-cell sequencing [Wan+18; Zec+14], machine learning [BC18; Nai+18], or neural network architectures [MR21; JPH21].

More fundamentally, the Mapper graph can be seen as a discrete version of the Reeb graph, which is a topological quotient space obtained by identifying the connected components of the level sets of a filter function, see Figure 1 for an illustration. Since the introduction of the Mapper algorithm, several metrics have been proposed to compare the resulting Mapper graph with its target Reeb graph, and more generally to compare several Mapper graphs and Reeb graphs together. As both objects are intrinsically combinatorial when computed over smooth enough filter functions (such as Morse type functions), these metrics focus on the combinatorial and algebraic aspects of Reeb and Mapper graphs. This point of view is also reinforced by the fact that the Mapper was initially introduced and studied in a deterministic setting for finite metric spaces.

A prominent example of such metrics is the one proposed in [DMP16], where Reeb graphs are studied from an algebraic point of view by considering them as cosheaves, i.e., covariant functors defined on the category of open intervals in R where morphisms are given by inclusions, that satisfy a specific gluing property. This property is satisfied in the case of Reeb graphs, after considering Reeb graphs as functors that associate to every open interval I the set of connected components of f -1 (I), and to every inclusion I ⊆ J the induced inclusion of the corresponding connected components. The authors of [MW16] then build on this point of view by proposing a similar categorified definition of the Mapper graph, as a functor on the category whose objects consist of the simplices of the nerve of an open cover with inclusions as morphisms and that, similarly to the Reeb graph, maps these objects to the set of connected components of the associated preimages of cover elements. In this context, the interleaving distance between the Reeb graph and the Mapper graph, defined by the authors in [DMP16], is well-defined and upper bounded by the resolution of the open cover, as proved in [MW16].

[Page 2]

Another line of work was initiated by [CO18], in which pseudometrics between Reeb and Mapper graphs are provided that focus on the topological features of graphs, as characterized by extended persistent homology . More precisely, by introducing an intermediate construction called the MultiNerve Mapper , the authors are able to link the extended persistence diagram of the Mapper graph to a pruned version of the extended persistence diagram of the Reeb graph. This in turn induces a natural alternative to the standard bottleneck distance between extended persistence diagrams, that allows to quantify how close the Reeb and Mapper graphs are, and how stable the Mapper graph is with respect to perturbations of filter functions and open covers.

Other notable distances for Reeb and Mapper graphs include the edit distance [BFL16; BLM21], which quantifies the number of edit transformations (such as, e.g., vertices and edges addition or deletion) one has to make in order to go from one graph to the other, and the functional distortion distance [BGW14], which measures how path lengths (w.r.t. filter function values) are distorted under the action of any continuous function that sends one graph onto the other. Both distances enjoy some theoretical guarantees: while the functional distortion distance is known to be stable, the edit distance has been proved to be universal [BLM21].

In practice, the Mapper graph comes in the form of a stochastic object, as it is usually built from a random set of data points sampled in a metric space according to some probability measure. It is thus natural to study the convergence of the Mapper graph to the Reeb graph with a statistical perspective, using the metrics mentioned above. For instance, an alternative Mapper construction called the enhanced Mapper is given in [Bro+21], in which it is proved to approximate the Reeb graph (up to the resolution of the cover) w.r.t. the interleaving distance with high probability, when computed on a large enough sample. Based on the extended persistent homology approach developed in [CO18], the authors in [CMO18] provide rates of convergence of Mapper graphs to the Reeb graph in expected value using an appropriate variation of the bottleneck distance. Finally, the statistical convergence of the Mapper complex (in the case of multivariate and stochastic filters) has been proposed in [CM22] using the Gromov-Hausdorff distance.

However, in all of these approaches, the target Reeb graph remains a fully deterministic object: although the sampling measure is taken into account in the construction of the Mapper graph or in the necessary hypotheses to ensure convergence, the Reeb graph does not come with a measureoriented description. In this article, we propose a new perspective on Mapper and Reeb graphs by considering these objects as metric measure spaces. This approach is motivated by theoretical considerations we want to enrich the mathematical descriptions of these objects and also by practical applications of Mapper graphs, in which they are frequently visualized with node sizes corresponding to their respective masses, i.e., how many points they contain.

Contributions. In this article, we study the Reeb and Mapper graphs of Morse filter functions for data points sampled on probability measures supported on Riemannian manifolds. Our contributions are two-fold:

• We endow Reeb and Mapper graphs with metric measure space structures , and then rigorously introduce the Gromov-Wassertein metric between these spaces. This point of view can be seen as the measure-aware version of the Gromov-Hausdorff metric proposed in [CM22]. Overall, this allows us to incorporate measure information in the computation of

[Page 3]

distances between Reeb and Mapper graphs, an information that is usually lost or hidden in other approaches, due to their combinatorial nature.

• We study the convergence of the Mapper to the Reeb graph in this framework. In our main result (Theorem 5.1), we provide an upper bound on the expectation of the p -Gromov-Wasserstein distance (for any p > 0) between the Mapper graph, chosen with an appropriate resolution, and the Reeb graph, under a sampling generative model. This bound is of the order of n − ν d + α , where ν = min   1 2 , d p ( d +1)   for any α > 0, and where d is the metric dimension of the measure. This upper bound relies in part on results on the convergence of the empirical measure in mean Wasserstein distance in a Polish metric space [WB19].

Related work. Since their introduction in [Me´m11] for object matching, Gromov-Wasserstein metrics have been widely used in many applications for comparing heterogeneous data, including, e.g., shape and graph matching [M´em09; Xu+19; XLC19; KDO23], which makes them particularly well-suited for comparing Reeb and Mapper graphs. While applications and computational aspects of the Gromov-Wasserstein metrics have been studied extensively, the statistical aspects have not been carefully studied until very recently [HRS23; Zha+24; RGK24]. In particular, in [Zha+24], the empirical quadratic Gromov-Wasserstein convergence rate over Euclidean spaces of different dimensions d x and d y is shown to be less than n − 2 / max(min( d x ,d y ) , 4) . Note that this result does not apply straightforwardly to the study of Reeb and Mapper graphs (which cannot be seen as Euclidean spaces), and we follow a different route for deriving our rates of convergence.

A close yet different approach than ours is the one proposed in [Wan+24] indeed, in this work, the authors also aim at adapting Reeb graphs and spaces for metric measure space inputs. However, a crucial difference with our method is in the treatment of the measure itself: while the goal of [Wan+24] is to produce a new measure-aware space and filter for the computation of Reeb graphs, and to study their stability properties, our approach directly incorporates the measure in the Reeb and Mapper graphs by considering them as metric measure spaces. Similarly, the authors of [RM24] have proposed a modification of the Mapper algorithm to account for the data distribution in the filter space, in order to define better suited open covers, and then provide rates of convergence with respect to the bottleneck distance, in a similar vein than [CMO18]. Again, our approach differs from this one in that the measure is not used for a better tuning of the Mapper parameters, but is rather directly incorporated in both Reeb and Mapper graphs, and in the distance between them.

Summary. Section 2 provides some necessary background on Reeb and Mapper graphs, as well as some elements of Riemannian geometry and Morse theory. In Section 3, we define a metric structure on the Reeb graph with the Hausdorff distance, and we study its induced topology. Similarly, we introduce a metric measure space structure for the Mapper graph in Section 4. The main result of this article about the rates of convergence of Mapper graphs to Reeb graphs in terms of the Gromov-Wasserstein distance is given in Section 5. Finally, in Section 6, we provide some illustrations of the practical use of Gromov-Wasserstein metrics to compare Mapper graphs.

# 2 Background

# 2.1 Reeb and Mapper graphs

We start by introducing Reeb graphs defined on general topological spaces.

Definition 1. Reeb graph.

Let X be a topological space and let f : X → R be a continuous function called filter function. Let ∼ f be the equivalence relation between two elements x and y in X defined by: x ∼ f y if and only if x and y are in the same connected component of f − 1 ( { v } ) for some v in f ( X ) . The Reeb graph R f ( X ) of X is then defined as the quotient space X/ ∼ f .

[Page 4]

![The image depicts a diagram of a circular insulator with a line of points labeled as \( \hat{r}_{1} \) and \( \hat{r}_{2} \). The insulator is a light pink color, and the line of points is a straight line. The insulator is labeled as \( \hat{r}_{1} \) and \( \hat{r}_{2} \). The insulator is a light pink color, and the line of points is a straight line. The insulator is labeled as \( \hat{r}_{1} \) and \( \hat{r}_{2} \). The insulator is a light pink color, and the line of points is a straight line. The insulator is labeled as \( \hat{r}_{1} \) and \( \hat{r}_{2} \). The insulator is a light pink color, and the line of points is a](<2506.02810v1/imageFile1.png>)

U3

Figure 1: Example of a Reeb graph computed on a torus using height as filter function. The critical values { v 1 ,v 2 ,v 3 ,v 4 } of the filter function f are represented in the middle. The Reeb graph is represented on the right.

![The image consists of five geometric shapes. The shapes are all different colors and have different patterns. Here is a detailed description of each shape: 1. **Large Pink Shape**: This shape has a solid pink color with small dots or dots scattered around it. It has a few lines extending from it, which are blue and green. 2. **Yellow Shape**: This shape is yellow with a dotted pattern. It has a few lines extending from it, which are blue and green. 3. **Blue Shape**: This shape is blue with a dotted pattern. It has a few lines extending from it, which are blue and green. 4. **Purple Shape**: This shape is purple with a dotted pattern. It has a few lines extending from it, which are blue and green. 5. **Green Shape**: This shape is green with a dotted pattern. It has a few lines extending from it, which are blue and green. The shapes are arranged in a](<2506.02810v1/imageFile2.png>)

Figure 2: Example of a Mapper graph computed on points sampled from a torus with its height as filter function. The three intervals used for covering its image are represented in different colors.

Figure 1 provides an illustration of a Reeb graph computed on a torus with its height as filter function.

Assume now that ( X, d) is a metric space and let f : X → R be a continuous function. The Mapper was introduced in [SMC+07] as a discrete and computable version of the Reeb graph R f ( X ). Assume that we are given a point cloud X n = { x 1 ,...,x n } ⊆ X with known pairwise dissimilarities, as well as a filter function f defined on each point of X n . The Mapper graph can then be computed with the following algorithm:

1. Cover the range of values Y n = f (X n ) with a set of consecutive intervals I 1 ,..., I r that overlap, i.e., one has I i ∩ I i +1 ̸ = ∅ for all 1 ≤ i ≤ r − 1.

̸

- 2. Group the points that fall in the same connected component of each preimage f − 1 (I j ), j ∈ { 1 ,...,r } . This defines a pullback cover C = {C 1 , 1 ,..., C 1 ,k 1 ,..., C r, 1 ,..., C r,k r } of X n .
- 3. The Mapper graph is defined as the nerve of C . Each node v j,k of the Mapper graph corresponds to an element C j,k of C , and two nodes v j,k and v j ′ ,k ′ are connected by an edge if and only if C j,k ∩ C j ′ ,k ′ ̸ = ∅ .


̸

Figure 2 provides a Mapper graph computed on points sampled from a torus with its height as filter function, and a cover of the filter image with three intervals.

[Page 5]

In practice, the second step of the above algorithm is performed using a clustering algorithm, but we leave this consideration aside as it is more suitable for a theoretical discussion. Note that some clustering algorithms come with guarantees as to whether they are able to successfully estimate the connected components of the original space X . For example, in [NSW08], Proposition 3.1. states that for points sampled from a Riemannian submanifold M of an Euclidean space, the union of Euclidean balls of radius ε centered on each sampled point for ε > 0 small enough has the same homology groups than M, given that the sample is ε/ 2 dense in M and under a geometric assumption on M (relating to its condition number). In particular, a clustering algorithm that consists in computing the connected components of an ε -neighborhood graph of the sampled points will correctly estimate the right connected components of M. Spectral clustering also comes with strong guarantees, see for instance [ACL11].

# 2.2 Elements of Riemannian geometry

We introduce here some elements of Riemannian geometry that will be used throughout this work. Indeed, we will typically assume further in this article that we observe data sampled from a Riemannian manifold, as well as the values of a continuous filter function defined on the points. Although these are standard results, we recall some proof elements in Appendix A for the sake of completeness. A general presentation of the results provided in this section, and notably a proof of Theorem 2.1, can be found in [Pet06].

We call Riemannian manifold every C ∞ -manifold M together with a Riemannian metric g. The Riemannian metric consists of an Euclidean inner product g p on each of the tangent spaces T p M of M, which satisfies that p  → g p ( X p ,Y p ) is smooth whenever X and Y are two smooth vector fields defined on M.

Given some local coordinates x ( p ) = ( x 1 ,...,x d ) on an open set U of M, they induce a basis of the tangent space T p M. We denote the associated coordinate vector fields as ( ∂ i ) d i =1 , and their dual 1-forms as ( dx i ) d i =1 . Under these notations, we can write the Riemannian metric g as:

$$
g = \sum _ { i , j } g ( \partial _ { i } , \partial _ { j } ) \cdot d x _ { i } \cdot d x _ { j } ,
$$

where dx i · dx j is the bilinear form: ( v,w )  → dx i ( v ) · dx j ( w ) . As such, g can be represented in local coordinates as a symmetric positive-definite matrix G( p ) with entries parametrized over U .

We will denote

and

$$
\lambda ( p ) = \min \{ \sqrt { \rho } , \, \rho \in S p ( G ( p ) ) \} ,
$$

$$
\mu ( p ) = \max \{ \sqrt { \rho } , \, \rho \in S p ( G ( p ) ) \} ,
$$

where Sp(G( p )) is the spectrum of G( p ). Notice that since the Riemannian metric is continuous, both λ and µ are continuous as well.

The following proposition states that the Riemannian metric is bi-Lipschitz equivalent to the Euclidean metric in local coordinates.

Proposition 2.1. Let U be an open set in M , on which we are given coordinates x ( p ) = ( x 1 ,...,x d ) . For every p ∈ M and for every v ∈ T p M :

$$
\lambda ( p ) \cdot \sqrt { g _ { 0 } ( v , v ) } \leq \sqrt { g ( v , v ) } \leq \mu ( p ) \cdot \sqrt { g _ { 0 } ( v , v ) } ,
$$

where g 0 =   i dx i · dx i is the Euclidean metric in the local coordinates.

Riemannian manifolds can be given a metric space structure as it is possible to measure the “length” of piecewise smooth curves using the Riemannian metric. This allows to define the geodesic distance d on M. The geodesic distance is locally bi-Lipschitz equivalent to the Euclidean distance in local coordinates.

[Page 6]

Proposition 2.2. Let p ∈ M . For every small enough neighborhood U of p , we have that for every q ∈ U :

$$
\lambda _ { 0 } \cdot d _ { 0 } ( p , q ) \leq d ( p , q ) \leq \mu _ { 0 } \cdot d _ { 0 } ( p , q ) ,
$$

where d 0 ( p,q ) is the Euclidean distance between the representations of p and q in local coordinates and where

$$
\lambda _ { 0 } = \inf _ { r \in U } \lambda ( r ) \ a n d \ \mu _ { 0 } = \sup _ { r \in U } \mu ( r ) .
$$

In particular λ 0 tends to λ ( p ) and µ 0 tends to µ ( p ) as Diam( U ) tends to 0 .

The Riemannian metric also induces a Borel measure Vol called the volume measure . It can be first defined by specifying the expected value of a function f : M → C compactly supported on a single chart φ : U ⊆ M → V ⊆ R d :

$$
\int _ { M } f \, d V o l = \int _ { V } \left ( f \cdot \sqrt { \det ( G ) } \right ) \circ \varphi ^ { - 1 } \, d \lambda ,
$$

where Note that in the above theorem, v ( d, k, ε ) is independent of the base point p . This is very convenient for providing bounds on the volume of geodesic balls in M that are uniform with respect to the choice of base points. Notice also that since S d k is a d -dimensional Riemannian manifold, Proposition 2.3 applies for v ( d, k, ε ), i.e.,

$$
G \colon U & \longrightarrow \mathbb { R } ^ { d \times d } \\ p & \longmapsto ( g ( \partial _ { i _ { | p } } , \partial _ { j _ { | p } } ) ) _ { i , j }
$$

and λ is the Lebesgue measure on R d .

It can be checked, with the help of the substitution rule, that the above definition does not depend on the choice of the coordinate neighborhood.

This definition can be extended to general compactly supported functions by using a partition of unity of a coordinate neighborhood cover of M: we simply sum the expected values over the elements of the partition.

Subsequently, as the expected value of compactly supported functions is well defined, the Riesz representation theorem allows to consider the unique associated positive Borel measure on M: this is the volume measure Vol.

We have the following asymptotic equivalence result for the volume of small geodesic balls.

Proposition 2.3. Volume of small geodesic balls.

Let B( p,ε ) be the geodesic ball around p ∈ M of radius ε . We have that:

$$
\text {Vol} \left ( B ( p , \varepsilon ) \right ) _ { \varepsilon \rightarrow 0 } \alpha _ { d } \cdot \varepsilon ^ { d } ,
$$

where α d is the volume of the unit ball in R d .

For the following result we will assume a lower bound on the Ricci curvature of M. The Ricci curvature Ric is a symmetric bilinear form on the tangent spaces and can be thought of as the Laplacian of the metric g, see for instance Chapter 9 in [Pet06]. We will adopt the convention that Ric ≥ k means that all eigenvalues ρ of Ric satisfy ρ ≥ k .

The next theorem allows to compare the volume of geodesic balls in M to the volume of geodesic balls in certain model manifolds S d k called constant-curvature space forms , d being the dimension of both M and S d k (see for example Subsection 9.1. in [Pet06]). Denote v ( d,k,r ) as the volume of a ball of radius r in S d k .

Theorem 2.1. Bishop-Cheeger-Gromov Theorem, see for instance Lemma 36 in [Pet06]. Suppose that M is a d -dimensional complete Riemannian manifold such that Ric ≥ ( d − 1) · k , for some k ∈ R . Then for any p ∈ M ,

$$
\varepsilon \longmapsto \frac { V o l \left ( B ( p , \varepsilon ) \right ) } { v ( d , k , \varepsilon ) }
$$

is a nonincreasing function whose limit is 1 as ε → 0 .

[Page 7]

$$
v ( d , k , \varepsilon ) \underset { \varepsilon \to 0 } { \sim } \alpha _ { d } \cdot \varepsilon ^ { d } .
$$

We now define the gradient of a smooth real valued function on a Riemannian manifold. Let x ( p ) = ( x 1 ,...,x d ) be some local coordinates on an open set U of M with associated coordinate vector fields ( ∂ i ) d i =1 . Let f : M → R be a smooth function. We can define its differential df as the 1-form that satisfies: df ( ∂ i ) = ∂f ∂x i , for all 1 ≤ i ≤ d. The gradient ∇ f of f is then defined as the vector field satisfying: g( ∇ f,v ) = df ( v ) , for all vector fields v . It can also be seen as the Riesz representative of df . Given our local coordinates, we can express the gradient vectors in their associated coordinate vector field bases.

Proposition 2.4. In the local coordinates associated to the open set U , we have that:

where:

In particular, we have:

$$
\nabla f = \sum _ { i = 1 } ^ { d } a _ { i } \cdot \partial _ { i } ,
$$

$$
( a _ { 1 } , \dots , a _ { d } ) = \left ( \frac { \partial f } { \partial x _ { 1 } } , \dots , \frac { \partial f } { \partial x _ { d } } \right ) \cdot [ g ( \partial _ { i } , \partial _ { j } ) ] _ { i , j } ^ { - 1 } \, .
$$

$$
\frac { 1 } { \mu ( p ) } \cdot \sqrt { \sum _ { i = 1 } ^ { d } \frac { \partial f ( p ) ^ { 2 } } { \partial x _ { i } } } \leq \sqrt { g \left ( \nabla f ( p ) , \nabla f ( p ) \right ) } \leq \frac { 1 } { \lambda ( p ) } \cdot \sqrt { \sum _ { i = 1 } ^ { d } \frac { \partial f ( p ) ^ { 2 } } { \partial x _ { i } } }
$$

# 2.3 Elements of Morse theory

In the article, we study the convergence of Mapper graphs towards Reeb graphs under the assumption that the filter function is a Morse function. Note that this assumption is not restrictive as the set of Morse functions is a dense open subset of C ∞ (M), for M a compact manifold. The aim of this section is to recall the cylindrical structures of preimages of intervals (under a Morse filter function) around non-critical points. See for instance [Mil63] for a comprehensive presentation of Morse theory.

Let M be a Riemannian manifold. Recall that a smooth map f : M → R is called a Morse function if its critical points are non-degenerate, i.e., the Hessian of f at the points where its gradient vanishes is non-singular. We first recall the standard Morse Lemma, which describes the behavior of a Morse function in the neighborhood of a critical point. Let Crit( f ) denote the set of critical points of a Morse function f : M → R defined on M. We will denote ∥ · ∥ 2 := g( · , · ).

Lemma 2.2. Morse Lemma.

Let f : M → R be a Morse function and c ∈ Crit( f ) . Then there exists a chart φ : U ⊆ M → R d containing c such that for every p ∈ U :

$$
f ( p ) = f ( c ) - \sum _ { j = 1 } ^ { i } x _ { j } ^ { 2 } + \sum _ { j = i + 1 } ^ { d } x _ { j } ^ { 2 } ,
$$

where x = φ ( p ) and the integer i depends only on the signature of the Hessian at the critical point c .

In particular, the Morse Lemma implies that the critical points of a Morse function are isolated, and Crit( f ) is finite when f is a Morse function defined on a compact manifold. Next, we recall a major result that relates the topology of a manifold to the analytic properties of a Morse function defined on it. See Figure 3 for an illustration.

[Page 8]

Lemma 2.3. Gradient flow of a smooth function.

Let f : M → R be a smooth function defined on a compact manifold M and a,b ∈ R such that a < b . If f − 1 ([ a,b ]) ∩ Crit( f ) = ∅ , then there exists a diffeomorphism ψ b − a between f − 1 ( { a } ) and f − 1 ( { b } ) .

We provide the proof of this well-known result here, as we will make use of this gradient flow several times later in the article.

Proof. Since f − 1 ([ a,b ]) does not contain critical points, the following function is well defined:

$$
\rho \colon \begin{cases} ^ { M } \longrightarrow \mathbb { R } \\ q \longmapsto \begin{cases} \frac { 1 } { \| \nabla f ( q ) \| ^ { 2 } } & \text {if } f ( q ) \in [ a , b ] , \, , \\ 0 & \text {otherwise} . \end{cases} \end{array}
$$

Now, consider the vector field defined by X q = ρ ( q ) · ∇ f ( q ) and the flow ( ψ t ) t ∈ R associated to X q . In other words, ψ t is the solution of the differential equation:

$$
\frac { d \psi _ { t } ( p ) } { d t } = X _ { \psi _ { t } ( p ) } , \, \psi _ { 0 } ( p ) = p .
$$

For details on the existence and uniqueness of ψ t , see Lemma 2 . 4 . of [Mil63]. The main assumption made is that X q vanishes outside a compact subset of M, which is true in our case since M is compact and f − 1 ([ a,b ]) is closed. Note that ∀ t ∈ R , ψ t is a diffeomorphism. Also, ψ t ◦ ψ s = ψ t + s .

Notice that we constructed ρ so that for every p ∈ f − 1 ([ a,b ]), with the notation q = ψ t ( p ), we have:

$$
\frac { d ( f \circ \psi _ { t } ) ( p ) } { d t } & = g \left ( \frac { d \psi _ { t } ( p ) } { d t } , \nabla f ( q ) \right ) \\ & = g \left ( \frac { \nabla f ( q ) } { \| \nabla f ( q ) \| ^ { 2 } } , \nabla f ( q ) \right ) \\ & = 1 .
$$

Consequently, f ◦ ψ t ( p ): t  → t + f ( p ), and therefore

$$
\psi _ { b - a } \left ( f ^ { - 1 } ( \{ a \} ) \right ) = f ^ { - 1 } ( \{ b \} ) ,
$$

and no critical points. Hence, let ε > 0 be such a positive real number. Then, for every v ′ ∈ [ v -ε, v + ε ], Lemma 2.3 ensures that f -1 ( { v } ) and f -1 ( { v ′ } ) are diffeomorphic, under the map ψ v ′ -v corresponding to the gradient flow of f . Define

$$
\psi _ { a - b } \left ( f ^ { - 1 } ( \{ b \} ) \right ) = f ^ { - 1 } ( \{ a \} ) .
$$

Finally, ψ b − a is a diffeomorphism between f − 1 ( { a } ) and f − 1 ( { b } ).

This following result is proved implicitly in Theorem 3.1 in [Mil63]. It is used for example in [DMP16] to show that Reeb graphs are an example of what the authors call constructible spaces . For the sake of completeness, we give here a full proof. See Figure 3.

Theorem 2.4. Cylindrical shape around non-critical points.

Let f : M → R be a Morse function defined on a connected compact Riemannian manifold M . Let also x ∈ M such that x ̸∈ Crit( f ) , and let v = f ( x ) . For a small enough ε > 0 such that f − 1 ([ v − ε,v + ε ]) does not contain critical points, the following map is a homeomorphism:

$$
\psi \colon f ^ { - 1 } ( \{ v \} ) \times [ - \varepsilon , \varepsilon ] & \longrightarrow f ^ { - 1 } ( [ v - \varepsilon , v + \varepsilon ] ) \\ ( q , t ) & \longmapsto \psi _ { t } ( q )
$$

[Page 9]

$$
\psi \colon f ^ { - 1 } ( \{ v \} ) \times [ - \varepsilon , \varepsilon ] & \longrightarrow f ^ { - 1 } ( [ v - \varepsilon , v + \varepsilon ] ) \\ ( q , t ) & \longmapsto \psi _ { t } ( q )
$$

The function ψ is continuous because it is locally Lipschitz:

- • for a fixed q ∈ f − 1 ( { v } ): t  → ψ t ( q ) is locally Lipschitz as it is continuously differentiable,
- • for a fixed t ∈ [ − ε,ε ]: q  → ψ t ( q ) is locally Lipschitz as it is a diffeomorphism.


The inverse of ψ is given by ψ − 1 : p  −→ ( ψ v − f ( p ) ( p ) ,f ( p ) − v ), which we now prove is continuous. Let p ∈ f − 1 ([ v − ε,v + ε ]) and ( p n ) n ∈ N a sequence in f − 1 ([ v − ε,v + ε ]) such that p n −→ n →∞ p . We have:

$$
d ( \psi _ { v - f ( p ) } ( p ) , \psi _ { v - f ( p _ { n } ) } ( p _ { n } ) ) \leq d ( \psi _ { v - f ( p ) } ( p ) , \psi _ { v - f ( p ) } ( p _ { n } ) ) + d ( \psi _ { v - f ( p ) } ( p _ { n } ) , \psi _ { v - f ( p _ { n } ) } ( p _ { n } ) ) .
$$

Now, as mentioned above, the function q  → ψ v − f ( p ) ( q ) is continuous because it is a diffeomorphism. Hence:

$$
d ( \psi _ { v - f ( p ) } ( p ) , \psi _ { v - f ( p ) } ( p _ { n } ) ) \underset { n \to \infty } { \longrightarrow } 0 .
$$

Moreover,

$$
d ( \psi _ { v - f ( p ) } ( p _ { n } ) , \psi _ { v - f ( p _ { n } ) } ( p _ { n } ) ) & \leq \left | \int _ { v - f ( p ) } ^ { v - f ( p _ { n } ) } \left \| \frac { d \psi _ { u } ( p _ { n } ) } { d u } \right \| d u \right | \\ & \leq \left | \int _ { v - f ( p ) } ^ { v - f ( p _ { n } ) } \frac { 1 } { \| \nabla f ( \psi _ { u } ( p _ { n } ) ) \| } \, d u \right | .
$$

As f is smooth and f − 1 ([ v − ε,v + ε ]) ∩ Crit( f ) = ∅ , and denoting:

$$
L \colon = \sup _ { q \in f ^ { - 1 } ( [ v - \varepsilon , v + \varepsilon ] ) } \frac { 1 } { \| \nabla f ( q ) \| } ,
$$

we have L < ∞ . Therefore,

$$
d ( \psi _ { v - f ( p ) } ( p _ { n } ) , \psi _ { v - f ( p _ { n } ) } ( p _ { n } ) \leq L \cdot | f ( p ) - f ( p _ { n } ) | \underset { n \to \infty } { \longrightarrow } 0 .
$$

Finally, we showed that

$$
d ( \psi _ { v - f ( p ) } ( p ) , \psi _ { v - f ( p _ { n } ) } ( p _ { n } ) ) \underset { n \to \infty } { \longrightarrow } 0 ,
$$

$$
) \underset { n \to \infty } { \longrightarrow } 0 ,
$$

and thus that ψ − 1 is continuous.

In the context of studying Reeb graphs built on Morse functions, we will use the following Proposition.

Proposition 2.5. Let f : M → R be a Morse function defined on a connected compact Riemannian manifold M . The level sets of f , f − 1 ( { v } ) for v ∈ f (M) , are locally path connected.

[Page 10]

![The image consists of a diagram with a circle and three cylinders. The circle is positioned at the top of the diagram. The diagram is labeled as ∂\overline{C} \overline{D} \overline{E} \overline{F} \overline{G} \overline{H} \overline{I} \overline{J} \overline{K} \overline{L} \overline{M} \overline{N} \overline{O} \overline{P} \overline{Q} \overline{R} \overline{S} \overline{T} \overline{U} \overline{V} \overline{W} \overline{X} \overline{Y} \overline{Z}. The diagram is labeled as ∂\overline{C} \overline{D} \](<2506.02810v1/imageFile3.png>)

~

Figure 3: Around a non-critical value v , the manifold is homeomorphic to a finite collection of cylinders, whose faces are given by the level set f − 1 ( { v } ).

Let c ∈ f − 1 ( { v } ) be a critical point. By Lemma 2.2, there exists an open neighborhood U ⊆ M of c and a chart φ : U ⊆ M → R d containing c such that for every p ∈ U :

$$
f ( p ) = f ( c ) - \sum _ { j = 1 } ^ { i } x _ { j } ^ { 2 } + \sum _ { j = i + 1 } ^ { d } x _ { j } ^ { 2 } ,
$$

where x = φ ( p ) and the integer i depends only on the signature of the Hessian at the critical point c . 1

Notice that the level set f − ( { v } ) is given in local coordinates by

$$
f ^ { - 1 } ( \{ v \} ) \cap U = \left \{ \varphi ^ { - 1 } ( x ) \colon \sum _ { j = 1 } ^ { i } x _ { j } ^ { 2 } = \sum _ { j = i + 1 } ^ { d } x _ { j } ^ { 2 } \right \} ,
$$

and that φ ( c ) = 0. For every p ∈ f − 1 ( { v } ) ∩ U , the path γ : t  → φ − 1 ( t · φ ( p )) between c and p stays in f − 1 ( { v } ) ∩ U . This is because for every t ∈ [0 , 1]

$$
\sum _ { j = 1 } ^ { i } \varphi ( \gamma ( t ) ) _ { j } ^ { 2 } - \sum _ { j = i + 1 } ^ { d } \varphi ( \gamma ( t ) ) _ { j } ^ { 2 } & = t ^ { 2 } \cdot \left ( \sum _ { j = 1 } ^ { i } \varphi ( p ) _ { j } ^ { 2 } - \sum _ { j = i + 1 } ^ { d } \varphi ( p ) _ { j } ^ { 2 } \right ) \\ & = 0 .
$$

Moreover, f − 1 ( { v } ) is locally path connected away from critical points. The set Crit( f ) is finite and hence closed. Therefore, M ′ = M \ Crit( f ) is an open submanifold of M. Using the Implicit Function Theorem on the restriction of f on M ′ as v is no longer a critical value, f − 1 ( { v } ) ∩ M ′ is locally path connected.

Proposition 2.5 has two major applications:

- • When looking at the level sets of a Morse function defined on a compact Riemannian manifold, connectedness and path connectedness are equivalent. Therefore, in what follows, we will use the term connected to mean both.
- • The number of connected components of a level set of a Morse function defined on a compact Riemannian manifold is always finite, as a level set is always compact and locally path connected.


[Page 11]

# 2.4 Geometry of Metric Measure Spaces

In this section, we present the framework of metric measure space geometry introduced in [Stu06]. We first recall that connected compact Riemannian manifolds fall under this framework, and then we introduce Gromov-Wasserstein metrics between metric measure spaces.

# 2.4.1 Metric Measure Spaces

A metric measure space (or mm-space for short) is a triple ( X, d , m) where ( X, d) is a Polish metric space (i.e., complete and separable) and where m is a measure on the Borel σ -algebra of ( X, d) that is locally finite. 1 The support of m is denoted as supp(m).

Proposition 2.6. A connected compact Riemannian manifold (M , g) together with its geodesic distance d and the volume measure (M , d , Vol) is a mm-space.

Proof. Since M is connected and compact, it is a complete metric space. It is also separable since it is second-countable. M being compact, the volume measure Vol is locally finite.

Let ( X 1 , d 1 , m 1 ) and ( X 2 , d 2 , m 2 ) be two mm-spaces. Recall that a map Φ: supp(m 1 ) → supp(m 2 ) is an isomorphism of mm-spaces between ( X 1 , d 1 , m 1 ) and ( X 2 , d 2 , m 2 ) if Φ is an isometry of metric spaces and if m 2 is the pushforward measure of m 1 under the map Φ. As isomorphisms induce an equivalence relation of mm-spaces, we denote the isomorphism equivalence class of a mm-space ( X, d , m) as [ X, d , m].

Metric measure space isomorphisms also induce the notion of variance of a mm-space ( X, d , m), defined as:

$$
V a r ( X , d , m ) = \inf _ { ( X ^ { \prime } , d ^ { \prime } , m ^ { \prime } ) \in [ X , d , m ] } \int _ { X ^ { \prime } } d ^ { \prime } ( x , z ) ^ { 2 } \, d m ^ { \prime } ( x ) .
$$

If ( X, d , m) is a compact mm-space then clearly one has inf z ∈ X   X d( x,z ) 2 d m( x ) < ∞ and then Var( X, d , m) is finite. Moreover, the variance is by definition an invariant of mm-space isomorphisms.

# 2.4.2 Gromov-Wasserstein Distance

# Definition 2. Wasserstein distance.

Let ( X, d) be a complete and separable metric space. Let m 1 and m 2 be two measures on the Borel σ -algebra of ( X, d) . For p ≥ 1 , the p -Wasserstein distance between m 1 and m 2 is defined as:

$$
W _ { p } ( m _ { 1 } , m _ { 2 } ) = \left ( \inf _ { m } \int _ { X \times X } d ( x , y ) ^ { p } \, d m ( x , y ) \right ) ^ { \frac { 1 } { p } } ,
$$

where the infimum is taken over all measures m on X × X that have marginals m 1 and m 2 .

# Definition 3. Metric coupling.

Let ( X 1 , d 1 , m 1 ) and ( X 2 , d 2 , m 2 ) be two mm-spaces. A metric coupling of d 1 and d 2 is any pseudometric 2 d on X 1 ⊔ X 2 that satisfies:

$$
\forall x , y \in \sup p ( m _ { 1 } ) \colon d ( x , y ) = d _ { 1 } ( x , y ) ,
$$

$$
\forall x , y \in \sup p ( m _ { 2 } ) \colon d ( x , y ) = d _ { 2 } ( x , y ) .
$$

1 By locally finite, we mean that ∀ x ∈ X, ∃ r > 0 such that m(B( x, r )) < ∞ . 2

A pseudometric can be zero for non-equal points.

[Page 12]

Definition 4. Gromov-Wasserstein distance.

For p ≥ 1 , the p -Gromov-Wasserstein distance between two mm-spaces ( X 1 , d 1 , m 1 ) and ( X 2 , d 2 , m 2 ) is defined as:

$$
G W _ { p } ( ( X _ { 1 } , d _ { 1 } , m _ { 1 } ) , ( X _ { 2 } , d _ { 2 } , m _ { 2 } ) ) = \left ( \inf _ { d , m } \int _ { X _ { 1 } \times X _ { 2 } } d ( x , y ) ^ { p } \, d m ( x , y ) \right ) ^ { \frac { 1 } { p } } ,
$$

where the infimum is taken over all measures m on X 1 × X 2 that have marginals m 1 and m 2 , and over all metric couplings d of d 1 and d 2 .

For the sake of simplicity, we will denote GW p (( X 1 , d 1 , m 1 ) , ( X 2 , d 2 , m 2 )) simply as GW p ( X 1 ,X 2 ) when there is no ambiguity over the metric and the measure associated to each space.

Proposition 2.7. The p -Gromov-Wasserstein distance between two mm-spaces, ( X 1 , d 1 , m 1 ) and ( X 2 , d 2 , m 2 ) , can be equivalently defined as:

$$
G W _ { p } ( ( X _ { 1 } , d _ { 1 } , m _ { 1 } ) , ( X _ { 2 } , d _ { 2 } , m _ { 2 } ) ) = \inf _ { \varphi _ { 1 } , \varphi _ { 2 } } W _ { p } ( m _ { 1 } \circ \varphi _ { 1 } ^ { - 1 } , m _ { 2 } \circ \varphi _ { 2 } ^ { - 1 } ) ,
$$

where the infimum is taken over all isometric embeddings φ 1 : supp(m 1 ) → X and φ 2 : supp(m 2 ) → X of the supports of m 1 and m 2 into a common metric space ( X, d) .

In [M´em11], an alternative definition of the p -Gromov-Wasserstein distance, inspired by a reformulation of the Gromov-Hausdorff distance, is provided as:

$$
G \hat { W } _ { p } ( ( X _ { 1 } , d _ { 1 } , m _ { 1 } ) , ( X _ { 2 } , d _ { 2 } , m _ { 2 } ) ) = \frac { 1 } { 2 } \left ( \inf _ { \substack { m \\ ( X _ { 1 } \times X _ { 2 } ) ^ { 2 } } } | d _ { 1 } ( x , x ^ { \prime } ) - d _ { 2 } ( y , y ^ { \prime } ) | ^ { p } \, d m ( x , y ) \otimes m ( x ^ { \prime } , y ^ { \prime } ) \right ) ^ { \frac { 1 } { p } } ,
$$

where the infimum is taken over all measures m on X 1 × X 2 that have marginals m 1 and m 2 . Due to its computational advantage for practical applications (such as, e.g., optimization), this formulation is popular in the literature, see for example [Zha+23; M´em09]. Moreover, it is shown in [M´em11] that ˆ GW p ≤ GW p (see Theorem 5.1(g) therein). In this article, we provide upper bounds for GW p which therefore automatically transfer to ˆ GW p .

Proposition 2.8. The 2 -Gromov-Wasserstein distance GW 2 is a metric on the set of mm-space isomorphism classes of finite variance.

For proofs of Propositions 2.7 and 2.8, see [Stu06].

# 3 Reeb graphs as Metric Measure Spaces

In this article, we aim at comparing Reeb graphs and Mapper graphs using tools and distances for metric spaces. In this section, we first define a metric structure on the Reeb graph using the Hausdorff distance, and then we study the induced topology of this corresponding metric space.

# 3.1 Hausdorff distance on Reeb graphs

We will denote the set of non-empty compact subsets of a metric space ( X, d) as C( X ). See [Hen99] for a comprehensive introduction of the Hausdorff distance.

# Definition 5. Hausdorff distance.

Let A,B be two subsets of a metric space ( X, d) , the Hausdorff distance between A and B is defined as:

$$
d _ { H } ( A , B ) = \max \left ( \sup _ { x \in A } d ( x , B ) , \sup _ { y \in B } d ( y , A ) \right ) ,
$$

where d( x,B ) = inf v ∈ B d( x,v ) and d( y,A ) = inf u ∈ A d( u,y ) .

[Page 13]

With the same notations as before, the Hausdorff distance can be equivalently defined as:

$$
d _ { H } ( A , B ) = \inf \left \{ \varepsilon > 0 , \, A \subseteq B ^ { \varepsilon } \text { and } B \subseteq A ^ { \varepsilon } \right \} ,
$$

where C ε =   z ∈ C B( z,ε ) for every subset C of X .

The Hausdorff distance d H is a metric on C( X ). Moreover, the topology of (C( X ) , d H ) follows closely from the topology of ( X, d). We illustrate this with the two following propositions proved in [Hen99].

Proposition 3.1. Suppose that ( X, d) is complete. Then (C( X ) , d H ) is also complete. Moreover, let ( a n ) n ∈ N be a Cauchy sequence in (C( X ) , d H ) , then a n −→ n →∞ a , where:

$$
a = \left \{ x \in X , \text { there exists a sequence } ( x _ { n } ) _ { n \in \mathbb { N } } \in \prod _ { n } a _ { n } \ s u c h \ t a t \ x _ { n } \underset { n \to \infty } { \longrightarrow } x \right \} .
$$

Proposition 3.2. If ( X, d) is compact then (C( X ) , d H ) is also compact.

Hereafter, (M , d) is assumed to be a connected compact Riemannian manifold together with its geodesic distance. We also consider a Morse function f : M → R . In this section, to alleviate notations, we will denote the Reeb graph R f (M) := M / ∼ f by R.

In order to use the Hausdorff distance, we prove the following:

Proposition 3.3. For every a ∈ R , a is closed as a subset of M .

Proof. Let a ∈ R, then a is defined as a connected component of f − 1 ( { v } ) for some v in f (M). The set a is therefore closed in f − 1 ( { v } ) since it is a connected component, as connected components are maximal connected subsets and the closure of a connected subset is also connected. Moreover, f − 1 ( { v } ) is closed in M because f is continuous. As such, a is closed in a closed subset and consequently closed in M.

The manifold M being compact, Proposition 3.3 proves that R ⊆ C(M). Thus, (R , d H ) is a metric space.

# 3.2 Topology of (R , d H )

In order to define Wasserstein and Gromov-Wasserstein distances on top of (R , d H ), the space R needs to be complete and separable. However, we first show that this metric space is not complete in general (see Figure 4). To circumvent this, we consider its closure R, which is compact and as such complete. We show that R differs from R only by a finite number of points. Let us start with preliminary results.

Proposition 3.4. Let a ∈ C(M) be an adherent point to R . Then a is connected and f is constant on a .

Proof. Let a ∈ C(M) such that there exists ( a n ) n ∈ N ∈ (R) N that converges to a .

• We first show that a is connected. Suppose that a is not connected. Since it is compact, this means that it can be written as the disjoint union of two compact sets a 0 ⊔ a 1 . Now, a 0 and a 1 being disjoint and both compact, there exists ε > 0 such that a ε 0 and a ε 1 are disjoint. To see this, consider for example ε := 1 2 inf x ∈ a 0 ,y ∈ a 1 d( x,y ) and notice that inf x ∈ a 0 ,y ∈ a 1 d( x,y ) > 0 by compactness of a 0 and a 1 . ε ε ε

Next, there exists a rank N ∈ N such that d H ( a N ,a ) ≤ ε 2 . This means that a N ⊆ a 2 = a 2 0 ⊔ a 2 1 , and since a N is connected, it is included in one of a ε 2 0 and a ε 2 1 and not in the other. W.l.o.g., we suppose that it is included in a ε 2 0 . Let x ∈ a 1 , there exists z ∈ a N such that d( x,z ) ≤ ε 2 because a ⊆ a ε 2 N . In the same way, there exists y ∈ a 1 that satisfies d( y,z ) ≤ ε 2 , since a N ⊆ a ε 2 0 . However,

[Page 14]

![The image is a diagram of a circle with a radius of 2. The center of the circle is marked as (0, 0). There are two concentric circles, one inside the other, with radii of 2 and 0.5. The points of tangency between the two circles are labeled as (0, 0) and (0.5, 0.5).](<2506.02810v1/imageFile4.png>)

Figure 4: Example of a Reeb graph sequence ( a n ) n ∈ N that has a Hausdorff limit a ∞ outside of the Reeb graph. Notice that this occurs when a ∞ is inside the level set of a critical value v c . In this example, f − 1 ( { v c } ) has only one connected component. The complement a c ∞ of a ∞ in this connected component is also represented here.

• We now show that f is constant on a . Let x,y ∈ a . By Proposition 3.1, we know that there exist two sequences ( x n ) n ∈ N and ( y n ) n ∈ N in ( a n ) n ∈ N such that x n −→ n →∞ x and y n −→ n →∞ y . Now, for every n , since x n ,y n ∈ a n , then f ( x n ) = f ( y n ) as f is constant on a n . Using this fact, we have:

$$
| f ( x ) - f ( y ) | & \leq | f ( x ) - f ( x _ { n } ) | + | f ( x _ { n } ) - f ( y _ { n } ) | + | f ( y _ { n } ) - f ( y ) | \\ & \leq | f ( x ) - f ( x _ { n } ) | + | f ( y _ { n } ) - f ( y ) | .
$$

By continuity of f , it yields that f ( x ) = f ( y ).

The second point of Proposition 3.4 proves that we can define a function ˜ f : R → R that associates to every adherent point of R the constant value that f takes on it. Proposition 3.4 as a whole might seem to indicate that R = R could also be proved, but this turns out to be false in general. The crucial property that the elements of R \ R do not satisfy is to be associated to maximal connected subsets. See Figure 4 for an example. However, we now prove that R and R differ only by a finite number of points.

Lemma 3.1. Let ( a n ) n ∈ N ∈ R N and ( x n ) n ∈ N ∈ M N , such that x n ∈ a n for every n ∈ N and such that ( x n ) n ∈ N admits a limit x ∈ M . If f ( x ) is not a critical value, then a n −→ n →∞ [ x ] ∼ f .

Proof. Let ( a n = [ x n ] ∼ f ) n ∈ N ∈ R N be such that ( x n ) n ∈ N admits a limit x ∈ M. As R is compact, it converges if and only if it has one and only one adherent point. It admits an adherent point by compactness of R and we have to show that it is unique and equal to [ x ] ∼ f . Let ( a ϕ ( n ) ) n ∈ N be a subsequence of a n that converges to an element a ∈ R.

• We first show that a ⊆ [ x ] ∼ f . By Proposition 3.1, we know that:

$$
a = \left \{ y \in M , \, \text { there exists a sequence } ( y _ { n } ) _ { n } \in \prod _ { n } a _ { \phi ( n ) } \text { such that } y _ { n } \underset { n \to \infty } { \longrightarrow } y \right \} .
$$

[Page 15]

Now, ( x ϕ ( n ) ) n ∈ N is a subsequence of ( x n ) n ∈ N and as such x ϕ ( n ) −→ n →∞ x . Therefore, x ∈ a , and furthermore by Proposition 3.4, a is connected and included in f − 1 ( { v } ), where v = f ( x ). These three points prove that a ⊆ [ x ] ∼ f , since by definition [ x ] ∼ f is the maximal subset that verifies these three exact requirements.

• We now show that [ x ] ∼ f ⊆ a . Let y ∈ [ x ] ∼ f . As v = f ( x ) is not a critical value, by Theorem 2.4, there exists ε > 0 and a homeomorphism ψ : f − 1 ( { v } ) × [ − ε,ε ] → f − 1 ([ v − ε,v + ε ]). Since f is continuous, there exists a rank N ∈ N such that ∀ n ≥ N : | f ( x ϕ ( n ) ) − v | ≤ ε . Therefore, the following sequence is well defined:

$$
\forall n \in \mathbb { N } \colon y _ { n } = \begin{cases} x _ { \phi ( n ) } & \text { if } n < N , \\ \psi \left ( y , f ( x _ { \phi ( n ) } ) - v \right ) & \text { otherwise.} \end{cases}
$$

By continuity of ψ and f , y n −→ n →∞ y (since ψ ( · , 0) is the identity by construction of ψ , see the proofs of Lemma 2.3 and Theorem 2.4). It only remains to show that for a large enough rank M ∈ N , one has y n ∈ a ϕ ( n ) , to conclude that y ∈ a .

On one hand, since v is not a critical value, f − 1 ( { v } ) is a submanifold of M by the Implicit Function Theorem (see Theorem 5.8. of [Boo86] for example). The set f − 1 ( { v } ) is hence locally path-connected. Thus, there exists δ > 0 such that for all z ∈ f − 1 ( { v } ), d( x,z ) ≤ δ implies that z ∈ [ x ] ∼ f .

On the other hand, by continuity, we have ψ − 1 ( x ϕ ( n ) ) −→ n →∞ ψ − 1 ( x ) = ( x, 0). From the expression of ψ − 1 given in the proof of Theorem 2.4, we find that ψ − 1 ( x ϕ ( n ) ) = ( z n ,w n ) where z n ∈ f − 1 ( { v } ) and w n = f ( x ϕ ( n ) ) − v . Thus, there exists a rank M ∈ N such that ∀ n ≥ M : d( z n ,x ) ≤ δ and then z n and y are in the same connected set [ x ] ∼ f . Moreover, by continuity of the map ψ ( · ,w n ) : f − 1 ( { v } ) → f − 1 ( { w n } ), the images x ϕ ( n ) = ψ ( z n ,w n ) and y n = ψ ( y,w n ) are carried to the same connected component of f − 1 ( { w n } ). Consequently y n ∈ [ x ϕ ( n ) ] ∼ f and we know that [ x ϕ ( n ) ] ∼ f = a ϕ ( n ) . See Figure 5 for an illustration.

Finally, ( a n ) n ∈ N admits one and only one adherent point, which is [ x ] ∼ f .

Proposition 3.5. R \ R is finite.

Proof. Recall that ˜ f : R → R is defined as the function that associates any adherent point of R to the constant value that f takes on it. Let ψ · be the gradient flow of f .

Let a ∈ R be non-empty. First, assume that v = ˜ f ( a ) is not a critical value of f . Let ( a n ) n ∈ N be a sequence of elements in R that converges to a . Let x ∈ a and choose a sequence ( x n ) n ∈ N such that x n ∈ a n for every n ∈ N and x n −→ n →∞ x . By Lemma 3.1, a n −→ n →∞ [ x ] ∼ f and as such a = [ x ] ∼ f and in particular a ∈ R. We thus have showed that for any a ∈ R \ R which is non empty, ˜ f ( a ) is a critical value.

We continue the proof of the proposition with the following lemma.

Lemma 3.2. Let a ∈ R such that v = ˜ f ( a ) is a critical value. Let v − (resp. v + ) denote the closest critical value v ′ to v satisfying v ′ < v (resp. v ′ > v ) if it exists. Let also ( a n ) n ∈ N be a sequence of elements in R that converges to a . Then, we can extract a subsequence ( a ϕ ( n ) ) n ∈ N from ( a n ) n ∈ N such that one of the three following assertions is satisfied:

- 1. ∀ n ∈ N , ˜ f ( a ϕ ( n ) ) = ˜ f ( a ) = v ;
- 2. there exists a connected component c of f − 1 ( { v − + v 2 } ) such that ∀ n ∈ N , a ϕ ( n ) = ψ v n − ( v − + v 2 ) ( c ) where v n ∈ ( v − + v 2 ,v ) , and ( v n ) n ∈ N converges to v ;
- 3. there exists a connected component c of f − 1 ( { v + v + 2 } ) such that ∀ n ∈ N , a ϕ ( n ) = ψ v n − ( v + v + 2 ) ( c ) where v n ∈ ( v, v + v + 2 ) , and ( v n ) n ∈ N converges to v .


[Page 16]

![The image consists of a diagram of a circle. The circle is labeled as F and has a radius labeled as r. The center of the circle is marked as C. There are two points labeled as A and B on the circumference of the circle. These points are connected to the center by two lines. The line segment AB is drawn from point A to point C, and the line segment BC is drawn from point B to point C.](<2506.02810v1/imageFile5.png>)

2k

ÿi

Yk

To(k)

Figure 5: Illustration of the argument made in the proof of Lemma 3.1. Two points x ϕ ( k ) and x ϕ ( l ) of the sequence ( x ϕ ( n ) ) n ∈ N are given as examples. The sequence ( z n ) n ∈ N is made of the projections of the elements of ( x ϕ ( n ) ) n ∈ N onto the level set where a belongs using the gradient flow. Similarly, the sequence ( y n ) n ∈ N is made of the projections of y onto the level sets f − 1 ( { x ϕ ( n ) } ). We see that, by continuity, ( z n ) n ∈ N must be in a (for a large enough rank). This means that after this rank, y n and x ϕ ( n ) must be in the same element of the Reeb graph.

Proof. If there exists a rank after which the sequence ( a n ) n ∈ N verifies ˜ f ( a n ) = v , then the first assertion is satisfied. In the following of the proof, we thus assume that ∀ n ∈ N , there exists N n ≥ n such that ˜ f ( a N n ) ̸ = v . This allows us to extract a subsequence ( a ϕ ( n ) ) n ∈ N that satisfies ˜ f ( a ϕ ( n ) ) ̸ = v for all n ∈ N . W.l.o.g., by re-extracting, we can assume that ∀ n ∈ N , ˜ f ( a ϕ ( n ) ) < v .

̸

̸

Notice that a ϕ ( n ) −→ n →∞ a . Let x ∈ a and ( x n ) n ∈ N ∈ ( a ϕ ( n ) ) n ∈ N such that x n −→ n →∞ x . By continuity of f , there exists N ∈ N , such that ∀ n ≥ N , f ( x n ) ∈ ( v − + v 2 ,v ). [ v − + v 2 ,f ( x n )] does not contain critical values. As such, we can construct a diffeomorphism ψ f ( x n ) − v − + v 2 between f − 1 ( { v − + v 2 } ) and f − 1 ( { f ( x n ) } ) using the gradient flow of f , see Lemma 2.3 for more details.

Consider now the connected components ( c i ) i of f − 1 ( { v − + v 2 } ). We will prove that:

$$
\exists i , \forall n \in \mathbb { N } , \exists k _ { n } \geq n \colon a _ { \phi ( k _ { n } ) } = \psi _ { f ( x _ { k _ { n } } ) } ( c _ { i } ) .
$$

The opposite of this means that for all connected components c i there is a rank n i such that ∀ k ≥ n i : a ϕ ( k ) is not diffeomorphic to c i . Since the number of ( c i ) i is finite (see Proposition 2.5), taking K = max i n i , we would have that ∀ k ≥ K , a ϕ ( k ) is not diffeomorphic to any of the c i under the flow ψ f ( x k ) − v − + v 2 . This contradicts that ∀ n ≥ N : f − 1 ( { f ( x n ) } ) and f − 1 ( { v − + v 2 } ) are diffeomorphic under the flow ψ f ( x k ) − v − + v 2 , and that this diffeomorphism induces a bijection between connected components. This can be understood as a application of the pigeonhole principle, see Figure 6 for an illustration.

This allows to extract a further subsequence ( a Φ( n ) ) n ∈ N that converges to a , and that consists only of diffeomorphic images of a fixed connected component c i , under well determined maps ( ψ v n − v − + v 2 ) n ∈ N such that v n −→ n →∞ v .

We therefore proved the lemma.

[Page 17]

![The image depicts a geometric figure involving a circle and a perpendicular line. The circle is depicted as a closed arc with a diameter that is labeled as diameter. The radius of the circle is marked as 2. The perpendicular line from the center of the circle to the diameter is labeled as 2. The angle at the center of the circle is labeled as 90 degrees. ### Description of the Figure: 1. **Circle**: The circle is a closed arc with a diameter. 2. **Perpendicular Line**: The perpendicular line from the center of the circle to the diameter is labeled as 2. 3. **Angle**: The angle at the center of the circle is labeled as 90 degrees. ### Analysis: - **Diameter**: The diameter of the circle is 2. - **Perpendicular Line**: The perpendicular line from the center of the circle to the diameter is 2. - **Angle**: The angle at](<2506.02810v1/imageFile6.png>)

8

92

Figure 6: Example of a convergent sequence ( a n ) n ∈ N of Reeb graph elements associated to filter values inside the open interval ( v − ,v ). Since the number of cylinders in f − 1 (( v − ,v )) is finite, ( a n ) n ∈ N must have an infinite amount of terms inside one of them.

We now consider a non-empty a ∈ R \ R and v = ˜ f ( a ) the corresponding critical value. By Lemma 2.2, the set of critical values is finite because M is compact. It remains to check that the preimage of a critical value is a finite set.

Let ( a n ) n ∈ N be a sequence of elements in R that converges to a . If the first case of Lemma 3.2 applies, then ( a n ) n ∈ N must be constant after a certain rank in order to converge because the number of connected components of f − 1 ( { v } ) is finite (see Proposition 2.5), which implies that a ∈ R leading to a contradiction because a ∈ R \ R.

W.l.o.g. we assume that the second point of Lemma 3.2 is satisfied for a . The following lemma allows to finish the proof of the proposition.

Lemma 3.3. Let a ∈ R such that v = ˜ f ( a ) is a critical value, and v − and v + defined as in Lemma 3.2. Let ( u n ) n ∈ N and ( v n ) n ∈ N be two sequences in [ v − + v 2 ,v ) such that u n −→ n →∞ v and v n −→ n →∞ v . Let c be a connected component of f − 1 ( { v − + v 2 } ) . Then we have:

n →∞

$$
d _ { H } ( \psi _ { u _ { n } - \frac { v ^ { - } + v } { 2 } } ( c ) , \psi _ { v _ { n } - \frac { v ^ { - } + v } { 2 } } ( c ) ) \underset { n \to \infty } { \longrightarrow } 0 .
$$

Proof. W.l.o.g., we assume that u n ≤ v n . Let ε > 0. Denote

$$
M _ { \varepsilon } = M \bigcup _ { c \in C r i t ( f ) } B \left ( c , \frac { \varepsilon } { 4 \left | C r i t ( f ) \right | } \right ) .
$$

Since there are no critical values in [ u n ,v n ], we can use the gradient flow ( ψ t ) t of f (see Lemma 2.3) to send any point p in ψ u n − v − + v 2 ( c ) to a point q in ψ v n − v − + v 2 ( c ) (and conversely) using the curve:

$$
\gamma \colon [ 0 , v _ { n } - u _ { n } ] \longrightarrow M \\ t \longmapsto \psi _ { t + u _ { n } - \frac { v - + v } { 2 } } ( p ) ,
$$

where γ (0) = p and γ ( v n − u n ) = q . Now, for every c ∈ Crit( f ), if γ enters B   c, ε 4 | Crit( f ) |   ,

consider

$$
t _ { 0 } ^ { c } = \inf \left \{ t \in [ 0 , v _ { n } - u _ { n } ] , \gamma ( t ) \in B \left ( c , \frac { \varepsilon } { 4 \left | C r i t ( f ) \right | } \right ) \right \} ,
$$

[Page 18]

and

$$
t _ { 1 } ^ { c } = \sup \left \{ t \in [ 0 , v _ { n } - u _ { n } ] , \gamma ( t ) \in B \left ( c , \frac { \varepsilon } { 4 \left | \text {Crit} ( f ) \right | } \right ) \right \} .
$$

Otherwise, take t c 0 = t c 1 = 0. Notice that

$$
d ( \gamma ( t _ { 0 } ^ { c } ) , \gamma ( t _ { 1 } ^ { c } ) ) \leq \frac { \varepsilon } { 2 \left | C r i t ( f ) \right | } .
$$

Denoting We showed that E is dense in R. Therefore, its closure in R verifies R ⊆ E . Hence, R = E .

$$
\mathcal { U } = [ 0 , v _ { n } - u _ { n } ] \ \bigcup _ { c \in C r i t ( f ) } [ t _ { 0 } ^ { c } , t _ { 1 } ^ { c } ] ,
$$

we have γ ( U ) ∈ M ε . We also have:

$$
\epsilon \, \gamma ( \mathcal { U } ) \in M _ { \varepsilon } . \, \text { we also have: } \\ & d ( p , q ) \leq \int _ { t \in \mathcal { U } } \left \| \frac { d \psi _ { t + u _ { n } - \frac { v - + v } { 2 } } ( p ) } { d t } \right \| \, d t + \sum _ { c \in \text {Crit} ( f ) } d ( \gamma ( t _ { 0 } ^ { c } ) , \gamma ( t _ { 1 } ^ { c } ) ) \\ & \leq \int _ { t \in \mathcal { U } } \frac { d t } { \| \nabla f ( \psi _ { t + u _ { n } - \frac { v - + v } { 2 } } ( p ) ) \| } + \sum _ { c \in \text {Crit} ( f ) } \frac { \varepsilon } { 2 \left | \text {Crit} ( f ) \right | } \\ & \leq \frac { | v _ { n } - u _ { n } | } { \inf _ { p \in M _ { \varepsilon } } \| \nabla f ( p ) \| } + \frac { \varepsilon } { 2 } . \\ n _ { p } - u _ { n } \, \longrightarrow \, 0 \, \text { and } \inf _ { p \in M _ { \varepsilon } } \| \nabla f ( p ) \| > C \text { for some } C > 0 , \, \text { for } n \, \text { large enough } w
$$

Since v n − u n −→ n →∞ 0 and inf p ∈ M ε ∥∇ f ( p ) ∥ > C for some C > 0, for n large enough we have d( p,q ) ≤ ε , and finally d H ( ψ u n − v − + v 2 ( c ) ,ψ v n − v − + v 2 ( c )) −→ n →∞ 0.

Let a ∈ R \ R which satisfies the second case in Lemma 3.2, we thus have a ϕ ( n ) = ψ v n − v − + v 2 ( c ) with c being a connected component of f − 1 ( { v − + v 2 } ) and v n −→ n →∞ v . According to Lemma 3.3, the limit of ( a ϕ ( n ) ) n ∈ N is the same as the limit of ( b n ) n ∈ N defined as:

$$
\forall n \in \mathbb { N } \colon b _ { n } = \begin{cases} c & \text {if } v - \frac { 1 } { n } \leq \frac { v ^ { - } + v } { 2 } , \\ \psi _ { v - \frac { 1 } { n } } ( c ) & \text {otherwise.} \end{cases}
$$

Finally, notice that ( b n ) n ∈ N depends only on c and we showed that it converges to a . The number of connected components of f − 1 ( { v − + v 2 } ) and f − 1 ( { v + + v 2 } ) being finite (see Proposition 2.5), the set   a ∈ R , ˜ f ( a ) = v   is therefore also finite.

We conclude that R \ R is finite.

Proposition 3.6. ( R , d H ) is separable.

Proof. On one hand, f is constant on the elements of R. As such, there is a map ˜ f | R , that assigns to each element of R the unique value that f takes on it. For each critical value v , ˜ f − 1 | R ( { v } ) is finite. This is because f − 1 ( { v } ) is compact and locally path-connected, and a locally path-connected compact space has finitely many connected components. Now, there are finitely many critical values of f by Lemma 2.2. Hence, denoting C the union over all critical values v of ˜ f − 1 | R ( { v } ), one has that C is finite.

On the other hand, M is separable, and therefore there exists a countable subset D of M such that M = D . We now show that the countable subset E = C ∪{ [ x ] ∼ f , x ∈ D } is dense in R. Let a ∈ R.

- • If ˜ f | R ( a ) is a critical value, then a ∈ C .
- • If ˜ f | R ( a ) is not a critical value, then let x ∈ a . Given that x ∈ M and D is dense in M, there exists ( x n ) n ∈ N in D such that x n −→ n →∞ x . Consider the sequence ([ x n ] ∼ f ) n ∈ N , by Lemma 3.1 we have [ x n ] ∼ f −→ n →∞ a .


[Page 19]

Consider the following map:

$$
\pi \colon M & \longrightarrow \overline { R } \\ x & \longmapsto [ x ] _ { \sim f } .
$$

The image of π is R and it consists of the composition of the natural map associated to the quotient of M by the equivalence relation ∼ f with the inclusion of R in its closure.

We introduce the notation F = { x ∈ M , f ( x ) is a critical value } for the set of points whose image under f is a critical value.

Proposition 3.7. For every measure m on (M , d) such that m( F ) = 0 , one has that π is continuous m -almost everywhere.

Proof. We will prove that the map π is continuous at every point x ∈ M such that f ( x ) is not a critical value. Let x be such a point and let ( x n ) n ∈ N be a sequence in M such that x n −→ n →∞ x . By

Lemma 3.1, one has

$$
\pi ( x _ { n } ) = [ x _ { n } ] _ { \sim _ { f \ n \to \infty } } \pi ( x ) = [ x ] _ { \sim _ { f } } .
$$

Lemma 3.4. If a measure m is absolutely continuous with respect to the volume measure, then m( F ) = 0 .

Proof. The set Crit( f ) is finite and hence closed. As such, M ′ = M \ Crit( f ) is an open submanifold of M. Now,

$$
\mathcal { F } = C r i t ( f ) \cup [ \mathcal { F } \cap M ^ { \prime } ] \, ,
$$

and by the Implicit Function Theorem (see Theorem 5.8. of [Boo86] for example), F ∩ M ′ is a finite union of ( d − 1) − dimensional submanifolds in M as the critical values of f in M are no longer critical in M ′ . Hence

$$
m \left ( \mathcal { F } \cap M ^ { \prime } \right ) = 0 .
$$

Finally, m( F ) = 0.

Corollary 3.4.1. π is Borel measurable.

Following Proposition 3.6 and Corollary 3.4.1, we can now consider the metric measure space ( R , d H , Vol ◦ π − 1 ). Notice that since the image of π is R, Vol ◦ π − 1 (R) = Vol(M).

# 4 Mapper graphs as Metric Measure Spaces

Let X n = { x 1 ,...,x n } ⊂ M be an n -sample taken inside M. In this section, we aim at turning the Mapper M n built from X n , and constructed using a function f : M → R and some cover of its image, as a metric measure space.

In this article, we restrict the focus to Mapper graphs , i.e., we only consider the 1-skeleton of the Mapper complexes. An element ∆ of M n is thus either a 0-dimensional simplex (vertex) or a 1-dimensional simplex (edge). See Section 2.1.

The Mapper graph M n can be embedded in C(M) ∪ { ∅ } in the following way:

[Page 20]

• A vertex ∆ 0 associated to a cluster C j,k is represented by

$$
\mathcal { C } _ { j , k } \wedge \left ( \bigcup _ { \substack { j ^ { \prime } \neq j \\ k ^ { \prime } \neq k } } \mathcal { C } _ { j ^ { \prime } , k ^ { \prime } } \right ) ,
$$

̸

̸

• An edge ∆ 1 associated to two vertices, and hence to the corresponding two clusters C j,k and C j ′ ,k ′ is represented by

$$
\mathcal { C } _ { j , k } \cap \mathcal { C } _ { j ^ { \prime } , k ^ { \prime } } \wedge \left ( \bigcup _ { j ^ { \prime \prime } \neq j , j ^ { \prime } } \mathcal { C } _ { j ^ { \prime \prime } , k ^ { \prime \prime } } \right ) .
$$

̸

̸

The elements of M n belong to the space of all subsets of X n , and are therefore compact subsets of X . It is possible for some simplex to have the empty set ∅ to be its representative, e.g., when a cluster is included in its intersection with another cluster. Moreover, we chose the representatives of the simplices in M n so as to have an element x i of X n be in one and only one ∆ ∈ M n . This allows to define a map

$$
\mathbb { D } \colon X _ { n } \longrightarrow \mathbb { M } _ { n } ,
$$

that associates x i to the unique simplex that contains it. Using the convention d H ( A, ∅ ) = Diam(M) for every A ⊆ M, it is clear that ( M n , d H ) is a metric space.

Any measure on X n , defined as a mixture of Dirac measures on M centered on each x i , can be pushed forward onto M n using ∆∆. For example, denoting the empirical measure by

$$
m _ { n } = \frac { 1 } { n } \sum _ { i = 1 } ^ { n } \delta _ { x _ { i } } ,
$$

for some points x i sampled according to a measure m on M, we have that ( M n , d H , m n ◦ ∆∆ − 1 ) is a metric measure space. Notice that the support of m n ◦ ∆∆ − 1 is M n \ ∆∆ − 1 ( { ∅ } ) .

# 5 A bound on the Gromov-Wasserstein distance between the Reeb graph and the Mapper graph

Let M be a compact connected Riemannian manifold of dimension d . Let f : M → R be a Morse function and m be a Borel probability measure on M.

We make two assumptions:

Assumption 1. The Ricci curvature Ric of M is lower bounded:

$$
R i c \geq ( d - 1 ) \cdot k
$$

where k ∈ R .

Assumption 2. The measure m is absolutely continuous with respect to the volume measure Vol and admits an upper bounded density, i.e.,

$$
\sup \frac { d m } { d V o l } < + \infty .
$$

Furthermore, we assume that m is fully supported on M .

Note that when M is a submanifold of an Euclidean space, Assumption 1 can be satisfied by assuming that the reach of M, denoted by τ M , is positive: τ M > 0 (see Proposition A.1. in [Aam+19]). Assumptions on the reach of submanifolds are popular in literature (see, e.g., [NSW08; BCL18]).

Let X n = { x 1 ,...,x n } be an n -sample taken in M with respect to m and let m n = 1 n   n i =1 δ x i be the associated empirical measure. In this section, we compare the Reeb graph R := R f (M) and the Mapper M n built on X n according to GW metrics.

[Page 21]

# 5.1 Main result

First, we specify the Mapper parameters that will be used in our results. We will assume a homogeneous covering of resolution r ∈ N , i.e., the intervals I = { I 1 ,..., I r } that cover the range of the filter f (X n ) in the Mapper are all of the same length and consecutive intervals have a percentage 0 < g < 1 / 2 of their length in common. More specifically, we take I j = [ a j ,b j ] where:

$$
a _ { j } & = \min f + ( j - 1 ) \cdot \frac { \max f - \min f } { r } - \frac { g } { 2 - 2 \cdot g } \cdot \frac { \max f - \min f } { r } , \\ b _ { j } & = \min f + j \cdot \frac { \max f - \min f } { r } + \frac { g } { 2 - 2 \cdot g } \cdot \frac { \max f - \min f } { r } .
$$

$$
r = 2 - 2 \cdot g = r
$$

Now, given a set of intervals I = { I 1 ,..., I r } used to cover the range of the filter f (X n ) in the Mapper, we define the refinement of I as:

$$
\mathbb { J } = \{ I _ { 1 } \ \lceil I _ { 2 } , I _ { 1 } \cap I _ { 2 } , I _ { 2 } \ \rceil \ ( I _ { 1 } \cup I _ { 3 } ) , I _ { 2 } \cap I _ { 3 } , \dots \} \, .
$$

The family J is a refinement of I in the sense that it corresponds to a collection of elementary blocks of I . Notice also that two elements of J intersect in at most one point.

We call maximal width W ( r ) of the cover I the maximal length of an element in J . Looking at the definition of I j = [ a j ,b j ] above, we see that

$$
\mathcal { W } ( r ) = \frac { \max f - \min f } { r } .
$$

Similarly, the minimal width of an element in J is given by g 1 − g · W ( r ) .

We now state the main theorem of this article. Its proof, as well as explicit constants, are provided in Proposition 5.1, Corollary 5.1.1 and Proposition 5.4.

Theorem 5.1. Under Assumptions 1 and 2, and using the definitions above, consider the two metric measure spaces ( R , d H , m ◦ π − 1 ) and ( M n , d H , m n ◦ ∆∆ − 1 ) corresponding to the Reeb graph and to the Mapper graph respectively. For any α > 0 , p ≥ 1 and a resolution r ( n ) of the Mapper satisfying r ( n ) ∼ n →∞ n 1 d + α , we then have, for any large enough sample size n :

$$
\mathbb { E } \left ( G W _ { p } ( \overline { R } , \mathbb { M } _ { n } ) \right ) \lesssim n ^ { - \frac { \nu } { d + \alpha } }
$$

where

$$
\nu = \min \left \{ \frac { 1 } { 2 } , \frac { d } { p ( d + 1 ) } \right \} .
$$

Remark 1. We give a discussion on the optimality of this bound. When d p ( d +1) ≤ 1 2 , the upper bound given in Theorem 5.1 is achieved by the bound on max J ∈ J Vol   f − 1 (J)   given in Lemma 5.5, see the proofs of Proposition 5.4 and Theorem 5.1 for details. Note moreover that this upper bound is sharp. To see this, consider for instance the case where M is the graph of the function x  → x 2 over the segment [ − 1 , 1] : M = { ( x,x 2 ) , x ∈ [ − 1 , 1] } , with the metric g being induced by the Euclidean metric in R 2 . We are interested in Vol   f − 1 ([0 , W ( r )])   . We have:

$$
\text {Euclidean metric in } \mathbb { R } ^ { 2 } . & \text { We are interested in } \text {Vol} \left ( f ^ { - 1 } ( [ 0 , \mathcal { W } ( r ) ] ) \right ) . \text { We have:} \\ & \text {Vol} \left ( f ^ { - 1 } ( [ 0 , \mathcal { W } ( r ) ] ) \right ) = \int _ { - \sqrt { \mathcal { W } ( r ) } } ^ { \sqrt { \mathcal { W } ( r ) } } \sqrt { 1 + 4 t ^ { 2 } } \, d t \\ & = \frac { 1 } { 4 } \left [ t \sqrt { 1 + t ^ { 2 } } + \log \left ( t + \sqrt { 1 + t ^ { 2 } } \right ) \right ] _ { - 2 \sqrt { \mathcal { W } ( r ) } } ^ { 2 \sqrt { \mathcal { W } ( r ) } } \\ & \underset { r \to \infty } { \sim } 2 \sqrt { \mathcal { W } ( r ) } = 2 \mathcal { W } ( r ) ^ { \frac { d } { a ^ { 1 } + 1 } } . \\ \intertext { t h a t in this e x a m p l e } \text { } M \ h a s \ a _ { \ } h o u n d a r y _ { \ } W e \ c a n \ c i r c u m v e n t \ h i s \ i f \ n e e d \ b u \ f o r \ e x a n d a r y _ { \ } W e
$$

Note that in this example M has a boundary. We can circumvent this if needed by, for example, attaching the two points in the boundary in a smooth manner. This will not change our argument since f − 1 ([0 , W ( r )]) is located away from the boundary.

[Page 22]

$$
G W _ { p } ( \overline { R } , \mathbb { M } _ { n } ) \leq E _ { n } + A _ { n } .
$$

On one hand, since d + α ν > max { 2 d, 2 p } , Corollary 5.1.1 shows that

$$
\mathbb { E } \left ( E _ { n } \right ) \lesssim n ^ { - \frac { \nu } { d + \alpha } } .
$$

On the other hand, Proposition 5.4 gives

$$
\mathbb { E } \left ( A _ { n } ^ { p } \right ) \leq \left ( \lambda + \xi \cdot \sqrt { \mathcal { W } ( r ) } \right ) ^ { p } + \tilde { \eta } \cdot \mathcal { W } ( r ) ^ { \frac { d } { d + 1 } } + \frac { \zeta } { \lambda ^ { d } } \cdot \exp \left ( \frac { - n a \lambda ^ { d } } { 2 ^ { d } } \right ) ,
$$

for every λ > 0 such that

g

· W

(

r

)

ω

(

λ

)

≤

.

f

1

-

g

4

where the constants ξ , ˜ η and ζ are given in Proposition 5.4. The third term, that is given by

$$
\frac { \zeta } { \lambda ^ { d } } \cdot \exp \left ( \frac { - n a \lambda ^ { d } } { 2 ^ { d } } \right ) ,
$$

converges to zero for λ ( n ) ≳ n − 1 d + α . Furthermore, since f is smooth on a compact manifold, it is L -Lipschitz for some L > 0. In Proposition 5.4, the assumption

$$
\omega _ { f } \left ( \lambda \right ) \leq \frac { g } { 1 - g } \cdot \frac { \mathcal { W } ( r ) } { 4 } ,
$$

where ω f is the modulus of continuity of f , can be satisfied by choosing

$$
\lambda = \frac { g } { 1 - g } \cdot \frac { \mathcal { W } ( r ) } { 4 L } .
$$

Therefore, in order to make the term above converge to zero (exponentially fast), a sufficient condition is 1

$$
\mathcal { W } ( r ) ( n ) _ { n \to \infty } n ^ { - \frac { 1 } { d + \alpha } } ,
$$

or equivalently

$$
r ( n ) \underset { n \to \infty } { \sim } n ^ { \frac { 1 } { d + \alpha } } .
$$

Moreover, the two other terms in Proposition 5.4 are dominated by W ( r ) pν . As such, E (A p n ) ≲ n − pν d + α , and therefore ν

$$
\mathbb { E } \left ( A _ { n } \right ) \lesssim n ^ { - \frac { \nu } { d + \alpha } }
$$

and finally

$$
\mathbb { E } \left ( G W _ { p } ( \overline { R } , \mathbb { M } _ { n } ) \right ) \lesssim n ^ { - \frac { \nu } { d + \alpha } } .
$$

# 5.2 Approximation-Estimation Decomposition

Proposition 5.1. Consider the two metric measure spaces ( R , d H , m ◦ π − 1 ) and ( M n , d H , m n ◦ ∆∆ − 1 ) corresponding to the Reeb graph and to the Mapper graph respectively. We have:

$$
G W _ { p } ( \overline { R } , \mathbb { M } _ { n } ) \leq W _ { p } ( m \circ \pi ^ { - 1 } , m _ { n } \circ \pi ^ { - 1 } ) + \left ( \frac { 1 } { n } \sum _ { i = 1 } ^ { n } d _ { H } ( [ x _ { i } ] _ { \sim f } , \mathbb { D } ( x _ { i } ) ) ^ { p } \right ) ^ { \frac { 1 } { p } } .
$$

[Page 23]

On one hand,

$$
G W _ { p } ( \overline { R } , \overline { R } _ { n } ) \leq W _ { p } ( m \circ \pi ^ { - 1 } , m _ { n } \circ \pi ^ { - 1 } ) .
$$

On the other hand,

$$
G W _ { p } ^ { p } ( \overline { R } _ { n } , \mathbb { M } _ { n } ) \leq \inf _ { \eta } \int _ { \overline { R } \times M _ { n } } d _ { H } ( a , \Delta ) ^ { p } \, d \eta ( a , \Delta ) ,
$$

where the infimum is taken over all coupling measures η on R × M n that have marginals m n ◦ π − 1 and m n ◦ ∆∆ − 1 . Indeed, the Hausdorff distance d H can be defined on R ⊔ M n via its inclusion in C(M), and is thus a metric coupling of d H restricted to R and d H restricted to M n . Furthermore, consider the map

$$
\phi \colon X _ { n } & \longrightarrow \overline { R } \times \mathbb { M } _ { n } \\ x _ { i } & \longmapsto ( [ x _ { i } ] _ { \sim f } , \mathbb { \Delta } ( x _ { i } ) )
$$

It is clear that m n ◦ ϕ − 1 is an example of such a coupling measure η . Hence,

$$
\mathbb { m } { 0 } \varphi & \quad \text {is an example of such a coupling measure} \, \eta . \text { Hence} , \\ \ G W _ { p } ^ { p } ( \overline { R } _ { n } , \mathbb { M } _ { n } ) & \leq \int _ { \overline { R } \times \mathbb { M } _ { n } } \, d _ { H } ( a , \Delta ) ^ { p } \, d m _ { n } \circ \phi ^ { - 1 } ( a , \Delta ) \\ & = \int _ { X _ { n } } \, d _ { H } ( [ x ] _ { \sim } , \Delta ( x ) ) ^ { p } \, d m _ { n } ( x ) \\ & = \frac { 1 } { n } \sum _ { i = 1 } ^ { n } d _ { H } ( [ x _ { i } ] _ { \sim } , \Delta ( x _ { i } ) ) ^ { p } .
$$

Finally,

$$
G W _ { p } ( \overline { R } , \mathbb { M } _ { n } ) & \leq G W _ { p } ( \overline { R } , \overline { R } _ { n } ) + G W _ { p } ( \overline { R } _ { n } , \mathbb { M } _ { n } ) \\ & \leq W _ { p } ( m \circ \pi ^ { - 1 } , m _ { n } \circ \pi ^ { - 1 } ) + \left ( \frac { 1 } { n } \sum _ { i = 1 } ^ { n } d _ { H } ( [ x _ { i } ] _ { \sim f } , \mathbb { D } ( x _ { i } ) ) ^ { p } \right ) ^ { \frac { 1 } { p } } .
$$

The bound given in Proposition 5.1 contains two terms:

$$
1 . \ E _ { n } = W _ { p } ( m \circ \pi ^ { - 1 } , m _ { n } \circ \pi ^ { - 1 } ) , \, \text {and}
$$

$$
& = W _ { p } ( m \circ \pi ^ { - 1 } , m _ { n } \circ \pi ^ { - 1 } ) , \text { and } \\ & = \left ( \frac { 1 } { n } \sum _ { i = 1 } ^ { n } d _ { H } ( [ x _ { i } ] _ { \sim _ { f } } , \mathbb { \Delta } ( x _ { i } ) ) ^ { p } \right ) ^ { \frac { 1 } { p } } .
$$

$$
2 . \ A _ { n } = \left ( \frac { 1 } { n } \sum _ { i = 1 } ^ { n } d _ { H } ( [ x _ { i } ] _ { \sim _ { f } } , \mathbb { D } ( x _ { i } ) ) ^ { p } \right ) ^ { \frac { 1 } { p } } .
$$

The term E n can be interpreted as an estimation error. It measures how representative the discrete measure associated to the n -sample X n is of the measure m, in terms of the distance in the Reeb graph. The term A n can be interpreted as an approximation error. It measures how well the Mapper graph captures the same stratification than the one in the Reeb graph. It is small if, for most of the sample points x i , the simplex containing x i is close (in Hausdorff distance) from the Reeb graph element that contains x i .

# 5.3 The estimation error E n

# 5.3.1 Covering numbers for Reeb graphs

Following the works of [BG14] and [WB19], in this section, we aim at bounding the estimation error E n using the ε -covering number of (R , d H ).

Definition 6. Given a metric space X , S ⊆ X and ε > 0 , the ε -covering number of S , N ε ( S ) , is the minimum integer m such that there exist balls B 1 ,...,B m in X of radius ε such that S ⊆   m i =1 B i .

[Page 24]

Note that we have N ε (R) = N ε ( ¯ R), since R ⊆ ¯ R and any ε -covering of R is also an ε -covering of ¯ R, as ¯ R is the minimal closed subset that contains R.

Proposition 5.2. There exists ε ′ > 0 such that for every ε ≤ ε ′ we have

$$
\mathcal { N } _ { \varepsilon } ( R ) \leq N _ { c } + \left ( \frac { \beta } { \varepsilon } \right ) ^ { 2 d } ,
$$

where N c is the number of elements in R associated to critical values, d is the dimension of M and β is a constant depending on f and M .

Proof. First, let ε > 0 and take N c balls B 1 ,..., B N c ⊆ R of radius ε , one around each element of R associated to a critical value.

Second, let { v 1 ,...,v k +1 } be the critical values of f , and let

$$
M _ { \varepsilon } \colon = M \bigcup _ { c \in C r i t ( f ) } B \left ( c , \frac { \varepsilon } { 4 \left | C r i t ( f ) \right | } \right ) .
$$

For 1 ≤ j ≤ k , let the sets { S j,l } j,l denote the connected components of M ∩ f − 1 (( v j ,v j +1 )). Notice that there are no critical points in any of the S j,l .

We start with the next technical result that connect balls of (M ,d ) with those of (R , d H ).

Assertion. For every ε > 0 , every ( j,l ) and every x ∈ S j,l :

$$
B \left ( x , \frac { \varepsilon \, \inf _ { p \in M _ { \varepsilon } } \| \nabla f ( p ) \| } { 2 \sup _ { p \in M } \| \nabla f ( p ) \| } \right ) \cap S _ { j , l } \subseteq \pi ^ { - 1 } ( B \left ( [ x ] _ { \sim f } , \varepsilon \right ) ) .
$$

Proof. Let y ∈ B   x, ε · inf p ∈ M ε ∥∇ f ( p ) ∥ 2 sup p ∈ M ∥∇ f ( p ) ∥   ∩ S j,l . W.l.o.g., we assume that f ( y ) > f ( x ).

From Lemma 2.3, we can introduce the gradient flow ( ψ t ) t of f , such that ψ f ( y ) − f ( x ) is a diffeomorphism between f − 1 ( { f ( x ) } ) and f − 1 ( { f ( y ) } ). Moreover, since x and y are both in S j,l , then ψ f ( y ) − f ( x ) is in particular a diffeomorphism between [ x ] ∼ f and [ y ] ∼ f . Thus, for any point p in [ x ] ∼ f , there exists a point q in [ y ] ∼ f (and conversely) such that the curve

$$
\gamma \colon [ 0 , f ( y ) - f ( x ) ] & \longrightarrow \mathbf M \\ u & \longmapsto \psi _ { u + f ( x ) } ( p )
$$

$$
)
$$

connect p = γ (0) to q := γ ( f ( y ) − f ( x )).

Now, for every c ∈ Crit( f ), if γ enters B   c, ε 4 | Crit( f ) |   consider

$$
u _ { 0 } ^ { c } = \inf \left \{ u \in [ 0 , f ( y ) - f ( x ) ] , \, \gamma ( u ) \in B \left ( c , \frac { \varepsilon } { 4 \left | \text {Circuit} ( f ) \right | } \right ) \right \} ,
$$

and

$$
u _ { 1 } ^ { c } = \sup \left \{ u \in [ 0 , f ( y ) - f ( x ) ] , \, \gamma ( u ) \in B \left ( c , \frac { \varepsilon } { 4 \left | \text {Circuit} ( f ) \right | } \right ) \right \} .
$$

Otherwise take u c 0 = u c 1 = 0. Notice that

$$
d ( \gamma ( u _ { 0 } ^ { c } ) , \gamma ( u _ { 1 } ^ { c } ) ) \leq \frac { \varepsilon } { 2 \left | C r i t ( f ) \right | } .
$$

Denoting

we have Furthermore:

$$
\mathcal { U } = [ 0 , f ( y ) - f ( x ) ] \bigcup _ { c \in C r i t ( f ) } [ u _ { 0 } ^ { c } , u _ { 1 } ^ { c } ] ,
$$

$$
\gamma \left ( \mathcal { U } \right ) \in M _ { \varepsilon }
$$

[Page 25]

$$
^ { \text {more:} } & & d ( p , q ) \leq \int _ { u \in \mathcal { U } } \left \| \frac { d \psi _ { u + f ( x ) } ( p ) } { d u } \right \| d u + \sum _ { c \in \text {Crit} ( f ) } d ( \gamma ( u _ { 0 } ^ { c } ) , \gamma ( u _ { 1 } ^ { c } ) ) \\ & \leq \int _ { u \in \mathcal { U } } \frac { d u } { \| \nabla f ( \psi _ { u + f ( x ) } ( p ) ) \| } + \sum _ { c \in \text {Crit} ( f ) } d ( \gamma ( u _ { 0 } ^ { c } ) , \gamma ( u _ { 1 } ^ { c } ) ) \\ & \leq \frac { f ( y ) - f ( x ) } { \inf _ { p \in M _ { \varepsilon } } \| \nabla f ( p ) \| } + \sum _ { c \in \text {Crit} ( f ) } \frac { \varepsilon } { 2 \left | \text {Crit} ( f ) \right | } \\ & \leq \frac { d ( x , y ) \cdot \sup _ { p \in M } \| \nabla f ( p ) \| } { \inf _ { p \in M _ { \varepsilon } } \| \nabla f ( p ) \| } + \frac { \varepsilon } { 2 } \\ & \leq \varepsilon \\ d _ { \text {H} } ( [ x ] _ { \sim f } , [ y ] _ { \sim f } ) & \leq \varepsilon .
$$

As such, d H ([ x ] ∼ f , [ y ] ∼ f ) ≤ ε .

Using the previous assertion, we will bound the ε -covering number of R.

Let e = ε · inf p ∈ M ε ∥∇ f ( p ) ∥ 2 sup p ∈ M ∥∇ f ( p ) ∥ and consider minimal e -coverings of each S j,l . Now, for every x such that f ( x ) is not a critical value, x is in one of the S j,l . As such, it is in one of the balls of radius e that cover S j,l , centered, say, on x i . Accordingly, by the previous assertion [ x ] ∼ f ∈ B   [ x i ] ∼ f ,ε   .

We proved that

$$
\mathcal { N } _ { \varepsilon } \left ( R \right ) \leq N _ { c } + \sum _ { j , l } \mathcal { N } _ { e } \left ( S _ { j , l } \right ) .
$$

Fix one of the S j,l . We now bound N e ( S j,l ). If { B( x i ,e ) } i is a minimal covering, the balls { B( x i ,e/ 2) } i are disjoint. Hence,

Also,

$$
\text {Vol} \left ( \bigcup B ( x _ { i } , e / 2 ) \right ) = \sum \text {Vol} \left ( B ( x _ { i } , e / 2 ) \right ) .
$$

$$
\text {Vol} \left ( \bigcup _ { i } B ( x _ { i } , e / 2 ) \right ) = \sum _ { i } \text {Vol} \left ( B ( x _ { i } , e / 2 ) \right ) . \\ \bigcup _ { i } B ( x _ { i } , e / 2 ) \subseteq M ,
$$

and as such

$$
\sum _ { i } \frac { V o l \left ( B ( x _ { i } , e / 2 ) \right ) } { V o l \left ( M \right ) } \leq 1 .
$$

We now use Theorem 2.1 since we assumed that the Ricci curvature of M verifies Ric ≥ ( d − 1) · k . We have:

and as such

$$
\frac { \text {Vol} \left ( B ( x _ { i } , e / 2 ) \right ) } { v ( d , k , e / 2 ) } \geq \frac { \text {Vol} \left ( M \right ) } { v ( d , k , D i a m ( M ) ) } ,
$$

Hence, Looking at Proposition 2.3, we know that

$$
\sum _ { i } \frac { v ( d , k , e / 2 ) } { v ( d , k , D i a m ( M ) ) } \leq \sum _ { i } \frac { V o l \left ( B ( x _ { i } , e / 2 ) \right ) } { V o l \left ( M \right ) } .
$$

$$
\mathcal { N } _ { e } \left ( S _ { j , l } \right ) \leq \frac { v ( d , k , D i a m ( M ) ) } { v ( d , k , e / 2 ) } .
$$

[Page 26]

$$
v ( d , k , e / 2 ) \underset { e \to 0 } { \sim } \alpha _ { d } \cdot \frac { e ^ { d } } { 2 ^ { d } } ,
$$

where α d is the volume of the unit ball in R d . For every small enough e , and hence small enough ϵ , we have d

$$
v ( d , k , e / 2 ) \geq \frac { \alpha _ { d } } { 2 } \cdot \frac { e ^ { d } } { 2 ^ { d } } .
$$

We conclude that

Accordingly

$$
\mathcal { N } _ { e } \left ( S _ { j , l } \right ) \leq \frac { 2 ^ { d + 1 } \cdot v ( d , k , D i a m ( M ) ) } { \alpha _ { d } \cdot e ^ { d } } .
$$

$$
\mathcal { N } _ { \epsilon } ( S ) \leq N _ { c } + \left ( \frac { \tilde { \beta } } { \varepsilon \cdot \Gamma ( \varepsilon ) } \right ) ^ { d } ,
$$

where:

$$
\tilde { \beta } & = \left ( \frac { 2 \left | \{ S _ { j , l } \} _ { j , l } \right | \cdot v ( d , k , D i a m ( M ) ) } { \alpha _ { d } } \right ) ^ { \frac { 1 } { d } } \cdot 4 \sup _ { p \in M } \| \nabla f ( p ) \| , \\ \Gamma ( \varepsilon ) & = \inf _ { p \in M _ { \varepsilon } } \| \nabla f ( p ) \| .
$$

It only remains now to investigate the behavior of Γ( ε ) when ε → 0.

For ε small enough, inf p ∈ M ε ∥∇ f ( p ) ∥ is reached in a neighborhood of a critical point c , at a point p such that ε

$$
d ( p , c ) \geq \frac { \varepsilon } { 4 \left | C r i t ( f ) \right | } .
$$

Now, writing f inside a Morse coordinate neighborhood U ⊆ M centered at c (see Lemma 2.2) gives:

$$
\sqrt { \sum _ { i = 1 } ^ { d } \frac { \partial f ( p ) ^ { 2 } } { \partial x _ { i } } } = 2 \sqrt { \sum _ { i = 1 } ^ { d } x _ { i } ^ { 2 } } ,
$$

where x ( p ) = ( x 1 ,...,x d ) is the representation of the point p ∈ U in local coordinates. Proposition 2.4 gives:

$$
\| \nabla f ( p ) \| \geq \frac { 1 } { \mu ( p ) } \cdot \sqrt { \sum _ { i = 1 } ^ { d } \frac { \partial f ( p ) ^ { 2 } } { \partial x _ { i } } } ,
$$

with µ ( p ) = max { √ ρ, ρ ∈ Sp(G) } where Sp(G) is the spectrum of the metric tensor G( p ) = [g( ∂ i ,∂ j )] i,j . The function µ is continuous and µ ( p ) → µ ( c ) < ∞ as p → c . Taking µ 0 = sup µ ( p ) in a small neighborhood of c , we have µ 0 < ∞ and

$$
\| \nabla f ( p ) \| \geq \frac { 1 } { \mu _ { 0 } } \cdot \sqrt { \sum _ { i = 1 } ^ { d } \frac { \partial f ( p ) ^ { 2 } } { \partial x _ { i } } } ,
$$

Moreover, Proposition 2.2 shows that:

$$
d _ { 0 } ( p , c ) = \sqrt { \sum _ { i = 1 } ^ { d } x _ { i } ^ { 2 } } \geq \frac { 1 } { \mu _ { 0 } } \cdot d ( p , c ) ,
$$

for small enough ε . Hence,

$$
\| \nabla f ( p ) \| \geq \frac { 2 } { \mu _ { 0 } ^ { 2 } } \cdot \frac { \varepsilon } { 4 \left | C r i t ( f ) \right | } .
$$

[Page 27]

$$
\mathcal { N } _ { \varepsilon } ( R ) \leq N _ { c } + \left ( \frac { \beta } { \varepsilon } \right ) ^ { 2 d } ,
$$

$$
\beta = \left ( 2 \tilde { \beta } \cdot \mu _ { 0 } ^ { 2 } \cdot | C r i t ( f ) | \right ) ^ { \frac { 1 } { 2 } }
$$

# 5.3.2 Convergence rate for the estimation error

We first restate Proposition 5. from [WB19].

Proposition 5.3. Let ( X,d ) be a Polish metric space with Diam( X ) ≤ 1 , m be a probability measure on X and m n be an empirical measure associated to a random sample X n taken from m . Let p ∈ [1 , ∞ ) . Suppose there exists ε ′ > 0 and s > 2 p such that for all ε ≤ ε ′

where

Then,

where

and

$$
- \frac { \log \mathcal { N } _ { \varepsilon , s } } { \log \varepsilon } \leq s ,
$$

$$
\mathcal { N } _ { \varepsilon , s } = \inf \left \{ \mathcal { N } _ { \varepsilon } ( B ) \, , \, m ( B ) \geq 1 - \varepsilon ^ { \frac { s p } { s - 2 p } } \right \} .
$$

$$
W _ { p } ^ { p } ( m , m _ { n } ) \leq C _ { 1 } ^ { \prime } n ^ { - \frac { p } { s } } + C _ { 2 } ^ { \prime } n ^ { - \frac { 1 } { 2 } } ,
$$

$$
C _ { 1 } ^ { \prime } = 3 ^ { \frac { 3 s p } { s - 2 p } + 1 } \left ( \frac { 1 } { 3 ^ { \frac { s } { 2 } - p } - 1 } + 3 \right ) ,
$$

$$
C _ { 2 } ^ { \prime } = ( 2 7 / \varepsilon ^ { \prime } ) ^ { \frac { \frac { \varepsilon } { 2 } } { 2 } } \, .
$$

We now apply Proposition 5.3 to bound the estimation error E n , using our asymptotic control over the covering number of R in Proposition 5.2.

Corollary 5.1.1. For every s > max(2 d, 2 p ) , we have

$$
\mathbb { E } \left ( E _ { n } ^ { p } \right ) \leq C _ { 1 } n ^ { - \frac { p } { s } } + C _ { 2 } n ^ { - \frac { 1 } { 2 } } ,
$$

where

and ε ′ > 0 .

$$
C _ { 1 } = D i a m \left ( M \right ) ^ { p } \cdot 3 ^ { \frac { 3 s _ { p } } { 8 - 2 p } + 1 } \left ( \frac { 1 } { 3 ^ { \frac { s } { 2 } } - p } + 3 \right ) ,
$$

$$
C _ { 2 } = D i a m \left ( M \right ) ^ { p } \cdot \left ( 2 7 / \varepsilon ^ { \prime } \right ) ^ { \frac { s } { 2 } } ,
$$

Proof. We have E n = W p (m ◦ π − 1 , m n ◦ π − 1 ). Moreover, m n ◦ π − 1 is an empirical measure associated to m ◦ π − 1 , since for every Borel subset A of R:

$$
m _ { n } \circ \pi ^ { - 1 } ( A ) & = \frac { 1 } { n } \sum _ { i = 1 } ^ { n } 1 _ { x _ { i } \in \pi ^ { - 1 } ( A ) } \\ & = \frac { 1 } { n } \sum _ { i = 1 } ^ { n } 1 _ { [ x _ { i } ] _ { \sim f } \in A } ,
$$

and because where We see that:

$$
[ x _ { i } ] _ { \sim f } \stackrel { i i d } { \sim } m \circ \pi ^ { - 1 } ,
$$

[Page 28]

$$
\mathbb { P } ( [ x _ { i } ] _ { \sim _ { f } } \in A ) & = \mathbb { P } ( x _ { i } \in \pi ^ { - 1 } ( A ) ) \\ & = m \circ \pi ^ { - 1 } ( A ) ,
$$

where we used that m n is an empirical measure associated to m.

In [WB19], the metric spaces X are assumed to verify Diam( X ) ≤ 1. This can be achieved in our case by considering a normalized version of d H in ¯ R, defined as d H / Diam(M). Notice that covering numbers N ′ ε ( · ) of this normalized space satisfy

$$
\mathcal { N } _ { \varepsilon } ^ { \prime } ( \cdot ) = \mathcal { N } _ { ( D i a m ( M ) \cdot \varepsilon ) } ( \cdot ) .
$$

Moreover, the Wasserstein distance W ′ p in the normalized space satisfies:

$$
W _ { p } ^ { \prime } ( \cdot , \cdot ) = \frac { 1 } { D i a m ( M ) } W _ { p } ( \cdot , \cdot ) .
$$

Now, we can use Proposition 5. in [WB19] to prove our result. The only assumption made in this proposition is that, for any small enough ε and s > 2 p :

$$
- \frac { \log \mathcal { N } _ { \varepsilon , s } ^ { \prime } } { \log \varepsilon } \leq s ,
$$

$$
\mathcal { N } _ { \varepsilon , s } ^ { \prime } = \inf \left \{ \mathcal { N } _ { \varepsilon } ^ { \prime } ( B ) \, , \, m \circ \pi ^ { - 1 } ( B ) \geq 1 - \varepsilon ^ { \frac { s p } { s - 2 p } } \right \} .
$$

$$
\mathcal { N } _ { \varepsilon , s } ^ { \prime } \leq \mathcal { N } _ { \varepsilon } ^ { \prime } ( R ) = \mathcal { N } _ { ( D i a m ( M ) \cdot \varepsilon ) } ( R ) .
$$

As such, using Proposition 5.2, for any small enough ε , we have

$$
- \frac { \log \mathcal { N } _ { \varepsilon , \varepsilon } ^ { \prime } } { \log \varepsilon } \leq - \frac { \log \left ( N _ { c } + \left ( \frac { \beta } { \text {Diam} ( M ) \cdot \varepsilon } \right ) ^ { 2 d } \right ) } { \log \varepsilon } \underset { \varepsilon \to 0 } { \longrightarrow } 2 d .
$$

Hence, for every s > max(2 d, 2 p ) there exists ε ′ > 0 such that for every ε ≤ ε ′ :

$$
- \frac { \log \mathcal { N } _ { \varepsilon , s } ^ { \prime } } { \log \varepsilon } \leq s .
$$

Applying Proposition 5.3 gives

$$
W _ { p } ^ { ^ { \prime } p } ( m \circ \pi ^ { - 1 } , m _ { n } \circ \pi ^ { - 1 } ) \leq C _ { 1 } ^ { \prime } n ^ { - \frac { p } { s } } + C _ { 2 } ^ { \prime } n ^ { - \frac { 1 } { 2 } } ,
$$

where

$$
C _ { 1 } ^ { \prime } = 3 ^ { \frac { 3 s p } { s - 2 p } + 1 } \left ( \frac { 1 } { 3 ^ { \frac { s } { 2 } - p } - 1 } + 3 \right ) ,
$$

and

$$
C _ { 2 } ^ { \prime } = ( 2 7 / \varepsilon ^ { \prime } ) ^ { \frac { \frac { s } { 2 } } { 2 } } \, .
$$

As such,

$$
W _ { p } ^ { p } ( m \circ \pi ^ { - 1 } , m _ { n } \circ \pi ^ { - 1 } ) \leq D i am \left ( M \right ) ^ { p } \cdot C _ { 1 } ^ { \prime } n ^ { - \frac { p } { s } } + D i am \left ( M \right ) ^ { p } \cdot C _ { 2 } ^ { \prime } n ^ { - \frac { 1 } { 2 } } ,
$$

which concludes the proof.

Note that the constants C 1 and C 2 in Corollary 5.1.1 are, up to a constant, the same as in Proposition 5 of [WB19], and ε ′ in the expression of C 2 is the same as in the proof of Corollary 5.1.1.

[Page 29]

# 5.4 The approximation error A n

# 5.4.1 Comparing Reeb graph and Mapper graph elements

We focus here on the second term in the upper bound of Proposition 5.1:

$$
A _ { n } = \left ( \frac { 1 } { n } \sum _ { i = 1 } ^ { n } d _ { H } ( [ x _ { i } ] _ { \sim _ { f } } , \mathbb { \Delta } ( x _ { i } ) ) ^ { p } \right ) ^ { \frac { 1 } { p } } .
$$

Recall that given a cover I = { I 1 ,..., I r } of the range of the filter f (X n ), the refinement of I is defined as:

$$
\mathbb { J } = \{ I _ { 1 } \ \lceil I _ { 2 } , I _ { 1 } \cap I _ { 2 } , I _ { 2 } \ \rceil \ ( I _ { 1 } \cup I _ { 3 } ) , I _ { 2 } \cap I _ { 3 } , \dots \} \, .
$$

For every critical point c ∈ Crit( f ), we know that there exists a Morse chart φ : U ⊆ M → R d around c by Lemma 2.2. Recall that the Riemannian metric g can be represented in this chart as a symmetric positive-definite matrix G( c ).

Lemma 5.2. Let x i ∈ X n and J ∈ J such that J does not contain critical values and is not adjacent to another refined cover element J ′ that contains critical values. For any large enough resolution r , if x i ∈ J , then for every x j ∈ ∆∆( x i ) :

$$
d ( x _ { j } , [ x _ { i } ] _ { \sim _ { f } } ) \leq \max _ { c \in C r i t ( f ) } \mu ( c ) \cdot \sqrt { \frac { 1 - g } { g } } \cdot \sqrt { \mathcal { W } ( r ) }
$$

where µ ( c ) is the square root of the spectral radius of G( c ) , see Section 2.2.

Proof. First, notice that if J does not correspond to an intersection of two intervals, i.e., it is of the form J = I j \ (I j − 1 ∪ I j +1 ), then ∆∆( x i ) is a subset of the connected component of x i in f − 1 (J).

Second, when J is an intersection, of the form J = I j ∩ I j +1 , our assumption that J does not contain critical values and is not adjacent to another refined cover element J ′ that contains critical values implies that f − 1 (I j ∪ I j +1 ) is homeomorphic to a finite collection of cylinders whose heights are given by the function values (see Theorem 2.4). As such, the connected components of f − 1 (J) are exactly the intersections of the connected components of f − 1 (I j ) and f − 1 (I j +1 ). Therefore, ∆∆( x i ) is also a subset of the connected component of x i in f − 1 (J) in this case.

Let x j ∈ ∆∆( x i ). Then x j is in the same connected component of f − 1 (J) than x i , as demonstrated above. Since J does not contain critical values, we can therefore send x j to a point ψ f ( x i ) − f ( x j ) ( x j ) in [ x i ] ∼ f using the gradient flow of f . See the proof of Theorem 2.4 for more details. We can therefore write:

$$
d ( x _ { j } , \psi _ { f ( x _ { i } ) - f ( x _ { j } ) } ( x _ { j } ) ) & \leq \left | \int _ { 0 } ^ { f ( x _ { i } ) - f ( x _ { j } ) } \left \| \frac { d \psi _ { u } ( p ) } { d u } \right \| \, d u \right | \\ & \leq \left | \int _ { 0 } ^ { f ( x _ { i } ) - f ( x _ { j } ) } \frac { 1 } { \| \nabla f ( \psi _ { u } ( p ) ) \| } \, d u \right | \\ & \leq \frac { \| f ( x _ { i } ) - f ( x _ { j } ) \| } { \inf _ { p \in f ^ { - 1 } ( J ) } \| \nabla f ( p ) \| } \\ & \leq \frac { \mathcal { W } ( r ) } { \inf _ { p \in f ^ { - 1 } ( J ) } \| \nabla f ( p ) \| } .
$$

Now, if f − 1 (J) is sufficiently close to a critical point c , in the sense that it intersects a Morse coordinate neighborhood U ⊆ M around c in a non-empty region, we can use Lemma 2.2 to write:

$$
f ( p ) = f ( c ) - \sum _ { j = 1 } ^ { i } x _ { j } ^ { 2 } + \sum _ { j = i + 1 } ^ { d } x _ { j } ^ { 2 } ,
$$

[Page 30]

where x ( p ) = ( x 1 ,...,x d ) is the representation of p ∈ U in local coordinates. Proposition 2.4 therefore gives:

$$
\| \nabla f ( p ) \| \geq \frac { 2 \| x ( p ) \| _ { 0 } } { \mu ( p ) } ,
$$

where ∥ · ∥ 0 is the Euclidean norm in local coordinates. However, these local coordinates also induce: 2

$$
| f ( p ) - f ( c ) | \leq \| x ( p ) \| _ { 0 } ^ { 2 } ,
$$

and therefore

$$
\| \nabla f ( p ) \| \geq \frac { 2 \sqrt { | f ( p ) - f ( c ) | } } { \mu ( p ) } .
$$

Since J does not contain critical values and is not adjacent to another refined cover element J ′ that contains critical values, this means that in U ∩ f − 1 (J), we have

$$
| f ( p ) - f ( c ) | \geq \frac { g } { 1 - g } \cdot \mathcal { W } ( r ) ,
$$

the latter being the minimal width of a refined cover element. As such:

$$
\frac { 1 } { \inf _ { p \in U \cap f ^ { - 1 } ( J ) } \| \nabla f ( p ) \| } \leq \frac { \sup _ { p \in U } \mu ( p ) } { 2 } \cdot \sqrt { \frac { 1 - g } { g } } \cdot \frac { 1 } { \sqrt { \mathcal { W } ( r ) } } .
$$

We see that sup p ∈ U µ ( p ) → µ ( c ) as p → c . Hence, upon shrinking U further if necessary, we can guarantee that

$$
\sup _ { p \in U } \mu ( p ) \leq 2 \mu ( c ) .
$$

Notice that the Morse charts around critical points (that we used so far in our proof) are fixed beforehand and do not depend on the resolution r . Moreover, away from critical points, ∥∇ f ( p ) ∥ is lower bounded by a constant C > 0 depending only on f and M.

Therefore,

$$
d ( x _ { j } , \psi _ { f ( x _ { i } ) - f ( x _ { j } ) } ( x _ { j } ) ) \leq \max \left ( \frac { 1 } { C } , \max _ { c \in \text {Crit} ( f ) } \mu ( c ) \cdot \sqrt { \frac { 1 - g } { g } } \cdot \frac { 1 } { \sqrt { \mathcal { W } ( r ) } } \right ) \cdot \mathcal { W } ( r ) .
$$

Finally, since W ( r ) −→ r →∞ 0, we proved our result.

We now define the modulus of continuity associated to f .

Definition 7. Let g : M → R be a uniformly continuous function. We call modulus of continuity of g the function:

$$
\omega _ { g } \colon \mathbb { R } ^ { + } & \longrightarrow \mathbb { R } ^ { + } \\ \lambda & \longmapsto \sup _ { d ( x , y ) \leq \lambda } | g ( x ) - g ( y ) | .
$$

In our case f is a Morse function and hence continuously differentiable. This means that the modulus of continuity ω f of f is well defined.

Lemma 5.3. Let λ > 0 such that d H (X n , M) ≤ λ and

$$
\omega _ { f } \left ( \lambda \right ) \leq \frac { g } { 1 - g } \frac { \mathcal { W } ( r ) } { 4 } .
$$

Let x i ∈ X n and J ∈ J such that J does not contain critical values and is not adjacent to another refined cover element J ′ that contains critical values. For any large enough resolution r , if x i ∈ J then for every y ∈ [ x i ] ∼ f :

$$
d ( y , \Delta ( x _ { i } ) ) \leq \lambda + \frac { 1 } { 2 } \cdot \max _ { c \in \text {Crit} ( f ) } \mu ( c ) \cdot \sqrt { \frac { 1 - g } { g } } \cdot \sqrt { \mathcal { W } ( r ) } .
$$

[Page 31]

Proof. Let J := [ v,w ] and let S be the connected component of x i inside f − 1 (J). We assumed that J does not contain critical values. As such, f ( S ) = J, otherwise f would reach either a local maximum or a local minimum in S . We will denote ˜ S λ = S \ ( f − 1 ( { v } ) ∪ f − 1 ( { w } )) λ , i.e., ˜ S λ is comprised of the points of S that are at a distance at least λ from f − 1 ( { v } ) or f − 1 ( { w } ).

The assumption ω f ( λ ) ≤ g 1 − g · W ( r ) 4 guarantees that ˜ S λ is not empty, as the set

$$
S \cap f ^ { - 1 } ( \left ( v + \frac { g } { 1 - g } \cdot \frac { \mathcal { W } ( r ) } { 4 } , w - \frac { g } { 1 - g } \cdot \frac { \mathcal { W } ( r ) } { 4 } \right ) )
$$

is included in ˜ S λ , and is non-empty because f ( S ) = J, like mentioned above.

We can also immediately see that d H (X n ∩ S, ˜ S λ ) ≤ λ , since we know that d H (X n , M) ≤ λ . Therefore, for every p ∈ ˜ S λ , there exists x j in X n ∩ S and hence in ∆∆( x i ), such that d( p,x j ) ≤ λ .

Now, let q be a point that is at a distance less than λ from either f − 1 ( { v } ) or f − 1 ( { w } ). Since we assumed that J does not contain critical values, we can send q to a point p = ψ v + w 2 − f ( q ) ( q ) in S ∩ f − 1 (   v + w 2   ) using the gradient flow of f (see the proof of Theorem 2.4). Notice that p ∈ ˜ S λ because f ( p ) = v + w 2 . We also have:

$$
d ( p , q ) & \leq \left | \int _ { 0 } ^ { f ( p ) - f ( q ) } \left \| \frac { d \psi _ { u } ( q ) } { d u } \right \| \, d u \\ & \leq \frac { \mathcal { W } ( r ) } { 2 \inf _ { p \in f ^ { - 1 } ( J ) } \| \nabla f ( p ) \| }
$$

since

$$
| f ( p ) - f ( q ) | \leq \frac { \mathcal { W } ( r ) } { 2 } .
$$

We further assumed that J does not contain critical values and is not adjacent to another refined cover element J ′ that contains critical values. The same argument made in the proof of Lemma 5.3 shows that

$$
\frac { 1 } { \inf _ { p \in f ^ { - 1 } ( J ) } \| \nabla f ( p ) \| } \leq \max \left ( \frac { 1 } { C } , \max _ { c \in C r i t ( f ) } \mu ( c ) \cdot \sqrt { \frac { 1 - g } { g } } \cdot \frac { 1 } { \sqrt { \mathcal { W } ( r ) } } \right )
$$

where C is a constant depending only on f and M. Taking a large enough resolution r , we have that

$$
d ( p , q ) \leq \frac { 1 } { 2 } \cdot \max _ { c \in C r i t ( f ) } \mu ( c ) \cdot \sqrt { \frac { 1 - g } { g } } \cdot \sqrt { \mathcal { W } ( r ) } .
$$

Since p ∈ ˜ S λ and d H (X n ∩ S, ˜ S λ ) ≤ λ , there exists x j ∈ X n ∩ S such that

$$
d ( x _ { j } , q ) & \leq d ( x _ { j } , p ) + d ( p , q ) \\ & \leq \lambda + \frac { 1 } { 2 } \cdot \max _ { c \in C r i t ( f ) } \mu ( c ) \cdot \sqrt { \frac { 1 - g } { g } } \cdot \sqrt { \mathcal { W } ( r ) } .
$$

Now, x j ∈ ∆∆( x i ) because x j ∈ S which proves our result.

# 5.4.2 Hausdorff convergence rate for the sample

We now focus on the convergence rate in Hausdorff distance between X n and M. Following the works of [Cha+14] and [CMO18], we recall that the measure m, with respect to which the points are sampled, is ( a,b )-standard.

Definition 8. Let ν be a Borel probability measure on M . Let a > 0 and b > 0 . We say that ν is ( a,b ) -standard if for every x ∈ M and r > 0 :

$$
\nu \left ( B \left ( x , r \right ) \right ) \geq \min \left ( 1 , a r ^ { b } \right ) .
$$

[Page 32]

The ( a,b )-standard assumption is fairly popular in the set estimation literature (see for example [Cue09]) and is verified for example when m is absolutely continuous with respect to the volume measure and b = d , the dimension of M.

We recall Theorem 2 of [Cha+14] that links the ( a,b )-standard assumption to the Hausdorff convergence rate between a sample and the support of m.

Theorem 5.4. Let ν be a Borel probability measure on M . Let X n be a sample of n points taken from ν . Denote X ν the support of ν . If ν is ( a,b ) -standard then for every ε > 0 :

$$
\mathbb { P } \left ( d _ { H } ( X _ { n } , X _ { \nu } ) > 2 \varepsilon \right ) \leq \min \left ( \frac { 2 ^ { b } } { a \varepsilon ^ { b } } \exp ( - n a \varepsilon ^ { b } ) , 1 \right ) .
$$

# 5.4.3 Convergence rate for the volume of a cover element preimage

We are interested here in an upper bound on the convergence rate of the volume of preimages of refined cover elements: 1

$$
\max _ { J \in \mathbb { J } } V o l \left ( f ^ { - 1 } ( J ) \right ) .
$$

Lemma 5.5. For any large enough resolution r , we have:

$$
\max _ { J \in \mathbb { J } } V o l \left ( f ^ { - 1 } ( J ) \right ) \leq \eta \cdot \mathcal { W } ( r ) ^ { \frac { d } { d + 1 } } ,
$$

where

$$
\eta = \max _ { c \in C r i t ( f ) } \mu ( c ) ^ { 2 } \cdot \sup _ { v \notin f ( C r i t ( f ) ) } \text {Vol} _ { d - 1 } \left ( f ^ { - 1 } ( \{ v \} ) \right ) + 2 \alpha _ { d } \left | C r i t ( f ) \right | ,
$$

and where Vol d − 1   f − 1 ( { v } )   is the ( d − 1) -dimensional volume of f − 1 ( { v } ) seen as a submanifold of M and α d is the volume of the unit ball in R d .

Proof. We will use the Coarea Formula (see Chapter II, Theorem 5.8. in [Sak96]) which states that for every integrable function u : M → R , we have:

$$
\int _ { M } u \| \nabla f \| \, d V o l = \int _ { \mathbb { R } } \int _ { f ^ { - 1 } ( \{ v \} ) } u \, d V o l _ { v } \, d v ,
$$

where Vol v is the volume measure induced on the ( d − 1)-dimensional manifold f − 1 ( { v } ), v being a non-critical value. Note that in the integral in the right hand side of the equation above, we only consider non-critical values v of f . This is possible because the set of critical values of f has measure 0 in R by Sard’s Theorem.

Let J be a refined cover element. Let ε > 0 and consider

$$
M _ { \varepsilon } = M \bigcup _ { c \in C r i t ( f ) } B \left ( c , \varepsilon \right ) .
$$

Applying the Coarea Formula to u = 1 f − 1 (J) ∩ M ε , gives

$$
\text {Applying the Coarea Formula to } u & = \mathbb { I } _ { f ^ { - 1 } ( J ) \cap M _ { \varepsilon } } , \, \text {gives} \\ \text {Vol} \left ( f ^ { - 1 } ( J ) \cap M _ { \varepsilon } \right ) & = \int _ { M } u d V o l \leq \frac { 1 } { \inf _ { p \in M _ { \varepsilon } } \| \nabla f ( p ) \| } \cdot \int _ { M } u \| \nabla f \| d V o l \\ & = \frac { 1 } { \inf _ { p \in M _ { \varepsilon } } \| \nabla f ( p ) \| } \cdot \int _ { \mathbb { R } } \int _ { f ^ { - 1 } ( \{ v \} ) } u d V o l _ { v } \, d v \\ & \leq \frac { 1 } { \inf _ { p \in M _ { \varepsilon } } \| \nabla f ( p ) \| } \cdot \int _ { J } \int _ { f ^ { - 1 } ( \{ v \} ) } d V o l _ { v } \, d v \\ & \leq \frac { 1 } { \inf _ { p \in M _ { \varepsilon } } \| \nabla f ( p ) \| } \cdot \mathcal { W } ( r ) \cdot \sup _ { v \notin f ( C r ( f ) ) } V o l _ { d - 1 } \left ( f ^ { - 1 } ( \{ v \} ) \right ) .
$$

[Page 33]

Moreover, for small enough ε , as inf p ∈ M ε ∥∇ f ( p ) ∥ is achieved in a neighborhood of a critical point c at a point p such that d( c,p ) ≥ ε , we have (see the proof of Proposition 5.2):

$$
\inf _ { p \in M _ { \varepsilon } } \| \nabla f ( p ) \| \geq \frac { 2 \varepsilon } { \mu _ { 0 } ^ { 2 } } ,
$$

where µ 0 → µ ( c ) as p → c . As such, for small enough ε , we have

$$
\mu _ { 0 } \leq \sqrt { 2 } \mu ( c ) ,
$$

and therefore

Accordingly,

$$
\inf _ { p \in M _ { \epsilon } } \| \nabla f ( p ) \| \geq \frac { \varepsilon } { \mu ( c ) ^ { 2 } } .
$$

$$
\text {Vol} \left ( f ^ { - 1 } ( J ) \cap M _ { \varepsilon } \right ) \leq \frac { \max _ { c \in \text {Crit} ( f ) } \mu ( c ) ^ { 2 } } { \varepsilon } \cdot \mathcal { W } ( r ) \cdot \sup _ { v \notin f ( \text {Crit} ( f ) ) } \text {Vol} _ { d - 1 } \left ( f ^ { - 1 } ( \{ v \} ) \right ) .
$$

Now, by Proposition 2.3, for every c ∈ Crit( f ), there exists ε ′ c such that for every ε ≤ ε ′ c we have

$$
\text {Vol} \left ( B \left ( c , \varepsilon \right ) \right ) \leq 2 \alpha _ { d } \cdot \varepsilon ^ { d } .
$$

Taking ε ′ = min c ∈ Crit( f ) ε ′ c , we have that for ε ≤ ε ′ :

$$
\sum _ { c \in C r i t ( f ) } V o l \left ( B \left ( c , \varepsilon \right ) \right ) \leq 2 \alpha _ { d } \left | C r i t ( f ) \right | \cdot \varepsilon ^ { d } .
$$

As such, for small enough ε > 0, we have

$$
V o l \left ( f ^ { - 1 } ( J ) \right ) & \leq V o l \left ( f ^ { - 1 } ( J ) \cap M _ { \varepsilon } \right ) + \sum _ { c \in C r i t ( f ) } V o l \left ( B \left ( c , \varepsilon \right ) \right ) \\ & \leq \frac { \max _ { c \in C r i t ( f ) } \mu ( c ) ^ { 2 } } { \varepsilon } \cdot \mathcal { W } ( r ) \cdot \sup _ { v \notin f ( C r i t ( f ) ) } V o l _ { d - 1 } \left ( f ^ { - 1 } ( \{ v \} ) \right ) + 2 \alpha _ { d } | C r i t ( f ) | \cdot \varepsilon ^ { d } .
$$

Since W ( r ) −→ r →∞ 0, we can choose

$$
\varepsilon = \mathcal { W } ( r ) ^ { \frac { 1 } { d + 1 } } ,
$$

and we will have, for a large enough resolution r ,

$$
\text {Vol} \left ( f ^ { - 1 } ( J ) \right ) \leq \left [ \max _ { c \in \text {Crit} ( f ) } \mu ( c ) ^ { 2 } \cdot \sup _ { v \notin f ( \text {Crit} ( f ) ) } \text {Vol} _ { d - 1 } \left ( f ^ { - 1 } ( \{ v \} ) \right ) + 2 \alpha _ { d } | \text {Crit} ( f ) | \right ] \cdot \mathcal { W } ( r ) ^ { \frac { d } { d + 1 } } .
$$

# 5.4.4 Convergence rate for the approximation error

We now put together Lemmas 5.2, 5.3 and 5.5 in the following proposition.

Proposition 5.4. Let λ > 0 such that

$$
\omega _ { f } \left ( \lambda \right ) \leq \frac { g } { 1 - g } \cdot \frac { \mathcal { W } ( r ) } { 4 } .
$$

For any large enough resolution r , we have

$$
\mathbb { E } \left ( A _ { n } ^ { p } \right ) \leq \left ( \lambda + \xi \cdot \sqrt { \mathcal { W } ( r ) } \right ) ^ { p } + \tilde { \eta } \cdot \mathcal { W } ( r ) ^ { \frac { d } { d + 1 } } + \frac { \zeta } { \lambda ^ { d } } \cdot \exp \left ( \frac { - n a \lambda ^ { d } } { 2 ^ { d } } \right ) ,
$$

where

$$
\xi = \max _ { c \in C r i t ( f ) } \mu ( c ) \cdot \sqrt { \frac { 1 - g } { g } } ,
$$

[Page 34]

$$
\tilde { \eta } = 3 | \text {Crit} ( f ) | \left [ \sup \frac { d m } { d \text {Vol} } \right ] \cdot \text {Diam} ( M ) ^ { p } \cdot \eta , \\ \eta = \max _ { c \in \text {Crit} ( f ) } \mu ( c ) ^ { 2 } \cdot \sup _ { v \notin f ( \text {Crit} ( f ) ) } \text {Vol} _ { d - 1 } \left ( f ^ { - 1 } \{ v \} \right ) + 2 \alpha _ { d } | \text {Crit} ( f ) | , \\ \zeta = \frac { 4 ^ { d } \text {Diam} ( M ) ^ { p } } { a } .
$$

$$
a
$$

Proof. By definition,

$$
A _ { n } ^ { p } = \frac { 1 } { n } \sum _ { i = 1 } ^ { n } d _ { H } ( [ x _ { i } ] _ { \sim _ { f } } , \mathbb { \Delta } ( x _ { i } ) ) ^ { p } .
$$

Since X n consists of i.i.d. samples:

$$
\mathbb { E } \left ( A _ { n } ^ { p } \right ) = \mathbb { E } \left ( d _ { H } ( [ x _ { 1 } ] _ { \sim _ { f } } , \mathbb { \Delta } ( x _ { 1 } ) ) ^ { p } \right ) .
$$

First, if d H (M , X n ) > λ , we use the loose bound

$$
d _ { H } ( [ x _ { 1 } ] _ { \sim f } , \mathbb { A } ( x _ { 1 } ) ) \leq D i a m ( M ) .
$$

Now, assume that d H (M , X n ) ≤ λ . We will denote J c ⊆ J as the collection of elements J in J that either contain critical values, or are adjacent to elements containing critical values. We see that | J c | ≤ 3 | Crit( f ) | .

- • If f ( x 1 ) ∈   J ∈ J c J, we use the bound d H ([ x 1 ] ∼ f , ∆∆( x 1 )) ≤ Diam(M).
- • If f ( x 1 ) / ∈   J ∈ J c J, we can use Lemmas 5.2 and 5.3 to prove that


$$
d _ { H } ( [ x _ { 1 } ] _ { \sim f } , \mathbb { \Delta } ( x _ { 1 } ) ) \leq \lambda + \xi \cdot \sqrt { W ( r ) } .
$$

As such,

$$
d _ { H } ( [ x _ { 1 } ] _ { \sim } , \Delta ( x _ { 1 } ) ) ^ { p } \leq \left ( \lambda + \xi \cdot \sqrt { \mathcal { W } ( r ) } \right ) ^ { p } + 1 _ { f ( x _ { 1 } ) \in \cup _ { J _ { c } \in J _ { c } } J } \cdot D i a m ( M ) ^ { p } + 1 _ { d _ { H } ( M , X _ { n } ) \rhd \lambda } \cdot D i a m ( M ) ^ { p } .
$$

Accordingly,

$$
\mathbb { E } \left ( d _ { H } ( [ x _ { 1 } ] _ { \sim } , \Delta ( x _ { 1 } ) ) ^ { p } \right ) & \leq \left ( \lambda + \xi \cdot \sqrt { \mathcal { W } ( r ) } \right ) ^ { p } + \mathbb { P } \left ( f ( x _ { 1 } ) \in \bigcup _ { J \in J _ { c } } J \right ) \cdot \text {Diam} ( M ) ^ { p } \\ & + \mathbb { P } \left ( d _ { H } ( M , X _ { n } ) > \lambda \right ) \cdot \text {Diam} ( M ) ^ { p } ,
$$

and by Lemma 5.5 we obtain

$$
\text {Lemma 5.5 we obtain} \\ \\ \mathbb { P } \left ( f ( x _ { 1 } ) \in \bigcup _ { J \in J _ { c } } J \right ) = m \left ( f ^ { - 1 } ( \bigcup _ { J \in J _ { c } } J ) \right ) \\ & \leq \sum _ { J \in J _ { c } } \max _ { J \in J } \left ( f ^ { - 1 } ( J ) \right ) \\ & \leq 3 | \text {Crit} ( f ) | \cdot \max _ { J \in J } \left ( f ^ { - 1 } ( J ) \right ) \\ & \leq 3 | \text {Crit} ( f ) | \left [ \sup _ { d \text {Vol} } \frac { d m } { J \in J } \right ] \cdot \max _ { J \in J } \text {Vol} \left ( f ^ { - 1 } ( J ) \right ) \\ & \leq 3 | \text {Crit} ( f ) | \left [ \sup _ { d \text {Vol} } \frac { d m } { J \in W ( r ) + \hat { r } } \right ] \eta \cdot \mathcal { W } ( r ) + \hat { r } .
$$

The measure m satisfies the ( a,b )-standard assumption for b = d and it is fully supported on M by Assumption 2. Using Theorem 5.4, we have:

[Page 35]

![The image is a graphical representation of a human figure. The figure is gold in color and is positioned in the center of the image. The background is white, and there are no other objects or elements in the image. The gold figure is the main focus of the image, and it is centrally placed. The image is a simple, two-dimensional representation of a human figure. The figure is a stylized representation, with a detailed and realistic appearance. The figure is depicted in a way that suggests it is a representation of a human, with its body and limbs clearly visible. The figure is positioned in a way that suggests it is a representation of a human, with its head and shoulders clearly visible. The background is white, and there are no other objects or elements in the image. The figure is the central focus of the image, and it is centrally placed. The image is a simple, two-dimensional representation of a human figure. The figure is gold in color](<2506.02810v1/imageFile7.png>)

Figure 7: Left: Mesh of a 3-dimensional point cloud representing a human shape. Right: Mapper graph computed on the point cloud using the height as filter function. Nodes are colored using the average value of the filter.

$$
\mathbb { P } \left ( d _ { H } ( M , X _ { n } ) > \lambda \right ) \leq \min \left ( \frac { 4 ^ { d } } { a \lambda ^ { d } } \exp \left ( \frac { - n a \lambda ^ { d } } { 2 ^ { d } } \right ) , 1 \right ) .
$$

Combining all three bounds leads to the result.

# 6 Computing the Gromov-Wasserstein distance for Mapper graphs in practice

We conclude this article with a section providing some numerical experiments on both our metric measure space version of the Mapper graph ( M n , d H ,δ ◦ ∆∆ − 1 ), as well as the GW p distances between them. In order to achieve this, we use optimal transport libraries for computing the GW p distances. We emphasize that this section contains merely toy experiments; a more comprehensive study would be needed to explore the relevance of such considered transport metrics for Reeb inference with Mapper in the general context of machine learning and data science.

Since it is finite, the metric space structure of the Mapper ( M n , d H ,δ ◦ ∆∆ − 1 ) can be summarized in a distance matrix D that contains the pairwise Hausdorff distances between the simplices of the Mapper. Furthermore, the measure δ ◦ ∆∆ − 1 being discrete, it can be represented with a vector P storing the measures associated to each simplex. Finally, we use the POT Python library [Fla+21] to compute the Gromov-Wasserstein distances between different Mapper graphs. 3 Our code is publicly available at the following repository [Oul].

# 6.1 Change of filter function

We first provide an experiment where Mapper graphs are computed on a 3-dimensional point cloud X n representing a human shape. See Figure 7.

We then explicitly compute 2-Gromov-Wasserstein distances (as the probability measure is fixed) between different Mapper graphs on this point cloud that are obtained using the same clustering

3 Note that this library actually computes the GW p formulation of [Me´m11].

[Page 36]

0.035

0.030

0.025

0.020

0.015

0.010

0.005

0.000

0.0

0.2

0.4

0.6

0.8

10

Figure 8: 2-Gromov-Wasserstein distances between the Mapper graph computed with f 0 and the Mapper graphs computed with f t with respect to ∥ f 0 − f t ∥ ∞ .

algorithm (KMeans with three clusters), the same gain g = 0 . 3 and resolution r = 25, and varying the filter function. More precisely, we use filter functions from a parametrized family { f t } t defined with

$$
f _ { t } ( x ) = \langle x , t \cdot u + ( 1 - t ) \cdot v \rangle ,
$$

with u and v being the unit vectors associated to the horizontal and vertical directions respectively. For reference, the Mapper computed with filter function f 0 is displayed in Figure 7.

In Figure 8, we plot the resulting 2-Gromov-Wasserstein distances between the Mapper graph computed with f 0 and the Mapper graphs computed with f t with respect to ∥ f 0 − f t ∥ ∞ = sup x ∈ X | f 0 ( x ) − f t ( x ) | . As one can see, the distances increase monotonically, indicating that Gromov-Wasserstein distances between Mapper graphs might be stable w.r.t. perturbations of filter functions a theoretical direction that we aim at pursuing in future work.

# 6.2 Change of measure

In this section, we illustrate the effect of varying the underlying probability measures, by computing Mapper graphs on point clouds sampled from a torus T 2 with different probability measures. The torus T 2 is a two dimensional manifold that can be parametrized with two angles ( θ,ϕ ) ∈ [0 , 2 π ] 2 and can be embedded in R 3 with

$$
h \colon [ 0 , 2 \pi ] ^ { 2 } & \longrightarrow \mathbb { R } ^ { 3 } \\ ( \theta , \phi ) & \longmapsto ( ( a + b \cos ( \theta ) ) \cos ( \phi ) , ( a + b \cos ( \theta ) ) \sin ( \phi ) , b \sin ( \theta ) )
$$

where a,b > 0 are two radius parameters. We use the height function f as filter, which is given by the projection of h over its first coordinate, i.e., f : ( θ,ϕ )  → ( a + b cos( θ ))cos( ϕ ). It can easily be checked that f is a Morse function, by differentiating twice over θ and ϕ . Moreover, the Riemannian metric g induced by the torus embedding in R 3 , is given at any point ( θ,ϕ ) by the positive definite matrix b 2 0

$$
G ( p ) = \begin{pmatrix} b ^ { 2 } & 0 \\ 0 & ( a + b \cos ( \theta ) ) ^ { 2 } \end{pmatrix} .
$$

We will consider several probability measures m p,q on T 2 that are parametrized by two values 0 < p,q < 1, that give the proportion of points sampled in the region ϕ ∈ [5 π/ 6 , 7 π/ 6], and the proportion in the region ϕ ∈ [0 ,π/ 6] ∪ [11 π/ 6 , 2 π ], respectively. More explicitly, we define

$$
d m _ { p , q } ( \theta , \phi ) = \frac { u _ { p , q } ( \phi ) ( a + b \cos ( \theta ) ) d \theta d \phi } { 2 \pi a } ,
$$

$$
u _ { p , q } ( \phi ) = p \frac { 3 } { \pi } 1 _ { [ 5 \pi / 6 , 7 \pi / 6 ] } ( \phi ) + q \frac { 3 } { \pi } 1 _ { [ 0 , \pi / 6 ] \cup [ 1 \pi / 6 , 2 \pi ] } ( \phi ) + ( 1 - p - q ) \frac { 3 } { 4 \pi } 1 _ { [ \pi / 6 , 5 \pi / 6 ] \cup [ 7 \pi / 6 , 1 1 \pi / 6 ] } ( \phi ) .
$$

[Page 37]

Notice that the special case p = q = 1 / 6 corresponds to the normalized volume measure on T 2 . We fix a = 0 . 75 and b = 0 . 25, and we pick values for p and q in the 3 × 3 grid [1 / 9 , 1 / 6 , 1 / 3] 2 , resulting in nine different probability measures. Then, we sample n p,q = 2 · 10 5 points from each probability measure m p,q , and construct the corresponding Mapper graphs { M n p,q } p,q using resolution r = 30 and gain g = 0 . 3 for all ( p,q ) pairs. See Figure 9 for an example of three of these samples, and Figure 10 for the corresponding Mapper graphs.

First, remark that the Mapper graphs { M n p,q } p,q are all identical as (combinatorial) simplicial complexes. However, as they are different as metric measure spaces, when considering the 2-GromovWasserstein distances ˆ GW 2 between these metric measure spaces { ( M n p,q , d H , m p,q ◦ ∆∆ − 1 ) } p,q , we obtain a 9 × 9 distance matrix with nonzero entries. In order to visualize it, we perform multidimensional scaling (MDS) to visualize this distance matrix as a 2-dimensional point cloud in Figure 11. The correspondence between the point labels in Figure 11 and the corresponding ( p,q ) parameters is given in Table 1. In Figure 11, we can see three pairs of points, namely ( b,d ), ( c,g ) and ( f,h ), that are close in the MDS representation, illustrating the fact that each of these pairs corresponds to two symmetric parameter pairs ( p,q ) that induce two symmetrical underlying probability measures on T 2 with respect to the vertical axis. One can also see that the points labeled a and i , which correspond to those two measures that assign the least and the most mass in the two regions of the torus, respectively, are the ones that are the farthest away from the remaining points, indicating that the corresponding Mapper graphs computed using these two measures are the most different from the others.

![In the image there are three pink colored rings.](<2506.02810v1/imageFile9.png>)

Figure 9: Point clouds sampled from m p,q for different values of p and q . Left: p = q = 1 / 12. Middle: p = q = 1 / 6 (uniform measure). Right: p = q = 1 / 3.

![In the image there are three circles. The circles are made up of dots.](<2506.02810v1/imageFile10.png>)

Figure 10: Mapper graphs computed on the samples shown in Figure 9. Vertices and edges are colored using their associated masses. Left: p = q = 1 / 12. Middle: p = q = 1 / 6 (uniform measure). Right: p = q = 1 / 3.

[Page 38]

0.03

0.02

0.01

0.00

0.02

~0.03

~0.02

0.00

0.02

0.04

0.06

Figure 11: MDS visualization of the Gromov-Wasserstein distance matrix between the different Mapper graphs.

|Label|p|q|
|---|---|---|
|a|1/12|1/12|
|b|1/12|1/6|
|c|1/12|1/3|
|d|1/6|1/12|
|e|1/6|1/6|
|f|1/6|1/3|
|g|1/3|1/12|
|h|1/3|1/6|
|i|1/3|1/3|


Table 1: Correspondence between the labels of Figure 11 and the parameters of m p,q .

# 7 Acknowledgements.

The authors would like to thank Gilles Carron and St´ephane Guillermou for their helpful insight and valuable discussions.

The research was supported by two grants from Agence Nationale de la Recherche: ANR JCJC TopModel ANR-23-CE23-0014 and ANR GeoDSIC ANR-22-CE40-0007. M.C. was also supported by the French government, through the 3IA Cote d’Azur Investments in the project managed by the National Research Agency (ANR) with the reference number ANR-23-IACL-0001.

[Page 39]

# A Proofs for Section 2

# A.1 Proof of Proposition 2.1

Denoting w = ( dx 1 ( v ) ,...,dx d ( v )), we have:

and

$$
g ( v , v ) = w ^ { T } \cdot G ( p ) \cdot w ,
$$

$$
g _ { 0 } ( v , v ) = w ^ { T } \cdot w ,
$$

where G( p ) := [g i,j ] i,j is the symmetric positive-definite matrix given by g i,j = g( ∂ i | p ,∂ j | p ).

Hence,

$$
\lambda ( p ) \cdot \sqrt { g _ { 0 } ( v , v ) } \leq \sqrt { g ( v , v ) } \leq \mu ( p ) \cdot \sqrt { g _ { 0 } ( v , v ) }
$$

# A.2 Proof of Proposition 2.2

Let p ∈ M and let U be a coordinate neighborhood of p . Up to shrinking U , we can assume that:

$$
U = \{ q \in M \, , \, d _ { 0 } ( p , q ) \leq \varepsilon \} ,
$$

for a given, small enough ε > 0, and where d 0 is the distance associated to the metric g 0 , given by

$$
d _ { 0 } ( p , q ) = \sqrt { x _ { 1 } ( q ) ^ { 2 } + \dots + x _ { d } ( q ) ^ { 2 } } ,
$$

where q is given in local coordinates by x ( q ) = ( x 1 ( q ) ,...,x d ( q )).

From Proposition 2.1, there exist continuous functions λ,µ : U → (0 , ∞ ) such that for every q ∈ U and v ∈ T q M, one has

$$
\lambda ( q ) \cdot \sqrt { g _ { 0 } ( v , v ) } \leq \sqrt { g ( v , v ) } \leq \mu ( q ) \cdot \sqrt { g _ { 0 } ( v , v ) } .
$$

Furthermore, λ ( q ) ,µ ( q ) → λ ( p ) ,µ ( p ) as q → p because λ and µ are continuous.

Now, let γ : [0 , 1] → M be a piecewiseC ∞ curve such that γ (0) = p and γ (1) = q ∈ U .

• If γ is a segment for d 0 , then it lies in U (as d 0 ( p,q ) ≤ ε ). We also have:

$$
\text {segment for } d _ { 0 } , \text { then it lies in } U \ ( \text {as } d _ { 0 } ( p , q ) \leq \varepsilon ) . \text { We also have: } \\ \intertext { d _ { 0 } ( p , q ) = \int _ { 0 } ^ { 1 } \sqrt { g _ { 0 } \left ( \frac { d \gamma ( t ) } { d t } , \frac { d \gamma ( t ) } { d t } \right ) } \, d t } \\ \geq \frac { 1 } { \sup _ { t \in [ 0 , 1 ] } \mu ( \gamma ( t ) ) } \int _ { 0 } ^ { 1 } \sqrt { g \left ( \frac { d \gamma ( t ) } { d t } , \frac { d \gamma ( t ) } { d t } \right ) } \, d t \\ = \frac { \text {length} ( \gamma ) } { \sup _ { t \in [ 0 , 1 ] } \mu ( \gamma ( t ) ) } \\ \geq \frac { d ( p , q ) } { \sup _ { t \in [ 0 , 1 ] } \mu ( \gamma ( t ) ) } .
$$

[Page 40]

$$
\text {inside} \, \mathbf o , \, \text {then.} \\ \text {length} ( \gamma ) & = \int _ { 0 } ^ { 1 } \sqrt { g \left ( \frac { d \gamma ( t ) } { d t } , \frac { d \gamma ( t ) } { d t } \right ) } \, d t \\ & \geq \inf _ { t \in [ 0 , 1 ] } \ \lambda ( \gamma ( t ) ) \cdot \int _ { 0 } ^ { 1 } \sqrt { g _ { 0 } } \left ( \frac { d \gamma ( t ) } { d t } , \frac { d \gamma ( t ) } { d t } \right ) \, d t \\ & \geq \inf _ { t \in [ 0 , 1 ] } \ \lambda ( \gamma ( t ) ) \cdot d _ { 0 } ( p , q )
$$

• If γ leaves U , then there exists a smallest t 0 such that γ ( t 0 ) / ∈ U . We have:

$$
t s \ U , \, \text {then there exists a smallest } t _ { 0 } \text { such that } \gamma ( t _ { 0 } ) \notin \ U \, . \, \text {We have:} \\ \text {length} ( \gamma ) \geq \int _ { 0 } ^ { t _ { 0 } } \sqrt { g \left ( \frac { d \gamma ( t ) } { d t } , \frac { d \gamma ( t ) } { d t } \right ) d t } \\ \geq \inf _ { t \in [ 0 , 1 ] } \ \lambda ( \gamma ( t ) ) \cdot \int _ { 0 } ^ { t _ { 0 } } \sqrt { g _ { 0 } \left ( \frac { d \gamma ( t ) } { d t } , \frac { d \gamma ( t ) } { d t } \right ) d t } \\ \geq \inf _ { t \in [ 0 , 1 ] } \ \lambda ( \gamma ( t ) ) \cdot \varepsilon \\ \geq \inf _ { t \in [ 0 , 1 ] } \ \lambda ( \gamma ( t ) ) \cdot d _ { 0 } ( p , q ) \\ \text {tioned above} \ \lambda ( q ) , \mu ( q ) \to \lambda ( p ) , \mu ( p ) \text { as } q \to p , \text { therefore } ( \text {and by shrinking} )
$$

Like mentioned above, λ ( q ) ,µ ( q ) → λ ( p ) ,µ ( p ) as q → p , therefore (and by shrinking U again if necessary) we can ensure that λ 0 = inf r ∈ U λ ( r ) > 0 and that µ 0 = sup r ∈ U µ ( r ) < ∞ . We have proved that for every q ∈ U , one has

$$
\lambda _ { 0 } \cdot d _ { 0 } ( p , q ) \leq d ( p , q ) \leq \mu _ { 0 } \cdot d _ { 0 } ( p , q ) ,
$$

and we finally see that, as q → p :

$$
\lambda _ { 0 } , \mu _ { 0 } \rightarrow \lambda ( p ) , \mu ( p ) .
$$

# A.3 Proof of Proposition 2.3

Let us consider a chart φ : U → R d around p such that φ ( p ) = (0 ,..., 0) and

$$
g ( \partial _ { i _ { | p } } , \partial _ { j _ { | p } } ) = \begin{cases} 1 , \text { if } i = j \\ 0 , \text { otherwise} \end{cases}
$$

so as to have g p = g 0 , where g 0 is the Euclidean metric in local coordinates.

For a small enough ε > 0 and by Proposition 2.2, we know that

$$
B _ { 0 } ( p , \varepsilon / \mu _ { 0 } ) \subseteq B ( p , \varepsilon ) \subseteq B _ { 0 } ( p , \varepsilon / \lambda _ { 0 } ) \subseteq U ,
$$

for λ 0 ,µ 0 > 0, where B 0 ( p, · ) stands for the Euclidean ball using the distance d 0 in local coordinates.

As such,

$$
\text {Vol} \left ( B _ { 0 } ( p , \varepsilon / \mu _ { 0 } ) \right ) \leq \text {Vol} \left ( B ( p , \varepsilon ) \right ) \leq \text {Vol} \left ( B _ { 0 } ( p , \varepsilon / \lambda _ { 0 } ) \right ) .
$$

Furthermore, λ 0 ,µ 0 → 1 as ε → 0 because g p = g 0 . Denote

.

$$
c & = \inf \{ \det ( G ( q ) ) , q \in B _ { 0 } ( p , \varepsilon / \mu _ { 0 } ) \} , \\ C & = \sup \{ \det ( G ( q ) ) , q \in B _ { 0 } ( p , \varepsilon / \lambda _ { 0 } ) \} ,
$$

$$
=
$$

where G = [g( ∂ i ,∂ j )] i,j . Since det(G( p )) = 1 (as g p = g 0 ) and G is smooth, we have c,C → 1 as ε → 0.

Now, denoting the Lebesgue measure in R d as l , we have

[Page 41]

$$
\text {hand:} \\ \text {Vol} \left ( B _ { 0 } ( p , \varepsilon / \mu _ { 0 } ) \right ) & = \int _ { \{ x \in \mathbb { R } ^ { d } , \, \| x \| \leq \varepsilon / \mu _ { 0 } \} } \sqrt { \det ( G ) \circ \varphi ^ { - 1 } \, d l } \\ & \geq \sqrt { c } \cdot \alpha _ { d } \cdot \left ( \frac { \varepsilon } { \mu _ { 0 } } \right ) ^ { d }
$$

• On the other hand:

$$
\text {ther hand} & \colon \\ & \text {Vol} \left ( B _ { 0 } ( p , \varepsilon / \lambda _ { 0 } ) \right ) = \int _ { \{ x \in \mathbb { R } ^ { d } , \, \| x \| \leq \varepsilon / \lambda _ { 0 } \} } \sqrt { \det ( G ) } \circ \varphi ^ { - 1 } \, d l \\ & \quad \leq \sqrt { C } \int _ { \{ x \in \mathbb { R } ^ { d } , \, \| x \| \leq \varepsilon / \lambda _ { 0 } \} } \, d l \\ & \quad \leq \sqrt { C } \cdot \alpha _ { d } \cdot \left ( \frac { \varepsilon } { \lambda _ { 0 } } \right ) ^ { d }
$$

As such,

$$
\text {Vol} \left ( B ( p , \varepsilon ) \right ) _ { \varepsilon \rightarrow 0 } \alpha _ { d } \cdot \varepsilon ^ { d }
$$

# A.4 Proof of Proposition 2.4

The coordinate vector fields are a basis of the tangent spaces T p M for all p ∈ U . As such, in U , the gradient ∇ f can be expressed as a linear combination:

$$
\nabla f = \sum _ { i = 1 } ^ { d } a _ { i } \cdot \partial _ { i } .
$$

Now, we know that for all i :

and as such

$$
g \left ( \nabla f , \partial _ { i } \right ) = d f ( \partial _ { i } ) = \frac { \partial f } { \partial x _ { i } } ,
$$

$$
\sum _ { j = 1 } ^ { d } a _ { j } \cdot g \left ( \partial _ { j } , \partial _ { i } \right ) = \frac { \partial f } { \partial x _ { i } } .
$$

This gives

and

$$
( a _ { 1 } , \dots , a _ { d } ) \cdot [ g ( \partial _ { i } , \partial _ { j } ) ] _ { i , j } = \left ( \frac { \partial f } { \partial x _ { 1 } } , \dots , \frac { \partial f } { \partial x _ { d } } \right ) ,
$$

$$
( a _ { 1 } , \dots , a _ { d } ) = \left ( \frac { \partial f } { \partial x _ { 1 } } , \dots , \frac { \partial f } { \partial x _ { d } } \right ) \cdot [ g ( \partial _ { i } , \partial _ { j } ) ] _ { i , j } ^ { - 1 } \, .
$$

Furthermore,

$$
g \left ( \nabla f , \nabla f \right ) & = \left ( a _ { 1 } , \dots , a _ { d } \right ) \cdot \left [ g ( \partial _ { i } , \partial _ { j } ) \right ] _ { i , j } \cdot ( a _ { 1 } , \dots , a _ { d } ) ^ { T } \\ & = \left ( \frac { \partial f } { \partial x _ { 1 } } , \dots , \frac { \partial f } { \partial x _ { d } } \right ) \cdot \left [ g ( \partial _ { i } , \partial _ { j } ) \right ] _ { i , j } ^ { - 1 } \cdot \left [ g ( \partial _ { i } , \partial _ { j } ) \right ] _ { i , j } \cdot \left [ g ( \partial _ { i } , \partial _ { j } ) \right ] _ { i , j } ^ { - 1 } \cdot \left ( \frac { \partial f } { \partial x _ { 1 } } , \dots , \frac { \partial f } { \partial x _ { d } } \right ) ^ { T } \\ & = \left ( \frac { \partial f } { \partial x _ { 1 } } , \dots , \frac { \partial f } { \partial x _ { d } } \right ) \cdot \left [ g ( \partial _ { i } , \partial _ { j } ) \right ] _ { i , j } ^ { - 1 } \cdot \left ( \frac { \partial f } { \partial x _ { 1 } } , \dots , \frac { \partial f } { \partial x _ { d } } \right ) ^ { T } .
$$

As such,

$$
\frac { 1 } { \mu ( p ) } \cdot \sqrt { \sum _ { i = 1 } ^ { d } \frac { \partial f ( p ) ^ { 2 } } { \partial x _ { i } } } \leq \sqrt { g \left ( \nabla f ( p ) , \nabla f ( p ) \right ) } \leq \frac { 1 } { \lambda ( p ) } \cdot \sqrt { \sum _ { i = 1 } ^ { d } \frac { \partial f ( p ) ^ { 2 } } { \partial x _ { i } } } .
$$

[Page 42]

# References

[Aam+19] Eddie Aamari et al. ‘Estimating the Reach of a Manifold’. In: Electronic Journal of Statistics (2019).

[ACL11] Ery Arias-Castro, Guangliang Chen and Gilad Lerman. ‘Spectral clustering based on local linear approximations’. In: Electronic Journal of Statistics 5 (2011), pp. 1537– 1587.

[BC18] Rickard Bru¨el-Gabrielsson and Gunnar Carlsson. ‘Exposition and interpretation of the topology of neural networks’. In: CoRR . arXiv:1810.03234, 2018.

[BCL18] Peter B¨urgisser, Felipe Cucker and Pierre Lairez. ‘Computing the homology of basic semialgebraic sets in weak exponential time’. In: Journal of the ACM (JACM) 66.1 (2018), pp. 1–30.

[BFL16] Ulrich Bauer, Barbara Di Fabio and Claudia Landi. ‘An Edit Distance for Reeb Graphs’. In: Eurographics Workshop on 3D Object Retrieval (3DOR 2016) . Ed. by Alfredo Ferreira, Andrea Giachetti and Daniela Giorgi. ISSN: 1997-0471. The Eurographics Association, 2016. isbn : 978-3-03868-004-8. doi : 10.2312/3dor.20161084 . url : https://doi.org/10.2312/3dor.20161084 .

[BG14] Emmanuel Boissard and Thibaut Le Gouic. ‘On the mean speed of convergence of empirical and occupation measures in Wasserstein distance’. In: Annales de l’Institut Henri Poincar´e, Probabilit´es et Statistiques 50.2 (2014), pp. 539–563. doi : 10.1214/12AIHP517 . url : https://doi.org/10.1214/12-AIHP517 .

[BGW14] Ulrich Bauer, Xiaoyin Ge and Yusu Wang. ‘Measuring Distance between Reeb Graphs’. In: Proceedings of the 30th Annual Symposium on Computational Geometry (SoCG 2014) . Association for Computing Machinery, 2014, pp. 464–473. isbn : 978-1-45032594-3. doi : 10.1145/2582112.2582169 .

[BLM21] Ulrich Bauer, Claudia Landi and Facundo M´emoli. ‘The Reeb Graph Edit Distance is Universal’. In: Foundations of Computational Mathematics (FoCM) 21.5 (2021), pp. 1441–1464. issn : 1615-3383. doi : 10.1007/s10208-020-09488-3 .

[Boo86] William M Boothby. An introduction to differentiable manifolds and Riemannian geometry . Academic press, 1986.

[Bro+21] Adam Brown et al. ‘Probabilistic convergence and stability of random mapper graphs’. In: Journal of Applied and Computational Topology 5.1 (2021), pp. 99–140.

[Cha+14] Fr´ed´eric Chazal et al. ‘Convergence rates for persistence diagram estimation in topological data analysis’. In: International Conference on Machine Learning . PMLR. 2014, pp. 163–171.

[CM22] Mathieu Carrie`re and Bertrand Michel. ‘Statistical analysis of Mapper for stochastic and multivariate filters’. In: Journal of Applied and Computational Topology 6.3 (2022), pp. 331–369.

[CMO18] Mathieu Carriere, Bertrand Michel and Steve Oudot. ‘Statistical analysis and parameter selection for mapper’. In: Journal of Machine Learning Research 19.12 (2018), pp. 1–39.

[CO18] Mathieu Carriere and Steve Oudot. ‘Structure and stability of the one-dimensional mapper’. In: Foundations of Computational Mathematics 18 (2018), pp. 1333–1396.

[Cue09] Antonio Cuevas. ‘Set estimation: Another bridge between statistics and geometry’. In: Bol. Estad. Investig. Oper 25.2 (2009), pp. 71–85.

[DMP16] Vin De Silva, Elizabeth Munch and Amit Patel. ‘Categorified reeb graphs’. In: Discrete & Computational Geometry 55.4 (2016), pp. 854–906.

[Fla+21] R´emi Flamary et al. ‘POT: Python Optimal Transport’. In: Journal of Machine Learning Research 22.78 (2021), pp. 1–8. url : http://jmlr.org/papers/v22/20-451.html .

[Hen99] Jeff Henrikson. ‘Completeness and total boundedness of the Hausdorff metric’. In: MIT Undergraduate Journal of Mathematics 1.69-80 (1999), p. 10.

[Page 43]

[HRS23] Yanjun Han, Philippe Rigollet and George Stepaniants. ‘Covariance alignment: from maximum likelihood estimation to Gromov-Wasserstein’. In: arXiv preprint arXiv:2311.13595 (2023).

[JPH21] Brian B Joseph, Trami Pham and Christopher Hastings. ‘Topological Data Analysis in Conjunction with Traditional Machine Learning Techniques to Predict Future MDAP PM Ratings’. In: Acquisition Research Program, 2021.

[KDO23] Patrice Koehl, Marc Delarue and Henri Orland. ‘Computing the Gromov-Wasserstein distance between two surface meshes using optimal transport’. In: Algorithms 16.3 (2023), p. 131.

[M´em09] Facundo Me´moli. ‘Spectral Gromov-Wasserstein distances for shape matching’. In: 2009 IEEE 12th International Conference on Computer Vision Workshops, ICCV Workshops . IEEE. 2009, pp. 256–263.

[M´em11] Facundo Me´moli. ‘Gromov–Wasserstein distances and the metric approach to object matching’. In: Foundations of computational mathematics 11 (2011), pp. 417–487.

[Mil63] John Willard Milnor. Morse theory . 51. Princeton university press, 1963.

[MR21] Satanik Mitra and Kameshwar Rao JV. ‘Experiments on Fraud Detection use case with QML and TDA Mapper’. In: 2021 IEEE International Conference on Quantum Computing and Engineering (QCE) . 2021, pp. 471–472. doi : 10.1109/QCE52317.2021. 00083 .

[MW16] Elizabeth Munch and Bei Wang. ‘Convergence between Categorical Representations of Reeb Space and Mapper’. In: 32nd International Symposium on Computational Geometry (SoCG 2016) . Ed. by S´ndor Fekete and Anna Lubiw. Vol. 51. Leibniz International Proceedings in Informatics (LIPIcs). Schloss Dagstuhl – Leibniz-Zentrum fu¨r Informatik, 2016, 53:1–53:16. isbn : 978-3-95977-009-5. doi : 10.4230/LIPIcs.SoCG. 2016.53 .

[Nai+18] Gregory Naitzat et al. ‘M-Boost: profiling and refining deep neural networks with topological data analysis’. In: KDD Workshop on Interactive Data Exploration and Analytics . 2018.

[NSW08] Partha Niyogi, Stephen Smale and Shmuel Weinberger. ‘Finding the homology of submanifolds with high confidence from random samples’. In: Discrete & Computational Geometry 39 (2008), pp. 419–441.

[Oul] Ziyad Oulhaj. Mapper Gromov-Wasserstein distance examples . url : https://github. com/ZiyadOulhaj/Mapper-MMS .

[Pet06] P Petersen. ‘Riemannian geometry’. In: Graduate Texts in Mathematics/SpringerVerlarg (2006).

[RGK24] Gabriel Rioux, Ziv Goldfeld and Kengo Kato. ‘Limit Laws for Gromov-Wasserstein Alignment with Applications to Testing Graph Isomorphisms’. In: arXiv preprint arXiv:2410.18006 (2024).

[RM24] Kaleb D Ruscitti and Leland McInnes. ‘Improving Mapper’s Robustness by Varying Resolution According to Lens-Space Density’. In: arXiv preprint arXiv:2410.03862 (2024).

[Ros+18] Paul Rosen et al. ‘Inferring quality in point cloud-based 3D printed objects using topological data analysis’. In: arXiv preprint arXiv:1807.02921 (2018).

[Sak96] Takashi Sakai. Riemannian geometry . Vol. 149. American Mathematical Soc., 1996.

[SMC+07] Gurjeet Singh, Facundo M´emoli, Gunnar E Carlsson et al. ‘Topological methods for the analysis of high dimensional data sets and 3d object recognition.’ In: PBG@ Eurographics 2 (2007), pp. 091–100.

[Stu06] Karl-Theodor Sturm. ‘On the geometry of metric measure spaces’. In: Acta Mathematica 196.1 (2006), pp. 65–131.

[Wan+18] Tongxin Wang et al. ‘Topological methods for visualization and analysis of high dimensional single-cell RNA sequencing data’. In: BIOCOMPUTING 2019: Proceedings of the Pacific Symposium . World Scientific. 2018, pp. 350–361.

[Page 44]

[Wan+24] Qingsong Wang et al. ‘Measure-Theoretic Reeb Graphs and Reeb Spaces’. In: 40th International Symposium on Computational Geometry (SoCG 2024) . Ed. by Wolfgang Mulzer and Jeff M. Phillips. Vol. 293. Schloss Dagstuhl – Leibniz-Zentrum fu¨r Informatik, 2024, 80:1–80:18. isbn : 978-3-95977-316-4. doi : 10.4230/LIPIcs.SoCG.2024.80 .

[Wan20] Ziqi Wang. ‘Exploration of Topological Data Analysis In 3D Printing’. In: 2020 International Conference on Information Science, Parallel and Distributed Systems (ISPDS) . IEEE. 2020, pp. 150–153.

[WB19] Jonathan Weed and Francis Bach. ‘Sharp asymptotic and finite-sample rates of convergence of empirical measures in Wasserstein distance’. In: Bernoulli 25.4A (2019), pp. 2620–2648.

[XLC19] Hongteng Xu, Dixin Luo and Lawrence Carin. ‘Scalable Gromov-Wasserstein learning for graph partitioning and matching’. In: Advances in neural information processing systems 32 (2019).

[Xu+19] Hongteng Xu et al. ‘Gromov-wasserstein learning for graph matching and node embedding’. In: International conference on machine learning . PMLR. 2019, pp. 6932– 6941.

[Zec+14] Sabrina Zechel et al. ‘Topographical transcriptome mapping of the mouse medial ganglionic eminence by spatially resolved RNA-seq’. In: Genome biology 15 (2014), pp. 1– 12.

- [Zha+23] Zhengxin Zhang et al. ‘Duality and Sample Complexity for the Gromov-Wasserstein Distance’. In: NeurIPS 2023 Workshop Optimal Transport and Machine Learning . 2023.
- [Zha+24] Zhengxin Zhang et al. ‘Gromov–Wasserstein distances: Entropic regularization, duality and sample complexity’. In: The Annals of Statistics 52.4 (2024), pp. 1616–1645.


