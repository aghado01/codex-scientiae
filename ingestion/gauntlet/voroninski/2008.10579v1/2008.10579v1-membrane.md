# Compressive Phase Retrieval: Optimal Sample Complexity with Deep Generative Priors

* †‡

Paul Hand, Oscar Leong, and Vladislav Voroninski

August 25, 2020

Abstract Advances in compressive sensing provided reconstruction algorithms of sparse signals from linear measurements with optimal sample complexity, but natural extensions of this methodology to nonlinear inverse problems have been met with potentially fundamental sample complexity bottlenecks. In particular, tractable algorithms for compressive phase retrieval with sparsity priors have not been able to achieve optimal sample complexity. This has created an open problem in compressive phase retrieval: under generic, phaseless linear measurements, are there tractable reconstruction algorithms that succeed with optimal sample complexity? Meanwhile, progress in machine learning has led to the development of new data-driven signal priors in the form of generative models, which can outperform sparsity priors with significantly fewer measurements. In this work, we resolve the open problem in compressive phase retrieval and demonstrate that generative priors can lead to a fundamental advance by permitting optimal sample complexity by a tractable algorithm in this challenging nonlinear inverse problem. We additionally provide empirics showing that exploiting generative priors in phase retrieval can significantly outperform sparsity priors. These results provide support for generative priors as a new paradigm for signal recovery in a variety of contexts, both empirically and theoretically. The strengths of this paradigm are that (1) generative priors can represent some classes of natural signals more concisely than sparsity priors, (2) generative priors allow for direct optimization over the natural signal manifold, which is intractable under sparsity priors, and (3) the resulting nonconvex optimization problems with generative priors can admit benign optimization landscapes at optimal sample complexity, perhaps surprisingly, even in cases of nonlinear measurements.

## Contents

- [Introduction](#introduction)
  - [Related Work](#related-work)
  - [Compressive Phase Retrieval with Generative Priors](#compressive-phase-retrieval-with-generative-priors)
  - [Deterministic](#deterministic)
    - [and](#and)
  - [Probabilistic](#probabilistic)
    - [Models](#models)
    - [for](#for)
  - [Generative](#generative)
    - [Priors](#priors)
  - [Algorithm](#algorithm)
    - [Main](#main)
    - [Results](#results)
  - [Experiments on MNIST](#experiments-on-mnist)
  - [Sketch of Proof for Theorem 3](#sketch-of-proof-for-theorem-3)
  - [Preliminaries for Proofs](#preliminaries-for-proofs)
  - [Supplementary Results](#supplementary-results)
- [Gaussian Matrices Satisfy the RRCP](#gaussian-matrices-satisfy-the-rrcp)
  - [Concentration Over a Fixed Subspace](#concentration-over-a-fixed-subspace)
  - [Uniform Concentration Over a Union of Subspaces](#uniform-concentration-over-a-union-of-subspaces)
  - [Application to Range of Generative Model](#application-to-range-of-generative-model)
  - [RRCP Supplementary Results](#rrcp-supplementary-results)
  - [Acknowledgements](#acknowledgements)
- [References](references/2008.10579v1.md)

## Introduction

The study of inverse problems pervades virtually all of the natural sciences including biological and astronomical imaging, X-ray crystallography, oil exploration, and shape optimization and reconstruction. An object of interest is observed via some forward mapping process, and the task is to recover the object, often subject to ill-posedness and noise. In order to increase fidelity of the estimate or decrease the number of required measurements, one can enforce structural assumptions or priors on the signal, a practice dating as far back as Tikhonov regularization [59] and the Nyquist sampling theorem [47]. A canonical example of an ill-posed inverse problem in the field of imaging is compressive sensing (CS), in which one aims to recover a signal from undersampled linear measurements. By exploiting the sparsity of natural images in the wavelet domain as a structural prior,

$$
*
$$

Department of Mathematics and Khoury College of Computer and Information Science, Northeastern University, Boston, MA

$$
†
$$

Department of Computational and Applied Mathematics, Rice University, Houston, TX

$$
‡
$$

Helm.ai, Menlo Park, CA

CS has led to a number of practical developments across the imaging sciences, such as speeding up some forms of MRI imaging by an order of magnitude [21]. In terms of theory, advances in CS have provided reconstruction algorithms using sparsity priors with information theoretically optimal sample complexity [13, 20]. A seminal result in the field

$$
m\times n
$$

states that if given $m < n$ undersampled linear measurements = where $A ∈ R$ has i.i.d.

$$
b_{* }Ay_{* }
n
$$

Gaussian entries and $∈ R$ is an $s$-sparse signal, recovery is guaranteed with high probability

$$
y_{* }
$$

when $m$ = $O$($s$log $n$) by solving the following convex program:

min s.t. $Ay$ =

$$
\|y\|_{1}b_{* }.
n
y\in R
$$

The success of compressive sensing has popularized the notion of signal sparsity throughout the imaging sciences, resulting in sparsity becoming a common choice as a structural prior. Sparsity-based priors when applied to nonlinear inverse problems such as phase retrieval have been met with potentially fundamental sample complexity bottlenecks. In phase retrieval, a sig-

$$
nn2
$$

nal $∈ R$ or $C$ is to be estimated from observations , $i$ = 1$,$2$,...m$. Compres-

$$
y_{* }|〈a_{i},y_{* }〉|
$$

sive phase retrieval considers the case $m < n$, which requires structural priors to enable recovery. While an $s$-sparse signal is information theoretically recoverable from $O$($s$log $n$) generic phaseless measurements, compressive phase retrieval algorithms have not achieved sample complexity below 2 2$O$($s$ log $n$) [36]. In fact, convex algorithms such as PhaseLift [14, 11], provably fail below $O$($s$ log $n$) measurements under natural extensions to incorporate sparsity [44, 49]. This has created an open problem in compressive phase retrieval to find a computationally efficient algorithm to reconstruct signals from generic, phaseless linear measurements with optimal sample complexity with respect to the signal’s intrinsic dimensionality. Furthermore, there is evidence to support that these sample complexity limitations may be fundamental for sparse phase retrieval. In the closely related sparse PCA problem, a reduction from planted clique was found, and it is widely conjectured to be NPhard [8, 6]. These observations open the question of whether other signal priors may successfully achieve sample-optimal reconstruction algorithms. Simultaneously, there has been tremendous progress on priors in the form of generative models given by a deep neural network, which in some cases significantly outperforms sparsity priors at compressive sensing. These generative models, such as Generative Adversarial Networks [29] and

$$
k
$$

Variational Autoencoders [41], learn an explicit mapping from a low-dimensional latent space $R$

$$
n
$$

to an approximation of the natural image manifold in $R$ and can be trained on datasets of various natural signal classes to create realistic, yet synthetic samples of human faces [39], MRIs [55], cells [48], human fingerprints [46], and more. Enforcing a generative prior in CS tasks by directly optimizing over the latent space has been shown to outperform sparsity-based methods such as Lasso by 5-10x fewer measurements [9] in some cases. Moreover, while the optimization problem posed over latent space is non-convex, [33] showed that when the number of measurements $m$ is proportional to $k$ up to log factors, the empirical risk minimization problem under a suitable random generator model exhibits favorable global geometry in the sense that there are no spurious local minima away from small neighborhoods of the true solution and a negative multiple thereof. The above empirical and theoretical evidence indicates that generative neural networks can potentially succeed as structural priors in nonlinear inverse problems where previous methods exploiting sparsity have thus far been met with likely fundamental bottlenecks. In this work, we resolve the open problem in compressive phase retrieval by presenting a computationally efficient algorithm that achieves optimal sample complexity with generic measurements under a generative prior. In particular, we consider a deep generative prior for compressive phase retrieval by supposing that the desired signal lives in the range of a feed-forward neural network with ReLU activation functions and latent code dimensionality $k$. We establish the sufficiency of

two deterministic conditions on the weights of the generative model and the measurement matrix to guarantee that the signal can be recovered by a subgradient descent algorithm. Moreover, we show that these conditions are satisfied with high probability for Gaussian weights and generic Gaussian measurements as soon as $m$ is proportional to $k$, up to log factors, which is information theoretically optimal in $k$. In addition to our theoretical results, we empirically establish that exploiting generative models in phase retrieval tasks can significantly outperform sparsity-based methods. Subsequent to the publication of preliminary versions of the results of this paper [31], generative priors have also been shown to break through sample complexity barriers in PCA. In particular, all known algorithms to achieve optimal statistical sample complexity in sparse PCA are computationally intractable and all known polynomial time algorithms exhibit a sub-optimal quadratic sample complexity on the sparsity of the true signal [42, 19]. Gaps of this nature have also been observed in a number of related problems [18, 52]. However, with respect to PCA, recent work in both the asymptotic [2] and non-asymptotic regimes [16] have shown that the low rank matrix recovery problem with generative priors does not exhibit a computational-to-statistical gap, offering further evidence of the benefit of generative priors in inverse problems. The results in the present work provide empirical and theoretical support to the notion that deep generative priors offer a new paradigm for signal recovery that offers fundamental advances. In this paradigm, a model of a natural signal class is learned from data in the form of a generative model. The generative model directly parameterizes a low-dimensional signal manifold, and recovering a signal subject to noisy measurements can be posed as a direct optimization problem whose search space is restricted to the range of the generative model. This paradigm has several strengths in comparison to sparsity priors. First, generative models may provide better compression of natural signals than sparsity priors. Precisely, the dimensionality of the manifold modeling the natural signal class under a generative prior may be lower than the sparsity level of the same signals. Second, generative priors allow for direct optimization over the natural signal manifold. In contrast, sparsity priors give rise to combinatorial optimization problems which can not be directly solved. Tractable convex relaxations have not been successful in important nonlinear settings. Third, the non-convex optimization problems under generative priors can admit benign optimization landscapes at optimal sample complexity even in the case of nonlinear measurements. This fundamental advance has so far not been realized by sparsity priors.

### Related Work

Phase Retrieval: Some of the earliest methods to solve phase retrieval tasks are the non-convex alternating minimization Gerchberg-Saxton [26] and Fienup [25] algorithms. Recently, a variety of methods have been introduced that enjoy theoretical guarantees. Convex methods, such as the seminal lifting-based approach PhaseLift [14], can achieve optimal sample complexity for unstructured signals [11]. Further recovery guarantees have been extended to non-convex formulations such as Wirtinger Flow [12, 58, 56] and its non-smooth variant Amplitude Flow [23, 56, 67]. Other approaches include Phasemax [27, 4], Phasecut [63], AltMinPhase [38], and alternating projection methods [62]. Since the success of exploiting sparsity in linear compressed sensing, many works have attempted to leverage similar techniques to solve the phase retrieval problem in the compressive setting $m < n$. When the $n$-dimensional signal is $s$-sparse, the information theoretic lower bound of $m$ = $O$($s$log $n$) measurements was shown to be required for the injectivity of phaseless Gaussian measurements [61]. However, attempts at achieving this optimal sample complexity via a polynomial time algorithm have proven quite difficult and, in some cases, impossible. For example, the natural $`_{1}$-penalized 2variant of Phaselift was shown to be able to recover an $s$-sparse signal with $O$($s$ log $n$) generic

measurements, but this bound was also proven to be tight [44, 49]. Moreover, there are a number of results that show, if one were able to construct a sufficiently accurate initializer of the true solution, then recovery from $O$($s$log $n$) Gaussian measurements is possible by a variety of methods [32, 64, 56]. 2Known initialization schemes to accomplish this, however, require $O$($s$ log $n$) measurements [10]. For a more complete discussion of prior methodologies for phase retrieval, we refer the reader to [24]. Some existing works in compressive phase retrieval establish optimal sample complexity recovery guarantees under non-generic measurements [36]. For example, [3] showed that assuming the mea-

$$
n
$$

surement vectors were chosen from an incoherent subspace, then recovery is possible with $O$($s$log )

$$
s
$$

measurements. Also, using the notion of polarization, [5] showed that $O$($s$log $n$) measurements also suffices for recovery when the measurement vectors have an associated graph with sufficient connectivity properties. However, these results would be difficult to generalize to the experimental setting as their measurement design architectures are often unrealistic, with generic measurements offering a closer model to the goal of Fourier diffraction measurements.

Signal recovery with generative priors: In [9], the authors studied enforcing a generative prior in the linear compressive sensing regime. In particular, given $m$ linear measurements

$$
Ay_{* }
n
$$

where $∈ R$ , the authors modelled natural signals as being in the range of a trained generative

$$
y_{* }
kn
$$

model $G$ : $R → R$ where $k  n$. To solve the inverse problem, they proposed to find a latent

$$
k
$$

code $∈ R$ such that $≈$ by solving the following least squares objective

$$
x_{* }G(x_{* })y_{* }
$$

$$
\| \|
$$

$$
\| \|
$$

min

$$
- .
\| AG(x)
Ay_{* }\| k
$$

$$
x\in R
$$

They provided empirical evidence showing that 5-10x fewer measurements were needed to achieve comparable reconstruction errors, compared to standard sparsity-based approaches such as Lasso in some parameter regimes. Based on the success of generative priors in compressive sensing, a number of followup works have considered a similar setup in a variety of inverse problems, ranging from compressed sensing [32, 35, 53, 28, 57], denoising [34], phase retrieval [31, 54], low-rank matrix recovery [16, 2], one-bit compressive sensing [51, 45], blind deconvolution [54, 30], and more. This framework, in the case of compressive sensing, enjoys multiple theoretical analyses. A subset of the authors in [32] presented the first global landscape analysis of the empirical risk minimization problem and showed that, in fact, when the network is sufficiently expansive with Gaussian weights and the number of measurements is proportional to $k$, there exists a descent direction everywhere outside of potentially two small neighborhoods of the minimizer and true solution. Followup work later established convergence guarantees of first order methods in compressed sensing [35] and denoising [34] under similar statistical assumptions on the generator. In the present work, we consider precisely the same random model in the context of compressive phase retrieval.

### Compressive Phase Retrieval with Generative Priors

The compressive phase retrieval problem is as follows. We consider the real-valued version out of

$$
n
$$

simplicity. Consider a signal $∈ R$ . Given $m$ phaseless linear measurements of the form

$$
y_{* }
$$

= + $η$

$$
b_{* }|Ay_{* }|
$$

$$
m\times nm
$$

where $m < n$, $A ∈ R$ is a known linear operator, and $η ∈ R$ denotes measurement noise, the goal is to recover from knowledge of and $A$. As $m < n$, additional structure must be exploited

$$
y_{* }b_{* }
$$

to accurately estimate In this work, we assume that belongs in or near the range of a trained

$$
y_{* }.y_{* }
kn
$$

generative model $G$ : $R → R$ . That is, $≈$ for some latent code In order to recover

$$
y_{* }G(x_{* })x_{* }.
$$

an estimate of a signal it suffices to recover and then compute

$$
y_{* },x_{* }G(x_{* }).
$$

the following nonlinear least squares problem:

We propose to solve

$$
\| \|
$$

$$
\| \| .
$$

min $f$($x$) := $−$

$$
\| |AG(x)|
b_{* }\| k
$$

$$
x\in R
$$

This formulation attempts to find the signal in the range of the generative model $G$ that is most consistent with provided measurements in a particular sense. It is motivated both by the nonconvex generative modeling formulation for compressed sensing in [9] with an Amplitude Flow [67] perspective from phase retrieval and was originally introduced by the present authors in [31]. As the underlying optimization problem is posed over an explicitly parameterized $k$-dimensional manifold where $k  n$, compressive phase retrieval may be possible from $m$ = Ω($k$) $ n$ measurements. In this paper, we prove that (3) can be solved with sample complexity proportional to $k$, under an appropriate model for $G$ and a generic measurement model. This theoretical result is in stark contrast to algorithms for compressive phase retrieval based on sparsity priors, where no known tractable algorithm achieves information theoretically optimal sample complexity under a generic measurement model. This result extends the work of [34, 35] which established similar algorithmic guarantees for recovery with generative priors in the linear measurement regimes of denoising and compressed sensing. Additionally, we provide experimental results that (3) can outperform sparsitybased compressive phase retrieval algorithms in the presence of a trained $G$ from standardly available datasets. The formulation assumes that the generative model $G$ is already known. In practice, it typically is a neural network whose parameters (weights) are learned from a large collection of training images belonging to a particular natural signal class. The field of generative modeling has demonstrated multiple types of neural networks which can be effectively trained (e.g. Variational Autoencoders [41] and Generative Adversarial Networks [29]). The dimensionality $k$ of the latent codes is fixed at training time and its value is selected in order to balance multiple effects; for example, the range of $G$ should be large enough to approximately include all of the desired signal class, and the image representations should be as concise as possible. A particular image of interest may not be exactly in the range of a trained model $G$ because the model has representation error, but this error is expected to become smaller as techniques for training generative models improve.

### Deterministic

#### and

### Probabilistic

#### Models

#### for

### Generative

#### Priors

In order to establish recovery guarantees for phase retrieval with generative priors, we will assume a neural network architecture and a model for the weights of the network once trained. Our intention is to analyze a model which is realistic enough to describe trained nets, yet tractable enough to permit rigorous analysis of sample complexity for a convergent optimization algorithm. To achieve both of these objectives we consider the following models. We assume that the generative model

$$
kn
$$

$G$ : $R → R$ is given by a $d$-layer feedforward neural network with ReLU activation functions and no bias terms. Specifically, we assume that

$G$($x$) = $...$relu($W_{2}$relu($W_{1}x$))$...$)relu($W_{d}$ (4)

$$
n_{i}\times n_{i- 1}
$$

where relu($x$) := max($x,$0) acts entrywise and each $∈ R$ for $i ∈$ [$d$] with $k$ = $< <$

$$
W_{i}n_{0}n_{1}
$$

$< ··· <$ = $n.$ Each matrix corresponds to the neural network weights of the $i$-th layer,

$$
n_{2}W_{i}
n_{d}
$$

and the $j$-th row of are the weights of the $j$-th neuron in the $i$-th layer.

$$
W_{i}
$$

We will assume an expansive-Gaussian probabilistic model for the weights of $G$. That is,

$$
n_{i}
$$

increases sufficiently with $i$, and the weights within each layer are i.i.d. Gaussians. This model was introduced by a subset of the authors in [33]. We additionally assume a Gaussian model of the

measurement matrix $A$. The justification for these assumptions is as follows. The Gaussianicity of $A$ ensures that measurements are suitably generic, and, indeed, achieving optimal sample complexity in sparse phase retrieval has not been attained for this measurement model. Regarding the expansivity assumption, we note that generative models with low dimensional latent spaces are inherently expansive when considered as a whole. In a sense, the network and each layer therein could be viewed as adding redundancy to a more compact representation, though in practice some successful network architectures do not have strict layerwise expansivity. Regarding the Gaussianicity model of neural network weights, it has been shown that neural networks, such as AlexNet, trained on real data have resulting weights with statistics similar to Gaussians [1]. We emphasize that the use of generative models as priors in regularizing inverse problems is nascent, and we use this model because it balances mathematical tractability with authenticity toward applications. In order to establish a recovery guarantee for this random model, we establish it for models satisfying deterministic conditions on $G$ and $A$. Then we show that an appropriate expansiveGaussian model satisfies these deterministic conditions with high probability. The first deterministic condition we consider roughly states that the neural network weights are approximately distributed

$$
n\times kk
$$

uniformly on a sphere of a particular radius. For $W ∈ R$ and $x ∈ R$ , define := diag($Wx >$

$$
W_{+,x}
$$

0)$W$ where the $i$-th diagonal entry of diag($v >$ 0) is 1 if $>$ 0 and 0 otherwise. Note that

$$
v_{i}
$$

= relu($Wx$). The condition is stated as follows and was introduced in [33]:

$$
W_{+,x}x
$$

$$
n\times k
$$

Definition 1 (Weight Distribution Condition). We say that $W ∈ R$ satisfies the Weight Dis-

$$
k
$$

tribution Condition (WDC) with constant $ε >$ 0 if for all nonzero $x,y ∈ R$ ,

$$
\| \|
$$

$$
\| \|
$$

$W − 6 ε$

$$
W_{+,y}Q_{x,y}
+,x
$$

where

$$
\pi - \theta _{x,y}
sin\theta _{x,y}
$$

$$
Q_{x,y}
I_{k}M_{ˆx↔ˆy}.
$$

2$π$ 2$π$ 1Here = $∠$($x,y$), ˆ$x$ = $x/\|x\|$, ˆ$y$ = $y/\|y\|$, is the $k × k$ identity matrix, and

$$
\theta _{x,y}
I_{k}M_{ˆx↔ˆy}
\perp
$$

matrix that sends ˆ$x 7→$ ˆ$y$, ˆ$y 7→$ ˆ$x$, and $z 7→$ 0 for any $z ∈$ span(${x,y}$) $.$

is the

The second deterministic condition provides an RIP-like property for the measurement matrix

$$
m\times nn
$$

$A$ when acting on pairs of secant directions within the range of $G$. For $A ∈ R$ and $z ∈ R$ , define := diag(sgn($Az$))$A$ where sgn acts entrywise, and sgn(0) = 0. Note that $z$ = $|Az|$. The

$$
A_{z}A_{z}
$$

condition is stated as follows and was introduced in a conference version of this work [31]:

Definition 2 (Range Restricted Concentration Property). We say that $A$ satisfies the Range Restricted Concentration Property (RRCP) with respect to $G$ with constant $ε >$ 0 if for all

$$
k
\in R:
x,y,x_{1},x_{2},x_{3},x_{4}
$$

$$
|〈(A
A- \Phi - -
$$

)($G$($x_{1}$) $G$($x_{2}$))$,G$($x_{3}$) $G$($x_{4}$)$〉|$$G$($y$) $G$($x$)$,G$($y$)$G$($x$)

$$
6- -
$$

$Lε\|G$($x_{1}$) $G$($x_{2}$)$\|\|G$($x_{3}$) $G$($x_{4}$)$\|$

where

$$
{\pi - 2\theta _{z,w}2sin\theta _{z,w}
$$

$$
I_{n}
M_{ˆz↔ˆw}
\pi \pi
$$

$$
\Phi _{z,w}
$$

if $z 6$= 0$,w 6$= 0$,$ otherwise.

Here, $L$ is a universal constant and can be taken to be 33. 1A formula for this matrix is as follows: consider a rotation matrix $R$ that sends ˆ$x 7→$ and ˆ$y 7→$ +$e$1 cos$θx,ye$1

$$


$$

$$
cos\theta x,ysin\theta x,y
$$

$$


$$

0where = $∠$($x,y$). Then = $R$ $R$ where is the $k −$ 2 $× k −$ 2

$$
sin\theta x,y- cos\theta x,y
sin\theta x,ye2\theta x,y
Mˆx↔ˆy
$$

0$k−$2 0 0 0$k−$2 T Tmatrix of zeros. Note that if = 0 or $π$, = ˆ$x$ˆ$x$ or $−$ˆ$x$ˆ$x$ , respectively.

$$
\theta x,y
Mˆx↔ˆy
$$

### Algorithm

We provide a subgradient algorithm for optimizing (3) under noisy measurements. We show that this algorithm converges up to the noise level when the WDC and RRCP properties are met. In order to state the algorithm, we need some notation. For a locally Lipschitz function $f$ : $X → R$ from a Hilbert space $X$ to $R$, the Clarke generalized directional derivative of $f$ at $x ∈ X$ in the direction $u$, is defined by

$$
f(y+tu)- f(y)
o
$$

$f$ ($x$;$u$) := limsup

$$
.
t
y\to x,t\downarrow 0
$$

Then the generalized subdifferential of $f$ at $x$ is defined by

$$
ko
$$

$∂f$($x$) = ${v ∈ R$ : $〈v,u〉 6 f$ ($x$;$u$)$,$ $∀u ∈ X}.$

Any element $∈ ∂f$($x$) is called a subgradient of $f$ at $x$.

$$
v_{x}{\nabla f(x)}.
$$

When $f$ is differentiable at $x$, $∂f$($x$) =

Algorithm 1 Deep Phase Retrieval (DPR) Subgradient method Require: Weights measurement matrix $A$, measurements = + $η$, & step size $α >$ 0

$$
W_{i},b_{* }|Ay_{* }|
k
$$

1: Choose an arbitrary initial point $∈ R \{$0$}$$x_{0}$ 2: for $t$ = 0$,$1$,$2$,...$ do 3: if $<$ then

$$
f(- x_{t})f(x_{t})
$$

4: $←$

$$
¯x_{t}- x_{t};
$$

5: else 6: $←$

$$
¯x_{t}x_{t};
$$

7: end if 8: Compute

$$
\in
$$

$∂f$(¯$x_{t}$);

$$
v_{¯x_{t}}
$$

9: = $−$ ;

$$
x_{t+1}¯x_{t}
\alpha v_{¯x_{t}}
$$

10: end for

We now introduce a subgradient descent scheme, given by Algorithm 1, whose intuition is as follows. In expectation, the optimization landscape is characterized by Figure 1. There exists two critical points away from the origin: the true minimizer and a negative multiple thereof. Moreover, the value of the objective function is higher near the negative multiple than near the global minimizer. At each iterate, we check the objective function value at the current latent code and its negative, choosing the point with smaller objective function value as our new iterate; see Steps 3–7. We then perform subgradient descent.

#### Main

#### Results

In this section, we outline our main results in both the probabilistic and deterministic settings. In particular, in Theorem 3 we show that if the weights of $G$ satisfy the WDC and the measurement matrix $A$ satisfies the RRCP, then the iterates of Algorithm 1 converge to the true solution up to the noise level in the measurements. Then, in Corollary 4 we show that the same conclusion holds when and $A$ are Gaussian with high probability as soon as $m$ = )).$W_{i}$ Ω($dk$ log($n_{1}n_{2}$

$$
...n_{d}
$$

We consider the possibly noisy measurements (2) and assume that the signal is in the range

$$
y_{* }
$$

of $G$ with latent code that is, = The following Theorem states that if the two

$$
x_{* };y_{* }G(x_{* }).
$$

deterministic conditions are satisfied with a sufficiently small parameter $ε$ and the noise is sufficiently small, then the iterates of Algorithm 1 will converge to up to the noise level.

$$
x_{* }
$$

*T 2 Figure 1: The landscape of (3) where = with = [1$,$ 0] $∈ R$ , $G$ is a 1-layer network, $y_{∗}G(x_{∗})x_{∗}$ and the two determinstic conditions are satisfied with $ε$ = 0. The objective function’s explicit $()$ ) ) 1 2 2 $π−2g(θ_{x,x∗}2sing(θ_{x,x∗}$ form is given by $F$($x$) := ($\|x\|$ + ) $−$ + where $\|x_{∗}\|x_{∗}〉\|x_{∗}\|\|x\|$ $〈x,Q_{x,x_{∗}}$ $4ππ$ = $g$ is defined in (10), is defined in (5) and whose $d$-layer form is given by $∠(x,x_{∗}),$ $θ_{x,x_{∗}}$ $Q_{x,x_{∗}}$ equation (62). We note that the idealized loss has only three critical points: the global minimizer a negative multiple thereof for some $∈$ (0$,$1), and the origin. $x_{∗},x_{∗}$ $−ρ_{d}ρ_{d}$*

1 . SupposeTheorem 3 (Deterministic Convergence Guarantee). Let $d >$ 2 and fix 0 $< ε <$ $c_{1}$ 102

$$
d
\|x_{* }\|
$$

the noise satisfies $\|η\| 6$ . Suppose each of $G$ satisfies the WDC with constant $ε$, and

$$
c_{2}W_{i}
d/2
$$

482 $d$ suppose $A$ satisfies the RRCP with respect to $G$ with constant $ε$. Then the iterates generated

$$
{x_{t}}_{t>0}
d
$$

2by Algorithm 1 with step size $α 6$ obey the following:$c_{3}$ 2

$$
d
$$

2$d$)$f$($x∗$)(21. there exists an $N ∈ N$ satisfying $N 6$ such that$C_{4}$ 6 2

$$
d
\alpha \varepsilon \|x_{* }\|
$$

$$
\sqrt
$$

12 9 $d/$2

$$
- 6+2\|\eta \|;
x_{* }\|C_{5}d\varepsilon \|x_{* }\|C_{6}d
\|x_{N}
$$

2. for all $t > N$, we have

$$
d/2
$$

$$
t+1- N
\|\eta \|,and
- 6\tau - +\vartheta \|x_{t+1}x_{* }\|x_{* }\|
\|x_{N}
$$

$$
d
$$

1$.$2 1$.$2

$$
t+1- N
- 6
\tau - +\vartheta \|\eta \|
\|G(x_{t+1})G(x_{* })\|
x_{* }\|
\|x_{N}
$$

$$
d/2
d
$$

$$
\alpha
$$

7where $τ$ := 1 $−$

$$
d
$$

2$c_{3}$$∈$ (0$,$1) and $ϑ$ := .

$$
1- \tau
$$

Here and are positive universal constants.

$$
c_{1},c_{2},c_{3},C_{4},C_{5},C_{6}
$$

This result asserts that the iterates of Algorithm 1 will eventually be in a small neighborhood of $−$1the true solution whose size depends on $ε$ and $\|η\|$ after $N$ = $O$($ε$ ) iterations. Furthermore, once in this neighborhood, the iterates will continue to converge linearly to the true solution up to the

$$
d
$$

noise level. If no noise is present, then the true signal will be recovered. Note that the 2 factors in the theorem are an artifact of the problem scaling. Roughly, the weights have spectral norm

$$
W_{i}
$$

approximately 1, and subsequent application of a ReLU will effectively zero out roughly half of the rows of The resulting rows of will have spectral norm of roughly 1$/$2. Hence $G$($x$) scales

$$
W_{i}.W_{i}
- d/2- d2d
$$

like 2 $\|x\|$, $f$($x$) scales like 2 $\|x\|$ , and any subgradient scales like 2 . We also assume the

$$
v_{x}
$$

![figure: p8 region 5](2008.10579v1-membrane/imageFile0.png)

$$
- d/2
$$

noise scales like 2 to ensure it is on the order of the measurements. Doubling the variance of each entry of would eliminate these factors, but we consider the unscaled version because it is

$$
W_{i}
$$

more convenient in the analysis. We now address the expansive-Gaussian model. We appeal to a result that shows that expansive (tall) Gaussian matrices satisfy the WDC with high probability.

$$
n\times k
$$

Lemma 1 (Lemma 11 in [33]). Fix 0 $< ε <$ 1 and suppose $W ∈ R$ has i.i.d. $N$(0$,$1$/n$) entries. Then if $n >$ then with probability at least 1 $− W$ satisfies the WDC with$C_{ε}k$ log $k$, 8$n$exp($−γ_{ε}k$), $−$1 $−$1constant $ε$. Here and $γ$ depend polynomially on $ε$ .

$$
C_{\varepsilon }
\varepsilon
$$

In this work, we establish that Gaussian matrices $A$ satisfies the RRCP with respect to an expansive-Gaussian $G$ with high probability if they are sufficiently tall. This result is proven in Section 3:

$$
m\times n
$$

Lemma 2 (RRCP). Fix 0 $< ε <$ 1 and suppose $A ∈ R$ has i.i.d. $N$(0$,$1$/m$) entries. Let $G$

$$
n_{i}\times n_{i- 1}
$$

be a generative model of the form (4) where each $∈ R$ has i.i.d. entries. If

$$
W_{i}
N(0,1/n_{i})
$$

4$k$$m >$ ), then with probability 1 $− γm A$ satisfies the RRCP with$C_{ε}kd$log($n_{1}n_{2}$ exp($−c_{ε}m$),

$$
...n_{d}
$$

$−$1respect to $G$ with constant $ε$. Here $γ$ is a universal constant and and $c$ depend polynomially

$$
C_{\varepsilon }
\varepsilon
$$

$−$1on $ε$ .

Hence for Gaussian measurements and weight ensembles, we can combine Lemma 1 and Lemma 2 with Theorem 3 to obtain the following Corollary:

1Corollary 4 (Probabilistic Convergence Guarantee). Fix 0 $< ε <$ and suppose the noise$c_{1}$ 102

$$
d
\|x_{* }\|
$$

satisfies $\|η\| 6$ for some universal constants and Suppose $G$ is such that $∈$

$$
c_{2}c_{1}c_{2}.W_{i}
d/2
$$

482 $d$

$$
n_{i}\times n_{i- 1}
m\times n_{d}
$$

has i.i.d. entries for $i$ = 1$,...,d$. Suppose that $A ∈ R$ has i.i.d. $N$(0$,$1$/m$)

$$
R
N(0,1/n_{i})
$$

entries independent from Then if $m >$ ) and $>$ for

$$
{W_{i}}.C_{\varepsilon }dklog(n_{1}n_{2}n_{i}C_{\varepsilon }n_{i- 1}logn_{i- 1}
...n_{d}
\sum d
$$

4$k$$− γm$ the same$i$ = 1$,...,d$, then with probability at least 1 $−$ $γn_{i}$ exp($−c_{ε}n_{i}_{−}_{1}$) exp($−c_{ε}m$), $i$=1 $−$1conclusion as Theorem 3 holds. Here depends polynomially on $ε$ , depends on $ε$, and $γ$ is a

$$
C_{\varepsilon }c_{\varepsilon }
$$

universal constant.

To the author’s knowledge, this is the first result establishing provable signal recovery with a computationally efficient algorithm for undersampled generic phaseless linear measurements with optimal sample complexity. This sample complexity in our result scales with $k$, which can not be improved. We made no attempt to obtain tight bounds on $d$, except to ensure that all dependences

$$
d
$$

on $d$ are polynomial. We remind the reader that any 2 terms that appear are due to the problem scaling. We further note that subsequent developments since the original release of [31] relaxed the logarithmic growth factor on the sizes of each layer of the generative model [17]. Lastly, we note that this result for compressive phase retrieval under optimal sample complexity implies recovery for linear compressive sensing under optimal sample complexity. As such, this work subsumes the work of a subset of the authors in [33]. This generalization of compressed sensing to compressive phase retrieval is conspicuously absent for structural priors based on sparsity, as the best known computationally efficient algorithms for sparsity priors require sample complexity that is quadratic in the sparsity level.

### Experiments on MNIST

In this section, we compare the generative modeling approach for compressive phase retrieval with three sparse phase retrieval algorithms: the sparse truncated amplitude flow algorithm (SPARTA) [64], Thresholded Wirtinger Flow (TWF) [10], and the alternating minimization algorithm CoPRAM [37]. For the generative modeling approach, we used a modified version of Algorithm 1 as

we empirically found the negation step (Steps 3-4) only occurred at the first iterate. Hence we ran two gradient descents, one starting from a random initial iterate and another starting from$x_{0}$ its negation We report results for the most successful reconstruction. Gradient descent was$−x_{0}$. performed using the Adam optimizer [40]. For the remainder of this section, we will refer to the generative modeling approach as DPR.

$$
m\times n
$$

In each task, the goal is to recover an image given where $A ∈ R$ has i.i.d. $N$(0$,$1$/m$)

$$
y_{* }|Ay_{* }|
$$

entries. The images were from the MNIST dataset [43]. This dataset consists of 60$,$000 28 $×$ 28 images of handwritten digits. The generative model was a pretrained Variational Autoencoder (VAE) from [9]. The encoder network is of size 784 $−$ 500 $−$ 500 $−$ 20 while the generator network $G$ is of size 20 $−$ 500 $−$ 500 $−$ 784. The latent code space dimension is $k$ = 20. For the sparse phase retrieval methods, we performed sparse recovery in the Daubechies-4 Wavelet domain. We zero-padded the images to be of size 32 $×$ 32. The resulting images generated by our algorithm were also uniformly padded with zeros around the border to obtain 32$×$32 images. For SPARTA and CoPRAM, we ran each algorithm with sparsity parameters ranging from 2 to 212 in increments of 15, choosing the best reconstruction in terms of lowest reconstruction error. We aimed to reconstruct 10 images from the MNIST test set. We allowed 5 random restarts for each algorithm and chose the result with the least reconstruction error per pixel. We also report$`_{2}$ the Structural Similarity Index Measure (SSIM) [65] for each reconstruction. The results in Figure 2 demonstrate the success of our algorithm with very few measurements. For 200 measurements, we can achieve accurate recovery with a mean SSIM value of over 0$.$9 while other algorithms require 1000 measurements or more. In terms of reconstruction error, our algorithm exhibits recovery with 200 measurements comparable to the alternatives requiring 750 measurements or more, which is where they begin to succeed. We note that while our algorithm succeeds with fewer measurements than the other methods, our performance, as measured by per-pixel reconstruction error, saturates as the number of measurements increases since our reconstruction accuracy is ultimately bounded by the generative model’s representational error. As generative models improve, their representational errors will decrease. Nonetheless, as can be seen in the reconstructed digits in Figure 3, the recoveries are semantically correct (the correct digit is legibly recovered) even though the reconstruction error does not decay to zero. In applications, such as MRI and molecular structure estimation via X-ray crystallography, semantic error measures would be a more informative estimates of recovery performance than per-pixel error measures.

![figure: Figure 2](2008.10579v1-membrane/imageFile2.png)

*Figure 2: Each algorithm’s average reconstruction error (top) and mean images from the MNIST test set for different numbers of measurements.*

![diagram: p11 region 8](2008.10579v1-membrane/imageFile1.png)

![figure: Figure 3](2008.10579v1-membrane/imageFile3.png)

*Figure 3: Each algorithm’s reconstructed images with 100 measurements (left) and 300 measurements (right). If an image is blank, then the reconstruction error between the blank image and the original image was lower than that of the algorithm’s reconstructed image and the original image. We note that even for as few as 100 measurements, nearly all of DPR’s reconstructions are semantically correct.*

## Proof of Convergence Result in Deterministic Setting

In this section, we will formally prove Theorem 3. Section 2.1 outlines the notation we will use throughout the proofs. Section 2.2 provides a high-level sketch of our proof and outlines its central arguments while Section 2.3 discusses preliminary results that are used throughout the proofs. Then Section 2.4 presents the proof of Theorem 3 which is broken down into four central results. Finally, Section 2.5 presents supplementary results and their proofs that aid in establishing Theorem 3.

## Notation

TLet ($·$) denote the real transpose. Let [$n$] = ${$1$,...,n}$. Let $B$($x,r$) denote the closed Euclidean ball centered at $x$ with radius $r$. Let $\| · \|$ denote the norm for vectors and spectral norm for$`_{2}$

$$
nn
$$

matrices. For any non-zero $x ∈ R$ , let ˆ$x$ = $x/\|x\|$. For non-zero $x,y ∈ R$ , let = $∠$($x,y$)

$$
\theta _{x,y}
$$

Let relu($x$) := max($x,$0). Define sgn : $R → R$ to be sgn($x$) = $x/|x|$ for non-zero $x ∈ R$ and

0.08 0.07 0.06 0.05 0.04 0.03 0.02

100 200

0 50 1000 1500300 500 750 Number of measurements

DPR with VAE SPARTA CoPRAM TWF

0.6 0.5 0.4

100 200

0 50 1000 1500300 500 750 Number of measurements

DPR with VAE SPARTA CoPRAM TWF

SSIM (bottom) over

$$
n
$$

sgn(0) = 0 otherwise. Let 1($E$) be the indicator function on the event $E$. For a vector $v ∈ R$ , diag(sgn($v$)) is in the $i$-th diagonal entry and diag($v >$ 0) is 1 in the $i$-th diagonal entrysgn($v_{i}$) 1 $k$if $>$ 0 and 0 otherwise. Let Π = For any $x ∈ R$ and $i ∈$ [$d$], define

$$
v_{i}W_{i}...W_{1}.
W_{d}W_{d- 1}
i=d
$$

1:= $>$ Set := Π and := Note that

$$
W_{i,+,x}diag(W_{i- 1,+,x}...W_{2,+,x}W_{1,+,x}x0)W_{i}.\Lambda _{x}W_{i,+,x}\Lambda _{x}x.
x_{d}
i=d
$$

1we have the following string of equalities: $G$($x$) = Π = = . Let be the $n × n$

$$
W_{i,+,x}x\Lambda _{x}xI_{n}
x_{d}
i=d
k- 1k
$$

identity matrix. Let $S$ denote the unit sphere in $R$ . We write $γ$ = Ω($δ$) when $γ > Cδ$ for some positive constant $C$. Similarly, we write $γ$ = $O$($δ$) when $γ 6 Cδ$ for some positive constant

$$
- 1- k
$$

$C$. When we say that a constant depends polynomially on $ε$ , this means that it is at least $Cε$ for some positive $C$ and positive integer $k$. Positive numerical constants will be denoted using $C$ or $K$ with various subscripts. In general, numerical constants larger than 1 will be denoted by capital letters and constants smaller than 1 with lower case letters. For notational convenience, we write $a$ = $b$ + if $\|a − b\| 6 ε$ where $\| · \|$ denotes $| · |$ for scalars, norm for vectors, and spectral$O_{1}$($ε$) $`_{2}$ norm for matrices.

Original

DPR with VAE (100 m)

SPARTA (100 m)

CoPRAM (100 m)

TWF (100 m)

Original

DPR with VAE (300 m)

SPARTA (300 m)

CoPRAM (300 m)

TWF (300 m)

### Sketch of Proof for Theorem 3

$$
k
$$

Theorem 3 is proven by showing that for all $x ∈ R$ , any subgradient $∈ ∂f$($x$) is approximated

$$
v_{x}
k
$$

by $∈ R$ which has an analytical expression and that does not vanish outside of neighborhoods

$$
h_{x}
$$

of the true solution and a negative multiple for some $∈$ (0$,$1). Thus any $∈ ∂f$($x$) is

$$
x_{* }x_{* }v_{x}
- \rho _{d}\rho _{d}
$$

bounded away from zero for $x$ outside of these two neighborhoods, leading to convergence towards one of these regions. Then we ensure that the negation step of our algorithm (Steps 3-7) will update any iterate near to be in a neighborhood of Finally, we ensure convergence to

$$
x_{* }x_{* }.x_{* }
- \rho _{d}
$$

up to the noise level by showing that the objective function exhibits a convexity-like property in a neighborhood of

$$
x_{* }.
$$

To provide our sketch, we define some quantities. Define the function $g$ : [0$,$2$π$] $→ R$ by

$$
(
)
$$

($π − θ$)cos$θ$ + sin$θ$ $−$1$g$($θ$) := cos

$$
.
\pi
$$

$$
kk
$$

For any $x ∈ R \{$0$}$, let $∈ R$ be defined as

$$
h_{x}
()()
d- 1
$$

$$
\pi - \pi - 2\theta _{d,x}\theta _{i,x}
\|x_{* }\|
$$

$$
h_{x}
ˆx_{* }
d
\pi \pi
$$

2 $i$=0

$$
()())]
[(
d- 1
d- 1
\sum
$$

$$
\pi - \pi -
$$

$$
2sin\theta _{d,x}2\theta _{d,x}
sin\theta _{i,x}\theta _{i,x}
$$

+ + ˆ$x$

$$
\|x\|- \|x_{* }\|
d
\pi \pi \pi \pi
$$

2 $j$=$i$+1$i$=0 where = and = for $i ∈$ [$d$]. We further define

$$
\theta _{0,x}∠(x,x_{* })\theta _{i,x}g(\theta _{i- 1,x})
()()
d- 1
d- 1
\sum
$$

˘˘ ˘ ˘

$$
\pi - \pi - \theta _{j}
$$

sin$θ_{i}$2sin$θ_{d}$ 2$θ_{d}$ := +

$$
\rho _{d}
\pi \pi \pi \pi
$$

$j$=$i$+1$i$=0 ˘ ˘ ˘where = $π$ and = for $i ∈$ [$d$]. For a parameter $β >$ 0, define

$$
\theta _{0}\theta _{i}g(\theta _{i- 1})
$$

$$
}{
$$

$$
k
\beta .
:=x\in R\{0}:6
max(\|x\|,\|x_{* }\|)
\|h_{x}\|
S_{\beta }
d
$$

A direct analysis in Lemma 8 shows that for appropriate values of $β$, is contained in the union

$$
S_{\beta }
$$

of neighborhoods of and

$$
x_{* }x_{* }:
- \rho _{d}
$$

$$
\sqrt
$$

2 9 2 12

$$
\subset d\cup d
B(x_{* },70000\pi \beta \|x_{* }\|)x_{* },77422\pi \beta \|x_{* }\|).
S_{\beta }B(- \rho _{d}
$$

$$
\sqrt
$$

+ $−$2 9 2 12Set $S$ := $∩ d$ and $S$ := $∩ d$

$$
B(x_{* },70000\pi \beta \|x_{* }\|)x_{* },77422\pi \beta \|x_{* }\|).
S_{\beta }S_{\beta }B(- \rho _{d}
\beta \beta
$$

A sketch of our proof is outlined as follows:

$\cdot$ First, we establish that all subgradients are bounded away from zero for iterates outside of . Specifically, we show that when the WDC and RRCP are satisfied with constant $ε$, any

$$
S_{\beta }
$$

$∈$ satisfies $≈$ and is bounded away from 0 by the definition of . Thus

$$
\partial f(x_{t})
S_{\beta }
v_{x_{t}}v_{x_{t}}h_{x_{t}}h_{x_{t}}
$$

$\|$ must be bounded away from zero for points $/∈$ . See Section 2.4.1.

$$
x_{t}
S_{\beta }
\|v_{x_{t}}
$$

$\cdot$ Next, we establish convergence to . In particular, we show that the previous result implies

$$
S_{\beta }
$$

that subgradient descent at each iteration makes progress in the sense that for each non-zero

$$
/\in x_{t}
S_{\beta }
- 6- C\varepsilon
$$

$f$($x_{t}_{+1}$) $f$($x_{t}$)

0. Thus afterfor some $C >$ Section 2.4.2.

$−$1Ω($ε$ ) iterations, the iterates will eventually belong to

$$
.
S_{\beta }
$$

See

$\cdot$ Third, we show that the negation step of our algorithm ensures that the iterates converge to +. Specifically, we prove that for points $x ≈$ and $y ≈ f$($x$) $< f$($y$). Thus if an

$$
S
x_{* }x_{* },
- \rho _{d}
\beta -
$$

iterate $∈ S$ , $<$ so the negation step of our algorithm (Steps 3–7) ensures

$$
x_{t}f(- x_{t})f(x_{t})
\beta
$$

+= and $∈ S$ . See Section 2.4.3.

$$
¯x_{t}- x_{t}¯x_{t}
\beta
$$

$\cdot$ Finally, we establish convergence to up to the noise level. Specifically, we prove that once

$$
x_{* }
$$

+, a convexity-like property near implies that the iterates converge to up to thein $S$

$$
x_{* }x_{* }
\beta
$$

noise level in the measurements. See Section 2.4.4.

### Preliminaries for Proofs

We will make use of the following fact concerning the Clarke subdifferential of the objective function

$$
k
$$

$f$. Since $f$ is piecewise quadratic, Theorem 9.6 from [15] asserts that for any $x ∈ R$ , the Clarke subdifferential $∂f$($x$) can be written equivalently as

$∂f$($x$) =

$$
conv(v_{1},v_{2},...,v_{s})
$$

$$
{s
\sum
$$

$$
c_{`}v_{`}
$$

$`$=1

$$
s
\sum
$$

$$
c_{`}
$$

$`$=1

= 1 and $c_{`}$ $>$ 0 for

$$
}
`\in [s]
$$

where conv($·$) denotes the convex hull, $s$ is the number of quadratic functions adjoint to $x$, and is

$$
v_{`}
$$

the gradient of the $`$-th quadratic function of $f$ at $x$. Moreover, for each , there exists a direction

$$
v_{`}w_{`}
$$

).and a sufficiently small $>$ 0 such that $f$ is differentiable at and = lim +

$$
\nabla f(x+\delta _{`}w_{`}
\delta _{`}x+\delta _{`}w_{`}v_{`}
$$

$$
\delta _{`}
$$

## Proof of Theorem 3

We now set out to prove Theorem 3. In Sections 2.4.1 - 2.4.4, we establish four main lemmas, each of which pertain to one of the items in the sketch of our proof from Section 2.2. Theorem 3 is then proven in Section 2.4.5. Prior to beginning the proof, we state the necessary assumptions we will make:

Assumptions A. We assume the following hold for some numerical constants and$c_{1}$, $c_{2}$, $c_{3}$:

![diagram: p13 region 12](2008.10579v1-membrane/imageFile4.png)

A1. $−$1020 $< ε <$ ,

$$
c_{1}d
$$

$$
\|x_{* }\|
$$

$c_{2}$A2. the noise $η$ satsifes $\|η\| 6$ , and

$$
d/2
$$

482 $d$

$$
d
$$

2the step size $α >$ 0 satisfies $α 6$ .$c_{3}$ 2

$$
d
$$

We note that Proposition 1 shows that after a polynomial number of steps, the iterates of our algorithm stay outside of a ball of the origin. Hence we assume throughout that the norm of our iterates are bounded away from zero. This result is proven in Section 2.5.1.

2.4.1 Uniform control over subgradients

We first show that the descent direction does not vanish for points outside of . The main idea of

$$
S_{\beta }
$$

this result is that for points $x$ such that is sufficiently bounded away from zero, any $∈ ∂f$($x$)

$$
\|h_{x}\|v_{x}
$$

is also bounded away from zero. To prove this, we require the following three lemmas. The first gives a simple upper bound on the norm of our descent direction.

$$
\sqrt
$$

$$
m\times n_{d}
$$

Lemma 3. Fix $ε >$ 0 such that $Kd$ $ε 6$ 1 where $K$ is a universal constant. Suppose $A ∈ R$

$$
n_{i}\times n_{i- 1}
$$

satisfies the RRCP with respect to $G$ with constant $ε$ and $G$ is such that each $∈ R$ satisfies

$$
W_{i}
k
$$

the WDC with constant $ε$ for $i ∈$ [$d$]. Then for any $x ∈ R \{$0$}$ and $∈ ∂f$($x$),

$$
v_{x}
$$

$$
Cd2
$$

$6$ +

$$
\|\eta \|
\|v_{x}\|max(\|x\|,\|x_{* }\|)
d
d/2
$$

where $C$ is a numerical constant.

The second shows that is Lipschitz with respect to $x$ for points away from the origin.

$$
h_{x}
$$

Lemma 4. For all $x,y 6$= 0, we have that

$$
)
(()
$$

2 1 1(2$d$ + (10$π$ + 8)$d$ + 1

$$
20\pi )\|x_{* }\|
$$

$,$ +$− \| 6$ max $\|x − y\|.$

$$
\|h_{x}h_{y}
2dd
\|x\|\|y\|
$$

$π$ 2 2

In particular, if $x,y /∈$ for some $r >$ 0, then

$$
B(0,r\|x_{* }\|)
$$

$$
)
(
$$

2 12$d$ + (10$π$ + 8)$d$ + 20$π$

$$
+\|x- y\|.
- \|6
\|h_{x}h_{y}
2dd
$$

$rπ$ 2 2

$$
k
$$

The third states that for any non-zero $x ∈ R$ and any $∈ ∂f$($x$), approximates well.

$$
v_{x}h_{x}v_{x}
$$

$−$4 2

$$
m\times n_{d}
$$

Lemma 5. Fix $ε >$ 0 such that $ε < d$ (1$/$16$π$) If $A ∈ R$ satisfies the RRCP with respect to

$$
n_{i}\times n_{i- 1}
$$

$G$ with constant $ε$ and $G$ is such that each $∈ R$ satisfies the WDC with constant $ε$ for

$$
W_{i}
$$

$i ∈$ [$d$], then for any $x 6$= 0 and $∈ ∂f$($x$)

$$
v_{x}
$$

$$
\sqrt
$$

$$
Kd\varepsilon 2
- 6+
\|\eta \|
\|v_{x}h_{x}\|max(\|x\|,\|x_{* }\|)
d
d/2
$$

where $K$ is a universal constant.

Each of these results are proven in Section 2.5.2. result of this section.

We are now ready to state and prove the main

![diagram: p14 region 13](2008.10579v1-membrane/imageFile5.png)

$$
\sqrt
$$

3 $d/$2Lemma 6. Suppose Assumptions A1-A3 are satisfied and set $β$ := 4$Kd ε$+11$\|η\|$2 where

$$
/\|x_{* }\|
m\times n
$$

$K$ is a numerical constant. Let $A ∈ R$ satisfy the RRCP with respect to $G$ with constant $ε$. Let

$$
n_{i}\times n_{i- 1}
$$

$G$ be such that $∈ R$ satisfy the WDC with constant $ε$ for all $i ∈$ [$d$]. Suppose that $x /∈$

$$
W_{i}
S_{\beta }
$$

and $x /∈$ for some numerical constant Then for any $∈ ∂f$($x$), we have

$$
B(0,c_{0}\|x_{* }\|)c_{0}.v_{x}
\sqrt
$$

$$
Kd\varepsilon
$$

1 $>$ (13)

$$
\|v_{x}\|\|x_{* }\|.
d
$$

Moreover, we have that for any $λ ∈$ [0$,$1],

$$
- 6
$$

$$
\|v_{˜x}v_{x}\|
\|v_{x}\|
$$

where ˜$x$ = $x − ∈ ∂f$($x$) and $∈ ∂f$(˜$x$).

$$
\lambda \alpha v_{x},v_{x}v_{˜x}
$$

Proof of Lemma 6. By Lemma 4, we have that is Lipschitz for all $x /∈$ i.e. there

$$
h_{x}B(0,c_{0}\|x_{* }\|),
$$

such that for any $x,y /∈$exists a numerical constant

$$
B(0,c_{0}\|x_{* }\|)
L_{c_{0}}
$$

$$
d
L_{c_{0}}
- \|6
\|x- y\|.
\|h_{x}h_{y}
d
$$

Moreover, Lemma 5 implies for any $x 6$= 0, we have

$$
\sqrt
$$

$$
Kd\varepsilon 2
- 6+\|\eta \|
\|v_{x}h_{x}\|max(\|x\|,\|x_{* }\|)
d
d/2
$$

for some numerical constant $K$. Hence

$$
- 6- +- +- \|v_{˜x}v_{x}\|\|v_{˜x}h_{˜x}\|\|h_{˜x}h_{x}\|\|h_{x}v_{x}\|
\sqrt \sqrt
$$

$$
Kd\varepsilon Kd\varepsilon 4
d
L_{c_{0}}
6+\|˜x- x\|++
\|\eta \|
max(\|˜x\|,\|x_{* }\|)max(\|x\|,\|x_{* }\|)
ddd
d/2
$$

2 2 2 2

$$
\sqrt \sqrt
$$

3 2 3

$$
\varepsilon \varepsilon KddKd4
L_{c_{0}}
\|\eta \|
$$

$6$ max($\|x −$ + $α$ + +

$$
\lambda \alpha v_{x}\|,\|x_{* }\|)\|v_{x}\|max(\|x\|,\|x_{* }\|)
ddd
d/2
$$

2 2 2 2

$$
\sqrt \sqrt
$$

$$
Kd
\varepsilon Kd\varepsilon 4
d
L_{c_{0}}
6
$$

max($\|x\|$ + + $α$ + + $\|η\|$

$$
\alpha \|v_{x}\|,\|x_{* }\|)\|v_{x}\|max(\|x\|,\|x_{* }\|)
ddd
d/2
$$

2 2 2 2

$$
()
\sqrt
$$

$$
Kd\varepsilon Cd6
d
L_{c_{0}}
$$

$6$ 2 + $α$ + $α$ + $\|η\|$ (15)

$$
max(\|x\|,\|x_{* }\|)\|v_{x}\|
ddd
d/2
$$

2 2 2 2

where we used Lemma 4 and Lemma 5 in the second inequality, the definition of ˜$x$ in the third inequality and Lemma 3 in the last inequality for some numerical constant $C$. Now, we lower bound Since $x /∈$ we have that

$$
\|v_{x}\|.
S_{\beta }
$$

$$
>- - \|v_{x}\|\|h_{x}\|\|h_{x}v_{x}\|
()
d/2
\sqrt
$$

$$
\beta - Kd\varepsilon - 2\|\eta \|
>
max(\|x\|,\|x_{* }\|)
d
$$

$$
\|x_{* }\|
(
)
d/2
\sqrt
$$

1 23= 3$Kd$

$$
\varepsilon +9\|\eta \|
$$

$$
max(\|x\|,\|x_{* }\|)
d
$$

$$
\|x_{* }\|
\sqrt
$$

$$
\varepsilon 3Kd
$$

$$
>
max(\|x\|,\|x_{* }\|)
d
$$

where we used the definition of $β$ and Lemma 5 in the second inequality. Note that this proves (13). Applying (16) to equation (15), we attain

$$
\sqrt
$$

$$
d
CdKd
$$

2 $ε$

$$
L_{c_{0}}
+\alpha \cdot +\alpha - 6
\|v_{x}\|max(\|x\|,\|x_{* }\|)\|v_{x}\|
\|v_{˜x}v_{x}\|
ddd
$$

3 2 2 2

$$
(
)
$$

$$
1Cdd
L_{c_{0}}
$$

$6$ 2 + $α$ + $α$

$$
\|v_{x}\|\|v_{x}\|
dd
$$

3 2 2

$$
()
$$

$$
\alpha Cd
6
$$

+ $·$

$$
\|v_{x}\|
d
$$

3 3 2 5

$$
6
\|v_{x}\|
$$

˜ . In the first inequality, we used (16). In the second inequality, we used (17). Thewhere $C$ =

$$
C+L_{c_{0}}
d
$$

$$
\alpha Cd
$$

1last inequality follows by choosing in the upper bound $α 6$ small enough so that $6$ .$c_{3} c_{3}$ 2

$$
d
$$

$$
d
$$

2.4.2 Convergence to neighborhoods of and

$$
x_{* }x_{* }
- \rho _{d}
$$

Using Lemma 6, we can now show that the iterates of our algorithm make sufficient progress at each step so that they eventually are in after a polynomial number of iterations.

$$
S_{\beta }
$$

$$
\sqrt
$$

3 $d/$2Lemma 7. Suppose Assumptions A1-A3 are satisfied and set $β$ := 4$Kd ε$+11$\|η\|$2 where

$$
/\|x_{* }\|
m\times n
$$

$K$ is a numerical constant . Let $A ∈ R$ satisfy the RRCP with respect to $G$ with constant $ε$. Let

$$
n_{i}\times n_{i- 1}
$$

$G$ be such that $∈ R$ satisfy the WDC with constant $ε$ for all $i ∈$ [$d$]. For $/∈$ , we have

$$
W_{i}x_{t}
S_{\beta }
$$

$$
9Kd\varepsilon
$$

$$
- 6- \alpha .
$$

$f$($x_{t}_{+1}$) $f$($x_{t}$)

$$
\|x_{* }\|
$$

2$d$6(2 )

2$d$)(2 )6$f$($x_{0}$Moreover, there exists an $N 6$ such that $∈$ where is the initial iterate of our$x_{0}$

$$
x_{N}
S_{\beta }
$$

2 6 2

$$
9Kd
\alpha \varepsilon \|x_{* }\|
$$

algorithm and $α >$ 0 is the step size.

Proof of Lemma 7. Recall that by Proposition 1, we may assume $/∈$ where is a

$$
x_{t}B(0,c_{0}\|x_{* }\|)c_{0}
$$

constant. We first consider the case when = Then we must have that $<$ Hence

$$
¯x_{t}- x_{t}.f(¯x_{t})f(x_{t}).
$$

for any $∈$ we have$∂f$(¯$x_{t}$),

$$
v_{¯x_{t}}
$$

$$
- =- +- <- )-
$$

$f$($x_{t}_{+1}$) $f$($x_{t}$) $f$($x_{t}_{+1}$) $f$(¯$x_{t}$) $f$(¯$x_{t}$) $f$($x_{t}$) $f$(¯$x_{t} f$(¯$x_{t}$)

$$
\alpha v_{¯x_{t}}
$$

where we used $<$ and the definition of in the first inequality. Thus observe that$f$(¯$x_{t}$) $f$($x_{t}$) $x_{t}_{+1}$ it suffices to establish the inequality for $−$ ) $−$ since this will also establish the case$f$(¯$x_{t} f$(¯$x_{t}$)

$$
\alpha v_{¯x_{t}}
$$

when =

$$
¯x_{t}x_{t}.
$$

Now, choose $∈$ By the generalized mean value theorem for the Clarke subdifferential$∂f$(¯$x_{t}$).

$$
v_{¯x_{t}}
$$

(Theorem 8.13 in [15]), there exists a $λ ∈$ [0$,$1] and $∈$ where = $−$ such that

$$
\partial f(˜x_{t})˜x_{t}¯x_{t}
˜v_{˜x_{t}}\lambda \alpha v_{¯x_{t}}
$$

we have

$$
- )- =〉
$$

$f$(¯$x_{t} f$(¯$x_{t}$)

$$
\alpha v_{¯x_{t}}〈˜v_{˜x_{t}},- \alpha v_{¯x_{t}}
=〉+- 〉
〈v_{¯x_{t}},- \alpha v_{¯x_{t}}〈˜v_{˜x_{t}}v_{¯x_{t}},- \alpha v_{¯x_{t}}
$$

$$
6\|+- \|
- \alpha \|v_{¯x_{t}}\alpha \|˜v_{˜x_{t}}v_{¯x_{t}}\|\|v_{¯x_{t}}
=\|- - \|)
- \alpha \|v_{¯x_{t}}\|(\|v_{¯x_{t}}\|˜v_{˜x_{t}}v_{¯x_{t}}
$$

![diagram: p16 region 20](2008.10579v1-membrane/imageFile6.png)

where we used the mean value theorem in the first equality. $\|$ from above and$\|$ and

$$
-
$$

We can now use our result from Section 2.4.1 to bound

$$
\|v_{¯x_{t}}
v_{¯x_{t}}
\|˜v_{˜x_{t}}
$$

below, respectively. Observe that by Lemma 6, we have

$$
(
)
$$

5 12 2

$$
- )- 6\|- - \|)6- \alpha 1- \|=- \|
$$

$f$(¯$x_{t} f$(¯$x_{t}$)

$$
\alpha v_{¯x_{t}}- \alpha \|v_{¯x_{t}}\|(\|v_{¯x_{t}}\|˜v_{˜x_{t}}v_{¯x_{t}}
\|v_{¯x_{t}}
\alpha \|v_{¯x_{t}}
$$

where we used (14) in the second inequality. But by our lower bound on $\|$, we have

$$
\|v_{¯x_{t}}
$$

$$
19Kd\varepsilon
$$

$$
)- 6- \|6- \alpha - f(¯x_{t})\|x_{* }\|
f(¯x_{t}
\alpha v_{¯x_{t}}\alpha \|v_{¯x_{t}}
$$

2$d$6 6(2 )

2$d$)(2 )6$f$($x_{0}$where we used (13) in the second inequality. Hence there are at most iterations for 2 6 2

$$
9Kd
\alpha \varepsilon \|x_{* }\|
$$

which $/∈$ where is the initial iterate of our algorithm. Thus there exists a natural number

$$
x_{t}x_{0}
S_{\beta }
$$

2$d$)(2 )6$f$($x_{0}$ and $∈$ .$N$ such that $N 6$

$$
x_{N}
S_{\beta }
$$

2 6 2

$$
9Kd
\alpha \varepsilon \|x_{* }\|
$$

2.4.3 Convergence to neighborhood of

$$
x_{* }
$$

We now show that if any iterate is in , then the negation step of the algorithm (Steps 3–7) ensures

$$
S_{\beta }
$$

that our iterates will now be in a neighborhood of as opposed to a neighborhood of We

$$
x_{* }x_{* }.
- \rho _{d}
$$

will use the following result that is contained in the union of neighborhoods of the true solution

$$
S_{\beta }
$$

and a negative multiple thereof if $β$ is sufficiently small:

$$
\sqrt
$$

2 6Lemma 8. If 0 $<$ 24$π d β 6$ 1, then

$$
\sqrt
$$

2 9 2 12

$$
\subset d\cup d
$$

$B$($x_{∗},$70000$π β\|x_{∗}\|$) $x_{∗},$77422$π$

$$
\beta \|x_{* }\|).
S_{\beta }B(- \rho _{d}
$$

We also need the following lemma which shows that the objective function is smaller near than

$$
x_{* }
$$

near

$$
x_{* }:
- \rho _{d}
$$

2 2Lemma 9. Fix 0 $< ε <$ 1$/$(16$πd$ ) and suppose Assumption A3 is satisfied. Suppose that $A ∈$

$$
n_{i}\times n_{i- 1}
m\times n_{d}
$$

$R$ satisfies the RRCP with respect to $G$ with constant $ε$ and $G$ is such that each $∈ R$

$$
W_{i}
$$

satisfies the WDC with constant $ε$. Then for any $∈ ,$1], we have that

$$
\phi _{d}[\rho _{d}
$$

$f$($x$) $< f$($y$) (18)

$−$12 $−$12for all $x ∈$ and $y ∈$ where

$$
x_{* },r_{1}d\|x_{* }\|)r_{1}
x_{* },r_{1}d\|x_{* }\|)
B(\phi _{d}B(- \phi _{d}
$$

is a universal constant.

These results are proven in Section 2.5.3. The main result of this section is as follows.

$$
\sqrt
$$

$$
d/2
$$

Lemma 10. Suppose Assumptions A1-A3 are satisfied and set $β$ := 4$Kd$ $ε$ + 11$\|η\|$2

$$
/\|x_{* }\|
m\times n
$$

where $K$ is a numerical constant. Let $A ∈ R$ satisfy the RRCP with respect to $G$ with constant

$$
n_{i}\times n_{i- 1}
$$

$ε$. Let $G$ be such that $∈ R$ satisfy the WDC with constant $ε$ for all $i ∈$ [$d$]. If $∈$ ,

$$
W_{i}x_{t}
S_{\beta }
$$

+then $∈ S$ , i.e.,

$$
¯x_{t}
\beta
$$

$$
\sqrt
$$

9 $d/$212

$$
+2\|\eta \|
- 6
\varepsilon \|x_{* }\|C_{6}d
\|¯x_{t}x_{* }\|C_{5}d
$$

where and are numerical constants.$C_{5} C_{6}$

Proof of Lemma 10. Suppose $∈$ . We require $β$ to satisfy the assumption of Lemma 8 and for

$$
x_{t}
S_{\beta }
$$

$−$12to be contained in the balls of radius from Lemma 9. Recall that by assumption

$$
r_{1}d\|x_{* }\|
S_{\beta }
\|x_{* }\|
$$

$c_{2}$$−$102 for some constants and Choosing and sufficiently

$$
0<\varepsilon <and\|\eta \|6
$$

$c_{1} c_{2}$. $c_{1} c_{2}$

$$
c_{1}d
d/2
$$

482 $d$ small enough, we can have that

$$
\sqrt d/2
$$

$$
\sqrt
$$

4$K$11$\|η\|$2

$$
r
$$

$c_{1}$ 11$c_{2}$3 1

$$
\beta =4Kd\varepsilon +
6+6.
$$

48 48 2 2 48$d d$ (77422$π$ ) $d$

$$
\|x_{* }\|
$$

$$
\sqrt
$$

2 12 $−$12Hence $β$ satisfies the assumptions of Lemma 8 and 77422$π d 6$ Note that

$$
\beta \|x_{* }\|r_{1}d\|x_{* }\|.
$$

+ $−$$−$12 $−$12this implies $S ⊂$ while $S ⊂$ Therefore, we can apply

$$
B(x_{* },r_{1}d\|x_{* }\|)x_{* },r_{1}d\|x_{* }\|).
B(- \rho _{d}
\beta \beta
$$

$−$ +equation (18) in Lemma 9 so that for any $y ∈ S$ and $x ∈ S$ , $f$($x$) $< f$($y$). Since $∈$ , either

$$
x_{t}
S_{\beta }
\beta \beta
$$

+ $−$ + + $−$$∈ S$ or $∈ S$ . If $∈ S$ , then $<$ so = $∈ S$ . Otherwise, $∈ S$ and

$$
x_{t}x_{t}x_{t}f(x_{t})f(- x_{t})¯x_{t}x_{t}x_{t}
\beta \beta \beta \beta \beta
$$

+ +$∈ S$ so that $<$ meaning = $∈ S$ . In either case, we must have that

$$
- x_{t}f(- x_{t})f(x_{t})¯x_{t}- x_{t}
\beta \beta
$$

+ +$∈ S$ . By the definition of $S$ , this establishes the inequality

$$
¯x_{t}
\beta \beta
$$

$$
\sqrt
$$

12 9 $d/$2

$$
+2\|\eta \|
- 6
\|¯x_{t}x_{* }\|C_{5}d\varepsilon \|x_{* }\|C_{6}d
$$

for some numerical constants and$C_{5} C_{6}$.

2.4.4 Convergence to up to noise

$$
x_{* }
$$

Finally, we show that once in a neighborhood of the iterates of our algorithm will converge to

$$
x_{* },x_{* }
$$

up to the noise level in the measurements. We will use the following convexity-like property around the minimizer:

$$
m\times n_{d}
$$

Lemma 11. Fix 0 $< ε <$ 1$/$(200 $d$ ). Suppose that $A ∈ R$ satisfies the RRCP with respect to

$$
n_{i}\times n_{i- 1}
$$

$G$ with constant $ε$ and $G$ is such that each $∈ R$ satisfies the WDC with constant $ε$ for

$$
W_{i}
\sqrt
$$

$i ∈$ [$d$]. Then for all $x ∈$ and any $∈ ∂f$($x$), we have

$$
B(x_{* },d
\varepsilon \|x_{* }\|)v_{x}
$$

$$
\| \| \| \|
$$

1 1 1 2

$$
\| \| - (x- 6\|x- +\|\eta \|.
v_{x}x_{* })x_{* }\|
\| \| dd
d/2
$$

82 2 2

We now prove the following lemma.

$$
\sqrt
$$

3 $d/$2Lemma 12. Suppose Assumptions A1-A3 are satisfied and set $β$ := 4$Kd ε$ + 11$\|η\|$2

$$
/\|x_{* }\|
m\times n
$$

where $K$ is a numerical constant. Let $A ∈ R$ satisfy the RRCP with respect to $G$ with constant

$$
n_{i}\times n_{i- 1}
$$

$ε$. Let $G$ be such that $∈ R$ satisfy the WDC with constant $ε$ for all $i ∈$ [$d$]. Suppose

$$
W_{i}
$$

+ $−$12$∈ S$ for some $N ∈ N$. Then for all $t > N$, we have that $∈$ = and

$$
¯x_{t}B(x_{* },r_{1}d\|x_{* }\|),¯x_{t}x_{t},
x_{N}
\beta
$$

$$
d/2
$$

$$
t+1- N
\|\eta \|
- 6\tau - +\vartheta \|x_{t+1}x_{* }\|x_{* }\|
\|x_{N}
$$

$$
d
$$

7 $α$where $τ$ := 1 $−$

$$
d
$$

2$c_{3}$$∈$ (0$,$1) and $ϑ$ := .

$$
1- \tau
$$

+ $−$12Proof of Lemma 12. Suppose $t$ = $N$ so we have that = $∈ S ⊂$ As

$$
¯x_{t}x_{t}B(x_{* },r_{1}d\|x_{* }\|).
\beta
$$

shown in Lemma 10, this inclusion holds by our assumptions on $ε$ and $η$. By Assumption A1, the

![diagram: p18 region 24](2008.10579v1-membrane/imageFile7.png)

$∈$ we haverequirements of Lemma 11 are met. Observe that for any $∂f$(¯$x_{t}$),

$$
v_{¯x_{t}}
$$

$$
\| \| \alpha \alpha \| \|
$$

$−$ +

$$
- =- - - - x_{* }
\|x_{t+1}x_{* }\|\| ¯x_{t}(¯x_{t}x_{* })(¯x_{t}x_{* })\| \alpha v_{¯x_{t}}
dd
$$

$$
\| \| (
)
\| \|
$$

$α$ 1

$$
\| \| 61- - - - +\alpha (¯x_{t}x_{* })
\|¯x_{t}x_{* }\|
v_{¯x_{t}}
\| \| dd
$$

$$
()
)(
$$

$α$ 1 2

$$
\alpha \|\eta \|
61- - +\alpha
$$

$−$ +

$$
\|¯x_{t}x_{* }\|
\|¯x_{t}x_{* }\|
dd
d/2
$$

82 2 2

$$
()
$$

$$
\alpha =1- - +\alpha \|\eta \|
\|¯x_{t}x_{* }\|
d
d/2
$$

8 2 2

$$
d
$$

2 $c_{2}$where we used Lemma 11 in the second inequality. Using $α 6$ and $\|η\| 6$ for suffi-$c_{3}$

$$
\|x_{* }\|
$$

2 $d$ 48

$$
d
$$

2 $d$ $−$12 $−$12ciently small constants and we have that if $∈$ then $∈$

$$
c_{2}c_{3},¯x_{t}B(x_{* },r_{1}d\|x_{* }\|),x_{t+1}B(x_{* },r_{1}d\|x_{* }\|)
$$

so the iterates stay within a small ball around the minimizer. Hence Lemma 9 yields =¯$xt$+1 $xt$+1. $−$12Repeatedly applying the above logic shows that for all $t > N$, $∈$ and =

$$
x_{t}B(x_{* },r_{1}d\|x_{* }\|)¯x_{t}x_{t}.
d
$$

2Finally, using $α 6$ in the second half of equation (19) yields$c_{3}$ 2

$$
d
$$

$$
(
)
d/2d/2
$$

7 $α$ 2 2

$$
- 61- - +\|\eta \|=:- +\|\eta \|
\|x_{t+1}x_{* }\|
\|x_{t}x_{* }\|2c_{3}\tau \|x_{t}x_{* }\|2c_{3}
$$

$$
d
8dd
$$

87 $α$ . Choosing so that $<$where $τ$ := 1$−$ implies $τ ∈$ (0$,$1). Starting at $t$ = $N$ and repeatedly$c_{3} c_{3}$

$$
d
$$

8 72 applying this inequality, we attain

$$
d/2
$$

$$
t+1- Nt- Nt- N- 1
- 6\tau - +(\tau +\tau +\cdot \cdot \cdot +\|\eta \|
\|x_{t+1}x_{* }\|x_{* }\|1)2c_{3}
\|x_{N}
$$

$$
d
d/2
$$

22$c_{3}$

$$
t+1- N
6\tau - +\|\eta \|
x_{* }\|
\|x_{N}
$$

$$
1- \tau d
d/2
$$

$$
t+1- N
=:\tau - +\vartheta \|\eta \|
x_{* }\|
\|x_{N}
$$

$$
d
$$

2$c_{3}$where $ϑ$ := $.$ This completes the proof.

$$
1- \tau
$$

2.4.5 Final proof of Theorem 3

With all of the necessary lemmas proven, we bring them together to prove Theorem 3.

$$
\sqrt d/2
$$

3 $ε$ + 11$\|η\|$2 where $K$ is a numerical constant. ByProof of Theorem 3. Set $β$ := 4$Kd$

$$
/\|x_{* }\|
$$

Proposition 1, we may assume that our initial iterate $/∈$ for some numerical con-

$$
x_{0}B(0,c_{0}\|x_{* }\|)
$$

2$d$)(2 )6$f$($x_{0}$ and $∈$ .stant Then by Lemma 7, there exists an $N ∈ N$ such that $N 6$$c_{0}$.

$$
x_{N}
S_{\beta }
$$

2 6 2

$$
9Kd
\alpha \varepsilon \|x_{* }\|
$$

+Then Lemma 10 implies $∈ S$ which establishes inequality (7). Finally, Lemma 12 establishes

$$
¯x_{N}
\beta
$$

inequality (8) for any $t > N$. Inequality (9) follows by using (8) and the following result with $j$ = $d$ which established Lipschitz continuity of $G$ for $x$ within a neighborhood of

$$
x_{* }:
$$

$$
\sqrt
$$

4 6 and $G$ is suchLemma 13 (Lemma A.8 in [35]). Suppose 0 $< ε <$ 1$/$(200 $d$ ), $x ∈$

$$
\varepsilon \|x_{* }\|),
B(x_{* },d
n_{i}\times n_{i- 1}
$$

that each $∈ R$ satisfies the WDC with constant $ε$ for $i ∈$ [$d$]. Then we have that for all

$$
W_{i}
$$

$j ∈$ [$d$],

$$
\| \|
$$

1$.$211

$$
\| \| 6
- \Pi \|x-
$$

$$
x_{* }
W_{i,+,x}xx_{* }\|.
W_{i,+,x_{* }}
i=j
i=j
j/2
$$

![diagram: p19 region 27](2008.10579v1-membrane/imageFile8.png)

### Supplementary Results

In the following sections, we provide proofs for auxillary results that were in used in the four main lemmas used to establish Theorem 3. Section 2.5.1 focuses on proving that after a polynomial number of iterations, the iterates of our algorithm are all bounded away from zero. Section 2.5.2 establishes supplementary results about controlling subgradients in Section 2.4.1. Then Section 2.5.3 establishes results concerning the zeros of and properties of the objective function used

$$
h_{x}
$$

in Section 2.4.3. Lastly, Section 2.5.4 focuses on establishing the convexity-like property near the minimizer which is formalized in Section 2.4.4.

2.5.1 Iterates are eventually bounded away from zero

We focus on proving the following proposition:

$$
\sqrt
$$

3Proposition 1. Fix $ε >$ 0 such that $Kd$ $ε 6$ 1 where $K$ is a universal constant. Suppose

$$
m\times n_{d}
$$

that $A ∈ R$ satisfies the RRCP with respect to $G$ with constant $ε$ and $G$ is such that each

$$
n_{i}\times n_{i- 1}
\in R
$$

satisfies the WDC with constant $ε$ for $i ∈$ [$d$]. Suppose that the step size $α$ and

$$
W_{i}
d
\|x_{* }\|
$$

2 $c_{2}$and $\|η\| 6$ where $C$ and are numerical constants. Ifnoise $η$ satisfy 0 $< α <$ $c_{2}$

$$
d/2
$$

) 2104$π$($Cd$+2$c_{2}$

$$
\lceil \rceil ()
$$

$$
d
$$

1 2 24$π$$∈ B$(0$,$ then after at most = iterations, we have that for all $t >$ and

$$
x_{t}\|x_{* }\|),N_{0}
$$

$N_{0}$ 52$π α$52$π$ 1$λ ∈$ [0$,$1], + (1 $− /∈ B$(0$,$

$$
\lambda ¯x_{t}\lambda )x_{t+1}\|x_{* }\|).
$$

104$π$ This result asserts that if an iterate of our algorithm lies within a ball of the origin, then after a polynomial number of steps, it will leave this region. To prove it, we require the following lemma that establishes certain properties of any subgradient $∈ ∂f$($x$) for points $x$ near the origin:

$$
v_{x}
$$

$$
\sqrt
$$

3Lemma 14. Fix $ε >$ 0 such that $Kd ε 6$ 1 where $K$ is a universal constant. Suppose that $A ∈$

$$
n_{i}\times n_{i- 1}
m\times n_{d}
$$

satisfies the RRCP with respect to $G$ with constant $ε$ and $G$ is such that each $∈ R$

$$
R
W_{i}
$$

1satisfies the WDC with constant $ε$ for $i ∈$ [$d$]. Then for all $x ∈ B$(0$,$ and any $∈ ∂f$($x$),

$$
\|x_{* }\|)v_{x}
$$

52$π$ we have that

1 1 $<$ 0 and $>$$〈x,v_{x}〉 \|v_{x}\| \|x_{∗}\|.$

$$
d
$$

24$π$2

We are now ready to proceed with a proof of Proposition 1.

1Proof of Proposition 1. Suppose that $∈ B$(0$,$ By Lemma 14, we have that and the

$$
x_{t}\|x_{* }\|).¯x_{t}
$$

52$π$ next iterate = $−$ form an obtuse triangle for any $∈$ Thus$x_{t}_{+1}$ ¯$x_{t} ∂f$(¯$x_{t}$).

$$
\alpha v_{¯x_{t}}v_{¯x_{t}}
$$

2 2 2 2 2

$$
=>+\alpha \|
\|¯x_{t+1}\|\|x_{t+1}\|\|¯x_{t}\|
\|v_{¯x_{t}}
$$

1 22 2

$$
>+\alpha \|x_{* }\|
\|¯x_{t}\|
$$

$d$ 2(2 24$π$)

where the last inequality follows from Lemma 14. Thus the norm of the iterates will increase until

$$
\lceil \rceil ()
$$

$$
d
$$

2 24$π$ 1after = iterations we have $/∈ B$(0$,$

$$
N_{0}\|x_{* }\|).
x_{t+N_{0}}
$$

$α$52$π$ 52$π$ 1Now consider $/∈ B$(0$,$ We will show that for any $λ ∈$ [0$,$1], + (1 $− /∈$

$$
x_{t}\|x_{* }\|).\lambda ¯x_{t}\lambda )x_{t+1}
$$

52$π$ 1 11 Note that for $/∈ B$(0$,$ we have = $>$ Then observe$B$(0$,$

$$
\|x_{* }\|).x_{t}\|x_{* }\|),\|¯x_{t}\|\|x_{t}\|\|x_{* }\|.
$$

104$π$ 52$π$ 52$π$

![diagram: p20 region 29](2008.10579v1-membrane/imageFile9.png)

$∈$ we havethat for any $∂f$(¯$x_{t}$),

$$
v_{¯x_{t}}
$$

$$
(
)
$$

$$
d/2
\|6\alpha Cd+
2\|\eta \|
max(\|¯x_{t}\|,\|x_{* }\|)
\alpha \|v_{¯x_{t}}
d
$$

$$
\|x_{* }\|
$$

$$
6\alpha
$$

max($\|$¯$x_{t}\|,\|x_{∗}\|$)($Cd$ 2$c_{2}$)

$$
d
$$

$$
\alpha
$$

$6$ +52$π\|$¯$x_{t}\|$($Cd$ 2$c_{2}$)

$$
d
$$

$$
6
\|¯x_{t}\|
$$

where the first inequality follows by Lemma 3, the second by the assumption on the noise energy

$$
\|x_{* }\|
$$

1$c_{2}$$\|η\| 6$ , the third due to $/∈ B$(0$,$ and the last inequality follows by the assumption

$$
x_{t}\|x_{* }\|),
d/2
$$

52$π$2 1on $α$. Thus since = $−$ , we have that + (1 $− /∈ B$(0$,$ for any

$$
x_{t+1}¯x_{t}\lambda ¯x_{t}\lambda )x_{t+1}\|x_{* }\|)
\alpha v_{¯x_{t}}
$$

104$π$ $λ ∈$ [0$,$1]$.$

We now focus on proving Lemma 14. To show this, we first require the following angle concentration property of the map $z 7→ z$ for $z$ in the range of $G$.

$$
A_{z}
$$

Lemma 15. Fix 0 $< ε <$ 1$/$(4$L$) where $L$ is the universal constant specified in the RRCP. Let

$$
m\times n
n_{i}\times n_{i- 1}
$$

$A ∈ R$ satisfy the RRCP with respect to $G$ with constant $ε$. Let $G$ be such that $∈ R$

$$
W_{i}
k
$$

satisfy the WDC with constant $ε$ for all $i ∈$ [$d$]. Then for all $x,y ∈ R \{$0$}$, the angle :=$θ_{1}$ $∠$($A G$($x$)$,A G$($y$)) is well-defined and$G$($x$) $G$($y$)

$$
- )|64L\varepsilon
$$

$|$cos$θ_{1}$ cos$ϕ$($θ_{d}$

where = $∠$($G$($x$)$,G$($y$)) and $ϕ$ : $R → R$ defined by

$$
\theta _{d}
$$

$$
()
$$

($π −$ 2$θ$)cos$θ$ + 2sin$θ$ $−$1$ϕ$($θ$) := cos $.$

$$
\pi
$$

$k$ 1Proof of Lemma 15. Fix $x,y ∈ R \{$0$}$. We use the shorthand notation := Π and

$$
\Lambda _{x}W_{i,+,x}
i=d
$$

:= Note that the WDC implies that for sufficiently small $ε$, we have that $y 6$= 0.

$$
\Lambda _{x}x.\Lambda _{x}x,\Lambda _{y}
x_{d}
$$

Hence we may assume, without loss of generality, that = $y\|$ = 1$.$ Now define the following

$$
\|\Lambda _{x}x\|\|\Lambda _{y}
$$

quantities:

$$
- y〉,
\delta _{1}〈\Lambda _{x}x,(A
$$

)Λ$_{y}$

$$
A_{y_{d}}
\Phi _{x_{d},y_{d}}
x_{d}
$$

T := $−$

$$
\delta _{2}〈\Lambda _{x}x,(AI)\Lambda _{x}x〉,
A_{x_{d}}
x_{d}
$$

$$
:=y,(A- y〉.
\delta _{3}〈\Lambda _{y}I)\Lambda _{y}
A_{y_{d}}
y_{d}
$$

Observe that by the RRCP, we have that $6 Lε.$ Hence if 0 $< ε <$ 1$/L$,max$i$=1$,$2$,$3 $|δi|$

$$
0<1- L\varepsilon 6
\Lambda _{x}x\|
\|A_{x_{d}}
$$

so $6$= 0. The same conclusion holds for $y\|$ so is well-defined. Furthermore, note

$$
\Lambda _{x}x\|\Lambda _{y}\theta _{1}
\|A_{x_{d}}\|A_{y_{d}}
$$

![diagram: p21 region 30](2008.10579v1-membrane/imageFile10.png)

that

$$
y〉
$$

Λ$_{y}$

$$
〈\Lambda _{x}x,A
A_{y_{d}}
x_{d}
$$

=cos$θ_{1}$

$$
y\|
$$

Λ$_{y}$

$$
\Lambda _{x}x\|\|A_{y_{d}}
\|A_{x_{d}}
$$

$$
y〉
$$

Λ$_{y}$

$$
〈\Lambda _{x}x,A
A_{y_{d}}
x_{d}
\sqrt
$$

$$
y〉
$$

Λ$_{y}$Λ$_{y}$

$$
y,A_{y_{d}}
\Lambda _{x}x〉〈A_{y_{d}}
\Lambda _{x}x,A_{x_{d}}
〈A_{x_{d}}
y〉+
$$

Λ$_{y} δ_{1}$

$$
〈\Lambda _{x}x,\Phi _{x_{d},y_{d}}
\sqrt
$$

= + $y〉$ +

$$
(〈\Lambda _{x}x,\Lambda _{x}x〉\delta _{2})(〈\Lambda _{y}y,\Lambda _{y}\delta _{3})
y〉+
$$

Λ$_{y} δ_{1}$

$$
〈\Lambda _{x}x,\Phi _{x_{d},y_{d}}
\sqrt
$$

= $.$(1 + +$δ_{2}$)(1 $δ_{3}$)

Thus

$$
\mid \mid \mid \mid y〉+
\mid \mid
$$

Λ$_{y} δ_{1}$

$$
〈\Lambda _{x}x,\Phi _{x_{d},y_{d}}
\sqrt y〉|6
- - \mid y〉\mid
$$

Λ$_{y}$ Λ$_{y}$$|$cos$θ_{1}$

$$
〈\Lambda _{x}x,\Phi _{x_{d},y_{d}}〈\Lambda _{x}x,\Phi _{x_{d},y_{d}}
\mid \mid
$$

(1 + +$δ_{2}$)(1 $δ_{3}$)

$$
\mid \mid \mid \mid
$$

$$
\mid \mid \sqrt 6
y〉|- \mid 1\mid
$$

Λ$_{y}$

$$
|〈\Lambda _{x}x,\Phi _{x_{d},y_{d}}
\mid \mid
$$

(1 + +$δ_{2}$)(1 $δ_{3}$)

$$
|\delta _{1}|
\sqrt
$$

+ (1 + +$δ_{2}$)(1 $δ_{3}$)

$$
\mid \mid \mid \mid 1L\varepsilon \mid \mid
$$

$6$ 2 1 $−$ +

$$
\mid \mid 1- L\varepsilon 1- L\varepsilon 3L\varepsilon 6
64L\varepsilon 1- L\varepsilon
$$

where we used $\| 6$ 2 in the third inequality and $Lε <$ 1$/$4 in the last inequality. The proof

$$
\|\Phi _{x_{d},y_{d}}
$$

1 [($π −$ + ]$.$concludes by noting that

$$
y〉=
$$

Λ$_{y}$ 2$θ_{d}$)cos$θ_{d}$ 2sin$θ_{d}$

$$
〈\Lambda _{x}x,\Phi _{x_{d},y_{d}}
\pi
$$

We also require upper bounds on quantities that will be useful throughout the remaining proofs.

$$
m\times n
$$

Lemma 16. Fix 0 $< ε <$ 1$/$(48$d$). Let $A ∈ R$ satisfy the RRCP with respect to $G$ with constant

$$
n_{i}\times n_{i- 1}
$$

$ε$. Let $G$ be such that $∈ R$ satisfy the WDC with constant $ε$ for all $i ∈$ [$d$]. Then for any

$$
W_{i}
k
$$

$x ∈ R$ , we have

$$
2- d
$$

$6$ 2 $,$ (20)

$$
\|\Lambda _{x}\|
$$

12 2 2$6$ (1 + (21)

$$
\Lambda _{x}\|L\varepsilon )\|\Lambda _{x}\|
\|A_{x_{d}}
$$

12Proof of Lemma 16. For equation (20), note that the WDC implies that $6$ +$ε$ for each

$$
\|W_{i,+,x}\|
$$

2 $i ∈$ [$d$] so

$$
)
(
d
d
$$

Y 1 1 1 + 4$εd$ 131 $d d$log(1+2$ε$) $−d$2 2 =+ $ε$ (1 + 2$ε$) = $e 6 6$ 2

$$
66
\|\Lambda _{x}\|\|W_{i,+,x}\|
ddd
$$

2 122 2 2 $i$=1

$$
u
$$

where we used the fact that log(1 + $u$) $6 u$ and $e 6$ 1 + 2$u$ for $u <$ 1 while the last inequality follows by our assumption on $ε$: $ε <$ 1$/$(48$d$). For equation (21), observe that by the RRCP and the local linearity of $G$, we have that for

$$
k
$$

sufficiently small $z ∈ R$ ,

$$
- 6\|z\|
\Lambda _{x}z〉〈\Lambda _{x}z,\Lambda _{x}z〉|L\varepsilon \|\Lambda _{x}\|
|〈A_{x_{d}}\Lambda _{x}z,A_{x_{d}}
$$

which implies that 2 2

$$
6(1+\|z\|.
\Lambda _{x}z〉|L\varepsilon )\|\Lambda _{x}\|
\Lambda _{x}z,A_{x_{d}}
|〈A_{x_{d}}
$$

$$
k
$$

$6$ (1 + .Since this holds for any $z ∈ R$ , we conclude

$$
\Lambda _{x}\|L\varepsilon )\|\Lambda _{x}\|
\|A_{x_{d}}
$$

Now we set out to prove Lemma 14.

Proof of Lemma 14. Suppose $f$ is differentiable at $x$ so that

$$
v_{x}
$$

first show that $<$ 0. Note that

$$
〈x,v_{x}〉
$$

is precisely the gradient of $f$. We

T T T T T T

$$
=〈\Lambda AAA\eta ,x〉.
x_{* },x〉- 〈\Lambda \Lambda _{x}x,x〉- 〈\Lambda 〈x,v_{x}〉
\Lambda _{x_{* }}
xxx
A_{x_{d}}A_{x_{* ,d}}
x_{d}x_{d}x_{d}
︸
︷︷︸︸︷︷︸
︸
︷︷︸
$$

($I$) ($III$)($II$)

We will bound the first and third term from above and the second from below. the second term as its proof will give us a result for the first term.

We first focus on

(II): For the second term, note that we can write it as

$$
〈\Lambda A=\|.
x_{* },x〉
x_{d}x_{* ,d}x_{d}x_{* ,d}
\Lambda _{x_{* }}
x
A_{x_{* ,d}}cos(∠(A_{x_{d}},A_{x_{* ,d}}))\|A_{x_{d}}\|\|A_{x_{* ,d}}
x_{d}
$$

By Lemma 15, we have that

$$
))- 4L\varepsilon 6
$$

)) $6$ )) + 4$Lε$cos($ϕ$($θ_{d}$

$$
x_{d}
x_{* ,d}cos(\varphi (\theta _{d}
$$

cos($∠$($A_{x}_{d}$

$$
,A_{x_{* ,d}}
$$

where = ). Thus

$$
\theta _{d}∠(x_{d},x_{* ,d}
$$

$$
〈\Lambda A>))- \|.
x_{* },x〉
(cos(\varphi (\theta _{d}x_{d}x_{* ,d}
\Lambda _{x_{* }}
x
A_{x_{* ,d}}4L\varepsilon )\|A_{x_{d}}\|\|A_{x_{* ,d}}
x_{d}
$$

However, note that

2($π −$ 2$θ$)cos$θ$ + 2sin$θ$

$$
>\forall \theta \in [0,\pi ].
$$

cos($ϕ$($θ$)) =

$$
\pi \pi
$$

Hence if $ε <$ 1$/$(4$Lπ$), applying (23) to (22) we have that

$$
〈\Lambda A>\|.
x_{* },x〉
x_{d}x_{* ,d}
\Lambda _{x_{* }}
x
A_{x_{* ,d}}\|A_{x_{d}}\|\|A_{x_{* ,d}}
x_{d}
\pi
$$

We now bound $\|$: observe that by the RRCP,

$$
x_{d}
\|A_{x_{d}}
$$

T 2 2 2 2

$$
|〈(A- 〉|6\|=\Rightarrow (1- \|6\|6(1+\|
I)x_{d},x_{d}L\varepsilon \|x_{d}L\varepsilon )\|x_{d}x_{d}L\varepsilon )\|x_{d}
A_{x_{d}}\|A_{x_{d}}
x_{d}
$$

which gives

$$
\sqrt \sqrt 1- \|6\|61+\|.
L\varepsilon \|x_{d}x_{d}L\varepsilon \|x_{d}
\|A_{x_{d}}
$$

By equation (11) of [33], we have that

$$
)()
(
d/2d/2
$$

$$
- \varepsilon \|x\|6\|6+\varepsilon \|x\|.
\|x_{d}
$$

Hence we attain

$$
))
((
d/2d/2
\sqrt \sqrt
$$

$$
\|x\|6\|6\|x\|.
- \varepsilon +\varepsilon 1- L\varepsilon 1+L\varepsilon x_{d}
\|A_{x_{d}}
$$

Analogous bounds hold for $\|$. Applying (25) to equation (24), we conclude that

$$
x_{* ,d}
\|A_{x_{* },d}
$$

$$
>\|
〈\Lambda A
x_{* },x〉
x_{* ,d}
x_{d}
\Lambda _{x_{* }}
x
A_{x_{* ,d}}
\|\|A_{x_{* ,d}}
\|A_{x_{d}}
x_{d}
\pi ()
d
$$

$$
>
(1- L\varepsilon )- \varepsilon \|x\|\|x_{* }\|.
$$

$π$ 2

$$
ddd
$$

If 2$dε <$ 2$/$3, we further have (1$/$2 $− ε$) $>$ (1 $−$ 2$dε$)$/$2 $>$ 1$/$3(1$/$2 )$.$ Then if $ε$ is chosen such that 1 $− Lε >$ 1$/$2, then we get

1 1T T

$$
>
〈\Lambda A
x_{* },x〉
\|x\|\|x_{* }\|.
\Lambda _{x_{* }}
x
A_{x_{* ,d}}
x_{d}
d
$$

6$π$ 2

This concludes the bound of the second term. We then proceed to bounding (I) and (III).

(I): Observe that by equation (25) and our choice of $ε$, we get

$$
)
(
d
$$

1 113131 2 2T T 2 2+ $ε$

$$
\|x\|=\|x\|.
〈\Lambda A=\|6(1+L\varepsilon )\|x\|62\cdot \Lambda _{x}x,x〉
x_{d}
x
A_{x_{d}}\|A_{x_{d}}
x_{d}
dd
$$

2 12 62 2

(III): Observe that

$$
\sqrt \sqrt
$$

213 1 $6$ (27)1 + $6$ (1 + $Lε$)

$$
6
L\varepsilon \|\Lambda _{x}\|
\Lambda _{x}\|
\|A_{x_{d}}
d/2d/2
$$

12 2 2 where we used (21) in the first inequality, (20) in the second inequality and our assumption on $ε$ in the last inequality. Thus we attain

2 1 12$c_{2}$T T

$$
|〈x,\Lambda A\eta 〉|6
6
\|\eta \|\|x\|66
\Lambda _{x}\|\|\eta \|\|x\|\|x_{* }\|
\|x\|\|x_{* }\|
x
\|x\|\|A_{x_{d}}
x_{d}
dd
d/2
$$

12$π$2 22 1$c_{2}$where the third inequality follows by $\|η\| 6$ .and the last inequality is due to $<$

$$
\|x_{* }\|c_{2}
d/2
$$

24$π$2 Using our results for (I), (II), and (III), we conclude that

T T T T T T= $〈$Λ $A$

$$
- 〈\Lambda A
- 〈\Lambda A\eta ,x〉
〈x,v_{x}〉
\Lambda _{x}x,x〉
x_{* },x〉
\Lambda _{x_{* }}
x
x
x
A_{x_{d}}
A_{x_{* ,d}}
x_{d}
x_{d}
x_{d}
)
(
$$

1 1131

$$
\|x\|+- \|x\|
6
\|x_{* }\|\|x_{* }\|
d
$$

6 12$π$ 6$π$2

$$
()
$$

1 13 1

$$
6\|x\|
\|x\|- .
\|x_{* }\|
d
$$

6 12$π$2

11 i.e. $x ∈ B$(0$,$ thenThus if $\|x\| <$

$$
\|x_{* }\|,\|x_{* }\|),
$$

52$π$ 52$π$ 1 1 $6 − <$ 0$.$ (28)

$$
〈x,v_{x}〉\|x\|\|x_{* }\|
d
$$

24$π$2 Lastly, observe that this gives

$$
〈〉
$$

$x$ 1 1

$$
- >
,v_{x}\|x_{* }\|.
d
\|x\|24\pi
$$

$$
〈
〉
x
$$

But by the Cauchy-Schwarz inequality, $−$ $6$ so we obtain

$$
,v_{x}\|v_{x}\|
\|x\|
$$

11 (29)

$$
>
\|x_{* }\|.
\|v_{x}\|
d
$$

24$π$2

$$
\sum s
$$

When $f$ is not differentiable at $x$, we have that by equation (12), we can write =

$$
v_{x}
c_{`}v_{`}
$$

$`$=1

$$
\sum s
$$

where $>$ 0 for $` ∈$ [$s$] and = 1. Applying our result for differentiable points $x$, we have

$$
c_{`}c_{`}
$$

$`$=1 that

$$
ss
\sum \sum
$$

1 11 1 = $<$ 0$.$

$$
〉6- =-
$$

$〈x,v_{x}〉 \|x\|\|x_{∗}\| \|x\|\|x_{∗}\|$

$$
c_{`}〈x,v_{`}c_{`}
dd
$$

24$π$ 24$π$2 2 $`$=1 $`$=1

For the lower bound on the norm of note that by the Cauchy-Schwarz inequality and equation

$$
v_{x},
$$

(28), we have that

$$
〉〉
〈〈
s
s
\sum \sum
$$

1 1 1

$$
xx
$$

1 = $>$

$$
=>,-
$$

=$\|v_{x}\|$ max$〈v_{x},u〉 v_{x},− \|x_{∗}\| \|x_{∗}\|$

$$
c_{`}v_{`}
c_{`}
dd
\|x\|\|x\|24\pi 24\pi
$$

$$
\|u\|=1
$$

$`$=1 $`$=1

as desired.

2.5.2 Proofs for Section 2.4.1

In this section, we focus on results that aided in establishing Lemma 6 in Section 2.4.1. The first result concerns a bound on the norm of our descent direction (Lemma 3). The second is that is

$$
h_{x}
k
$$

Lipschitz with respect to $x ∈ R$ outside of a ball of the origin (Lemma 4) and the third is that for

$$
k
$$

all $x ∈ R$ , approximates any $∈ ∂f$($x$) (Lemma 5). Prior to beginning the proof of Lemma

$$
h_{x}v_{x}
$$

$$
\pi - 2\theta d- 1
d,x\pi - \theta _{j+1,x}
$$

3, we outline some notation. For $x 6$= 0, set := , and := . Based on

$$
\zeta _{j+1,x}
\psi _{d,x}
i=j
\pi \pi
$$

this notation, can be written as

$$
h_{x}
$$

$$
[(())]
d- 1
\sum
$$

1 2sin$θ_{d,x}$

$$
sin\theta _{i,x}
$$

= + $\|x\| −$ + ˆ$x .$

$$
h_{x}\zeta _{0,x}\|x_{* }\|ˆx_{* }\|x_{* }\|\zeta _{i+1,x}
\psi _{d,x}\psi _{d,x}
d
\pi \pi
$$

2 $i$=0

˘and will be useful.In the remaining proofs, a number of results concerning properties of

$$
\theta _{i,x}\theta _{i}
$$

The following lemma records these results:

Lemma 17 (Bounds from Lemma 10 in [33]). For $x 6$= 0, let := and :=

$$
\theta _{0,x}∠(x,x_{* })\theta _{i,x}g(\theta _{i- 1,x})
$$

![diagram: p25 region 38](2008.10579v1-membrane/imageFile11.png)

![diagram: p25 region 39](2008.10579v1-membrane/imageFile12.png)

˘ ˘ ˘for $i ∈$ [$d$]. Let := $π$ and = for $i ∈$ [$d$]. Then the following all hold:

$$
\theta _{0}\theta _{i}g(\theta _{i- 1})
\mid \mid \mid d- 1\mid
$$

$$
\pi - \mid \theta _{i,x}\mid
$$

$6$ 1$,$ (30)

$$
\mid \mid \mid \mid \pi
$$

$i$=0

$$
d- 1
$$

$$
\pi - \pi - \theta _{i,x}\theta _{0,x}
$$

$> ,$ (31) 3

$$
\pi \pi d
$$

$i$=0

$$
\mid ()\mid d- 1
\mid \mid d- 1
\sum
$$

$$
\pi - d
\mid \mid sin\theta _{i,x}\theta _{j,x}
6
$$

$$
\mid \mid
$$

sin$θ_{0}_{,x},$

$$
\mid \mid \pi \pi \pi
$$

$j$=$i$+1$i$=0 ˘= $π$ + =$⇒$ = + (33)

$$
\theta _{0,x}O_{1}(\delta )\theta _{i,x}\theta _{i}O_{1}(i\delta ),
\mid \mid \mid \mid d- 1
$$

$$
\pi - \delta \mid \theta _{i,x}\mid
$$

$6 ,$ (34)= $π$ + =$⇒$

$$
\mid \mid \theta _{0,x}O_{1}(\delta )
\mid \mid \pi \pi
$$

$i$=0

$$
\mid \mid \mid \mid \pi - 2\theta _{i,x}
\mid \mid
$$

$6$ 1 $∀ i >$ 0$,$ (35)

$$
\mid \mid \pi ()
$$

1$−$1$6$ cos $∀ d >$ 2$,$ (36)

$$
\theta _{d,x}
\pi
$$

3$π$˘

$$
6
$$

$∀ i >$ 0$,$ (37)

$$
\theta _{i}
$$

$i$ + 3

$$
\pi
$$

˘ $∀ i >$ 0$.$ (38)

$$
>
\theta _{i}
$$

$i$ + 1

We first focus on proving Lemma 3.

Proof of Lemma 3. Suppose $f$ is differentiable at $x$. By (30) and (35), we have that $6$

$$
|,|\zeta _{i,x}|)
max(|\psi _{d,x}
$$

1 for any $i$ = 0$,...,d$. Hence we have the bound

$$
(
)
d- 1
\sum |
$$

$$
|sin\theta _{i,x}|
2|sin\theta _{d,x}
6
+\|x\|++|
\|h_{x}\|
||\zeta _{0,x}|\|x_{* }\|\|x_{* }\||\zeta _{i+1,x}|\|x_{* }\|
|\psi _{d,x}|\psi _{d,x}
d
\pi \pi
$$

2 $i$=0

$$
4+d/\pi
$$

$$
6
max(\|x\|,\|x_{* }\|).
d
$$

Combining equation (39) and Lemma 5, we attain

$$
6+- \|v_{x}\|\|h_{x}\|\|h_{x}v_{x}\|
\sqrt
$$

$$
\varepsilon 4+d/\pi Kd2
6++\|\eta \|
max(\|x\|,\|x_{* }\|)max(\|x\|,\|x_{* }\|)
dd
d/2
$$

2 2 2 2

$$
Cd
$$

+$6 \|η\|$ (40)

$$
max(\|x\|,\|x_{* }\|)
d
d/2
$$

$$
\sqrt
$$

3where in the last inequality we used $Kd ε 6$ 1 and set $C$ = 5 + 1$/π$.

$$
\sum s
$$

When $f$ is not differentiable at $x$, we have that by equation (12), we can write =

$$
v_{x}
c_{`}v_{`}
$$

$`$=1

$$
\sum s
$$

where each $>$ 0 and = 1. Applying (40) for differentiable points, we have that

$$
c_{`}
c_{`}
$$

$`$=1

$$
)
(
ss
\sum \sum
$$

2 $Cd$ 2

$$
Cd
+\|\eta \|=+\|\eta \|.
6\|6
max(\|x\|,\|x_{* }\|)max(\|x\|,\|x_{* }\|)
\|v_{x}\|
c_{`}\|v_{`}c_{`}
dd
d/2d/2
$$

2 22 2 $`$=1 $`$=1

![diagram: p26 region 42](2008.10579v1-membrane/imageFile13.png)

We now show that is Lipschitz for $x$ outside of a ball of the origin.

$$
h_{x}
$$

Proof of Lemma 4. Throughout the proof, we will use the following result from Lemma 5.1 in [35]:

$$
()
$$

$$
- |64max\|x- y\|.
,
|\theta _{0,x}\theta _{0,y}
\|x\|\|y\|
$$

For any $x,y 6$= 0, we have that

$$
\| \| \| \|
$$

$$
\| \|
$$

2sin$θ_{d,x}$ 2sin$θ_{d,y}$

$$
\|x_{* }\|
- \|6- \|x- y\|+
ˆx- \| ˆy\| \zeta _{0,y}|\|x_{* }\|+
\|h_{x}h_{y}\zeta _{0,x}
|\psi _{d,x}\psi _{d,y}
ddd
\| \| \pi \pi
$$

2 2 2

$$
︸︷︷︸
︷︷︸
︸
$$

($I$) ($II$)

$$
\| \| d- 1d- 1
\| \| \sum \sum \| \| sin\theta _{i,x}sin\theta _{i,y}
\|x_{* }\|
$$

ˆ $−$+ $.$

$$
ˆy\| \zeta _{i+1,x}x\zeta _{i+1,y}
\| \psi _{d,x}\psi _{d,y}
d
\| \| \pi \pi
$$

2 $i$=0 $i$=0

$$
︸
︷︷︸
(III)
$$

We will focus on bounding each of the individual quantities.

(I): The triangle inequality gives $− | 6 − |$ + $− |.$ By

$$
|\psi _{d,x}\zeta _{d,x}\psi _{d,y}\zeta _{d,y}|\psi _{d,x}||\zeta _{d,x}\zeta _{d,y}|\zeta _{d,y}||\psi _{d,x}\psi _{d,y}
$$

(30) and (35), we have $|} 6$ 1 for all $x 6$= 0. In addition,

$$
max{|\psi _{d,x}|,|\zeta _{d,x}
$$

2 $− |.$ (42)

$$
- |=
|\theta _{d,x}\theta _{d,y}
|\psi _{d,x}\psi _{d,y}
\pi '
$$

Since $g$ ($θ$) $∈$ [0$,$1] for all $θ ∈$ [0$,π$] and = we have that $| 6 |.$

$$
\theta _{i,x}g(\theta _{i- 1,x}),|\theta _{i,x}- \theta _{i,y}|\theta _{i- 1,x}- \theta _{i- 1,y}
$$

Repeatedly applying this inequality for each $i ∈$ [$d$], we attain

$$
()
$$

$$
- |6- |64max,\|x- y\|(43)
|\theta _{0,x}\theta _{0,y}
|\theta _{d,x}\theta _{d,y}
\|x\|\|y\|
$$

where we used (41) in the last inequality. Hence combining (42) and (43), we get

$$
()
$$

2 8 1 1

$$
- |=
- |6max,\|x- y\|.
|\psi _{d,x}\psi _{d,y}
|\theta _{d,x}\theta _{d,y}
\pi \pi \|x\|\|y\|
$$

Using the definition of another application of (41) gives

$$
\zeta _{0,x},
()
$$

$d$ 4$d$ 1 1

$$
- |6- |6max,\|x- y\|.
|\zeta _{0,x}\zeta _{0,y}|\theta _{0,x}\theta _{0,y}
\pi \pi \|x\|\|y\|
$$

8+4$d$Combining our results, if := then$K_{1}$

$$
\pi (
)
$$

$$
\|x_{* }\|
$$

max

$$
(I)6,\|x- y\|(44)
$$

$K_{1}$

$$
d
\|x\|\|y\|
$$

(II): Observe that we have

$$
()
$$

22sin$θ_{d,x}$ 2sin$θ_{d,x}$ ˆ$x$ = ˆ$y$ + $\|$ˆ$x −$ ˆ$y\|$$O_{1}$

$$
\pi \pi \pi (
)
()()
$$

2 22sin$θ_{d,y}$

$$
+- |ˆy+\|ˆx- ˆy\|
$$

= $O_{1} O_{1}$

$$
|\theta _{d,x}\theta _{d,y}
\pi \pi \pi
$$

$$
(
)()
$$

2 4 1 12sin$θ_{d,y}$ ˆ$y$ + $·$ 4 + $,$= max $\|x − y\|$$O_{1}$

$$
\pi \pi \pi \|x\|\|y\|
$$

where the second line follows from $6$ and the third from (43) and $\|$ˆ$x−$ ˆ$y\| 6$$|$sin$θ_{1} −$sin$θ_{2}| |θ_{1} −θ_{2}|$

$$
()
$$

1 12max $, \|x − y\|$. Thus if := 12$/π$,$K_{2}$

$$
\|x\|\|y\|
)
(
$$

$$
\|x_{* }\|
\|x- y\|(45)
$$

($II$) $6$ max $,$$K_{2}$

$$
d
\|x\|\|y\|
$$

(III): The final term follows from

$$
())
(
d- 1
d- 1
\sum \sum
$$

8$d$ 1 1

$$
sin\theta _{i,x}sin\theta _{i,x}
\|x- y\|
$$

ˆ = ˆ + max $,$

$$
\zeta _{i+1,x}x\zeta _{i+1,x}xO_{1}
\psi _{d,x}
\psi _{d,y}
$$

$$
\pi \pi \pi \|x\|\|y\|
$$

$i$=0 $i$=0

$$
()
)(
d- 1
\sum
$$

4$d$ 8$d$ 1 1

$$
sin\theta _{i,y}
=\|x- y\|
$$

ˆ + + max $,$

$$
\zeta _{i+1,x}xO_{1}
\psi _{d,y}
$$

$$
\pi \pi \pi \|x\|\|y\|
$$

$i$=0

$$
(
)
d- 1
d- 1
\sum \sum d- i- 1
$$

$$
sin\theta _{i,y}
$$

= ˆ$x$ + $− |$

$$
\zeta _{i+1,y}O_{1}|\theta _{i- 1,x}\theta _{i- 1,y}
\psi _{d,y}
\pi \pi \pi
$$

$i$=0$i$=0

$$
)()
(
$$

8$d$ 1 14$d$

$$
+max,\|x- y\|
$$

+ $O_{1}$ 2

$$
\pi \pi \|x\|\|y\|
)()
(
d- 1
$$

$$
\sum
$$

4$d$ 8$d$ 1 12$d$

$$
sin\theta _{i,y}
$$

= ˆ$x$ + + + max $, \|x − y\|$

$$
\zeta _{i+1,y}O_{1}
\psi _{d,y}
$$

$$
\pi \pi \pi \pi \|x\|\|y\|
$$

$i$=0

$$
()()
d- 1
$$

$$
\sum
$$

2$d$ 2$d$ 4$d$ 8$d$ 1 1

$$
sin\theta _{i,y}
$$

ˆ$y$ + + + + max $, \|x − y\|$=

$$
\zeta _{i+1,y}O_{1}
\psi _{d,y}
$$

$$
\pi \pi \pi \pi \pi \|x\|\|y\|
$$

$i$=0

where the first line follows from equations (42) and (43) and using $6$ 1 for any $i$ =

$$
|sin\theta _{i,x}\zeta _{i+1,x}|
d- i- 1
$$

0$,...,d−$1 and $x$; the second line from (43); the third from $| 6 |$;

$$
|\zeta _{i+1,x}- \zeta _{i+1,y}|\theta _{i- 1,x}- \theta _{i- 1,y}
\pi \sum d- 1
$$

1($d−i−$1) =the fourth from $| 6 |$, (41), and ($d−$1)$d$; and the fifth

$$
|\theta _{i- 1,x}- \theta _{i- 1,y}|\theta _{0,x}- \theta _{0,y}
$$

$i$=0 2

$$
()
$$

1 1from $| 6$ 1 for all $i$ = 0$,...,d −$ 1 and $\|$ˆ$x −$ ˆ$y\| 6$ 2max $\|x − y\|$.Combining

$$
,
|sin\theta _{i,y}\zeta _{i+1,y}
\|x\|\|y\|
$$

28$d$+2$d$ 6$d$our results, we have that if := then+$K_{3}$ 2

$$
\pi \pi )
(
$$

$$
\|x_{* }\|
(III)6,\|x- y\|.(46)
$$

max$K_{3}$

$$
d
\|x\|\|y\|
$$

Thus for all $x,y 6$= 0, using equations (44), (45), and (46), we conclude that

$$
- \|6(I)+
\|x- y\|+(II)+(III)
\|h_{x}h_{y}
d
$$

$$
))
((
$$

$$
6,+1\|x- y\|
$$

+ +$\|x_{∗}\|$($K_{1} K_{2} K_{3}$)max

$$
d
\|x\|\|y\|
$$

$$
(())
$$

2(2$d$ + (10$π$ + 8)$d$ + 1 1 1

$$
20\pi )\|x_{* }\|
=max,+\|x- y\|.
2dd
\|x\|\|y\|
$$

$π$ 2 2

Then if $x,y /∈ B$(0$,r$), we can further conclude that

$$
(
)
$$

2(2$d$ + (10$π$ + 8)$d$ + 1

$$
20\pi )\|x_{* }\|
- \|6+
\|x- y\|.
\|h_{x}h_{y}
2dd
$$

$rπ$ 2 2

We can now show that approximates any $∈ ∂f$($x$), which is formalized in Lemma 5. Prior

$$
h_{x}v_{x}
$$

to this proof, we define

T:= Λ $−$

$$
x_{* }).
w_{x}(\Lambda _{x}x
\Lambda _{x_{* }}
x
\Phi _{x_{d},x_{* ,d}}
$$

The key idea is that the RRCP and WDC together imply $≈$ and the WDC further implies

$$
v_{x}w_{x}
$$

$≈$ which is shown in Lemma 19.

$$
w_{x}h_{x}
$$

T TProof of Lemma 5. Suppose $f$ is differentiable at $x$ so that = where = Λ $A$

$$
v_{x}\Lambda _{x}x- v_{x}- q_{x}v_{x}
x
(A_{x_{d}}
x_{d}
$$

T Tand = Λ $A η$. Observe that

$$
x_{* })q_{x}
\Lambda _{x_{* }}
x
A_{x_{* ,d}}
x_{d}
$$

$$
\| \| \| \|
$$

T TT T

$$
\| \| \| \| - \|x\|+\Lambda (A
- - 6\Lambda (A
\|x_{* }\|.
$$

)Λ$_{x}$

$$
\|v_{x}w_{x}\|
$$

)Λ$_{x}_{∗}$

$$
x
x
\Phi _{x_{d},x_{* ,d}}
A_{x_{* ,d}}
I_{n_{d}}
A_{x_{d}}
x_{d}
x_{d}
$$

By the local linearity of $G$, for sufficiently small $z ∈$

$$
k
$$

the RRCP, we have for sufficiently small $z,$ ˜$z ∈ R$ ,

and

$$
k
$$

$R$ , we have $G$($x$+$z$)$−G$($x$) = Λ$_{x}z$. Hence by

T 2 2$|〈$($A − 6 \|z\|$

$$
)\Lambda _{x}z,\Lambda _{x}z〉|L\varepsilon \|\Lambda _{x}\|
A_{x_{d}}I_{n_{d}}
x_{d}
$$

$$
|〈(A
- ˜z〉|6\|\|z\|\|˜z\|.
)\Lambda _{x}z,\Lambda _{x_{* }}L\varepsilon \|\Lambda _{x}\|\|\Lambda _{x_{* }}
A_{x_{* ,d}}
\Phi _{x_{d},x_{* ,d}}
x_{d}
$$

$$
k
$$

Since this holds for any $z,$ ˜$z ∈ R$ , we conclude that

$$
\| \|
$$

$$
\| \|
$$

$6$ andΛ ($A −$

$$
L\varepsilon \|\Lambda _{x}\|
$$

)Λ$_{x}$

$$
x
A_{x_{d}}I_{n_{d}}
x_{d}
$$

$$
\| \|
$$

$$
\| \| 6\|.
$$

Λ ($A −$

$$
L\varepsilon \|\Lambda _{x}\|\|\Lambda _{x_{* }}
$$

)Λ$_{x}_{∗}$

$$
x
A_{x_{* ,d}}\Phi _{x_{d},x_{* ,d}}
x_{d}
$$

This implies

$$
()
$$

$$
- 6L\varepsilon +\|
$$

$\|v_{x} w_{x}\| \|$Λ$_{x}\|$ max($\|x\|,\|x_{∗}\|$)

$$
\|\Lambda _{x}\|\|\Lambda _{x_{* }}
()
d
$$

$$
62L\varepsilon +\varepsilon max(\|x\|,\|x_{* }\|)
$$

where the last inequality follows by the WDC. Furthermore, by Lemma 19, we have that

$$
\sqrt
$$

390$d ε$

$$
- 6
\|w_{x}h_{x}\|
max(\|x\|,\|x_{* }\|).
d
$$

Combining these two bounds, we have

$$
- 6- +- \|v_{x}h_{x}\|\|v_{x}w_{x}\|\|w_{x}h_{x}\|
()
$$

$d$ 3

$$
\sqrt
$$

(1 + 2$ε$) $d$ $6 ε$ 2$L$ + 90

$$
max(\|x\|,\|x_{* }\|)
dd
$$

2 2 3

$$
\sqrt d
6\varepsilon K
max(\|x\|,\|x_{* }\|)
d
$$

$$
d
$$

for some universal constant $K$ where the third inequality follows since 2$εd 6$ 1 =$⇒$ (1 + 2$ε$) $6$

$$
2\varepsilon dd
$$

$e 6$ 1 + 4$εd$ so choosing $ε <$ 1$/$(4$d$) implies (1 + 2$ε$) $6$ 2. Lastly, to bound observe that

$$
\|q_{x}\|,
$$

$$
\sqrt \sqrt
$$

13 1 2 $6 6$ (1 + $Lε$) $\|η\| 6 \|η\|.$1 + $6$

$$
\|q_{x}\|\Lambda _{x}\|\|\eta \|
L\varepsilon \|\Lambda _{x}\|\|\eta \|
\|A_{x_{d}}
d/2d/2
$$

12 2 2

![diagram: p29 region 46](2008.10579v1-membrane/imageFile14.png)

where in the second inequality we used (21) and in the third inequality we used (20). The last

$$
\sqrt
$$

13inequality follows by choosing $ε$ such that (1 + $Lε$) $6$ 2. Then we can combine (48) and (49) 12 to obtain

$$
\sqrt
$$

$$
d\varepsilon 2
$$

$− 6 \|η\|.$

$$
- +6K+
\|v_{x}h_{x}\|
\|v_{x}h_{x}\|\|q_{x}\|max(\|x\|,\|x_{* }\|)
d
d/2
$$

$$
\sum s
$$

When $f$ is not differentiable at $x$, we can use (12) to write = where $>$ 0 for $` ∈$ [$s$].

$$
v_{x}
c_{`}v_{`}c_{`}
$$

$`$=1 Moreover, note that for each , there exists a direction such that = lim $∇f$($x$ + )+

$$
v_{`}w_{`}v_{`}\delta _{`}w_{`}
$$

$$
\delta _{`}
$$

and $f$ is differentiable at $x$ + for sufficiently small $>$ 0. Appealing to the continuity of

$$
h_{x}
\delta _{`}w_{`}\delta _{`}
$$

for $x 6$= 0, we obtain

$$
ss
\| \| \sum \sum \| \| - 6
$$

$−$ = lim $∇f$($x$ + ) $−$

$$
\|v_{x}h_{x}\|
h_{x}\|
h_{x}\| c_{`}\|v_{`}c_{`}\| \delta _{`}w_{`}
$$

+$→$0

$$
\delta _{`}
$$

$`$=1 $`$=1

$$
s
\sum
$$

= lim $\|∇f$($x$ + ) $− \|$

$$
c_{`}\delta _{`}w_{`}
h_{x+\delta _{`}w_{`}}
$$

+$→$0

$$
\delta _{`}
$$

$`$=1

$$
s
\sum
$$

= lim $− \|$

$$
c_{`}
\|v_{x+\delta _{`}w_{`}}h_{x+\delta _{`}w_{`}}
$$

+$→$0

$$
\delta _{`}
$$

$`$=1

$$
)
(
\sqrt s
$$

$$
\sum
$$

$ε$ 2

$$
d
$$

$$
\|\eta \|
6K
max(\|x\|,\|x_{* }\|)
c_{`}
d
d/2
$$

2 2 $`$=1

$$
\sqrt
$$

3 $ε$ 2

$$
d
$$

$$
=K\|\eta \|.
max(\|x\|,\|x_{* }\|)
d
d/2
$$

We now establish a technical result that shows is approximated by Prior to this proof,

$$
w_{x}h_{x}.
$$

we highlight the following result that summarizes some useful bounds from [33]:

$−$4 2Lemma 18 (Results from Lemma 5 in [33]). Fix 0 $< ε < d$ (1$/$16$π$) and let $d >$ 2. Let satisfy

$$
W_{i}
k
$$

the WDC with constant $ε$ for $i$ = 1$,...d$. Then for any non-zero $x,y ∈ R$ , the following hold:

$$
\sqrt \| \|
$$

$$
d\varepsilon \| \|
$$

$$
y- 624
\|y\|,
$$

$$
\Lambda _{y}h_{x,y}\| x
d
$$

$$
\|x\|\|y\|,
y〉>
〈\Lambda _{x}x,\Lambda _{y}
d
$$

4$π$ 2

$$
\mid \mid \mid \mid \|
\|y\|
\|y\|
\|y_{d}
\mid \mid 68d\varepsilon - ,
\mid \mid \|\|x\|\|x\|
\|x_{d}
\sqrt - |64d\varepsilon |\theta _{d}\theta _{d}
$$

$$
\circd
$$

˜ is defined aswhere := ), := $g$ ($∠$($x,y$)), and the vector

$$
h_{x,y}
\theta _{d}∠(x_{d},y_{d}\theta _{d}
$$

$$
[()()]
d- 1
d- 1d- 1
\sum
$$

$$
\pi -
$$

$$
\pi - \|y\|
\theta _{j}
\theta _{i}sin\theta _{i}
$$

$$
y+x
h_{x,y}
d
\pi \pi \pi \|x\|
$$

2 $i$=0 $j$=$i$+1$i$=0

with $θ_{0}$ := $∠$($x,y$) and $θ_{i}$ :=

$$
g(\theta _{i- 1})
$$

for $i ∈$ [$d$].

We now establish that is approximated by

$$
w_{x}h_{x}.
$$

![diagram: p30 region 50](2008.10579v1-membrane/imageFile15.png)

$−$4 2Lemma 19. Fix 0 $< ε < d$ (1$/$16$π$) . Let satisfy the WDC with constant $ε$ for $i$ = 1$,...d$.

$$
W_{i}
k
$$

For any non-zero $x ∈ R$ , we have 3

$$
\sqrt
$$

90$d$

$$
- 6
\|w_{x}h_{x}\|\varepsilon max(\|x\|,\|x_{* }\|)
d
$$

2 where is defined by (47).

$$
w_{x}
k
$$

Proof. Fix $x ∈ R \{$0$}$ and set := ). Note that by the definition of and ,

$$
\Phi _{z,w}
\theta _{d}∠(x_{d},x_{* ,d}M_{ˆz↔ˆw}
$$

can be written as

$$
w_{x}
\pi - x_{* }\|
$$

2$θ_{d}$ 2sin$θ_{d}$

$$
\|\Lambda _{x_{* }}
$$

T T T= Λ $−$ Λ $−$ Λ

$$
w_{x}\Lambda _{x}xx_{* }\Lambda _{x}x
\Lambda _{x_{* }}
xxx
\pi \pi \|\Lambda _{x}x\|
$$

where := )$.$ Observe that

$$
\theta _{d}∠(x_{d},x_{* ,d}
\| \| \| \| \| \| \| \| \pi - \pi -
$$

$$
2\theta _{d}2\theta _{d}
$$

TT ˜

$$
\| \| \| \| -
$$

$x$ + Λ

$$
- 6\Lambda - x_{* }
\|w_{x}h_{x}\|\Lambda _{x}x
\Lambda _{x_{* }}
x
x
h_{x,x_{* }}\| \| \| \| d
\pi \pi
$$

$$
\| \| \| \|
$$

$$
x_{* }\|
\|x_{* }\|
$$

2sin$θ_{d}$ 2sin$θ_{d}$

$$
\|\Lambda _{x_{* }}
$$

$$
\| \|
$$

$$
\Lambda - x
\Lambda _{x}x
x
\| \| d
\pi \pi \|x\|
$$

$$
\|\Lambda _{x}x\|
$$

$$
\circd
$$

˜where := $g$ and is defined in (54).

$$
(∠(x,x_{* }))
\theta _{d}
h_{x,x_{* }}
$$

We focus on bounding each individual quantity separately. For the first term, we have that by (50) in Lemma 18,

$$
()
$$

$$
\sqrt
$$

1 24$d$ TΛ = $x$ + (55)

$$
\Lambda _{x}x
O_{1}\varepsilon max(\|x\|,\|x_{* }\|).
x
dd
$$

2 2 For the second term, observe that

$$
)
(
\sqrt
$$

$$
\varepsilon \pi - \pi -
$$

24$d$

$$
2\theta _{d}2\theta _{d}
$$

T ˜Λ = + $\|x\|$

$$
x_{* }O_{1}
\Lambda _{x_{* }}h_{x,x_{* }}
x
d
\pi \pi
$$

$$
()))
((
\sqrt
$$

$$
\sqrt \pi - \varepsilon
$$

2 24$d$

$$
2\theta _{d}
$$

$$
=+\cdot 4d\varepsilon \|x\|
$$

+$O_{1}$ $O_{1}$

$$
h_{x,x_{* }}
d
\pi \pi
$$

$$
(
)
$$

$$
\sqrt
$$

$π −$ 8$d$ (1 + $d/π$) 24$d$

$$
2\theta _{d}
$$

˜= + $·$ (56)+

$$
O_{1}\varepsilon max(\|x\|,\|x_{* }\|)
h_{x,x_{* }}
dd
\pi \pi
$$

2 2 ˜where in the first equality we used (50) and in the second we used (53) and the fact that $\| 6$

$$
\|h_{x,x_{* }}
- d
$$

2 (1 + For the final term, observe that

$$
d/\pi )\|x_{* }\|.
(())
\sqrt
$$

31 24$d ε$

$$
x_{* }\|x_{* }\|
$$

2sin$θ_{d}$2sin$θ_{d}$

$$
\|\Lambda _{x_{* }}\|\Lambda _{x_{* }}
$$

$$
x+\|x\|
\Lambda _{x}x
$$

$O_{1}$

$$
x
dd
\pi \pi
$$

$$
\|\Lambda _{x}x\|\|\Lambda _{x}x\|
)
(
\sqrt
$$

31 $ε$24$d$ 2

$$
x_{* }\|x_{* }\|
$$

2sin$θ_{d}$

$$
\|\Lambda _{x_{* }}\|\Lambda _{x_{* }}
\|x\|
$$

= $x$ + $O_{1}$

$$
dd
\pi \pi
$$

$$
\|\Lambda _{x}x\|\|\Lambda _{x}x\|
()
(())
\sqrt
$$

34 $·$ 24$d$1 $ε$

$$
\|x_{* }\|\|x_{* }\|
$$

2sin$θ_{d}$ = $x$ ++ 8$dε$

$$
O_{1}\|x_{* }\|
$$

$O_{1}$

$$
dd
\pi \|x\|\|x\|
$$

2 $π$2

$$
()
\sqrt
$$

31 16$dε$ 4 $·$ 24$d ε$

$$
\|x_{* }\|
$$

2sin$θ_{d}$

$$
=x+\|x\|+
O_{1}\|x_{* }\|
ddd
\pi \|x\|
$$

2 $π$2 $π$2

$$
(
())()
\sqrt \sqrt
$$

318$d ε$ 16$dε$ 4 $·$ 24$d ε$

$$
\|x_{* }\|
$$

2sin$θ_{d}$ =

$$
+x+\|x\|+
O_{1}O_{1}\|x_{* }\|
ddd
\pi \pi \|x\|
$$

2 $π$2 $π$2

$$
(
())
$$

$$
\sqrt
$$

11 24$d$ + 4 $·$ 24$d$

$$
\|x_{* }\|
$$

2sin$θ_{d}$ $x$ += $O_{1}$

$$
\varepsilon max(\|x\|,\|x_{* }\|).
dd
\pi \|x\|\pi
$$

2 2 (57)

where the first line follows from (50); the second line from $|$sin$θ| 6$ 1; the third line from (52); and the fifth line from (53). Combining equations (55), (56), and (57) achieves the desired result.

2.5.3 Proofs for Section 2.4.3

We first establish Lemma 8 which shows that the zeros of occur near and a particular negative

$$
h_{x}x_{* }
$$

multiple Here the lemma is stated more precisely.

$$
x_{* }.
- \rho _{d}
$$

$$
\sqrt
$$

6Proposition 2. Suppose $β >$ 0 obeys 24$πd$ $β 6$ 1 and define as in (11). If $x ∈$ , then either

$$
S_{\beta }S_{\beta }
$$

or

$$
682\pi d\beta and|\|x\|- 6838\pi d
$$

$|θ_{0}_{,x}| \|x_{∗}\|| β\|x_{∗}\|$

$$
\sqrt \sqrt
$$

2 4 8

$$
- \pi |624\pi d\beta and|\|x\|- 63517d
$$

$|θ_{0}_{,x} \|x_{∗}\|| β\|x_{∗}\|.$

$$
\rho _{d}
$$

In particular, we have

$$
\sqrt
$$

2 9 2 12

$$
\subset d\cup d
$$

$B$($x_{∗},$70000$π β\|x_{∗}\|$) $x_{∗},$77422$π$

$$
\beta \|x_{* }\|).
S_{\beta }B(- \rho _{d}
$$

Additionally, $→$ 1 as $d → ∞$.

$$
\rho _{d}
$$

Proof of Proposition 2. Without loss of generality, let = and = 1 where is the first

$$
x_{* }e_{1}\|x_{* }\|e_{1}
k
$$

standard basis vector in $R$ . We also let $x$ = + where = For$r$ cos$θ_{0}e_{1} r$ sin$θ_{0}e_{2} θ_{0} ∠$($x,x_{∗}$). simplicity, we use the shorthand notation = for $i ∈$ [$d$]. Set

$$
\theta _{i}\theta _{i,x}
$$

$$
(
)()
()()
d- 1
d- 1
d- 1
\sum
$$

$$
\pi - \pi - \pi - \pi - \theta _{j}
\theta _{i}sin\theta _{i}
$$

2$θ_{d}$ 2sin$θ_{d}$ 2$θ_{d}$ $ξ$ = +and $α$ = $.$

$$
\pi \pi \pi \pi \pi \pi
$$

$i$=0 $j$=$i$+1$i$=0 Note that we can write 1 + ($r − α$)ˆ$x$)=

$$
(- \xi ˆx_{* }
h_{x}
d
$$

2 Then if $x ∈$ , we have that

$$
S_{\beta }
$$

$$
- \alpha )|6\beta M
|- \xi +
$$

cos$θ_{0}$($r$ (58)

$$
- \alpha )|6\beta M
$$

$|$sin$θ_{0}$($r$ (59)

where $M$ := max($r,$1). To prove the Proposition, we first show that it is sufficient to only consider the small and large angle case. Then, we show that in the small and large angle case, $x ≈$ and $x ≈$

$$
x_{* }x_{* },
- \rho _{d}
$$

respectively. We begin by proving that $6$ 6$d$ for any $x ∈$ .

$$
max(\|x\|,\|x_{* }\|)
S_{\beta }
$$

Bound on maximal norm in : It suffices to show that $r 6$ 6$d$. Suppose $r >$ 1 since if

$$
S_{\beta }
\sqrt \sqrt \sqrt
$$

$>$ 1$/$ 2 or $>$ 1$/$ 2. If $>$ 1$/$ 2$r 6$ 1, the result is immediate. Then either $|$sin$θ_{0}| |$cos$θ_{0}| |$sin$θ_{0}|$ then (59) gives

$$
\sqrt \sqrt
$$

$|r − α| 6$ 2$βr$ =$⇒$ (1 $−$ 2$β$)$r 6 |α|.$

But

$|α| 6$

$$
\mid ()\mid )
(
d- 1
d- 1
\mid \mid \sum
$$

$$
\pi -
$$

$$
d
\pi - \mid \mid sin\theta _{i}\theta _{i}
2\theta _{d}
$$

$|$ + $6$ 1 +

$$
\mid \mid
$$

$|$sin$θ_{d}$

$$
\mid \mid
$$

$π π π π π$$j$=$i$+1$i$=0

![diagram: p32 region 53](2008.10579v1-membrane/imageFile16.png)

![diagram: p32 region 54](2008.10579v1-membrane/imageFile17.png)

where the second inequality used equations (32) and (35). Thus

$$
)
(
d
$$

$$
d
\pi \sqrt 62+d62d
$$

$r 6 6$ 2 1 +

$$
\pi
$$

1 $−$ 2$β$

$$
\sqrt
$$

provided $β <$ 1$/$4 and $d >$ 2. If $>$ 1$/$ 2, then (58) gives$|$cos$θ_{0}|$

$$
\sqrt \sqrt \sqrt |r- \alpha |6
2(\beta r+|\xi |)=\Rightarrow (1- 2\beta )r62|\xi |+\alpha .
$$

But by (30),

$$
\mid ()\mid )
(
d- 1
\mid \mid
$$

$$
\pi - \pi - \mid \mid \theta _{i}
2\theta _{d}
$$

$6$ 1 since $∈$ [0$,π/$2] $∀ i$

$$
|\xi |=
\mid \mid \theta _{i}
\mid \mid \pi \pi
$$

$i$=0

$>$ 1$.$

Hence if $β <$ 1$/$4,

$$
\sqrt \sqrt \sqrt
$$

2 + 2$d$

$$
\sqrt
$$

2 + 4$d 6$ 2$d$ + 4$d 6$ 6$d.$

$$
r662
$$

1 $−$ 2$β$

Thus in any case, $r 6$ 6$d$ =$⇒ M 6$ 6$d$. We now show that it is sufficient to only consider the small angle case $θ_{0}$ angle case $≈ π$.$θ_{0}$ Sufficiency: We have three possible cases:

$≈$ 0 and the large

$\cdot$ 4

$$
648\pi d\beta :
$$

sin$θ_{0}$ 4 4Then we have that = $β$) or = $π$ + $β$).$θ_{0} O_{1}$(82$πd θ_{0} O_{1}$(82$πd$

$$
\sqrt
$$

4$>$ 48$πd β$ and $|r − α| > βM$: Observe that due to equation (59), we have that

$$
\cdot
$$

sin$θ_{0}$

$$
\beta M
|r- \alpha |6
$$

$.$ Thus using this inequality in equation (58), we have that sin$θ_{0}$ 2$βM$ 2$βM$ 12$d$ 1

$$
\beta M
$$

$$
666=d
|\xi |6\beta M+
$$

4 448$πd β$ 48$πd$ 4$π$sin$θ_{0}$ sin$θ_{0}$

4where we used the assumption $>$ 48$πd β$ in the second to last inequality and $M 6$ 6$d$sin$θ_{0}$ in the last inequality. In addition, (36) implies

$$
\mid )\mid (
\mid \mid
$$

1$−$1

$$
\mid \mid |\pi - |>
$$

$π −$ 2cos $>$

$$
2\theta _{d}
\mid \mid \pi
$$

$$
.
$$

Combining this inequality with (31) and (60), we obtain

$$
()
$$

1 $π −$ 1$θ_{0}$ $−$3 $−$3

$$
d6|\xi |6d.
2\pi \pi 4\pi
$$

$$
\sqrt \pi
$$

From this, we can conclude that $> .$ Moreover, since $|r − α| > βM$, then (59) implies$θ_{0}$ 2

$$
\sqrt \sqrt \pi
$$

$6 β$ so we must have that = $π$ + $β$) since $>$ and $β <$ 1.that $|$sin$θ_{0}| θ_{0} O_{1}$(2 $θ_{0}$ 2

$$
\sqrt
$$

$\cdot |r − α| 6 βM$ : Then (58) implies

$$
\sqrt
$$

$|ξ| 6$ 2 $βM.$

But note that by (31),

$$
()
(
)
d- 1
$$

$$
\pi - \pi - (\pi - )(\pi - \theta _{i}\theta _{0})
2\theta _{d}2\theta _{d}
\xi =>
.
$$

$$
\pi \pi d\pi
$$

$i$=0

![diagram: p33 region 55](2008.10579v1-membrane/imageFile18.png)

1In addition, since $|π − | >$ by (61), we have

$$
2\theta _{d}
$$

$$
|(\pi - )(\pi - |\pi - \theta _{0})|\theta _{0}|
2\theta _{d}
|\xi |>>
$$

3 2 3 2

$$
d\pi 2d\pi
$$

which implies

$$
\sqrt \sqrt
$$

3 2 4 2$|π − 6$ 4$d π βM 6$ 24$d π β.$

$$
\theta _{0}|
$$

$$
\sqrt
$$

4 2Thus = $π$ + $π β$).$θ_{0} O_{1}$(24$d$

Since only one of these situations can hold, it suffices to consider either the small angle case

$$
\sqrt
$$

4 4 2= $β$) or the large angle case = $π$ + $π β$). Now, we show that in the small$θ_{0} O_{1}$(82$πd θ_{0} O_{1}$(24$d$ angle case, $x ≈$ while in the large angle case, $x ≈$

$$
x_{* },x_{* }.
- \rho _{d}
$$

4Small Angle Case: Assume = where we set $δ$ := 82$πd β$. Note that since $6 6 δ$$θ_{0} O_{1}$($δ$) $θ_{i} θ_{0}$ for each $i$, we have that

$$
))
((
d- 1
d
$$

$$
\pi - \delta 2d\delta \theta _{i}
$$

$>$ 1 $−$ = 1 + $O_{1}$

$$
\pi \pi \pi
$$

$i$=0 provided $dδ/π 6$ 1$/$2. Hence

$$
(
)
(
)())())
((
d- 1
$$

$$
\pi - \pi - 2\delta 2d\delta \theta _{i}
2\theta _{d}
$$

$ξ$ = $>$ 1 + 1 +$O_{1} O_{1}$

$$
\pi \pi \pi \pi
$$

$i$=0

where we used (33) in the second inequality. In addition, $| 6 | 6 δ$ and (32) imply that

$$
|sin\theta _{d}|\theta _{d}
\mid ()\mid d- 1
\mid d- 1
\mid \sum
$$

$$
\pi - d
\mid \mid \theta _{j}
$$

sin$θ_{i}$

$$
|6d\delta .
6
\mid \mid
$$

$|$sin$θ_{d}$

$$
\mid \mid \pi \pi \pi
$$

$j$=$i$+1$i$=0 Hence

$$
()
())(())
(
d- 1
d- 1
\sum
$$

$$
\pi - \pi - 2\delta
$$

$$
\theta _{j}
$$

sin$θ_{i}$2sin$θ_{d}$ 2$θ_{d}$ $α$ = + + 1 += $O_{1} O_{1}$($dδ$)$O_{1}$

$$
\pi \pi \pi \pi 3\pi \pi
$$

$j$=$i$+1$i$=0

$$
()
$$

2(4 + 3$dπ$ + 6$d$ )$δ$ = $O_{1}$ 3$π$

where we used $δ <$ 1 in the last equality. Thus since $| − ξ$ + $− α$)$| 6 βM$ and $M 6$ 6$d$, wecos$θ_{0}$($r$ attain

$$
))(())(())
((
$$

22$dδ$ (4 + 3$dπ$ + 6$d$ )$δ$2$δ$ 1 + + (1 + $r$ + =$−$ 1 + $O_{1} O_{1}$($δ$)) $O_{1} O_{1}$(6$dβ$)$.$$O_{1}$

$$
\pi \pi 3\pi
$$

Rearranging, this gives

$$
()
$$

2 22$dδ$ 2$δ$ 16$dδ$ (4 + 3$dπ$ + 6$d$ )$δ$ $r −$ 1 = + + + ($δ$ + 1) + +$O_{1} O_{1}$(12$dβ$) $O_{1}$(6$dβ$)

$$
\pi \pi \pi 3\pi ()
$$

(12$d$ + 12 + 48$d$)$δ$ + (2$ε$ + 1)(4 + 3$πd$ + 12$d$)$δ$ = + 18$dβ$$O_{1}$ 3$π$ = + 18$dβ$)$O_{1}$(10$dδ$ 5= $β$)$O_{1}$(838$πd$

![diagram: p34 region 56](2008.10579v1-membrane/imageFile19.png)

where we used $δ <$ 1$/$2 and $d >$ 2 in the second to last equality and the definition of $δ$ in the final equality.

$$
\sqrt
$$

4 2Large Angle Case: Assume = $π$ + where $δ$ := 24$d π β.$ We first prove that $α$ is$θ_{0} O_{1}$($δ$) ˘close to . Recall that = + Then by the mean value theorem:$O_{1}$($dδ$).

$$
\rho _{d}\theta _{d}\theta _{d}
$$

$$
- |6- |6d\delta |sin\theta _{d}sin\theta _{d}|\theta _{d}\theta _{d}
()
\sum
$$

$$
d- 1
d- 1
\pi - \theta _{j}
$$

sin$θ_{i}$˘so = + Let := and note that$O_{1}$($dδ$).sin$θ_{d}$ sin$θ_{d}$ Γ$_{d}$

$$
\rho _{d}
$$

$i$=0 $j$=$i$+1

$$
\pi \pi )
(
$$

$$
\pi - 2\theta _{d}
$$

$.$ In [33], it was shown that if $d δ/π 6$ 1, then $| 6 d$ and

$$
\Gamma _{d}|\Gamma _{d}
\pi
$$

˘2sin$θ_{d}$

$$
\pi
$$

$$
()
d- 1
d- 1
\sum
$$

$$
\pi - \theta _{j}
$$

sin$θ_{i}$ 3= + $δ$)$.$$O_{1}$(3$d$Γ$_{d}$

$$
\pi \pi
$$

$j$=$i$+1$i$=0

$$
\sqrt
$$

12By the condition, $d δ/π 6$ 1, we require $β 6 .$ Thus for sufficiently small $β$, we have 624$πd$

$$
()
()
d- 1
d- 1
\sum
$$

$$
\pi - \pi - \theta _{j}
$$

sin$θ_{i}$2sin$θ_{d}$ 2$θ_{d}$ $α$ = +

$$
\pi \pi \pi \pi
$$

$j$=$i$+1$i$=0

$$
()
()()
$$

$$
()
2d\delta \pi - 2d\delta
$$

2sin$θ_{d}$ 2$θ_{d}$ 3= + + + + $δ$)$O_{1} O_{1} O_{1}$(3$d$Γ$_{d}$

$$
\pi \pi \pi \pi
$$

$$
(
)
()())
(
$$

$$
()
\pi - 2d\delta 2d\delta 6d\delta 2\theta _{d}
$$

3= + + + 3$d δ$ +$O_{1} O_{1}$ $O_{1}$ $O_{1}$

$$
\rho _{d}\Gamma _{d}
\pi \pi \pi \pi
$$

$$
()()()
$$

2 4 2

$$
()
2d\delta 2d\delta 6d\delta
$$

3= + + + 3$d δ$ +$O_{1} O_{1} O_{1} O_{1}$

$$
\rho _{d}
\pi \pi \pi
$$

4= + $δ$)$.$$O_{1}$(7$d$

$$
\rho _{d}
$$

We now prove $r$ is close to . Since $x ∈$ ,

$$
\rho _{d}S_{\beta }
$$

$$
|- \beta +
- \alpha )|6\beta M.
$$

cos$θ_{0}$($r$

2= 1 + $/$2), we have thatAlso note that $|β| 6 δ/π$ by (34). Since cos$θ_{0} O_{1}$($θ$ 0 2 4+ (1 + $/$2))($r −$ + $δ$)) =$O_{1}$($δ/π$) $O_{1}$($δ O_{1}$(7$d O_{1}$($βM$)$.$

$$
\rho _{d}
$$

$$
\sqrt
$$

4 2Using $r 6$ 6$d$, $6$ 2$d$, and $δ$ = 24$d π$ $β 6$ 1, we get

$$
\rho _{d}
$$

$$
()()()
$$

2 4 3

$$
\delta 7d\delta \delta
$$

4$r −$ + ($r −$ ) + $δ$) + = +$O_{1} O_{1}$(7$d O_{1} O_{1}$($βM$) $O_{1}$

$$
\rho _{d}\rho _{d}
$$

2 2 $π$

$$
()
$$

$$
7d\delta \delta
$$

2 4=$⇒ r −$ = 4$dδ$ + 7$d δ$ + + 6$dβ$ +$O_{1}$

$$
\rho _{d}
$$

2 $π$

$$
(())
$$

47$d$ 1 4= 6$dβ$ + $δ$ 4$d$ + 7$d$ + +$O_{1}$ 2 $π$

$$
)
((())
$$

$$
\sqrt
$$

121$d$ 4 2= + $β$6$d$ + 24$d π$ 4$d$ +$O_{1}$ 2 $π$

$$
\sqrt
$$

8= $β$)$.$$O_{1}$(3517$d$

![diagram: p35 region 57](2008.10579v1-membrane/imageFile20.png)

Finally, to complete the proof we use the inequality

$$
\|x- 6|\|x\|- ++|\|x\|- x_{* }\|\|x_{* }\||(\|x_{* }\|\|x_{* }\||)\theta _{0}.
$$

This inequality states that if a two dimensional point is known to be within ∆$r$ of magnitude $r$ and an angle ∆$θ$ away from 0, then it is at most a Euclidean distance of ∆$r$ + ($r$ + ∆$r$)∆$θ$ away from 4 5the point ($r,$0) in polar coordinates. Thus for = $β$), we have $r$ = 1 + $β$) so$θ_{0} O_{1}$(82$πd O_{1}$(838$πd$

5 5 4 2 9$\|x − 6$ 838$πd β$ + (1 + 838$πd β$)82$πd β 6$ 70000$π d β.$

$$
x_{* }\|
$$

$$
\sqrt \sqrt
$$

4 2 8= $π$ + $π β$), $r$ = + $β$) so thatThen if $θ_{0} O_{1}$(24$d O_{1}$(3517$d$

$$
\rho _{d}
$$

$$
\sqrt \sqrt \sqrt \sqrt
$$

8 8 4 2 2 12$\|x$ + $6$ 3517$d β$ + + 3517$d β$)24$d π β 6$ 77422$π d β.$

$$
x_{* }\|
\rho _{d}(\rho _{d}
$$

Hence we attain

$$
\sqrt
$$

2 9 2 12

$$
\beta ).
\subset d\beta )\cup d
$$

$B$($x_{∗},$70000$π x_{∗},$77422$π$

$$
S_{\beta }B(- \rho _{d}
$$

˘The result that $→$ 1 as $d → ∞$ follows from the following facts: by (37), we have that $→$

$$
\rho _{d}\theta _{d}
$$

˘2sin$θ_{d}$0 as $d → ∞$ which implies $→$ 0 as $d → ∞.$ Moreover, in [33], it was shown that

$$
\pi
$$

Hence

$$
()
d- 1
d- 1
\sum
$$

$$
\pi - \theta _{j}
$$

sin$θ_{i}$

$$
\to 1asd\to \infty .
\pi \pi
$$

$j$=$i$+1$i$=0

$$
)()
(
d- 1
d- 1
\sum
$$

$$
\pi - \pi - \theta _{j}
$$

sin$θ_{i}$

$$
2\theta _{d}
\to 1asd\to \infty \pi \pi \pi
$$

$j$=$i$+1$i$=0

so $→$ 1 as $d → ∞$.

$$
\rho _{d}
$$

We now aim to show that the objective function value for points near the minimizer are lower than near the negative multiple which is formally stated in Lemma 9. We first define

$$
\| \|
$$

$$
\| \|
$$

:= $−$

$$
\| |AG(x)|
$$

$f_{0}$($x$) $|AG$($x_{∗}$)$|∥$ 2

which is the objective function without noise and ($x$) = $− 〈|AG$($x$)$| −$ Then

$$
f_{\eta }f_{0}(x)|AG(x_{* })|,\eta 〉.
$$

1 2$\|η\|$ . We will first show that the objective function without noise cannote that $f$($x$) = ($x$) +

$$
f_{\eta }
$$

2 be closely approximated by a particular function $F$ which is defined by

$$
()
()
d- 1
$$

$$
\pi -
$$

1 1 $π −$

$$
\theta _{j}
2\theta _{d}
$$

2 2$F$($x$) := ($\|x\|$ + ) $−$

$$
\|x_{* }\|〈x,x_{* }〉
$$

$d$+1 $d$

$$
\pi \pi
$$

2 2 $j$=0

$$
(())
()
d- 1
d- 1
\sum
$$

$$
1\pi - \pi - sin\theta _{i}\theta _{i}
$$

2sin$θ_{d}$ 2$θ_{d}$ $−$ +

$$
\|x\|\|x_{* }\|
d
\pi \pi \pi \pi
$$

2 $j$=$i$+1$i$=0

where $θ_{0}$ := $∠$($x,y$) and $θ_{i}$ := $g$($θ_{i}_{−}_{1}$) for $i$ $∈$ [$d$]. This result is formalized in the following lemma:

![diagram: p36 region 58](2008.10579v1-membrane/imageFile21.png)

![diagram: p36 region 60](2008.10579v1-membrane/imageFile22.png)

$$
m\times n_{d}
$$

Lemma 20. Fix 0 $< ε <$ 1$/$(16$πd$ ) . Suppose that $A ∈ R$ satisfies the RRCP with respect

$$
n_{i}\times n_{i- 1}
$$

to $G$ with constant $ε$ and $G$ is such that each $∈ R$ satisfies the WDC with constant $ε$ for

$$
W_{i}
k
$$

$i ∈$ [$d$]. Then we have that for all non-zero $∈ R$ :

$$
x,x_{* }
\sqrt \sqrt
$$

33 ($L$ + 12)$d$ 2$Lε$

$$
\varepsilon \varepsilon
$$

($L$ + 12)$d$ 2 2

$$
\|x\|++
- F(x)|6
\|x_{* }\|\|x\|\|x_{* }\|
$$

$|f_{0}$($x$)

$$
ddd
$$

2 2 2

$$
[()]
$$

$$
\sqrt
$$

1 $d$ 48$d$ + 48$d$ 3+ 24$d$ + 8$d$ 1 + +

$$
\varepsilon \|x\|\|x_{* }\|.
d
\pi \pi
$$

$$
k
$$

Proof of Lemma 20. Fix $∈ R \{$0$}$. For notational simplicity, define

$$
x,x_{* }
()(())
d- 1
d- 1d- 1
\sum
$$

$$
\pi - \pi - \pi - \pi - \theta _{i}sin\theta _{i}\theta _{i}
$$

2$θ_{d}$ 2$θ_{d}$ 2sin$θ_{d}$ + +:=

$$
〈x,x_{* }〉\|x_{* }\|\|x\|.
\xi _{x,x_{* }}
\pi \pi \pi \pi \pi \pi
$$

$i$=0 $j$=$i$+1$i$=0 1 12 2Then observe that $F$ can be written more compactly as $F$($x$) = $.$ Then

$$
(\|x\|)- +\|x_{* }\|
$$

$d$+1 $d$

$$
\xi _{x,x_{* }}
$$

2 2 the following bound shows we need to approximate three particular terms:

$$
\mid \mid \mid \mid \mid \mid \mid \mid
$$

1 11 12 22 2

$$
\mid \mid \mid \mid - F(x)|6
\|x\|+
\|- \|-
$$

$|f_{0}$($x$)

$$
\|x_{* }\|
x_{d}x_{* ,d}
\|A_{x_{d}}\|A_{x_{* ,d}}
\mid \mid \mid \mid dd
$$

2 22 2

$$
\mid \mid \mid \mid
$$

$$
\mid \mid
$$

$$
.
〉- x_{d}
x_{* ,d}
〈A_{x_{d}}
,A_{x_{* ,d}}
\mid \xi _{x,x_{* }}\mid d
$$

Bounds on the first two terms follow directly by the RRCP and WDC in the following way. Note that

$$
\mid \mid \mid \mid \mid \mid \mid \mid \mid \mid
$$

11 2 2 2 2 22

$$
\mid \mid \mid \mid \mid \mid
$$

$$
\|x\|6\|- \|\|- \|x\|.
\|- x_{d}\|x_{d}\|x_{d}
x_{d}
\|A_{x_{d}}
\|A_{x_{d}}
\mid \mid \mid \mid dd
$$

Since $A$ satisfies the RRCP with respect to $G$, we have that

$$
(
)
d
\mid \mid
$$

12 22 2 2

$$
\mid \mid 6\|x\|6L\varepsilon \|- \|+\varepsilon \|x\|
L\varepsilon \|\Lambda _{x}\|
x_{d}\|x_{d}
\|A_{x_{d}}
$$

where the last inequality follows by the WDC. Then by (50), we have

$$
\mid \mid \sqrt
$$

$$
\mid \mid \varepsilon
$$

1 $d$ 2 2 2

$$
\mid \mid \|- \|x\|624\|x\|
\|x_{d}
\mid \mid dd
$$

Using these two bounds, we have

$$
\mid \mid (
)
\sqrt d
$$

$$
\mid \mid
$$

11 $d$

$$
\varepsilon
$$

2 2 2 2

$$
\mid \mid 6L\varepsilon \|- \|x\|\|x\|+24\|x\|
$$

+ $ε$

$$
x_{d}
\|A_{x_{d}}
\mid \mid dd
$$

$$
\sqrt
$$

3(2$L$ + 24)$d ε$ 2$6 \|x\|$ (63)

$$
d
$$

$$
d2\varepsilon d
$$

since (1 + 2$ε$) $6 e 6$ 1 + 4$εd 6$ 2 for $ε 6$ 1$/$(4$d$). By the same logic, we have that

$$
\mid \mid \sqrt
$$

$$
\mid \mid
$$

1 (2$L$ + 24)$d$

$$
\varepsilon
$$

2 2 2

$$
\mid \mid \|- 6
$$

$.$ (64)

$$
\|x_{* }\|\|x_{* }\|
x_{* ,d}
\|A_{x_{* ,d}}
\mid \mid dd
$$

For the last term, note that

$$
\mid \mid \mid \mid \mid \mid \mid \mid \mid \mid
$$

$$
\mid \mid \mid \mid \mid \mid 6.
〉- 〉- 〉
$$

$$
〉- x_{d}x_{* ,d}x_{d}x_{* ,d}x_{d},x_{* ,d}
x_{d},x_{* ,d}
〈A_{x_{d}},A_{x_{* ,d}}〈A_{x_{d}},A_{x_{* ,d}}〈\Phi _{x_{d},x_{* ,d}}
〈\Phi _{x_{d},x_{* ,d}}
\xi _{x,x_{* }}\mid \xi _{x,x_{* }}\mid \mid \mid dd
$$

For the first term, the RRCP and WDC imply

$$
(
)
d
\mid \mid
$$

$$
2L\varepsilon \mid \mid 6L\varepsilon 〉
〉- +\varepsilon 6
\|x\|\|x_{* }\|
\|x\|\|x_{* }\|
x_{d},x_{* ,d}
x_{* ,d}
x_{d}
〈\Phi _{x_{d},x_{* ,d}}
,A_{x_{* ,d}}
〈A_{x_{d}}
d
$$

for $ε 6$ 1$/$(4$d$). For the second term, by the definition of and , we have

$$
\Phi _{z,w}
M_{ˆz↔ˆw}
$$

$∣ ∣ ∥$

$$
\|
$$

$∣ ∣ ∥$

$$
\| \pi -
$$

$$
\pi - 2\theta _{d}
2\theta _{d}
$$

T ˜ $∣ ∣ ∥$

$$
\| 〉- 6\|x\|\Lambda - x_{* }
x_{d},x_{* ,d}
\Lambda _{x_{* }}
x
〈\Phi _{x_{d},x_{* ,d}}
$$

$∣ ξx,x∗∣ hx,x∗∥$

$$
\| d
\pi \pi
$$

$$
︷︷︸
︸
$$

($I$)

$$
\| \| \| \| \|
$$

$$
\|x_{* ,d}
\|x_{* }\|
$$

2sin$θ_{d}$2sin$θ_{d}$ T

$$
\| \| +\|x\|
\Lambda - x
\Lambda _{x}x
x
\| \| d
\pi \|\pi \|x\|
$$

$$
\|x_{d}
︷︷︸
︸
$$

($II$)

˜where is defined in (54). It was shown in the proof of Lemma 19 that

$$
h_{x,x_{* }}
$$

and

$$
(())
\sqrt 8dd
$$

1 324$d$ + 1 +($I$) $6$

$$
\varepsilon \|x_{* }\|
d
\pi \pi
$$

$$
)
(
$$

$d$ 3

$$
\sqrt
$$

1 24$d$(1 + 2$ε$) + 48$d$

$$
(II)6
\varepsilon \|x_{* }\|.
d
\pi
$$

Combining the results for (65), (I), and (II) we have

$$
\mid \mid (())
\mid \mid \sqrt 2L\varepsilon
$$

1 1 8$d d$ 3

$$
\mid \mid 6
$$

$〉 −$ + 24$d$ + 1 +

$$
\|x\|\|x_{* }\|\varepsilon \|x_{* }\|
x_{d}x_{* ,d}
〈A_{x_{d}},A_{x_{* ,d}}
\xi _{x,x_{* }}\mid \mid ddd
\pi \pi
$$

2 2 2

$$
)
(
$$

$d$ 3

$$
\sqrt
$$

1 24$d$(1 + 2$ε$) + 48$d$ +

$$
\varepsilon \|x_{* }\|.
d
\pi
$$

Combining equations (63), (64), and (66) achieves the desired result.

Now that we have established that the objective function without noise can be approximated by $F$, we now show that $F$ satsifies particular quadratic upper and lower bounds to establish the desired properties of the true objective function $f$:

1Lemma 21. Fix 0 $< r <$ and let $κ$ := $>$ 0. Then for any $∈ ,$1], we have that

$$
min_{d>2}\rho _{d}\phi _{d}[\rho _{d}
$$

$$
4d\pi
$$

$$
(
)
$$

2 245$d$

$$
\|x_{* }\|\|x_{* }\|
$$

2$φ −$ + $∀ x ∈$ (67)

$$
F(x)6r+
x_{* },r\|x_{* }\|),
2\phi _{d}B(\phi _{d}
d
$$

3$d$+1 $d$+1

$$
\kappa
$$

2 2 2 2

$$
\|x_{* }\|\|x_{* }\|
$$

2 4$F$($y$) $>$ ($φ − −$ 139$d r$) + $∀ y ∈$ (68)

$$
x_{* },r\|x_{* }\|).
2\rho _{d}\phi _{d}B(- \phi _{d}
d
$$

$d$+1 $d$+12 2 Q

$$
\pi - 2\theta d- 1
\pi - \theta _{j}
$$

$_{d}$ sin$θ_{i}$Proof of Lemma 21. Define := := $,$ and := $.$ Then note that we

$$
,\zeta _{i+1}\alpha _{i}
\psi _{d}
$$

$j$=$i$+1

$$
\pi \pi \pi
$$

can write $F$ as

$$
(())
d- 1
\sum
$$

1 12 2$F$($x$) := ($\|x\|$ + ) $−$ + + $.$

$$
\|x_{* }\|\zeta _{0}〈x,x_{* }〉\alpha _{i}\zeta _{i+1}\|x\|\|x_{* }\|
\psi _{d}2\alpha _{d}\psi _{d}
$$

$d$+1 $d$2 2 $i$=0

![diagram: p38 region 62](2008.10579v1-membrane/imageFile23.png)

$$
\pi r
and6\|x\|6
$$

Fix $x ∈$ Then observe that we have $6$

$$
- r)\|x_{* }\|+r)\|x_{* }\|.
x_{* },r\|x_{* }\|).\theta _{0}
(\phi _{d}(\phi _{d}
B(\phi _{d}
2\phi _{d}
$$

$$
\theta
$$

0Furthermore, $>$ 1 $−$ $.$ Thus, we have the following bounds:cos$θ_{0}$ 2

$$
()
d- 1
$$

$$
r\pi r
r
$$

1 $− ,$ and $>$ 1 $−$

$$
>1- ,>.
$$

cos$θ_{0}$$ζ_{0}$

$$
\psi _{d}
\phi _{d}2\phi _{d}8\phi _{d}
$$

$i$=0

Hence we see that

$$
\|x\|1
\|x_{* }\|
6- F(x)- \zeta _{0}cos\theta _{0}\|x\|\|x_{* }\|
\psi _{d}
$$

$d$+1 $d$+1 $d$2 2 2 1 2 2 2

$$
6+r)- - \|x_{* }\|\zeta _{0}r)\|x_{* }\|
$$

($φ_{d} ψ_{d}$ cos$θ_{0}$($φ_{d}$ $d$+1 $d$2 2

$$
(()()())
$$

2 2 2

$$
rdr\pi r
\|x_{* }\|
$$

$$
6\phi ++r- 21- 1- - r)1- .
2r\phi _{d}(\phi _{d}
d
$$

2$d$+12 8$φ$

$$
\phi _{d}\phi _{d}
d
\sum d- 1
$$

where in the first inequality we used + $>$ 0$.$ Noting that $∈ ,$1] and $r <$ 1,

$$
\alpha _{i}\zeta _{i+1}
2\alpha _{d}\psi _{d}\phi _{d}[\rho _{d}
$$

$i$=0 with some algebra we attain

$$
(
)
)()(
$$

$$
r
dr\pi r
$$

2 2$φ$ + + $r −$ 2 1 $−$

$$
1- - r)1- 2r\phi _{d}
(\phi _{d}
d
$$

28$φ$

$$
\phi _{d}\phi _{d}
d
$$

2(8$d$ + (2$d$ + 1)$π$ + 7)$r$ 2 $−$ +

$$
6\phi 2\phi _{d}
d
$$

$$
\phi d
$$

45$d$ 2 $−$ +

$$
6\phi r
2\phi _{d}
d
$$

$$
\kappa
$$

so we may conclude that for $x ∈$

$$
x_{* },r\|x_{* }\|),
B(\phi _{d}
()
$$

2 2 45$d$

$$
\|x_{* }\|\|x_{* }\|
$$

$$
6\phi - +r.
F(x)- 2\phi _{d}
d
$$

3$d$+1 $d$+1

$$
\kappa
$$

$$
\pi
$$

Fix $x ∈$ Then note that we have $6 r$ and $6 \|x\| 6$ +

$$
x_{* },r\|x_{* }\|).\pi - \theta _{0}- r)\|x_{* }\|
B(- \phi _{d}(\phi _{d}(\phi _{d}
$$

2 Furthermore, for sufficiently small $r >$ 0, we have that $6$ 0 so that $>$ 0$r$)$\|x_{∗}\|$. $〈x,x_{∗}〉 ζ_{0}〈x,x_{∗}〉$

$$
- \psi _{d}
$$

(note that $6 π/$2). Thus

$$
\theta _{d}
()
d- 1
\sum
$$

1 1 12 2

$$
F(x)- =- \|x\|++
\|x_{* }\|\zeta _{0}〈x,x_{* }〉\alpha _{i}\zeta _{i+1})\|x\|\|x_{* }\|
\psi _{d}(\psi _{d}2\alpha _{d}
$$

$d$+1 $d$+1 $d$2 2 2 $i$=0

$$
d- 1
\sum
$$

11 2 2 + +

$$
- r)- >
\alpha _{i}\zeta _{i+1}r)\|x_{* }\|.
\|x_{* }\|
2\alpha _{d})(\phi _{d}
(\phi _{d}(\psi _{d}
$$

$d$+1 $d$2 2 $i$=0

2 2 2Note that we have = $π$ + $/$2). As shown in Proposition 2, if $d$ ($rπ /$2)$/π 6$ 1, then we

$$
\theta _{0}O_{1}(r\pi
$$

have that

$$
d- 1
\sum
$$

4 2+ = + $rπ /$2)$.$$α_{i}ζ_{i}_{+1} O_{1}$(7$d$

$$
\psi _{d}2\alpha _{d}\rho _{d}
$$

$i$=0

Hence we have 2 1 1

$$
\|x_{* }\|
$$

2 2 2 4 2

$$
>- r)- +7\pi d+
F(x)- \|x_{* }\|r)\|x_{* }\|
(\phi _{d}(\rho _{d}r/2)(\phi _{d}
$$

$d$+1 $d$+1 $d$2 2 2 2

$$
\|x_{* }\|
$$

2 2 2 4 2 4 2 2 =

$$
(\phi - +r- ++7\pi d/2+7\pi dr
/2))\|x_{* }\|
2r\phi _{d}2(\rho _{d}\phi _{d}r\rho _{d}r\phi _{d}
d
$$

$d$+12 2

$$
\|x_{* }\|
$$

2 2 4 2 4 2

$$
>(\phi - 2r- - 2r- 7\pi dr- 7\pi dr)
2\rho _{d}\phi _{d}
d
$$

$d$+12 2

$$
\|x_{* }\|
$$

2 4$>$ ($φ − −$ 139$d r$)

$$
2\rho _{d}\phi _{d}
d
$$

$d$+12 where we used the fact that $∈ ,$1] and 0 $< r <$ 1$.$ This completes the proof.

$$
\phi _{d}[\rho _{d}
$$

With this result, we are equipped to prove Lemma 9.

$$
6
$$

Proof of Lemma 9. By the same argument for (49), we have that

$$
\|x\|\|\eta \|for
\Lambda _{x}x,\eta 〉|
d/2
|〈A_{x_{d}}
$$

$$
k
$$

any $x ∈ R$ . Thus for $x ∈$

$$
x_{* },\varphi \|x_{* }\|),
B(\phi _{d}
$$

$$
|〈|AG(x)|- 6
$$

$$
|AG(x_{* })|,\eta 〉|
\Lambda _{x}x,\eta 〉|
x_{* },\eta 〉|
\Lambda _{x_{* }}
|〈A_{x_{d}}
|〈A_{x_{* ,d}}
$$

$$
\|\eta \|
6(\|x\|+
\|x_{* }\|)
d/2
$$

$$
6+\|\eta \|
(\varphi \|x_{* }\|2\|x_{* }\|)
d/2
$$

2 where we used the fact that $\|x\| 6$ + $6$ (1 + in the last inequality.

$$
\varphi )\|x_{* }\|\varphi )\|x_{* }\|
(\phi _{d}
()
$$

3$d$ 48$d$+48$d$3Let $κ$ := . If $x ∈$ and := 24$d$ +8$d$ 1 + + , then Lemma

$$
x_{* },\varphi \|x_{* }\|)
min_{d>2}\rho _{d}B(\phi _{d}K_{d}
\pi \pi
$$

20 and Lemma 21 give

$$
(x)6F(x)+- F(x)|+|〈|AG(x)|- f_{\eta }|f_{0}(x)|AG(x_{* })|,\eta 〉|
()
\sqrt
$$

2 2 345$d$ ($L$ + 12)$d ε$

$$
\|x_{* }\|\|x_{* }\|
$$

$$
6\phi - +\varphi ++\|x\|
2\phi _{d}
d
$$

3$d$+1 $d$+1 $d$

$$
\kappa
$$

2 2 2

$$
\sqrt
$$

$$
\sqrt
$$

($L$ + 12)$d ε$ 2$Lε$ 1 2 2+ + + + + $\|η\|$

$$
\|x_{* }\|\|x\|\|x_{* }\|\varepsilon \|x\|\|x_{* }\|(\varphi \|x_{* }\|2\|x_{* }\|)
K_{d}
ddd
d/2
$$

2 2 2 2 2

$$
()
\sqrt \sqrt
$$

45$d$

$$
\|x_{* }\|
$$

2 3 2$−$ +$6$ ($φ ϕ$ + 1 + 2($L$ + 12)$d ε$ + $ϕ$) + 1 + 4$L$ + $ϕ$) + + $ϕ$))

$$
2\phi _{d}
(\phi _{d}\varepsilon (\phi _{d}2K_{d}(\phi _{d}
d
$$

3$d$+1

$$
\kappa
$$

$$
++\|\eta \|
(\varphi \|x_{* }\|2\|x_{* }\|)
d/2
$$

$$
\sqrt \sqrt
$$

245$d$

$$
\|x_{* }\|
$$

2 ˜$ε$ + $ε$) + +

$$
6(1+\phi - +\|\eta \|
(\varphi \|x_{* }\|2\|x_{* }\|)
K_{d}
2\phi _{d}
d
$$

3$d$+1

$$
d/2
\kappa
$$

2 2 3˜where := 6($L$ + 12)$d$ , in the second inequality we used $\|x\| 6$ and in

$$
+\varphi )\|x_{* }\|,
K_{d}+8L+4K_{d}(\phi _{d}
\sqrt
$$

$ε$, $6$ 1 and $ϕ <$ 1.the last inequality we used $ε <$

$$
\rho _{d}
$$

Similarly, if $y ∈$ then

$$
x_{* },\varphi \|x_{* }\|),
B(- \phi _{d}
$$

$$
(y)>F(y)- - F(y)|- |〈|AG(y)|- f_{\eta }|f_{0}(y)|AG(x_{* })|,\eta 〉|
\sqrt
$$

2 2 3($L$ + 12)$d ε$

$$
\|x_{* }\|\|x_{* }\|
$$

2 4 2

$$
>(\phi - - 139d\varphi )+- \|x\|
2\rho _{d}\phi _{d}
d
$$

$d$+1 $d$+1 $d$2 2 2

$$
\sqrt
$$

$$
\sqrt \varepsilon
$$

($L$ + 12)$d$ 2$Lε$ 1 2 2

$$
\|\eta \|
- - - - +
\|x_{* }\|\|x\|\|x_{* }\|\varepsilon \|x\|\|x_{* }\|(\varphi \|x_{* }\|2\|x_{* }\|)
K_{d}
ddd
d/2
$$

2 2 2 2 2

$$
\sqrt \sqrt
$$

$$
\|x_{* }\|
$$

2 4 ˜

$$
>(1+\phi - - 139d\|\eta \|.
\varepsilon - \varepsilon )- +
(\varphi \|x_{* }\|2\|x_{* }\|)
2\rho _{d}\phi _{d}
K_{d}
d
$$

$d$+1

$$
d/2
$$

In sum, we have for $x ∈$

$$
x_{* },\varphi \|x_{* }\|),
B(\phi _{d}
$$

$$
()
\sqrt
$$

$$
\sqrt
$$

45$d ε$ 2

$$
\|x_{* }\|
$$

$$
+\|\eta \|
$$

($x$) $6$ 1 + $φ −$ + $ε$ + +

$$
f_{\eta }(\varphi \|x_{* }\|2\|x_{* }\|)
K_{d}
2\phi _{d}
d
$$

3$d$+1

$$
d/2
\kappa
$$

while for $y$ $∈$

$$
x_{* },\varphi \|x_{* }\|),
B(- \phi _{d}
$$

$$
()
$$

$$
\sqrt \sqrt
$$

$$
\|x_{* }\|
$$

2 4 ˜($y$) $> \|η\|.$

$$
1+\phi - - 139d\varepsilon - \varepsilon - +
f_{\eta }
(\varphi \|x_{* }\|2\|x_{* }\|)
2\rho _{d}\phi _{d}K_{d}
d
$$

$d$+1

$$
d/2
$$

Note that we require the lower bound in (70) to be larger than the upper bound in (69). Setting

$$
\sqrt \|x_{* }\|
$$

$ϕ$ = $ε$ and using both $ε <$ , we see that we require

$$
\varepsilon and\|\eta \|6
$$

$c_{2}$

$$
d/2
$$

482 $d$

$$
(
)
$$

248(1 $−$ ) $−$

$$
8c_{2}/d
2\phi _{d}\rho _{d}
\varepsilon 6
.
$$

45$d$ 4˜+ + 139$d$

$$
2K_{d}
$$

$$
\kappa
$$

2By Lemma 22, we have 1 $− >$ 1$/$($C$($d$ + 2) ) for some numerical constant $C$ and $> κ$. Hence

$$
\rho _{d}\phi _{d}
\kappa
$$

12$c_{2}$ $6$ and $ϕ$ = $ε 6$ for some numerical constants andit suffices to have

$$
r_{1}/dr_{1}c_{2}.
$$

48 2$d$ 8$C$($d$+2)

Lemma 22. We have that satisfies $>$ 0 and for some numerical constant $C$,

$$
\rho _{d}min_{d>2}\rho _{d}
$$

$$
61- \forall d>2.
\rho _{d}
$$

2$C$($d$ + 2)

$$
()
\sum
$$

$$
d- 1
d- 1
\pi - \theta _{j}
$$

sin$θ_{i}$Proof of Lemma 22. Let := . In Lemma A.4 of [35], it has been es-Γ$_{d}$ $i$=0 $j$=$i$+1

$$
\pi \pi
$$

˘tablished that $∈$ [0$,$1] and $>$ 0. By (37) and (38), we have that $6$ 3$π/$($d$ + 3) andΓ$_{d}$ min$_{d}_{>}_{2}$ Γ$_{d} θ_{d}$ ˘ $> π/$($d$ + 1) for all $d >$ 2. Since sin(2$x$) $−$ 3$x/$4 $>$ 0 for all $x ∈$ [0$,π/$3], observe that

$$
\theta _{d}
$$

$$
()
\pi ()
$$

2sin˘ $d$+1 $π$ 32 32sin$θ_{d}$ =

$$
>>\cdot \forall d>2.
$$

$π π π$ 4 2($d$ + 1) 4($d$ + 1)

Thus for any $d >$ 2,

$$
(
)
$$

6$π$˘ ˘

$$
\pi - \pi -
$$

32sin$θ_{d}$ 2$θ_{d}$ $d$+3 = + +

$$
>
\rho _{d}
$$

Γ$_{d}$Γ$_{d}$ $π π$ 4($d$ + 1) $π$

$$
\pi -
$$

3 $d$+3$>$ +Γ$_{d}$ Γ$_{d}$ 4($d$ + 1) $π$

$$
(
)
$$

3 $d −$ 3 1 = + $>$Γ$_{d}$ Γ$_{d}$ 4($d$ + 1) $d$ + 3 20

where the second inequality is due to $∈$ [0$,$1]. We conclude that $> >$ 0$.$Γ$_{d}$ min$_{d}_{>}_{2} ρ_{d}$ 1$/$20min$_{d}_{>}_{2}$ Γ$_{d}$ We now establish the lower bound on 1 $−$ for all $d >$ 2. It was shown in Lemma A.4 of [35]

$$
\rho _{d}
$$

1 for some numerical constant Observe thatthat 1 $− >$ $a_{7}$.Γ$_{d}$ 2($d$+2)$a_{7}$

$$
(
)
()
$$

2 222 ˘ ˘˘ ˘ ˘= 1 $− − 6$ += + (1 $−$ )$.$+

$$
\rho _{d}sin\theta _{d}\theta _{d}\Gamma _{d}\Gamma _{d}
\theta _{d}sin\theta _{d}\Gamma _{d}\theta _{d}\Gamma _{d}
$$

Γ$_{d}$

$$
\pi \pi \pi \pi
$$

![diagram: p41 region 71](2008.10579v1-membrane/imageFile24.png)

$−$1˘ ˘Furthermore, note that for all $d >$ 2, $6$ = $g$($g$($π$)) = $g$($π/$2) = cos (1$/π$)$.$ Hence for all $d >$ 2,$θ_{2}$

$$
\theta _{d}
$$

$$
()
$$

2 2˘ ˘

$$
1- >1- -
$$

(1 $−$ ) = (1 $−$ ) 1 $−$

$$
\rho _{d}\Gamma _{d}
\theta _{d}\Gamma _{d}\Gamma _{d}\theta _{d}
\pi \pi ())
(
$$

121 $−$11 $−$ cos

$$
>
$$

2+ 2) $π π$$a_{7}$($d$ 0$.$2

$$
>
.
$$

2+ 2)$a_{7}$($d$

2.5.4 Proofs for Section 2.4.4

Here we prove the convexity-like property of $f$ around the minimizer

$$
x_{* }.
$$

Proof of Lemma 11. Suppose our objective function $f$ is differentiable at $x$. Recall that the gradient T TT T and = Λ $A η$. We willof $f$ is given by = where = Λ $A$

$$
x_{* })q_{x}
v_{x}
v_{x}- q_{x}v_{x}
\Lambda _{x_{* }}
x
x
\Lambda _{x}x- A_{x_{* ,d}}
(A_{x_{d}}
x_{d}
x_{d}
$$

first show that satisfies

$$
v_{x}
$$

$$
\| \|
$$

$$
\| \| - \Lambda - 6\|x-
$$

$v_{x}$ (Λ$_{x}x$ Λ$_{x}x_{∗}$) $x_{∗}\|.$

$$
x
d
$$

16 2 Note that by the triangle inequality, we have that

$$
\| \| \| \|
$$

T T TT

$$
\| \| \| \| \Lambda A- - \Lambda - 6
- \Lambda - \Lambda _{x}xx_{* })(\Lambda _{x}xx_{* })
v_{x}(\Lambda _{x}x\Lambda _{x}x_{* })
\Lambda _{x_{* }}\Lambda _{x_{* }}
xx
x
(A_{x_{d}}A_{x_{d}}
x_{d}
︸
︷︷︸
$$

$T_{1}$

$$
\| \|
$$

$$
\| \| \Lambda A- .
$$

$$
x_{* }
$$

)Λ$_{x}_{∗}$

$$
x
(A_{x_{d}}A_{x_{* ,d}}
x_{d}
︸
︷︷︸
$$

$T_{2}$ We will establish control of each of these terms separately.

Controlling Since $f$ is differentiable at $x$, note$T_{1}$:

$$
k
$$

for sufficiently small $z ∈ R$ , $G$($x$ + $z$) $− G$($x$) = Λ$_{x}z$.

thatthat by the local linearity of $G$ we have Hence for all $z$, the RRCP implies that

$$
- - - 6- (\Lambda _{x}xx_{* })〉〈\Lambda _{x}z,\Lambda _{x}xx_{* }〉|L\varepsilon \|\Lambda _{x}\|\|\Lambda _{x}xx_{* }\|\|z\|.
\Lambda _{x_{* }}\Lambda _{x_{* }}\Lambda _{x_{* }}
|〈A_{x_{d}}
\Lambda _{x}z,A_{x_{d}}
$$

Since this holds for all $z$, we have that

T T T

$$
\|\Lambda A- - \Lambda - 6- \Lambda _{x}xx_{* })(\Lambda _{x}xx_{* })\|L\varepsilon \|\Lambda _{x}\|\|\Lambda _{x}xx_{* }\|.
\Lambda _{x_{* }}\Lambda _{x_{* }}\Lambda _{x_{* }}
xx
(A_{x_{d}}A_{x_{d}}
x_{d}
$$

$$
\sqrt
$$

4 6 thenIn addition, we have that by Lemma 13, if $ε <$ 1$/$(200 $d$ ) and $x ∈$

$$
\varepsilon \|x_{* }\|)
B(x_{* },d
$$

1$.$2

$$
- 6\|x-
$$

$\|$Λ$_{x}x x_{∗}\| x_{∗}\|.$

$$
\Lambda _{x_{* }}
d/2
$$

Combining (72), (73), and (20) in Lemma 16 we see that

$$
\sqrt
$$

$$
1.2L\varepsilon \| \|
$$

12T T T

$$
\| \|
$$

Λ $A − −$ Λ $− 6 \|x −$

$$
\Lambda _{x}xx_{* })(\Lambda _{x}xx_{* })x_{* }\|.
\Lambda _{x_{* }}\Lambda _{x_{* }}
xx
(A_{x_{d}}A_{x_{d}}
x_{d}
d
$$

$$
\sqrt
$$

Thus choosing $ε$ so that $ε <$ 1$/$(32 $·$ 1$.$2 13$/$12$L$) in (74) shows that

$$
()
$$

$$
\|x-
$$

$$
x_{* }\|.
$$

$T_{1} O_{1}$

$$
d
$$

![diagram: p42 region 75](2008.10579v1-membrane/imageFile25.png)

![diagram: p42 region 76](2008.10579v1-membrane/imageFile26.png)

Controlling We will first show that for sufficiently small $ε$,$T_{2}$:

$$
\sqrt
$$

48$d$1$.$44(4$L$ + ) $ε$ 2 2

$$
\pi 6
- \|x- .
x_{* }\|
x_{* }\|
$$

)Λ$_{x}_{∗}$

$$
A_{x_{* ,d}}
\|(A_{x_{d}}
d
$$

$$
m
$$

Letting denote the rows of $A$, observe that we can write

$$
{a_{i}}
$$

$i$=1

$$
\| \| \| \|
$$

$$
\| \| \| \| -
$$

$$
- x_{* }
)x_{* ,d}
$$

)Λ$_{x}_{∗}$

$$
A_{x_{* ,d}}
(A_{x_{d}}
A_{x_{* ,d}}
(A_{x_{d}}
m
\sum
$$

2 2 =

$$
〉)- 〉))〉
(sgn(〈a_{i},x_{d}sgn(〈a_{i},x_{* ,d}〈a_{i},x_{* ,d}
$$

$i$=1

$$
m
\sum
$$

$$
6
〉)- 〉))- )〉
(sgn(〈a_{i},x_{d}sgn(〈a_{i},x_{* ,d}〈a_{i},(x_{d}x_{* ,d}
$$

$i$=1

$$
m
\sum
$$

2 = $〉 6$= 0) + $〉 6$= 0) $− −$ )$〉$

$$
(1(〈a_{i},x_{d}1(〈a_{i},x_{* ,d}2sgn(〈a_{i},x_{d}〉〈a_{i},x_{* ,d}〉))〈a_{i},(x_{d}x_{* ,d}
$$

$i$=1 2 2 T = $−$ )$\|$ +

$$
- )\|- - ,A
- )〉.
(x_{d}x_{* ,d}
(x_{d}x_{* ,d}2〈x_{d}x_{* ,d}
(x_{d}x_{* ,d}
\|A_{x_{d}}
\|A_{x_{* ,d}}
A_{x_{* ,d}}
x_{d}
$$

We first establish concentration of $−$ ). Since $A$ satisfies the RRCP with respect to $G$,

$$
(x_{d}x_{* ,d}
A_{x_{d}}
$$

we have that T 2

$$
|〈(A- - - 〉|6- \|
)(x_{d}x_{* ,d}),x_{d}x_{* ,d}L\varepsilon \|x_{d}x_{* ,d}
A_{x_{d}}I_{n_{d}}
x_{d}
$$

which ultimately gives

2 2$−$ )$\| 6$ (1 + $− \| .$ (76)

$$
(x_{d}x_{* ,d}L\varepsilon )\|x_{d}x_{* ,d}
\|A_{x_{d}}
$$

Likewise the same upper bound holds for $−$ ):

$$
(x_{d}x_{* ,d}
A_{x_{* ,d}}
$$

2 2$−$ )$\| 6$ (1 + $− \| .$ (77)

$$
(x_{d}x_{* ,d}L\varepsilon )\|x_{d}x_{* ,d}
\|A_{x_{* ,d}}
$$

TWe now aim to upper bound the inner product $− ,A$ $−$ )$〉$. We first note

$$
- 2〈x_{d}x_{* ,d}
(x_{d}x_{* ,d}
A_{x_{* ,d}}
x_{d}
$$

that since $A$ satisfies the RRCP, we have

$$
- ,(A- - )〉|6- \|.
|〈x_{d}x_{* ,d})(x_{d}x_{* ,d}L\varepsilon \|x_{d}x_{* ,d}
A_{x_{* ,d}}\Phi _{x_{d},x_{* ,d}}
x_{d}
$$

Hence we have that

$$
- ,A- )〉=- - )〉+- \|.(78)
〈x_{d}x_{* ,d}(x_{d}x_{* ,d}〈x_{d}x_{* ,d}(x_{d}x_{* ,d}O_{1}(L\varepsilon )\|x_{d}x_{* ,d}
A_{x_{* ,d}},\Phi _{x_{d},x_{* ,d}}
x_{d}
\sqrt \sqrt
$$

which implies $6$ 2$d ε$. Since $| 6$ we haveBut recall that $x ∈$

$$
\varepsilon \|x_{* }\|)|\theta _{0,x}||\theta _{0,x}|
B(x_{* },d
|\theta _{d,x}
\sqrt
$$

$| 6$ 2$d ε$. Also equation (53) gives

$$
|\theta _{d,x}
\sqrt - |64d\varepsilon .
|\theta _{d,x}\theta _{d,x}
\sqrt
$$

$ε.$ Thus is approximately an isometry sinceHence we have that $| 6$ 6$d$

$$
|\theta _{d,x}
\Phi _{x_{d},x_{* ,d}}
\sqrt \| \| |
|
$$

24$d ε$

$$
2|\theta _{d,x}
2|sin\theta _{d,x}
\| \| - I6
\|I\|+\|6.
\Phi _{x_{d},x_{* ,d}}
\|M_{ˆx_{d}↔ˆx_{* ,d}}
\pi \pi \pi
$$

This implies that

$$
()
\sqrt
$$

24$d ε$ 2 2$− −$ )$〉$ = $− \|$ + $− \| .$ (79)$O_{1}$

$$
〈x_{d}x_{* ,d}(x_{d}x_{* ,d}\|x_{d}x_{* ,d}\|x_{d}x_{* ,d}
,\Phi _{x_{d},x_{* ,d}}
\pi
$$

Combining (78) and (79) we attain

$$
(
)
\sqrt
$$

24$d$

$$
\varepsilon
$$

$$
- )〉=- \|+
- ,A+L\varepsilon - \|.
$$

$O_{1}$

$$
(x_{d}x_{* ,d}\|x_{d}x_{* ,d}
〈x_{d}x_{* ,d}\|x_{d}x_{* ,d}
A_{x_{* ,d}}
x_{d}
\pi
$$

Note that this implies that

$$
()
\sqrt
$$

48$d ε$ 2T $−$ )$〉 6 −$2 + + 2$Lε − \| .$ (80)

$$
- ,A
(x_{d}x_{* ,d}\|x_{d}x_{* ,d}
- 2〈x_{d}x_{* ,d}
A_{x_{* ,d}}
x_{d}
\pi
$$

we can use (76), (77) and (80) to

$$
-
$$

Returning to establishing concentration of

$$
x_{* },
$$

)Λ$_{x}_{∗}$

$$
A_{x_{* ,d}}
(A_{x_{d}}
$$

obtain

$$
\| \|
$$

2 2 T2

$$
\| \| - )〉
- )\|- - ,A
$$

$−$ )$\|$ +

$$
6
- x_{* }
(x_{d}x_{* ,d}
(x_{d}x_{* ,d}2〈x_{d}x_{* ,d}
(x_{d}x_{* ,d}
$$

)Λ$_{x}_{∗}$

$$
A_{x_{* ,d}}
\|A_{x_{* ,d}}
\|A_{x_{d}}
A_{x_{* ,d}}
(A_{x_{d}}
x_{d}
(
)
\sqrt
$$

48$d$

$$
\varepsilon
$$

2$6$ 2 + 2$Lε −$ 2 +

$$
+2L\varepsilon - \|
\|x_{d}x_{* ,d}
\pi ()
\sqrt
$$

48$d ε$ 2

$$
=4L\varepsilon +- \|.
\|x_{d}x_{* ,d}
\pi \sqrt
$$

$ε$, we attainUsing this inequality, equation (73), and the fact that $ε <$

$$
\sqrt )
(
\sqrt
$$

48$d$

$$
\| \|
$$

1$.$44(4$L$ + ) $ε$

$$
\varepsilon
$$

48$d$2 2 2

$$
\pi \| \| - - \|6\|x- .(81)
64L\varepsilon +
x_{* }\|
x_{* }
\|x_{d}x_{* ,d}
$$

)Λ$_{x}_{∗}$

$$
(A_{x_{d}}
A_{x_{* ,d}}
d
\pi
$$

Then by equations (20) and (21) from Lemma 16, we have that

$$
\sqrt \sqrt
$$

13 1 1 + $6$ (1 + $Lε$)$6 .$ (82)

$$
L\varepsilon \|\Lambda _{x}\|
\Lambda _{x}\|
\|A_{x_{d}}
d/2
$$

$$
\sqrt
$$

13Combining (82) and (81) and choosing $ε$ so that (1 + $Lε$) $6$ 2, we attain 12

$$
\sqrt ()
\| \| \sqrt
$$

48$d$ 1

$$
\| \| - 62
$$

1$.$44 4$L$ + $ε$

$$
\|x- \Lambda _{x}\|x_{* }
x_{* }\|.
$$

)Λ$_{x}_{∗}$

$$
\|A_{x_{d}}(A_{x_{d}}A_{x_{* ,d}}
d
\pi
$$

Thus if

11$/$4

$$
\sqrt \varepsilon <
()
$$

48$d$64 1$.$44 4$L$ +

$$
\pi
$$

we attain

$$
\| \|
$$

$$
\| \| - 6
\|x- \Lambda _{x}\|x_{* }
x_{* }\|
$$

)Λ$_{x}_{∗}$

$$
\|A_{x_{d}}(A_{x_{d}}A_{x_{* ,d}}
d
$$

i.e., satisfies$T_{2}$

$$
()
$$

1 1 $\|x −$ (83)=

$$
x_{* }\|.
$$

$T_{2} O_{1}$

$$
d
$$

Combining our results for and in equations (75) and (83) we ultimately get$T_{1} T_{2}$

$$
\| \|
$$

$$
\| \|
$$

$\|x −$ (84)

$$
- \Lambda - 6
x_{* }\|.
v_{x}(\Lambda _{x}x\Lambda _{x}x_{* })
x
d
$$

To finish establishing concentration of we appeal to Lemma A.9 of [35] which showed that if

$$
v_{x},
\sqrt
$$

4 6$ε <$ 1$/$(200 $d$ ) and $x ∈$ then

$$
B(x_{* },d\varepsilon \|x_{* }\|)
$$

$$
\| \| \| \|
$$

1 1 1T

$$
\| \| - \Lambda - (x- 6\|x- x_{* })
(\Lambda _{x}xx_{* })x_{* }\|.
\Lambda _{x_{* }}
x
\| \| dd
$$

162 2

Thus by combining equations (84) and (85), we finally attain

$$
\| \| \| \| \| \| \| \| \| \|
$$

11 1 1T T

$$
\| \| \| \| \| \| - - (x- 6- \Lambda - +\Lambda - (x- 6\|x- x_{* })
$$

$v_{x} x_{∗}$) $v_{x}$ (Λ$_{x}x$ Λ$_{x}x_{∗}$) (Λ$_{x}x x_{∗}$) $x_{∗}\|.$

$$
\Lambda _{x_{* }}
xx
\| \| \| \| ddd
$$

82 2 2

as desired. Including the bound on from (49), we achieve the final desired result:

$$
\|q_{x}\|
$$

$$
\| \| \| \| \| \| \| \|
$$

11 1 21

$$
\| \| \| \| \|\eta \|.
- (x- - (x- \|x- +
6+6
v_{x}x_{* })v_{x}x_{* })x_{* }\|
\|q_{x}\|
\| \| \| \| ddd
d/2
$$

82 2 2 2

Finally, for non-differentiable $x 6$= 0 and $v ∈ ∂f$($x$), by (12) we have that there exists $>$ 0 for

$$
c_{`}
\sum \sum ss
$$

= 1 and $v$ = . Hence$` ∈$ [$s$] such that

$$
c_{`}c_{`}v_{`}
$$

$`$=1 $`$=1

$$
\| \| \| \| s
\sum \| \| \| \|
$$

1 1 1 21

$$
\| \| \| \| v- (x- - (x- \|x- +\|\eta \|.
66
x_{* })x_{* })x_{* }\|
c_{`}v_{`}
\| \| \| \| ddd
d/2
$$

82 2 2 2 $`$=1

## Gaussian Matrices Satisfy the RRCP

We set out to prove that Gaussian $A$ satisfies the RRCP with respect to $G$ with high probability. The particular result is stated as follows:

Proposition 3 (Range Restricted Concentration Property (RRCP)). Fix 0 $< ε <$ 1. Let $∈$

$$
W_{i}
$$

$$
n_{i}\times n_{i- 1}
m\times n_{d}
$$

have i.i.d. entries for $i$ = 1$,...,d$. Let $A ∈ R$ have i.i.d. $N$(0$,$1$/m$)

$$
R
N(0,1/n_{i})
$$

˜entries independent from Then if $m >$ ), then with probability at least

$$
{W_{i}}.C_{\varepsilon }dklog(n_{1}n_{2}
...n_{d}
(
)
$$

˜4$k$

$$
k
c_{\varepsilon }
$$

1 $−$ ˜$γm$ exp $−$ $m$ , we have that for all $∈ R$ ,

$$
x,y,x_{1},x_{2},x_{3},x_{4}
$$

$$
|〈(AA- \Phi - -
$$

)($G$($x_{1}$) $G$($x_{2}$))$,G$($x_{3}$) $G$($x_{4}$)$〉|$$G$($y$) $G$($x$)$,G$($y$)$G$($x$)

$$
6- -
$$

$Lε\|G$($x_{1}$) $G$($x_{2}$)$\|\|G$($x_{3}$) $G$($x_{4}$)$\|$

˜Here ˜$γ$ and $L$ are positive universal constants, ˜ depends polynomially on $ε$, and depends poly-

$$
c_{\varepsilon }C_{\varepsilon }
$$

$−$1nomially on $ε$ .

We will prove Proposition 3 via the following steps:

$n$ T1. We first establish that for any fixed non-zero $z,w ∈ R$ , the inner product $〈A x,y〉$ con-

$$
A_{w}
z
$$

centrates around its expectation $x,y〉$ for all $x$ and $y$ in a fixed $k$-dimensional subspace

$$
〈\Phi _{z,w}
n
$$

of $R$ .

2. Then we show that this concentration holds uniformly for all $z,w,x,y$ that live in the union

$$
n
$$

of a finite number of $k$-dimensional subspaces of $R$ .

3. To complete the proof, we apply the result from Step 2 for all $z,w,x,y$ in the range of the generative model which precisely lives in the union of $k$-dimensional subspaces.

![diagram: p45 region 79](2008.10579v1-membrane/imageFile27.png)

![diagram: p45 region 80](2008.10579v1-membrane/imageFile28.png)

### Concentration Over a Fixed Subspace

TWe first show that the matrix $A$ concentrates around for any fixed $z 6$= $w$ while acting on

$$
A_{w}\Phi _{z,w}
z
n
$$

a fixed $k$-dimensional subspace $T$ of $R$ . We will refer to this result as the Restricted Concentration Property (RCP).

$$
m\times n
$$

Proposition 4 (Variant of Lemma 5.1 in [7]; RCP). Fix 0 $< ε <$ 1 and $k < m$. Let $A ∈ R$

$$
nn
$$

have i.i.d. $N$(0$,$1$/m$) entries and fix $z,w ∈ R \{$0$}$. Let $T ⊂ R$ be a $k$-dimensional subspace. Then if $m > Ck$, we have that with probability exceeding 1 $−$ 2exp($−c_{1}m$),

and

T 2$|〈A x,x〉 − x,x〉| 6 ε\|x\| ∀ x ∈ T$

$$
A_{w}〈\Phi _{z,w}
z
$$

T$|〈A x,y〉 − x,y〉| 6$ 3$ε\|x\|\|y\|$

$$
A_{w}〈\Phi _{z,w}
z
$$

$∀$ $x,y$ $∈ T.$ (87)

$$
⋃
⋃
N
M
n
$$

where and are subspaces of $R$ of dimensionand $V$ =Furthermore, let $U$ =

$$
V_{j}U_{i}V_{j}
U_{i}
$$

$j$=1$i$=1 at most $k$ for all $i ∈$ [$M$] and $j ∈$ [$N$]. Then if $m >$ 2$Ck$

$$
\mid \mid
$$

$$
\mid \mid 63\varepsilon \|u\|\|v\|\forall u\in U,
〈Au,v〉- u,v〉
A_{w}〈\Phi _{z,w}
z
$$

$v ∈ V,$ (88)

exceedingwith probability $−$1 $−$1Ω($ε$ log $ε$ ).

$$
1- 2MN
$$

exp($−c_{1}m$). Here $c_{1}$ depends polynomially on $ε$ and $C$ =

For the proof, we require the following large deviation inequality for subexponential random variables:

Lemma 23 (Corollary 5.17 in [60]). Let be independent, centered, subexponential random

$$
Y_{1},...,Y_{m}
$$

variables. Let $K$ = max . Then for all $ε >$ 0,

$$
i\in [m]
\|Y_{i}\|_{\psi _{1}}
$$

$$
\mid \mid (
)
)]
[(
m
\mid
$$

$$
\mid \sum
$$

$$
\varepsilon \varepsilon \mid \mid P
,m
$$

$6$ 2exp $−c$min

$$
>\varepsilon \mid Y_{i}\mid
$$

$$
\mid \mid mKK
$$

$i$=1

$$
1/p
- 1p
$$

where $c >$ 0 is an absolute constant. Here is the subexponential norm: := sup $p$ ($E|X|$ ) .

$$
p>1
\|\cdot \|_{\psi _{1}}\|X\|_{\psi _{1}}
$$

We also require the following simple technical result.

$$
nn
$$

Proposition 5. Fix $z,w ∈ R$ $\{$0$}$ and 0 $< ε <$ 1. Let $T$ be a subspace of $R$ . If

then

$$
\mid \mid
$$

$$
\mid \mid
$$

$〈A x,x〉 − x,x〉 6 ε\|x\| ∀ x ∈ T$

$$
A_{w}〈\Phi _{z,w}
z
$$

$$
\mid \mid
$$

$$
\mid \mid
$$

$〈A x,y〉 − x,y〉 6$ 3$ε\|x\|\|y\| ∀ x,y ∈ T.$

$$
A_{w}〈\Phi _{z,w}
z
$$

With these two results, we are now equipped to prove Proposition 4.

$$
n- 1
$$

Proof of Proposition 4. Without loss of generality, it suffices to show concentration over $T ∩ S$ . TFor notational simplicity, set := $A −$ .

$$
\Sigma _{z,w}A_{w}\Phi _{z,w}
z
$$

$$
n- 1
$$

Step 1: Approximation. We first show that if concentration over an $ε$-net of $T ∩ S$ holds,

$$
\varepsilon n- 1
$$

then a continuity argument establishes concentration over all points in $T ∩S$ . Choose an -net 14

$$
n- 1kn- 1
$$

$⊂ T ∩ S$ such that $| 6$ (42$/ε$) and for any $x ∈ T ∩ S$ ,

$$
Q_{T}|Q_{T}
$$

$$
\varepsilon
$$

min $\|x − q\| 6 .$ 14

$$
q\in Q_{T}
$$

We will prove that

$$
q,q〉|6
|〈\Sigma _{z,w}
$$

$ε$ 8

$∀$ $q$

$$
\in Q_{T}
$$

$$
x,x〉|6\varepsilon |〈\Sigma _{z,w}
$$

$∀$ $x ∈ T.$ (91)

Now, define

$$
* \alpha
$$

$$
{
$$

inf $α$ $>$ 0 2: $x,x〉| 6 α\|x\|$

$$
|〈\Sigma _{z,w}
$$

$$
}
$$

$x ∈ T .$ (92)

$$
* n- 1
$$

We want to show that $α 6 ε$. Fix $x ∈ T ∩ S$ . Then there exists a $q ∈$

$$
Q_{T}
$$

$\|x − q\| 6 ε/$14$.$ In addition, observe that $x − q ∈ T$ since $q ∈ ⊂ T$ so by (92),

$$
Q_{T}
$$

such that

$$
\varepsilon * 2* .
(x- q),x- q〉|6\alpha \|x- q\|6\alpha |〈\Sigma _{z,w}
$$

$$
*
$$

Now, note that by the definition of $α$ ,

$∗$ 2

$$
x,x〉|6\alpha \|x\|\forall x\in T.
|〈\Sigma _{z,w}
$$

Thus Proposition 5 gives

$$
* x,y〉|63\alpha \|x\|\|y\|\forall x,y\in T.
|〈\Sigma _{z,w}
$$

Applying this result to $x − q$ and $q$ gives

$$
* * .
(x- q),q〉|63\alpha \|x- q\|6\alpha |〈\Sigma _{z,w}
$$

$$
\varepsilon
$$

Let $E$ be the event that $q,q〉| 6$ for any $q ∈$ . Using $x,x〉$ = ($x − q$)$,x − q〉$ +

$$
|〈\Sigma _{z,w}〈\Sigma _{z,w}〈\Sigma _{z,w}
Q_{T}
$$

8 $x,q〉 − q,q〉$ and $x,q〉$ = ($x − q$)$,q〉$ + $q,q〉$, we have that on $E$,

$$
2〈\Sigma _{z,w}〈\Sigma _{z,w}〈\Sigma _{z,w}〈\Sigma _{z,w}〈\Sigma _{z,w}
$$

$$
x,x〉|6(x- q),x- q〉|+x,q〉|+q,q〉|
|〈\Sigma _{z,w}|〈\Sigma _{z,w}2|〈\Sigma _{z,w}|〈\Sigma _{z,w}
6(x- q),x- q〉|+(x- q),q〉|+q,q〉|
|〈\Sigma _{z,w}2|〈\Sigma _{z,w}3|〈\Sigma _{z,w}
$$

$$
\varepsilon 3\varepsilon 3\varepsilon * * 6\alpha
$$

+ $α$ + 196 7 8

$$
()
$$

2 3$ε$

$$
\varepsilon 3\varepsilon *
$$

= $α$ + + 196 7 8

where we used (93), (94), and the event $E$ in the third inequality. Thus

$$
()
$$

$$
\varepsilon 3\varepsilon 3\varepsilon * n- 1
x,x〉|6\alpha ++\forall x\in T\cap S.
|〈\Sigma _{z,w}
$$

196 7 8

$$
*
$$

However, recall that $α$ was defined to be the smallest number such that

$$
* x,x〉|6\alpha |〈\Sigma _{z,w}
$$

$$
n- 1
$$

$x ∈ T ∩ S .$

$$
*
$$

Hence $α$ must be smaller than the right hand side of (95), i.e.

$$
()
(
)
$$

$$
\varepsilon
$$

3$ε$ 3$ε$ 13$ε$

$$
* * *
$$

$α 6 α$ + =$⇒ α 6 6 ε$+ 2

$$
\varepsilon 3\varepsilon
$$

196 7 8 8

$$
1- -
$$

196 7 since 0 $<$ $ε$ $<$ 1. This establishes (91).

![diagram: p47 region 83](2008.10579v1-membrane/imageFile29.png)

Step 2: Concentration. observe that

$$
n- 1
$$

We now establish concentration for a fixed point $x ∈ S$ . Then

$$
x,x〉|=
|〈\Sigma _{z,w}
$$

$$
\mid \mid m
\mid \mid \sum
$$

$$
\mid \mid \mid Y_{i}\mid \mid \mid m
$$

$i$=1 2where = $−$ = , and each $∼$ Hence are

$$
Y_{i}X_{i}E[X_{i}],X_{i}sgn(〈˜a_{i},z〉〈˜a_{i},w〉)〈˜a_{i},x〉˜a_{i}N(0,I_{n}).Y_{i}
$$

independent, centered, subexponential random variables. We now estimate their subexponential norm prior to invoking Lemma 23. By Remark 5.18 in [60], the subexponential norm satisfies

$$
=- 6.
\|X_{i}
$$

$\|Y_{i}\|_{ψ}_{1} E$[$X_{i}$]$\|_{ψ}_{1}$ 2$\|X_{i}\|_{ψ}_{1}$ (96)

Let := $∼ N$(0$,$1). Then $6$ for some absolute constant where $\| ·$ is the

$$
Z_{i}〈˜a_{i},x〉K_{1}K_{1}
\|Z_{i}\|_{\psi _{2}}\|_{\psi _{2}}
$$

$p$ 2 $p$ 2sub-gaussian norm. Observe that $6 E|Z$ $|$ which implies $6 \|Z$ . Thus we have

$$
E|X_{i}|
\|X_{i}\|_{\psi _{1}}
\|_{\psi _{1}}
i
i
$$

$$
64K
662\|Z6
4\|Z_{i}\|
$$

$$
i
\|Y_{i}\|_{\psi _{1}}2\|X_{i}\|_{\psi _{1}}\|_{\psi _{1}}
$$

$ψ_{2}$ where we used equation (96) in the first inequality and Lemma 5.14 in [60] in the second to last 2 2inequality. Thus $K$ = max $6$ 4$K$ for an absolute constant Defining := 4$K$ ,$K_{1}$. $K_{2}$

$$
i\in [m]
\|Y_{i}\|_{\psi _{1}}
$$

$$
n
$$

Lemma 23 guarantees that for any fixed $z,w,x ∈ R \{$0$}$ and $ε >$ 0,

$$
x,x〉|>\varepsilon )6
$$

$P$($|〈$Σ$_{z,w}$ 2exp($−c_{0}$($ε$)$m$) (97)

2 2where = $c$min($ε /K$ and $c >$ 0 is an absolute constant.$c_{0}$($ε$) $,ε/K_{2}$) 2

$$
k
$$

Step 3: Union Bound. We now show concentration over holds. Recall that $| 6$ (42$/ε$)

$$
Q_{T}|Q_{T}
$$

so we can apply a union bound to (97) to attain

$$
()
k
(
)(())
\varepsilon
$$

42 $ε$

$$
Pq,q〉|>
\forall q\in 62expm.
- c_{0}
|〈\Sigma _{z,w}
Q_{T}
$$

8 $ε$ 8

By equations (91) and (98), we conclude that

$$
()
k
())
(
()
\varepsilon
$$

42 2$P x,x〉| > ε\|x\| ∀ x ∈ T 6$ 2 exp $m .$

$$
|〈\Sigma _{z,w}- c_{0}
$$

$ε$ 8

The probability bound in the proposition can be shown by noting that

$$
(())
$$

$$
k
$$

1 $−$ 2(42$/ε$) = 1 $−$ 2exp + $k$ log $.$exp($−c_{0}$($ε/$8)$m$) $−c_{0}$($ε/$8)$m$

$$
\varepsilon
$$

Thus if

$$
)
(
$$

2 42 log

$$
k6Ck6m
\varepsilon
$$

$c_{0}$($ε/$8)

$−$1 $−$1where $C$ = Ω($ε$ log $ε$ ), we have that the result holds with probability exceeding

$$
))
((
$$

$$
>1-
$$

1 $−$ 2exp + $k$ log 2exp($−c_{1}m$)

$$
- c_{0}(\varepsilon /8)m
\varepsilon
$$

where = Applying Proposition 5 to our result gives (87) with the same probability.$c_{1} c_{0}$($ε/$8)$/$2. The extension to the union of subspaces follows by applying (87) to all subspaces of the form ) and using a union bound. Note that these subspaces have dimension at most 2$k$,span($U_{i},V_{j}$ accounting for the extra factor of 2 in the bound on $m$.

![diagram: p48 region 84](2008.10579v1-membrane/imageFile30.png)

### Uniform Concentration Over a Union of Subspaces

We will now set out to prove a stronger version of Proposition 4 that holds uniformly for all $z$ and $w$ in (possibly) different $k$-dimensional subspaces:

$$
m\times n
$$

Proposition 6 (Uniform RCP). Fix 0 $< ε <$ 1 and $k < m$. Let $A ∈ R$ have i.i.d. $N$(0$,$1$/m$)

$$
n
$$

entries. Let $T$, $W$, and $Z$ be fixed $k$-dimensional subspaces of $R$ . Then if $m >$ then with

$$
C_{\varepsilon }k,
$$

4$k$probability at least 1 $−$ ˆ$γm$ ˜ we haveexp($−c_{ε}m$)$,$

$$
\mid \mid
$$

$$
\mid \mid 6L\varepsilon \|x\|\|y\|
〈Ax,y〉- x,y〉
A_{w}〈\Phi _{z,w}
z
$$

$∀$ $x,y$ $∈ T,$ $w$ $∈ W,$ $z$ $∈ Z$ (99)

$−$1where ˆ$γ$ is a positive universal constant, ˜ depends on $ε$ and depends polynomially on $ε$ .

$$
c_{\varepsilon }C_{\varepsilon }
⋃
⋃
⋃
⋃
$$

$N_{4}$$N_{3}$$N_{2}$$N_{1}$ be the union of at, and $Z$ =, $W$ =$V$ =Furthermore, let $U$ =

$$
V_{j}
U_{i},
Z_{`}
W_{k}
$$

$j$=1$i$=1 $`$=1$k$=1

$$
n
$$

most $k$-dimensional subspaces of $R$ . Then if $m >$

$$
2C_{\varepsilon }k,
$$

$$
\mid \mid
$$

$$
\mid \mid
$$

$〈A u,v〉 − u,v〉 6 Lε\|u\|\|v\|$

$$
A_{w}〈\Phi _{z,w}
z
$$

$∀$ $u ∈ U,$ $v$ $∈ V,$ $w$ $∈ W,$ $z$ $∈ Z$ (100)

4$k$with probability exceeding ˜ Here $L$ is a positive universal constant.1 $− N_{1}N_{2}N_{3}N_{4}$ˆ$γm$ exp($−c_{ε}m$).

TNote that Proposition 4 established concentration of $〈A x,y〉$ around $x,y〉$ for $x$ and $y$

$$
A_{w}〈\Phi _{z,w}
z
n
$$

in a fixed $k$-dimensional subspace for fixed $z,w ∈ R \{$0$}$. We are interested in showing that this concentration holds uniformly for all $z$ and $w$ in the range of our generative model. The proof of this result uses an interesting fact from 1-bit compressed sensing which establishes that if a sufficient number of random hyperplanes cut the unit sphere, the diameter of each tesselation piece is small with high probability [50]. We state the theorem here for convenience:

$$
(
)
$$

1$/$5

$$
s
n
$$

Theorem 5 (Theorem 2.1 in [50]). Let $n,m,s >$ 0 and set $δ$ = log(2$n/s$) . Let $∈ R$$C_{1}$

$$
a_{i}
m
$$

have i.i.d. $N$(0$,$1) entries for $i ∈$ [$m$]. Then with probability at least exp($−cδm$), the following1$−C_{2}$

$$
\sqrt \sqrt n
$$

holds uniformly for all $x,$ ˜$x ∈ R$ that satisfy = = 1, $6 s$, and $6 s$ for

$$
\|x\|_{2}\|˜x\|_{2}\|x\|_{1}\|˜x\|_{1}
s6n:
$$

$$
>0,
〈a_{i},˜x〉〈a_{i},x〉
$$

$∀$ $i ∈$ [$m$] =$⇒$

$$
\|˜x- x\|_{2}
$$

$6 δ.$ (101)

Here are positive universal constants.

$$
C_{1},C_{2},c
$$

We will use this result to prove the following: given a sufficient number of random hyperplanes and a $k$-dimensional subspace $Z$, there exists a finite set of points $⊂ Z$ that live in the interior of$Z_{0}$ the tesselation pieces generated by the random hyperplanes such that any point in $Z$ can be closely approximated by a point in with high probability.$Z_{0}$

$$
m\times nm
$$

Lemma 24. Fix 0 $< ε <$ 1. Let $A ∈ R$ have i.i.d. $N$(0$,$1$/m$) entries with rows $}$ . Let $Z$

$$
{a_{`}
$$

$`$=1

$$
n
$$

be a $k$-dimensional subspace of $R$ . Define to be the event that there exists a set $⊂ Z$ with$Z_{0}$

$$
E_{Z,A}
$$

the following properties:

$\cdot$ each $∈$ satisfies $6$= 0 for all $` ∈$ [$m$],

$$
z_{0}Z_{0},z_{0}〉
〈a_{`}
$$

2$k$$\cdot 6$ 10$m$ , and

$$
|Z_{0}|
$$

$\cdot$ for all $z ∈ Z$ such that $\|z\|$ = 1, there exists a $∈$ such that $\|z − 6 ε.$

$$
z_{0}Z_{0}z_{0}\|
$$

ˆIf $m > Ck$, then ) $>$ 1 $−$ exp($−cεm$). Here and $c$ are positive absolute constants and$C_{2} C_{2}$

$$
P(E_{Z,A}
$$

$−$1ˆ$C$ depends polynomially on $ε .$

Proof of Lemma 24. By the rotational invariance of the Gaussian distribution, we may take $Z$ to be in the span of the first $k$ standard basis vectors. We may further without loss of generality

$$
m\times k
$$

assume $A ∈ R$ . We will invoke the following lemma which establishes that the unit sphere of $Z$

$$
2km
$$

is partitioned into at most 10$m$ regions by the rows $}$ of $A$ with probability 1:

$$
{a_{`}
$$

$`$=1 Let $V$ be a subspaceLemma 25. probability 1,

$$
n
$$

of $R$ . Let $A$ $∈$

$$
m\times n
R
$$

have i.i.d. $N$(0$,$1$/m$) entries. With

2dim$V$$|{$diag(sgn($Av$))$A$ : $v ∈ V}| 6$ 10$m .$

In each tesselation piece defined by the rows of $A$, choose a single point from $Z$ with unit$z_{0}$ Tnorm such that $a 6$= 0 for all $` ∈$ [$m$] (if such a point exists in the tesselation piece). Let$z_{0} Z_{0}$

$$
`
$$

denote this collection of points and set := By Lemma 25 with $V$ = $Z$, the cardinality

$$
|Z_{0}|.
I_{Z,A}
$$

2$k$of is bounded with probability 1: $6$ 10$m$ . Then observe that we can set the parameters$Z_{0}$

$$
I_{Z,A}
m\times k
$$

$n$ and $s$ in Theorem 5 equal to $k$ since $A ∈ R$ and $Z$ is in the span of the first $k$ standard

$$
(
)
$$

5 5 ˆbasis vectors. Then if $m > C$ log(2)$/ε$ $k$ =: $Ck$, we have that the quantity $δ$ in the theorem is 1 bounded by $ε$:

$$
(
)
$$

1$/$5

$$
k
$$

$δ$ := log(2) $6 ε$$C_{1}$

$$
m
$$

ˆso ) $>$ exp($−cεm$) for some positive universal constants $c$, and and $C$ depends1$−C_{2} C_{1}$, $C_{2}$

$$
P(E_{Z,A}
$$

$−$1polynomially on $ε$ .

We now proceed with the proof of the Uniform RCP.

Proof of Proposition 6. Let be the event defined in Lemma 24. By Lemma 24, we have that

$$
E_{Z,A}
$$

ˆif $m > Ck$, there exists an event with ) $>$ 1 $−$ exp($−cεm$) on which there exists a$C_{2}$

$$
E_{Z,A}P(E_{Z,A}
$$

2$k$finite subset of $Z$ with cardinality $6$ 10$m$ such that for any $z ∈ Z$ with $\|z\|$ = 1, there$Z_{0}$

$$
I_{Z,A}
$$

2$k$exists a $∈$ such that $6 ε$. The analogous finite set with cardinality $6$ 10$m$

$$
z_{0}Z_{0}\|z- z_{0}\|W_{0}
I_{W,A}
$$

ˆalso exists on the event with probability at least 1 $−$ exp($−cεm$). Thus if $m > Ck$, the$C_{2}$

$$
E_{W,A}
$$

event := $∩$ satisfies

$$
E_{Z,W}E_{Z,A}E_{W,A}
$$

) $>$ 1 $−$ exp($−cεm$)$.$2$C_{2}$

$$
P(E_{Z,W}
$$

We now establish concentration over and Let be the event that$Z_{0} W_{0}$. $E_{0}$

$$
\mid \mid
$$

$$
\mid \mid 〈A
x,y〉- x,y〉63\varepsilon \|x\|\|y\|
A_{w_{0}}
〈\Phi _{z_{0},w_{0}}
$$

$z_{0}$

$∀$ $x,y$ $∈ T,$ $z_{0}$

$$
\in Z_{0},
$$

$w_{0}$

$$
\in W_{0}.
$$

By Proposition 4, if $m > Ck$, we have that the following holds for fixed $∈$ and $∈$ with$z_{0} Z_{0} w_{0} W_{0}$ probability exceeding 1 $−$ 2exp($−c_{1}m$):

$$
\mid \mid
$$

$$
\mid \mid
$$

$〈A x,y〉 − x,y〉 6$ 3$ε\|x\|\|y\| ∀ x,y ∈ T.$

$$
A_{w_{0}}〈\Phi _{z_{0},w_{0}}
$$

$z_{0}$ Furthermore, on , a union bound over all $∈$ and $∈$ shows that$z_{0} Z_{0} w_{0} W_{0}$

$$
E_{Z,W}
$$

$$
)()
(
c_{1}m
c_{1}m
$$

$$
>1- \gamma mexp-
$$

$>$ 1 $−$ exp $−$$P$($E_{0}$)

$$
2I_{Z,A}I_{W,A}
$$

2 2 where $γ$ is a positive absolute constant and depends on $ε$.$c_{1}$ For the remainder of this proof, we work on the event $∩$ .$E_{0}$

$$
E_{Z,W}
$$

$w ∈ W$. Define the following set:

Fix non-zero $z ∈ Z$ and

Ω$z,w$ := ${` ∈$ [$m$] : $,z〉$ =

$$
〈a_{`}
$$

0 or

$$
,w〉=
〈a_{`}
$$

0$}.$

![diagram: p50 region 87](2008.10579v1-membrane/imageFile31.png)

Note that since $Z$ and $W$ are $k$-dimensional and any subset of $k$ rows of $A$ are linearly independent with probability 1, at most $k$ entries of $Az$ are zero and similarly for $Aw$. Hence $| 6$ 2$k$.

$$
|Ω_{z,w}
$$

Furthermore, observe that

$$
m
\sum
$$

T T$A$ =

$$
a
A_{w}
sgn(〈a_{`},z〉〈a_{`},w〉)a_{`}
z
`
$$

$`$=1

$$
\sum \sum
$$

TT

$$
a
$$

$a$ +=

$$
sgn(〈a_{`},z〉〈a_{`},w〉)a_{`}
sgn(〈a_{`},z〉〈a_{`},w〉)a_{`}
`
`
c
`\in Ω
`\in Ω_{z,w}
z,w
\sum
$$

T = $a$

$$
sgn(〈a_{`},z〉〈a_{`},w〉)a_{`}
`
c
`\in Ω
z,w
$$

by the definition of . However, on the event , there exists a $∈$ and $∈$ such

$$
Ω_{z,w}z_{0}Z_{0}w_{0}W_{0}
E_{Z,W}
c
$$

that for all $` ∈$ Ω ,

$$
z,w
$$

$,z〉$) = and $,w〉$) =

$$
,z_{0}〉),w_{0}〉)
$$

sgn($〈a_{`}$ sgn($〈a_{`}$ sgn($〈a_{`}$ sgn($〈a_{`}$

$$
m
$$

. Hencei.e. $z$ and (likewise $w$ and lie on the same side of each hyperplane defined by $}$$z_{0} w_{0}$)

$$
{a_{`}
$$

$`$=1 we have

$$
\sum \sum
$$

TT T = $a$

$$
A=a
A_{w}
sgn(〈a_{`},z_{0}〉〈a_{`},w_{0}〉)a_{`}
sgn(〈a_{`},z〉〈a_{`},w〉)a_{`}
z
`
`
c
c
`\in Ω
`\in Ω
z,w
z,w
\sum
$$

T T= $A$

$$
- a
sgn(〈a_{`},z_{0}〉〈a_{`},w_{0}〉)a_{`}
A_{w_{0}}
`
$$

$z_{0}$

$$
`\in Ω_{z,w}
$$

T ˜$^{T}$ ˜=: $A$

$$
- A
$$

$.$ (102)

$$
A_{w_{0}}
A_{w_{0}}
$$

$z_{0}$ $z_{0}$ ˜$^{T}$ ˜We now use the following lemma which says that $A$ is small when acting on $T$.

$$
A_{w_{0}}
$$

$z_{0}$

$$
m\times n
$$

Lemma 26. Fix 0 $< ε <$ 1 and $k < m$. Suppose that $A ∈ R$ has i.i.d. $N$(0$,$1$/m$) entries.

$$
nn
$$

Let $T ⊂ R$ be a $k$-dimensional subspace and and be subsets of $R$ . Let $E$ be the event the$W_{0} Z_{0}$ following inequality holds for all Ω $⊂$ [$m$] satisfying $|$Ω$| 6$ 2$k$:

˜$^{T}$ ˜

$$
|〈A
x,y〉|6\varepsilon \|x\|\|y\|\forall x,y\in T,\in \in (103)
w_{0}W_{0},z_{0}Z_{0}
A_{w_{0}}
$$

$z_{0}$ where

$$
\sum
$$

T˜$^{T}$ ˜

$$
A:=a.
sgn(〈a_{`},z_{0}〉〈a_{`},w_{0}〉)a_{`}
A_{w_{0}}
`
$$

$z_{0}$

$$
`\in Ω
$$

$−$1Then there exists a $>$ 0 such that if $m >$ 9$ε k$ and 2$k 6 P$($E$) $>$ 1 $−$ 2$m$exp($−εm/$36).

$$
\delta _{\varepsilon }\delta _{\varepsilon }m,
$$

Let $E$ be the event defined in Lemma 26. On the event $E ∩ ∩$ , we have that for all$E_{0}$

$$
E_{Z,W}
n- 1n- 1
$$

$z ∈ Z ∩ S$ and $w ∈ W ∩ S$ , there exists a $∈$ and $∈$ such that for any $x,y ∈ T$,$z_{0} Z_{0} w_{0} W_{0}$

$$
\mid \mid \mid \mid \mid \mid
$$

T T ˜$^{T}$ ˜

$$
\mid \mid 〈Ax,y〉- x,y〉=x,y〉- 〈Ax,y〉- \mid 〈Ax,y〉\mid A_{w}〈\Phi _{z,w}〈\Phi _{z,w}
z
A_{w_{0}}A_{w_{0}}
$$

$z_{0} z_{0}$

$$
\mid \mid
$$

T ˜$^{T}$ ˜

$$
\mid \mid 6〈Ax,y〉- x,y〉+|〈Ax,y〉|
〈\Phi _{z,w}
A_{w_{0}}A_{w_{0}}
$$

$z_{0} z_{0}$

$$
\mid \mid
$$

T ˜˜$^{T}$

$$
\mid \mid 6x,y〉- x,y〉x,y〉|
〈A+x,y〉- x,y〉|+|〈A
〈\Phi _{z,w}
A_{w_{0}}〈\Phi _{z_{0},w_{0}}A_{w_{0}}
|〈\Phi _{z_{0},w_{0}}
$$

$z_{0} z_{0}$ 88

$$
63\varepsilon \|x\|\|y\|+
\varepsilon \|x\|\|y\|+\varepsilon \|x\|\|y\|
\pi =:L\varepsilon \|x\|\|y\|
$$

88where we define $L$ := 3+ +1 $<$ 33. In the first equality, we used the event and (102). In the

$$
E_{Z,W}
\pi
$$

last inequality, we used the continuity of from Lemma 27 along with the event $∩E$. Letting

$$
\Phi _{z,w}E_{0}
$$

$−$1 ˆ ˆ:= 9$ε$ $C$ where $C$ is given by Lemma 24, we have that if $m >$ the event $E ∩ ∩$

$$
C_{\varepsilon }
C_{\varepsilon }k,E_{0}
E_{Z,W}
$$

holds with probability exceeding

$$
()
c_{1}m
$$

4$k$$P$($E ∩ ∩$ ) $>$ 1 $−$ 2$m$exp($−εm/$36) $− γm$ exp $− −$ exp($−cεm$)$E_{0}$ 2$C_{2}$

$$
E_{Z,W}
$$

2 4$k$

$$
>1- ˆ\gamma m˜
$$

exp($−c_{ε}m$)

where ˆ$γ$ is a positive absolute constant and ˜ depends polynomially on $ε$. The extension to the

$$
c_{\varepsilon }
$$

union of subspaces follows by applying (99) to all combinations of subspaces = ),

$$
T_{ij}span(U_{i},V_{j}
$$

, and where each have dimension at most 2$k$ and using a union bound.

$$
T_{ij}
W_{k}Z_{`}
$$

### Application to Range of Generative Model

We now apply Proposition 6 to prove Proposition 3:

Proof of Proposition 3. For pedagogical purposes, we first establish the lemma in the $d$ = 2 case.

$$
k
$$

In order to apply Proposition 6, we will show that ${G$($x$) $− G$($y$) : $x,y ∈ R}$ is a subset of the 6 2 2$k$union of at most 10 ($n$ subspaces of dimensionality at most 2$k$.$n_{2}$) 1 For fixed let = : $x 6$= 0$}$ and = : $x 6$= 0$}$. By Lemma 15

$$
W_{1},W_{2},A_{+,1}{W_{1,+,x}B_{+,2}{W_{2,+,x}
k
$$

in [33], there exists a probability 1 event, $E$, over on which $6$ 10$n$ and $6$

$$
(W_{1},W_{2})|A_{+,1}||B_{+,2}|
$$

$$
2kk
$$

10 $n n$ . On $E$, 1 2 3 2 $k$: $x 6$= 0$}| 6$ 10 ($n .$

$$
|{W_{2,+,x}W_{1,+,x}n_{2})
$$

1 Note that $6 k$ for all $x 6$= 0. Hencedimrange($W_{2}_{,}_{+}_{,x}W_{1}_{,}_{+}_{,x}$)

$$
kk- 1
$$

${G$($x$) : $x ∈ R} ⊂$ : $x,w ∈ S} ⊂ V$

$$
{W_{2,+,x}W_{1,+,x}w
$$

3 2 $k$where $V$ the union of at most 10 ($n$ subspaces of dimensionality at most $k$. This implies that$n_{2}$) 1

$$
k'
$$

${G$($x$) $− G$($y$) : $x,y ∈ R} ⊂ V$

$′$ 6 2 2$k$where $V$ is the union of at most 10 ($n$ subspaces of dimensionality at most 2$k$.$n_{2}$) 1

$$
' '
$$

By applying the second half of Proposition 6 to the sets $V$ , $V$ , $V$ , and $V$ , we get that for fixed $W_{1}$, $W_{2}$,

$$
|〈(AA- \Phi - -
$$

)($G$($x_{1}$) $G$($x_{2}$))$,G$($x_{3}$) $G$($x_{4}$)$〉|$$G$($y$) $G$($x$)$,G$($y$)$G$($x$)

$$
6- -
$$

$Lε\|G$($x_{1}$) $G$($x_{2}$)$\|\|G$($x_{3}$) $G$($x_{4}$)$\|$ (104)

with probability at least

3(2)+6(2) 2 2$k$+4$k$ 4$k$ ˜ 4$k$ ˜

$$
- c_{\varepsilon }m- c_{\varepsilon }m/2
$$

1 $−$ 10 ($n$ ˆ$γm e >$ 1 $−$ ˜$γm e ,$$n_{2}$) 1 $−$1ˆ ˜ ˆprovided $m >$ ˜ =: where ˜$γ$ and $K$ are positive universal constants,$KC_{ε}c k$ log($n_{1}n_{2}$) $C_{ε}k$ log($n_{1}n_{2}$),

$$
\varepsilon
$$

$−$1˜ depends polynomially on $ε$, and depends polynomially on $ε$ .

$$
c_{\varepsilon }C_{\varepsilon }
$$

Integrating over the probability space of independence of $A$ and implies($W_{1},W_{2}$), ($W_{1},W_{2}$) that (104) holds for random with the same probability bound. Continuing from (104), we($W_{1},W_{2}$) have

$$
|〈(AA- \Phi - -
$$

)($G$($x_{1}$) $G$($x_{2}$))$,G$($x_{3}$) $G$($x_{4}$)$〉|$$G$($y$) $G$($x$)$,G$($y$)$G$($x$)

$$
6- -
$$

$Lε\|G$($x_{1}$) $G$($x_{2}$)$\|\|G$($x_{3}$) $G$($x_{4}$)$\|$

$k$ 4$k$ ˜

$$
- c_{\varepsilon }m/2
$$

$∈ R$ with probability at least 1 $−$ ˜$γm e$ for some positive absolute con-

$$
\forall x,y,x_{1},x_{2},x_{3},x_{4}
$$

stant ˜$γ$ and ˜ depends polynomially on $ε$.

$$
c_{\varepsilon }
$$

The case for $d >$ 2 follows similarly. We have

$$
d- 1
$$

1 ($d$ ) $d$ 2 $k$$|{$Π : $x 6$= 0$}| 6$ 10 ($n n ···n$ )

$$
W_{i,+,x}
n_{d}
$$

$$
i=dd- 1
$$

$$
k1k- 1
$$

on the probability 1 event. This implies that ${G$($x$) : $x ∈ R} ⊂{$Π : $x,w ∈ S}$ is

$$
W_{i,+,x}w
i=d
$$

$$
d- 1
$$

($d$ ) $d$ 2 $k$a subset of the union of at most 10 ($n n ···n$ ) subspaces of dimensionality at most $k$.

$$
n_{d}
$$

$$
d- 1
k
$$

Moreover, ${G$($x$) $− G$($y$) : $x,y ∈ R}$ is a subset of the union of at most

$$
d- 1
$$

(2$d$ ) $d$ 2 2$k$10 ($n n ···n$ )

$$
n_{d}
$$

$$
d- 1
$$

2 subspaces of dimensionality at most 2$k$. Hence the analogous bound (104) holds for all $∈$

$$
x,y,x_{1},x_{2},x_{3},x_{4}
k
$$

$R$ with probability at least

$$
d- 1
$$

(2$d$ +4$d$ ) $d$ 2 2$k$+4$k$ 4$k$ ˜ 4$k$ ˜

$$
- c_{\varepsilon }m- c_{\varepsilon }m/2
$$

1 $−$ 10 ($n n ···n$ ) ˆ$γm e >$ 1 $−$ ˜$γm e ,$

$$
n_{d}
$$

$$
d- 1
$$

2 ˜provided $m >$ ), where ˜$γ$ is a positive absolute constant, ˜ depends polynomi-

$$
C_{\varepsilon }dklog(n_{1}n_{2}c_{\varepsilon }
\cdot \cdot \cdot n_{d}
$$

$−$1˜ally on $ε$, and depends polynomially on $ε$ .

$$
C_{\varepsilon }
$$

### RRCP Supplementary Results

Proof of Proposition 5. Fix 0 $< ε <$ 1. Suppose (89) holds and fix $x,y ∈ T$. Without loss of generality, assume $x$ and $y$ are unit normed. We will use the shorthand notation Φ = . Since

$$
\Phi _{z,w}
$$

$T$ is a subspace, $x − y ∈ T$ so by (89),

$$
\mid \mid
$$

$$
\mid \mid 6\varepsilon \|x- y\|
〈A(x- y),x- y〉- 〈\Phi (x- y),x- y〉
A_{w}
z
$$

or equivalently

2 T 2$〈$Φ($x − y$)$,x − y〉 − ε\|x − y\| 6 〈A$ ($x − y$)$,x − y〉 6 〈$Φ($x − y$)$,x − y〉$ + $ε\|x − y\| .$

$$
A_{w}
z
$$

(105)

Note that

and

2$\|x − y\|$ = 2 $−$ 2$〈x,y〉,$

$〈$Φ($x − y$)$,x − y〉$ = $〈$Φ$x,x〉$ + $〈$Φ$y,y〉 −$ 2$〈$Φ$x,y〉,$

T T T T$〈A$ ($x − y$)$,x − y〉$ = $〈A x,x〉$ + $〈A y,y〉 −$ 2$〈A x,y〉$

$$
A_{w}A_{w}A_{w}A_{w}
zzzz
$$

Twhere we used the fact that Φ and $A$

$$
A_{w}
z
$$

are symmetric. Rearranging (105) yields

$$
()()()
$$

T T T2 $〈$Φ$x,y〉 − 〈A x,y〉 6 〈$Φ$x,x〉 − 〈A x,x〉$ + $〈$Φ$y,y〉 − 〈A y,y〉$ + (2 $−$ 2$〈x,y〉$)$ε.$

$$
A_{w}A_{w}A_{w}
zzz
$$

By assumption, the first two terms are bounded from above by $ε$. Thus

so

$$
()
$$

T2 $〈$Φ$x,y〉 − 〈A x,y〉 6$ 2$ε$ + (2 $−$ 2$〈x,y〉$)$ε$ = 2(2 $− 〈x,y〉$)$ε 6$ 6$ε$

$$
A_{w}
z
$$

$$
x,y〉63\varepsilon .
〈\Phi x,y〉- 〈A
A_{w}
z
$$

The lower bound is identical and establishes the desired result.

Proof of Lemma 25. It suffices to prove the same upperbound for $|{$sgn($Av$) : $v ∈ V}|.$ Let $`$ = dim$V$ . By rotational invariance of Gaussians, we may take $V$ = ) without loss of

$$
span(e_{1},...,e_{`}
`
$$

generality. Without loss of generality, we may let $A$ have dimensions $m × `$ and take $V$ = $R$ .

$$
`
$$

We will appeal to a classical result from sphere covering [66]. If $m$ hyperplanes in $R$ contain the origin and are such that the normal vectors to any subset of $`$ of those hyperplanes are independent, then the complement of the union of these hyperplanes is partitioned into at most

$$
()
`- 1
\sum m- 1
$$

$$
i
$$

$i$=0

disjoint regions. Each region uniquely corresponds to a constant value of sgn($Av$) that has all non-zero entries. With probability 1, any subset of $`$ rows of $A$ are linearly independent, and thus,

$$
()
`- 1
(
)
\sum `
m- 1em
`
`
|{sgn(Av):v\in R,6=0\forall i}|6262`
$$

$6$ 10$m$

$$
(Av)_{i}
i
`
$$

$i$=0

$$
)
(
m
`
$$

$6$ ($em/`$) and the second inequality uses thatwhere the first inequality uses the fact that

$$
`
`
$$

2$`$($e/`$) $6$ 10 for all $` >$ 1. For arbitrary $v$, at most $`$ entries of $Av$ can be zero by linear independence of the rows of $A$. At each $v$, there exists a direction ˜$v$ such that ($A$($v 6$= 0 for all $i$ and for all $δ$ sufficiently small.+ $δ$˜$v$))$_{i}$

$$
`
$$

Hence, sgn($Av$) differs from one of ${$sgn($Av$) : $v ∈ R , 6$= 0 $∀ i}$ by at most $`$ entries. Thus,

$$
(Av)_{i}
$$

$$
()
m
````2`
$$

$|{$sgn($Av$) : $v ∈ R}| 6 |{$sgn($Av$) : $v ∈ R , 6$= 0 $∀ i}| 6 m$ 10$m$ = 10$m .$

$$
(Av)_{i}
`
$$

TProof of Lemma 26. For any Ω $⊂$ [$m$], let denote the submatrix of $A$ with rows $a$ where $` ∈$ Ω.$A_{Ω}$

$$
`
$$

We claim that it suffices to show

$$
\sqrt x\|6\varepsilon \|x\|\forall x\in T\forall Ω\subset [m]satisfying|Ω|62k6
\delta _{\varepsilon }m.
\|A_{Ω}
$$

(106)

To see this, observe that for any $∈ ∈$ and $x,y ∈ T$ and Ω $⊂$ [$m$], we have that$w_{0} W_{0}$, $z_{0} Z_{0}$,

˜$^{T}$ ˜

$$
|〈A
x,y〉|=
$$

$z_{0}$)$|〈$diag(sgn($A_{Ω}$

$$
A_{w_{0}}
$$

$z_{0}$

$$
6
$$

$z_{0}$)$\|$diag(sgn($A_{Ω}$

$$
6y\|
\|A_{Ω}x\|\|A_{Ω}
$$

$$
y〉|
$$

sgn($A_{Ω}w_{0}$))$A_{Ω}x,A_{Ω}$

$$
y\|
$$

sgn($A_{Ω}w_{0}$))$\|\|A_{Ω}x\|\|A_{Ω}$

where we used the Cauchy-Schwarz inequality in the first inequality. Hence establishing (106) will imply the desired conclusion. By the rotational invariance of the Gaussian distribution, we may take $T$ to be in the span of

$$
m\times k
$$

the first $k$ standard basis vectors. We may further without loss of generality assume $A ∈ R$ so

$$
\sqrt
$$

$ε$. Fix Ω $⊂$ [$m$] satisfying $|$Ω$| 6$ 2$k$. By Corollary 5.35 in [60], weit suffices to establish $\| 6$

$$
\|A_{Ω}
$$

2have that for any $t >$ 0, it holds with probability 1 $−$ 2exp($−t /$2) that

$$
\sqrt \sqrt \sqrt \|6|Ω|+k+t.
m\|A_{Ω}
$$

$$
\sqrt \sqrt
$$

Taking $t$ = $εm/$3, we conclude that if $|$Ω$| 6 εm/$9 and $m >$ 9$k/ε$, then $ε$ with probability

$$
\|6
\|A_{Ω}
$$

1 $−$ 2exp($−εm/$18)$.$

![diagram: p54 region 92](2008.10579v1-membrane/imageFile32.png)

![diagram: p54 region 93](2008.10579v1-membrane/imageFile33.png)

$$
\sqrt
$$

We now establish that $\| 6 ε$ holds simultaneously over all subsets Ω $⊂$ [$m$] of a sufficiently

$$
\|A_{Ω}
(
)
\delta e
$$

small size with a union bound. Observe that since = 1, there exists a $>$ 0 such that

$$
\delta _{* }
$$

lim$_{δ}_{→}_{0}$

$$
\delta )
(
\delta _{* }
\sqrt e
$$

$6$ exp($ε/$36). Put := Let $E$ be the event that $\| 6 ε$ for all subsets

$$
\delta _{\varepsilon }min{\varepsilon ,\delta _{* }}.
\|A_{Ω}
\delta _{* }
$$

$−$1Ω $⊂$ [$m$] satisfying $|$Ω$| 6$ 2$k 6$ If $m >$ 9$ε k$, a union bound shows that this event holds with

$$
\delta _{\varepsilon }m.
$$

probability at least

$$
()()
b\delta _{\varepsilon }mc
\sum mm
$$

exp($−εm/$18) $>$ 1 $−$ exp($−εm/$18)1 $−$ 2

$$
2b\delta _{\varepsilon }mc
`
b\delta _{\varepsilon }mc
$$

$`$=1

$$
(
)
\delta _{\varepsilon }m
em
>1-
$$

exp($−εm/$18)

$$
2b\delta _{\varepsilon }mc
\delta _{\varepsilon }m
]
[
m
)
(
\delta _{\varepsilon }
e
$$

= 1 $−$ exp($−εm/$18)

$$
2b\delta _{\varepsilon }mc
\delta _{\varepsilon }
$$

$$
>1-
$$

2$bδ_{ε}mc$exp($−εm/$36) $>$ 1 $−$ 2$m$exp($−εm/$36)

$$
)
(
\delta _{\varepsilon }
e
$$

$6$ exp($ε/$36) in the second to last inequality.where we used the fact that

$$
\delta _{\varepsilon }
$$

$$
n
$$

We now prove the continuity of for non-zero $z,w ∈ R$ . Recall that

$$
\Phi _{z,w}
$$

$$
\pi - 2\theta _{z,w}2sin\theta _{z,w}
$$

$$
\Phi _{z,w}I_{n}
M_{ˆz↔ˆw}
\pi \pi
$$

where := $∠$($z,w$) and is the matrix that sends ˆ$z 7→$ ˆ$w 7→$ + and

$$
\theta _{z,w}M_{z↔w}e_{1},cos\theta _{z,w}e_{1}sin\theta _{z,w}e_{2},
\perp
$$

$h 7→$ 0 for all $h ∈$ span(${z,w}$ )$.$

$$
n- 1
$$

Lemma 27 (Continuity of ). Fix 0 $< ε <$ 1 and $z,w ∈ S$ . If $\|$˜$z − z\| 6 ε$ and $\|$ ˜$w − w\| 6 ε$

$$
\Phi _{z,w}
n- 1
$$

for some ˜$z,$ ˜$w ∈ S$ , then

$$
- \|6\varepsilon .
\|\Phi _{˜z,˜w}\Phi _{z,w}
\pi
$$

Proof of Lemma 27. In this proof, we will utilize the following three inequalities:

$$
n- 1
- |6|,\forall \in S
,y,yx_{1},x_{2},y
|\theta _{x_{1}}\theta _{x_{2}}|\theta _{x_{1},x_{2}}
n- 1
/2)6\|x- y\|,\forall x,y\in S
$$

2sin($θ_{x,y}$ $θ/$4 $6$ sin($θ/$2)$, ∀ θ ∈$ [0$,π$]$.$

(107)

(108) (109)

Observe that

$$
\| \| \| \| - |
2|\theta _{˜z,˜w}\theta _{z,w}
$$

2sin$θ_{˜}_{z,}_{˜}_{w}$ 2sin$θ_{z,w}$

$$
\| \| - \|6
.
$$

+ $−$

$$
\|\Phi _{˜z,˜w}\Phi _{z,w}
\|I_{n}\|M_{˜z↔˜w}M_{z↔w}
\| \| \pi \pi \pi
$$

First, observe that by (107), we have that

$|θ$˜$z,$ ˜$w$

$$
- |6
\theta _{z,w}|\theta _{˜z,˜w}
$$

$$
- |+
\theta _{z,˜w}|\theta _{z,˜w}
$$

$− | 6 |$ + $|.$

$$
\theta _{z,w}|\theta _{˜z,z}|\theta _{˜w,w}
$$

Then, by (108) and (109), we have that

$$
|6/2)62\|˜z- z\|62\varepsilon .
|\theta _{˜z,z}4sin(\theta _{˜z,z}
$$

The same upper bound holds for $|$. Thus we attain

$$
|\theta _{˜w,w}
$$

$|θ$˜$z,$ ˜$w$ $− | 6 |$ + $| 6$ 4$ε.$

$$
\theta _{z,w}|\theta _{˜z,z}|\theta _{˜w,w}
$$

(110)

Let $R$ be a rotation matrix that maps $z 7→$ and $w 7→$ + where and$e_{1}$ cos$θ_{z,w}e_{1}$ sin$θ_{z,w}e_{2} e_{1} e_{2}$ ˜are the first and second standard basis vectors, respectively. Let $R$ denote the matrix that applies T ˜$^{T}$ ˜ ˜the same rotatation to the system ˜$z$ and ˜$w$. Recall that := $R DR$ and := $R DR$

$$
M_{z↔w}M_{˜z↔˜w}
$$

where

$$

cos\theta _{z,w}

$$

$D$ :=

$$
sin\theta _{z,w}
$$

$$
sin\theta _{z,w}
$$

$−$cos$θ_{z,w}$ 0

0 0 0$k−$2

$$

$$

cos $θ$˜$z,$ ˜$w$ ˜

$$

$$

and $D$ := sin $θ$˜$z,$ ˜$w$ 0

sin $θ$˜$z,$ ˜$w$ $−$cos$θ_{˜}_{z,}_{˜}_{w}$ 0

0 0 0$k−$2

$$

$$

$$

.
$$

An elementary calculation shows that $D$ has 2 pairs of non-zero eigenvalues and eigenvectors ($λ_{1},d_{1}$) and where($λ_{2},d_{2}$)

while

= $−$1 and = $−$ +$λ_{1} d_{1}$ (cos$θ_{z,w}$ 1)$e_{1}$ sin$θ_{z,w}e_{2}$

= 1 and = + +$λ_{2} d_{2}$ (cos$θ_{z,w}$ 1)$e_{1}$ sin$θ_{z,w}e_{2}.$

T TLet $D$ = + be the eigenvalue decomposition for $D$. Then by the definition of ,

$$
- d_{1}dd_{2}dM_{z↔w}
$$

1 2 T T T T T T T

$$
=RDR=- RR+RR=:+
$$

$M_{z}_{↔}_{w} d_{1}d d_{2}d −v_{1}v v_{2}v$ 1 2 1 2 T Tso := $R$ and := $R$ are the eigenvectors of with corresponding eigenvalues $−$1 and

$$
v_{1}d_{1}v_{2}d_{2}M_{z↔w}
$$

1, respectively. Then, recall that $Rz$ = while $Rw$ = Thus the eigenvectors$e_{1}$ cos$θ_{z,w}e_{1}$ +sin$θ_{z,w}e_{2}$. and can be written as$d_{1} d_{2}$

$$
=Rw- Rzand=Rw+Rz.
$$

$d_{1} d_{2}$

Thus the eigenvectors of are precisely

$$
M_{z↔w}
$$

= $w − z$ and = $w$ + $z.$$v_{1} v_{2}$

By the same argument, the eigenvectors of are

$$
M_{˜z↔˜w}
$$

˜ = ˜$w −$ ˜$z$ and ˜ = ˜$w$ + ˜$z$$v_{1} v_{2}$

with corresponding eigenvalues $−$1 and 1, respectively. Hence, we have that

$$
(
)
$$

2sin$θ_{z,w}$ 2sin$θ_{z,w}$ T T = +

$$
M_{z↔w}
- v_{1}vv_{2}v
$$

$$
\pi \pi ()
$$

2sin$θ_{z,w}$ T T$−$($w − z$)($w − z$) + ($w$ + $z$)($w$ + $z$)=

$$
\pi
$$

and likewise

$$
(
)
$$

2sin$θ_{˜}_{z,}_{˜}_{w}$ 2sin$θ_{˜}_{z,}_{˜}_{w}$ T T = $−$( ˜$w −$ ˜$z$)( ˜$w −$ ˜$z$) + ( ˜$w$ + ˜$z$)( ˜$w$ + ˜$z$)

$$
.
M_{˜z↔˜w}
\pi \pi
$$

For simplicity of notation, let $h$ = $w − z$,

˜$h$ = ˜$w −$ ˜$z$, $g$ = $w$ + $z$, and ˜$g$ = ˜$w$ + ˜$z$. Then

$$
\| \| \| )\| (
\| \| ()
$$

$$
\| \|
$$

2sin$θ_{˜}_{z,}_{˜}_{w}$2sin$θ_{z,w}$ T T T T˜˜

$$
\| \| - =- hh+gg+hh- ˜g˜g
\| M_{z↔w}M_{˜z↔˜w}
$$

$∥$sin$θ_{z,w}$ sin$θ_{˜}_{z,}_{˜}_{w}$

$$
\| \| \pi \pi \pi ()
$$

2 T T T T˜˜

$$
hh- hh\|+gg- ˜g˜g\|.
6
$$

$\|$sin$θ_{z,w}$ sin$θ_{˜}_{z,}_{˜}_{w} \|$sin$θ_{z,w}$ sin$θ_{˜}_{z,}_{˜}_{w}$

$$
\pi
$$

![diagram: p56 region 96](2008.10579v1-membrane/imageFile34.png)

$$
n- 1
$$

˜Note that since $z,w,$ ˜$z,$ ˜$w ∈ S$ , $\|h\|,\|h\|,\|g\|,\|$˜$g\| 6$ 2. In addition,

˜$\|h − h\| 6 \|z −$ ˜$z\|$ + $\|w −$ ˜$w\| 6$ 2$ε$

and (110) implies

Hence

$|$sin$θ_{z,w}$

$$
- |6
sin\theta _{˜z,˜w}|\theta _{z,w}
$$

$− | 6$ 4$ε.$

$$
\theta _{˜z,˜w}
$$

T T T T T T˜ ˜˜˜˜ ˜

$$
hh- hh\|6hh- hh\|+hh- hh\|
\|sin\theta _{z,w}sin\theta _{˜z,˜w}\|sin\theta _{z,w}sin\theta _{z,w}\|sin\theta _{z,w}sin\theta _{z,w}
$$

T T˜˜ ˜˜

$$
+hh- hh\|
$$

$\|$sin$θ_{z,w}$ sin$θ_{˜}_{z,}_{˜}_{w}$ T˜ ˜ ˜ ˜˜

$$
6|\|h\|\|h- h\|+|\|h\|\|h- h\|+\|hh- |
|sin\theta _{z,w}|sin\theta _{z,w}\||sin\theta _{z,w}sin\theta _{˜z,˜w}
$$

$6$ 20$ε.$

T TThe same bound holds for $gg −$ ˜$g$˜$g \|$. Hence we attain$\|$sin$θ_{z,w}$ sin$θ_{˜}_{z,}_{˜}_{w}$

$$
\| \| \| \|
$$

802sin$θ_{z,w}$ 2sin$θ_{˜}_{z,}_{˜}_{w}$

$$
\| \| - \varepsilon .
6
M_{z↔w}M_{˜z↔˜w}
\| \| \pi \pi \pi
$$

(111)

Combining (110) and (111), we see that

$$
\| \| \| \| - |
$$

$$
2|\theta _{˜z,˜w}\theta _{z,w}2sin\theta _{˜z,˜w}2sin\theta _{z,w}
\| \|
$$

$− \| 6$ + $− 6 ε.$

$$
\|\Phi _{˜z,˜w}\Phi _{z,w}\|I_{n}\|M_{˜z↔˜w}M_{z↔w}
\| \| \pi \pi \pi \pi
$$

We now prove the inequalities used in the proof of Lemma 27.

Proof of equations (107), (108), and (109). of [22]. Observe that we can write

and

For (107), we proceed similarly to the proof on page 12

$$
\perp
$$

= $y$ + $y$

$$
x_{1,y,y}
$$

cos$θ_{x}_{1}$ sin$θ_{x}_{1}$

$$
\perp
$$

= $y$ + $y$

$$
x_{2,y,y}
$$

cos$θ_{x}_{2}$ sin$θ_{x}_{2}$

$$
\perp \perp
$$

where $y$ and $y$ are unit vectors that are orthogonal to $y$. 1 2

Then observe that

$$
\perp \perp =y+yy+y〉
$$

$〈x$1$, x$2$〉 ,y ,y ,y ,y$ 1 2

$〈$cos$θ_{x}_{1}$ sin$θ_{x}_{1} ,$cos$θ_{x}_{2}$ sin$θ_{x}_{2}$

$$
\perp \perp =+〈y,y〉.
,y,y,y,y
$$

cos$θ_{x}_{1}$ cos$θ_{x}_{2}$ sin$θ_{x}_{1}$ sin$θ_{x}_{2}$

$$
\perp \perp \perp \perp
$$

Since $∈$ [0$,π$], we have that $>$ 0. In addition, $〈y ,y 〉 6 \|y \|\|y \|$ = 1

$$
,y,y,y,y
$$

$θ_{x}_{1} ,θ_{x}_{2}$ sin$θ_{x}_{1}$ sin$θ_{x}_{2}$ 1 2 1 2 so we attain

$6$ + = $−$ )$〈x$1$, x$2$〉 ,y ,y ,y ,y ,y ,y$cos$θ_{x}_{1}$ cos$θ_{x}_{2}$ sin$θ_{x}_{1}$ sin$θ_{x}_{2}$ cos($θ_{x}_{1} θ_{x}_{2}$

$−$1by the trigonometric identity cos($α ∓ β$) = cos$α$cos$β ±$ sin$α$sin$β.$ Since the function cos is decreasing on [$−$1$,$1], we see that

$−$1$− 6$ cos = $.$

$$
,y,y(〈x_{1},x_{2}〉)
$$

$θx_{1} θx_{2} θx_{1},x_{2}$

![diagram: p57 region 97](2008.10579v1-membrane/imageFile35.png)

![diagram: p57 region 98](2008.10579v1-membrane/imageFile36.png)

$$
|.
$$

so we attain $− | 6$Similarly, $− 6$

$$
,y,y
,y,y
|\theta _{x_{1}}\theta _{x_{2}}|\theta _{x_{1},x_{2}}
\theta _{x_{2}}\theta _{x_{1}}\theta _{x_{1},x_{2}}
n- 1
$$

For (108), observe that for $x,y ∈ S$ ,

2 2 2$\|x − y\|$ = $\|x\|$ + $\|y\| −$ 2$〈x,y〉$ 2 2

$$
=\|x\|+\|y\|- 2\|x\|\|y\|cos\theta _{x,y}
$$

= 2(1 $−$ )$.$

$$
cos\theta _{x,y}
$$

Thus, using the half angle formula

$$
\sqrt \rfloor )
(\lfloor \theta
$$

$θ$ 1 $−$ cos$θ$ sin = sgn 2$π − θ$ + 4$π$ 2 4$π$ 2

we see that

$$
\sqrt \sqrt
$$

1 $−$

$$
cos\theta _{x,y}\theta _{x,y}
$$

2(1 $−$ ) = 2$\|x − y\|$ = $>$ 2sin $.$

$$
cos\theta _{x,y}
$$

$$
\theta
$$

$− θ$ is positive for all $θ ∈$ [0$,π$]$.$For (109), one can note that the function $ψ$($θ$) := 4sin 2

### Acknowledgements

PH is partially supported by NSF CAREER Grant DMS-1848087. OL acknowledges support by the NSF Graduate Research Fellowship under Grant No. DGE-1450681.

![diagram: p58 region 99](2008.10579v1-membrane/imageFile37.png)

