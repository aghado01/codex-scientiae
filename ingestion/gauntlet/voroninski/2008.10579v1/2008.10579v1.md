[Page 1]

# Compressive Phase Retrieval: Optimal Sample Complexity with Deep Generative Priors

Paul Hand ∗ , Oscar Leong † , and Vladislav Voroninski ‡

August 25, 2020

# Abstract

Advances in compressive sensing provided reconstruction algorithms of sparse signals from linear measurements with optimal sample complexity, but natural extensions of this methodology to nonlinear inverse problems have been met with potentially fundamental sample complexity bottlenecks. In particular, tractable algorithms for compressive phase retrieval with sparsity priors have not been able to achieve optimal sample complexity. This has created an open problem in compressive phase retrieval: under generic, phaseless linear measurements, are there tractable reconstruction algorithms that succeed with optimal sample complexity? Meanwhile, progress in machine learning has led to the development of new data-driven signal priors in the form of generative models, which can outperform sparsity priors with signiﬁcantly fewer measurements. In this work, we resolve the open problem in compressive phase retrieval and demonstrate that generative priors can lead to a fundamental advance by permitting optimal sample complexity by a tractable algorithm in this challenging nonlinear inverse problem. We additionally provide empirics showing that exploiting generative priors in phase retrieval can signiﬁcantly outperform sparsity priors. These results provide support for generative priors as a new paradigm for signal recovery in a variety of contexts, both empirically and theoretically. The strengths of this paradigm are that (1) generative priors can represent some classes of natural signals more concisely than sparsity priors, (2) generative priors allow for direct optimization over the natural signal manifold, which is intractable under sparsity priors, and (3) the resulting nonconvex optimization problems with generative priors can admit benign optimization landscapes at optimal sample complexity, perhaps surprisingly, even in cases of nonlinear measurements.

# 1 Introduction

The study of inverse problems pervades virtually all of the natural sciences including biological and astronomical imaging, X-ray crystallography, oil exploration, and shape optimization and reconstruction. An object of interest is observed via some forward mapping process, and the task is to recover the object, often subject to ill-posedness and noise. In order to increase ﬁdelity of the estimate or decrease the number of required measurements, one can enforce structural assumptions or priors on the signal, a practice dating as far back as Tikhonov regularization [ 59 ] and the Nyquist sampling theorem [ 47 ]. A canonical example of an ill-posed inverse problem in the ﬁeld of imaging is compressive sensing (CS), in which one aims to recover a signal from undersampled linear measurements. By exploiting the sparsity of natural images in the wavelet domain as a structural prior,

∗ Department of Mathematics and Khoury College of Computer and Information Science, Northeastern University, Boston, MA

† Department of Computational and Applied Mathematics, Rice University, Houston, TX

‡ Helm.ai, Menlo Park, CA

[Page 2]

In terms of theory, advances in CS have provided reconstruction algorithms using sparsity priors with information theoretically optimal sample complexity [ 13 , 20 ]. A seminal result in the ﬁeld states that if given m < n undersampled linear measurements b ∗ = Ay ∗ where A ∈ R m × n has i.i.d. Gaussian entries and y ∗ ∈ R n is an s -sparse signal, recovery is guaranteed with high probability when m = O ( s log n ) by solving the following convex program:

$$
\min _ { y \in \mathbb { R } ^ { n } } \| y \| _ { 1 } \ s . t . \ A y = b _ { * } .
$$

The success of compressive sensing has popularized the notion of signal sparsity throughout the imaging sciences, resulting in sparsity becoming a common choice as a structural prior.

Sparsity-based priors when applied to nonlinear inverse problems such as phase retrieval have been met with potentially fundamental sample complexity bottlenecks. In phase retrieval, a signal y ∗ ∈ R n or C n is to be estimated from observations |  a i ,y ∗  | 2 , i = 1 , 2 ,...m . Compressive phase retrieval considers the case m < n , which requires structural priors to enable recovery. While an s -sparse signal is information theoretically recoverable from O ( s log n ) generic phaseless measurements, compressive phase retrieval algorithms have not achieved sample complexity below O ( s 2 log n ) [ 36 ]. In fact, convex algorithms such as PhaseLift [ 14 , 11 ], provably fail below O ( s 2 log n ) measurements under natural extensions to incorporate sparsity [ 44 , 49 ]. This has created an open problem in compressive phase retrieval to ﬁnd a computationally eﬃcient algorithm to reconstruct signals from generic, phaseless linear measurements with optimal sample complexity with respect to the signal’s intrinsic dimensionality. Furthermore, there is evidence to support that these sample complexity limitations may be fundamental for sparse phase retrieval. In the closely related sparse PCA problem, a reduction from planted clique was found, and it is widely conjectured to be NPhard [ 8 , 6 ]. These observations open the question of whether other signal priors may successfully achieve sample-optimal reconstruction algorithms.

Simultaneously, there has been tremendous progress on priors in the form of generative models given by a deep neural network, which in some cases signiﬁcantly outperforms sparsity priors at compressive sensing. These generative models, such as Generative Adversarial Networks [ 29 ] and Variational Autoencoders [ 41 ], learn an explicit mapping from a low-dimensional latent space R k to an approximation of the natural image manifold in R n and can be trained on datasets of various natural signal classes to create realistic, yet synthetic samples of human faces [ 39 ], MRIs [ 55 ], cells [ 48 ], human ﬁngerprints [ 46 ], and more. Enforcing a generative prior in CS tasks by directly optimizing over the latent space has been shown to outperform sparsity-based methods such as Lasso by 5-10x fewer measurements [ 9 ] in some cases. Moreover, while the optimization problem posed over latent space is non-convex, [ 33 ] showed that when the number of measurements m is proportional to k up to log factors, the empirical risk minimization problem under a suitable random generator model exhibits favorable global geometry in the sense that there are no spurious local minima away from small neighborhoods of the true solution and a negative multiple thereof.

The above empirical and theoretical evidence indicates that generative neural networks can potentially succeed as structural priors in nonlinear inverse problems where previous methods exploiting sparsity have thus far been met with likely fundamental bottlenecks.

In this work, we resolve the open problem in compressive phase retrieval by presenting a computationally efficient algorithm that achieves optimal sample complexity with generic measurements under a generative prior. In particular, we consider a deep generative prior for compressive phase retrieval by supposing that the desired signal lives in the range of a feed-forward neural network with ReLU activation functions and latent code dimensionality k . We establish the sufficiency of two deterministic conditions on the weights of the generative model and the measurement matrix to guarantee that the signal can be recovered by a subgradient descent algorithm. Moreover, we show that these conditions are satisfied with high probability for Gaussian weights and generic Gaussian measurements as soon as m is proportional to k , up to log factors, which is information theoretically optimal in k . In addition to our theoretical results, we empirically establish that exploiting generative models in phase retrieval tasks can significantly outperform sparsity-based methods.

[Page 3]

Subsequent to the publication of preliminary versions of the results of this paper [ 31 ], generative priors have also been shown to break through sample complexity barriers in PCA. In particular, all known algorithms to achieve optimal statistical sample complexity in sparse PCA are computationally intractable and all known polynomial time algorithms exhibit a sub-optimal quadratic sample complexity on the sparsity of the true signal [ 42 , 19 ]. Gaps of this nature have also been observed in a number of related problems [ 18 , 52 ]. However, with respect to PCA, recent work in both the asymptotic [ 2 ] and non-asymptotic regimes [ 16 ] have shown that the low rank matrix recovery problem with generative priors does not exhibit a computational-to-statistical gap, oﬀering further evidence of the beneﬁt of generative priors in inverse problems.

The results in the present work provide empirical and theoretical support to the notion that deep generative priors oﬀer a new paradigm for signal recovery that oﬀers fundamental advances. In this paradigm, a model of a natural signal class is learned from data in the form of a generative model. The generative model directly parameterizes a low-dimensional signal manifold, and recovering a signal subject to noisy measurements can be posed as a direct optimization problem whose search space is restricted to the range of the generative model. This paradigm has several strengths in comparison to sparsity priors. First, generative models may provide better compression of natural signals than sparsity priors. Precisely, the dimensionality of the manifold modeling the natural signal class under a generative prior may be lower than the sparsity level of the same signals. Second, generative priors allow for direct optimization over the natural signal manifold. In contrast, sparsity priors give rise to combinatorial optimization problems which can not be directly solved. Tractable convex relaxations have not been successful in important nonlinear settings. Third, the non-convex optimization problems under generative priors can admit benign optimization landscapes at optimal sample complexity even in the case of nonlinear measurements. This fundamental advance has so far not been realized by sparsity priors.

# 1.1 Related Work

Phase Retrieval: Some of the earliest methods to solve phase retrieval tasks are the non-convex alternating minimization Gerchberg-Saxton [ 26 ] and Fienup [ 25 ] algorithms. Recently, a variety of methods have been introduced that enjoy theoretical guarantees. Convex methods, such as the seminal lifting-based approach PhaseLift [ 14 ], can achieve optimal sample complexity for unstructured signals [ 11 ]. Further recovery guarantees have been extended to non-convex formulations such as Wirtinger Flow [ 12 , 58 , 56 ] and its non-smooth variant Amplitude Flow [ 23 , 56 , 67 ]. Other approaches include Phasemax [ 27 , 4 ], Phasecut [ 63 ], AltMinPhase [ 38 ], and alternating projection methods [ 62 ].

Since the success of exploiting sparsity in linear compressed sensing, many works have attempted to leverage similar techniques to solve the phase retrieval problem in the compressive setting m < n . When the n -dimensional signal is s -sparse, the information theoretic lower bound of m = O ( s log n ) measurements was shown to be required for the injectivity of phaseless Gaussian measurements [ 61 ]. However, attempts at achieving this optimal sample complexity via a polynomial time algorithm have proven quite diﬃcult and, in some cases, impossible. For example, the natural   1 -penalized variant of Phaselift was shown to be able to recover an s -sparse signal with O ( s 2 log n ) generic

[Page 4]

Some existing works in compressive phase retrieval establish optimal sample complexity recovery guarantees under non-generic measurements [ 36 ]. For example, [ 3 ] showed that assuming the measurement vectors were chosen from an incoherent subspace, then recovery is possible with O ( s log n s ) measurements. Also, using the notion of polarization, [ 5 ] showed that O ( s log n ) measurements also suﬃces for recovery when the measurement vectors have an associated graph with suﬃcient connectivity properties. However, these results would be diﬃcult to generalize to the experimental setting as their measurement design architectures are often unrealistic, with generic measurements oﬀering a closer model to the goal of Fourier diﬀraction measurements.

Signal recovery with generative priors: In [ 9 ], the authors studied enforcing a generative prior in the linear compressive sensing regime. In particular, given m linear measurements Ay ∗ where y ∗ ∈ R n , the authors modelled natural signals as being in the range of a trained generative model G : R k → R n where k   n . To solve the inverse problem, they proposed to ﬁnd a latent code x ∗ ∈ R k such that G ( x ∗ ) ≈ y ∗ by solving the following least squares objective

$$
\min _ { x \in \mathbb { R } ^ { k } } \frac { 1 } { 2 } \| A G ( x ) - A y _ { * } \| ^ { 2 } .
$$

They provided empirical evidence showing that 5-10x fewer measurements were needed to achieve comparable reconstruction errors, compared to standard sparsity-based approaches such as Lasso in some parameter regimes. Based on the success of generative priors in compressive sensing, a number of followup works have considered a similar setup in a variety of inverse problems, ranging from compressed sensing [ 32 , 35 , 53 , 28 , 57 ], denoising [ 34 ], phase retrieval [ 31 , 54 ], low-rank matrix recovery [ 16 , 2 ], one-bit compressive sensing [ 51 , 45 ], blind deconvolution [ 54 , 30 ], and more. This framework, in the case of compressive sensing, enjoys multiple theoretical analyses. A subset of the authors in [ 32 ] presented the ﬁrst global landscape analysis of the empirical risk minimization problem and showed that, in fact, when the network is suﬃciently expansive with Gaussian weights and the number of measurements is proportional to k , there exists a descent direction everywhere outside of potentially two small neighborhoods of the minimizer and true solution. Followup work later established convergence guarantees of ﬁrst order methods in compressed sensing [ 35 ] and denoising [ 34 ] under similar statistical assumptions on the generator. In the present work, we consider precisely the same random model in the context of compressive phase retrieval.

# 1.2 Compressive Phase Retrieval with Generative Priors

The compressive phase retrieval problem is as follows. We consider the real-valued version out of simplicity. Consider a signal y ∗ ∈ R n . Given m phaseless linear measurements of the form

$$
b _ { * } = | A y _ { * } | + \eta
$$

where m < n , A ∈ R m × n is a known linear operator, and η ∈ R m denotes measurement noise, the goal is to recover y ∗ from knowledge of b ∗ and A . As m < n , additional structure must be exploited to accurately estimate y ∗ . In this work, we assume that y ∗ belongs in or near the range of a trained generative model G : R k → R n . That is, y ∗ ≈ G ( x ∗ ) for some latent code x ∗ . In order to recover

[Page 5]

$$
\min _ { x \in \mathbb { R } ^ { k } } f ( x ) \colon = \frac { 1 } { 2 } \left \| | A G ( x ) | - b _ { * } \right \| ^ { 2 } .
$$

This formulation attempts to ﬁnd the signal in the range of the generative model G that is most consistent with provided measurements in a particular sense. It is motivated both by the nonconvex generative modeling formulation for compressed sensing in [ 9 ] with an Amplitude Flow [ 67 ] perspective from phase retrieval and was originally introduced by the present authors in [ 31 ]. As the underlying optimization problem is posed over an explicitly parameterized k -dimensional manifold where k   n , compressive phase retrieval may be possible from m = Ω( k )   n measurements.

In this paper, we prove that ( 3 ) can be solved with sample complexity proportional to k , under an appropriate model for G and a generic measurement model. This theoretical result is in stark contrast to algorithms for compressive phase retrieval based on sparsity priors, where no known tractable algorithm achieves information theoretically optimal sample complexity under a generic measurement model. This result extends the work of [ 34 , 35 ] which established similar algorithmic guarantees for recovery with generative priors in the linear measurement regimes of denoising and compressed sensing. Additionally, we provide experimental results that ( 3 ) can outperform sparsitybased compressive phase retrieval algorithms in the presence of a trained G from standardly available datasets.

The formulation assumes that the generative model G is already known. In practice, it typically is a neural network whose parameters (weights) are learned from a large collection of training images belonging to a particular natural signal class. The ﬁeld of generative modeling has demonstrated multiple types of neural networks which can be eﬀectively trained (e.g. Variational Autoencoders [ 41 ] and Generative Adversarial Networks [ 29 ]). The dimensionality k of the latent codes is ﬁxed at training time and its value is selected in order to balance multiple eﬀects; for example, the range of G should be large enough to approximately include all of the desired signal class, and the image representations should be as concise as possible. A particular image of interest may not be exactly in the range of a trained model G because the model has representation error , but this error is expected to become smaller as techniques for training generative models improve.

# 1.3 Deterministic and Probabilistic Models for Generative Priors

In order to establish recovery guarantees for phase retrieval with generative priors, we will assume a neural network architecture and a model for the weights of the network once trained. Our intention is to analyze a model which is realistic enough to describe trained nets, yet tractable enough to permit rigorous analysis of sample complexity for a convergent optimization algorithm. To achieve both of these objectives we consider the following models. We assume that the generative model G : R k → R n is given by a d -layer feedforward neural network with ReLU activation functions and no bias terms. Speciﬁcally, we assume that

$$
G ( x ) = \text {relu} ( W _ { d } \dots \text {relu} ( W _ { 2 } \text {relu} ( W _ { 1 } x ) ) \dots )
$$

where relu ( x ) := max( x, 0) acts entrywise and each W i ∈ R n i × n i − 1 for i ∈ [ d ] with k = n 0 < n 1 < n 2 < ··· < n d = n. Each matrix W i corresponds to the neural network weights of the i -th layer, and the j -th row of W i are the weights of the j -th neuron in the i -th layer.

We will assume an expansive-Gaussian probabilistic model for the weights of G . That is, n i increases sufficiently with i , and the weights within each layer are i.i.d. Gaussians. This model was introduced by a subset of the authors in [33]. We additionally assume a Gaussian model of the measurement matrix A . The justification for these assumptions is as follows. The Gaussianicity of A ensures that measurements are suitably generic, and, indeed, achieving optimal sample complexity in sparse phase retrieval has not been attained for this measurement model. Regarding the expansivity assumption, we note that generative models with low dimensional latent spaces are inherently expansive when considered as a whole. In a sense, the network and each layer therein could be viewed as adding redundancy to a more compact representation, though in practice some successful network architectures do not have strict layerwise expansivity. Regarding the Gaussianicity model of neural network weights, it has been shown that neural networks, such as AlexNet, trained on real data have resulting weights with statistics similar to Gaussians [1]. We emphasize that the use of generative models as priors in regularizing inverse problems is nascent, and we use this model because it balances mathematical tractability with authenticity toward applications.

[Page 6]

In order to establish a recovery guarantee for this random model, we establish it for models satisfying deterministic conditions on G and A . Then we show that an appropriate expansiveGaussian model satisﬁes these deterministic conditions with high probability. The ﬁrst deterministic condition we consider roughly states that the neural network weights are approximately distributed uniformly on a sphere of a particular radius. For W ∈ R n × k and x ∈ R k , deﬁne W + ,x := diag( Wx > 0) W where the i -th diagonal entry of diag( v > 0) is 1 if v i > 0 and 0 otherwise. Note that W + ,x x = relu ( Wx ) . The condition is stated as follows and was introduced in [ 33 ]:

Deﬁnition 1 (Weight Distribution Condition) . We say that W ∈ R n × k satisﬁes the Weight Distribution Condition (WDC) with constant ε > 0 if for all nonzero x,y ∈ R k ,

$$
\left \| W _ { + , x } ^ { T } W _ { + , y } - Q _ { x , y } \right \| \leqslant \varepsilon
$$

where glyph[negationslash]

$$
Q _ { x , y } \coloneqq \frac { \pi - \theta _ { x , y } } { 2 \pi } I _ { k } + \frac { \sin \theta _ { x , y } } { 2 \pi } M _ { \hat { x } \leftrightarrow \hat { y } } .
$$

Here θ x,y = ∠ ( x,y ) , ˆ x = x/   x   , ˆ y = y/   y   , I k is the k × k identity matrix, and M ˆ x ↔ ˆ y 1 is the matrix that sends ˆ x  → ˆ y , ˆ y  → ˆ x , and z  → 0 for any z ∈ span ( { x,y } ) ⊥ .

The second deterministic condition provides an RIP-like property for the measurement matrix A when acting on pairs of secant directions within the range of G . For A ∈ R m × n and z ∈ R n , deﬁne A z := diag(sgn( Az )) A where sgn acts entrywise, and sgn(0) = 0 . Note that A z z = | Az | . The condition is stated as follows and was introduced in a conference version of this work [ 31 ]:

Deﬁnition 2 (Range Restricted Concentration Property) . We say that A satisﬁes the Range Restricted Concentration Property (RRCP) with respect to G with constant ε > 0 if for all x,y,x 1 ,x 2 ,x 3 ,x 4 ∈ R k :

$$
| \langle ( A _ { G ( x ) } ^ { T } A _ { G ( y ) } - \Phi _ { G ( x ) , G ( y ) } ) ( G ( x _ { 1 } ) - G ( x _ { 2 } ) ) , G ( x _ { 3 } ) - G ( x _ { 4 } ) \rangle | \\ & \leqslant L _ { \varepsilon } \| G ( x _ { 1 } ) - G ( x _ { 2 } ) \| \| G ( x _ { 3 } ) - G ( x _ { 4 } ) \|
$$

where

$$
\Phi _ { z , w } \colon = \begin{cases} \frac { \pi - 2 \theta _ { z , w } } { \pi } I _ { n } + \frac { 2 \sin \theta _ { z , w } } { \pi } M _ { \hat { z } \leftrightarrow \hat { w } } & \text {if } z \neq 0 , w \neq 0 , \\ 0 & \text {otherwise.} \end{cases}
$$

glyph[negationslash]

Here, L is a universal constant and can be taken to be 33 .

1 A formula for this matrix is as follows: consider a rotation matrix R that sends ˆ x  → e 1 and ˆ y  → cos θ x,y e 1 + sin θ x,y e 2 where θ x,y = ∠ ( x, y ) . Then M ˆ x ↔ ˆ y = R T   cos θ x,y sin θ x,y 0 sin θ x,y − cos θ x,y 0 0 0 0 k − 2   R where 0 k − 2 is the k − 2 × k − 2 matrix of zeros. Note that if θ x,y = 0 or π , M ˆ x ↔ ˆ y = ˆ x ˆ x T or − ˆ x ˆ x T , respectively.

[Page 7]

# 1.4 Algorithm

We provide a subgradient algorithm for optimizing ( 3 ) under noisy measurements. We show that this algorithm converges up to the noise level when the WDC and RRCP properties are met. In order to state the algorithm, we need some notation. For a locally Lipschitz function f : X → R from a Hilbert space X to R , the Clarke generalized directional derivative of f at x ∈ X in the direction u , is deﬁned by

$$
f ^ { o } ( x ; u ) \colon = \lim _ { y \to x , t \downarrow 0 } \frac { f ( y + t u ) - f ( y ) } { t } .
$$

Then the generalized subdiﬀerential of f at x is deﬁned by

$$
\partial f ( x ) = \{ v \in \mathbb { R } ^ { k } \colon \langle v , u \rangle \leqslant f ^ { o } ( x ; u ) , \, \forall u \in \mathcal { X } \} .
$$

Any element v x ∈ ∂f ( x ) is called a subgradient of f at x . When f is diﬀerentiable at x , ∂f ( x ) = {∇ f ( x ) } .

# Algorithm 1 Deep Phase Retrieval (DPR) Subgradient method

Require: Weights W i , measurement matrix A , measurements b ∗ = | Ay ∗ | + η , & step size α > 0 1: Choose an arbitrary initial point x 0 ∈ R k \ { 0 } 2: for t = 0 , 1 , 2 ,... do 3: if f ( − x t ) < f ( x t ) then 4: ¯ x t ← − x t ; 5: else 6: ¯ x t ← x t ; 7: end if 8: Compute v ¯ x t ∈ ∂f (¯ x t ) ; 9: x t +1 = ¯ x t − αv ¯ x t ; 10: end for

We now introduce a subgradient descent scheme, given by Algorithm 1 , whose intuition is as follows. In expectation, the optimization landscape is characterized by Figure 1 . There exists two critical points away from the origin: the true minimizer and a negative multiple thereof. Moreover, the value of the objective function is higher near the negative multiple than near the global minimizer. At each iterate, we check the objective function value at the current latent code and its negative, choosing the point with smaller objective function value as our new iterate; see Steps 3 – 7 . We then perform subgradient descent.

# 1.5 Main Results

In this section, we outline our main results in both the probabilistic and deterministic settings. In particular, in Theorem 3 we show that if the weights of G satisfy the WDC and the measurement matrix A satisﬁes the RRCP, then the iterates of Algorithm 1 converge to the true solution up to the noise level in the measurements. Then, in Corollary 4 we show that the same conclusion holds when W i and A are Gaussian with high probability as soon as m = Ω( dk log( n 1 n 2 ...n d )) .

We consider the possibly noisy measurements ( 2 ) and assume that the signal y ∗ is in the range of G with latent code x ∗ ; that is, y ∗ = G ( x ∗ ) . The following Theorem states that if the two deterministic conditions are satisﬁed with a suﬃciently small parameter ε and the noise is suﬃciently small, then the iterates of Algorithm 1 will converge to x ∗ up to the noise level.

[Page 8]

![The image is a graph that shows the temperature distribution of a certain object. The graph is titled Temperature Distribution and is labeled as 1°C. The graph has a color gradient that transitions from dark blue to light blue, with the gradient increasing from left to right. The gradient is symmetrical, with the color gradient being the same in both the left and right sides of the graph. The graph has a title, Temperature Distribution, and is labeled as 1°C. The title is in a sans-serif font, which makes it easy to read. The graph is labeled 1°C, which is the temperature range of the graph. The graph has a legend on the right side of the graph. The legend is labeled 1°C and is in a sans-serif font. The legend is in a color gradient that transitions from dark blue to light blue. The gradient is symmetrical, with the color gradient being the same](<2008.10579v1/imageFile1.png>)

0.9

0.8

0.7

0.6

0.5

0.4

0.3

0.2

0.1

Figure 1: The landscape of ( 3 ) where y ∗ = G ( x ∗ ) with x ∗ = [1 , 0] T ∈ R 2 , G is a 1 -layer network, and the two determinstic conditions are satisﬁed with ε = 0 . The objective function’s explicit form is given by F ( x ) := 1 4 (   x   2 +   x ∗   2 ) −   π − 2 g ( θ x,x ∗ ) π   x,Q x,x ∗ x ∗   + 2 sin g ( θ x,x ∗ ) π   x ∗    x     where θ x,x ∗ = ∠ ( x,x ∗ ) , g is deﬁned in ( 10 ), Q x,x ∗ is deﬁned in ( 5 ) and whose d -layer form is given by equation ( 62 ). We note that the idealized loss has only three critical points: the global minimizer x ∗ , a negative multiple thereof − ρ d x ∗ for some ρ d ∈ (0 , 1) , and the origin.

Theorem 3 (Deterministic Convergence Guarantee) . Let d   2 and ﬁx 0 < ε < c 1 1 d 102 . Suppose the noise satisﬁes   η     c 2   x ∗   2 d/ 2 d 48 . Suppose each W i of G satisﬁes the WDC with constant ε , and suppose A satisﬁes the RRCP with respect to G with constant ε . Then the iterates { x t } t   0 generated by Algorithm 1 with step size α   c 3 2 d d 2 obey the following:

1. there exists an N ∈ N satisfying N   C 4 f ( x ∗ )(2 2 d ) d 6 αε   x ∗   2 such that

$$
\| x _ { N } - x _ { * } \| \leqslant C _ { 5 } d ^ { 1 2 } \sqrt { \varepsilon } \| x _ { * } \| + C _ { 6 } d ^ { 9 } 2 ^ { d / 2 } \| \eta \| ;
$$

for all t glyph[greaterorequalslant] N , we have

$$
\| x _ { t + 1 } - x _ { * } \| & \leqslant \tau ^ { t + 1 - N } \| x _ { N } - x _ { * } \| + \vartheta \frac { 2 ^ { d / 2 } } { d ^ { 2 } } \| \eta \| , \ a n d \\
$$

$$
\| G ( x _ { t + 1 } ) - G ( x _ { * } ) \| \leqslant \frac { 1 . 2 } { 2 ^ { d / 2 } } \tau ^ { t + 1 - N } \| x _ { N } - x _ { * } \| + \frac { 1 . 2 } { d ^ { 2 } } \vartheta \| \eta \|
$$

where τ := 1 − 7 8 α 2 d ∈ (0 , 1) and ϑ := 2 c 3 1 − τ .

Here c 1 ,c 2 ,c 3 ,C 4 ,C 5 , and C 6 are positive universal constants.

This result asserts that the iterates of Algorithm 1 will eventually be in a small neighborhood of the true solution whose size depends on ε and   η   after N = O ( ε − 1 ) iterations. Furthermore, once in this neighborhood, the iterates will continue to converge linearly to the true solution up to the noise level. If no noise is present, then the true signal will be recovered. Note that the 2 d factors in the theorem are an artifact of the problem scaling. Roughly, the weights W i have spectral norm approximately 1 , and subsequent application of a ReLU will eﬀectively zero out roughly half of the rows of W i . The resulting rows of W i will have spectral norm of roughly 1 / 2 . Hence G ( x ) scales like 2 − d/ 2   x   , f ( x ) scales like 2 − d   x   2 , and any subgradient v x scales like 2 d . We also assume the

[Page 9]

We now address the expansive-Gaussian model. We appeal to a result that shows that expansive (tall) Gaussian matrices satisfy the WDC with high probability.

Lemma 1 (Lemma 11 in [ 33 ]) . Fix 0 < ε < 1 and suppose W ∈ R n × k has i.i.d. N (0 , 1 /n ) entries. Then if n   C ε k log k , then with probability at least 1 − 8 n exp( − γ ε k ) , W satisﬁes the WDC with constant ε . Here C ε and γ − 1 ε depend polynomially on ε − 1 .

In this work, we establish that Gaussian matrices A satisﬁes the RRCP with respect to an expansive-Gaussian G with high probability if they are suﬃciently tall. This result is proven in Section 3 :

Lemma 2 (RRCP) . Fix 0 < ε < 1 and suppose A ∈ R m × n has i.i.d. N (0 , 1 /m ) entries. Let G be a generative model of the form ( 4 ) where each W i ∈ R n i × n i − 1 has i.i.d. N (0 , 1 /n i ) entries. If m   C ε kd log( n 1 n 2 ...n d ) , then with probability 1 − γm 4 k exp( − c ε m ) , A satisﬁes the RRCP with respect to G with constant ε . Here γ is a universal constant and C ε and c − 1 ε depend polynomially on ε − 1 .

Hence for Gaussian measurements and weight ensembles, we can combine Lemma 1 and Lemma 2 with Theorem 3 to obtain the following Corollary:

Corollary 4 (Probabilistic Convergence Guarantee) . Fix 0 < ε < c 1 1 d 102 and suppose the noise satisﬁes   η     c 2   x ∗   2 d/ 2 d 48 for some universal constants c 1 and c 2 . Suppose G is such that W i ∈ R n i × n i − 1 has i.i.d. N (0 , 1 /n i ) entries for i = 1 ,...,d . Suppose that A ∈ R m × n d has i.i.d. N (0 , 1 /m ) entries independent from { W i } . Then if m   C ε dk log( n 1 n 2 ...n d ) and n i   C ε n i − 1 log n i − 1 for i = 1 ,...,d , then with probability at least 1 −   d i =1 γn i exp( − c ε n i − 1 ) − γm 4 k exp( − c ε m ) , the same conclusion as Theorem 3 holds. Here C ε depends polynomially on ε − 1 , c ε depends on ε , and γ is a universal constant.

To the author’s knowledge, this is the ﬁrst result establishing provable signal recovery with a computationally eﬃcient algorithm for undersampled generic phaseless linear measurements with optimal sample complexity. This sample complexity in our result scales with k , which can not be improved. We made no attempt to obtain tight bounds on d , except to ensure that all dependences on d are polynomial. We remind the reader that any 2 d terms that appear are due to the problem scaling. We further note that subsequent developments since the original release of [ 31 ] relaxed the logarithmic growth factor on the sizes of each layer of the generative model [ 17 ].

Lastly, we note that this result for compressive phase retrieval under optimal sample complexity implies recovery for linear compressive sensing under optimal sample complexity. As such, this work subsumes the work of a subset of the authors in [ 33 ]. This generalization of compressed sensing to compressive phase retrieval is conspicuously absent for structural priors based on sparsity, as the best known computationally eﬃcient algorithms for sparsity priors require sample complexity that is quadratic in the sparsity level.

# 1.6 Experiments on MNIST

In this section, we compare the generative modeling approach for compressive phase retrieval with three sparse phase retrieval algorithms: the sparse truncated amplitude flow algorithm (SPARTA) [64], Thresholded Wirtinger Flow (TWF) [10], and the alternating minimization algorithm CoPRAM [37]. For the generative modeling approach, we used a modified version of Algorithm 1 as we empirically found the negation step (Steps 3-4) only occurred at the first iterate. Hence we ran two gradient descents, one starting from a random initial iterate x 0 and another starting from its negation -x 0 . We report results for the most successful reconstruction. Gradient descent was performed using the Adam optimizer [40]. For the remainder of this section, we will refer to the generative modeling approach as DPR.

[Page 10]

In each task, the goal is to recover an image y ∗ given | Ay ∗ | where A ∈ R m × n has i.i.d. N (0 , 1 /m ) entries. The images were from the MNIST dataset [ 43 ]. This dataset consists of 60 , 000 28 × 28 images of handwritten digits. The generative model was a pretrained Variational Autoencoder (VAE) from [ 9 ]. The encoder network is of size 784 − 500 − 500 − 20 while the generator network G is of size 20 − 500 − 500 − 784 . The latent code space dimension is k = 20 .

For the sparse phase retrieval methods, we performed sparse recovery in the Daubechies-4 Wavelet domain. We zero-padded the images to be of size 32 × 32 . The resulting images generated by our algorithm were also uniformly padded with zeros around the border to obtain 32 × 32 images. For SPARTA and CoPRAM, we ran each algorithm with sparsity parameters ranging from 2 to 212 in increments of 15 , choosing the best reconstruction in terms of lowest reconstruction error.

We aimed to reconstruct 10 images from the MNIST test set. We allowed 5 random restarts for each algorithm and chose the result with the least   2 reconstruction error per pixel. We also report the Structural Similarity Index Measure (SSIM) [ 65 ] for each reconstruction. The results in Figure 2 demonstrate the success of our algorithm with very few measurements. For 200 measurements, we can achieve accurate recovery with a mean SSIM value of over 0 . 9 while other algorithms require 1000 measurements or more. In terms of reconstruction error, our algorithm exhibits recovery with 200 measurements comparable to the alternatives requiring 750 measurements or more, which is where they begin to succeed.

We note that while our algorithm succeeds with fewer measurements than the other methods, our performance, as measured by per-pixel reconstruction error, saturates as the number of measurements increases since our reconstruction accuracy is ultimately bounded by the generative model’s representational error. As generative models improve, their representational errors will decrease. Nonetheless, as can be seen in the reconstructed digits in Figure 3 , the recoveries are semantically correct (the correct digit is legibly recovered) even though the reconstruction error does not decay to zero. In applications, such as MRI and molecular structure estimation via X-ray crystallography, semantic error measures would be a more informative estimates of recovery performance than per-pixel error measures.

# 2 Proof of Convergence Result in Deterministic Setting

In this section, we will formally prove Theorem 3 . Section 2.1 outlines the notation we will use throughout the proofs. Section 2.2 provides a high-level sketch of our proof and outlines its central arguments while Section 2.3 discusses preliminary results that are used throughout the proofs. Then Section 2.4 presents the proof of Theorem 3 which is broken down into four central results. Finally, Section 2.5 presents supplementary results and their proofs that aid in establishing Theorem 3 .

# 2.1 Notation

Let ( · ) T denote the real transpose. Let [ n ] = { 1 , . . . , n } . Let B ( x, r ) denote the closed Euclidean ball centered at x with radius r . Let ‖ · ‖ denote the glyph[lscript] 2 norm for vectors and spectral norm for matrices. For any non-zero x ∈ R n , let ˆ x = x/ ‖ x ‖ . For non-zero x, y ∈ R n , let θ x,y = ∠ ( x, y ) Let relu ( x ) := max( x, 0) . Define sgn : R → R to be sgn( x ) = x/ | x | for non-zero x ∈ R and sgn(0) = 0 otherwise. Let ✶ ( E ) be the indicator function on the event E . For a vector v ∈ R n , diag (sgn( v )) is sgn( v i ) in the i -th diagonal entry and diag ( v > 0) is 1 in the i -th diagonal entry if v i > 0 and 0 otherwise. Let Π 1 i = d W i = W d W d -1 . . . W 1 . For any x ∈ R k and i ∈ [ d ] , define W i, + ,x := diag ( W i -1 , + ,x . . . W 2 , + ,x W 1 , + ,x x > 0) W i . Set Λ x := Π 1 i = d W i, + ,x and x d := Λ x x . Note that we have the following string of equalities: G ( x ) = Π 1 i = d W i, + ,x x = Λ x x = x d . Let I n be the n × n identity matrix. Let S k -1 denote the unit sphere in R k . We write γ = Ω( δ ) when γ glyph[greaterorequalslant] Cδ for some positive constant C . Similarly, we write γ = O ( δ ) when γ glyph[lessorequalslant] Cδ for some positive constant C . When we say that a constant depends polynomially on ε -1 , this means that it is at least Cε -k for some positive C and positive integer k . Positive numerical constants will be denoted using C or K with various subscripts. In general, numerical constants larger than 1 will be denoted by capital letters and constants smaller than 1 with lower case letters. For notational convenience, we write a = b + O 1 ( ε ) if ‖ a -b ‖ glyph[lessorequalslant] ε where ‖ · ‖ denotes | · | for scalars, glyph[lscript] 2 norm for vectors, and spectral norm for matrices.

[Page 11]

![The image is a line graph that shows the number of measurements for different colored markers. The x-axis represents the number of measurements, while the y-axis represents the number of measurements. The markers are colored in different colors: green, orange, purple, and blue. The graph is titled Number of measurements and has a legend at the top that indicates the different colors. The graph has a linear scale of range 0 to 1.0, with a minimum of 0 and a maximum of 1.0. The y-axis is labeled number of measurements and has a scale from 0 to 100. The x-axis is labeled number of measurements and has a scale from 0 to 100. The graph shows the following data points: 1. Green markers: 0.04 2. Orange markers: 0.03 3. Purple markers: 0.0](<2008.10579v1/imageFile2.png>)

0.1

DPR with VAE SPARTA

SPARTA

0.09

CoPRAM

TWF

0.08

Reconstruction error (per pixel)

0.07

0.06

0.05

0.04

0.03

0.02

0.01

0

50

100

200

300

500 of

750

1000

1500

2500

measurements

1

0.9

0.8

0.7

0.6

Mean SSIM

DPR with VAE SPARTA

SPARTA

0.5

CoPRAM

TWF

0.4

0.3

0.2

0.1

0

50

100

200

300

500 of

750

1000

1500

2500

measurements

Figure 2: Each algorithm’s average reconstruction error (top) and mean SSIM (bottom) over 10 images from the MNIST test set for diﬀerent numbers of measurements.

[Page 12]

Original

DPR with VAE (100 m)

SPARTA (100 m)

CoPRAM (100 m)

TWF (100 m)

![This is a page that has letters and numbers on it.](<2008.10579v1/imageFile2.png>)

Original

DPR with VAE (300 m)

SPARTA (300 m)

4 < 7

CoPRAM (300 m)

TWF (300 m)

Figure 3: Each algorithm’s reconstructed images with 100 measurements (left) and 300 measurements (right). If an image is blank, then the reconstruction error between the blank image and the original image was lower than that of the algorithm’s reconstructed image and the original image. We note that even for as few as 100 measurements, nearly all of DPR’s reconstructions are semantically correct.

# 2.2 Sketch of Proof for Theorem 3

Theorem 3 is proven by showing that for all x ∈ R k , any subgradient v x ∈ ∂f ( x ) is approximated by h x ∈ R k which has an analytical expression and that does not vanish outside of neighborhoods of the true solution x ∗ and a negative multiple − ρ d x ∗ for some ρ d ∈ (0 , 1) . Thus any v x ∈ ∂f ( x ) is bounded away from zero for x outside of these two neighborhoods, leading to convergence towards one of these regions. Then we ensure that the negation step of our algorithm (Steps 3 7 ) will update any iterate near − ρ d x ∗ to be in a neighborhood of x ∗ . Finally, we ensure convergence to x ∗ up to the noise level by showing that the objective function exhibits a convexity-like property in a neighborhood of x ∗ .

$$
& \cdot \cdot \cdot \\ & \text {sketch, we define some quantities. Define the function $g \colon [0, 2\pi] \to \mathbb { R }$ by } \\ & \quad g ( \theta ) \coloneqq \cos ^ { - 1 } \left ( \frac { ( \pi - \theta ) \cos \theta + \sin \theta } { \pi } \right ) . \\ [ 0 \} , \, \text {let $h_{x} \in \mathbb{R}^{k}$ be defined as} \\ & \| \left ( \frac { \pi - 2 \bar { \theta } _ { d , x } } { \pi } \right ) \left ( \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i , x } } { \pi } \right ) \hat { x } _ { * } \\ & \quad .
$$

To provide our sketch, we deﬁne some quantities. Deﬁne the function g : [0 , 2 π ] → R by

$$
g ( \theta ) \colon = & \cos ^ { - 1 } \left ( \frac { ( \pi - \theta ) \cos \theta + \sin \theta } { \pi } \right ) . \\ \text {For any } x \in \mathbb { R } \ \{ 0 \} , \, \text {let } h _ { x } \in \mathbb { R } ^ { k } \, \text { be defined as } \\ h _ { x } \colon = & \frac { \| x _ { * } \| } { 2 ^ { d } } \left ( \frac { \pi - 2 \bar { \theta } _ { d , x } } { \pi } \right ) \left ( \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i , x } } { \pi } \right ) \hat { x } _ { * } \\ & + \frac { 1 } { 2 ^ { d } } \left [ \| x \| - \| x _ { * } \| \left ( \frac { 2 \sin \theta _ { d , x } } { \pi } + \left ( \frac { \pi - 2 \bar { \theta } _ { d , x } } { \pi } \right ) \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \theta _ { i , x } } { \pi } \left ( \prod _ { j = i + 1 } ^ { \pi } \frac { \pi - \theta _ { i , x } } { \pi } \right ) \right ) \right ] \hat { x } _ { * } \\ \text {where } \bar { \theta } _ { 0 , x } = \mathcal { L } ( x , x _ { * } ) \text { and } \bar { \theta } _ { i , x } = g ( \bar { \theta } _ { i - 1 , x } ) \text { for } i \in [ d ] . \text { We further define } \\ & \rho _ { d } \colon = \frac { 2 \sin \check { \theta } _ { d } } { \pi } + \left ( \frac { \pi - 2 \check { \theta } _ { d } } { \pi } \right ) \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \check { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \check { \theta } _ { j } } { \pi } \right ) \\ \text {where } \check { \theta } _ { 0 } = \pi \text { and } \check { \theta } _ { i } = g ( \check { \theta } _ { i - 1 } ) \text { for } i \in [ d ] . \text { For a parameter } \beta > 0 , \, \text { define }
$$

$$
\rho _ { d } \colon = \frac { 2 \sin \check { \theta } _ { d } } { \pi } + \left ( \frac { \pi - 2 \check { \theta } _ { d } } { \pi } \right ) \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \check { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \check { \theta } _ { j } } { \pi } \right )
$$

where ˘ θ 0 = π and ˘ θ i = g ( ˘ θ i − 1 ) for i ∈ [ d ] . For a parameter β > 0 , deﬁne

$$
\mathcal { S } _ { \beta } \colon = \left \{ x \in \mathbb { R } ^ { k } \ \{ 0 \} \colon \| h _ { x } \| \leqslant \frac { 1 } { 2 ^ { d } } \beta \max ( \| x \| , \| x _ { * } \| ) \right \} .
$$

[Page 13]

A direct analysis in Lemma 8 shows that for appropriate values of β , S β is contained in the union of neighborhoods of x ∗ and − ρ d x ∗ :

$$
\mathcal { S } _ { \beta } \subset \mathcal { B } ( x _ { * } , 7 0 0 0 0 \pi ^ { 2 } d ^ { 9 } \beta \| x _ { * } \| ) \cup \mathcal { B } ( - \rho _ { d } x _ { * } , 7 7 4 2 2 \pi ^ { 2 } d ^ { 1 2 } \sqrt { \beta } \| x _ { * } \| ) .
$$

Set S + β := S β ∩ B ( x ∗ , 70000 π 2 d 9 β   x ∗   ) and S − β := S β ∩ B ( − ρ d x ∗ , 77422 π 2 d 12 √ β   x ∗   ) . A sketch of our proof is outlined as follows:

- • First, we establish that all subgradients are bounded away from zero for iterates outside of S β . Speciﬁcally, we show that when the WDC and RRCP are satisﬁed with constant ε , any v x t ∈ ∂f ( x t ) satisﬁes v x t ≈ h x t and h x t is bounded away from 0 by the deﬁnition of S β . Thus   v x t   must be bounded away from zero for points x t / ∈ S β . See Section 2.4.1 .
- • Next, we establish convergence to S β . In particular, we show that the previous result implies that subgradient descent at each iteration makes progress in the sense that for each non-zero x t / ∈ S β


$$
f ( x _ { t + 1 } ) - f ( x _ { t } ) \leqslant - C \varepsilon
$$

for some C > 0 . Thus after Ω( ε − 1 ) iterations, the iterates will eventually belong to S β . See Section 2.4.2 .

- • Third, we show that the negation step of our algorithm ensures that the iterates converge to S + β . Speciﬁcally, we prove that for points x ≈ x ∗ and y ≈ − ρ d x ∗ , f ( x ) < f ( y ) . Thus if an iterate x t ∈ S − β , f ( − x t ) < f ( x t ) so the negation step of our algorithm (Steps 3 – 7 ) ensures ¯ x t = − x t and ¯ x t ∈ S + β . See Section 2.4.3 .
- • Finally, we establish convergence to x ∗ up to the noise level. Speciﬁcally, we prove that once in S + β , a convexity-like property near x ∗ implies that the iterates converge to x ∗ up to the noise level in the measurements. See Section 2.4.4 .


# 2.3 Preliminaries for Proofs

We will make use of the following fact concerning the Clarke subdiﬀerential of the objective function f . Since f is piecewise quadratic, Theorem 9.6 from [ 15 ] asserts that for any x ∈ R k , the Clarke subdiﬀerential ∂f ( x ) can be written equivalently as

$$
\partial f ( x ) = \text {conv} ( v _ { 1 } , v _ { 2 } , \dots , v _ { s } ) = \left \{ \sum _ { \ell = 1 } ^ { s } c _ { \ell } v _ { \ell } \colon \sum _ { \ell = 1 } ^ { s } c _ { \ell } = 1 \text { and } c _ { \ell } \geqslant 0 \text { for } \ell \in [ s ] \right \} \quad ( 1 2 )
$$

where conv ( · ) denotes the convex hull, s is the number of quadratic functions adjoint to x , and v   is the gradient of the   -th quadratic function of f at x . Moreover, for each v   , there exists a direction w   and a suﬃciently small δ   > 0 such that f is diﬀerentiable at x + δ   w   and v   = lim δ   → 0 + ∇ f ( x + δ   w   ) .

# 2.4 Proof of Theorem 3

We now set out to prove Theorem 3 . In Sections 2.4.1 2.4.4 , we establish four main lemmas, each of which pertain to one of the items in the sketch of our proof from Section 2.2 . Theorem 3 is then proven in Section 2.4.5 . Prior to beginning the proof, we state the necessary assumptions we will make:

Assumptions A. We assume the following hold for some numerical constants c 1 , c 2 , and c 3 :

[Page 14]

- A1. 0 < ε < c 1 d − 102 ,
- A2. the noise η satsifes   η     c 2   x ∗   2 d/ 2 d 48 , and
- A3. the step size α > 0 satisﬁes α   c 3 2 d d 2 .


We note that Proposition 1 shows that after a polynomial number of steps, the iterates of our algorithm stay outside of a ball of the origin. Hence we assume throughout that the norm of our iterates are bounded away from zero. This result is proven in Section 2.5.1 .

# 2.4.1 Uniform control over subgradients

We ﬁrst show that the descent direction does not vanish for points outside of S β . The main idea of this result is that for points x such that   h x   is suﬃciently bounded away from zero, any v x ∈ ∂f ( x ) is also bounded away from zero.

To prove this, we require the following three lemmas. The ﬁrst gives a simple upper bound on the norm of our descent direction.

Lemma 3. Fix ε > 0 such that Kd 3 √ ε   1 where K is a universal constant. Suppose A ∈ R m × n d satisﬁes the RRCP with respect to G with constant ε and G is such that each W i ∈ R n i × n i − 1 satisﬁes the WDC with constant ε for i ∈ [ d ] . Then for any x ∈ R k \ { 0 } and v x ∈ ∂f ( x ) ,

$$
\| v _ { x } \| \leqslant \frac { C d } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \|
$$

where C is a numerical constant.

The second shows that h x is Lipschitz with respect to x for points away from the origin.

Lemma 4. For all x,y   = 0 , we have that

glyph[negationslash]

$$
\| h _ { x } - h _ { y } \| \leqslant \left ( \frac { ( 2 d ^ { 2 } + ( 1 0 \pi + 8 ) d + 2 0 \pi ) \| x _ { * } \| } { \pi ^ { 2 } 2 ^ { d } } \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) + \frac { 1 } { 2 ^ { d } } \right ) \| x - y \| .
$$

In particular, if x,y / ∈ B (0 ,r   x ∗   ) for some r > 0 , then

$$
\| h _ { x } - h _ { y } \| \leqslant \left ( \frac { 2 d ^ { 2 } + ( 1 0 \pi + 8 ) d + 2 0 \pi } { r \pi ^ { 2 } 2 ^ { d } } + \frac { 1 } { 2 ^ { d } } \right ) \| x - y \| .
$$

The third states that for any non-zero x ∈ R k and any v x ∈ ∂f ( x ) , h x approximates v x well.

Lemma 5. Fix ε > 0 such that ε < d − 4 (1 / 16 π ) 2 If A ∈ R m × n d satisﬁes the RRCP with respect to G with constant ε and G is such that each W i ∈ R n i × n i − 1 satisﬁes the WDC with constant ε for i ∈ [ d ] , then for any x   = 0 and v x ∈ ∂f ( x )

glyph[negationslash]

$$
\| v _ { x } - h _ { x } \| \leqslant \frac { K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \|
$$

where K is a universal constant.

Each of these results are proven in Section 2.5.2 . We are now ready to state and prove the main result of this section.

[Page 15]

Lemma 6. Suppose Assumptions A1-A3 are satisﬁed and set β := 4 Kd 3 √ ε +11   η   2 d/ 2 /   x ∗   where K is a numerical constant. Let A ∈ R m × n satisfy the RRCP with respect to G with constant ε . Let G be such that W i ∈ R n i × n i − 1 satisfy the WDC with constant ε for all i ∈ [ d ] . Suppose that x / ∈ S β and x / ∈ B (0 ,c 0   x ∗   ) for some numerical constant c 0 . Then for any v x ∈ ∂f ( x ) , we have

$$
\frac { 1 } { 3 } \| v _ { x } \| \geq \frac { K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \| x _ { * } \| .
$$

Moreover, we have that for any λ ∈ [0 , 1] ,

$$
\| v _ { \tilde { x } } - v _ { x } \| \leqslant \frac { 5 } { 6 } \| v _ { x } \|
$$

where ˜ x = x − λαv x , v x ∈ ∂f ( x ) and v ˜ x ∈ ∂f (˜ x ) .

Proof of Lemma 6 . By Lemma 4 , we have that h x is Lipschitz for all x / ∈ B (0 ,c 0   x ∗   ) , i.e. there exists a numerical constant L c 0 such that for any x,y / ∈ B (0 ,c 0   x ∗   )

$$
\| h _ { x } - h _ { y } \| \leqslant \frac { L _ { c _ { 0 } } d ^ { 2 } } { 2 ^ { d } } \| x - y \| .
$$

Moreover, Lemma 5 implies for any x   = 0 , we have

glyph[negationslash]

$$
\| v _ { x } - h _ { x } \| \leqslant \frac { K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \|
$$

for some numerical constant K . Hence

$$
\text {for some numerical constant $K$. Hence} \\ \| v _ { \tilde { x } } - v _ { x } \| & \leq \| v _ { \tilde { x } } - h _ { \tilde { x } } \| + \| h _ { \tilde { x } } - h _ { x } \| + \| h _ { x } - v _ { x } \| \\ & \leq \frac { K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| \tilde { x } \| , \| x _ { * } \| ) + \frac { L _ { c _ { 0 } } d ^ { 2 } } { 2 ^ { d } } \| \tilde { x } - x \| + \frac { K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 4 } { 2 ^ { d / 2 } } \| \eta \| \\ & \leq \frac { K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x - \lambda \alpha _ { x } \| , \| x _ { * } \| ) + \alpha \frac { L _ { c _ { 0 } } d ^ { 2 } } { 2 ^ { d } } \| v _ { x } \| + \frac { K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 4 } { 2 ^ { d / 2 } } \| \eta \| \\ & \leq \frac { K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| + \alpha \| v _ { x } \| , \| x _ { * } \| ) + \alpha \frac { L _ { c _ { 0 } } d ^ { 2 } } { 2 ^ { d } } \| v _ { x } \| + \frac { K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 4 } { 2 ^ { d / 2 } } \| \eta \| \\ & \leq \frac { K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \left ( 2 + \alpha \frac { C d } { 2 ^ { d } } \right ) \max ( \| x \| , \| x _ { * } \| ) + \alpha \frac { L _ { c _ { 0 } } d ^ { 2 } } { 2 ^ { d } } \| v _ { x } \| + \frac { 6 } { 2 ^ { d / 2 } } \| \eta \| \\ \text {where we used Lemma 4 and Lemma 5 in the second inequality, the definition of } \tilde { x } \text { in the third}
$$

where we used Lemma 4 and Lemma 5 in the second inequality, the deﬁnition of ˜ x in the third inequality and Lemma 3 in the last inequality for some numerical constant C . Now, we lower bound   v x   . Since x / ∈ S β we have that

$$
\| v _ { x } \| & \geqslant \| h _ { x } \| - \| h _ { x } - v _ { x } \| \\ & \geqslant \frac { 1 } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) \left ( \beta - K d ^ { 3 } \sqrt { \varepsilon } - 2 \| \eta \| \frac { 2 ^ { d / 2 } } { \| x _ { * } \| } \right ) \\ & = \frac { 1 } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) \left ( 3 K d ^ { 3 } \sqrt { \varepsilon } + 9 \| \eta \| ^ { \frac { 2 d / 2 } { 2 } } \right ) \\ & \geqslant \frac { 3 K d ^ { 3 } \sqrt { \varepsilon } } { 2 d } \max ( \| x \| , \| x _ { * } \| )
$$

$$
\geqslant \frac { 3 K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| )
$$

[Page 16]

$$
\text {elying} \, ( 1 6 ) \, \text {to equation} \, ( 1 5 ) , \, \text {we obtain} \\ \| v _ { \tilde { x } } - v _ { x } \| & \leqslant \frac { 2 } { 3 } \| v _ { x } \| + \alpha \frac { C d } { 2 ^ { d } } \cdot \frac { K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \alpha \frac { L _ { c _ { 0 } } d ^ { 2 } } { 2 ^ { d } } \| v _ { x } \| \\ & \leqslant \frac { 1 } { 3 } \left ( 2 + \alpha \frac { C d } { 2 ^ { d } } \right ) \| v _ { x } \| + \alpha \frac { L _ { c _ { 0 } } d ^ { 2 } } { 2 ^ { d } } \| v _ { x } \| \\ & \leqslant \left ( \frac { 2 } { 3 } + \frac { \alpha } { 3 } \cdot \frac { \tilde { C } d ^ { 2 } } { 2 ^ { d } } \right ) \| v _ { x } \| \\ & \leqslant \frac { 5 } { 6 } \| v _ { x } \| \\ \text {are } \tilde { C } = C + L _ { c _ { 0 } } . \text { In the first inequality, we used } ( 1 6 ) . \text { In the second inequality, we used } ( 1 7 ) .
$$

where ˜ C = C + L c 0 . In the ﬁrst inequality, we used ( 16 ). In the second inequality, we used ( 17 ). The last inequality follows by choosing c 3 in the upper bound α   c 3 2 d d 2 small enough so that α 3 ˜ Cd 2 2 d   1 6 .

# 2.4.2 Convergence to neighborhoods of x ∗ and − ρ d x ∗

Using Lemma 6 , we can now show that the iterates of our algorithm make suﬃcient progress at each step so that they eventually are in S β after a polynomial number of iterations.

Lemma 7. Suppose Assumptions A1-A3 are satisﬁed and set β := 4 Kd 3 √ ε +11   η   2 d/ 2 /   x ∗   where K is a numerical constant . Let A ∈ R m × n satisfy the RRCP with respect to G with constant ε . Let G be such that W i ∈ R n i × n i − 1 satisfy the WDC with constant ε for all i ∈ [ d ] . For x t / ∈ S β , we have

$$
f ( x _ { t + 1 } ) - f ( x _ { t } ) \leqslant - \alpha \frac { 9 K ^ { 2 } d ^ { 6 } \varepsilon } { 6 ( 2 ^ { 2 d } ) } \| x _ { * } \| ^ { 2 } .
$$

Moreover, there exists an N   6 f ( x 0 )(2 2 d ) 9 K 2 d 6 αε   x ∗   2 such that x N ∈ S β where x 0 is the initial iterate of our algorithm and α > 0 is the step size.

Proof of Lemma 7 . Recall that by Proposition 1 , we may assume x t / ∈ B (0 ,c 0   x ∗   ) where c 0 is a constant. We ﬁrst consider the case when ¯ x t = − x t . Then we must have that f (¯ x t ) < f ( x t ) . Hence for any v ¯ x t ∈ ∂f (¯ x t ) , we have

$$
f ( x _ { t + 1 } ) - f ( x _ { t } ) = f ( x _ { t + 1 } ) - f ( \bar { x } _ { t } ) + f ( \bar { x } _ { t } ) - f ( x _ { t } ) < f ( \bar { x } _ { t } - \alpha v _ { \bar { x } _ { t } } ) - f ( \bar { x } _ { t } )
$$

where we used f (¯ x t ) < f ( x t ) and the deﬁnition of x t +1 in the ﬁrst inequality. Thus observe that it suﬃces to establish the inequality for f (¯ x t − αv ¯ x t ) − f (¯ x t ) since this will also establish the case when ¯ x t = x t .

Now, choose v ¯ x t ∈ ∂f (¯ x t ) . By the generalized mean value theorem for the Clarke subdiﬀerential (Theorem 8.13 in [ 15 ]), there exists a λ ∈ [0 , 1] and ˜ v ˜ x t ∈ ∂f (˜ x t ) where ˜ x t = ¯ x t − λαv ¯ x t such that we have

$$
f ( \bar { x } _ { t } - \alpha v _ { \bar { x } _ { t } } ) - f ( \bar { x } _ { t } ) & = \langle \tilde { v } _ { \bar { x } _ { t } } - \alpha v _ { \bar { x } _ { t } } \rangle \\ & = \langle v _ { \bar { x } _ { t } } , - \alpha v _ { \bar { x } _ { t } } \rangle + \langle \tilde { v } _ { \bar { x } _ { t } } - v _ { \bar { x } _ { t } } , - \alpha v _ { \bar { x } _ { t } } \rangle \\ & \leqslant - \alpha \| v _ { \bar { x } _ { t } } \| ^ { 2 } + \alpha \| \tilde { v } _ { \bar { x } _ { t } } - v _ { \bar { x } _ { t } } \| \| v _ { \bar { x } _ { t } } \| \\ & = - \alpha \| v _ { \bar { x } _ { t } } \| \left ( \| v _ { \bar { x } _ { t } } \| - \| \tilde { v } _ { \bar { x } _ { t } } - v _ { \bar { x } _ { t } } \| \right )
$$

[Page 17]

We can now use our result from Section 2.4.1 to bound   ˜ v ˜ x t − v ¯ x t   and   v ¯ x t   from above and below, respectively. Observe that by Lemma 6 , we have

$$
f ( \bar { x } _ { t } - \alpha v _ { \bar { x } _ { t } } ) - f ( \bar { x } _ { t } ) \leqslant - \alpha \| v _ { \bar { x } _ { t } } \| ( \| v _ { \bar { x } _ { t } } \| - \| \tilde { v } _ { \bar { x } _ { t } } - v _ { \bar { x } _ { t } } \| ) \leqslant - \alpha \left ( 1 - \frac { 5 } { 6 } \right ) \| v _ { \bar { x } _ { t } } \| ^ { 2 } = - \frac { 1 } { 6 } \alpha \| v _ { \bar { x } _ { t } } \| ^ { 2 }
$$

where we used ( 14 ) in the second inequality. But by our lower bound on   v ¯ x t   , we have

$$
f ( \bar { x } _ { t } - \alpha v _ { \bar { x } _ { t } } ) - f ( \bar { x } _ { t } ) \leqslant - \frac { 1 } { 6 } \alpha \| v _ { \bar { x } _ { t } } \| ^ { 2 } \leqslant - \alpha \frac { 9 K ^ { 2 } d ^ { 6 } \varepsilon } { 6 ( 2 ^ { 2 d } ) } \| x _ { * } \| ^ { 2 }
$$

where we used ( 13 ) in the second inequality. Hence there are at most 6 f ( x 0 )(2 2 d ) 9 K 2 d 6 αε   x ∗   2 iterations for which x t / ∈ S β where x 0 is the initial iterate of our algorithm. Thus there exists a natural number N such that N   6 f ( x 0 )(2 2 d ) 9 K 2 d 6 αε   x ∗   2 and x N ∈ S β .

# 2.4.3 Convergence to neighborhood of x ∗

We now show that if any iterate is in S β , then the negation step of the algorithm (Steps 3 – 7 ) ensures that our iterates will now be in a neighborhood of x ∗ as opposed to a neighborhood of − ρ d x ∗ . We will use the following result that S β is contained in the union of neighborhoods of the true solution and a negative multiple thereof if β is suﬃciently small:

Lemma 8. If 0 < 24 π 2 d 6 √ β   1 , then

$$
S _ { \beta } \subset \mathcal { B } ( x _ { * } , 7 0 0 0 0 \pi ^ { 2 } d ^ { 9 } \beta \| x _ { * } \| ) \cup \mathcal { B } ( - \rho _ { d } x _ { * } , 7 7 4 2 2 \pi ^ { 2 } d ^ { 1 2 } \sqrt { \beta } \| x _ { * } \| ) .
$$

We also need the following lemma which shows that the objective function is smaller near x ∗ than near − ρ d x ∗ :

Lemma 9. Fix 0 < ε < 1 / (16 πd 2 ) 2 and suppose Assumption A3 is satisﬁed. Suppose that A ∈ R m × n d satisﬁes the RRCP with respect to G with constant ε and G is such that each W i ∈ R n i × n i − 1 satisﬁes the WDC with constant ε . Then for any φ d ∈ [ ρ d , 1] , we have that

$$
f ( x ) < f ( y )
$$

for all x ∈ B ( φ d x ∗ ,r 1 d − 12   x ∗   ) and y ∈ B ( − φ d x ∗ ,r 1 d − 12   x ∗   ) where r 1 is a universal constant.

These results are proven in Section 2.5.3 . The main result of this section is as follows.

Lemma 10. Suppose Assumptions A1-A3 are satisﬁed and set β := 4 Kd 3 √ ε + 11   η   2 d/ 2 /   x ∗   where K is a numerical constant. Let A ∈ R m × n satisfy the RRCP with respect to G with constant ε . Let G be such that W i ∈ R n i × n i − 1 satisfy the WDC with constant ε for all i ∈ [ d ] . If x t ∈ S β , then ¯ x t ∈ S + β , i.e.,

$$
\| \bar { x } _ { t } - x _ { * } \| \leqslant C _ { 5 } d ^ { 1 2 } \sqrt { \varepsilon } \| x _ { * } \| + C _ { 6 } d ^ { 9 } 2 ^ { d / 2 } \| \eta \|
$$

where C 5 and C 6 are numerical constants.

[Page 18]

Proof of Lemma 10 . Suppose x t ∈ S β . We require β to satisfy the assumption of Lemma 8 and for S β to be contained in the balls of radius r 1 d − 12   x ∗   from Lemma 9 . Recall that by assumption 0 < ε < c 1 d − 102 and   η     c 2   x ∗   2 d/ 2 d 48 for some constants c 1 and c 2 . Choosing c 1 and c 2 suﬃciently small enough, we can have that

$$
\beta = 4 K d ^ { 3 } \sqrt { \varepsilon } + \frac { 1 1 \| \eta \| ^ { 2 d / 2 } } { \| x _ { * } \| } \leqslant \frac { 4 K \sqrt { c _ { 1 } } } { d ^ { 4 8 } } + \frac { 1 1 c _ { 2 } } { d ^ { 4 8 } } \leqslant \frac { r _ { 1 } ^ { 2 } } { ( 7 7 4 2 2 \pi ^ { 2 } ) ^ { 2 } d ^ { 4 8 } } .
$$

Hence β satisﬁes the assumptions of Lemma 8 and 77422 π 2 d 12 √ β   x ∗     r 1 d − 12   x ∗   . Note that this implies S + β ⊂ B ( x ∗ ,r 1 d − 12   x ∗   ) while S − β ⊂ B ( − ρ d x ∗ ,r 1 d − 12   x ∗   ) . Therefore, we can apply equation ( 18 ) in Lemma 9 so that for any y ∈ S − β and x ∈ S + β , f ( x ) < f ( y ) . Since x t ∈ S β , either x t ∈ S + β or x t ∈ S − β . If x t ∈ S + β , then f ( x t ) < f ( − x t ) so ¯ x t = x t ∈ S + β . Otherwise, x t ∈ S − β and − x t ∈ S + β so that f ( − x t ) < f ( x t ) meaning ¯ x t = − x t ∈ S + β . In either case, we must have that ¯ x t ∈ S + β . By the deﬁnition of S + β , this establishes the inequality

$$
\| \bar { x } _ { t } - x _ { * } \| \leqslant C _ { 5 } d ^ { 1 2 } \sqrt { \varepsilon } \| x _ { * } \| + C _ { 6 } d ^ { 9 } 2 ^ { d / 2 } \| \eta \|
$$

for some numerical constants C 5 and C 6 .

# 2.4.4 Convergence to x ∗ up to noise

Finally, we show that once in a neighborhood of x ∗ , the iterates of our algorithm will converge to x ∗ up to the noise level in the measurements. We will use the following convexity-like property around the minimizer:

Lemma 11. Fix 0 < ε < 1 / (200 4 d 6 ) . Suppose that A ∈ R m × n d satisﬁes the RRCP with respect to G with constant ε and G is such that each W i ∈ R n i × n i − 1 satisﬁes the WDC with constant ε for i ∈ [ d ] . Then for all x ∈ B ( x ∗ ,d √ ε   x ∗   ) and any v x ∈ ∂f ( x ) , we have

$$
\left \| v _ { x } - \frac { 1 } { 2 ^ { d } } ( x - x _ { * } ) \right \| \leqslant \frac { 1 } { 8 } \frac { 1 } { 2 ^ { d } } \| x - x _ { * } \| + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| .
$$

We now prove the following lemma.

Lemma 12. Suppose Assumptions A1-A3 are satisﬁed and set β := 4 Kd 3 √ ε + 11   η   2 d/ 2 /   x ∗   where K is a numerical constant. Let A ∈ R m × n satisfy the RRCP with respect to G with constant ε . Let G be such that W i ∈ R n i × n i − 1 satisfy the WDC with constant ε for all i ∈ [ d ] . Suppose x N ∈ S + β for some N ∈ N . Then for all t   N , we have that ¯ x t ∈ B ( x ∗ ,r 1 d − 12   x ∗   ) , ¯ x t = x t , and

$$
\| x _ { t + 1 } - x _ { * } \| \leqslant \tau ^ { t + 1 - N } \| x _ { N } - x _ { * } \| + \vartheta \frac { 2 ^ { d / 2 } } { d ^ { 2 } } \| \eta \|
$$

where τ := 1 − 7 8 α 2 d ∈ (0 , 1) and ϑ := 2 c 3 1 − τ .

Proof of Lemma 12 . Suppose t = N so we have that ¯ x t = x t ∈ S + β ⊂ B ( x ∗ ,r 1 d − 12   x ∗   ) . As shown in Lemma 10 , this inclusion holds by our assumptions on ε and η . By Assumption A1, the

[Page 19]

$$
\begin{array} { r l } & { \text {requirements of Lemma 11 are met. Observe that for any } v _ { \bar { x } _ { t } } \in \partial f ( \bar { x } _ { t } ) , \, w e h a v e } \\ & { \quad \| x _ { t + 1 } - x _ { * } \| = \left \| \bar { x } _ { t } - \alpha v _ { \bar { x } _ { t } } - x _ { * } + \frac { \alpha } { 2 ^ { d } } ( \bar { x } _ { t } - x _ { * } ) - \frac { \alpha } { 2 ^ { d } } ( \bar { x } _ { t } - x _ { * } ) \right \| } \\ & { \quad \leq \left ( 1 - \frac { \alpha } { 2 ^ { d } } \right ) \| \bar { x } _ { t } - x _ { * } \| + \alpha \left \| v _ { \bar { x } _ { t } } - \frac { 1 } { 2 ^ { d } } ( \bar { x } _ { t } - x _ { * } ) \right \| } \\ & { \quad \leq \left ( 1 - \frac { \alpha } { 2 ^ { d } } \right ) \| \bar { x } _ { t } - x _ { * } \| + \left ( \frac { \alpha } { 8 } \right ) \frac { 1 } { 2 ^ { d } } \| \bar { x } _ { t } - x _ { * } \| + \alpha \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| } \\ & { = \left ( 1 - \frac { 7 } { 8 } \frac { \alpha } { 2 ^ { d } } \right ) \| \bar { x } _ { t } - x _ { * } \| + \alpha \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| } \\ & { \quad ( 1 ) } \\ \end{array}
$$

where we used Lemma 11 in the second inequality. Using α   c 3 2 d d 2 and   η     c 2 2 d d 48   x ∗   for suﬃciently small constants c 2 and c 3 , we have that if ¯ x t ∈ B ( x ∗ ,r 1 d − 12   x ∗   ) , then x t +1 ∈ B ( x ∗ ,r 1 d − 12   x ∗   ) so the iterates stay within a small ball around the minimizer. Hence Lemma 9 yields ¯ x t +1 = x t +1 . Repeatedly applying the above logic shows that for all t   N , x t ∈ B ( x ∗ ,r 1 d − 12   x ∗   ) and ¯ x t = x t . d

Finally, using α   c 3 2 d 2 in the second half of equation ( 19 ) yields

$$
\| x _ { t + 1 } - x _ { * } \| \leqslant \left ( 1 - \frac { 7 } { 8 } \frac { \alpha } { 2 ^ { d } } \right ) \| x _ { t } - x _ { * } \| + 2 c _ { 3 } \frac { 2 ^ { d / 2 } } { d ^ { 2 } } \| \eta \| = \colon \tau \| x _ { t } - x _ { * } \| + 2 c _ { 3 } \frac { 2 ^ { d / 2 } } { d ^ { 2 } } \| \eta \|
$$

where τ := 1 − 7 8 α 2 d . Choosing c 3 so that c 3 < 8 7 implies τ ∈ (0 , 1) . Starting at t = N and repeatedly applying this inequality, we attain

$$
\| x _ { t + 1 } - x _ { * } \| & \leqslant \tau ^ { t + 1 - N } \| x _ { N } - x _ { * } \| + ( \tau ^ { t - N } + \tau ^ { t - N - 1 } + \dots + 1 ) 2 c _ { 3 } \frac { 2 ^ { d / 2 } } { d ^ { 2 } } \| \eta \| \\ & \leqslant \tau ^ { t + 1 - N } \| x _ { N } - x _ { * } \| + \frac { 2 c _ { 3 } } { 1 - \tau } \frac { 2 ^ { d / 2 } } { d ^ { 2 } } \| \eta \| \\ & = \colon \tau ^ { t + 1 - N } \| x _ { N } - x _ { * } \| + \vartheta ^ { 2 } \frac { d / 2 } { d ^ { 2 } } \| \eta \|
$$

where ϑ := 2 c 3 1 − τ . This completes the proof.

# 2.4.5 Final proof of Theorem 3

With all of the necessary lemmas proven, we bring them together to prove Theorem 3 .

Proof of Theorem 3 . Set β := 4 Kd 3 √ ε + 11   η   2 d/ 2 /   x ∗   where K is a numerical constant. By Proposition 1 , we may assume that our initial iterate x 0 / ∈ B (0 ,c 0   x ∗   ) for some numerical constant c 0 . Then by Lemma 7 , there exists an N ∈ N such that N   6 f ( x 0 )(2 2 d ) 9 K 2 d 6 αε   x ∗   2 and x N ∈ S β . Then Lemma 10 implies ¯ x N ∈ S + β which establishes inequality ( 7 ). Finally, Lemma 12 establishes inequality ( 8 ) for any t   N . Inequality ( 9 ) follows by using ( 8 ) and the following result with j = d which established Lipschitz continuity of G for x within a neighborhood of x ∗ :

Lemma 13 (Lemma A.8 in [ 35 ]) . Suppose 0 < ε < 1 / (200 4 d 6 ) , x ∈ B ( x ∗ ,d √ ε   x ∗   ) , and G is such that each W i ∈ R n i × n i − 1 satisﬁes the WDC with constant ε for i ∈ [ d ] . Then we have that for all j ∈ [ d ] ,

$$
\left \| \Pi _ { i = j } ^ { 1 } W _ { i , + , x } x - \Pi _ { i = j } ^ { 1 } W _ { i , + , x _ { * } } x _ { * } \right \| \leqslant \frac { 1 . 2 } { 2 ^ { j / 2 } } \| x - x _ { * } \| .
$$

[Page 20]

# 2.5 Supplementary Results

In the following sections, we provide proofs for auxillary results that were in used in the four main lemmas used to establish Theorem 3 . Section 2.5.1 focuses on proving that after a polynomial number of iterations, the iterates of our algorithm are all bounded away from zero. Section 2.5.2 establishes supplementary results about controlling subgradients in Section 2.4.1 . Then Section 2.5.3 establishes results concerning the zeros of h x and properties of the objective function used in Section 2.4.3 . Lastly, Section 2.5.4 focuses on establishing the convexity-like property near the minimizer which is formalized in Section 2.4.4 .

# 2.5.1 Iterates are eventually bounded away from zero

We focus on proving the following proposition:

3 √ ε

Proposition 1. Fix ε > 0 such that Kd   1 where K is a universal constant. Suppose that A ∈ R m × n d satisﬁes the RRCP with respect to G with constant ε and G is such that each W i ∈ R n i × n i − 1 satisﬁes the WDC with constant ε for i ∈ [ d ] . Suppose that the step size α and noise η satisfy 0 < α < 2 d 104 π ( Cd +2 c 2 ) and   η     c 2   x ∗   2 d/ 2 where C and c 2 are numerical constants. If x t ∈ B (0 , 1 52 π   x ∗   ) , then after at most N 0 =     2 d 24 π α 52 π   2   iterations, we have that for all t > N 0 and λ ∈ [0 , 1] , λ ¯ x t + (1 − λ ) x t +1 / ∈ B (0 , 1 104 π   x ∗   ) .

This result asserts that if an iterate of our algorithm lies within a ball of the origin, then after a polynomial number of steps, it will leave this region. To prove it, we require the following lemma that establishes certain properties of any subgradient v x ∈ ∂f ( x ) for points x near the origin:

Lemma 14. Fix ε > 0 such that Kd 3 √ ε   1 where K is a universal constant. Suppose that A ∈ R m × n d satisﬁes the RRCP with respect to G with constant ε and G is such that each W i ∈ R n i × n i − 1 satisﬁes the WDC with constant ε for i ∈ [ d ] . Then for all x ∈ B (0 , 1 52 π   x ∗   ) and any v x ∈ ∂f ( x ) , we have that

$$
\langle x , v _ { x } \rangle < 0 \ a n d \, \| v _ { x } \| \geqslant \frac { 1 } { 2 ^ { d } } \frac { 1 } { 2 4 \pi } \| x _ { * } \| .
$$

We are now ready to proceed with a proof of Proposition 1 .

Proof of Proposition 1 . Suppose that x t ∈ B (0 , 1 52 π   x ∗   ) . By Lemma 14 , we have that ¯ x t and the next iterate x t +1 = ¯ x t − αv ¯ x t form an obtuse triangle for any v ¯ x t ∈ ∂f (¯ x t ) . Thus

$$
\| \bar { x } _ { t + 1 } \| ^ { 2 } & = \| x _ { t + 1 } \| ^ { 2 } \geqslant \| \bar { x } _ { t } \| ^ { 2 } + \alpha ^ { 2 } \| v _ { \bar { x } _ { t } } \| ^ { 2 } \\ & \geqslant \| \bar { x } _ { t } \| ^ { 2 } + \alpha ^ { 2 } \frac { 1 } { ( 2 ^ { d } 2 4 \pi ) ^ { 2 } } \| x _ { * } \| ^ { 2 }
$$

where the last inequality follows from Lemma 14 . Thus the norm of the iterates will increase until after N 0 =     2 d 24 π α 52 π   2   iterations we have x t + N 0 / ∈ B (0 , 1 52 π   x ∗   ) .

Now consider x t / ∈ B (0 , 1 52 π   x ∗   ) . We will show that for any λ ∈ [0 , 1] , λ ¯ x t + (1 − λ ) x t +1 / ∈ B (0 , 1 104 π   x ∗   ) . Note that for x t / ∈ B (0 , 1 52 π   x ∗   ) , we have   ¯ x t   =   x t     1 52 π   x ∗   . Then observe

[Page 21]

$$
\bar { x } _ { t } & \in 0 J \left ( x _ { t } \right ) , \, \text {we have} \\ & \alpha \| v _ { \bar { x } _ { t } } \| \leqslant \alpha \frac { 1 } { 2 ^ { d } } \max ( \| \bar { x } _ { t } \| , \| x _ { * } \| ) \left ( C d + \frac { 2 } { \| x _ { * } \| } 2 ^ { d / 2 } \| \eta \| \right ) \\ & \leqslant \alpha \frac { 1 } { 2 ^ { d } } \max ( \| \bar { x } _ { t } \| , \| x _ { * } \| ) \left ( C d + 2 c _ { 2 } \right ) \\ & \leqslant \frac { \alpha } { 2 ^ { d } } 5 2 \pi \| \bar { x } _ { t } \| \left ( C d + 2 c _ { 2 } \right ) \\ & \leqslant \frac { 1 } { 2 } \| \bar { x } _ { t } \| \\ & \text {inequality follows by Lemma 3, the second by the assumption on the}
$$

where the ﬁrst inequality follows by Lemma 3 , the second by the assumption on the noise energy   η     c 2   x ∗   2 d/ 2 , the third due to x t / ∈ B (0 , 1 52 π   x ∗   ) , and the last inequality follows by the assumption on α . Thus since x t +1 = ¯ x t − αv ¯ x t , we have that λ ¯ x t + (1 − λ ) x t +1 / ∈ B (0 , 1 104 π   x ∗   ) for any λ ∈ [0 , 1] .

We now focus on proving Lemma 14 . To show this, we ﬁrst require the following angle concentration property of the map z  → A z z for z in the range of G .

Lemma 15. Fix 0 < ε < 1 / (4 L ) where L is the universal constant speciﬁed in the RRCP. Let A ∈ R m × n satisfy the RRCP with respect to G with constant ε . Let G be such that W i ∈ R n i × n i − 1 satisfy the WDC with constant ε for all i ∈ [ d ] . Then for all x,y ∈ R k \ { 0 } , the angle θ 1 := ∠ ( A G ( x ) G ( x ) ,A G ( y ) G ( y )) is well-deﬁned and

$$
| \cos \theta _ { 1 } - \cos \varphi ( \theta _ { d } ) | \leqslant 4 L \varepsilon
$$

where θ d = ∠ ( G ( x ) ,G ( y )) and ϕ : R → R deﬁned by

$$
\varphi ( \theta ) \colon = \cos ^ { - 1 } \left ( \frac { ( \pi - 2 \theta ) \cos \theta + 2 \sin \theta } { \pi } \right ) .
$$

Proof of Lemma 15 . Fix x,y ∈ R k \ { 0 } . We use the shorthand notation Λ x := Π 1 i = d W i, + ,x and x d := Λ x x . Note that the WDC implies that for suﬃciently small ε , we have that Λ x x, Λ y y   = 0 . Hence we may assume, without loss of generality, that   Λ x x   =   Λ y y   = 1 . Now deﬁne the following quantities:

glyph[negationslash]

$$
\delta _ { 1 } & \colon = \langle \Lambda _ { x } x , ( A _ { x _ { d } } ^ { \mathrm T } A _ { y _ { d } } - \Phi _ { x _ { d } , y _ { d } } ) \Lambda _ { y } y \rangle , \\ \delta _ { 2 } & \colon = \langle \Lambda _ { x } x , ( A _ { x _ { d } } ^ { \mathrm T } A _ { x _ { d } } - I ) \Lambda _ { x } x \rangle , \\ \delta _ { 3 } & \colon = \langle \Lambda _ { y } y , ( A _ { y _ { d } } ^ { \mathrm T } A _ { y _ { d } } - I ) \Lambda _ { y } y \rangle .
$$

Observe that by the RRCP, we have that max i =1 , 2 , 3 | δ i |   Lε. Hence if 0 < ε < 1 /L ,

$$
0 < 1 - L \varepsilon \leqslant \| A _ { x _ { d } } \Lambda _ { x } x \| ^ { 2 }
$$

so   A x d Λ x x     = 0 . The same conclusion holds for   A y d Λ y y   so θ 1 is well-deﬁned. Furthermore, note

[Page 22]

that

$$
\cos \theta _ { 1 } & = \frac { \langle \Lambda _ { x } x , A _ { x _ { d } } ^ { T } A _ { y _ { d } } \Lambda _ { y } y \rangle } { \| A _ { x _ { d } } \Lambda _ { x } x \| \| A _ { y _ { d } } \Lambda _ { y } y \| } \\ & = \frac { \sqrt { \langle A _ { x _ { d } } \Lambda _ { x } x , A _ { x _ { d } } \Lambda _ { x _ { d } } x \rangle \langle A _ { y _ { d } } \Lambda _ { y } y , A _ { y _ { d } } \Lambda _ { y } y \rangle } } { \sqrt { \langle A _ { x _ { d } } \Lambda _ { x } x , \Phi _ { x _ { d } , y _ { d } } \Lambda _ { y } y \rangle + \delta _ { 1 } } } \\ & = \frac { \sqrt { ( \langle \Lambda _ { x } x , \Lambda _ { x } x \rangle + \delta _ { 2 } ) \left ( \langle \Lambda _ { y } y , \Lambda _ { y } y \rangle + \delta _ { 3 } \right ) } } { \sqrt { ( \Lambda _ { x } x , \Phi _ { x _ { d } , y _ { d } } \Lambda _ { y } y \rangle + \delta _ { 1 } } } .
$$

Thus

$$
\text {Thus} & & | \cos \theta _ { 1 } - \langle \Lambda x , \Phi _ { x d , y d } \Lambda y \rangle | \leq & \left | \frac { \langle \Lambda _ { x } x , \Phi _ { x d , y d } \Lambda y \rangle + \delta _ { 1 } } { \sqrt { ( 1 + \delta _ { 2 } ) \left ( 1 + \delta _ { 3 } \right ) } } - \langle \Lambda _ { x } x , \Phi _ { x d , y d } \Lambda y \rangle \right | \\ & \leq & | \langle \Lambda _ { x } x , \Phi _ { x d , y d } \Lambda _ { y } y \rangle | \left | 1 - \frac { 1 } { \sqrt { ( 1 + \delta _ { 2 } ) \left ( 1 + \delta _ { 3 } \right ) } } \right | \\ & + & \frac { | \delta _ { 1 } | } { \sqrt { ( 1 + \delta _ { 2 } ) \left ( 1 + \delta _ { 3 } \right ) } } \\ & \leq & 2 \left | 1 - \frac { 1 } { 1 - L \varepsilon } \right | + \frac { L \varepsilon } { 1 - L \varepsilon } \\ & \leq & \frac { 3 L \varepsilon } { 1 - L \varepsilon } \leq 4 L \varepsilon \\ \text {where we used } & \| \Phi _ { x d , y d } \| \leq 2 \text { in the third inequality and } L \varepsilon < 1 / 4 \text { in the last inequality. The proof} .
$$

where we used   Φ x d ,y d     2 in the third inequality and Lε < 1 / 4 in the last inequality. The proof concludes by noting that   Λ x x, Φ x d ,y d Λ y y   = 1 π [( π − 2 θ d )cos θ d + 2sin θ d ] .

We also require upper bounds on quantities that will be useful throughout the remaining proofs.

Lemma 16. Fix 0 < ε < 1 / (48 d ) . Let A ∈ R m × n satisfy the RRCP with respect to G with constant ε . Let G be such that W i ∈ R n i × n i − 1 satisfy the WDC with constant ε for all i ∈ [ d ] . Then for any x ∈ R k , we have

$$
\| \Lambda _ { x } \| ^ { 2 } & \leqslant \frac { 1 3 } { 1 2 } 2 ^ { - d } , & & ( 2 0 ) \\ & = ( 1 - 1 ) ^ { 2 } , & &
$$

‖ A x d Λ x ‖ 2 glyph[lessorequalslant] (1 + Lε ) ‖ Λ x ‖ 2 (21) Proof of Lemma 16. For equation (20), note that the WDC implies that ‖ W i, + ,x ‖ 2 glyph[lessorequalslant] 1 2 + ε for each i ∈ [ d ] so ❨

$$
\| \Lambda _ { x } \| ^ { 2 } \leqslant \prod _ { i = 1 } ^ { d } \| W _ { i , + , x } \| ^ { 2 } \leqslant \left ( \frac { 1 } { 2 } + \varepsilon \right ) ^ { d } = \frac { 1 } { 2 ^ { d } } ( 1 + 2 \varepsilon ) ^ { d } = \frac { 1 } { 2 ^ { d } } e ^ { d \log ( 1 + 2 \varepsilon ) } \leqslant \frac { 1 + 4 \varepsilon d } { 2 ^ { d } } \leqslant \frac { 1 3 } { 1 2 } 2 ^ { - d }
$$

where we used the fact that log(1 + u )   u and e u   1 + 2 u for u < 1 while the last inequality follows by our assumption on ε : ε < 1 / (48 d ) .

For equation ( 21 ), observe that by the RRCP and the local linearity of G , we have that for suﬃciently small z ∈ R k ,

$$
| \langle A _ { x _ { d } } \Lambda _ { x } z , A _ { x _ { d } } \Lambda _ { x } z \rangle - \langle \Lambda _ { x } z , \Lambda _ { x } z \rangle | \leqslant L \varepsilon \| \Lambda _ { x } \| ^ { 2 } \| z \| ^ { 2 }
$$

[Page 23]

which implies that

$$
| \langle A _ { x _ { d } } \Lambda _ { x } z , A _ { x _ { d } } \Lambda _ { x } z \rangle | \leqslant ( 1 + L \varepsilon ) \| \Lambda _ { x } \| ^ { 2 } \| z \| ^ { 2 } .
$$

Since this holds for any z ∈ R k , we conclude   A x d Λ x   2   (1 + Lε )   Λ x   2 .

Now we set out to prove Lemma 14 .

Proof of Lemma 14 . Suppose f is diﬀerentiable at x so that v x is precisely the gradient of f . We ﬁrst show that   x,v x   < 0 . Note that

$$
\langle x , v _ { x } \rangle = \underbrace { \langle \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } A _ { x _ { d } } \Lambda _ { x } x , x \rangle } _ { ( I ) } - \underbrace { \langle \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } A _ { x _ { * } , d } \Lambda _ { x _ { * } } x _ { * } , x \rangle } _ { ( I I ) } - \underbrace { \langle \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } \eta , x \rangle } _ { ( I I I ) } .
$$

We will bound the ﬁrst and third term from above and the second from below. We ﬁrst focus on the second term as its proof will give us a result for the ﬁrst term.

(II): For the second term, note that we can write it as

$$
\langle \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } A _ { x _ { * , d } } \Lambda _ { x _ { * } , x } , x \rangle = \cos ( \zeta ( A _ { x _ { d } } x _ { d } , A _ { x _ { * , d } } x _ { * , d } ) ) \| A _ { x _ { d } } x _ { d } \| \| A _ { x _ { * , d } } x _ { * , d } \| .
$$

By Lemma 15 , we have that

$$
\cos ( \varphi ( \theta _ { d } ) ) - 4 L \varepsilon \leqslant \cos ( \zeta ( A _ { x _ { d } } x _ { d } , A _ { x _ { * , d } } x _ { * , d } ) ) \leqslant \cos ( \varphi ( \theta _ { d } ) ) + 4 L \varepsilon
$$

where θ d = ∠ ( x d ,x ∗ ,d ) . Thus

$$
\langle \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } A _ { x _ { * } , d } \Lambda _ { x _ { * } } x _ { * } , x \rangle \geqslant ( \cos ( \varphi ( \theta _ { d } ) ) - 4 L \varepsilon ) \| A _ { x _ { d } } x _ { d } \| \| A _ { x _ { * } , d } x _ { * } \| .
$$

However, note that

$$
\cos ( \varphi ( \theta ) ) = \frac { ( \pi - 2 \theta ) \cos \theta + 2 \sin \theta } { \pi } \geqslant \frac { 2 } { \pi } \ \forall \ \theta \in [ 0 , \pi ] .
$$

Hence if ε < 1 / (4 Lπ ) , applying ( 23 ) to ( 22 ) we have that

$$
\langle \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } A _ { x _ { * } , d } \Lambda _ { x _ { * } } x _ { * } , x \rangle \geqslant \frac { 1 } { \pi } \| A _ { x _ { d } } x _ { d } \| \| A _ { x _ { * } , d } x _ { * , d } \| .
$$

We now bound   A x d x d   : observe that by the RRCP,

$$
| \langle ( A _ { x _ { d } } ^ { \text {T} } A _ { x _ { d } } - I ) x _ { d } , x _ { d } \rangle | \leqslant L \varepsilon \| x _ { d } \| ^ { 2 } \Longrightarrow ( 1 - L \varepsilon ) \| x _ { d } \| ^ { 2 } \leqslant \| A _ { x _ { d } } x _ { d } \| ^ { 2 } \leqslant ( 1 + L \varepsilon ) \| x _ { d } \| ^ { 2 }
$$

which gives

$$
\sqrt { 1 - L \varepsilon } \| x _ { d } \| \leqslant \| A _ { x _ { d } } x _ { d } \| \leqslant \sqrt { 1 + L \varepsilon } \| x _ { d } \| .
$$

By equation (11) of [ 33 ], we have that

$$
\left ( \frac { 1 } { 2 } - \varepsilon \right ) ^ { d / 2 } \| x \| \leqslant \| x _ { d } \| \leqslant \left ( \frac { 1 } { 2 } + \varepsilon \right ) ^ { d / 2 } \| x \| .
$$

Hence we attain

$$
\sqrt { 1 - L \varepsilon } \left ( \frac { 1 } { 2 } - \varepsilon \right ) ^ { d / 2 } \| x \| \leqslant \| A _ { x _ { d } } x _ { d } \| \leqslant \sqrt { 1 + L \varepsilon } \left ( \frac { 1 } { 2 } + \varepsilon \right ) ^ { d / 2 } \| x \| .
$$

[Page 24]

Analogous bounds hold for   A x ∗ ,d x ∗ ,d   . Applying ( 25 ) to equation ( 24 ), we conclude that

$$
\langle \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } A _ { x _ { * } , d } \Lambda _ { x _ { * } } x _ { * } , x \rangle & \geqslant \frac { 1 } { \pi } \| A _ { x _ { d } } x _ { d } \| \| A _ { x _ { * } , d } x _ { * , d } \| \\ & \geqslant \frac { 1 } { \pi } ( 1 - L \varepsilon ) \left ( \frac { 1 } { 2 } - \varepsilon \right ) ^ { d } \| x \| \| x _ { * } \| .
$$

If 2 dε < 2 / 3 , we further have (1 / 2 − ε ) d   (1 − 2 dε ) / 2 d   1 / 3(1 / 2 d ) . Then if ε is chosen such that 1 − Lε   1 / 2 , then we get

$$
\langle \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } A _ { x _ { * , d } } \Lambda _ { x _ { * } } x _ { * } , x \rangle \geqslant \frac { 1 } { 6 \pi } \frac { 1 } { 2 ^ { d } } \| x \| \| x _ { * } \| .
$$

This concludes the bound of the second term. We then proceed to bounding (I) and (III).

(I): Observe that by equation ( 25 ) and our choice of ε , we get

$$
\langle \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } A _ { x _ { d } } \Lambda _ { x } x , x \rangle = \| A _ { x _ { d } } x _ { d } \| ^ { 2 } \leqslant ( 1 + L \varepsilon ) \left ( \frac { 1 } { 2 } + \varepsilon \right ) ^ { d } \| x \| ^ { 2 } \leqslant 2 \cdot \frac { 1 3 } { 1 2 } \frac { 1 } { 2 ^ { d } } \| x \| ^ { 2 } = \frac { 1 3 } { 6 } \frac { 1 } { 2 ^ { d } } \| x \| ^ { 2 } .
$$

(III): Observe that

$$
\| A _ { x _ { d } } \Lambda _ { x } \| \leq & \sqrt { 1 + L _ { \varepsilon } } \| \Lambda _ { x } \| \leq \sqrt { \frac { 1 3 } { 1 2 } ( 1 + L _ { \varepsilon } ) } \frac { 1 } { 2 ^ { d / 2 } } \leqslant \frac { 2 } { 2 ^ { d / 2 } }
$$

where we used ( 21 ) in the ﬁrst inequality, ( 20 ) in the second inequality and our assumption on ε in the last inequality. Thus we attain

$$
| \langle x , \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } \eta \rangle & \leqslant \| x \| \| A _ { x _ { d } } \Lambda _ { x } \| \| \eta \| \leqslant \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| \| x \| \leqslant \frac { 2 c _ { 2 } } { 2 ^ { d } } \| x \| \| x _ { * } \| \leqslant \frac { 1 } { 2 ^ { d } } \frac { 1 } { 1 2 \pi } \| x \| \| x _ { * } \|
$$

where the third inequality follows by   η     c 2 2 d/ 2   x ∗   and the last inequality is due to c 2 < 1 24 π . Using our results for (I), (II), and (III), we conclude that

$$
\langle x , v _ { x } \rangle & = \langle \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } A _ { x _ { d } } A _ { x } x , x \rangle - \langle \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } A _ { x _ { d } } \Lambda _ { x } x _ { * } , x \rangle - \langle \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } \eta , x \rangle \\ & \leqslant \frac { 1 } { 2 } \| x \| \left ( \frac { 1 3 } { 6 } \| x \| + \frac { 1 } { 1 2 \pi } \| x _ { * } \| - \frac { 1 } { 6 \pi } \| x _ { * } \| \right ) \\ & \leqslant \frac { 1 } { 2 } \| x \| \left ( \frac { 1 3 } { 6 } \| x \| - \frac { 1 } { 1 2 \pi } \| x _ { * } \| \right ) .
$$

Thus if   x   < 1 52 π   x ∗   , i.e. x ∈ B (0 , 1 52 π   x ∗   ) , then

Lastly, observe that this gives When f is not differentiable at x , we have that by equation (12), we can write v x = ∑ s glyph[lscript] =1 c glyph[lscript] v glyph[lscript] where c glyph[lscript] glyph[greaterorequalslant] 0 for glyph[lscript] ∈ [ s ] and ∑ s glyph[lscript] =1 c glyph[lscript] = 1 . Applying our result for differentiable points x , we have that

$$
\langle x , v _ { x } \rangle \leqslant - \frac { 1 } { 2 ^ { d } } \frac { 1 } { 2 4 \pi } \| x \| \| x _ { * } \| < 0 .
$$

$$
\left \langle - \frac { x } { \| x \| } , v _ { x } \right \rangle \geqslant \frac { 1 } { 2 ^ { d } } \frac { 1 } { 2 4 \pi } \| x _ { * } \| .
$$

But by the Cauchy-Schwarz inequality,   − x   x   ,v x       v x   so we obtain

$$
\| v _ { x } \| \geqslant \frac { 1 } { 2 ^ { d } } \frac { 1 } { 2 4 \pi } \| x _ { * } \| .
$$

[Page 25]

$$
\langle x , v _ { x } \rangle = \sum _ { \ell = 1 } ^ { s } c _ { \ell } \langle x , v _ { \ell } \rangle \leqslant - \frac { 1 } { 2 ^ { d } } \frac { 1 } { 2 4 \pi } \| x | \| x _ { * } \| \sum _ { \ell = 1 } ^ { s } c _ { \ell } = - \frac { 1 } { 2 ^ { d } } \frac { 1 } { 2 4 \pi } \| x \| \| x _ { * } \| < 0 .
$$

For the lower bound on the norm of v x , note that by the Cauchy-Schwarz inequality and equation ( 28 ), we have that

$$
\| v _ { x } \| = \max _ { \| u \| = 1 } \langle v _ { x } , u \rangle \geqslant \left \langle v _ { x } , - \frac { x } { \| x \| } \right \rangle = \sum _ { \ell = 1 } ^ { s } c _ { \ell } \left \langle v _ { \ell } , - \frac { x } { \| x \| } \right \rangle \geqslant \frac { 1 } { 2 ^ { d } } \frac { 1 } { 2 4 \pi } \| x _ { * } \| \sum _ { \ell = 1 } ^ { s } c _ { \ell } = \frac { 1 } { 2 ^ { d } } \frac { 1 } { 2 4 \pi } \| x _ { * } \|
$$

as desired.

2.5.2 Proofs for Section 2.4.1 In this section, we focus on results that aided in establishing Lemma 6 in Section 2.4.1 . The ﬁrst result concerns a bound on the norm of our descent direction (Lemma 3 ). The second is that h x is Lipschitz with respect to x ∈ R k outside of a ball of the origin (Lemma 4 ) and the third is that for all x ∈ R k , h x approximates any v x ∈ ∂f ( x ) (Lemma 5 ). Prior to beginning the proof of Lemma 3 , we outline some notation. For x   = 0 , set ψ d,x := π − 2 θ d,x π , and ζ j +1 ,x :=   d − 1 i = j π − θ j +1 ,x π . Based on this notation, h x can be written as

glyph[negationslash]

$$
h _ { x } = \frac { 1 } { 2 ^ { d } } \left [ \psi _ { d , x } \zeta _ { 0 , x } \| x _ { * } \| \hat { x } _ { * } + \left ( \| x \| - \| x _ { * } \| \left ( \frac { 2 \sin \bar { \theta } _ { d , x } } { \pi } + \psi _ { d , x } \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i , x } } { \pi } \zeta _ { i + 1 , x } \right ) \right ) \hat { x } \right ] .
$$

In the remaining proofs, a number of results concerning properties of θ i,x and ˘ θ i will be useful. The following lemma records these results:

Lemma 17 (Bounds from Lemma 10 in [ 33 ]) . For x   = 0 , let θ 0 ,x := ∠ ( x,x ∗ ) and θ i,x := g ( θ i − 1 ,x )

glyph[negationslash]

[Page 26]

$$
\varrho _ { i } = g ( \check { \theta } _ { i - 1 } ) \text { for } i \in [ d ] . \text { Then the following all hold:} \\ \left | \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i , x } } { \pi } \right | & \leqslant 1 , & ( 3 ) \\ \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i , x } } { \pi } \geqslant \frac { \pi - \bar { \theta } _ { 0 , x } } { \pi d ^ { 3 } } , & ( 3 ) \\ \eta \bar { \theta } _ { i } \cdot \left ( d - 1 \right ) _ { x } \pi - \bar { \theta } _ { i } \cdot \right ) & \right | _ { x } d
$$

for i ∈ [ d ] . Let ˘ θ 0 := π and ˘ θ i = g ( ˘ θ i − 1 ) for i ∈ [ d ] . Then the following all hold:

$$
\mathbb { J } . \ L e t \, \theta _ { 0 } \coloneqq \pi \ a n d \, \theta _ { i } = g ( \theta _ { i - 1 } ) \, \text { for } i \in [ d ] . \ \text { Then the following all hold:} \\ & \left | \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i , x } } { \pi } \right | \leqslant 1 , \\ & \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i , x } } { \pi } \geqslant \frac { \pi - \bar { \theta } _ { 0 , x } } { \pi d ^ { 3 } } , \\ & \left | \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \theta _ { i , x } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \theta _ { j , x } } { \pi } \right ) \right | \leqslant \frac { d } { \pi } \sin \bar { \theta } _ { 0 , x } , \\ & \bar { \theta } _ { 0 , x } = \pi + O _ { 1 } ( \delta ) \Longrightarrow \bar { \theta } _ { i , x } = \check { \theta } _ { i } + O _ { 1 } ( i \delta ) , \\ & \bar { \theta } _ { 0 , x } = \pi + O _ { 1 } ( \delta ) \Longrightarrow \left | \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i , x } } { \pi } \right | \leqslant \frac { \delta } { \pi } , \\ & \left | \pi - 2 \bar { \theta } _ { i , x } \right | \leqslant 1 \, \forall \, \mathring { \theta } _ { i } > 0
$$

$$
\bar { \theta } _ { 0 , x } = \pi + O _ { 1 } ( \delta ) \Longrightarrow \left | \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i , x } } { \pi } \right | \leqslant \frac { \delta } { \pi } ,
$$

$$
\left | \frac { \pi - 2 \bar { \theta } _ { i , x } } { \pi } \right | \leqslant 1 \, \forall \, i \geqslant 0 ,
$$

$$
\bar { \theta } _ { d , x } \leqslant \cos ^ { - 1 } \left ( \frac { 1 } { \pi } \right ) \ \forall \ d \geqslant 2 ,
$$

$$
\check { \theta } _ { i } \leqslant \frac { 3 \pi } { i + 3 } \ \forall \ i \geqslant 0 ,
$$

$$
\check { \theta } _ { i } \geqslant \frac { \pi } { i + 1 } \ \forall \ i \geqslant 0 .
$$

We ﬁrst focus on proving Lemma 3 .

Proof of Lemma 3 . Suppose f is diﬀerentiable at x . By ( 30 ) and ( 35 ), we have that max( | ψ d,x | , | ζ i,x | )   1 for any i = 0 ,...,d . Hence we have the bound

$$
\| h _ { x } \| & \leqslant \frac { 1 } { 2 ^ { d } } \left ( | \psi _ { d , x } | \zeta _ { 0 , x } | | | _ { x } | + | | x | | + \frac { 2 | \sin \bar { \theta } _ { d , x } | } { \pi } | | x _ { * } | + | \psi _ { d , x } | \sum _ { i = 0 } ^ { d - 1 } \frac { | \sin \bar { \theta } _ { i , x } | } { \pi } | \zeta _ { i + 1 , x } | | | x _ { * } | \right ) \\ & \leqslant \frac { 4 + d / \pi } { 2 ^ { d } } \max ( | | x | | , | | x _ { * } | | ) .
$$

Combining equation ( 39 ) and Lemma 5 , we attain

$$
\| v _ { x } \| & \leqslant \| h _ { x } \| + \| h _ { x } - v _ { x } \| \\ & \leqslant \frac { 4 + d / \pi } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { K d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| \\ & \leqslant \frac { C d } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| \\
$$

where in the last inequality we used Kd 3 √ ε   1 and set C = 5 + 1 /π .

When f is not diﬀerentiable at x , we have that by equation ( 12 ), we can write v x =   s   =1 c   v   where each c     0 and   s   =1 c   = 1 . Applying ( 40 ) for diﬀerentiable points, we have that

$$
\| v _ { x } \| & \leqslant \sum _ { \ell = 1 } ^ { s } c _ { \ell } \| v _ { \ell } \| \leqslant \sum _ { \ell = 1 } ^ { s } c _ { \ell } \left ( \frac { C d } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| \right ) = \frac { C d } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| .
$$

[Page 27]

Proof of Lemma 4 . Throughout the proof, we will use the following result from Lemma 5.1 in [ 35 ]:

$$
| \bar { \theta } _ { 0 , x } - \bar { \theta } _ { 0 , y } | \leqslant 4 \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \| .
$$

For any x,y   = 0 , we have that

glyph[negationslash]

$$
\text {For any } x , y \neq 0 , \text { we have that} \\ \| h _ { x } - h _ { y } \| & \leq \underbrace { \frac { 1 } { 2 ^ { d } } | \psi _ { d , x } \zeta _ { 0 , x } - \psi _ { d , y } \zeta _ { 0 , y } | | | x _ { * } | | } _ { ( I ) } + \frac { 1 } { 2 ^ { d } } \| x - y \| + \underbrace { \frac { | | x _ { * } | | } { 2 ^ { d } } \left \| \frac { 2 \sin \bar { \theta } _ { d , x } } { \pi } \hat { x } - \frac { 2 \sin \bar { \theta } _ { d , y } } { \pi } \hat { y } \right \| } _ { ( I I ) } } \\ & + \underbrace { \frac { | | x _ { * } | | } { 2 ^ { d } } \left \| \psi _ { d , x } \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i , x } } { \pi } \zeta _ { i + 1 , x } \hat { x } - \psi _ { d , y } \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i , y } } { \pi } \zeta _ { i + 1 , y } \hat { y } \right \| } _ { ( I I I ) } } \\ \text {We will focus on bounding each of the individual quantities.}
$$

We will focus on bounding each of the individual quantities.

(I): The triangle inequality gives | ψ d,x ζ d,x − ψ d,y ζ d,y |   | ψ d,x || ζ d,x − ζ d,y | + | ζ d,y || ψ d,x − ψ d,y | . By ( 30 ) and ( 35 ), we have max {| ψ d,x | , | ζ d,x |}   1 for all x   = 0 . In addition,

glyph[negationslash]

$$
| \psi _ { d , x } - \psi _ { d , y } | = \frac { 2 } { \pi } | \bar { \theta } _ { d , x } - \bar { \theta } _ { d , y } | .
$$

Since g   ( θ ) ∈ [0 , 1] for all θ ∈ [0 ,π ] and θ i,x = g ( θ i − 1 ,x ) , we have that | θ i,x − θ i,y |   | θ i − 1 ,x − θ i − 1 ,y | . Repeatedly applying this inequality for each i ∈ [ d ] , we attain

$$
| \bar { \theta } _ { d , x } - \bar { \theta } _ { d , y } | \leqslant | \bar { \theta } _ { 0 , x } - \bar { \theta } _ { 0 , y } | \leqslant 4 \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \|
$$

where we used ( 41 ) in the last inequality. Hence combining ( 42 ) and ( 43 ), we get

$$
| \psi _ { d , x } - \psi _ { d , y } | = \frac { 2 } { \pi } | \bar { \theta } _ { d , x } - \bar { \theta } _ { d , y } | \leqslant \frac { 8 } { \pi } \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \| .
$$

Using the deﬁnition of ζ 0 ,x , another application of ( 41 ) gives

$$
| \zeta _ { 0 , x } - \zeta _ { 0 , y } | & \leqslant \frac { d } { \pi } | \bar { \theta } _ { 0 , x } - \bar { \theta } _ { 0 , y } | \leqslant \frac { 4 d } { \pi } \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \| .
$$

Combining our results, if K 1 := 8+4 d π then

$$
( I ) \leqslant \frac { \| x _ { * } \| } { 2 ^ { d } } \, K _ { 1 } \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \|
$$

(II): Observe that we have

$$
\colon \, & \text {Observe that we have} \\ & \frac { 2 \sin \bar { \theta } _ { d , x } } { \pi } \hat { x } = \frac { 2 \sin \bar { \theta } _ { d , x } } { \pi } \hat { y } + O _ { 1 } \left ( \frac { 2 } { \pi } \| \hat { x } - \hat { y } \| \right ) \\ & = \left ( \frac { 2 \sin \bar { \theta } _ { d , y } } { \pi } + O _ { 1 } \left ( \frac { 2 } { \pi } | \bar { \theta } _ { d , x } - \bar { \theta } _ { d , y } | \right ) \right ) \hat { y } + O _ { 1 } \left ( \frac { 2 } { \pi } \| \hat { x } - \hat { y } \| \right ) \\ & = \frac { 2 \sin \bar { \theta } _ { d , y } } { \pi } \hat { y } + O _ { 1 } \left ( \frac { 2 } { \pi } \cdot 4 + \frac { 4 } { \pi } \right ) \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \|
$$

[Page 28]

$$
( I I ) \leqslant \frac { \| x _ { * } \| } { 2 ^ { d } } K _ { 2 } \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \|
$$

(III): The ﬁnal term follows from

$$
( I I ) \colon \, \text { The final term follows from } \\ \psi _ { d , x } \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta _ { i , x } } } { \pi } \zeta _ { + 1 , x } \hat { x } = \psi _ { d , y } \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta _ { i , x } } } { \pi } \zeta _ { + 1 , x } \hat { x } + O _ { 1 } \left ( \frac { 8 d } { \pi ^ { 2 } } \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \| \right ) \\ = \psi _ { d , y } \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta _ { i , y } } } { \pi } \zeta _ { + 1 , x } \hat { x } + O _ { 1 } \left ( \frac { 4 d } { \pi } + 8 d \right ) \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \| \\ = \psi _ { d , y } \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta _ { i , y } } } { \pi } \zeta _ { + 1 , y } \hat { x } + \frac { 1 } { \pi } ^ { d - 1 } O _ { 1 } \left ( \frac { d - i - 1 } { \pi } | \bar { \theta _ { i , 1 - x } } - \bar { \theta _ { i - 1 , x } } | \right ) \\ + O _ { 1 } \left ( \frac { 4 d } { \pi } + 8 d \right ) \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \| \\ = \psi _ { d , y } \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta _ { i , y } } } { \pi } \zeta _ { + 1 , y } \hat { x } + O _ { 1 } \left ( \frac { 2 d ^ { 2 } } { \pi ^ { 2 } } + \frac { 4 d } { \pi } + \frac { 8 d } { \pi ^ { 2 } } \right ) \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \| \\ = \psi _ { d , y } \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta _ { i , y } } } { \pi } \zeta _ { + 1 , y } \hat { x } + O _ { 1 } \left ( \frac { 2 d } { \pi } + \frac { 2 d ^ { 2 } } { \pi ^ { 2 } } + \frac { 4 d } { \pi } + \frac { 8 d } { \pi ^ { 2 } } \right ) \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \| \\ = \psi _ { d , y } \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta _ { i , y } } } { \pi } \zeta _ { + 1 , y } \hat { x } + O _ { 1 } \left ( \frac { \pi } { \pi } + \frac { \pi ^ { 2 } } { \pi ^ { 2 } } + \frac { \pi ^ { 2 } } { \pi } + \frac { \pi ^ { 2 } } { \pi ^ { 2 } } \right ) \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \| \\ \intertext { w h e r e t h e f r i n g l e f o u l s f o r q u e n t i o n ( 4 3 ) and ( 4 3 ) } O _ { 1 } \cdot \dots , d - 1 a n d r ; the second line f o r m e g h e r d f o r | \zeta _ { i + 1 , x } - \zeta _ { i + 1 , y } | < \frac { d - i - 1 } { \pi } | \theta _ { i - 1 , x } - \bar { \theta _ { i - 1 , y } } | ;
$$

where the ﬁrst line follows from equations ( 42 ) and ( 43 ) and using | sin θ i,x ζ i +1 ,x |   1 for any i = 0 ,...,d − 1 and x ; the second line from ( 43 ); the third from | ζ i +1 ,x − ζ i +1 ,y |   d − i − 1 π | θ i − 1 ,x − θ i − 1 ,y | ; the fourth from | θ i − 1 ,x − θ i − 1 ,y |   | θ 0 ,x − θ 0 ,y | , ( 41 ), and   d − 1 i =0 ( d − i − 1) = 1 2 ( d − 1) d ; and the ﬁfth from | sin θ i,y ζ i +1 ,y |   1 for all i = 0 ,...,d − 1 and   ˆ x − ˆ y     2max   1   x   , 1   y       x − y   .Combining our results, we have that if K 3 := 8 d +2 d 2 π 2 + 6 d π then

$$
( I I I ) \leqslant \frac { \| x _ { * } \| } { 2 ^ { d } } K _ { 3 } \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) \| x - y \| .
$$

Thus for all x,y   = 0 , using equations ( 44 ), ( 45 ), and ( 46 ), we conclude that

glyph[negationslash]

$$
\left \| h _ { x } - h _ { y } \right \| & \leq ( I ) + \frac { 1 } { 2 ^ { d } } \| x - y \| + ( I I ) + ( I I I ) \\ & \leq \frac { 1 } { 2 ^ { d } } \left ( \| x _ { * } \| ( K _ { 1 } + K _ { 2 } + K _ { 3 } ) \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) + 1 \right ) \| x - y \| \\ & = \left ( \frac { ( 2 d ^ { 2 } + ( 1 0 \pi + 8 ) d + 2 \pi ) \| x _ { * } \| } { \pi ^ { 2 d } } \max \left ( \frac { 1 } { \| x \| } , \frac { 1 } { \| y \| } \right ) + \frac { 1 } { 2 ^ { d } } \right ) \| x - y \| .
$$

Then if x,y / ∈ B (0 ,r ) , we can further conclude that

$$
\| h _ { x } - h _ { y } \| \leqslant \left ( \frac { ( 2 d ^ { 2 } + ( 1 0 \pi + 8 ) d + 2 0 \pi ) \| x _ { * } \| } { r \pi ^ { 2 } 2 ^ { d } } + \frac { 1 } { 2 ^ { d } } \right ) \| x - y \| .
$$

[Page 29]

$$
w _ { x } \colon = \Lambda _ { x } ^ { \text {T} } ( \Lambda _ { x } x - \Phi _ { x _ { d } , x _ { * } , d } \Lambda _ { x _ { * } } x _ { * } ) .
$$

The key idea is that the RRCP and WDC together imply v x ≈ w x and the WDC further implies w x ≈ h x which is shown in Lemma 19 .

Proof of Lemma 5 . Suppose f is diﬀerentiable at x so that v x = v x − q x where v x = Λ T x A T x d ( A x d Λ x x − A x ∗ ,d Λ x ∗ x ∗ ) and q x = Λ T x A T x d η . Observe that

$$
\| \overline { v } _ { x } - w _ { x } \| & \leqslant \left \| \Lambda _ { x } ^ { T } ( A _ { x _ { d } } ^ { T } A _ { x _ { d } } - I _ { n _ { d } } ) \Lambda _ { x } \right \| \left \| x \right \| + \left \| \Lambda _ { x } ^ { T } ( A _ { x _ { d } } ^ { T } A _ { x _ { * } , d } - \Phi _ { x _ { d } , x _ { * } , d } ) \Lambda _ { x _ { * } } \right \| \left \| x _ { * } \right \| .
$$

By the local linearity of G , for suﬃciently small z ∈ R k , we have G ( x + z ) − G ( x ) = Λ x z . Hence by the RRCP, we have for suﬃciently small z, ˜ z ∈ R k ,

and

$$
| \langle ( A _ { x _ { d } } ^ { T } A _ { x _ { d } } - I _ { n _ { d } } ) \Lambda _ { x } z , \Lambda _ { x } z \rangle | \leqslant L \varepsilon \| \Lambda _ { x } \| ^ { 2 } \| z \| ^ { 2 }
$$

$$
| \langle ( A _ { x _ { d } } ^ { T } A _ { x _ { * , d } } - \Phi _ { x _ { d } , x _ { * , d } } ) \Lambda _ { x } z , \Lambda _ { x _ { * } } \tilde { z } \rangle | \leqslant L \varepsilon \| \Lambda _ { x } \| \| \Lambda _ { x _ { * } } \| \| z \| \| \tilde { z } \| .
$$

Since this holds for any z, ˜ z ∈ R k , we conclude that

$$
\left \| \Lambda _ { x } ^ { T } ( A _ { x _ { d } } ^ { T } A _ { x _ { d } } - I _ { n _ { d } } ) \Lambda _ { x } \right \| & \leqslant L \varepsilon \| \Lambda _ { x } \| ^ { 2 } \text { and } \| \Lambda _ { x } ^ { T } ( A _ { x _ { d } } ^ { T } A _ { x _ { * } , d } - \Phi _ { x _ { d } , x _ { * } , d } ) \Lambda _ { x _ { * } } \| \leqslant L \varepsilon \| \Lambda _ { x } \| \| \Lambda _ { x _ { * } } \| .
$$

This implies

$$
\| \bar { v } _ { x } - w _ { x } \| & \leqslant L \varepsilon \left ( \| \Lambda _ { x } \| ^ { 2 } + \| \Lambda _ { x } \| \| \Lambda _ { x _ { * } } \| \right ) \max ( \| x \| , \| x _ { * } \| ) \\ & \leqslant 2 L \varepsilon \left ( \frac { 1 } { 2 } + \varepsilon \right ) ^ { d } \max ( \| x \| , \| x _ { * } \| )
$$

where the last inequality follows by the WDC. Furthermore, by Lemma 19 , we have that

$$
\| w _ { x } - h _ { x } \| \leqslant \frac { 9 0 d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) .
$$

Combining these two bounds, we have

$$
\| \bar { v } _ { x } - h _ { x } \| & \leqslant \| \bar { v } _ { x } - w _ { x } \| + \| w _ { x } - h _ { x } \| \\ & \leqslant \sqrt { \varepsilon } \left ( 2 L \frac { ( 1 + 2 \varepsilon ) ^ { d } } { 2 ^ { d } } + 9 0 \frac { d ^ { 3 } } { 2 ^ { d } } \right ) \max ( \| x \| , \| x _ { * } \| ) \\ & \leqslant \sqrt { \varepsilon } K \frac { d ^ { 3 } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| )
$$

for some universal constant K where the third inequality follows since 2 εd   1 = ⇒ (1 + 2 ε ) d   e 2 εd   1 + 4 εd so choosing ε < 1 / (4 d ) implies (1 + 2 ε ) d   2 . Lastly, to bound   q x   , observe that

$$
\| q _ { x } \| \leqslant \| A _ { x _ { d } } \Lambda _ { x } \| \| \eta \| \leqslant \sqrt { 1 + L \varepsilon } \| \Lambda _ { x } \| \| \eta \| \leqslant \sqrt { \frac { 1 3 } { 1 2 } ( 1 + L \varepsilon ) } \frac { 1 } { 2 ^ { d / 2 } } \| \eta \| \leqslant \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| .
$$

[Page 30]

$$
\| v _ { x } - h _ { x } \| \leqslant \| \overline { v } _ { x } - h _ { x } \| + \| q _ { x } \| \leqslant K \frac { d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| .
$$

When f is not diﬀerentiable at x , we can use ( 12 ) to write v x =   s   =1 c   v   where c     0 for   ∈ [ s ] . Moreover, note that for each v   , there exists a direction w   such that v   = lim δ   → 0 + ∇ f ( x + δ   w   ) and f is diﬀerentiable at x + δ   w   for suﬃciently small δ   > 0 . Appealing to the continuity of h x for x   = 0 , we obtain

glyph[negationslash]

$$
and j \text { is differentiable at } x + \delta w _ { \ell } \text { for sufficiently small } \delta _ { \ell } > 0 . \text { Appending to the continuity of } h _ { x } \\ \text { for } x \neq 0 , \text { we obtain} \\ & \| v _ { x } - h _ { x } \| \leq \sum _ { \ell = 1 } ^ { s } c _ { \ell } \| v _ { \ell } - h _ { x } \| = \sum _ { \ell = 1 } ^ { s } c _ { \ell } \| \lim _ { \delta \to 0 ^ { + } } \nabla f ( x + \delta w _ { \ell } ) - h _ { x } \| \\ & = \sum _ { \ell = 1 } ^ { s } c _ { \ell } \lim _ { \delta \to 0 ^ { + } } \| \nabla f ( x + \delta w _ { \ell } ) - h _ { x } + \delta w _ { \ell } \| \\ & = \sum _ { \ell = 1 } ^ { s } c _ { \ell } \lim _ { \delta \to 0 ^ { + } } \| v _ { x + \delta w _ { \ell } } - h _ { x + \delta w _ { \ell } } \| \\ & \leq \sum _ { \ell = 1 } ^ { s } c _ { \ell } \left ( K \frac { d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| \right ) \\ & = K \frac { d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \max ( \| x \| , \| x _ { * } \| ) + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| .
$$

We now establish a technical result that shows w x is approximated by h x . Prior to this proof, we highlight the following result that summarizes some useful bounds from [ 33 ]:

Lemma 18 (Results from Lemma 5 in [ 33 ]) . Fix 0 < ε < d − 4 (1 / 16 π ) 2 and let d   2 . Let W i satisfy the WDC with constant ε for i = 1 ,...d . Then for any non-zero x,y ∈ R k , the following hold:

$$
\left \| \Lambda _ { x } ^ { T } \Lambda _ { y } y - \tilde { h } _ { x , y } \right \| \leqslant 2 4 \frac { d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \| y \| ,
$$

$$
\langle \Lambda _ { x } x , \Lambda _ { y } y \rangle & \geqslant \frac { 1 } { 4 \pi } \frac { 1 } { 2 ^ { d } } \| x \| \| y \| , \\
$$

$$
\left | \frac { \| y _ { d } \| } { \| x _ { d } \| } - \frac { \| y \| } { \| x \| } \right | \leqslant 8 d \varepsilon \frac { \| y \| } { \| x \| } ,
$$

$$
\left | \frac { \| y _ { d } \| } { \| x _ { d } \| } - \frac { \| y \| } { \| x \| } \right | & \leqslant 8 d \varepsilon \frac { \| y \| } { \| x \| } , \\ | \theta _ { d } - \bar { \theta } _ { d } | & \leqslant 4 d \sqrt { \varepsilon } \\ \intertext { s o r e } \theta _ { d } \cdot = \frac { ( x _ { d } , y _ { d } ) , \ \bar { \theta } _ { d } \cdot = g ^ { d } ( z ( x , y ) ) , \ \text { and } \text { the vector } \tilde { h } _ { x , y } \text { is defined as } \\ \tilde { h } _ { x , y } & \colon = \frac { 1 } { 2 ^ { d } } \left [ \left ( \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \right ) y + \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { j } } { \pi } \right ) \frac { \| y \| } { \| x \| } x \right ] \\ \bar { h } _ { x , y } & = - \left ( x _ { d } \, y \right ) \, \text {and} \, \bar { \theta } _ { i } = - \alpha \bar { \theta } _ { i } \, .
$$

where θ d := ∠ ( x d ,y d ) , θ d := g ◦ d ( ∠ ( x,y )) , and the vector ˜ h x,y is deﬁned as

with θ 0 := ∠ ( x,y ) and θ i := g ( θ i − 1 ) for i ∈ [ d ] .

We now establish that w x is approximated by h x .

[Page 31]

Lemma 19. Fix 0 < ε < d − 4 (1 / 16 π ) 2 . Let W i satisfy the WDC with constant ε for i = 1 ,...d . For any non-zero x ∈ R k , we have

where w x is deﬁned by ( 47 ) .

$$
\| w _ { x } - h _ { x } \| \leqslant \frac { 9 0 d ^ { 3 } } { 2 ^ { d } } \sqrt { \varepsilon } \max ( \| x \| , \| x _ { * } \| )
$$

Proof. Fix x ∈ R k \ { 0 } and set θ d := ∠ ( x d ,x ∗ ,d ) . Note that by the deﬁnition of Φ z,w and M ˆ z ↔ ˆ w , w x can be written as

$$
w _ { x } = \Lambda _ { x } ^ { T } \Lambda _ { x } x - \frac { \pi - 2 \theta _ { d } } { \pi } \Lambda _ { x } ^ { T } \Lambda _ { x * } x _ { * } - \frac { 2 \sin \theta _ { d } } { \pi } \frac { \| \Lambda _ { x _ { * } } x _ { * } \| } { \| \Lambda _ { x } x \| } \Lambda _ { x } ^ { T } \Lambda _ { x } x
$$

where θ d := ∠ ( x d ,x ∗ ,d ) . Observe that

$$
\| w _ { x } - h _ { x } \| & \leqslant \left \| \Lambda _ { x } ^ { T } \Lambda _ { x } x - \frac { 1 } { 2 ^ { d } } x \right \| + \left \| \frac { \pi - 2 \theta _ { d } } { \pi } \Lambda _ { x } ^ { T } \Lambda _ { x } x _ { * } - \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \tilde { h } _ { x , x } \right \| \\ & + \left \| \frac { 2 \sin \theta _ { d } } { \pi } \frac { \| \Lambda _ { x } x _ { * } \| } { \| \Lambda _ { x } x \| } \Lambda _ { x } ^ { T } \Lambda _ { x } x - \frac { 2 \sin \bar { \theta } _ { d } } { \pi } \frac { \| x _ { * } \| } { \| x \| } \frac { 1 } { 2 ^ { d } } x \right \| \\ \bar { \theta } _ { d } & = d ( \epsilon _ { x } - 1 ) \\
$$

  where θ d := g ◦ d ( ∠ ( x,x ∗ )) and ˜ h x,x ∗ is deﬁned in ( 54 ).

We focus on bounding each individual quantity separately. For the ﬁrst term, we have that by ( 50 ) in Lemma 18 ,

$$
\Lambda _ { x } ^ { T } \Lambda _ { x } x = \frac { 1 } { 2 ^ { d } } x + O _ { 1 } \left ( \frac { 2 4 d ^ { 3 } } { 2 ^ { d } } \right ) \sqrt { \varepsilon } \max ( \| x \| , \| x _ { * } \| ) .
$$

For the second term, observe that

$$
For the second term, observe that \\ \frac { \pi - 2 \theta _ { d } } { \pi } \Lambda _ { x } \Lambda _ { x * } = \frac { \pi - 2 \theta _ { d } } { \pi } \tilde { h } _ { x , x * } + O _ { 1 } \left ( \frac { 2 4 d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { 2 d } \| x \| } \right ) \\ = \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } + O _ { 1 } \left ( \frac { 2 } { \pi } \cdot 4 d \sqrt { \varepsilon } \right ) \right ) \tilde { h } _ { x , x * } + O _ { 1 } \left ( \frac { 2 4 d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { 2 d } } \| x \| \right ) \\ = \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \tilde { h } _ { x , x * } + O _ { 1 } \left ( \frac { 8 d } { \pi } \cdot \frac { ( 1 + d / \pi ) } { 2 ^ { d } } + \frac { 2 4 ^ { 3 } } { 2 ^ { d } } \right ) \sqrt { \varepsilon } \max ( \| x \| , \| x * \| ) \\ \intertext { w h e i n the f i n s e l a t y u s e w i d ( 5 ) a n d i n g e s t h e c o n d t a t h e t h a t h e t h a n g a l l s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e s t h a t h e s e
$$

where in the ﬁrst equality we used ( 50 ) and in the second we used ( 53 ) and the fact that   ˜ h x,x ∗     2 − d (1 + d/π )   x ∗   . For the ﬁnal term, observe that

$$
\text {where in the first equality we used (50) and in the second we used ( 53) and the fact that \| \tilde { h } _ { x , x } \| \leq \\ 2 ^ { - d ( 1 + d / \pi ) \| x _ { * } \| . \text { For the final term, observe that} \\ \frac { 2 \sin \theta _ { d } \| \Lambda _ { x , x } \| _ { \Lambda _ { x } } T _ { \Lambda _ { x } } } { \pi } \frac { 2 \sin \theta _ { d } \| \Lambda _ { x , x } \| ^ { \text { } } \left ( \frac { 1 } { 2 ^ { d } } + O _ { 1 } \left ( \frac { 2 4 ^ { d } \sqrt { \varepsilon } } { 2 ^ { d } } \| x \| \right ) \right ) } { \pi } \\ = \frac { 2 \sin \theta _ { d } \| \Lambda _ { x } x \| ^ { \text { } } } { \pi } \frac { 2 \sin \theta _ { x } } { \| \Lambda _ { x } x \| ^ { \frac { 2 d } { 2 } } + O _ { 1 } \left ( \frac { 2 4 ^ { d } \sqrt { \varepsilon } } { 2 ^ { d } } \frac { 2 \| \Lambda _ { x } x \| ^ { \ast } } { \pi } \| x \| \right ) } \\ = \frac { 2 \sin \theta _ { d } } { \pi } \left ( \frac { \| x \| ^ { \ast } } { \| x \| } + O _ { 1 } \left ( 8 d \frac { \| x _ { * } \| } { \| x \| } \right ) \right ) \frac { 1 } { 2 ^ { d } } x + O _ { 1 } \left ( 4 \cdot 2 4 ^ { d } \sqrt { \varepsilon } \| x _ { * } \| \right ) \\ = \frac { 2 \sin \theta _ { d } } { \pi } \frac { \| x _ { * } \| } { \| x \| ^ { 2 } } \frac { 1 } { 2 ^ { d } } x + O _ { 1 } \left ( \frac { 1 6 d \varepsilon } { \pi 2 d } \| x \| + 4 \cdot 2 4 ^ { d } \sqrt { \varepsilon } \| x _ { * } \| \right ) \\ = \left ( 2 \sin \theta _ { d } + O _ { 1 } \left ( \frac { 8 d \sqrt { \varepsilon } } { \pi } \right ) \right ) \right ) \| x \| ^ { 2 } _ { x } + O _ { 1 } \left ( \frac { 1 6 d \varepsilon } { \pi 2 d } \| x \| + 4 \cdot 2 4 ^ { d } \sqrt { \varepsilon } \| x _ { * } \| \right ) \\ = \frac { 2 \sin \theta _ { d } } { \pi } \frac { \| x _ { * } \| } { \| x \| ^ { 2 } } \frac { 1 } { 2 ^ { d } } x + O _ { 1 } \left ( \frac { 1 } { 2 ^ { d } } \left ( \frac { 2 4 d + 4 \cdot 2 4 ^ { d } } { \pi } \right ) \right ) \sqrt { \varepsilon } \max ( \| x \| , \| x _ { * } \| ) . \\
$$

[Page 32]

# 2.5.3 Proofs for Section 2.4.3

We ﬁrst establish Lemma 8 which shows that the zeros of h x occur near x ∗ and a particular negative multiple − ρ d x ∗ . Here the lemma is stated more precisely.

Proposition 2. Suppose β > 0 obeys 24 πd 6 √ β   1 and deﬁne S β as in ( 11 ) . If x ∈ S β , then either

$$
| \bar { \theta } _ { 0 , x } | \leqslant 8 2 \pi d ^ { 4 } \beta \ a n d \, | | x | | - \| x _ { * } \| | \leqslant 8 3 8 \pi d ^ { 5 } \beta \| x _ { * } \|
$$

or

$$
| \bar { \theta } _ { 0 , x } - \pi | \leqslant 2 4 \pi ^ { 2 } d ^ { 4 } \sqrt { \beta } \ a n d \ \| x \| - \rho _ { d } \| x _ { * } \| \leqslant 3 5 1 7 d ^ { 8 } \sqrt { \beta } \| x _ { * } \| .
$$

In particular, we have

$$
\mathcal { S } _ { \beta } \subset \mathcal { B } ( x _ { * } , 7 0 0 0 0 \pi ^ { 2 } d ^ { 9 } \beta \| x _ { * } \| ) \cup \mathcal { B } ( - \rho _ { d } x _ { * } , 7 7 4 2 2 \pi ^ { 2 } d ^ { 1 2 } \sqrt { \beta } \| x _ { * } \| ) .
$$

Additionally, ρ d → 1 as d → ∞ . Proof of Proposition 2 . Without loss of generality, let x ∗ = e 1 and   x ∗   = 1 where e 1 is the ﬁrst standard basis vector in R k . We also let x = r cos θ 0 e 1 + r sin θ 0 e 2 where θ 0 = ∠ ( x,x ∗ ) . For simplicity, we use the shorthand notation θ i = θ i,x for i ∈ [ d ] . Set ξ =   π − 2 θ d π     d − 1   π − θ i π   and α = 2sin θ d π +   π − 2 θ d π   d − 1   sin θ i π   d − 1   π − θ j π   .

$$
\xi = \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \left ( \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \right ) \text { and } \alpha = \frac { 2 \sin \bar { \theta } _ { d } } { \pi } + \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { j } } { \pi } \right ) .
$$

Note that we can write

Then if x ∈ S β , we have that

$$
h _ { x } = \frac { 1 } { 2 ^ { d } } \left ( - \xi \hat { x } _ { * } + ( r - \alpha ) \hat { x } \right )
$$

$$
| - \xi + \cos \overline { \theta } _ { 0 } ( r - \alpha ) | \leqslant \beta M
$$

$$
| \sin \bar { \theta } _ { 0 } ( r - \alpha ) | \leqslant \beta M
$$

where M := max( r, 1) .

To prove the Proposition, we ﬁrst show that it is suﬃcient to only consider the small and large angle case. Then, we show that in the small and large angle case, x ≈ x ∗ and x ≈ − ρ d x ∗ , respectively. We begin by proving that max(   x   ,   x ∗   )   6 d for any x ∈ S β .

Bound on maximal norm in S β : It suﬃces to show that r   6 d . Suppose r > 1 since if r   1 , the result is immediate. Then either | sin θ 0 |   1 / √ 2 or | cos θ 0 |   1 / √ 2 . If | sin θ 0 |   1 / √ 2 then ( 59 ) gives

$$
\text {round on maximal norm in } \mathcal { S } _ { \beta } \colon \text { It suffices to show that } r \leqslant 6 d . \text { Suppose } r > 1 \sin \delta . \\ , \text { the result is immediate. Then either } | \sin \bar { \theta } _ { 0 } | \geqslant 1 / \sqrt { 2 } \text { or } | \cos \bar { \theta } _ { 0 } | \geqslant 1 / \sqrt { 2 } . \text { If } | \sin \bar { \theta } _ { 0 } | \geqslant \\ | r - \alpha | \leqslant \sqrt { 2 } \beta r \Longrightarrow ( 1 - \sqrt { 2 } \beta ) r \leqslant | \alpha | . \\ | r - \alpha | \leqslant \sqrt { 2 } \beta r \Longrightarrow ( 1 - \sqrt { 2 } \beta ) r \leqslant | \alpha | . \\ | \alpha | \leqslant \frac { 2 } { \pi } | \sin \bar { \theta } _ { d } | + \left | \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \right ) \right | \leqslant 1 + \frac { d } { \pi }
$$

But

[Page 33]

$$
r \leqslant \frac { 1 + \frac { d } { \pi } } { 1 - \sqrt { 2 } \beta } \leqslant 2 \left ( 1 + \frac { d } { \pi } \right ) \leqslant 2 + d \leqslant 2 d
$$

− provided β < 1 / 4 and d   2 . If | cos θ 0 |   1 / √ 2 , then ( 58 ) gives

$$
\vec { l } \leqslant \frac { 1 - \sqrt { 2 } \beta } { 1 - \sqrt { 2 } \beta } \leqslant 2 \left ( 1 + \frac { } { \pi } \right ) \leqslant 2 + a \leqslant 2 a \\ \vec { d } ( 3 ) = \\ | r - \alpha | \leqslant \sqrt { 2 } ( \beta r + | \xi | ) \Longrightarrow ( 1 - \sqrt { 2 } \beta ) r \leqslant \sqrt { 2 } | \xi | + \alpha . \\ \intertext { y ( 3 0 ) , } | \xi | = \left | \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \left ( \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \right ) \right | \leqslant 1 \, \text {since} \, \bar { \theta } _ { i } \in [ 0 , \pi / 2 ] \, \forall \, i \, \geqslant 1 . \\ \intertext { i f } \intertext { a f }
$$

But by ( 30 ),

Hence if β < 1 / 4 ,

$$
r \leqslant \frac { \sqrt { 2 } + 2 d } { 1 - \sqrt { 2 } \beta } \leqslant 2 \sqrt { 2 } + 4 d \leqslant \sqrt { 2 } d + 4 d \leqslant 6 d .
$$

Thus in any case, r glyph[lessorequalslant] 6 d = ⇒ M glyph[lessorequalslant] 6 d .

We now show that it is suﬃcient to only consider the small angle case θ 0 ≈ 0 and the large angle case θ 0 ≈ π .

Suﬃciency: We have three possible cases:

- • sin θ 0   48 πd 4 β : Then we have that θ 0 = O 1 (82 πd 4 β ) or θ 0 = π + O 1 (82 πd 4 β ) .
- • sin θ 0 > 48 πd 4 β and | r − α |   √ βM : Observe that due to equation ( 59 ), we have that | r − α |   βM sin θ 0 . Thus using this inequality in equation ( 58 ), we have that


$$
| \xi | \leqslant \beta M + \frac { \beta M } { \sin \bar { \theta } _ { 0 } } \leqslant \frac { 2 \beta M } { \sin \bar { \theta } _ { 0 } } \leqslant \frac { 2 \beta M } { 4 8 \pi d ^ { 4 } \beta } \leqslant \frac { 1 2 d } { 4 8 \pi d ^ { 4 } } = \frac { 1 } { 4 \pi } d ^ { - 3 }
$$

where we used the assumption sin θ 0 > 48 πd 4 β in the second to last inequality and M   6 d in the last inequality. In addition, ( 36 ) implies

$$
| \pi - 2 \bar { \theta } _ { d } | \geqslant \left | \pi - 2 \cos ^ { - 1 } \left ( \frac { 1 } { \pi } \right ) \right | \geqslant \frac { 1 } { 2 } .
$$

Combining this inequality with ( 31 ) and ( 60 ), we obtain

$$
\frac { 1 } { 2 \pi } \left ( \frac { \pi - \bar { \theta } _ { 0 } } { \pi } \right ) d ^ { - 3 } \leqslant | \xi | \leqslant \frac { 1 } { 4 \pi } d ^ { - 3 } .
$$

From this, we can conclude that θ 0   π 2 . Moreover, since | r − α |   √ βM , then ( 59 ) implies that | sin θ 0 |   √ β so we must have that θ 0 = π + O 1 (2 √ β ) since θ 0   π 2 and β < 1 .

| r -α | glyph[lessorequalslant] √ βM : Then (58) implies

$$
\left | \leqslant \sqrt { \beta } \, \text { so we must have that } \bar { \theta } _ { 0 } = \pi + O _ { 1 } ( 2 \sqrt { \beta } ) \, \sinc \bar { \theta } _ { 0 } \geqslant \frac { \pi } { 2 } \, \text { and } \beta < \\ \beta M \colon \text { Then } ( 5 8 ) \text { implies} \\ | \xi | \leqslant 2 \sqrt { \beta } M . \\ \intertext { a t by ( 3 1 ) , } \xi = \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \left ( \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \right ) \geqslant \frac { ( \pi - 2 \bar { \theta } _ { d } ) ( \pi - \bar { \theta } _ { 0 } ) } { d ^ { 3 } \pi ^ { 2 } } .
$$

But note that by ( 31 ),

[Page 34]

In addition, since | π − 2 θ d |   1 2 by ( 61 ), we have

which implies where we used δ < 1 / 2 and d glyph[greaterorequalslant] 2 in the second to last equality and the definition of δ in the final equality. √

$$
| \xi | \geqslant \frac { | ( \pi - 2 \bar { \theta } _ { d } ) ( \pi - \bar { \theta } _ { 0 } ) | } { d ^ { 3 } \pi ^ { 2 } } \geqslant \frac { | \pi - \bar { \theta } _ { 0 } | } { 2 d ^ { 3 } \pi ^ { 2 } }
$$

$$
| \pi - \overline { \theta } _ { 0 } | \leqslant 4 d ^ { 3 } \pi ^ { 2 } \sqrt { \beta } M \leqslant 2 4 d ^ { 4 } \pi ^ { 2 } \sqrt { \beta } .
$$

Thus θ 0 = π + O 1 (24 d 4 π 2 √ β ) .

Since only one of these situations can hold, it suﬃces to consider either the small angle case θ 0 = O 1 (82 πd 4 β ) or the large angle case θ 0 = π + O 1 (24 d 4 π 2 √ β ) . Now, we show that in the small angle case, x ≈ x ∗ , while in the large angle case, x ≈ − ρ d x ∗ . Small Angle Case: Assume θ 0 = O 1 ( δ ) where we set δ := 82 πd 4 β . Note that since θ i   θ 0   δ for each i , we have that d − 1   π − θ i π     1 − δ π   d = 1 + O 1   2 dδ π  

$$
\begin{array} { r l } & { \tt s a s s u m e b { 0 } { - O } ( \bar { o } ) \text { where we set } \bar { o } . - \bar { 2 } \Omega \bar { a } \bar { p } . \bar { n } \bar { o } } \\ & { \tt a t } \\ & { \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \geqslant \left ( 1 - \frac { \delta } { \pi } \right ) ^ { d } = 1 + O _ { 1 } \left ( \frac { 2 d \delta } { \pi } \right ) } \\ & { H e n c } \\ & { \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \left ( \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \right ) \geqslant \left ( 1 + O _ { 1 } \left ( \frac { 2 \delta } { \pi } \right ) \right ) \left ( 1 + O _ { 1 } \left ( \frac { 2 \bar { \theta } _ { i } } { \pi } \right ) \right ) } \end{array}
$$

provided dδ/π   1 / 2 . Hence

$$
& \text {vied} \, d \theta / \pi \leqslant 1 / 2 . \text { Hence} \\ & \quad \xi = \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \left ( \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \right ) \geqslant \left ( 1 + O _ { 1 } \left ( \frac { 2 \delta } { \pi } \right ) \right ) \left ( 1 + O _ { 1 } \left ( \frac { 2 d \delta } { \pi } \right ) \right ) \\ & \quad \text {were used (33) in the second inequality. In addition, } | \sin \bar { \theta } _ { d } | \leqslant | \bar { \theta } _ { d } | \leqslant \delta \text { and } ( 3 ) \text { imply that } \left | \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { j } } { \pi } \right ) \right | \leqslant \frac { d } { \pi } | \sin \bar { \theta } _ { d } | \leqslant d \delta .
$$

where we used ( 33 ) in the second inequality. In addition, | sin θ d |   | θ d |   δ and ( 32 ) imply that

$$
\text {in the second line equality. In addition, } | \sin \bar { \theta } _ { d } | \leq & \, | \bar { \theta } _ { d } | \leq \delta \, \text {and} \\ \left | \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { j } } { \pi } \right ) \right | \leq & \, \frac { d } { \pi } | \sin \bar { \theta } _ { d } | \leq d \delta . \\ \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { j } } { \pi } \right ) = O _ { 1 } \left ( \frac { 2 \delta } { 3 \pi } \right ) + \left ( 1 + O _ { 1 } \right )
$$

Hence

$$
\alpha & = \frac { 2 \sin \bar { \theta } _ { d } } { \pi } + \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { j } } { \pi } \right ) = O _ { 1 } \left ( \frac { 2 \delta } { 3 \pi } \right ) + \left ( 1 + O _ { 1 } \left ( \frac { 2 \delta } { \pi } \right ) \right ) O _ { 1 } ( d \delta ) \\ & = O _ { 1 } \left ( \frac { ( 4 + 3 d \pi + 6 d ^ { 2 } ) \delta } { 3 \pi } \right )
$$

where we used δ < 1 in the last equality. Thus since | − ξ + cos θ 0 ( r − α ) |   βM and M   6 d , we attain

$$
- \left ( 1 + O _ { 1 } \left ( \frac { 2 \delta } { \pi } \right ) \right ) \left ( 1 + O _ { 1 } \left ( \frac { 2 d \delta } { \pi } \right ) \right ) + ( 1 + O _ { 1 } ( \delta ) ) \left ( r + O _ { 1 } \left ( \frac { ( 4 + 3 d \pi + 6 d ^ { 2 } ) \delta } { 3 \pi } \right ) \right ) = O _ { 1 } ( 6 d \beta ) .
$$

Rearranging, this gives

$$
R \arg r a n g { , \, \text {this gives} } \\ r - 1 = O _ { 1 } \left ( \frac { 2 d \delta } { \pi } + \frac { 2 \delta } { \pi } + \frac { 1 6 d \delta ^ { 2 } } { \pi } + ( \delta + 1 ) \frac { ( 4 + 3 d \pi + 6 d ^ { 2 } ) \delta } { 3 \pi } \right ) + O _ { 1 } ( 1 2 d \beta ) + O _ { 1 } ( 6 d \beta ) \\ = O _ { 1 } \left ( ( 1 2 d + 1 2 + 4 8 d ) \delta + ( 2 \varepsilon + 1 ) ( 4 + 3 \pi d + 1 2 d ) \delta + 1 8 d \beta \right ) \\ = O _ { 1 } ( 1 0 d \delta + 1 8 d \beta ) \\ = O _ { 1 } ( 8 3 \pi d ^ { 5 } \beta )
$$

[Page 35]

equality. Large Angle Case: Assume θ 0 = π + O 1 ( δ ) where δ := 24 d 4 π 2 √ β. We ﬁrst prove that α is close to ρ d . Recall that θ d = ˘ θ d + O 1 ( dδ ) . Then by the mean value theorem: | sin θ d − sin ˘ θ d |   | θ d − ˘ θ d |   dδ so sin θ d = sin ˘ θ d + O 1 ( dδ ) . Let Γ d :=   d − 1 i =0 sin ˘ θ i π     d − 1 j = i +1 π − ˘ θ j π   and note that ρ d = 2 sin ˘ θ d π +

so sin θ d = sin ˘ θ d + O 1 ( dδ ) . Let Γ d := ∑ d -1 i =0 sin ˘ θ i π ( d -1 j = i +1 π -˘ θ j π ) and note that ρ d = 2 sin ˘ θ d π + ( π -2 ˘ θ d π ) Γ d . In [33], it was shown that if d 2 δ/π glyph[lessorequalslant] 1 , then | Γ d | glyph[lessorequalslant] d and ❨

$$
\sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { j } } { \pi } \right ) = \Gamma _ { d } + O _ { 1 ( 3 d ^ { 3 } \delta ) } . \\ \\ \delta / \pi \leqslant 1 , \, w e r e q u i r e \, \sqrt { \beta } \leqslant \frac { 1 } { 2 4 \pi ^ { 6 } } . \, T h u s \, f o r \, s u f f i c t i l y \, s m a l s a l \\ \bar { \theta } _ { d } + \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { j } } { \pi } \right )
$$

By the condition, d 2 δ/π   1 , we require √ β   1 24 πd 6 . Thus for suﬃciently small β , we have

$$
By the condition, d ^ { 2 } \delta / \pi & \leq 1 , \, \text {we require } \sqrt { \beta } \leq \frac { 1 } { 2 4 \pi ^ { \delta } } . \text { Thus for sufficiently small $\beta$, we have} \\ & \alpha = \frac { 2 \sin \bar { \theta } _ { d } } { \pi } + \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { j } } { \pi } \right ) \\ & = \frac { 2 \sin \check { \theta } _ { d } } { \pi } + O _ { 1 } \left ( \frac { 2 d \delta } { \pi } \right ) + \left ( \frac { \pi - 2 \check { \theta } _ { d } } { \pi } + O _ { 1 } \left ( \frac { 2 d \delta } { \pi } \right ) \right ) \left ( \Gamma _ { d } + O _ { 1 } ( 3 d ^ { 3 } \delta ) \right ) \\ & = \rho _ { d } + O _ { 1 } \left ( \frac { 2 d \delta } { \pi } \right ) + \Gamma _ { d } O _ { 1 } \left ( \frac { 2 d \delta } { \pi } \right ) + \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) O _ { 1 } \left ( 3 d ^ { 3 } \delta \right ) + O _ { 1 } \left ( \frac { 6 d ^ { 4 } \delta ^ { 2 } } { \pi } \right ) \\ & = \rho _ { d } + O _ { 1 } \left ( \frac { 2 d \delta } { \pi } \right ) + O _ { 1 } \left ( \frac { 2 d ^ { 2 } \delta } { \pi } \right ) + O _ { 1 } \left ( 3 d ^ { 3 } \delta \right ) + O _ { 1 } \left ( \frac { 6 d ^ { 4 } \delta ^ { 2 } } { \pi } \right ) \\ & = \rho _ { d } + O _ { 1 } ( 7 d ^ { 4 } \delta ) . \\ \text {We now prove } r \text { is close to } \rho _ { d } . \text { Since } x \in \mathcal { S } _ { \beta } ,
$$

We now prove r is close to ρ d . Since x ∈ S β ,

$$
| - \beta + \cos \bar { \theta } _ { 0 } ( r - \alpha ) | \leqslant \beta M .
$$

Also note that | β |   δ/π by ( 34 ). Since cos θ 0 = 1 + O 1 ( θ 2 0 / 2) , we have that

$$
O _ { 1 } ( \delta / \pi ) + ( 1 + O _ { 1 } ( \delta ^ { 2 } / 2 ) ) ( r - \rho _ { d } + O _ { 1 } ( 7 d ^ { 4 } \delta ) ) = O _ { 1 } ( \beta M ) .
$$

Using r glyph[lessorequalslant] 6 d , ρ d glyph[lessorequalslant] 2 d , and δ = 24 d 4 π 2 √ β glyph[lessorequalslant] 1 , we get

$$
Using r \leqslant 6 d , \, \rho _ { d } \leqslant 2 d , \, and \, \delta = 2 4 d ^ { 4 } \pi ^ { 2 } \sqrt { \beta } \leqslant 1 , \, w e g t \\ r - \rho _ { d } + O _ { 1 } \left ( \frac { \delta ^ { 2 } } { 2 } \right ) ( r - \rho _ { d } ) + O _ { 1 } ( 7 d ^ { 4 } \delta ) + O _ { 1 } \left ( \frac { 7 d ^ { 4 } \delta ^ { 3 } } { 2 } \right ) = O _ { 1 } ( \beta M ) + O _ { 1 } \left ( \frac { \delta } { \pi } \right ) \\ \Longrightarrow r - \rho _ { d } = O _ { 1 } \left ( 4 d \delta ^ { 2 } + 7 d ^ { 4 } \delta + \frac { 7 d ^ { 4 } \delta ^ { 3 } } { 2 } + 6 d \beta + \frac { \delta } { \pi } \right ) \\ = O _ { 1 } \left ( 6 d \beta + \delta \left ( 4 d + 7 d ^ { 4 } + \frac { 7 d ^ { 4 } } { 2 } + \frac { 1 } { \pi } \right ) \right ) \\ = O _ { 1 } \left ( \left ( 6 d + 2 4 d ^ { 4 } \pi ^ { 2 } \left ( 4 d + \frac { 2 1 d ^ { 4 } } { 2 } + \frac { 1 } { \pi } \right ) \right ) \sqrt { \beta } \right ) \\ = O _ { 1 } ( 3 5 1 7 d ^ { 8 } \sqrt { \beta } ) .
$$

[Page 36]

Finally, to complete the proof we use the inequality

$$
\| x - x _ { * } \| \leqslant | \| x \| - \| x _ { * } \| + ( \| x _ { * } \| + | \| x \| - \| x _ { * } \| | ) \, \overline { \theta } _ { 0 } .
$$

This inequality states that if a two dimensional point is known to be within ∆ r of magnitude r and an angle ∆ θ away from 0 , then it is at most a Euclidean distance of ∆ r + ( r + ∆ r )∆ θ away from the point ( r, 0) in polar coordinates. Thus for θ 0 = O 1 (82 πd 4 β ) , we have r = 1 + O 1 (838 πd 5 β ) so

$$
\| x - x _ { * } \| \leqslant 8 3 8 \pi d ^ { 5 } \beta + ( 1 + 8 3 8 \pi d ^ { 5 } \beta ) 8 2 \pi d ^ { 4 } \beta \leqslant 7 0 0 0 0 \pi ^ { 2 } d ^ { 9 } \beta .
$$

Then if θ 0 = π + O 1 (24 d 4 π 2 √ β ) , r = ρ d + O 1 (3517 d 8 √ β ) so that

$$
\| x + \rho _ { d } x _ { * } \| \leqslant 3 5 1 7 d ^ { 8 } \sqrt { \beta } + ( \rho _ { d } + 3 5 1 7 d ^ { 8 } \sqrt { \beta } ) 2 4 d ^ { 4 } \pi ^ { 2 } \sqrt { \beta } \leqslant 7 7 4 2 2 \pi ^ { 2 } d ^ { 1 2 } \sqrt { \beta } .
$$

Hence we attain

S β ⊂ B ( x ∗ , 70000 π 2 d 9 β ) ∪ B ( − ρ d x ∗ , 77422 π 2 d 12   β ) . The result that ρ d → 1 as d → ∞ follows from the following facts: by ( 37 ), we have that ˘ θ d → 0 as d → ∞ which implies 2 sin ˘ θ d π → 0 as d → ∞ . Moreover, in [ 33 ], it was shown that d − 1   sin ˘ θ i π   d − 1   π − ˘ θ j π   → 1 as d → ∞ .

$$
\text {pplies} \ \frac { \frac { d - 1 } { \pi } } { \pi } & \rightarrow 0 \text { as } d \rightarrow \infty . \ \text {Moreover, in [33] , it was shock} \\ \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \check { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \check { \theta } _ { j } } { \pi } \right ) & \rightarrow 1 \text { as } d \rightarrow \infty . \\ \\ \\ ( \frac { \pi - 2 \check { \theta } _ { d } } { \pi } ) & \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \check { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \check { \theta } _ { j } } { \pi } \right ) \rightarrow 1 \text { as } d \rightarrow \infty
$$

Hence

$$
\left ( \frac { \pi - 2 \check { \theta } _ { d } } { \pi } \right ) \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \check { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \check { \theta } _ { j } } { \pi } \right ) \to 1 \, \text {as} \, d \to \infty
$$

so ρ d → 1 as d → ∞ .

We now aim to show that the objective function value for points near the minimizer are lower than near the negative multiple which is formally stated in Lemma 9 . We ﬁrst deﬁne

$$
f _ { 0 } ( x ) \colon = \frac { 1 } { 2 } \left \| | A G ( x ) | - | A G ( x _ { * } ) | \right \| ^ { 2 }
$$

f 0 ( x ) := 2   | AG ( x ) | − | AG ( x ∗ ) |   which is the objective function without noise and f η ( x ) = f 0 ( x ) −  | AG ( x ) | − | AG ( x ∗ ) | ,η   . Then note that f ( x ) = f η ( x ) + 1 2   η   2 . We will ﬁrst show that the objective function without noise can be closely approximated by a particular function F which is deﬁned by F ( x ) := 1 2 d +1 (   x   2 +   x ∗   2 ) − 1 2 d   π − 2 θ d π     d − 1   π − θ j π     x,x ∗  

$$
\text {which is the objective function without noise and } f _ { \eta } ( x ) & = f _ { 0 } ( x ) - \langle | A G ( x ) | - | A G ( x _ { * } ) | , \eta \rangle . \text {Then} \\ \text {note that } f ( x ) & = f _ { \eta } ( x ) + \frac { 1 } { 2 ^ { | \eta | } } \| ^ { 2 } . \text { We will first show that the objective function without noise can } \\ \text {be closely approximated by a particular function } \mathcal { F } \text { which is defined by } \\ \\ \mathcal { F } ( x ) & \coloneqq \frac { 1 } { 2 ^ { d + 1 } } ( \| x \| ^ { 2 } + \| x _ { * } \| ^ { 2 } ) - \frac { 1 } { 2 ^ { d } } \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \left ( \prod _ { j = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { j } } { \pi } \right ) \langle x , x _ { * } \rangle \\ \\ & - \frac { 1 } { 2 ^ { d } } \left ( \frac { 2 \sin \bar { \theta } _ { d } } { \pi } + \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \right ) \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \right ) \right ) \| x \| \| x _ { * } \| \\ \intertext { w h o r $ \bar { \theta } _ { e } \coloneqq ( x , y ) $ and } \bar { \theta } _ { e } & = \sigma ( \bar { \theta } _ { e } ) + \text {for } i \in [ d ] \text { . This result is formalized in the following lemma}
$$

where θ 0 := ∠ ( x,y ) and θ i := g ( θ i − 1 ) for i ∈ [ d ] . This result is formalized in the following lemma:

[Page 37]

Lemma 20. Fix 0 < ε < 1 / (16 πd 2 ) 2 . Suppose that A ∈ R m × n d satisﬁes the RRCP with respect to G with constant ε and G is such that each W i ∈ R n i × n i − 1 satisﬁes the WDC with constant ε for i ∈ [ d ] . Then we have that for all non-zero x,x ∗ ∈ R k :

$$
i \in [ d ] . \text { Then we have that for all non-zero } x , x _ { * } \in \mathbb { R } ^ { k } \colon \\ | f _ { 0 } ( x ) - \mathcal { F } ( x ) | & \leqslant \frac { ( L + 1 2 ) d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \| x \| ^ { 2 } + \frac { ( L + 1 2 ) d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \| x _ { * } \| ^ { 2 } + \frac { 2 L \varepsilon } { 2 ^ { d } } \| x \| _ { x _ { * } } \| x \| ^ { 2 } \\ & + \frac { 1 } { 2 ^ { d } } \left [ 2 4 d ^ { 3 } + 8 d \left ( 1 + \frac { d } { \pi } \right ) + \frac { 4 8 d + 4 8 d ^ { 3 } } { \pi } \right ] \sqrt { \varepsilon } \| x \| _ { x _ { * } } \| x \| . \\ \intertext { P r o f o f L e m n a $ 2 0 . $ Fix x , x _ { * } \in \mathbb { R } ^ { k } \ \{ 0 \} . $ For notational simplicity, define } \\ \xi _ { x , x _ { * } } \colon = \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \left ( \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \right ) \langle x , x _ { * } \rangle + \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \right ) + \frac { 2 \sin \bar { \theta } _ { d } } { \pi } \right ) \| x _ { * } \| \| x \| .
$$

$$
\xi _ { x , x _ { * } } \colon = \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \left ( \prod _ { i = 0 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \right ) \langle x , x _ { * } \rangle + \left ( \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \bar { \theta } _ { i } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \theta } _ { i } } { \pi } \right ) + \frac { 2 \sin \bar { \theta } _ { d } } { \pi } \right ) \| x _ { * } \| \| x \| .
$$

Then observe that F can be written more compactly as F ( x ) = 1 2 d +1 (   x   2 +   x ∗   2 ) − 1 2 d ξ x,x ∗ . Then the following bound shows we need to approximate three particular terms:

$$
| f _ { 0 } ( x ) - \mathcal { F } ( x ) | & \leqslant \frac { 1 } { 2 } \left | \| A _ { x _ { d } } x _ { d } \| ^ { 2 } - \frac { 1 } { 2 ^ { d } } \| x \| ^ { 2 } \right | + \frac { 1 } { 2 } \left | \| A _ { x _ { * } , d } x _ { * } , d \| ^ { 2 } - \frac { 1 } { 2 ^ { d } } \| x _ { * } \| ^ { 2 } \right | \\ & + \left | \langle A _ { x _ { d } } x _ { d } , A _ { x _ { * } , d } x _ { * } , d \rangle - \frac { 1 } { 2 ^ { d } } \xi _ { x , x _ { * } } \right | .
$$

    Bounds on the ﬁrst two terms follow directly by the RRCP and WDC in the following way. Note that

$$
\left | \| A _ { x _ { d } } x _ { d } \| ^ { 2 } - \frac { 1 } { 2 ^ { d } } \| x \| ^ { 2 } \right | \leqslant \left | \| A _ { x _ { d } } x _ { d } \| ^ { 2 } - \| x _ { d } \| ^ { 2 } \right | + \left | \| x _ { d } \| ^ { 2 } - \frac { 1 } { 2 ^ { d } } \| x \| ^ { 2 } \right | .
$$

    Since A satisﬁes the RRCP with respect to G , we have that

$$
\left | \| A _ { x _ { d } } x _ { d } \| ^ { 2 } - \| x _ { d } \| ^ { 2 } \right | \leqslant L \varepsilon \| \Lambda _ { x } \| ^ { 2 } \| x \| ^ { 2 } \leqslant L \varepsilon \left ( \frac { 1 } { 2 } + \varepsilon \right ) ^ { d } \| x \| ^ { 2 }
$$

where the last inequality follows by the WDC. Then by ( 50 ), we have

$$
\left | \| x _ { d } \| ^ { 2 } - \frac { 1 } { 2 ^ { d } } \| x \| ^ { 2 } \right | \leqslant 2 4 \frac { d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \| x \| ^ { 2 }
$$

  Using these two bounds, we have

$$
\text {these two bounds, we have} \\ & \left | \| A _ { x _ { d } } x _ { d } \| ^ { 2 } - \frac { 1 } { 2 ^ { d } } \| x \| ^ { 2 } \right | \leqslant L \varepsilon \left ( \frac { 1 } { 2 } + \varepsilon \right ) ^ { d } \| x \| ^ { 2 } + 2 4 \frac { d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \| x \| ^ { 2 } \\ & \leqslant \frac { ( 2 L + 2 4 ) d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \| x \| ^ { 2 }
$$

since (1 + 2 ε ) d glyph[lessorequalslant] e 2 εd glyph[lessorequalslant] 1 + 4 εd glyph[lessorequalslant] 2 for ε glyph[lessorequalslant] 1 / (4 d ) . By the same logic, we have that

$$
\left | \| A _ { x _ { * } , d ^ { x _ { * } , d } } \| ^ { 2 } - \frac { 1 } { 2 ^ { d } } \| x _ { * } \| ^ { 2 } \right | \leqslant \frac { ( 2 L + 2 4 ) d ^ { 3 } \sqrt { \varepsilon } } { 2 ^ { d } } \| x _ { * } \| ^ { 2 } .
$$

  For the last term, note that

$$
\left | \langle A _ { x _ { d } } x _ { d } , A _ { x _ { * , d } } x _ { * , d } \rangle - \frac { 1 } { 2 ^ { d } } \xi _ { x , x _ { * } } \right | \leqslant \langle A _ { x _ { d } } x _ { d } , A _ { x _ { * , d } } x _ { * , d } \rangle - \langle \Phi _ { x _ { d } , x _ { * } , x _ { d } } x _ { d } , x _ { * , d } \rangle + \left | \langle \Phi _ { x _ { d } , x _ { * } , x _ { d } } x _ { d } , x _ { * , d } \rangle - \frac { 1 } { 2 ^ { d } } \xi _ { x , x _ { * } } \right | .
$$

[Page 38]

For the ﬁrst term, the RRCP and WDC imply

$$
| \langle A _ { x _ { d } } x _ { d } , A _ { x _ { * , d } } x _ { * , d } \rangle - \langle \Phi _ { x _ { d } , x _ { * , d } } x _ { d } , x _ { * , d } \rangle | & \leqslant L \varepsilon \left ( \frac { 1 } { 2 } + \varepsilon \right ) ^ { d } \| x \| \| x _ { * } \| \leqslant \frac { 2 L \varepsilon } { 2 ^ { d } } \| x \| \| x _ { * } \|
$$

for ε   1 / (4 d ) . For the second term, by the deﬁnition of Φ z,w and M ˆ z ↔ ˆ w , we have

$$
\left | \langle \Phi _ { x _ { d } , x _ { * } , d } x _ { d } , x _ { * } \rangle - \frac { 1 } { 2 ^ { d } } \xi _ { x , x _ { * } } \right | & \leqslant \| x \| \underbrace { \left \| \frac { \pi - 2 \theta _ { d } } { \pi } \Lambda _ { x } ^ { T } \Lambda _ { x } x _ { * } - \frac { \pi - 2 \bar { \theta } _ { d } } { \pi } \tilde { h } _ { x , x _ { * } } } _ { ( I ) } \right \| } \\ & + \| x \| \underbrace { \left \| \frac { 2 \sin \theta _ { d } } { \pi } \frac { \| x _ { * } \| } { \| x _ { d } \| } \Lambda _ { x } ^ { T } \Lambda _ { x } x - \frac { 2 \sin \bar { \theta } _ { d } } { \pi } \frac { \| x _ { * } \| } { \| x \| } \frac { 1 } { 2 ^ { d } } x } _ { ( I I ) } \right \| } \\
$$

where ˜ h x,x ∗ is deﬁned in ( 54 ). It was shown in the proof of Lemma 19 that

$$
( I ) \leqslant \frac { 1 } { 2 ^ { d } } \left ( 2 4 d ^ { 3 } + \frac { 8 d } { \pi } \left ( 1 + \frac { d } { \pi } \right ) \right ) \sqrt { \varepsilon } \| x _ { * } \|
$$

and Fix x ∈ B ( φ d x ∗ , r ‖ x ∗ ‖ ) . Then observe that we have θ 0 glyph[lessorequalslant] πr 2 φ d and ( φ d -r ) ‖ x ∗ ‖ glyph[lessorequalslant] ‖ x ‖ glyph[lessorequalslant] ( φ d + r ) ‖ x ∗ ‖ . Furthermore, cos θ 0 glyph[greaterorequalslant] 1 -θ 2 0 2 . Thus, we have the following bounds: ❨

$$
( I I ) \leqslant \frac { 1 } { 2 ^ { d } } \left ( \frac { 2 4 d ( 1 + 2 \varepsilon ) ^ { d } + 4 8 d ^ { 3 } } { \pi } \right ) \sqrt { \varepsilon } \| x _ { * } \| .
$$

Combining the results for ( 65 ), (I), and (II) we have

$$
\left | \langle A _ { x _ { d } } x _ { d } , A _ { x _ { * } , d } x _ { * } \rangle - \frac { 1 } { 2 ^ { d } } \xi _ { x , x _ { * } } \right | & \leqslant \frac { 2 L \varepsilon } { 2 ^ { d } } \| x \| \| x _ { * } \| + \frac { 1 } { 2 ^ { d } } \left ( 2 4 d ^ { 3 } + \frac { 8 d } { \pi } \left ( 1 + \frac { d } { \pi } \right ) \right ) \sqrt { \varepsilon } \| x _ { * } \| \\ & + \frac { 1 } { 2 ^ { d } } \left ( \frac { 2 4 d ( 1 + 2 \varepsilon ) ^ { d } + 4 8 d ^ { 3 } } { \pi } \right ) \sqrt { \varepsilon } \| x _ { * } \| .
$$

Combining equations ( 63 ), ( 64 ), and ( 66 ) achieves the desired result.

Now that we have established that the objective function without noise can be approximated by F , we now show that F satsiﬁes particular quadratic upper and lower bounds to establish the desired properties of the true objective function f :

Lemma 21. Fix 0 < r < 1 4 d 2 π and let κ := min d   2 ρ d > 0 . Then for any φ d ∈ [ ρ d , 1] , we have that

$$
\text {Lemma 21.} \ \ F i x \, 0 < r \, < \frac { 1 } { 4 \pi ^ { 2 } } \ \text { and let } \kappa \, \colon = \min _ { d \geq 2 } & \rho _ { d } > 0 . \ \text { Then for any } \phi _ { d } \in [ \rho _ { d } , 1 ] , \ \text { we have that} \\ \mathcal { F } ( x ) \leq \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } \left ( \phi _ { d } ^ { 2 } - 2 \phi _ { d } + \frac { 4 5 d } { \kappa ^ { 3 } } r \right ) + \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } \ \forall \ x \in B ( \phi _ { d } x _ { * } , r \| x _ { * } \| ) , \\ \mathcal { F } ( y ) \geq \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } ( \phi _ { d } ^ { 2 } - 2 \rho _ { d } \phi _ { d } - 1 3 9 d ^ { 4 } r ) + \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } \ \forall \ y \in B ( - \phi _ { d } x _ { * } , r \| x _ { * } \| ) . \\ \intertext { P o f o f $ \text {Lemma 21.} \ \text { Define } \psi _ { d } \colon = \frac { \pi - 2 \bar { \sigma } _ { d } } { \pi } , \zeta _ { i + 1 } \colon = \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \bar { \sigma } _ { j } } { \pi } , \text { and } \alpha _ { i } \colon = \frac { \sin \bar { \sigma } _ { i } } { \pi } . \ \text { Then note that we } \\
$$

Proof of Lemma 21 . Deﬁne ψ d := π − 2 θ d π ,ζ i +1 :=   d − 1 j = i +1 π − θ j π , and α i := sin θ i π . Then note that we can write F as

$$
\mathcal { F } ( x ) \coloneqq \frac { 1 } { 2 ^ { d + 1 } } ( \| x \| ^ { 2 } + \| x _ { * } \| ^ { 2 } ) - \frac { 1 } { 2 ^ { d } } \left ( \psi _ { d } \zeta _ { 0 } \langle x , x _ { * } \rangle + \left ( 2 \alpha _ { d } + \psi _ { d } \sum _ { i = 0 } ^ { d - 1 } \alpha _ { i } \zeta _ { i + 1 } \right ) \| x \| \| x _ { * } \| \right ) .
$$

[Page 39]

$$
\psi _ { d } \geqslant 1 - \frac { r } { \phi _ { d } } , \, \zeta _ { 0 } \geqslant \prod _ { i = 0 } ^ { d - 1 } \left ( 1 - \frac { r } { 2 \phi _ { d } } \right ) , \, \text { and } \cos \theta _ { 0 } \geqslant 1 - \frac { \pi ^ { 2 } r ^ { 2 } } { 8 \phi _ { d } } .
$$

Hence we see that

$$
\mathcal { F } ( x ) - \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } & \leqslant \frac { \| x \| ^ { 2 } } { 2 ^ { d + 1 } } - \frac { 1 } { 2 ^ { d } } \psi _ { d } \zeta _ { 0 } \cos \theta _ { 0 } \| x \| \| x \| ^ { * } \\ & \leqslant \frac { 1 } { 2 ^ { d + 1 } } ( \phi _ { d } + r ) ^ { 2 } \| x _ { * } \| ^ { 2 } - \frac { \gamma } { 2 ^ { d } } \psi _ { 0 } \zeta _ { 0 } \cos \theta _ { 0 } ( \phi _ { d } - r ) \| x _ { * } \| ^ { 2 } \\ & \leqslant \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } \left ( \phi _ { d } ^ { 2 } + 2 r \phi _ { d } + r ^ { 2 } - 2 \left ( 1 - \frac { r } { \phi _ { d } } \right ) \left ( 1 - \frac { d r } { \phi _ { d } } \right ) ( \phi _ { d } - r ) \left ( 1 - \frac { \pi ^ { 2 } r ^ { 2 } } { 8 \phi _ { d } ^ { 2 } } \right ) \right ) .
$$

where in the ﬁrst inequality we used 2 α d + ψ d   d − 1 i =0 α i ζ i +1   0 . Noting that φ d ∈ [ ρ d , 1] and r < 1 , with some algebra we attain

$$
\phi _ { d } ^ { 2 } + 2 r \phi _ { d } + r ^ { 2 } - 2 \left ( 1 - \frac { r } { \phi _ { d } } \right ) \left ( 1 - \frac { d r } { \phi _ { d } } \right ) ( \phi _ { d } - r ) \left ( 1 - \frac { \pi ^ { 2 } r ^ { 2 } } { 8 \phi _ { d } ^ { 2 } } \right ) \\ \leqslant \phi _ { d } ^ { 2 } - 2 \phi _ { d } + \frac { ( 8 d + ( 2 d + 1 ) \pi ^ { 2 } + 7 ) r } { \phi _ { d } ^ { 3 } } \\ \leqslant \phi _ { d } ^ { 2 } - 2 \phi _ { d } + \frac { 4 5 d } { \kappa ^ { 3 } } r \\ \text {may conclude that for } x \in \mathcal { B } ( \phi _ { x } \gamma _ { x } \ r | | x \ | )
$$

so we may conclude that for x ∈ B ( φ d x ∗ ,r   x ∗   ) ,

$$
\mathcal { F } ( x ) - \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } \leqslant \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } \left ( \phi _ { d } ^ { 2 } - 2 \phi _ { d } + \frac { 4 5 d } { \kappa ^ { 3 } } r \right ) .
$$

Fix x ∈ B ( − φ d x ∗ ,r   x ∗   ) . Then note that we have π − θ 0   π 2 2 r and ( φ d − r )   x ∗       x     ( φ d + r )   x ∗   . Furthermore, for suﬃciently small r > 0 , we have that   x,x ∗     0 so that − ψ d ζ 0   x,x ∗     0 (note that θ d   π/ 2 ). Thus

$$
\mathcal { F } ( x ) - \frac { 1 } { 2 ^ { d + 1 } } \| x _ { * } \| ^ { 2 } & = \frac { 1 } { 2 ^ { d + 1 } } \| x \| ^ { 2 } - \frac { 1 } { 2 ^ { d } } \left ( \psi _ { d } \Omega _ { 0 } ( x , x _ { * } ) + ( \psi _ { d } \sum _ { i = 0 } ^ { d - 1 } \alpha _ { i } \zeta _ { i + 1 } + 2 \alpha _ { d } ) \| x \| \| x _ { * } \| \right ) \\ & \geq \frac { 1 } { 2 ^ { d + 1 } } ( \phi _ { d } - r ) ^ { 2 } \| x _ { * } \| ^ { 2 } - \frac { 1 } { 2 ^ { d } } ( \psi _ { d } \sum _ { i = 0 } ^ { d - 1 } \alpha _ { i } \zeta _ { i + 1 } + 2 \alpha _ { d } ) ( \phi _ { d } + r ) \| x _ { * } \| .
$$

Note that we have θ 0 = π + O 1 ( rπ 2 / 2) . As shown in Proposition 2 , if d 2 ( rπ 2 / 2) /π   1 , then we have that d 1

$$
\psi _ { d } \sum _ { i = 0 } ^ { d - 1 } \alpha _ { i } \zeta _ { i + 1 } + 2 \alpha _ { d } = \rho _ { d } + O _ { 1 } ( 7 d ^ { 4 } r \pi ^ { 2 } / 2 ) .
$$

[Page 40]

Hence we have

$$
\text {Hence we have} \\ \mathcal { F } ( x ) - \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } & \geq \frac { 1 } { 2 ^ { d + 1 } } ( \phi _ { d } - r ) ^ { 2 } \| x _ { * } \| ^ { 2 } - \frac { 1 } { 2 ^ { d } } ( \rho _ { d } + 7 \pi ^ { 2 } d ^ { 4 } r / 2 ) ( \phi _ { d } + r ) \| x _ { * } \| ^ { 2 } \\ & = \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } ( \phi _ { d } ^ { 2 } - 2 r \phi _ { d } + r ^ { 2 } - 2 ( \rho _ { d } \phi _ { d } + r \rho _ { d } + 7 \pi ^ { 2 } d ^ { 4 } r \phi _ { d } / 2 + 7 \pi ^ { 2 } d ^ { 4 } r ^ { 2 } / 2 ) ) \| x _ { * } \| ^ { 2 } \\ & \geq \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } ( \phi _ { d } ^ { 2 } - 2 r - 2 \rho _ { d } \phi _ { d } - 2 r - 7 \pi ^ { 2 } d ^ { 4 } r - 7 \pi ^ { 2 } d ^ { 4 } r ^ { 2 } ) \\ & \geq \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } ( \phi _ { d } ^ { 2 } - 2 \rho _ { d } \phi _ { d } - 1 3 9 d ^ { 4 } r ) \\ \text {where we used the fact that } \phi _ { d } \in [ \rho _ { d } , 1 ] \text { and } 0 < r < 1 . \text {This completes the proof.} \quad \square
$$

where we used the fact that φ d ∈ [ ρ d , 1] and 0 < r < 1 . This completes the proof.

With this result, we are equipped to prove Lemma 9 .

Proof of Lemma 9 . By the same argument for ( 49 ), we have that |  A x d Λ x x,η  |   2 2 d/ 2   x    η   for any x ∈ R k . Thus for x ∈ B ( φ d x ∗ ,ϕ   x ∗   ) ,

$$
| \langle | A G ( x ) | - | A G ( x _ { * } ) | , \eta \rangle | & \leqslant | \langle A _ { x _ { d } } \Lambda _ { x } x , \eta \rangle | + | \langle A _ { x _ { , d } } \Lambda _ { x } x _ { * } , \eta \rangle | \\ & \leqslant ( \| x \| + \| x _ { * } \| ) \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| \\ & \leqslant ( \varphi \| x _ { * } \| + 2 \| x _ { * } \| ) \frac { 2 } { 2 ^ { d / 2 } } \| \eta \|
$$

where we used the fact that   x     ( φ d + ϕ )   x ∗     (1 + ϕ )   x ∗   in the last inequality. 3

Let κ := min d   2 ρ d . If x ∈ B ( φ d x ∗ ,ϕ   x ∗   ) and K d := 24 d 3 +8 d   1 + d π   + 48 d +48 d π , then Lemma 20 and Lemma 21 give

$$
2 0 \, \text {and} \, \text {Lemma} \, 2 1 \, \text {give} \\ f _ { \eta } ( x ) & \leq \mathcal { F } ( x ) + | f _ { 0 } ( x ) - \mathcal { F } ( x ) | + | ( | A G ( x ) | - | A G ( x ) | ) , \eta | \\ & \leq \frac { | x _ { * } | ^ { 2 } } { 2 d + 1 } \left ( \phi _ { d } ^ { 2 } - 2 \phi _ { d } + \frac { 4 5 d } { \kappa ^ { 3 } } \varphi \right ) + \frac { | x _ { * } | ^ { 2 } } { 2 d + 1 } + \frac { ( L + 1 2 ) d ^ { 3 } \sqrt { \varepsilon } } { 2 d } \| x \| ^ { 2 } \\ & + \frac { ( L + 1 2 ) d ^ { 3 } \sqrt { \varepsilon } } { 2 d } \| x _ { * } \| ^ { 2 } + \frac { 2 L \varepsilon } { 2 d } \| x \| \| x _ { * } \| + \frac { 1 } { 2 d } K _ { d } \sqrt { \varepsilon } \| x \| \| x _ { * } \| + ( \varphi \| x _ { * } \| + 2 \| x _ { * } \| ) ^ { \frac { 2 } { 2 d / 2 } } \| \eta \| \\ & \leq \| x _ { * } \| ^ { 2 } ( \phi _ { d } ^ { 2 } - 2 \phi _ { d } + \frac { 4 5 d } { \kappa ^ { 3 } } \varphi + 1 + 2 ( L + 1 2 ) d ^ { 3 } \sqrt { \varepsilon } \left ( ( \phi _ { d } + \varphi ) ^ { 2 } + 1 \right ) + 4 L \sqrt { \varepsilon } ( \phi _ { d } + \varphi ) + 2 K _ { d } ( \phi _ { d } + \varphi ) ) \\ & + ( \varphi \| x _ { * } \| + 2 \| x _ { * } \| ) ^ { \frac { 2 } { 2 d / 2 } } \| \eta \| \\ & \leq \| x _ { * } \| ^ { 2 } ( 1 + \phi _ { d } ^ { 2 } - 2 \phi _ { d } + \frac { 4 5 d } { \kappa ^ { 3 } } \sqrt { \varepsilon } + \tilde { K } _ { d } \sqrt { \varepsilon } ) + ( \varphi \| x _ { * } \| + 2 \| x _ { * } \| ) ^ { \frac { 2 } { 2 d / 2 } } \| \eta \| \\ \text {where} \, \tilde { K } _ { d } \equiv 6 ( L + 1 2 ) d ^ { 3 } + 8 L + 4 K _ { d } , \text { in the second inequality we used } \| x \| \leq ( \phi _ { d } + \varphi ) \| x _ { * } \| , \text { and in} \\ \text {the last inequality} \, \text {we used} \, \tilde { \varsigma } \leq \sqrt { \varepsilon } , \, \ < 1 \, \text {and} \, \varphi \leq 1 \\
$$

where ˜ K d := 6( L + 12) d 3 + 8 L + 4 K d , in the second inequality we used   x     ( φ d + ϕ )   x ∗   , and in the last inequality we used ε < √ ε , ρ d   1 and ϕ < 1 .

Similarly, if y ∈ B ( − φ d x ∗ ,ϕ   x ∗   ) , then

$$
\begin{array} { r l } & { \mathsf f _ { \eta } ( y ) \geq \mathcal { F } ( y ) - | f _ { 0 } ( y ) - \mathcal { F } ( y ) | - | \langle A G ( y ) | - | A G ( x _ { * } ) | , \eta | } \\ & { \geq \frac { \| x _ { * } \| ^ { 2 } } { 2 d + 1 } ( \phi _ { d } ^ { 2 } - 2 \rho d _ { d } - 1 3 d ^ { 4 } \varphi ) + \frac { \| x _ { * } \| ^ { 2 } } { 2 d + 1 } - \frac { ( L + 1 ) 2 d ^ { 3 } \sqrt { \varepsilon } } { 2 d } \| x _ { \| } \| ^ { 2 } } \\ & { \quad - \frac { ( L + 1 ) 2 d ^ { 3 } \sqrt { \varepsilon } } { 2 d } \| x _ { * } \| ^ { 2 } - \frac { 2 L \varepsilon } { 2 d } \| x _ { \| } \| x _ { * } \| - \frac { 1 } { 2 d } K _ { d } \sqrt { \varepsilon } \| x _ { \| } \| x _ { * } \| - ( \varphi \| x _ { * } \| + 2 \| x _ { * } \| ) \frac { 2 } { 2 d / 2 } \| \eta \| } \\ & { \geq \frac { \| x _ { * } \| ^ { 2 } } { 2 d + 1 } ( 1 + \phi _ { d } ^ { 2 } - 2 \rho d _ { d } - 1 3 d ^ { 4 } \sqrt { \varepsilon } - \tilde { K } _ { d } \sqrt { \varepsilon } ) - ( \varphi \| x _ { * } \| + 2 \| x _ { * } \| ) \frac { 2 } { 2 d / 2 } \| \eta \| . } \end{array}
$$

[Page 41]

In sum, we have for x ∈ B ( φ d x ∗ ,ϕ   x ∗   ) ,

$$
f _ { \eta } ( x ) \leqslant \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } \left ( 1 + \phi _ { d } ^ { 2 } - 2 \phi _ { d } + \frac { 4 5 d \sqrt { \varepsilon } } { \kappa ^ { 3 } } + \tilde { K } _ { d } \sqrt { \varepsilon } \right ) + ( \varphi \| x _ { * } \| + 2 \| x _ { * } \| ) \frac { 2 } { 2 ^ { d / 2 } } \| \eta \|
$$

while for y ∈ B ( − φ d x ∗ ,ϕ   x ∗   ) ,

$$
f _ { \eta } ( y ) \geqslant \frac { \| x _ { * } \| ^ { 2 } } { 2 ^ { d + 1 } } \left ( 1 + \phi _ { d } ^ { 2 } - 2 \rho _ { d } \phi _ { d } - 1 3 9 d ^ { 4 } \sqrt { \varepsilon } - \tilde { K } _ { d } \sqrt { \varepsilon } \right ) - ( \varphi \| x _ { * } \| + 2 \| x _ { * } \| ) \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| .
$$

Note that we require the lower bound in ( 70 ) to be larger than the upper bound in ( 69 ). Setting ϕ = ε and using both ε < √ ε and   η     c 2   x ∗   2 d/ 2 d 48 , we see that we require

$$
\varepsilon \leqslant \left ( \frac { 2 \phi _ { d } ( 1 - \rho _ { d } ) - 8 c _ { 2 } / d ^ { 4 8 } } { \frac { 4 5 d } { \kappa ^ { 3 } } + 2 \tilde { K } _ { d } + 1 3 9 d ^ { 4 } } \right ) ^ { 2 } .
$$

By Lemma 22 , we have 1 − ρ d   1 / ( C ( d + 2) 2 ) for some numerical constant C and φ d   κ . Hence it suﬃces to have c 2 d 48   κ 8 C ( d +2) 2 and ϕ = ε   r 1 /d 12 for some numerical constants r 1 and c 2 .

$$
& \geqslant 1 / ( C ( a + 2 ) ^ { - } ) \text { for some numerical critical } 2 \\ & \frac { 1 } { C ( d + 2 ) ^ { 2 } } \leqslant \varepsilon \leqslant r _ { 1 } / d ^ { 1 2 } \text { for some number } \\ & \quad s a t i s f i e s \min _ { d \geqslant 2 } \rho _ { d } > 0 \text { and for some } r \\ & \quad \frac { 1 } { C ( d + 2 ) ^ { 2 } } \leqslant 1 - \rho _ { d } \ \forall \ d \geqslant 2 . \\ & = \sum _ { i = 0 } ^ { d - 1 } \frac { \sin \check { \theta _ { i } } } { \pi } \left ( \prod _ { j = i + 1 } ^ { d - 1 } \frac { \pi - \check { \theta _ { j } } } { \pi } \right ) . \text { In Lemm } \\ & \min _ { d \geqslant 2 } \Gamma _ { d } > 0 . \text { By (37) and (38), we have }
$$

Lemma 22. We have that ρ d satisﬁes min d   2 ρ d > 0 and for some numerical constant C ,

Proof of Lemma 22 . Let Γ d :=   d − 1 i =0 sin ˘ θ i π     d − 1 j = i +1 π − ˘ θ j π   . In Lemma A.4 of [ 35 ], it has been established that Γ d ∈ [0 , 1] and min d   2 Γ d > 0 . By ( 37 ) and ( 38 ), we have that ˘ θ d   3 π/ ( d + 3) and ˘ θ d   π/ ( d + 1) for all d   2 . Since sin(2 x ) − 3 x/ 4   0 for all x ∈ [0 ,π/ 3] , observe that

$$
\frac { 2 \sin \check { \theta } _ { d } } { \pi } \geqslant \frac { 2 \sin \left ( \frac { \pi } { d + 1 } \right ) } { \pi } \geqslant \frac { 2 } { \pi } \cdot \frac { 3 } { 4 } \left ( \frac { \pi } { 2 ( d + 1 ) } \right ) = \frac { 3 } { 4 ( d + 1 ) } \ \forall \ d \geqslant 2 .
$$

Thus for any d glyph[greaterorequalslant] 2 ,

$$
\text {for any } d \geqslant 2 , \\ \rho _ { d } = \frac { 2 \sin \check { \theta } _ { d } } { \pi } + \left ( \frac { \pi - 2 \check { \theta } _ { d } } { \pi } \right ) \Gamma _ { d } \geqslant \frac { 3 } { 4 ( d + 1 ) } + \frac { \pi - \frac { 6 \pi } { d + 3 } } { \pi } \Gamma _ { d } \\ \geqslant \frac { 3 } { 4 ( d + 1 ) } \Gamma _ { d } + \frac { \pi - \frac { 6 \pi } { d + 3 } } { \pi } \Gamma _ { d } \\ = \left ( \frac { 3 } { 4 ( d + 1 ) } + \frac { d - 3 } { d + 3 } \right ) \Gamma _ { d } \geqslant \frac { 1 } { 2 0 } \Gamma _ { d } \\
$$

where the second inequality is due to Γ d ∈ [0 , 1] . We conclude that min d   2 ρ d   1 / 20min d   2 Γ d > 0 . We now establish the lower bound on 1 − ρ d for all d   2 . It was shown in Lemma A.4 of [ 35 ] that 1 − Γ d   1 a 7 ( d +2) 2 for some numerical constant a 7 . Observe that

$$
\rho _ { d } = \left ( 1 - \frac { 2 } { \pi } \check { \theta } _ { d } \right ) \Gamma _ { d } + \frac { 2 } { \pi } \sin \check { \theta } _ { d } = \Gamma _ { d } + \frac { 2 } { \pi } \left ( \sin \check { \theta } _ { d } - \check { \theta } _ { d } \Gamma _ { d } \right ) \leqslant \Gamma _ { d } + \frac { 2 } { \pi } \check { \theta } _ { d } ( 1 - \Gamma _ { d } ) .
$$

[Page 42]

Furthermore, note that for all d   2 , ˘ θ d   ˘ θ 2 = g ( g ( π )) = g ( π/ 2) = cos − 1 (1 /π ) . Hence for all d   2 ,

$$
1 - \rho _ { d } & \geqslant 1 - \Gamma _ { d } - \frac { 2 } { \pi } \check { \theta } _ { d } ( 1 - \Gamma _ { d } ) = ( 1 - \Gamma _ { d } ) \left ( 1 - \frac { 2 } { \pi } \check { \theta } _ { d } \right ) \\ & \geqslant \frac { 1 } { a _ { 7 } ( d + 2 ) ^ { 2 } } \left ( 1 - \frac { 2 } { \pi } \cos ^ { - 1 } \left ( \frac { 1 } { \pi } \right ) \right ) \\ & \geqslant \frac { 0 . 2 } { a _ { 7 } ( d + 2 ) ^ { 2 } } .
$$

# 2.5.4 Proofs for Section 2.4.4

Here we prove the convexity-like property of f around the minimizer x ∗ .

Proof of Lemma 11 . Suppose our objective function f is diﬀerentiable at x . Recall that the gradient of f is given by v x = v x − q x where v x = Λ T x A T x d ( A x d Λ x x − A x ∗ ,d Λ x ∗ x ∗ ) and q x = Λ T x A T x d η . We will ﬁrst show that v x satisﬁes

$$
\left \| \overline { v } _ { x } - \Lambda _ { x } ^ { T } ( \Lambda _ { x } x - \Lambda _ { x } x _ { * } ) \right \| \leqslant \frac { 1 } { 1 6 } \frac { 1 } { 2 ^ { d } } \| x - x _ { * } \| .
$$

Note that by the triangle inequality, we have that

$$
\left \| \overline { v } _ { x } - \Lambda _ { x } ^ { T } ( \Lambda _ { x } x - \Lambda _ { x } x _ { * } ) \right \| & \leqslant \underbrace { \left \| \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } ( A _ { x _ { d } } \Lambda _ { x } x - A _ { x _ { d } } \Lambda _ { x _ { * } } x _ { * } ) - \Lambda _ { x } ^ { T } ( \Lambda _ { x } x - \Lambda _ { x _ { * } } x _ { * } ) \right \| } _ { T _ { 1 } } \\ & + \underbrace { \left \| \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } ( A _ { x _ { d } } - A _ { x _ { * } , d } ) \Lambda _ { x _ { * } , x _ { * } } \right \| } _ { T _ { 2 } } .
$$

We will establish control of each of these terms separately.

Controlling T 1 : Since f is diﬀerentiable at x , note that by the local linearity of G we have that for suﬃciently small z ∈ R k , G ( x + z ) − G ( x ) = Λ x z . Hence for all z , the RRCP implies that

$$
| \langle A _ { x _ { d } } \Lambda _ { x } z , A _ { x _ { d } } ( \Lambda _ { x } x - \Lambda _ { x _ { * } } x _ { * } ) \rangle - \langle \Lambda _ { x } z , \Lambda _ { x } x - \Lambda _ { x _ { * } } x _ { * } \rangle | \leqslant L \varepsilon \| \Lambda _ { x } \| \| \Lambda _ { x } x - \Lambda _ { x _ { * } } x _ { * } \| \| z \| .
$$

Since this holds for all z , we have that

$$
\| \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } ( A _ { x _ { d } } \Lambda _ { x } x - A _ { x _ { d } } \Lambda _ { x _ { * } } x _ { * } ) - \Lambda _ { x } ^ { T } ( \Lambda _ { x } x - \Lambda _ { x _ { * } } x _ { * } ) \| \leqslant L _ { \varepsilon } \| \Lambda _ { x } \| \| \Lambda _ { x } x - \Lambda _ { x _ { * } } x _ { * } \| .
$$

d ∗ ∗ ∗ ∗ ∗ ∗ In addition, we have that by Lemma 13 , if ε < 1 / (200 4 d 6 ) and x ∈ B ( x ∗ ,d √ ε   x ∗   ) then

$$
\| \Lambda _ { x } x - \Lambda _ { x _ { * } } x _ { * } \| \leqslant \frac { 1 . 2 } { 2 ^ { d / 2 } } \| x - x _ { * } \| .
$$

Combining ( 72 ), ( 73 ), and ( 20 ) in Lemma 16 we see that

$$
\| \Lambda _ { x } ^ { T } A _ { x _ { d } } ^ { T } ( A _ { x _ { d } } \Lambda _ { x } x - A _ { x _ { d } } \Lambda _ { x _ { * } } x _ { * } ) - \Lambda _ { x } ^ { T } ( \Lambda _ { x } x - \Lambda _ { x _ { * } } x _ { * } ) \| \leqslant \frac { 1 . 2 \sqrt { \frac { 1 3 } { 1 2 } L _ { E } } } { 2 ^ { d } } \| x - x _ { * } \| .
$$

Thus choosing ε so that ε < 1 / (32 · 1 . 2   13 / 12 L ) in ( 74 ) shows that

$$
T _ { 1 } = O _ { 1 } \left ( \frac { 1 } { 3 2 } \right ) \frac { 1 } { 2 ^ { d } } \| x - x _ { * } \| .
$$

[Page 43]

Controlling T 2 : We will ﬁrst show that for suﬃciently small ε ,

$$
\| ( A _ { x _ { d } } - A _ { x _ { * } , d } ) \Lambda _ { x _ { * } } x _ { * } \| ^ { 2 } \leqslant \frac { 1 . 4 4 ( 4 L + \frac { 4 8 d } { \pi } ) \sqrt { \varepsilon } } { 2 ^ { d } } \| x - x _ { * } \| ^ { 2 } .
$$

Letting { a i } m i =1 denote the rows of A , observe that we can write

$$
L e t t i g \left \{ a _ { i } \right \} _ { i = 1 } ^ { m } \det n o t e { the r o w s of A , o b s e r v e } & \, \text {that we can write} \\ \| ( A _ { x _ { d } } - A _ { x _ { * } , d } ) \Lambda _ { x } . x _ { * } \right \| ^ { 2 } & \equiv \| ( A _ { x _ { d } } - A _ { x _ { * } , d } ) x _ { * , d } \right \| ^ { 2 } \\ & = \sum _ { i = 1 } ^ { m } \left ( s g n ( \langle a _ { i } , x _ { d } \rangle ) - s g n ( \langle a _ { i } , x _ { * , d } \rangle ) \right ) ^ { 2 } \langle a _ { i } , x _ { * , d } \rangle ^ { 2 } \\ & \leq \sum _ { i = 1 } ^ { m } \left ( s g n ( \langle a _ { i } , x _ { i } \rangle ) - s g n ( \langle a _ { i } , x _ { * , d } \rangle ) \right ) ^ { 2 } \langle a _ { i } , ( x _ { i } - x _ { * , d } ) \rangle ^ { 2 } \\ & = \sum _ { i = 1 } ^ { m } \left ( 1 \langle ( a _ { i } , x _ { d } ) \neq 0 \rangle + 1 \left ( \langle a _ { i } , x _ { * , d } \rangle \neq 0 \right ) - 2 s g n ( \langle a _ { i } , x _ { d } \rangle \langle a _ { i } , x _ { * , d } \rangle ) \right ) \langle a _ { i } , ( x _ { d } - x _ { * , d } ) \rangle ^ { 2 } \\ & = \| A _ { x _ { d } } ( x _ { d } - x _ { * , d } ) \| ^ { 2 } + \| A _ { x _ { * } , d } ( x _ { d } - x _ { * , d } ) \| ^ { 2 } - 2 \langle x _ { d } - x _ { * , d } , A _ { x _ { d } , x _ { * } , ( x _ { d } - x _ { * , d } ) } \rangle . \\ \text {We first establish concentration of } A _ { x _ { d } } ( x _ { d } - x _ { * , d } ) . \text { Since } A \text { satisfies the } R R C P \text { with respect to } G ,
$$

We ﬁrst establish concentration of A x d ( x d − x ∗ ,d ) . Since A satisﬁes the RRCP with respect to G , we have that T 2

$$
| \langle ( A _ { x _ { d } } ^ { T } A _ { x _ { d } } - I _ { n _ { d } } ) ( x _ { d } - x _ { * , d } ) , x _ { d } - x _ { * , d } \rangle | \leqslant L \varepsilon \| x _ { d } - x _ { * , d } \| ^ { 2 }
$$

which ultimately gives

$$
\| A _ { x _ { d } } ( x _ { d } - x _ { * , d } ) \| ^ { 2 } \leqslant ( 1 + L \varepsilon ) \| x _ { d } - x _ { * , d } \| ^ { 2 } .
$$

Likewise the same upper bound holds for A x ∗ ,d ( x d − x ∗ ,d ) :

$$
\| A _ { x _ { * , d } } ( x _ { d } - x _ { * , d } ) \| ^ { 2 } \leqslant ( 1 + L \varepsilon ) \| x _ { d } - x _ { * , d } \| ^ { 2 } .
$$

We now aim to upper bound the inner product − 2   x d − x ∗ ,d ,A T x d A x ∗ ,d ( x d − x ∗ ,d )   . We ﬁrst note that since A satisﬁes the RRCP, we have

$$
| \langle x _ { d } - x _ { * , d } , ( A _ { x _ { d } } ^ { T } A _ { x _ { * , d } } - \Phi _ { x _ { d } , x _ { * , d } } ) ( x _ { d } - x _ { * , d } ) \rangle | \leqslant L \varepsilon \| x _ { d } - x _ { * , d } \| ^ { 2 } .
$$

Hence we have that

$$
\langle x _ { d } - x _ { * , d } , A _ { x _ { d } } ^ { T } A _ { x _ { * } , d } ( x _ { d } - x _ { * , d } ) \rangle = \langle x _ { d } - x _ { * , d } , \Phi _ { x _ { d } , x _ { * } , d } ( x _ { d } - x _ { * , d } ) \rangle + O _ { 1 } ( L \varepsilon ) \| x _ { d } - x _ { * , d } \| ^ { 2 } .
$$

But recall that x ∈ B ( x ∗ ,d √ ε   x ∗   ) which implies | θ 0 ,x |   2 d √ ε . Since | θ d,x |   | θ 0 ,x | we have | θ d,x |   2 d √ ε . Also equation ( 53 ) gives

$$
| \theta _ { d , x } - \bar { \theta } _ { d , x } | \leqslant 4 d \sqrt { \varepsilon } .
$$

Hence we have that | θ d,x |   6 d √ ε. Thus Φ x d ,x ∗ ,d is approximately an isometry since

$$
\left \| \Phi _ { x _ { d } , x _ { * , d } } - I \right \| & \leqslant \frac { 2 | \theta _ { d , x } | } { \pi } \| I \| + \frac { 2 | \sin \theta _ { d , x } | } { \pi } \| M _ { \hat { x } _ { d } \leftrightarrow \hat { x } _ { * , d } } \| \leqslant \frac { 2 4 d \sqrt { \varepsilon } } { \pi } .
$$

This implies that

$$
\langle x _ { d } - x _ { * , d } , \Phi _ { x _ { 1 } , x _ { * } , d } ( x _ { d } - x _ { * , d } ) \rangle = \| x _ { d } - x _ { * , d } \| ^ { 2 } + O _ { 1 } \left ( \frac { 2 4 d \sqrt { \varepsilon } } { \pi } \right ) \| x _ { d } - x _ { * , d } \| ^ { 2 } .
$$

[Page 44]

Combining ( 78 ) and ( 79 ) we attain

$$
\langle x _ { d } - x _ { * , d } , A _ { x _ { d } } ^ { T } A _ { x _ { * , d } } ( x _ { d } - x _ { * , d } ) \rangle = \| x _ { d } - x _ { * , d } \| ^ { 2 } + O _ { 1 } \left ( \frac { 2 4 d \sqrt { \varepsilon } } { \pi } + L \varepsilon \right ) \| x _ { d } - x _ { * , d } \| ^ { 2 } .
$$

Note that this implies that

$$
- 2 \langle x _ { d } - x _ { * , d } , A _ { x _ { d } } ^ { T } A _ { x _ { * , d } } ( x _ { d } - x _ { * , d } ) \rangle & \leqslant \left ( - 2 + \frac { 4 8 d \sqrt { \varepsilon } } { \pi } + 2 L \varepsilon \right ) \| x _ { d } - x _ { * , d } \| ^ { 2 } .
$$

Returning to establishing concentration of ( A x d − A x ∗ ,d )Λ x ∗ x ∗ , we can use ( 76 ), ( 77 ) and ( 80 ) to obtain

$$
\| ( A _ { x _ { d } } - A _ { x , d } ) A _ { x , * } \| ^ { 2 } & \leqslant \| A _ { x _ { d } } ( x _ { d } - x _ { * , d } ) \| ^ { 2 } + \| A _ { x _ { * } , d } ( x _ { d } - x _ { * , d } ) \| ^ { 2 } - 2 \langle x _ { d } - x _ { * , d } , A _ { x _ { d } , * } ^ { T } ( x _ { d } - x _ { * , d } ) \rangle \\ & \leqslant \left ( 2 + 2 L \varepsilon - 2 + \frac { 4 8 \sqrt { \varepsilon } } { \pi } + 2 L \varepsilon \right ) \| x _ { d } - x _ { * , d } \| ^ { 2 } \\ & = \left ( 4 L \varepsilon + \frac { 4 8 d \sqrt { \varepsilon } } { \pi } \right ) \| x _ { d } - x _ { * , d } \| ^ { 2 } .
$$

Using this inequality, equation ( 73 ), and the fact that ε < √ ε , we attain

$$
\left \| ( A _ { x _ { d } } - A _ { x _ { * } , d } ) \Lambda _ { x _ { * } , x _ { * } } \right \| ^ { 2 } \leq & \left ( 4 L \varepsilon + \frac { 4 8 d \sqrt { \varepsilon } } { \pi } \right ) \| x _ { d } - x _ { * , d } \| ^ { 2 } \leqslant \frac { 1 . 4 4 ( 4 L + \frac { 4 8 d } { \pi } ) \sqrt { \varepsilon } } { 2 ^ { d } } \| x - x _ { * } \| ^ { 2 } .
$$

Then by equations ( 20 ) and ( 21 ) from Lemma 16 , we have that

$$
\| A _ { x _ { d } } \Lambda _ { x } \| \leq & \sqrt { 1 + L \varepsilon } \| \Lambda _ { x } \| \leq \sqrt { \frac { 1 3 } { 1 2 } ( 1 + L \varepsilon ) } \frac { 1 } { 2 ^ { d / 2 } } .
$$

Combining ( 82 ) and ( 81 ) and choosing ε so that   13 12 (1 + Lε )   2 , we attain

$$
\| A _ { x _ { d } } \Lambda _ { x } \| \left ( \| A _ { x _ { d } } - A _ { x _ { * , d } } \right ) \Lambda _ { x _ { * } } x _ { * } \| \leqslant 2 \sqrt { 1 . 4 4 \left ( 4 L + \frac { 4 8 d } { \pi } \right ) \sqrt { \varepsilon } \frac { 1 } { 2 ^ { d } } } \| x - x _ { * } \| .
$$

Thus if

we attain

i.e., T 2 satisﬁes

$$
\varepsilon ^ { 1 / 4 } < \frac { 1 } { 6 4 \sqrt { 1 . 4 4 \left ( 4 L + \frac { 4 8 d } { \pi } \right ) } }
$$

$$
\| A _ { x _ { d } } \Lambda _ { x } \| \left \| ( A _ { x _ { d } } - A _ { x _ { * } , d } ) \Lambda _ { x _ { * } } x _ { * } \right \| \leqslant \frac { 1 } { 3 2 } \frac { 1 } { 2 ^ { d } } \| x - x _ { * } \|
$$

$$
T _ { 2 } = O _ { 1 } \left ( \frac { 1 } { 3 2 } \right ) \frac { 1 } { 2 ^ { d } } \| x - x _ { * } \| .
$$

Combining our results for T 1 and T 2 in equations ( 75 ) and ( 83 ) we ultimately get

$$
\left \| \bar { v } _ { x } - \Lambda _ { x } ^ { \text {T} } ( \Lambda _ { x } x - \Lambda _ { x } x _ { * } ) \right \| \leqslant \frac { 1 } { 1 6 } \frac { 1 } { 2 ^ { d } } \| x - x _ { * } \| .
$$

[Page 45]

To ﬁnish establishing concentration of v x , we appeal to Lemma A.9 of [ 35 ] which showed that if ε < 1 / (200 4 d 6 ) and x ∈ B ( x ∗ ,d √ ε   x ∗   ) then

$$
\left \| \Lambda _ { x } ^ { T } ( \Lambda _ { x } x - \Lambda _ { x _ { * } } x _ { * } ) - \frac { 1 } { 2 ^ { d } } ( x - x _ { * } ) \right \| \leqslant \frac { 1 } { 1 6 } \frac { 1 } { 2 ^ { d } } \| x - x _ { * } \| .
$$

Thus by combining equations ( 84 ) and ( 85 ), we ﬁnally attain

$$
\left \| \overline { v } _ { x } - \frac { 1 } { 2 ^ { d } } ( x - x _ { * } ) \right \| \leqslant \| \overline { v } _ { x } - \Lambda _ { x } ^ { T } ( \Lambda _ { x } x - \Lambda _ { x } x _ { * } ) \| + \left \| \Lambda _ { x } ^ { T } ( \Lambda _ { x } x - \Lambda _ { x } x _ { * } ) - \frac { 1 } { 2 ^ { d } } ( x - x _ { * } ) \right \| \leqslant \frac { 1 } { 8 } \frac { 1 } { 2 ^ { d } } \| x - x _ { * } \| .
$$

as desired. Including the bound on   q x   from ( 49 ), we achieve the ﬁnal desired result:

$$
\left \| v _ { x } - \frac { 1 } { 2 ^ { d } } ( x - x _ { * } ) \right \| \leqslant \left \| \overline { v } _ { x } - \frac { 1 } { 2 ^ { d } } ( x - x _ { * } ) \right \| + \| q _ { x } \| \leqslant \frac { 1 } { 8 } \frac { 1 } { 2 ^ { d } } \| x - x _ { * } \| + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| .
$$

Finally, for non-diﬀerentiable x   = 0 and v ∈ ∂f ( x ) , by ( 12 ) we have that there exists c     0 for   ∈ [ s ] such that   s   =1 c   = 1 and v =   s   =1 c   v   . Hence

glyph[negationslash]

$$
\left \| v - \frac { 1 } { 2 ^ { d } } ( x - x _ { * } ) \right \| \leqslant \sum _ { \ell = 1 } ^ { s } c _ { \ell } \left \| v _ { \ell } - \frac { 1 } { 2 ^ { d } } ( x - x _ { * } ) \right \| \leqslant \frac { 1 } { 8 } \frac { 1 } { 2 ^ { d } } \| x - x _ { * } \| + \frac { 2 } { 2 ^ { d / 2 } } \| \eta \| .
$$

# 3 Gaussian Matrices Satisfy the RRCP

We set out to prove that Gaussian A satisﬁes the RRCP with respect to G with high probability. The particular result is stated as follows:

Proposition 3 (Range Restricted Concentration Property (RRCP)) . Fix 0 < ε < 1 . Let W i ∈ R n i × n i − 1 have i.i.d. N (0 , 1 /n i ) entries for i = 1 ,...,d . Let A ∈ R m × n d have i.i.d. N (0 , 1 /m ) entries independent from { W i } . Then if m > ˜ C ε dk log( n 1 n 2 ...n d ) , then with probability at least 1 − ˜ γm 4 k exp   − ˜ c ε 2 m   , we have that for all x,y,x 1 ,x 2 ,x 3 ,x 4 ∈ R k ,

$$
| \langle ( A _ { G ( x ) } ^ { T } A _ { G ( y ) } - \Phi _ { G ( x ) , G ( y ) } ) ( G ( x _ { 1 } ) - G ( x _ { 2 } ) ) , G ( x _ { 3 } ) - G ( x _ { 4 } ) \rangle | \\ & \leqslant L \varepsilon \| G ( x _ { 1 } ) - G ( x _ { 2 } ) \| \| G ( x _ { 3 } ) - G ( x _ { 4 } ) \|
$$

Here ˜ γ and L are positive universal constants, ˜ c ε depends polynomially on ε , and ˜ C ε depends polynomially on ε − 1 .

We will prove Proposition 3 via the following steps:

- 1. We ﬁrst establish that for any ﬁxed non-zero z,w ∈ R n , the inner product   A T z A w x,y   concentrates around its expectation   Φ z,w x,y   for all x and y in a ﬁxed k -dimensional subspace of R n .
- 2. Then we show that this concentration holds uniformly for all z,w,x,y that live in the union of a ﬁnite number of k -dimensional subspaces of R n .
- 3. To complete the proof, we apply the result from Step 2 for all z,w,x,y in the range of the generative model which precisely lives in the union of k -dimensional subspaces.


[Page 46]

# 3.1 Concentration Over a Fixed Subspace

We ﬁrst show that the matrix A T z A w concentrates around Φ z,w for any ﬁxed z   = w while acting on a ﬁxed k -dimensional subspace T of R n . We will refer to this result as the Restricted Concentration Property (RCP).

glyph[negationslash]

Proposition 4 (Variant of Lemma 5.1 in [ 7 ]; RCP) . Fix 0 < ε < 1 and k < m . Let A ∈ R m × n have i.i.d. N (0 , 1 /m ) entries and ﬁx z,w ∈ R n \ { 0 } . Let T ⊂ R n be a k -dimensional subspace. Then if m   Ck , we have that with probability exceeding 1 − 2exp( − c 1 m ) ,

$$
| \langle A _ { z } ^ { T } A _ { w } x , x \rangle - \langle \Phi _ { z , w } x , x \rangle | \leqslant \varepsilon \| x \| ^ { 2 } \, \forall \, x \in T
$$

and

$$
| \langle A _ { z } ^ { T } A _ { w } x , y \rangle - \langle \Phi _ { z , w } x , y \rangle | \leqslant 3 \varepsilon \| x \| \| y \| \, \forall \, x , y \in T .
$$

Furthermore, let U =   M i =1 U i and V =   N j =1 V j where U i and V j are subspaces of R n of dimension at most k for all i ∈ [ M ] and j ∈ [ N ] . Then if m   2 Ck

$$
\left | \langle A _ { z } ^ { T } A _ { w } u , v \rangle - \langle \Phi _ { z , w } u , v \rangle \right | \leqslant 3 \varepsilon \| u \| \| v \| \, \forall \, u \in U , \ v \in V ,
$$

with probability exceeding 1 − 2 MN exp( − c 1 m ) . Here c 1 depends polynomially on ε and C = Ω( ε − 1 log ε − 1 ) .

For the proof, we require the following large deviation inequality for subexponential random variables:

Lemma 23 (Corollary 5.17 in [ 60 ]) . Let Y 1 ,...,Y m be independent, centered, subexponential random variables. Let K = max i ∈ [ m ]   Y i   ψ 1 . Then for all ε > 0 ,

$$
\mathbb { P } \left ( \frac { 1 } { m } \left | \sum _ { i = 1 } ^ { m } Y _ { i } \right | \geqslant \varepsilon \right ) \leqslant 2 \exp \left [ - c \min \left ( \frac { \varepsilon ^ { 2 } } { K ^ { 2 } } , \frac { \varepsilon } { K } \right ) m \right ]
$$

where c > 0 is an absolute constant. Here  ·  ψ 1 is the subexponential norm:   X   ψ 1 := sup p   1 p − 1 ( E | X | p ) 1 /p .

We also require the following simple technical result.

Proposition 5. Fix z,w ∈ R n \ { 0 } and 0 < ε < 1 . Let T be a subspace of R n . If

$$
| \langle A _ { z } ^ { T } A _ { w } x , x \rangle - \langle \Phi _ { z , w } x , x \rangle | \leqslant \varepsilon \| x \| ^ { 2 } \, \forall \, x \in T
$$

then Step 1: Approximation. We first show that if concentration over an ε -net of T ∩ S n -1 holds, then a continuity argument establishes concentration over all points in T ∩S n -1 . Choose an ε 14 -net Q T ⊂ T ∩ S n -1 such that | Q T | glyph[lessorequalslant] (42 /ε ) k and for any x ∈ T ∩ S n -1 ,

$$
\left | \langle A _ { z } ^ { T } A _ { w } x , y \rangle - \langle \Phi _ { z , w } x , y \rangle \right | \leqslant 3 \varepsilon \| x \| \| y \| \, \forall \ x , y \in T .
$$

With these two results, we are now equipped to prove Proposition 4 .

Proof of Proposition 4 . Without loss of generality, it suﬃces to show concentration over T ∩ S n − 1 . For notational simplicity, set Σ z,w := A T z A w − Φ z,w .

[Page 47]

We will prove that

Now, deﬁne

$$
\min _ { q \in Q _ { T } } \| x - q \| \leqslant \frac { \varepsilon } { 1 4 } .
$$

$$
| \langle \Sigma _ { z , w } q , q \rangle | \leqslant \frac { \varepsilon } { 8 } \, \forall \, q \in Q _ { T } \Longrightarrow | \langle \Sigma _ { z , w } x , x \rangle | \leqslant \varepsilon \, \forall \, x \in T .
$$

$$
\alpha ^ { * } \coloneqq \inf \left \{ \alpha > 0 \colon | \langle \Sigma _ { z , w } x , x \rangle | \leqslant \alpha \| x \| ^ { 2 } \, \forall \, x \in T \right \} .
$$

We want to show that α ∗   ε . Fix x ∈ T ∩ S n − 1 . Then there exists a q ∈ Q T such that   x − q     ε/ 14 . In addition, observe that x − q ∈ T since q ∈ Q T ⊂ T so by ( 92 ),

$$
| \langle \Sigma _ { z , w } ( x - q ) , x - q \rangle | \leqslant \alpha ^ { * } \| x - q \| ^ { 2 } \leqslant \alpha ^ { * } \frac { \varepsilon ^ { 2 } } { 1 9 6 } .
$$

Now, note that by the deﬁnition of α ∗ ,

$$
| \langle \Sigma _ { z , w } x , x \rangle | \leqslant \alpha ^ { * } \| x \| ^ { 2 } \ \forall \ x \in T .
$$

Thus Proposition 5 gives Step 2: Concentration. We now establish concentration for a fixed point x ∈ S n -1 . Then observe that

$$
| \langle \Sigma _ { z , w } x , y \rangle | \leqslant 3 \alpha ^ { * } \| x \| \| y \| \, \forall \, x , y \in T .
$$

Applying this result to x − q and q gives

$$
| \langle \Sigma _ { z , w } ( x - q ) , q \rangle | \leqslant 3 \alpha ^ { * } \| x - q \| \leqslant \alpha ^ { * } \frac { 3 \varepsilon } { 1 4 } .
$$

Let E be the event that |  Σ z,w q,q  |   ε 8 for any q ∈ Q T . Using   Σ z,w x,x   =   Σ z,w ( x − q ) ,x − q   + 2   Σ z,w x,q   −   Σ z,w q,q   and   Σ z,w x,q   =   Σ z,w ( x − q ) ,q   +   Σ z,w q,q   , we have that on E ,

$$
\langle \Delta _ { z , w } x , q \rangle & \quad \langle \Delta _ { z , w } q , q \rangle \, d \Lambda \, \left ( \Delta _ { z , w } x , q \right ) / = \langle \Delta _ { z , w } q , q \rangle , \, w \, C h a v \, \max \, \text {on} \, L , \\ & \quad | \langle \Sigma _ { z , w } x , x \rangle | \leqslant | \langle \Sigma _ { z , w } ( x - q ) , x - q \rangle | + 2 | \langle \Sigma _ { z , w } x , q \rangle | + | \langle \Sigma _ { z , w } q , q \rangle | \\ & \leqslant | \langle \Sigma _ { z , w } ( x - q ) , x - q \rangle | + 2 | \langle \Sigma _ { z , w } ( x - q ) , q \rangle | + 3 | \langle \Sigma _ { z , w } q , q \rangle | \\ & \leqslant \alpha ^ { 2 } \frac { \varepsilon ^ { 2 } } { 1 9 6 } + \alpha ^ { * } \frac { 3 \varepsilon } { 7 } + \frac { 3 \varepsilon } { 8 } \\ & = \alpha ^ { * } \left ( \frac { \varepsilon ^ { 2 } } { 1 9 6 } + \frac { 3 \varepsilon } { 7 } \right ) + \frac { 3 \varepsilon } { 8 } \\ \intertext { s h e r w e s u d ( 93 ) } \text {where} \, \text {we used } ( 9 3 ) \, \left ( 9 4 \right ) \, \text { and the event } F \text { in the third inequality } \text { Thus}
$$

where we used ( 93 ), ( 94 ), and the event E in the third inequality. Thus

$$
| \langle \Sigma _ { z , w } x , x \rangle | \leqslant \alpha ^ { * } \left ( \frac { \varepsilon ^ { 2 } } { 1 9 6 } + \frac { 3 \varepsilon } { 7 } \right ) + \frac { 3 \varepsilon } { 8 } \ \forall \ x \in T \cap \mathcal { S } ^ { n - 1 } .
$$

However, recall that α ∗ was deﬁned to be the smallest number such that

$$
| \langle \Sigma _ { z , w } x , x \rangle | \leqslant \alpha ^ { * } \, \forall \, x \in T \cap \mathcal { S } ^ { n - 1 } .
$$

Hence α ∗ must be smaller than the right hand side of ( 95 ), i.e.

$$
\alpha ^ { * } \leqslant \alpha ^ { * } \left ( \frac { \varepsilon ^ { 2 } } { 1 9 6 } + \frac { 3 \varepsilon } { 7 } \right ) + \frac { 3 \varepsilon } { 8 } \Longrightarrow \alpha ^ { * } \leqslant \frac { 3 \varepsilon } { 8 } \left ( \frac { 1 } { 1 - \frac { \varepsilon ^ { 2 } } { 1 9 6 } - \frac { 3 \varepsilon } { 7 } } \right ) \leqslant \varepsilon
$$

since 0 < ε < 1 . This establishes ( 91 ).

[Page 48]

$$
| \langle \Sigma _ { z , w } x , x \rangle | = \frac { 1 } { m } \left | \sum _ { i = 1 } ^ { m } Y _ { i } \right |
$$

    where Y i = X i − E [ X i ] , X i = sgn(   ˜ a i ,z    ˜ a i ,w   )   ˜ a i ,x   2 , and each ˜ a i ∼ N (0 ,I n ) . Hence Y i are independent, centered, subexponential random variables. We now estimate their subexponential norm prior to invoking Lemma 23 .

By Remark 5.18 in [ 60 ], the subexponential norm satisﬁes

$$
\| Y _ { i } \| _ { \psi _ { 1 } } = \| X _ { i } - \mathbb { E } [ X _ { i } ] \| _ { \psi _ { 1 } } \leqslant 2 \| X _ { i } \| _ { \psi _ { 1 } } .
$$

Let Z i :=   ˜ a i ,x   ∼ N (0 , 1) . Then   Z i   ψ 2   K 1 for some absolute constant K 1 where   ·   ψ 2 is the sub-gaussian norm. Observe that E | X i | p   E | Z 2 i | p which implies   X i   ψ 1     Z 2 i   ψ 1 . Thus we have

$$
\| Y _ { i } \| _ { \psi _ { 1 } } \leqslant 2 \| X _ { i } \| _ { \psi _ { 1 } } \leqslant 2 \| Z _ { i } ^ { 2 } \| _ { \psi _ { 1 } } \leqslant 4 \| Z _ { i } \| _ { \psi _ { 2 } } ^ { 2 } \leqslant 4 K _ { 1 } ^ { 2 }
$$

where we used equation ( 96 ) in the ﬁrst inequality and Lemma 5.14 in [ 60 ] in the second to last inequality. Thus K = max i ∈ [ m ]   Y i   ψ 1   4 K 2 1 for an absolute constant K 1 . Deﬁning K 2 := 4 K 2 1 , Lemma 23 guarantees that for any ﬁxed z,w,x ∈ R n \ { 0 } and ε > 0 ,

$$
\mathbb { P } \left ( | \langle \Sigma _ { z , w } x , x \rangle | \geqslant \varepsilon \right ) \leqslant 2 \exp ( - c _ { 0 } ( \varepsilon ) m )
$$

where c 0 ( ε ) = c min( ε 2 /K 2 2 ,ε/K 2 ) and c > 0 is an absolute constant.

Step 3: Union Bound. We now show concentration over Q T holds. Recall that | Q T |   (42 /ε ) k so we can apply a union bound to ( 97 ) to attain

$$
\mathbb { P } \left ( | \langle \Sigma _ { z , w } q , q \rangle | \geq \frac { \varepsilon } { 8 } \, \forall \, q \in Q _ { T } \right ) \leqslant 2 \left ( \frac { 4 2 } { \varepsilon } \right ) ^ { k } \exp \left ( - c _ { 0 } \left ( \frac { \varepsilon } { 8 } \right ) m \right ) .
$$

By equations ( 91 ) and ( 98 ), we conclude that

$$
\mathbb { P } \left ( | \langle \Sigma _ { z , w } x , x \rangle | \geqslant \varepsilon \| x \| ^ { 2 } \, \forall \, x \in T \right ) \leqslant 2 \left ( \frac { 4 2 } { \varepsilon } \right ) ^ { k } \exp \left ( - c _ { 0 } \left ( \frac { \varepsilon } { 8 } \right ) m \right ) .
$$

The probability bound in the proposition can be shown by noting that

$$
1 - 2 ( 4 2 / \varepsilon ) ^ { k } \exp ( - c _ { 0 } ( \varepsilon / 8 ) m ) = 1 - 2 \exp \left ( - c _ { 0 } ( \varepsilon / 8 ) m + k \log \left ( \frac { 4 2 } { \varepsilon } \right ) \right ) .
$$

Thus if

$$
\frac { 2 } { c _ { 0 } ( \varepsilon / 8 ) } \log \left ( \frac { 4 2 } { \varepsilon } \right ) k \leqslant C k \leqslant m
$$

where C = Ω( ε − 1 log ε − 1 ) , we have that the result holds with probability exceeding

$$
1 - 2 \exp \left ( - c _ { 0 } ( \varepsilon / 8 ) m + k \log \left ( \frac { 4 2 } { \varepsilon } \right ) \right ) \geqslant 1 - 2 \exp ( - c _ { 1 } m )
$$

where c 1 = c 0 ( ε/ 8) / 2 . Applying Proposition 5 to our result gives ( 87 ) with the same probability. The extension to the union of subspaces follows by applying ( 87 ) to all subspaces of the form span ( U i ,V j ) and using a union bound. Note that these subspaces have dimension at most 2 k , accounting for the extra factor of 2 in the bound on m .

[Page 49]

# 3.2 Uniform Concentration Over a Union of Subspaces

We will now set out to prove a stronger version of Proposition 4 that holds uniformly for all z and w in (possibly) diﬀerent k -dimensional subspaces:

Proposition 6 (Uniform RCP) . Fix 0 < ε < 1 and k < m . Let A ∈ R m × n have i.i.d. N (0 , 1 /m ) entries. Let T , W , and Z be ﬁxed k -dimensional subspaces of R n . Then if m   C ε k , then with probability at least 1 − ˆ γm 4 k exp( − ˜ c ε m ) , we have

$$
\left | \langle A _ { z } ^ { T } A _ { w } x , y \rangle - \langle \Phi _ { z , w } x , y \rangle \right | \leqslant L \varepsilon \| x \| \| y \| \, \forall \, x , y \in T , \ w \in W , \ z \in Z
$$

where ˆ γ is a positive universal constant, ˜ c ε depends on ε and C ε depends polynomially on ε − 1 . Furthermore, let U =   N 1 i =1 U i , V =   N 2 j =1 V j , W =   N 3 k =1 W k , and Z =   N 4   =1 Z   be the union of at most k -dimensional subspaces of R n . Then if m   2 C ε k ,

$$
\left | \langle A _ { z } ^ { \mathrm T } A _ { w } u , v \rangle - \langle \Phi _ { z , w } u , v \rangle \right | \leqslant L \varepsilon \| u \| \| v \| \, \forall \ u \in U , \ v \in V , \ w \in W , \ z \in Z
$$

with probability exceeding 1 − N 1 N 2 N 3 N 4 ˆ γm 4 k exp( − ˜ c ε m ) . Here L is a positive universal constant.

Note that Proposition 4 established concentration of   A T z A w x,y   around   Φ z,w x,y   for x and y in a ﬁxed k -dimensional subspace for ﬁxed z,w ∈ R n \ { 0 } . We are interested in showing that this concentration holds uniformly for all z and w in the range of our generative model. The proof of this result uses an interesting fact from 1 -bit compressed sensing which establishes that if a suﬃcient number of random hyperplanes cut the unit sphere, the diameter of each tesselation piece is small with high probability [ 50 ]. We state the theorem here for convenience:

Theorem 5 (Theorem 2.1 in [ 50 ]) . Let n,m,s > 0 and set δ = C 1   s m log(2 n/s )   1 / 5 . Let a i ∈ R n have i.i.d. N (0 , 1) entries for i ∈ [ m ] . Then with probability at least 1 − C 2 exp( − cδm ) , the following holds uniformly for all x, ˜ x ∈ R n that satisfy   x   2 =   ˜ x   2 = 1 ,   x   1   √ s , and   ˜ x   1   √ s for s   n :

$$
\langle a _ { i } , \tilde { x } \rangle \langle a _ { i } , x \rangle \geqslant 0 , \ \forall \ i \in [ m ] \Longrightarrow \| \tilde { x } - x \| _ { 2 } \leqslant \delta .
$$

Here C 1 ,C 2 ,c are positive universal constants.

We will use this result to prove the following: given a suﬃcient number of random hyperplanes and a k -dimensional subspace Z , there exists a ﬁnite set of points Z 0 ⊂ Z that live in the interior of the tesselation pieces generated by the random hyperplanes such that any point in Z can be closely approximated by a point in Z 0 with high probability.

Lemma 24. Fix 0 < ε < 1 . Let A ∈ R m × n have i.i.d. N (0 , 1 /m ) entries with rows { a   } m   =1 . Let Z be a k -dimensional subspace of R n . Deﬁne E Z,A to be the event that there exists a set Z 0 ⊂ Z with the following properties:

each z 0 ∈ Z 0 satisfies 〈 a glyph[lscript] , z 0 〉 glyph[negationslash] = 0 for all glyph[lscript] ∈ [ m ] ,

| Z 0 | glyph[lessorequalslant] 10 m 2 k , and

• for all z ∈ Z such that   z   = 1 , there exists a z 0 ∈ Z 0 such that   z − z 0     ε.

If m   ˆ Ck , then P ( E Z,A )   1 − C 2 exp( − cεm ) . Here C 2 and c are positive absolute constants and ˆ C depends polynomially on ε − 1 .

[Page 50]

Proof of Lemma 24 . By the rotational invariance of the Gaussian distribution, we may take Z to be in the span of the ﬁrst k standard basis vectors. We may further without loss of generality assume A ∈ R m × k . We will invoke the following lemma which establishes that the unit sphere of Z is partitioned into at most 10 m 2 k regions by the rows { a   } m   =1 of A with probability 1 :

Lemma 25. Let V be a subspace of R n . Let A ∈ R m × n have i.i.d. N (0 , 1 /m ) entries. With probability 1,

$$
| \{ d i a g ( s g n ( A v ) ) A \colon v \in V \} | \leqslant 1 0 m ^ { 2 \dim V } .
$$

In each tesselation piece deﬁned by the rows of A , choose a single point z 0 from Z with unit norm such that a T   z 0   = 0 for all   ∈ [ m ] (if such a point exists in the tesselation piece). Let Z 0 denote this collection of points and set I Z,A := | Z 0 | . By Lemma 25 with V = Z , the cardinality of Z 0 is bounded with probability 1 : I Z,A   10 m 2 k . Then observe that we can set the parameters n and s in Theorem 5 equal to k since A ∈ R m × k and Z is in the span of the ﬁrst k standard basis vectors. Then if m     C 5 1 log(2) /ε 5   k =: ˆ Ck , we have that the quantity δ in the theorem is bounded by ε :

glyph[negationslash]

$$
\delta \colon = C _ { 1 } \left ( \frac { k } { m } \log ( 2 ) \right ) ^ { 1 / 5 } \leqslant \varepsilon
$$

so P ( E Z,A )   1 − C 2 exp( − cεm ) for some positive universal constants c , C 1 , and C 2 and ˆ C depends polynomially on ε − 1 .

We now proceed with the proof of the Uniform RCP.

Proof of Proposition 6 . Let E Z,A be the event deﬁned in Lemma 24 . By Lemma 24 , we have that if m   ˆ Ck , there exists an event E Z,A with P ( E Z,A )   1 − C 2 exp( − cεm ) on which there exists a ﬁnite subset Z 0 of Z with cardinality I Z,A   10 m 2 k such that for any z ∈ Z with   z   = 1 , there exists a z 0 ∈ Z 0 such that   z − z 0     ε . The analogous ﬁnite set W 0 with cardinality I W,A   10 m 2 k also exists on the event E W,A with probability at least 1 − C 2 exp( − cεm ) . Thus if m   ˆ Ck , the event E Z,W := E Z,A ∩ E W,A satisﬁes

$$
\mathbb { P } ( E _ { Z , W } ) \geqslant 1 - 2 C _ { 2 } \exp ( - c \varepsilon m ) .
$$

We now establish concentration over Z 0 and W 0 . Let E 0 be the event that

$$
\left | \langle A _ { z _ { 0 } } ^ { T } A _ { w _ { 0 } } x , y \rangle - \langle \Phi _ { z _ { 0 } , w _ { 0 } } x , y \rangle \right | \leqslant 3 \varepsilon \| x \| \| y \| \, \forall \, x , y \in T , \, z _ { 0 } \in Z _ { 0 } , \, w _ { 0 } \in W _ { 0 } .
$$

By Proposition 4 , if m   Ck , we have that the following holds for ﬁxed z 0 ∈ Z 0 and w 0 ∈ W 0 with probability exceeding 1 − 2exp( − c 1 m ) :

$$
| \langle A _ { z _ { 0 } } ^ { T } A _ { w _ { 0 } } x , y \rangle - \langle \Phi _ { z _ { 0 } , w _ { 0 } } x , y \rangle | \leqslant 3 \varepsilon \| x \| \| y \| \, \forall \, x , y \in T .
$$

Furthermore, on E Z,W , a union bound over all z 0 ∈ Z 0 and w 0 ∈ W 0 shows that

$$
\mathbb { P } ( E _ { 0 } ) \geqslant 1 - 2 I _ { Z , A } I _ { W , A } \exp \left ( - \frac { c _ { 1 } m } { 2 } \right ) \geqslant 1 - \gamma m ^ { 4 k } \exp \left ( - \frac { c _ { 1 } m } { 2 } \right )
$$

where γ is a positive absolute constant and c 1 depends on ε .

For the remainder of this proof, we work on the event E 0 ∩ E Z,W . Fix non-zero z ∈ Z and w ∈ W . Deﬁne the following set:

$$
\Omega _ { z , w } \coloneqq \{ \ell \in [ m ] \colon \langle a _ { \ell } , z \rangle = 0 \text { or } \langle a _ { \ell } , w \rangle = 0 \} \, .
$$

[Page 51]

Note that since Z and W are k -dimensional and any subset of k rows of A are linearly independent with probability 1 , at most k entries of Az are zero and similarly for Aw . Hence | Ω z,w |   2 k . Furthermore, observe that

$$
r \text {hermore, observe that} \\ A _ { z } ^ { T } A _ { w } = \sum _ { \ell = 1 } ^ { m } s g n ( \langle a _ { \ell } , z \rangle \langle a _ { \ell } , w \rangle ) a _ { \ell } a _ { \ell } ^ { T } \\ = \sum _ { \ell \in \Omega _ { z , w } } s g n ( \langle a _ { \ell } , z \rangle \langle a _ { \ell } , w \rangle ) a _ { \ell } a _ { \ell } ^ { T } + \sum _ { \ell \in \Omega _ { z , w } ^ { c } } s g n ( \langle a _ { \ell } , z \rangle \langle a _ { \ell } , w \rangle ) a _ { \ell } a _ { \ell } ^ { T } \\ = \sum _ { \ell \in \Omega _ { z , w } ^ { c } } s g n ( \langle a _ { \ell } , z \rangle \langle a _ { \ell } , w \rangle ) a _ { \ell } a _ { \ell } ^ { T } \\ \text {the definition of } Q \quad \text {However, on the event } F _ { z } \text {, there exists a } z \in Z _ { \ell } \text {, and } w \in W _ { \ell } \text {, }
$$

by the deﬁnition of Ω z,w . However, on the event E Z,W , there exists a z 0 ∈ Z 0 and w 0 ∈ W 0 such that for all   ∈ Ω c z,w ,

$$
\text {sgn} ( \langle a _ { \ell } , z \rangle ) = \text {sgn} ( \langle a _ { \ell } , z _ { 0 } \rangle ) \text { and } \text {sgn} ( \langle a _ { \ell } , w \rangle ) = \text {sgn} ( \langle a _ { \ell } , w _ { 0 } \rangle )
$$

i.e. z and z 0 (likewise w and w 0 ) lie on the same side of each hyperplane deﬁned by { a   } m   =1 . Hence we have

$$
A _ { z } ^ { T } A _ { w } & = \sum _ { \ell \in \Omega _ { z , w } ^ { c } } \ s g n ( \langle a _ { \ell } , z \rangle \langle a _ { \ell } , w \rangle ) a _ { \ell } a _ { \ell } ^ { T } = \sum _ { \ell \in \Omega _ { z , w } ^ { c } } \ s g n ( \langle a _ { \ell } , z _ { 0 } \rangle \langle a _ { \ell } , w _ { 0 } \rangle ) a _ { \ell } a _ { \ell } ^ { T } \\ & = A _ { z _ { 0 } } ^ { T } A _ { w _ { 0 } } - \sum _ { \ell \in \Omega _ { z , w } ^ { c } } \ s g n ( \langle a _ { \ell } , z _ { 0 } \rangle \langle a _ { \ell } , w _ { 0 } \rangle ) a _ { \ell } a _ { \ell } ^ { T } \\ & = \colon A _ { z _ { 0 } } ^ { T } A _ { w _ { 0 } } - \tilde { A } _ { z _ { 0 } } ^ { T } \tilde { A } _ { w _ { 0 } } .
$$

We now use the following lemma which says that ˜ A T z 0 ˜ A w 0 is small when acting on T .

Lemma 26. Fix 0 < ε < 1 and k < m . Suppose that A ∈ R m × n has i.i.d. N (0 , 1 /m ) entries. Let T ⊂ R n be a k -dimensional subspace and W 0 and Z 0 be subsets of R n . Let E be the event the following inequality holds for all Ω ⊂ [ m ] satisfying | Ω |   2 k :

$$
| \langle \tilde { A } _ { z _ { 0 } } ^ { T } \tilde { A } _ { w _ { 0 } } x , y \rangle | \leqslant \varepsilon \| x \| \| y \| \, \forall \ x , y \in T , \ w _ { 0 } \in W _ { 0 } , \ z _ { 0 } \in Z _ { 0 }
$$

where where we define L := 3+ 88 π +1 < 33 . In the first equality, we used the event E Z,W and (102). In the last inequality, we used the continuity of Φ z,w from Lemma 27 along with the event E 0 ∩ E . Letting C ε := 9 ε -1 ˆ C where ˆ C is given by Lemma 24, we have that if m glyph[greaterorequalslant] C ε k , the event E ∩ E 0 ∩ E Z,W holds with probability exceeding

$$
\tilde { A } _ { z _ { 0 } } ^ { T } \tilde { A } _ { w _ { 0 } } \colon = \sum _ { \ell \in \Omega } s g n ( \langle a _ { \ell } , z _ { 0 } \rangle \langle a _ { \ell } , w _ { 0 } \rangle ) a _ { \ell } a _ { \ell } ^ { T } .
$$

Then there exists a δ ε > 0 such that if m   9 ε − 1 k and 2 k   δ ε m , P ( E )   1 − 2 m exp( − εm/ 36) .

Let E be the event deﬁned in Lemma 26 . On the event E ∩ E 0 ∩ E Z,W , we have that for all z ∈ Z ∩ S n − 1 and w ∈ W ∩ S n − 1 , there exists a z 0 ∈ Z 0 and w 0 ∈ W 0 such that for any x,y ∈ T ,

$$
& \quad & & \geq & 2 | T ^ { 2 } - 1 \text { and } w \in W \ | & T ^ { 2 } - 1 , \ \text { there exists a } 2 \circ 2 \text { and } w _ { 0 } \in W _ { 0 } \text { such that for any } x , y \in 1 , \\ & | ( A _ { z } ^ { T } A _ { w } x , y ) - \langle \Phi _ { z , w } x , y \rangle | & \, \Big | = & \left | \langle A _ { z } ^ { T } A _ { w _ { 0 } } x , y \rangle - \langle \tilde { A } _ { z } ^ { T } A _ { w _ { 0 } } x , y \rangle - \langle \Phi _ { z , w } x , y \rangle \right | \\ & \leq & | \langle A _ { z } ^ { T } A _ { w _ { 0 } } x , y \rangle - \langle \Phi _ { z , w } x , y \rangle | + | \langle \tilde { A } _ { z } ^ { T } \tilde { A } _ { w _ { 0 } } x , y \rangle | \\ & \leq & | ( A _ { z } ^ { T } A _ { w _ { 0 } } x , y ) - \langle \Phi _ { z , w } x , y \rangle | + | \langle \Phi _ { z , w _ { 0 } } x , y \rangle - \langle \Phi _ { z , w } x , y \rangle | + | ( \tilde { A } _ { z _ { 0 } } \tilde { A } _ { w _ { 0 } } x , y ) | \\ & \leq & 3 \epsilon \| x \| \| y \| + \frac { \varepsilon } { \pi } \epsilon \| x \| \| y \| + \varepsilon \| x \| \| y \| \\ & = & L \varepsilon \| x \| \| y \|
$$

[Page 52]

$$
\mathbb { P } ( E \cap E _ { 0 } \cap E _ { Z , W } ) & \geqslant 1 - 2 m \exp ( - \varepsilon m / 3 6 ) - \gamma m ^ { 4 k } \exp \left ( - \frac { c _ { 1 } m } { 2 } \right ) - 2 C _ { 2 } \exp \left ( - c \varepsilon m \right ) \\ & \geqslant 1 - \hat { \gamma } m ^ { 4 k } \exp \left ( - \tilde { c } _ { \varepsilon } m \right )
$$

where ˆ γ is a positive absolute constant and ˜ c ε depends polynomially on ε . The extension to the union of subspaces follows by applying ( 99 ) to all combinations of subspaces T ij = span ( U i ,V j ) , W k , and Z   where each T ij have dimension at most 2 k and using a union bound.

# 3.3 Application to Range of Generative Model

We now apply Proposition 6 to prove Proposition 3 :

Proof of Proposition 3 . For pedagogical purposes, we ﬁrst establish the lemma in the d = 2 case. In order to apply Proposition 6 , we will show that { G ( x ) − G ( y ) : x,y ∈ R k } is a subset of the union of at most 10 6 ( n 2 1 n 2 ) 2 k subspaces of dimensionality at most 2 k .

For ﬁxed W 1 ,W 2 , let A + , 1 = { W 1 , + ,x : x   = 0 } and B + , 2 = { W 2 , + ,x : x   = 0 } . By Lemma 15 in [ 33 ], there exists a probability 1 event, E , over ( W 1 ,W 2 ) on which |A + , 1 |   10 n k 1 and |B + , 2 |   10 2 n k 1 n k 2 . On E , 3 2 k

glyph[negationslash]

glyph[negationslash]

$$
| \{ W _ { 2 , + , x } W _ { 1 , + , x } \colon x \neq 0 \} | \leqslant 1 0 ^ { 3 } ( n _ { 1 } ^ { 2 } n _ { 2 } ) ^ { k } .
$$

glyph[negationslash]

Note that dim range ( W 2 , + ,x W 1 , + ,x )   k for all x   = 0 . Hence

glyph[negationslash]

$$
\{ G ( x ) \colon x \in \mathbb { R } ^ { k } \} \subset \{ W _ { 2 , + , x } W _ { 1 , + , x } w \colon x , w \in \mathcal { S } ^ { k - 1 } \} \subset V
$$

where V the union of at most 10 3 ( n 2 1 n 2 ) k subspaces of dimensionality at most k . This implies that

$$
\{ G ( x ) - G ( y ) \colon x , y \in \mathbb { R } ^ { k } \} \subset V ^ { \prime }
$$

where V   is the union of at most 10 6 ( n 2 1 n 2 ) 2 k subspaces of dimensionality at most 2 k .

By applying the second half of Proposition 6 to the sets V   , V   , V , and V , we get that for ﬁxed W 1 , W 2 ,

$$
| \langle ( A _ { G ( x ) } ^ { T } A _ { G ( y ) } - \Phi _ { G ( x ) , G ( y ) } ) ( G ( x _ { 1 } ) - G ( x _ { 2 } ) ) , G ( x _ { 3 } ) - G ( x _ { 4 } ) \rangle | \\ & \leqslant L \varepsilon \| G ( x _ { 1 } ) - G ( x _ { 2 } ) \| \| G ( x _ { 3 } ) - G ( x _ { 4 } ) \|
$$

with probability at least

$$
1 - 1 0 ^ { 3 ( 2 ) + 6 ( 2 ) } ( n _ { 1 } ^ { 2 } n _ { 2 } ) ^ { 2 k + 4 k } \hat { \gamma } m ^ { 4 k } e ^ { - \tilde { c } _ { \varepsilon } m } \geqslant 1 - \tilde { \gamma } m ^ { 4 k } e ^ { - \tilde { c } _ { \varepsilon } m / 2 } ,
$$

provided m   ˆ KC ε ˜ c − 1 ε k log( n 1 n 2 ) =: ˜ C ε k log( n 1 n 2 ) , where ˜ γ and ˆ K are positive universal constants, ˜ c ε depends polynomially on ε , and C ε depends polynomially on ε − 1 .

Integrating over the probability space of ( W 1 ,W 2 ) , independence of A and ( W 1 ,W 2 ) implies that ( 104 ) holds for random ( W 1 ,W 2 ) with the same probability bound. Continuing from ( 104 ), we have

$$
| \langle ( A _ { G ( x ) } ^ { T } A _ { G ( y ) } - \Phi _ { G ( x ) , G ( y ) } ) ( G ( x _ { 1 } ) - G ( x _ { 2 } ) ) , G ( x _ { 3 } ) - G ( x _ { 4 } ) \rangle | \\ & \leqslant L \varepsilon \| G ( x _ { 1 } ) - G ( x _ { 2 } ) \| \| G ( x _ { 3 } ) - G ( x _ { 4 } ) \|
$$

[Page 53]

∀ x,y,x 1 ,x 2 ,x 3 ,x 4 ∈ R k with probability at least 1 − ˜ γm 4 k e − ˜ c ε m/ 2 for some positive absolute constant ˜ γ and ˜ c ε depends polynomially on ε .

The case for d   2 follows similarly. We have

$$
| \{ \Pi _ { i = d } ^ { 1 } W _ { i , + , x } \colon x \neq 0 \} | \leqslant 1 0 ^ { ( d ^ { 2 } ) } ( n _ { 1 } ^ { d } n _ { 2 } ^ { d - 1 } \cdots n _ { d - 1 } ^ { 2 } n _ { d } ) ^ { k }
$$

glyph[negationslash]

on the probability 1 event. This implies that { G ( x ) : x ∈ R k } ⊂ { Π 1 i = d W i, + ,x w : x,w ∈ S k − 1 } is a subset of the union of at most 10 ( d 2 ) ( n d 1 n d − 1 2 ··· n 2 d − 1 n d ) k subspaces of dimensionality at most k . Moreover, { G ( x ) − G ( y ) : x,y ∈ R k } is a subset of the union of at most

$$
1 0 ^ { ( 2 d ^ { 2 } ) } ( n _ { 1 } ^ { d } n _ { 2 } ^ { d - 1 } \cdots n _ { d - 1 } ^ { 2 } n _ { d } ) ^ { 2 k }
$$

subspaces of dimensionality at most 2 k . Hence the analogous bound ( 104 ) holds for all x,y,x 1 ,x 2 ,x 3 ,x 4 ∈ R k with probability at least

$$
1 - 1 0 ^ { ( 2 d ^ { 2 } + 4 d ^ { 2 } ) } ( n _ { 1 } ^ { d } n _ { 2 } ^ { d - 1 } \cdots n _ { d - 1 } ^ { 2 } n _ { d } ) ^ { 2 k + 4 k } \hat { \gamma } m ^ { 4 k } e ^ { - \tilde { c } _ { \varepsilon } m } \geqslant 1 - \tilde { \gamma } m ^ { 4 k } e ^ { - \tilde { c } _ { \varepsilon } m / 2 } ,
$$

provided m   ˜ C ε dk log( n 1 n 2 ··· n d ) , where ˜ γ is a positive absolute constant, ˜ c ε depends polynomially on ε , and ˜ C ε depends polynomially on ε − 1 .

# 3.4 RRCP Supplementary Results

Proof of Proposition 5 . Fix 0 < ε < 1 . Suppose ( 89 ) holds and ﬁx x,y ∈ T . Without loss of generality, assume x and y are unit normed. We will use the shorthand notation Φ = Φ z,w . Since T is a subspace, x − y ∈ T so by ( 89 ),

$$
\left | \langle A _ { z } ^ { T } A _ { w } ( x - y ) , x - y \rangle - \langle \Phi ( x - y ) , x - y \rangle \right | \leqslant \varepsilon \| x - y \| ^ { 2 }
$$

or equivalently

$$
\langle \Phi ( x - y ) , x - y \rangle - \varepsilon \| x - y \| ^ { 2 } \leqslant \langle A _ { z } ^ { \top } A _ { w } ( x - y ) , x - y \rangle \leqslant \langle \Phi ( x - y ) , x - y \rangle + \varepsilon \| x - y \| ^ { 2 } .
$$

Note that

$$
\| x - y \| ^ { 2 } = 2 - 2 \langle x , y \rangle ,
$$

$$
\langle \Phi ( x - y ) , x - y \rangle = \langle \Phi x , x \rangle + \langle \Phi y , y \rangle - 2 \langle \Phi x , y \rangle ,
$$

and

$$
\langle A _ { z } ^ { \mathrm T } A _ { w } ( x - y ) , x - y \rangle = \langle A _ { z } ^ { \mathrm T } A _ { w } x , x \rangle + \langle A _ { z } ^ { \mathrm T } A _ { w } y , y \rangle - 2 \langle A _ { z } ^ { \mathrm T } A _ { w } x , y \rangle
$$

where we used the fact that Φ and A T z A w are symmetric. Rearranging ( 105 ) yields

$$
2 \left ( \langle \Phi x , y \rangle - \langle A _ { z } ^ { T } A _ { w } x , y \rangle \right ) & \leqslant \left ( \langle \Phi x , x \rangle - \langle A _ { z } ^ { T } A _ { w } x , x \rangle \right ) + \left ( \langle \Phi y , y \rangle - \langle A _ { z } ^ { T } A _ { w } y , y \rangle \right ) + ( 2 - 2 \langle x , y \rangle ) \varepsilon .
$$

By assumption, the ﬁrst two terms are bounded from above by ε . Thus

$$
2 \left ( \langle \Phi x , y \rangle - \langle A _ { z } ^ { T } A _ { w } x , y \rangle \right ) \leqslant 2 \varepsilon + ( 2 - 2 \langle x , y \rangle ) \varepsilon = 2 ( 2 - \langle x , y \rangle ) \varepsilon \leqslant 6 \varepsilon
$$

so

$$
\langle \Phi x , y \rangle - \langle A _ { z } ^ { T } A _ { w } x , y \rangle \leqslant 3 \varepsilon .
$$

The lower bound is identical and establishes the desired result.

[Page 54]

Proof of Lemma 25 . It suﬃces to prove the same upperbound for |{ sgn( Av ) : v ∈ V }| . Let   = dim V . By rotational invariance of Gaussians, we may take V = span ( e 1 ,...,e   ) without loss of generality. Without loss of generality, we may let A have dimensions m ×   and take V = R   .

We will appeal to a classical result from sphere covering [ 66 ]. If m hyperplanes in R   contain the origin and are such that the normal vectors to any subset of   of those hyperplanes are independent, then the complement of the union of these hyperplanes is partitioned into at most

$$
2 \sum _ { i = 0 } ^ { \ell - 1 } \binom { m - 1 } { i }
$$

disjoint regions. Each region uniquely corresponds to a constant value of sgn( Av ) that has all non-zero entries. With probability 1 , any subset of   rows of A are linearly independent, and thus,

$$
| \{ s g n ( A v ) \colon v \in \mathbb { R } ^ { \ell } , \ ( A v ) _ { i } \neq 0 \, \forall \, i \} | \leqslant 2 \sum _ { i = 0 } ^ { \ell - 1 } \binom { m - 1 } { i } \leqslant 2 \ell \left ( \frac { e m } { \ell } \right ) ^ { \ell } \leqslant 1 0 m ^ { \ell }
$$

glyph[negationslash]

where the first inequality uses the fact that ( m glyph[lscript] ) glyph[lessorequalslant] ( em/glyph[lscript] ) glyph[lscript] and the second inequality uses that 2 glyph[lscript] ( e/glyph[lscript] ) glyph[lscript] glyph[lessorequalslant] 10 for all glyph[lscript] glyph[greaterorequalslant] 1 .

For arbitrary v , at most   entries of Av can be zero by linear independence of the rows of A . At each v , there exists a direction ˜ v such that ( A ( v + δ ˜ v )) i   = 0 for all i and for all δ suﬃciently small. Hence, sgn( Av ) diﬀers from one of { sgn( Av ) : v ∈ R   , ( Av ) i   = 0 ∀ i } by at most   entries. Thus,

glyph[negationslash]

glyph[negationslash]

$$
| \{ s g n ( A v ) \colon v \in \mathbb { R } ^ { \ell } \} | \leqslant \binom { m } { \ell } | \{ s g n ( A v ) \colon v \in \mathbb { R } ^ { \ell } , \ ( A v ) _ { i } \neq 0 \ \forall \ i \} | \leqslant m ^ { \ell } 1 0 m ^ { \ell } = 1 0 m ^ { 2 \ell } .
$$

glyph[negationslash]

Proof of Lemma 26 . For any Ω ⊂ [ m ] , let A Ω denote the submatrix of A with rows a T   where   ∈ Ω . We claim that it suﬃces to show

$$
\| A _ { \Omega } x \| \leq \sqrt { \varepsilon } \| x \| \, \forall \, x \in T \, \forall \, \Omega \subset [ m ] \, \text {satisfying } | \Omega | \leq 2 k \leq \delta _ { \varepsilon } m .
$$

To see this, observe that for any w 0 ∈ W 0 , z 0 ∈ Z 0 , and x,y ∈ T and Ω ⊂ [ m ] , we have that

$$
| \langle \tilde { A } _ { z _ { 0 } } ^ { T } \tilde { A } _ { w _ { 0 } } x , y \rangle | & = | \langle \text {diag} ( s g n ( A _ { \Omega } z _ { 0 } ) \odot s g n ( A _ { \Omega } w _ { 0 } ) ) A _ { \Omega } x , A _ { \Omega } y \rangle | \\ & \leqslant \| \text {diag} ( s g n ( A _ { \Omega } z _ { 0 } ) \odot s g n ( A _ { \Omega } w _ { 0 } ) ) \| \| A _ { \Omega } x \| \| A _ { \Omega } y \| \\ & \leqslant \| A _ { \Omega } x \| \| A _ { \Omega } y \|
$$

where we used the Cauchy-Schwarz inequality in the ﬁrst inequality. Hence establishing ( 106 ) will imply the desired conclusion.

By the rotational invariance of the Gaussian distribution, we may take T to be in the span of the ﬁrst k standard basis vectors. We may further without loss of generality assume A ∈ R m × k so it suﬃces to establish   A Ω     √ ε . Fix Ω ⊂ [ m ] satisfying | Ω |   2 k . By Corollary 5.35 in [ 60 ], we have that for any t   0 , it holds with probability 1 − 2exp( − t 2 / 2) that

$$
\sqrt { m } \| A _ { \Omega } \| \leqslant \sqrt { | \Omega | } + \sqrt { k } + t .
$$

Taking t = √ εm/ 3 , we conclude that if | Ω |   εm/ 9 and m   9 k/ε , then   A Ω     √ ε with probability 1 − 2exp( − εm/ 18) .

[Page 55]

We now establish that   A Ω     √ ε holds simultaneously over all subsets Ω ⊂ [ m ] of a suﬃciently small size with a union bound. Observe that since lim δ → 0   e δ   δ = 1 , there exists a δ ∗ > 0 such that   e δ ∗   δ ∗   exp( ε/ 36) . Put δ ε := min { ε,δ ∗ } . Let E be the event that   A Ω     √ ε for all subsets Ω ⊂ [ m ] satisfying | Ω |   2 k   δ ε m . If m   9 ε − 1 k , a union bound shows that this event holds with probability at least

$$
\text {probability at least} \\ 1 - 2 \sum _ { \ell = 1 } ^ { \lfloor \delta _ { e } m \rfloor } \binom { m } { \ell } \exp ( - \varepsilon m / 1 8 ) & \geqslant 1 - 2 \lfloor \delta _ { \varepsilon } m \rfloor \binom { m } { \lfloor \delta _ { \varepsilon } m \rfloor } \exp ( - \varepsilon m / 1 8 ) \\ & \geqslant 1 - 2 \lfloor \delta _ { \varepsilon } m \rfloor \left ( \frac { e m } { \delta _ { \varepsilon } m } \right ) ^ { \delta _ { \varepsilon } m } \exp ( - \varepsilon m / 1 8 ) \\ & = 1 - 2 \lfloor \delta _ { \varepsilon } m \rfloor \left [ \left ( \frac { e } { \delta _ { \varepsilon } } \right ) ^ { \delta _ { \varepsilon } } \right ] ^ { m } \exp ( - \varepsilon m / 1 8 ) \\ & \geqslant 1 - 2 \lfloor \delta _ { \varepsilon } m \rfloor \exp ( - \varepsilon m / 3 6 ) \\ & \geqslant 1 - 2 m \exp ( - \varepsilon m / 3 6 ) \\ \intertext { h o r o w used the fact that } \intertext { b h o r o w used the fact that } \intertext { c h o r o w }
$$

where we used the fact that   e δ ε   δ ε   exp( ε/ 36)

in the second to last inequality.

We now prove the continuity of Φ z,w for non-zero z,w ∈ R n . Recall that

$$
\Phi _ { z , w } \colon = \frac { \pi - 2 \theta _ { z , w } } { \pi } I _ { n } + \frac { 2 \sin \theta _ { z , w } } { \pi } M _ { \hat { z } \leftrightarrow \hat { w } }
$$

where θ z,w := ∠ ( z,w ) and M z ↔ w is the matrix that sends ˆ z  → e 1 , ˆ w  → cos θ z,w e 1 + sin θ z,w e 2 , and h  → 0 for all h ∈ span ( { z,w } ⊥ ) .

Lemma 27 (Continuity of Φ z,w ) . Fix 0 < ε < 1 and z,w ∈ S n − 1 . If   ˜ z − z     ε and   ˜ w − w     ε for some ˜ z, ˜ w ∈ S n − 1 , then

$$
\| \Phi _ { \tilde { z } , \tilde { w } } - \Phi _ { z , w } \| \leqslant \frac { 8 8 } { \pi } \varepsilon .
$$

Proof of Lemma 27 . In this proof, we will utilize the following three inequalities:

$$
| \theta _ { x _ { 1 } , y } - \theta _ { x _ { 2 } , y } | \leq | \theta _ { x _ { 1 } , x _ { 2 } } | , \, \forall \ x _ { 1 } , x _ { 2 } , y \in \mathcal { S } ^ { n - 1 }
$$

$$
2 \sin ( \theta _ { x , y } / 2 ) \leqslant \| x - y \| , \, \forall \, x , y \in \mathcal { S } ^ { n - 1 }
$$

$$
\theta / 4 \leqslant \sin ( \theta / 2 ) , \, \forall \, \theta \in [ 0 , \pi ] .
$$

Observe that

$$
\| \Phi _ { \tilde { z } , \tilde { w } } - \Phi _ { z , w } \| & \leqslant \frac { 2 | \theta _ { \tilde { z } , \tilde { w } } - \theta _ { z , w } | } { \pi } \| I _ { n } \| + \left \| \frac { 2 \sin \theta _ { \tilde { z } , \tilde { w } } } { \pi } M _ { \tilde { z } \leftrightarrow \tilde { w } } - \frac { 2 \sin \theta _ { z , w } } { \pi } M _ { z \leftrightarrow w } \right \| .
$$

First, observe that by ( 107 ), we have that

$$
| \theta _ { \tilde { z } , \tilde { w } } - \theta _ { z , w } | \leqslant | \theta _ { \tilde { z } , \tilde { w } } - \theta _ { z , \tilde { w } } | + | \theta _ { z , \tilde { w } } - \theta _ { z , w } | \leqslant | \theta _ { \tilde { z } , z } | + | \theta _ { \tilde { w } , w } | .
$$

Then, by ( 108 ) and ( 109 ), we have that

$$
| \theta _ { \tilde { z } , z } | \leqslant 4 \sin ( \theta _ { \tilde { z } , z } / 2 ) \leqslant 2 \| \tilde { z } - z \| \leqslant 2 \varepsilon .
$$

[Page 56]

The same upper bound holds for | θ ˜ w,w | . Thus we attain

$$
| \theta _ { \tilde { z } , \tilde { w } } - \theta _ { z , w } | \leqslant | \theta _ { \tilde { z } , z } | + | \theta _ { \tilde { w } , w } | \leqslant 4 \varepsilon .
$$

Let R be a rotation matrix that maps z  → e 1 and w  → cos θ z,w e 1 + sin θ z,w e 2 where e 1 and e 2 are the ﬁrst and second standard basis vectors, respectively. Let ˜ R denote the matrix that applies the same rotatation to the system ˜ z and ˜ w . Recall that M z ↔ w := R T DR and M ˜ z ↔ ˜ w := ˜ R T ˜ D ˜ R where

$$
D \coloneqq \left [ \begin{array} { c c c c } \cos \theta _ { z , w } & \sin \theta _ { z , w } & 0 \\ \sin \theta _ { z , w } & - \cos \theta _ { z , w } & 0 \\ 0 & 0 & 0 _ { k - 2 } \end{array} \right ] \text { and } \tilde { D } \coloneqq \left [ \begin{array} { c c c c } \cos \theta _ { \bar { z } , \bar { w } } & \sin \theta _ { \bar { z } , \bar { w } } & 0 \\ \sin \theta _ { \bar { z } , \bar { w } } & - \cos \theta _ { \bar { z } , \bar { w } } & 0 \\ 0 & 0 & 0 _ { k - 2 } \end{array} \right ] .
$$

An elementary calculation shows that D has 2 pairs of non-zero eigenvalues and eigenvectors ( λ 1 ,d 1 ) and ( λ 2 ,d 2 ) where

while Note that since z, w, ˜ z, ˜ w ∈ S n -1 , ‖ h ‖ , ‖ ˜ h ‖ , ‖ g ‖ , ‖ ˜ g ‖ glyph[lessorequalslant] 2 . In addition,

$$
\lambda _ { 1 } = - 1 \text { and } d _ { 1 } = ( \cos \theta _ { z , w } - 1 ) e _ { 1 } + \sin \theta _ { z , w } e _ { 2 }
$$

$$
\lambda _ { 2 } = 1 \text { and } d _ { 2 } = ( \cos \theta _ { z , w } + 1 ) e _ { 1 } + \sin \theta _ { z , w } e _ { 2 } .
$$

Let D = − d 1 d T 1 + d 2 d T 2 be the eigenvalue decomposition for D . Then by the deﬁnition of M z ↔ w ,

$$
M _ { z \leftrightarrow w } = R ^ { T } D R = - R ^ { T } d _ { 1 } d _ { 1 } ^ { T } R + R ^ { T } d _ { 2 } d _ { 2 } ^ { T } R = \colon - v _ { 1 } v _ { 1 } ^ { T } + v _ { 2 } v _ { 2 } ^ { T }
$$

so v 1 := R T d 1 and v 2 := R T d 2 are the eigenvectors of M z ↔ w with corresponding eigenvalues − 1 and 1 , respectively. Then, recall that Rz = e 1 while Rw = cos θ z,w e 1 +sin θ z,w e 2 . Thus the eigenvectors d 1 and d 2 can be written as

$$
d _ { 1 } = R w - R z \text { and } d _ { 2 } = R w + R z .
$$

Thus the eigenvectors of M z ↔ w are precisely

$$
v _ { 1 } = w - z \text { and } v _ { 2 } = w + z .
$$

By the same argument, the eigenvectors of M ˜ z ↔ ˜ w are

$$
\tilde { v } _ { 1 } = \tilde { w } - \tilde { z } \text { and } \tilde { v } _ { 2 } = \tilde { w } + \tilde { z }
$$

with corresponding eigenvalues − 1 and 1 , respectively. Hence, we have that

and likewise

$$
\frac { 2 \sin \theta _ { z , w } } { \pi } M _ { z \leftrightarrow w } & = \frac { 2 \sin \theta _ { z , w } } { \pi } \left ( - v _ { 1 } v _ { 1 } ^ { T } + v _ { 2 } v _ { 2 } ^ { T } \right ) \\ & = \frac { 2 \sin \theta _ { z , w } } { \pi } \left ( - ( w - z ) ( w - z ) ^ { T } + ( w + z ) ( w + z ) ^ { T } \right )
$$

$$
\frac { 2 \sin \theta _ { \tilde { z } , \tilde { w } } } { \pi } M _ { \tilde { z } \leftrightarrow \tilde { w } } = \frac { 2 \sin \theta _ { \tilde { z } , \tilde { w } } } { \pi } \left ( - ( \tilde { w } - \tilde { z } ) ( \tilde { w } - \tilde { z } ) ^ { T } + ( \tilde { w } + \tilde { z } ) ( \tilde { w } + \tilde { z } ) ^ { T } \right ) .
$$

For simplicity of notation, let h = w − z , ˜ h = ˜ w − ˜ z , g = w + z , and ˜ g = ˜ w + ˜ z . Then

$$
\left \| \frac { 2 \sin \theta _ { z , w } } { \pi } M _ { z \leftrightarrow w } - \frac { 2 \sin \theta _ { z , \tilde { w } } } { \pi } M _ { \tilde { z } \leftrightarrow \tilde { w } } \right \| & = \frac { 2 } { \pi } \left \| \sin \theta _ { z , w } \left ( - h h ^ { T } + g g ^ { T } \right ) + \sin \theta _ { \tilde { z } , \tilde { w } } \left ( \tilde { h } \tilde { h } ^ { T } - \tilde { g } \tilde { g } ^ { T } \right ) \right \| \\ & \leqslant \frac { 2 } { \pi } \left ( \| \sin \theta _ { z , w } h h ^ { T } - \sin \theta _ { \tilde { z } , \tilde { w } } \tilde { h } ^ { T } \| + \| \sin \theta _ { z , w } g g ^ { T } - \sin \theta _ { \tilde { z } , \tilde { w } } \tilde { g } ^ { T } \| \right ) .
$$

[Page 57]

and ( 110 ) implies

$$
\| h - \tilde { h } \| \leqslant \| z - \tilde { z } \| + \| w - \tilde { w } \| \leqslant 2 \varepsilon
$$

$$
| \sin \theta _ { z , w } - \sin \theta _ { \tilde { z } , \tilde { w } } | \leqslant | \theta _ { z , w } - \theta _ { \tilde { z } , \tilde { w } } | \leqslant 4 \varepsilon .
$$

Hence

$$
\| \sin \theta _ { z , w } h h ^ { T } - \sin \theta _ { \bar { z } , \tilde { w } } \tilde { h } \tilde { T } \| & \leq \| \sin \theta _ { z , w } h h ^ { T } - \sin \theta _ { z , w } h \tilde { h } ^ { T } \| + \| \sin \theta _ { z , w } h \tilde { h } ^ { T } - \sin \theta _ { z , w } \tilde { h } \tilde { h } ^ { T } \| \\ & + \| \sin \theta _ { z , w } \tilde { h } \tilde { h } ^ { T } - \sin \theta _ { \bar { z } , \tilde { w } } \tilde { h } \tilde { h } ^ { T } \| \\ & \leq | \sin \theta _ { z , w } \| \| h \| - \tilde { h } \| + | \sin \theta _ { z , w } \| | \tilde { h } \| h - \tilde { h } \| + \| \tilde { h } \tilde { h } ^ { T } \| \sin \theta _ { z , w } - \sin \theta _ { \bar { z } , \tilde { w } } | \\ & \leq 2 0 \epsilon .
$$

The same bound holds for   sin θ z,w gg T − sin θ ˜ z, ˜ w ˜ g ˜ g T   . Hence we attain

$$
\left \| \frac { 2 \sin \theta _ { z , w } } { \pi } M _ { z \leftrightarrow w } - \frac { 2 \sin \theta _ { \tilde { z } , \tilde { w } } } { \pi } M _ { \tilde { z } \leftrightarrow \tilde { w } } \right \| \leqslant \frac { 8 0 } { \pi } \varepsilon .
$$

Combining ( 110 ) and ( 111 ), we see that

$$
\| \Phi _ { \tilde { z } , \tilde { w } } - \Phi _ { z , w } \| \leqslant \frac { 2 | \theta _ { \tilde { z } , \tilde { w } } - \theta _ { z , w } | } { \pi } \| I _ { n } \| + \left \| \frac { 2 \sin \theta _ { \tilde { z } , \tilde { w } } } { \pi } M _ { \tilde { z } \leftrightarrow \tilde { w } } - \frac { 2 \sin \theta _ { z , w } } { \pi } M _ { z \leftrightarrow w } \right \| \leqslant \frac { 8 8 } { \pi } \varepsilon .
$$

$$
\| l \| -
$$

We now prove the inequalities used in the proof of Lemma 27 .

Proof of equations ( 107 ) , ( 108 ) , and ( 109 ) . For ( 107 ), we proceed similarly to the proof on page 12 of [ 22 ]. Observe that we can write

$$
x _ { 1 } = \cos \theta _ { x _ { 1 } , y } y + \sin \theta _ { x _ { 1 } , y } y _ { 1 } ^ { \perp }
$$

and Similarly, θ x 2 ,y -θ x 1 ,y glyph[lessorequalslant] θ x 1 ,x 2 so we attain | θ x 1 ,y -θ x 2 ,y | glyph[lessorequalslant] | θ x 1 ,x 2 | . For (108), observe that for x, y ∈ S n -1 ,

$$
x _ { 2 } = \cos \theta _ { x _ { 2 } , y } y + \sin \theta _ { x _ { 2 } , y } y _ { 2 } ^ { \perp }
$$

where y ⊥ 1 and y ⊥ 2 are unit vectors that are orthogonal to y . Then observe that

$$
\langle x _ { 1 } , x _ { 2 } \rangle & = \langle \cos \theta _ { x _ { 1 } , y } y + \sin \theta _ { x _ { 1 } , y } y _ { 1 } ^ { \perp } , \cos \theta _ { x _ { 2 } , y } y + \sin \theta _ { x _ { 2 } , y } y _ { 2 } ^ { \perp } \rangle \\ & = \cos \theta _ { x _ { 1 } , y } \cos \theta _ { x _ { 2 } , y } + \sin \theta _ { x _ { 1 } , y } \sin \theta _ { x _ { 2 } , y } \langle y _ { 1 } ^ { \perp } , y _ { 2 } ^ { \perp } \rangle .
$$

Since θ x 1 ,y ,θ x 2 ,y ∈ [0 ,π ] , we have that sin θ x 1 ,y sin θ x 2 ,y   0 . In addition,   y ⊥ 1 ,y ⊥ 2       y ⊥ 1    y ⊥ 2   = 1 so we attain

$$
\langle x _ { 1 } , x _ { 2 } \rangle \leqslant \cos \theta _ { x _ { 1 } , y } \cos \theta _ { x _ { 2 } , y } + \sin \theta _ { x _ { 1 } , y } \sin \theta _ { x _ { 2 } , y } = \cos ( \theta _ { x _ { 1 } , y } - \theta _ { x _ { 2 } , y } )
$$

by the trigonometric identity cos( α ∓ β ) = cos α cos β ± sin α sin β. Since the function cos − 1 is decreasing on [ − 1 , 1] , we see that

$$
\theta _ { x _ { 1 } , y } - \theta _ { x _ { 2 } , y } \leqslant \cos ^ { - 1 } ( \langle x _ { 1 } , x _ { 2 } \rangle ) = \theta _ { x _ { 1 } , x _ { 2 } } .
$$

[Page 58]

$$
\| x - y \| ^ { 2 } & = \| x \| ^ { 2 } + \| y \| ^ { 2 } - 2 \langle x , y \rangle \\ & = \| x \| ^ { 2 } + \| y \| ^ { 2 } - 2 \| x \| \| y \| \cos \theta _ { x , y } \\ & = 2 ( 1 - \cos \theta _ { x , y } ) .
$$

Thus, using the half angle formula

$$
\sin \frac { \theta } { 2 } = s g n \left ( 2 \pi - \theta + 4 \pi \left \lfloor \frac { \theta } { 4 \pi } \right \rfloor \right ) \sqrt { \frac { 1 - \cos \theta } { 2 } }
$$

we see that

$$
\| x - y \| = \sqrt { 2 ( 1 - \cos \theta _ { x , y } ) } = 2 \sqrt { \frac { 1 - \cos \theta _ { x , y } } { 2 } } \geqslant 2 \sin \frac { \theta _ { x , y } } { 2 } .
$$

For ( 109 ), one can note that the function ψ ( θ ) := 4sin θ 2 − θ is positive for all θ ∈ [0 ,π ] .

# Acknowledgements

PH is partially supported by NSF CAREER Grant DMS-1848087. OL acknowledges support by the NSF Graduate Research Fellowship under Grant No. DGE-1450681.

# References

- [1] Sanjeev Arora, Yingyu Liang, and Tengyu Ma. Why are deep nets reversible: A simple theory with implications for training. CoRR , abs/1511.05653, 2015.
- [2] Benjamin Aubin, Bruno Loureiro, Antoine Maillard, Florent Krzakala, and Lenka Zdeborová. The spiked matrix model with generative priors. arXiv preprint , arXiv:1905.12385, 2013.
- [3] Sohail Bahmani and Justin Romberg. Eﬃcient compressive phase retrieval with constrained sensing vectors. Advances in Neural Information Processing Systems (NIPS 2015) , pages 523– 531, 2015.
- [4] Sohail Bahmani and Justin Romberg. Phase retrieval meets statistical learning theory: A ﬂexible convex relaxation. International Conference on Artiﬁcial Intelligence and Statistics (AISTATS) , 54:252–260, 2017.
- [5] Afonso S. Bandeira and Dustin G. Mixon. Near-optimal phase retrieval of sparse vectors. arXiv preprint , arXiv:1308.0143, 2013.
- [6] Boaz Barak, Samuel B Hopkins, Jonathan Kelner, Pravesh K Kothari, Ankur Moitra, and Aaron Potechin. A nearly tight sum-of-squares lower bound for the planted clique problem. arXiv preprint , arXiv:1604.03084, 2016.
- [7] Richard Baraniuk, Mark Davenport, Ronald DeVore, and Michael Wakin. A simple proof of the restricted isometry property for random matrices. Constructive Approximation , 28(3):253–263, 2008.


[Page 59]

- [8] Quentin Berthet and Philippe Rigollet. Computational lower bounds for sparse pca. arXiv preprint , arXiv:1304.0828, 2013.
- [9] Ashish Bora, Alexandros G. Dimakis, Ajil Jalal, and Eric Price. Compressed sensing using generative models. arXiv preprint arXiv:1703.03208 , 2017.
- [10] Tony Cai, Xiaodong Li, and Zongming Ma. Optimal rates of convergence for noisy sparse phase retrieval via thresholded wirtinger ﬂow. The Annals of Statistics , 44(5):2221–2251, 2016.
- [11] Emmanuel J. Candès and Xiaodong Li. Solving quadratic equations via phaselift when there are about as many equations as unknowns. Foundations of Computational Mathematics , 14.5:1017– 1026, 2014.
- [12] Emmanuel J. Candès, Xiaodong Li, and Mahdi Soltanolkotabi. Phase retrieval via wirtinger ﬂow: Theory and applications. IEEE Transactions on Information Theory , 61(4):195–2007, 2017.
- [13] Emmanuel J. Candès, Justin K. Romberg, and Terence Tao. Stable signal recovery from incomplete and inaccurate measurements. Communications on Pure and Applied Mathematics , 59(8):1207–1223, 2006.
- [14] Emmanuel J. Candès, Thomas Strohmer, and Vladislav Voroninski. Phaselift: Exact and stable signal recovery from magnitude measurements via convex programming. Comm. Pure Applied Math , 66(8):1241–1274, 2013.
- [15] Christian Clason. Nonsmooth analysis and optimization. arXiv preprint arXiv:1708.04180 , 2017.
- [16] Jorio Cocola, Paul Hand, and Vladislav Voroninski. Nonasymptotic guarantees for low-rank matrix recovery with generative priors. arXiv preprint arXiv:2006.07953 , 2020.
- [17] Constantinos Daskalakis, Dhruv Rohatgi, and Manolis Zampetakis. Constant-expansion suﬃces for compressed sensing with generative priors. arXiv preprint , arXiv:2006.04237, 2020.
- [18] Aurelien Decelle, Florent Krzakala, Cristopher Moore, , and Lenka Zdeborová. Asymptotic analysis of the stochastic block model for modular networks and its algorithmic applications. Physical Review E , 84(6):066106, 2011.
- [19] Yash Deshpande and Andrea Montanari. Sparse pca via covariance thresholding. Advances in Neural Information Processing Systems (NeurIPS) , pages 334–342, 2014.
- [20] David Donoho. For most large underdetermined systems of linear equations the minimal l1norm solution is also the sparsest solution. Communications on Pure and Applied Mathematics , 59(6), 2006.
- [21] David Donoho, Michael Lustig, and John M. Pauly. Sparse mri: The application of compressed sensing for rapid mr imaging. Magnetic Resonance in Medicine , 58(6):1182–1195, 2007.
- [22] S. W. Drury. Honours analysis lecture notes, mcgill university. http://www.math.mcgill.ca/ drury/notes354.pdf , 2001.
- [23] Yonina C. Eldar, Georgios B. Giannakis, and Gang Wang. Solving systems of random quadratic equations via truncated amplitude ﬂow. IEEE Transactions on Information Theory , 23(26):773–794, 2017.


[Page 60]

- [24] Albert Fannjiang and Thomas Strohmer. The numerics of phase retrieval. arXiv preprint arXiv:2004.05788 , 2020.
- [25] J.R. Fienup. Phase retrieval algorithms: A comparison. Applied Optics , 21:2758–2768, 1982.
- [26] R.W. Gerchberg and W.O. Saxton. A practical algorithm for the determination of phase from image and diﬀraction plane pictures. Optik , 35:237–246, 1972.
- [27] Tom Goldstein and Christoph Struder. Phasemax: Convex phase retrieval via basis pursuit. arXiv preprint arXiv: 1610.07531 , 2016.
- [28] Fabian Latorre Gómez, Armin Eftekhari, and Volkan Cevher. Fast and provable admm for learning with generative priors. Advances in Neural Information Processing Systems (NeurIPS) , 2019.
- [29] Ian J. Goodfellow, Jean Pouget-Abadie, Mehdi Mirza, Bing Xu, David Warde-Farley, Sherjil Ozair, Aaron Courville, and Yoshua Bengio. Generative adversarial nets. arXiv preprint arXiv:1406.2661 , 2014.
- [30] Paul Hand and Babhru Joshi. Global guarantees for blind demodulation with generative priors. Advances in Neural Information Processing Systems (NeurIPS) , 2019.
- [31] Paul Hand, Oscar Leong, and Vladislav Voroninski. Phase retrieval under a generative prior. Advances in Neural Information Processing Systems (NeurIPS) , 2018.
- [32] Paul Hand and Vladislav Voroninski. Compressed sensing from phaseless gaussian measurements via linear programming in the natural parameter space. arXiv preprint arXiv:1611.05985 , 2016.
- [33] Paul Hand and Vladislav Voroninski. Global guarantees for enforcing deep generative priors by empirical risk. IEEE Transactions on Information Theory , 66(1):401–418, 2019.
- [34] Reinhard Heckel, Wen Huang, Paul Hand, and Vladislav Voroninski. Rate-optimal denoising with deep neural networks. Information and Inference: A Journal of the IMA , 2020.
- [35] Wen Huang, Paul Hand, Reinhard Heckel, and Vladislav Voroninski. A provably convergent scheme for compressive sensing under random generative priors. arXiv preprint , arXiv:1812.04176, 2018.
- [36] Kishore Jaganathan, Samet Oymak, and Babak Hassibi. Sparse phase retrieval: Convex algorithms and limitations. 2013 IEEE International Symposium on Information Theory Proceedings (ISIT) , pages 1022–1026, 2013.
- [37] Gauri Jagatap and Chinmay Hegde. Sample-eﬃcient algorithms for recovering structured signals from magnitude-only measurements. Advances in Neural Information Processing Systems (NIPS 2017) , 2017.
- [38] Prateek Jain, Praneeth Netrapalli, and Sujay Sanghavi. Phase retrieval using alternating minimization. Advances in Neural Information Processing Systems (NIPS 2013) , 2013.
- [39] Tero Karras, Samuli Laine, and Timo Aila. A style-based generator architecture for generative adversarial networks. IEEE CVPR , 2019.


[Page 61]

- [40] Diederik Kingma and Jimmy Ba. Adam. Adam: A method for stochastic optimization. arXiv preprint arXiv:1412.6980 , 2014.
- [41] Diederik P. Kingma and Max Welling. Auo-encoding variational bayes. ICLR , 2014.
- [42] Robert Krauthgamer, Boaz Nadler, Dan Vilenchik, and et al. Do semideﬁnite relaxations solve sparse pca up to the information limit? The Annals of Statistics , 43(3):1300âĂŞ1322, 2015.
- [43] Yann LeCun, Leon Bottou, Yoshua Bengio, and Patrick Haﬀner. Gradient-based learning applied to document recognition. Proceedings of the IEEE , 86(11):2278–2324, 1998.
- [44] Xiaodong Li and Vladislav Voroninski. Sparse signal recovery from quadratic measurements via convex programming. SIAM Journal on Mathematical Analysis , 45(5):3019–3033, 2013.
- [45] Zhaoqiang Liu, Selwyn Gomes, Avtansh Tiwari, and Jonathan Scarlett. Sample complexity bounds for 1-bit compressive sensing and binary stable embeddings with generative priors. Proceedings of the 37th International Conference on Machine Learning (ICML) , 2020.
- [46] Shervin Minaee and Amirali Abdolrashidi. Finger-gan: Generating realistic ﬁngerprint images using connectivity imposed gan. arXiv preprint , arXiv:1812.10482, 2018.
- [47] Harry Nyquist. Certain topics in telegraph transmission theory. Trans. AIEE. , 47(2):617–644, 1928.
- [48] Anton Osokin, Anatole Chessel, Rafael E. Carazo Salas, and Federico Vaggi. Gans for biological image synthesis. arXiv preprint , arXiv:1708.04692, 2017.
- [49] Samet Oymak, Amin Jalali, Maryam Fazel, Yonina C. Eldar, and Babak Hassibi. Simultaneously structured models with application to sparse and low-rank matrices. IEEE Transactions on Information Theory , 61(5):2886–2908, 2015.
- [50] Yaniv Plan and Roman Vershynin. One-bit compressed sensing by linear programming. Communications on Pure and Applied Mathematics , 66(8):1275–1297, 2013.
- [51] Shuang Qiu, Xiaohan Wei, and Zhuoran Yang. Robust one-bit recovery via relu generative networks: Improved statistical rates and global landscape analysis. arXiv preprint arXiv:1908.05368 , 2019.
- [52] Emile Richard and Andrea Montanari. A statistical model for tensor pca. Advances in Neural Information Processing Systems (NeurIPS) , pages 2897–2905, 2014.
- [53] Viraj Shah and Chinmay Hedge. Solving linear inverse problems using gan priors: An algorithm with provable guarantees. Proc. IEEE Int. Conf. Acoust., Speech, and Signal Processing (ICASSP) , 2018.
- [54] Fahad Shamshad and Ali Ahmed. Robust compressive phase retrieval via deep generative priors. arXiv preprint arXiv:1808.05854 , page 0, 2018.
- [55] Hoo-Chang Shin, Neil A. Tenenholtz, Jameson K. Rogers, Christopher G. Schwarz, Matthew L. Senjem, Jeﬀrey L. Gunter, Katherine Andriole, and Mark Michalski. Medical image synthesis for data augmentation and anonymization using generative adversarial networks. arXiv preprint , arXiv:1807.10225, 2018.


[Page 62]

- [56] Mahdi Soltanolkotabi. Structured signal recovery from quadratic measurements: Breaking sample complexity barriers via nonconvex optimization. IEEE Transactions on Information Theory , 65(4):2374 – 2400, 2019.
- [57] Ganlin Song, Zhou Fan, and John Laﬀerty. Surﬁng: Iterative optimization over incrementally trained deep networks. Advances in Neural Information Processing Systems (NeurIPS) , pages 15034–15043, 2019.
- [58] Ju Sun, Qing Qu, and John Wright. A geometric analysis of phase retrieval. Information Theory (ISIT), 2016 IEEE International Symposium , pages 2379–2383, 2016.
- [59] Andrey Nikolayevich Tikhonov. On stability of inverse problems. Dokl. Akad. Nauk SSSR , 39(5):176–179, 1943.
- [60] Roman Vershynin. Introduction to the non-asymptotic analysis of random matrices. Compressed Sensing: Theory and Applications , Cambridge University Press, 2012.
- [61] Vladislav Voroninski and Zhiqiang Xu. A strong restricted isometry property with an application to phaseless compressed sensing. arXiv preprint , arxiv:1404.3811, 2014.
- [62] Irène Waldspurger. Phase retrieval with random gaussian sensing vectors by alternating projections. IEEE Transactions on Information Theory , 64(5):3301–3312, 2018.
- [63] Irène Waldspurger, Alexandre d’Aspremont, and Stéphane Mallat. Phase recovery, maxcut and complex semideﬁnite programming. Mathematical Programming , 149(1-2):47–81, 2015.
- [64] Gang Wang, Liang Zhang, Georgios B. Giannakis, Mehmet Akçakaya, and Jie Chen. Sparse phase retrieval via truncated amplitude ﬂow. IEEE Transactions on Signal Processing , 66:479– 491, 2018.
- [65] Zhou Wang, A.C. Bovik, H.R. Sheikh, and E.P. Simoncelli. Image quality assessment: from error visibility to structural similarity. IEEE transactions on image processing , 13(4):600–612, 2004.
- [66] James G. Wendel. A problem in geometric probability. Math. Scand. , 11:109–111, 1962.
- [67] Huishuai Zhang, Yingbin Liang, and Yuejie Chi. A nonconvex approach for phase retrieval: Reshaped wirtinger ﬂow and incremental algorithms. Journal of Machine Learning Research (JMLR) , 18(141):1–35, 2017.


