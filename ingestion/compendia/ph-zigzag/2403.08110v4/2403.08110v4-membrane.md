# C OMPUTING G ENERALIZED R ANKS OF P ERSISTENCE M ODULES VIA U NFOLDING TO Z IGZAG M ODULES

A P REPRINT

Tamal K. Dey ∗ tamaldey@purdue.edu

Department of Computer Science Purdue University

A BSTRACT

For a persistence module M indexed by a finite poset P , its generalized rank is defined as the rank of the limit-to-colimit map for the diagram of constituent vector spaces and linear maps of M over the poset P . For 2 -parameter persistence modules, a zigzag persistence based algorithm has been proposed that takes advantage of the fact that generalized rank for 2 -parameter modules is equal to the number of full intervals in a zigzag module defined on the ‘boundary’ of the poset. Analogous definition of boundary for d -parameter persistence modules or general poset indexed persistence modules does not seem plausible. To overcome this difficulty, we first unfold a given module M into a zigzag module M ZZ and then check how many full interval modules in a decomposition of M ZZ can be folded back to remain full in a decomposition of M . This number determines the generalized rank of M . We give an efficient algorithm to compute this number for the case when M is induced by a filtration of a simplicial complex over a finite poset. For special cases where M is the degreed homology functor on a filtration of a d -complex, we obtain a more efficient algorithm including a linear time algorithm for degree1 homology for graphs.

## Contents

- [1 Introduction](#1-introduction)
- [2 Preliminaries and idea](#2-preliminaries-and-idea)
- [2.1 Persistence modules](#21-persistence-modules)
- [2.2 Limits and colimits and generalized rank](#22-limits-and-colimits-and-generalized-rank)
- [2.3 Idea of unfolding to zigzag module](#23-idea-of-unfolding-to-zigzag-module)
- [3 Folding and Unfolding](#3-folding-and-unfolding)
- [4 Limit and complete modules](#4-limit-and-complete-modules)
- [4.1 Limit modules](#41-limit-modules)
- [4.2 Complete modules](#42-complete-modules)
- [5 Zigzag module](#5-zigzag-module)
- [5.1 Unfolding to a zigzag path and zigzag module](#51-unfolding-to-a-zigzag-path-and-zigzag-module)
- [6 Algorithm](#6-algorithm)
- [6.1 Convertibility of I and computing Convert( I )](#61-convertibility-of-i-and-computing-convert-i)
- [6.2 Invertibility of I](#62-invertibility-of-i)
- [7 Special case of degreed homology for d -complexes](#7-special-case-of-degreed-homology-for-d--complexes)
- [8 Conclusions and discussions](#8-conclusions-and-discussions)
- [Acknowledgment.](#acknowledgment)
- [References](references/2403.08110v4.md)

## 1 Introduction

It is well known that one-parameter persistence modules decompose into interval modules whose supports constitute persistence diagram , or equivalently barcode of the given module, a fundamental object of study in topological data analysis [ 6 , 17 , 19 , 33 ]. There are many situations in which persistence modules are parameterized over more than one parameter [ 5 , 9 , 20 , 27 , 31 ]. Unfortunately, such multiparameter persistence modules do not necessarily admit a nice decomposition into intervals only. Instead, they may decompose into indecomposables that are more complicated [ 9 ]. To overcome this difficulty, inspired by the work of Patel [ 34 ], Kim and Mémoli [ 25 ] proposed a decomposition of poset-indexed modules (satisfying some mild condition) into signed interval modules. In analogy to the oneparameter case, the supports of these signed interval modules with multiplicity are called the signed barcode of the given module [ 3 ]. The multiplicity of the intervals is given by the Möbius inversion of a rank-invariant function; see [ 25 , 34 ]. Botnan, Oppermann, and Oudot [ 3 ] recently showed that a unique minimal signed barcode of a given persistence module in terms of rectangles can be computed efficiently and raised the question of efficient computation of other types of signed barcodes or invariants; see the recent work in [ 12 ] for more exposition along this direction.

At the core of computing these signed barcodes for a persistence module M is the problem of computing the generalized rank for an interval I that is defined as the rank of the limit-to-colimit map for the diagram of vector spaces of the restricted module $M | I$ ; see [ 25 ]. Recently, Dey, Kim, Mémoli [ 16 ] showed that, for a 2 -parameter persistence module, this generalized rank is given by the number of full intervals in the decomposition of a zigzag module M ZZ that is a restriction of M . This becomes immediately useful because there are efficient algorithms known for computing the barcode of a zigzag module [ 14 , 15 , 30 , 32 ]. However, this result is limited to 2 -parameter persistence modules because the zigzag module needs to be defined on the boundary of a “two dimensional” interval. Beyond the 2 -

∗ This research is supported by NSF grants CCF 2049010 , DMS 2301360 , and CCF 2437030

In this paper, we address the above problem and present an algorithm to compute generalized rank efficiently for finitedimensional modules indexed by finite posets. The approach uses the idea of straightening up the input persistence module into a module defined over a zigzag path. We call the process unfolding the module. We compute a decomposition of the resulting zigzag module into interval modules using a known algorithm. Then, we design an algorithm that aims to fold the full interval modules (supported on entire zigzag path) in the decomposition of the zigzag module back to the original module. The ones which fold successfully to a full interval summand (supported on the entire poset) in the original module gives us the generalized rank according to a result of Chambers and Letscher [ 11 ].

A viable approach to compute the generalized rank would be to compute the limit and colimit of a given persistence module separately, say with the recent algorithm in [ 35 ], and then compute the rank of the limit-to-colimit map. Finding an efficient implementation of this approach remains open. Each of the computations for limit, colimit, and then the rank of the map between them may incur considerable computational cost in practice. Our approach also has three distinct computations, unfolding the module, then computing a zigzag persistence followed by a folding process. Among these, the first one is done by a simple graph traversal, the second one can be done with recent efficient practical zigzag persistence algorithms [ 14 , 15 ]. The folding process is the only costly step for which we provide an efficient algorithm.

One could also argue that a full decomposition algorithm such as the one in [ 18 ] or the well-known Meataxe algorithm can be used to compute the generalized rank because they compute all full interval summands. However, the algorithm in [ 18 ] does not work for nondistinctly graded modules and the Meataxe algorithm has a high time complexity ( O ( t 18 ) , t is the maximum of poset and filtration sizes), as pointed out in [ 18 ]). Furthermore, these algorithms expect the input in matrix forms (presentations or linear maps) instead of filtrations that are common in practice. We alleviate these issues; more precisely, highlights of our approach are:



• It introduces an unfolding/folding technique for P -modules, that is, finite dimensional persistence modules defined on a finite poset P , which may be of independent interest.

• It provides an algorithm that, given a simplicial P -filtration of a simplicial complex inducing a P -module M by homology functor, computes the generalized rank rk ( M ) in O ( t ω +2 ) time, ω < 2 . 373 , where the description size of P and the given filtration is at most t (see Eq. (9) and Theorem 6.2). The algorithm does not need to go through an extra step of computing a presentation of M from its inducing P -filtration. In fact, currently all published efficient algorithms for computing presentations work for 2 -parameter modules ( P ⊆ R 2 , Z 2 ) [24, 28] and not for a general P -module indexed by a finite poset P .

• It computes full interval summands of M representing its “global sections" supported on P .

• It gives a more efficient O ( t ω ) algorithm for the special cases of degree d -homology for d -complexes and a linear time algorithm for degree1 homology in graphs.

## 2 Preliminaries and idea

## 2.1 Persistence modules

We consider finite dimensional persistence modules indexed by connected finite posets.

Definition 2.1. For a poset P , let $\leq P$ denote the partial order defining it. We also treat P as a category with every $p \in P$ as its object and $\leq P$ inducing the morphisms between them. Two points $p \leq P q$ in P are called immediate and written $p \to q$ or $q \gets p$ if and only if there is no $r \in P , r$ ̸∈ { p,q } , satisfying $p \leq P r \leq P q$ . We also write $p ↔ q$ to denote that either $p \to q$ or $p \gets q$ .

Definition 2.2. An interval I of a poset P is a non-empty subset $I \subseteq P$ so that (i) I is convex with the partial order of P , that is, if $p,q \in I$ and $p \leq P r \leq P q$ , then $r \in I$ ; (ii) I is connected , that is, for any $p,q \in I$ , there is a sequence $p = p 0$ ,p 2 , ··· ,p $m = q$ of elements of I with $p i ↔ p i$ +1 for $i \in{0 , \cdot \cdot \cdot ,m - 1}$ . Assuming P is finite and connected, P is also an interval called the the full interval .

Definition 2.3. Given a poset P , we define a P -module to be a functor $M : P \to vec F$ where vec F is the category of finite dimensional vector spaces over a fixed field F with the morphisms being the linear maps among them. For two points $p \leq P q$ , we also write $M ( p \leq P q )$ to denote the morphisms of M .

Definition 2.4. A P -module M is called indecomposable if there is no direct sum $M \sim = M 1 \oplus M 2$ so that both M 1 and M 2 are nontrivial P -modules.

Any (pointwise) finite dimensional P -module is a direct sum of indecomposables with local endomorphism ring [ 13 ]; see also [ 4 ]. Such a decomposition is essentially unique up to automorphism according to Azumaya-Krull-RemakSchmidt theorem [ 1 ]: (Also see [ 26 , Theorem 1.11 ]).

Theorem 2.1. Every P -module has a unique decomposition up to isomorphism $M \sim = M 1 \oplus$ ··· $\oplus M k$ where each $M_{i}$ is an indecomposable module.

For a decomposable module M , there exist submodules $M_{i} , i \in [ k ]$ , of M with inclusions $j i : M_{i} \to M$ so that $( j i : M_{i} \to M ) i \in [ k ]$ make M the direct sum of $M_{i}$ s written as $M = M 1 \oplus$ ··· $\oplus M k$ . In light of this, we replace the isomorphism in Theorem 2.1 $M \sim = M 1 \oplus$ ··· $\oplus M k$ with equality $M = M 1 \oplus$ ··· $\oplus M k$ where each $M_{i}$ is indecomposable and is a submodule of M ; see Figure 1. We call such a decomposition an internal direct decomposition or simply direct decomposition denoted D := D ( M ) . We treat D as the set ${M_{i}} i \in [ k ]$ of indecomposables if there is no confusion. Notice that the uniqueness of such a decomposition is up to automorphisms of M (and permutations of $M_{i}$ s). This aspect plays an important role in our algorithm to follow.

*Figure 1: (left) A P -module with vector spaces generated by bases shown at the points of P (arrows represent the partial order) and internal maps shown as matrices; (right) a direct decomposition that contains two full interval modules (top,middle) and another interval module (bottom) which is not full.*

For an interval I of P , any module $I_{I} : P \to vec F$ is an interval module if:

$$
I^{I} ( p ) \cong \begin{cases} F & \text{if} p \in I , \\ 0 & \text{if} I^{I} ( p \leq_{P} q ) \cong \begin{cases} \text{id}_{F} & \text{if} p , q \in I , \, p \leq_{P} q , \\ 0 & \text{otherwise} . \end{cases} \end{cases}
$$

Definition 2.5. An interval module $I = I_{p}$ ( $I ( p ) ̸ = 0$ for any $p \in P$ ) is a full interval module (Figure 1 ).̸



## 2.2 Limits and colimits and generalized rank

We recall the notions of limit and colimit from category theory [ 29 ]. Although it is known that limit and colimit may not exist for all functors, they do exist for functors defined on finite posets, which is the case we consider. The following definitions are reproduced from [ 16 ]. Let C denote a category in the following definitions.

Definition 2.6 (Cone) . Let $F : P \to C$ be a functor. A cone over F is a pair $( L, ( \pi p ) p \in P )$ consisting of an object L in C and a collection $( \pi p ) p \in P$ of morphisms $\pi p : L \to F ( p )$ that commute with the arrows in the diagram of F , i.e. if $p \leq q$ in P , then $\pi q = F ( p \leq q ) ◦ \pi p$ in C , i.e. the diagram below commutes.

$$
F ( p ) \xrightarrow{F ( p \leq q )} F ( q ) \\ \searrow_{\pi_{p}} \nearrow_{L} \nearrow_{\pi_{q}}
$$

The cone $( L, ( \pi p ) p \in P )$ over F sometimes is denoted simply by L , suppressing the morphisms $( \pi p ) p \in P$ if it causes no confusion. A limit of $F : P \to C$ is an object in the collection of all cones over F that is terminal:

Definition 2.7 (Limit) . Let $F : P \to C$ be a functor. A limit of F is a cone over F , denoted by $( lim F, ( \pi p ) p \in P )$ or simply lim F , with the following (universal) terminal property: For any cone $L ' , ( \pi ' p ) p \in P$ of F , there is a unique morphism $u : L ' \to lim F$ such that $\pi ' p = \pi p ◦ u$ for all $p \in P$ .

Cocones and colimits are defined in a dual manner:

Definition 2.8 (Cocone) . Let $F : P \to C$ be a functor. A cocone over F is a pair $( C, ( i_{p} ) p \in P )$ consisting of an object C in C and a collection $( i_{p} ) p \in P$ of morphisms $i_{p} : F ( p ) \to C$ that commute with the arrows in the diagram of F , i.e. if $p \leq q$ in P , then $i_{p} = i_{q} ◦ F ( p \leq q )$ in C , i.e. the diagram below commutes.

$$
F ( p ) \xrightarrow{i_{p}} F ( q )
$$

In Definition 2.8 , a cocone $( C, ( i_{p} ) p \in P )$ over F is sometimes denoted simply by C , suppressing the collection $( i_{p} ) p \in P$ of morphisms. A colimit of $F : P \to C$ is an initial object in the collection of cocones over F :

Definition 2.9 (Colimit) . Let $F : P \to C$ be a functor. A colimit of F is a cocone, denoted by ( colim F, $( i_{p} ) p \in P$ ) or simply colim F , with the following initial property: If there is another cocone $C ' , ( i ' p ) p \in P$ of F , then there is a unique morphism u : colim $F \to C '$ such that $i ' p = u ◦ i_{p}$ for all $p \in P$ .

The following proposition gives a standard way of constructing a limit of a P -module M . See for example [ 16 , 25 ]).

Notation 2.1. Let $p,q \in P$ and let $v_{p} \in M ( p )$ and $v_{q} \in M ( q )$ . We write $v_{p} \sim v_{q}$ if p and q are comparable, and either $v_{p}$ is mapped to $v_{q}$ via $M ( p \leq P q )$ or $v_{q}$ is mapped to $v_{p}$ via $M ( q \leq P p )$ .

Proposition 2.1. (i) The limit of M is (isomorphic to) the pair $( W, ( \pi p ) p \in P )$ described as follows:

$$
W \coloneqq \begin{cases} ( v_{p} )_{p \in P} \in \bigoplus_{p \in P} M ( p ) \colon \forall p \leq q \, i n \, P , \, v_{p} \sim v_{q} \, \} \\ \end{cases}
$$

and for each $p \in P$ , the map $\pi p : W \to M ( p )$ is the canonical projection. An element of W is called a global section of M .

(ii) The colimit of M is (isomorphic to) the pair $( U, ( i_{p} ) p \in P )$ described as follows: For $p \in P$ , let the map $j p : M ( p ) \to p \in P M ( p )$ be the canonical injection. The space U is the quotient space $p \in P M ( p )$ /T , where T is the subspace of $p \in P M ( p )$ which is generated by the vectors of the form $j p ( v_{p} ) - j q ( v_{q} ) , v_{p} \sim v_{q}$ , the map $i_{p} : M ( p ) \to U$ is the composition $\rho ◦ j p$ , where $\rho$ is the quotient map $p \in P M ( p ) \to U$ .

These definitions imply that, for every $p \leq P q$ in $P , M ( p \leq P q ) ◦ \pi p = \pi q$ and $i_{q} ◦ M ( p \leq P q ) = i_{p}$ , which in turn imply $i_{p} ◦ \pi p = i_{q} ◦ \pi q : L \to C$ for any $p,q \in P$ .

Definition 2.10 ([ 25 ]) . The canonical limit-to-colimit map $\psi M : lim M \to$ colim M is the linear map $i_{p} ◦ \pi p$ for any $p \in P$ . The generalized rank of M is rk ( M ) := rank( $\psi M$ ) .

The following result allows us to compute rk ( M ) as the number of the full interval modules in a direct decomposition of M .

Theorem 2.2 ([ 11 , Lemma 3.1 ]) . The rank rk ( M ) is equal to the number of full interval modules in a direct decomposition of M .

## 2.3 Idea of unfolding to zigzag module

The overall idea of our approach is to 'straighten up' the given P -module M into a zigzag module M ZZ which is a zigzag module defined over a linear poset P ZZ . It is well known that a zigzag module like M ZZ decomposes into interval modules and efficient algorithms for computing them exist. After computing these interval modules, we attempt to fold back the full interval modules in this decomposition of M ZZ to full interval summands of the original module M . We know that full interval modules that are summands of M unfolds into full interval modules that are summands of M ZZ . However, the converse may not hold, that is, not all full interval summands of M ZZ fold back to full interval summands of M . So, the main challenge becomes to determine which full interval modules in the computed decomposition of M ZZ do indeed fold back (possibly with some modifications) to full interval summands of M .



Figure 2 illustrates this idea. The module M is defined on a four point poset $P ={A,B,C,D}$ shown on left. Bases of the vector spaces are shown in open brackets ‘ () ’ and linear maps in these bases are shown as matrices. The poset is straightened into a zigzag path $A \to D \to C \gets D \gets B$ . One way to look at this straightening is to view P as a directed graph and to traverse all its vertices and edges sequentially possibly with repetition. Starting from the vertex A and moving to an adjacent node disregarding the direction while noting down the visited node and the directed edge produces the zigzag path. This process of unfolding a poset into a zigzag path is formalized in section 5.1. The module M is unfolded into the zigzag module M ZZ by copying the vector spaces and linear maps at vertices and edges respectively into their unfolded versions. For the module shown on the top, we get three interval modules (bars) in a decomposition of M ZZ , the full interval module supported on [ A,B ] and the other two supported on two copies of D respectively. Bases of one dimensional vector spaces for the interval modules are indicated beneath them. When we fold back P ZZ to P (reversing the process of unfolding) sending M ZZ to M , the full interval module does fold back to a summand that is a full interval module because the vectors $v 1 + v 2 2$ at two copies of D are the same and hence map to the same vector in M ( D ) . The other two interval modules supported on single-points in the decomposition of M ZZ also fold back to a summand of M supported on a single point D where M ( D ) is generated by the vector v 2 .

*Figure 2: Unfolding two modules (defined over Z 2 ) on the same poset into zigzag modules; hollow circles represent zero vectors. (top row) Decomposition of the zigzag module folds back to two interval modules, one with support on the entire poset giving rise to a generalized rank equal to one, and the other with support on D . (bottom row) Full interval module in the decomposition of the zigzag module does not fold back to a full interval module (attempted folding shows the discrepancy at point D ), giving rise to generalized rank equal to zero.*

The case for the module shown in the bottom row of Figure 2 is not the same. In this case, M ZZ also has the barcode consisting of the same three intervals, but the corresponding interval modules are not the same. The full interval module in this case has different vector spaces spanned by v 1 and v 2 respectively at the two copies of D . Thus, this interval module does not fold into a full interval submodule of M as an attempt on right indicates. We can determine such full interval modules in a decomposition of the zigzag module by checking if the vectors at the copied vertices are the same or not. However, even if this check succeeds, the interval module may not fold to a summand of M . Figure 3 (top and bottom) shows such examples. On the other hand, even if this check fails, it may be possible to change the full interval module to have the vectors to be the same at the copied vertices. Figure 4 in section 4.2 illustrates such an example. Determining such cases and taking actions accordingly are key aspects of our algorithm.

*Figure 3: (left) M , (right) M ZZ : (top) the blue interval module is foldable whereas its complement (red) is not invertible, so neither folds to a summand of M ; (bottom) the top gray interval module is foldable and its complement is foldable (hence invertible), so the gray interval module folds to a summand of M . The blue interval module is foldable, but its complement is not invertible (foldable), so it does not fold to a summand in M .*

*Figure 4: The full red interval module in M ZZ as shown in top-middle does not fold to a full interval submodule of M because the vectors at D mismatch (top-right), but after adding the blue interval module (limit module), it is converted to a full interval module as shown in bottom-middle that folds back to a full interval module (pink) that is a summand of M (bottom-right).*

2 this is a vector addition, not a direct sum

## 3 Folding and Unfolding

As already indicated, our algorithm operates on two main constructs called folding and unfolding of persistence modules. In this section, we introduce these concepts formally for general modules and then later specialize them to zigzag modules for the algorithm.

Definition 3.1. Let Q be a finite poset. A poset Fld s Q is a folded poset of Q if there exists a surjection $s : Q \to$ Fld s Q , which (i) preserves order, that is, $p \leq Q_{Q}$ only if $s ( p ) \leq$ Fld s Q s ( q ) for all $p,q \in Q$ , and (ii) surjects also on the Hasse diagram of Fld s Q , that is, for every immediate pair $u \to v$ in Fld s Q , there is a pair $p \leq Q_{Q}$ where $s ( p ) = u$ and $s ( q ) = v$ . We say s is a folding of Q and Q is an unfolded poset of Fld s Q .

Viewing posets as categories, a folding $s : Q \to$ Fld s Q can be viewed as a functor from Q to Fld s Q .

Definition 3.2. Let $P =$ Fld s Q and $M : P \to vec F$ be a P -module and $N : Q \to vec F$ be a Q -module. We say M is an s -folding of N ( N s -folds or simply folds into M ) and N is an s -unfolding of M ( M s -unfolds or simply unfolds into N ) if there is a folding $s : Q \to P$ so that

$$
N ( q ) = M ( s ( q ) ) \, \text{(equality as sets)} \, \forall q \in Q \, ; \, N ( p \leq_{Q} q ) = M ( s ( p ) \leq_{P} s ( q ) ) \, \forall ( p \leq_{Q} q ) .
$$

We write $M =$ Fld s ( N ) and $N =$ Fld $- 1 s ( M )$ .

Remark 3.1. Observe that for a given folding $s : Q \to P , a P$ -module M always has an induced s -unfolding M ◦ s by pre-composition with s . However, for a given Q -module N , an s -folding may not exist because it may happen that $N ( q ) ̸ = N ( q ' )$ where $s ( q ' ) = s ( q )$ , or $N ( q 1 \to q 2 ) ̸ = N ( q ' 1 \to q ' 2 )$ where $s ( q 1 ) = s ( q ' 1 )$ and $s ( q 2 ) = s ( q ' 2 )$ .

An interesting and important fact is that two isomorphic modules may have different s -foldings. Figure 2 shows such an example. Two zigzag modules shown in the middle are isomorphic (barcodes are the same), but they are not exactly the same as modules (even though vector spaces are pointwise equal, morphisms are not). So, even if they are isomorphic, they fold to different modules as shown in left. Nevertheless, if a folding exists, a module necessarily folds to a unique module as Proposition 3.1 states.

Definition 3.3. Let M 1 be a summand of M . A complement of M 1 denoted M 1 is a summand so that $M = M 1 \oplus M 1$ . Observe that M 1 is not necessarily unique though for a given decomposition, it is uniquely identified .

Definition 3.4. For a folding $s : Q \to$ Fld s ( Q ) and a Q -module N , we say N is s foldable ( or simply foldable ) if $N ( q ) = N ( q ' )$ for every pair $q,q ' \in Q$ where $s ( q ) = s ( q ' )$ .

We weaken the condition of foldability to invertibility in the following definition. Definition 3.5. For a folding $s : Q \to$ Fld s ( Q ) and a Q -module N , a summand N ′ in a decomposition $N = N ' \oplus N$ ′′ is called invertible if there exists a foldable summand Q of N so that $N = Q \oplus N$ ′′ .

Foldability of a summand and its complement of a module guarantees that they both remain summands after the folding of the module. However, we can weaken the requirement on foldability for the complement. If foldability holds for a summand then it remains a summand after the folding if and only if its complement is invertible. These are the statements of the following theorem.

Theorem 3.1. Let M be a P -module and N be a Q -module where $M =$ Fld s ( N ) for some folding $s : Q \to P$ .



1. If N = N 1 ⊕ N 1 and both N 1 and N 1 are foldable, then Fld s ( N 1 ) and Fld s ( N 1 ) exist and M = Fld s ( N 1 ) ⊕ Fld s ( N 1 ) .

2. Conversely, if M = M 1 ⊕ M 1 , then Fld − 1 s ( M 1 ) and Fld − 1 s ( M 1 ) necessarily exist and N = Fld − 1 s ( M 1 ) ⊕ Fld − 1 s ( M 1 ) .

3. If N = N 1 ⊕ N 1 where N 1 is foldable, then Fld s ( N 1 ) necessarily exists and is a summand of M if and only if N 1 is invertible.

In Figure 3 (bottom), the gray interval is foldable and its complement (direct sum of the red and blue intervals) is foldable. So, the gray interval and its complement fold into summands whereas in the top figure in Figure 3 , none of the interval modules folds into a summand because even if the blue one is foldable, its complement red one is neither foldable nor invertible.

The following propositions help proving Theorem 3.1.

Proposition 3.1. For a Q -module N and a folding $s : Q \to P$ , if Fld s ( N ) exists, then Fld s ( N ) is unique.



Proof. If there were two modules M 1 and M 2 that are s -foldings of N , then both $M 1 ( p ) = M 2 ( p ) = N ( s - 1 ( p ))$ for every $p \in P$ . Furthermore, $M 1 ( p \leq P p ' ) = M 2 ( p \leq P p ' ) = N ( s - 1 ( p ) \leq Q s - 1 ( p ' )) \forall ( p \leq P p ' )$ by definition. This immediately shows that $M 1 = M 2$ .

Proposition 3.2 , Proposition 3.3 , and Proposition 3.4 below are used to prove Theorem 3.1 that characterizes modules which fold into summand modules.

Proposition 3.2 ([ 8 ]) . Let N be a submodule of a P -module M where there is a submodule N of M so that $M ( p ) = N ( p ) \oplus N ( p )$ for every $p \in P$ . Then, N is a summand of M , that is, $M = N \oplus N$ .

Proposition 3.3. Let N be a Q -module where Fld s ( N ) exists for some folding $s : Q \to P$ . Then, for any submodule $N ' \subseteq N$ that is foldable Fld s ( N ′ ) exists.

Proof. Construct a P -module M as follows: First, put $M ( p ) = N ' ( q )$ where $p = s ( q )$ . This is well defined because N ′ is foldable. Next, put $M ( p \leq P p ' ) = N ' ( q \leq Q_{Q} ' )$ where $p = s ( q )$ and $p ' = s ( q ' )$ . This is also well defined because for every pair $q \leq Q_{Q} '$ so that $p = s ( q )$ and $p ' = s ( q ' )$ , we have $N ' ( q \leq Q_{Q} ' )$ to be a restriction of $N ( q \leq Q_{Q} ' )$ where Fld s ( N ) exists. Observe that $M =$ Fld s ( N ′ ) by Definition 3.2.

Proposition 3.4. Let M be a P -module and N be a Q -module where $M =$ Fld s ( N ) for some folding $s : Q \to P$ . For any submodule $N ' \subseteq N$ , if Fld s ( N ′ ) exists, then it is a submodule of M . Conversely, if M ′ is a submodule of M , then Fld $- 1 s ( M ' )$ is a submodule of N .

Proof. We have $N ' ( q ) \subseteq N ( q )$ for $\forall q \in Q$ and $N ' ( p \leq Q_{Q} )$ is a restriction of $N ( p \leq Q_{Q} )$ on $N ' ( p ) \forall p \leq Q_{Q}$ because N ′ is a submodule of N . Then, by Definition 3.2 ,

$$
( N^{\prime} ) ( s ( a ) ) \ = \ N^{\prime} ( a )
$$

$$
\begin{aligned}
\ F l d_{s} ( N^{\prime} ) ( s ( q ) ) \ & = \ N^{\prime} ( q ) \subseteq N ( q ) = M ( s ( q ) ) \ \forall q \in Q \text{and} \\ \ F l d_{s} ( N^{\prime} ) ( s ( p ) \leq_{P} s ( q ) ) \ & = \ N^{\prime} ( p \leq_{Q} q ) = N ( p \leq_{Q} q ) |_{N^{\prime} ( p )} = M ( s ( p ) \leq_{P} s ( q ) ) |_{\L l d_{s} ( N^{\prime} ) ( s ( p ) )} ,
\end{aligned}
$$

which establishes that Fld s ( N ′ ) is a submodule of M .

For the converse statement, check that Fld $- 1 s ( M ' )$ necessarily exists and it is a submdoule of M by definition of unfolding.

## Proof of Theorem 3.1.

( 1 ) By Proposition 3.3 , both Fld s ( $N_{1}$ ) and Fld s ( $N_{1}$ ) exist and they are submodules of M by Proposition 3.4. If we show that $M ( p ) =$ Fld $s ( N_{1} )( p ) \oplus$ Fld s ( $N_{1}$ )( p ) for every $p \in P$ , then $M =$ Fld $s ( N_{1} ) \oplus$ Fld s ( $N_{1}$ ) due to Proposition 3.2.

We have $M ( p ) =$ Fld $s ( N )( p ) =$ Fld $s ( N_{1} \oplus N_{1} )( p ) =$ Fld $s ( N_{1} )( p ) \oplus$ Fld s ( $N_{1}$ )( p ) .



(2) For the converse, observe that Fld − 1 s ( M 1 ) and Fld − 1 s ( M 1 ) necessarily exist and they are submodules of N by Proposition 3.4. Furthermore, N ( q ) = Fld − 1 s ( M )( q ) = (Fld − 1 s ( M 1 ⊕ M 1 ))( q ) = Fld − 1 s ( M 1 )( q ) ⊕ Fld − 1 s ( M 1 )( q ) . Then, by Proposition 3.2, we have N = Fld − 1 s ( M 1 ) ⊕ Fld − 1 s ( M 1 ) .

(3) First, assume that Fld s ( N 1 ) exists and is a summand of M . Then, we have M = Fld s N 1 ⊕ Fld s N 1 . Applying (2) above, we have N = Fld − 1 s (Fld s N 1 ) ⊕ Fld − 1 s ( Fld s N 1 ) = N 1 ⊕ Fld − 1 s ( Fld s N 1 ) . Let Q = Fld − 1 s ( Fld s N 1 ) which is foldable. Then,

$$
\aleph_{1} \oplus \overline{N_{1}} = \aleph = \aleph_{1} \oplus Q
$$

Since Q is foldable, we have that $N_{1}$ is invertible.

Now, assume that $N_{1}$ is invertible. By definition, $N = N_{1} \oplus Q$ for some foldable summand Q . Apply ( 1 ) above to conclude that Fld s ( $N_{1}$ ) exists and is a summand of M .

## 4 Limit and complete modules

## 4.1 Limit modules

We introduce some special types of submodules of a module M which we call limit modules . These limit modules when further specialized to zigzag modules exhibit additional properties, which we utilize in our algorithm.

Definition 4.1 (Limit module) . We call a submodule $I \subseteq M a$ limit module for $M : P \to vec F$ if (i) $I = \oplus i_{I} I_{I}$ where each $I_{I}$ i is an interval module with $I_{I} \cap I j = \emptyset$ for $i ̸ = j$ and (ii) for every pair $p \leq P q$ , the morphism $I ( p \leq P q )$ is a surjection (this allows some particular types of interval modules to be limit modules, see e.g. Eq. ( 7 ) ). We call an ordered sequence of vectors $v_{p}$ 0 ,...,v $p m , v_{p} i \in M ( p i ) , a$ limit representative (or simply representative ) if there is a limit module I for M so that I ( p i ) is spanned by the vector $v_{p}$ i for each $i \in{0 ,...,m}$ . Conversely, every limit module I admits a representative $b^{I}$ p 0 ,..., $b^{I}$ p m where $b^{I}$ ( p i ) spans I ( p i ) . ̸

In what follows we often say a limit module for M simply a limit module when M is understood from the context. The reader can observe that limit representatives are elements of lim M .

The following observations about limit modules help understand the roles they play in the rest of the paper.

First, observe that a limit module I , in general, can be either a full interval module or a direct sum of one or more non-overlapping interval modules such as the ones separated by the red arrows in ( 7 ).

Second, observe that some of the interval modules in a direct decomposition of M ZZ $= i_{I} i$ may be limit modules.

Third, if $v_{p}$ 0 ,...,$v_{p}$ m is a representative of a limit module I , then α$v_{p}$ 0 ,..., α$v_{p}$ m is also a representative of I for any scalar $0 ̸ = \alpha \in F$ . In regard to this fact, we introduce the following notation. ̸

For a limit module $I \subseteq M$ ZZ , if $b^{I}$ : $b^{I}$ p 0 ,..., $b^{I}$ p m is a chosen representative for I , then for $0 ̸ = \alpha \in F , \alpha b^{I}$ denotes the representative $\alpha b^{I} p 0$ ,..., $\alpha b^{I} p m$ .̸

The reader may realize that a chosen representative of a limit module I is an element in lim I representing a global section of M ZZ . They can be added to produce other sections. This addition is given by pointwise vector addition which should not be confused with direct sums.

Observation 4.1 (representative sums) . For two limit modules I and I ′ and for $\alpha , \alpha ' \in F$ , the sequence of vectors $( \alpha b^{I} p 0 + \alpha ' b^{I} ' p 0 )$ ,..., $( \alpha b^{I} p m + \alpha ' b^{I} ' p m )$ is a representative. We denote this representative as the sum $\alpha b^{I} + \alpha ' b^{I} '$ .

The representative $\alpha b^{I} + \alpha ' b^{I} '$ can be viewed as an element in the space $lim I \oplus lim I '$ obtained by fixing an element $b^{I}$ in lim I and $b^{I}$ ′ in lim I ′ and mapping them to the direct sum by inclusions.

## 4.2 Complete modules

Definition 4.2. Let $P =$ Fld s Q and $N : Q \to vec F$ be a Q -module so that $M =$ Fld s ( N ) exists. An interval module I in a direct decomposition D : $i_{I}$ i of N is called s -complete if and only if (i) I is a full interval module that is foldable in D and (ii) its complement I in D is invertible. Let $\kappa ( D )$ denote the number of s -complete interval modules in D . We call $D_{s}$ -complete if $\kappa ( D ) =$ rk ( M ) .

Theorem 3.1 helps us to prove the following proposition which guarantees that a foldable module N has a s -complete decomposition. Additionally, it states that no direct decomposition of N can have more s -complete interval modules than a s -complete decomposition can have.

Proposition 4.1. Let $P =$ Fld s Q be a folded poset of a finite poset Q . Let $N : Q \to vec F$ be a Q -module so that the s -folding $M =$ Fld s ( N ) exists. Then, a s -complete decomposition of N exists. Furthermore, any direct decomposition D of N has $\kappa ( D ) \leq$ rk ( M ) .

Proof. First, we prove the second conclusion. If a direct decomposition D of N had $\kappa ( D ) >$ rk ( M ) , then D would have more than rk ( M ) s -complete interval modules as its summand each of which would fold to a full interval summand of M (Theorem 3.1( 3 )). This is not possible because in that case M would have more than rk ( M ) summands that are full intervals, an impossibility according to Theorem 2.2.

Next, we show the first conclusion. Consider a direct decomposition $M = I 1 \oplus$ ··· $\oplus I r \oplus M ' 1 \oplus$ ··· $\oplus M ' k$ where I 1 ,..., I r are full interval modules. By Theorem 2.2 , $r =$ rk ( M ) . Then, there is a decomposition D : Fld $- 1 s ( I 1 ) \oplus$ ···⊕ Fld $- 1 s ( I r ) \oplus$ Fld $- 1 s ( M ' 1 )$ ⊕···⊕ Fld $- 1 s ( M ' k )$ of N by Theorem 3.1( 2 ). Furthermore, each of Fld $- 1 s ( I_{I} ) , 1 \leq i \leq r$ , is a full interval module because each $I_{I}$ is so. By definition, both Fld $- 1 s ( I_{I} )$ and its complement are foldable (hence also invertible). Therefore, each Fld $- 1 s ( I_{I} )$ is s -complete. A direct decomposition D ′ (into indecomposables) that refines D is an s -complete decomposition of N because it has $r =$ rk ( M ) s -complete interval modules and it cannot have any more s -complete interval modules as $\kappa ( D ) \leq$ rk ( M ) .

## 5 Zigzag module

Definition 5.1. A poset P is called a zigzag poset iff there is a linear ordering p 0 ,...,p m of the points in P , called the zigzag path, so that for $i \in{0 , 1 ,...,m - 1} , p i ↔ p i$ +1 are the only and all immediate pairs in P , i.e., zigzag path represents the Hasse diagram of P . We write [ p i ,p j ] to denote an interval $I \subseteq P$ with the zigzag path p i ,p i +1 , ··· ,p j .

Definition 5.2. A zigzag module M ZZ : P ZZ $\to vec F$ is a persistence module where the poset P ZZ is a zigzag poset. Assuming that p 0 ,p 1 ,...,p m is the zigzag path for P ZZ , we write the zigzag module as:

$$
m{1}_{Z Z} \colon V_{p_{0}} \longleftrightarrow \dots \overset{\phi_{i - 1}}{\longleftrightarrow} V_{p_{i}} \longleftrightarrow V_{p_{i + 1}} \overset{\phi_{i + 1}}{\longleftrightarrow} \dots \overset{\phi_{m - 1}}{\longleftrightarrow} V_{p_{m}}
$$

where for $i \in{0 , 1 ,...,m - 1} , V_{p} i = M$ ZZ ( p i ) denote the vector spaces and $\varphi i = M$ ZZ $( p i \leq P ZZ p i +1 )$ or M ZZ $( p i +1 \leq P ZZ p i )$ denote the morphisms (linear maps).

We will be interested in interval submodules I [ $b^{I}$ ,d i ] of M ZZ . Such an interval module is either full or can be of four types determined by the types of its end points. The point $b^{I}$ is called closed if $b^{I} ̸ = 0$ and the arrow between $b^{I} - 1$ ,$b^{I}$ is a forward arrow $‘ \to ’$ and is called open otherwise. Similarly, the point d i is called closed if $d i ̸ = m$ and the arrow between d i ,d i +1 is a backward arrow and is called open otherwise. The interval module I [ $b^{I}$ ,d i ] is called open-open, open-closed, closed-open, or closed-closed depending on whether $b^{I}$ and d i are both open, $b^{I}$ is open and d i closed, $b^{I}$ is closed and d i is open, or both $b^{I}$ and d i are closed respectively. Notice that a full interval module is considered open-open by definition. ̸ ̸

By Theorem 2.1 , a zigzag module M ZZ : P ZZ $\to vec F$ over a finite zigzag poset P ZZ also decomposes uniquely into indecomposables. By quiver theory [ 21 , 33 ], these indecomposables are interval modules, that is, M ZZ $= I 1$ ⊕···⊕ I k where each $I_{I}$ := $I_{I}$ i is an interval module defined over an interval $I_{I}$ := [ p b i ,$p_{d}$ i ] .

We will be interested in limit modules in direct decompositions of zigzag modules. Observe that limit modules for a zigzag module M ZZ are dierct sum of open-open interval modules and take the following form.

$$
0 \leftrightarrow \cdots 0 \lceil ( p_{d_{j}} ) \leftrightarrow \cdots \leftrightarrow I ( p_{d_{j}} ) \rightarrow 0 \leftrightarrow \cdots \leftrightarrow 0 \leftarrow ( p_{d_{j}} ) \leftrightarrow \cdots \leftrightarrow I ( p_{d_{j}} ) \rightarrow 0 \leftrightarrow \cdots \leftrightarrow 0
$$

The following proposition says that in a sense any representative of a full interval submodule of M ZZ is in the span of the representatives of the limit modules present in any direct decomposition of M ZZ . This property does not extend beyond zigzag module. The persistence module M shown in Figure 3(top left) has a submodule which is a full interval module. However, it cannot be expressed as a direct sum of any limit modules in a direct decomposition of M because M itself is indecomposable and is not even an interval module. Also, see Conclusions (section 8 ) for further comments on this aspect.

Proposition 5.1. Let L be the set of limit modules in a direct decomposition D of M ZZ . For any full interval module $I \subseteq M$ ZZ , there exist unique $\alpha i \in F$ so that $b^{I} = I_{I} \in L \alpha i b I_{I}$ where the sum is defined as in Observation 4.1.

Proof. Let I 1 ,..., I k be the set of all interval modules in the direct decomposition D of the P ZZ -module M ZZ . For any $p \in P$ ZZ , since M ZZ $( p ) =$ span ( $b^{I}$ 1 p , ··· , $b^{I}$ k p ) , there exist uniquely determined $\alpha p i \in F$ so that $b^{I} p = i \alpha p i b^{I} i_{p}$ where $\alpha p i$ is taken to be zero if $b^{I} i_{p} = 0$ . Let $L p ={I_{I} | \alpha p i ̸ = 0}$ and $L ' = \cup p L p$ . It is not difficult to show that $\alpha p i = \alpha q i$ := $\alpha i$ for any two points p,q in the support of $I_{I}$ . Then, we can write $L ' ={I_{I} | \alpha i ̸ = 0}$ . We claim that L ′ is a subset of limit modules (open-open) in D , that is, $L ' \subseteq L$ . If not, there is an interval module $I ' \in L '$ that is not a limit module, that is, I ′ has an end point p j ̸∈ { p 0 ,p m } satisfying either of the following two cases: (i) the arrow for $\varphi j - 1$ is forward and the cokernel of $\varphi j - 1$ restricted to I ′ ( p j ) is non-zero. It follows that the cokernel of $\varphi j - 1$ restricted to I ( p j ) is also non-zero. This is impossible as I is a full interval module and p j ̸∈ { p 0 ,p m } , (ii) the arrow for $\varphi j$ is backward: again, we reach an impossibility with a similar argument. So, $L ' \subseteq L$ and it follows that $b^{I} = I_{I} \in L \alpha i b I_{I}$ where $\alpha i = 0$ for $I_{I} \in L \ L '$ establishing the claim of the proposition.

Proposition 5.1 suggests the following approach to compute the generalized rank of a given P -module M : first unfold P into a zigzag poset (path) P ZZ and construct a zigzag module M ZZ that is an s -unfolding of M . It follows that Fld s ( M ZZ ) exists and $M =$ Fld s ( M ZZ ) as required by Proposition 5.1. Then, after computing a direct decomposition of M ZZ with a zigzag persistence algorithm, convert it to an s -complete decomposition and determine how many full interval modules (if any) in this decomposition are s -complete.





1

2

Consider the P -module M shown in Figure 4 (left). After unfolding the module to a zigzag module (middle), suppose we get a decomposition into interval modules as indicated in the middle-top picture. Just like the example in Figure 2 (bottom), the full interval module in this decomposition of M ZZ does not fold into a full interval submodule of M because the representative vectors at the two copies of D do not match. In the example of Figure 2 , we could not repair this deficiency. However, now we can do so using the limit modules. Observe that the open-open interval module (blue) supported on $D \to C \gets D$ is a limit module. We can add its representative to the representative of the full interval module to obtain a new full interval module shown in middle-bottom picture. This new module is complete because it and its complement are foldable and thus the module folds into a full interval summand of M (Theorem 3.1( 1 )). Observe that any of the other two limit modules (grey) could also serve the purpose. The algorithm

It is instructive to point out that the blue module which is not a full module can also be made foldable by adding the representative of one of the grey modules, say the left one, to its representative, which has been done to obtain the decomposition shown on bottom right in Figure 4. Our algorithm does not do this because we are interested only on folding full interval modules.

## 5.1 Unfolding to a zigzag path and zigzag module

A finite poset P is represented by a directed acyclic graph $G = ( P,E ( P ))$ where (i) every directed edge $( p,q ) \in E ( P )$ satisfies $p \leq P q$ and (ii) every immediate pair $p \to q$ in P must correspond to an edge $( p,q ) \in E ( P )$ . The size of the poset $| P |$ with such a representation is measured as the total number of vertices and edges in G . Given a directed graph $G = ( P,E ( P ))$ for a finite poset P , we construct a zigzag poset P ZZ using the concept of Eulerian tour in graphs so that G ZZ $= ( P ZZ ,E ( P ZZ ))$ represents the zigzag path for P ZZ and $P =$ Fld $s_{p}$ ZZ for some folding s . Given a connected graph $G = ( V,E )$ , an Eulerian tour in G is an ordered sequence of its vertices, possibly with



repetitions, u 0 ,...,u i ,u i +1 ,...u $t = u 0$ so that every edge $( p,q ) \in E$ appears exactly once as a consecutive pair $( p = u i ,u i +1 = q )$ of vertices in the sequence. It is known that if G has even degree at every vertex, then G necessarily has an Eulerian tour which can be computed in $O ( | V | + | E | )$ time. We consider the undirected version $G = ( P,E ( P ))$ of the poset graph $G = ( P,E ( P ))$ and straighten it up using an Eulerian tour, see Figure 5. However, the graph G may not satisfy the vertex degree requirement. So, we double every edge, that is, put a parallel edge in the graph for every edge (this is equivalent to wrapping a thread around as shown in Figure 5 ). The modified graph G then has only even-degree vertices. We compute an Eulerian tour T in G and for every adjacent pair of vertices p,q in the tour representing an edge ( p,q ) in G we impose the order $p \leq T q$ if and only if the directed edge ( p,q ) is in G . The poset $( T, \leq T )$ is taken as the zigzag poset P ZZ and the tour (zigzag path) as its representation. Clearly, the number of edges in the tour (immediate pairs in P ZZ ) is at most twice the number of edges in G and the number of vertices in P ZZ is one more than that number. We have

*Figure 5: Unfolding P (left) to a zigzag poset P ZZ ; wrapping a thread around (right) gives a zigzag path (bottom) in P ZZ*

\text{Fact} \, 5 . 1 . \ P = \text{Fld}_{s} P_{Z Z} \text{for some folding} s \text{where} | P_{Z Z} | \leq 2 | P | + 1 .

In our algorithm, we assume that M is implicitly given by a P -filtration: A P -filtration F ( K ) of a simplicial complex K is a family of subcomplexes $F ( K ) ={K p \subseteq K} p \in P$ so that $K p \subseteq K q$ if $p \leq P q$ . We assume that both K and P are finite. Applying the homology functor H k ( · ) to the filtration F ( K ) , one obtains a module M := M F ( K ) in degree k where $M ( p ) = H k ( K p )$ and $M ( p \leq P q ) : H k ( K p ) \to H k ( K q )$ induced by the inclusion $K p \subseteq K q$ .

First, we unfold the poset P into a zigzag poset P ZZ using the method described before. Let s be the resulting folding given by Fact 5.1. To unfold $M : P \to vec F$ into a zigzag module M ZZ : P ZZ $\to vec F$ , we build a zigzag filtration F ZZ $={K p} p \in P$ ZZ by assigning K p := K s ( p ) . To check that F ZZ is indeed a zigzag filtration, observe that $K p \subseteq K q$ for every $p \leq P$ ZZ q because (i) $s ( p ) \leq P s ( q )$ by definition of folding s and (ii) $K s ( p ) \subseteq K s ( q )$ by definition

## 6 Algorithm

The algorithm (G EN R ANK in pseudocode) takes a P -fitration F and a degree k for the homology group. First, it s unfolds P to a zigzag path of P ZZ and computes the filtration F ZZ (Step 1 ). Let M and M ZZ be the modules obtained by applying the homology functor in degree k on F and F ZZ respectively as described above. We need to compute interval modules from F ZZ that represents a direct decomposition of M ZZ , i.e., we need a zigzag persistence algorithm that computes the intervals in the barcode with a representative (step 3 ). A sequence of k -cycles z b ,...,z d constitutes a representative of a limit module I := I [ p b ,$p_{d}$ ] if [ z i ] is chosen as $b^{I}$ p i for $p i \in [ p b ,p_{d} ]$ . The zigzag persistence algorithm in [ 15 ] can compute these representatives efficiently. Next, the algorithm checks how many interval modules in the computed decomposition of M ZZ can be converted to s -complete modules which provides rk ( M ) according to the definition of s -completeness.

Next proposition tells us that it is sufficient to check only the full interval modules in a direct decomposition of M ZZ if they can be converted to s -complete modules.

Proposition 6.1. Let M ZZ be an s -unfolding of M and I 1 ,..., I ℓ be the set of limit modules in a direct decomposition D of M ZZ . There exist unique $\alpha i \in F , i \in [ ℓ ]$ , so that every s -complete interval module I in any direct decomposition D ′ of M ZZ satisfies that $b^{I} = ℓ i$ =1 $\alpha i b I_{I}$ where for at least one $i \in [ ℓ ] , I_{I}$ is a full interval module and $\alpha i ̸ = 0$ .

Proof. First, Proposition 5.1 allows us to write

$$
b^{I} = \sum_{i = 1}^{\ell} \alpha_{i} b^{I_{i}} \text{for some unique} \alpha_{i} \in F , i \in [ \ell ] .
$$

Suppose that the claim of the proposition is not true. Fix a point $p \in P$ ZZ . We have $b^{I} p = ℓ i$ =1 $\alpha i b I_{I} p$ . Recall the quotient map $\rho$ for colimit in Proposition 2.1(ii). For each vector $v i = b^{I} i_{p}$ , the quotient vector $\rho ( v i )$ is a zero element in the colimit colim M ZZ because the limit module $I_{I}$ which is not full either has a sequence of vectors $v i ↔$ ··· $\to 0$ or $0 \gets$ ··· $↔ v i$ in its representative and thus $v i \sim 0$ (Notation 2.1 ). It follows that any representative of I is sent to a zero element in colim M ZZ . Since I is s -complete, Fld s ( I ) exists and its representative is an element of lim M . The definition of the folding implies that the limit-to-colimit map sends the representative of Fld s ( I ) also to a zero element in colim M .

Since I is s -complete, it is foldable and its complement is also foldable. Then, Fld s ( I ) is a full interval summand of M according to Theorem 3.1( 1 ). Since the full interval summand Fld s ( I ) is sent to zero by the limit-to-colimit map, we have rk $( M ' ) =$ rk ( M ) where $M =$ Fld $s ( I ) \oplus M '$ . However, rk $( M ' ) \leq$ rk $( M ) - 1$ according to Theorem 2.2 reaching a contradiction.

The above proposition suggests that we try to convert every full interval module I in a direct decomposition D of M ZZ to a foldable module first, and then appealing to Theorem 3.1( 3 ) check if the complement module I in D is invertible.

Definition 6.1. Let M ZZ be an s -unfolding of M . We say a full interval module I in a direct decomposition D of M ZZ is convertible in D if either (i) I is foldable, or (ii) there exists a set of limit modules { $I_{I}$ } in D none of which is equal to I so that I ′ with representative $b^{I}$ ′ := $b^{I} + i \alpha i b I_{I} , 0 ̸ = \alpha i \in F$ , is foldable.

Earlier, we defined $\kappa ( D )$ as the number of s -complete interval modules in a decomposition D of a zigzag module M ZZ . Recall that some of the full interval modules in D may not be s -complete but can be converted to be s -complete. To account for those, we introduce the following notation.

Notation 6.1. Let M ZZ be an s -unfolding of M and D be any of its direct decomposition. Denote by $\tau ( D )$ the number of interval modules I in D such that I is convertible and ¯ I is invertible in D . Observe that $\kappa ( D ) \leq \tau ( D )$ because s -complete interval modules are convertible by condition (i) in Definition 6.1 and their complements are invertible by definition.

Proposition 6.2. $\kappa ( D ) \leq$ rk $( M ) \leq \tau ( D )$ .

Proof. It follows from Proposition 4.1 that $\kappa ( D ) \leq$ rk ( M ) . So, we only show rk $( M ) \leq \tau ( D )$ . Observe that if rk $( M ) = 0$ there is nothing to prove since $\tau ( D )$ is non-negative by definition, so assume rk $( M ) ̸ = 0$ . Consider an s -complete decomposition D ∗ of M ZZ which exists according to Proposition 4.1. Let J 1 ,..., $J r , r =$ rk ( M ) , denote ̸

the set of these s -complete modules in D ∗ . We show by induction that there is a set of full modules I 1 ,..., I r in the direct decomposition D of M ZZ so that $I_{I}$ is convertible to J i and ¯ $I_{I}$ invertible, $i \in [ 1 ,r ]$ establishing the claim.

For the base case, consider J 1 . By Proposition 6.1 , there is a set of limit modules I ′ i in D so that there exist unique $0 ̸ = \alpha i \in F$ giving $b J 1 = i \alpha i b I ' i$ . Choose any full module among I ′ i s as I 1 which is guaranteed to exist by Proposition 6.1. Then, consider the decomposition D 1 that only replaces I 1 with J 1 in $D 0 = D$ . Observe that M ZZ $= I 1 \oplus I 1 = J 1 \oplus J 1 = I 1 \oplus J 1$ establishing that $I 1 \sim = J 1$ where J 1 is invertible (actually foldable). This means I 1 is invertible as required.

To complete the induction, assume that, for $1 \leq j < r$ , we already have that I 1 ,..., I j are convertible to J 1 ,..., J j and I 1 ,..., I j are invertible. Also, we have the decompositions D 0 ,..., D j of M ZZ where D i is obtained inductively from $D i - 1$ by replacing $I_{I}$ with J i . By Proposition 6.1 , we have b J j +1 $= i \alpha i b I ' i , \alpha i \in F$ , for some limit modules in D j . We claim that the collection { I ′ i } includes a full module other than J 1 ,..., J j . If not, we have b J j +1 $= ( i \gamma i b J i ) + ( k \gamma ' k b I ' k )$ for $\gamma i , \gamma ' k \in F$ where none of { I ′ k } is full. Then, b J j +1 $- i \gamma i b J i = k \gamma ' k b I ' k$ . The LHS gives a representative of an s -complete module which is a sum of representatives of limit modules(RHS) none of which is full contradicting Proposition 6.1. Thus, the set { I ′ i } includes a full module other than J 1 ,..., J j . Let I j +1 in D j be any such full module. Observe that I j +1 is also in D . It follows that I j +1 in D is convertible to J j +1 . Also, with the same reasoning as in the base case, one can check that I j +1 in D is invertible.

## Theorem 6.1. Let M ZZ be an s -unfolding of M and D be any direct decomposition of M ZZ .



1. If every convertible full module I in D where I is invertible is s -complete, then κ ( D ) = rk ( M ) ( D is s complete) else

2. Let I be a convertible module where I in D is invertible. Let D ′ be the direct decomposition obtained from D by replacing I with the converted module (Convert( I ) in step 4 of G EN R ANK ) , then κ ( D ′ ) = κ ( D ) + 1 .

Proof. For ( 1 ), observe that, $\kappa ( D ) = \tau ( D )$ in this case implying $\kappa ( D ) =$ rk $( M ) = \tau ( D )$ due to Proposition 6.2.

For ( 2 ), observe that an interval module $J ̸ = I$ in D is foldable iff it is foldable in D ′ because the only affected module in D is I . We observe that J remains invertible in D ′ if and only if it were so in D . It follows that, compared to D , the decomposition D ′ has exactly one more foldable module, namely Convert( I ) , with its complement being invertible. Hence $\kappa ( D ' ) = \kappa ( D ) + 1$ .̸

The algorithm G EN R ANK draws upon Theorem 6.1. To accommodate computations with a fixed finite precision, we assume that the field F is finite. The algorithm takes every full interval module I that is not foldable and checks if it is convertible. If it is convertible, it converts it and then check if its complement I for invertibility. It continues converting such modules to foldable modules until it cannot find any to convert. The current direct decomposition D of M ZZ changes with these conversions. At the end of this process, all convertible modules in the current decomposition whose complements are invertible become foldable themselves. Their number then coincides with rk ( M ) . To determine the existence of the limit modules whose addition makes I foldable, we take the help of an annotation matrix A p [ 17 , Chaper 4 ] for each complex $K p , p \in P$ ZZ , computed in step 2. Without further elaborations, we only mention that annotation for a k -cycle $z \in Z k ( K p )$ (cycle group in degree k ) is the coordinate of its class [ z ] in a chosen basis of H k ( K p ) ; see [ 7 ] (annotation algorithm in [ 7 ] is described for $F = Z 2$ though it easily extends for any finite field F ). The representative cycles maintained for every interval module in the decomposition of M ZZ at point p form a cycle basis [ z 1 ] ,..., [ z g ] of H k ( K p ) . We will see later that testing a full module for foldability amounts to calculating the annotations of the representative cycles of limit modules for certain points $p \in P$ ZZ and performing certain matrix reductions with them; see section 6.1.

Algorithm G EN R ANK ( P -filtration $F , k \geq 0$ )



• Step 1. Unfold P and F into a zigzag path P ZZ and a zigzag filtration F ZZ respectively

• Step 2. Compute an annotation matrix A p for every complex K p in F ZZ , p ∈ P ZZ

• Step 3. Compute a barcode for F ZZ with representative k -cycles; Let I denote the set of full interval modules corresponding to full bars in the current decomposition D

• Step 4. For every module I ∈ I do

– If I is convertible in D , update D with $I \gets$ Convert( I )

– If I is invertible in D , mark I complete

• Output the number of complete interval modules/*may output converted modules*/

Now we focus on the crucial checks in step 4. Fix the degree of all homology groups to be $k \geq 0$ , which form the vector spaces of the modules in our discussion.

## 6.1 Convertibility of I and computing Convert( I )

Assume that I is a full interval module in the current direct decomposition D of M ZZ $= i_{I} i = i_{I} [ p b i ,p_{d} i ]$ . For each interval module $I_{I} = I [ p b i ,p_{d} i ]$ the algorithm computes a sequence of representative k -cycles ${z i p j \in Z k ( K p j ) | p j \in{p b i ,p b i +1 ,...,p_{d} i} \subseteq P ZZ} . A$ representative for a limit module I [ p b i ,$p_{d}$ i ] is given by the homology classes [

For any point $p \in P$ ZZ , call the set of all points that fold to s ( p ) its partners and denote it as prt( p ) . We check if there are limit modules whose representatives when added to the representative of I makes the vectors at each point in prt( p ) the same for every point $p \in P$ ZZ because that converts I to a foldable interval module. The partner sets partition P ZZ . We designate an arbitrary point, say p , in each partner set prt( p ) where $|$ prt( p ) $| > 1$ as the leader of prt( p ) . Let L denote the set of these leaders. For every point $p \in L$ , we do the following.

Let I 1 ,..., I ℓ denote the set of limit modules in the current decomposition D of M ZZ and WLOG assume $I = I 1$ . The module I is convertible iff for all $p \in L$ , there exist $\alpha i \in F$ so that $b^{I} 1 p + ℓ i$ =2 $\alpha i b I_{I} p = ( b^{I} 1 p j + ℓ i =2 \alpha i b I_{I} p j )$ for every $p j \in$ prt( p ) . Written differently, for all $p \in L$ and for every $p j \in$ prt( p ) , we require

$$
b_{p}^{\natural_{1}} + b_{p_{j}}^{\natural_{1}} = \sum_{i = 2}^{\ell} \alpha_{i} ( b_{p}^{\natural_{i}} + b_{p_{j}}^{\natural_{i}} ) = \sum_{i = 2}^{\ell} \alpha_{i} v_{j}^{i} .
$$

where v i j := $b^{I} i_{p} + b^{I} i_{p} j$ . Our goal is to determine coefficients $\alpha i s$ if Eq. ( 8 ) holds. To do this, for every $i = 1$ ,...,ℓ and

p p1 p2 pt

I1 I2

p1 p2 pt



pA1 pA2

pA2

pA vp p pA A1glyph[lscript]

Lin.

I glyph[lscript] pAglyph[lscript]

every $p \in L$ , consider the matrices A p i whose columns are annotations of the representative cycles ˆ z i p j of v i j for every $p j \in$ prt( p ) ; see Figure 6 for an illustration. We can compute the F -sum of two chains $ˆ z i p j = z i p j + z i p$ and add their annotation vectors which is well defined because their annotation vectors have the same dimension $g p = dim H k ( K p )$ since all complexes at p and p j s were made equal during unfolding. Notice that, if $t p = |$ prt( p ) $|$ , the matrix A p i has dimensions g p × t p . Then, checking Eq. ( 8 ) boils down to determining $\alpha i s$ so that $A p 1 = \sum ℓ i$ =2 $\alpha i A p i$ . This can be done by using the linearization trick presented in [ 18 ]. Each of the g p × t p matrices $A p i , i \in{2 , . . . , ℓ}$ , is linearized into a vector v i of length t p g p and concatenated into a matrix of dimensions t p g p × ℓ . Let A p denote this matrix (Figure 6 ,bottom-right).

*Figure 6: Illustration of matrix reduction with linearization to check Eq. (8); (top) p is representative of prt( p ) = { p } ∪ { p 1 ,...,p t } , each row I i is a limit module with annotations of representatives at points in prt( p ) ; (bottom left) row I i consists of annotations of cycles { ˆ z i p j } j where ˆ z i p j = z i p + z i p j ; (bottom middle) annotations of cycles in row I i form matrix A p i in the stacks of matrices; (bottom right) each matrix A p i , i = 2 ,...,ℓ , linearized into a vector and combined into a larger matrix A p and A p 1 into vector v p .*

Suppose that there are $t = p \in L t p$ points in $\cup p$ prt( p ) and $g = max p{g p}$ . We need to check Eq. ( 8 ) simultaneously for all $p \in L$ to determine the $\alpha i$ s. To do this, we concatenate matrices A p for all $p \in L$ each of dimensions t p g p × ℓ to create a matrix A of dimensions ( p g p t p ) × ℓ . Then, a vector v of dimension p g p t p is created by concatenating vectors $v_{p}$ of dimension t p g p obtained by linearizing the matrices A p 1 for all $p \in L$ . Checking if Eq. ( 8 ) holds simultaneously for all $p \in L$ boils down to checking if v is in the column space of A . This is a matrix rank computation on a matrix of dimensions ( p g p t p ) × ℓ which can be done in $O ( ℓ \omega - 1 p g p t p ) = O ( ℓ \omega - 1 gt )$ time where $\omega < 2 . 373$ is the matrix multiplication exponent [ 22 ].

If Eq. ( 8 ) holds, we need to compute $\alpha = [ \alpha 2 ,..., \alpha ℓ ] T$ which can be done by solving the linear system Aα $= v$ . Then, we update I to Convert( I ) with the representative $b^{I} + ℓ i$ =2 $\alpha i b I_{I}$ to get the new decomposition D . This takes $O ( ℓ \omega - 1 gt )$ time again.

Notice that $g = O ( n ) = O ( t )$ . There are O ( t ) interval modules in the computed decomposition of M ZZ and there are at most O ( n ) full interval modules among them. So, we check at most O ( n ) full interval modules for convertibility. If there are $ℓ = O ( t )$ number of limit modules, each convertibility check takes $O ( ℓ \omega - 1 tg ) = O ( t \omega n )$ time giving a total of $O ( t \omega n 2 )$ time for converting all convertible interval modules in step 4.

To finish the analysis of step 4 , we need to consider the time spent for creating the annotations for cycle representatives of the limit modules. For a representative cycle z p , we get its annotation in the complex K p by adding the annotations of length $g p = dim H k ( K p )$ for every simplex in z p . Thus, to compute the annotations of all g p representative cycles at point p (classes of non-trivial representative cycles at p form a basis of H k ( K p ) ), we consider a g p × n p matrix E whose columns are annotations for each of the n p k -simplices in K p and another matrix G of dimensions n p × g p whose columns represent each cycle representative in the chain space of K p . The product matrix EG provides the annotations for each of the cycle representatives. This takes $O ( n p g \omega - 1 p ) = O ( ng \omega - 1 )$ time. Accouting for all $p \in P$ ZZ and $g = O ( n )$ , we obtain a complexity of $O ( tn \omega )$ . Then, over all iterations of convertibility, we get a time $O ( tn \omega +1 )$ .

## 6.2 Invertibility of I

To check invertibility of I , we do the following: first, we observe that Fld s ( I ) is well defined because I is already converted to be foldable. Consider the quotient module $Q = M /$ (Fld s ( I )) . If Q is a submodule of M , we know that Fld s ( I ) is a summand. In other words, I is then invertible due to Theorem 3.1( 3 ). We construct a P -filtration ˆ F that induces Q and check algorithmically if it is a submodule of M as follows.

After converting I to a foldable module, suppose that we obtain the representative cycles ${[ z p ]} p \in P$ ZZ for I . We cone these cycles in the P -filtration F of the simplicial complex K that induces M . It means that, we use a dummy vertex $\omega$ and for every simplicial complex K Fld $s ( p ) , p \in P$ ZZ , we introduce the simplex $\sigma \cup \omega$ for every $\sigma \in z p$ if it is not already present. Let the new P -filtration be ˆ F which filters the coned complex denoted ˆ K . The P -module induced by ˆ F is the quotient module Q . We unfold Q implicitly by unfolding ˆ F to ˆ F ZZ onto P ZZ . Let Q ZZ denote the zigzag module obtained by this unfolding process. We compute a direct decomposition D of Q ZZ from ˆ F ZZ by the zigzag persistence algorithm in [ 15 ]. We say D is extendable if there is a direct decomposition D ′ of M ZZ so that $D ' = D \cup{I}$ .

Proposition 6.3. I is invertible if and only if D is extendable.

Proof. If D is extendable, we have M ZZ $= I \oplus Q$ ZZ by definition of extendability and the fact that D is a direct decomposition of Q ZZ . It follows that I is invertible becasue Q ZZ is foldable by construction.

For the opposite direction, assume that I is invertible. Since I is foldable, Fld s ( I ) exists and is a summand of M according to Theorem 3.1( 3 ). The quotient $Q = M /$ (Fld s ( I )) is a submodule of M . This means the morphisms $Q ( p \leq P q )$ is a restriction of $M ( p \leq P q )$ for every pairs $p \leq P q$ . Therefore, by the definition of unfolding, every

To prove the claim, observe that, due to the quotienting, the vector generating I ( p ) cannot lie in the vector space Q ZZ ( p ) for every $p \in P$ ZZ . Then, M ZZ $( p ) = I ( p ) \oplus Q$ ZZ ( p ) for every $p \in P$ ZZ . Also, for every summand I ′ of Q ZZ in D , the morphism $I ' ( p \leq P ZZ q )$ is a restriction of M ZZ $( p \leq P ZZ q )$ because we already established that Q ZZ $( p \leq P ZZ q )$ is a restriction of M ZZ $( p \leq P ZZ q )$ . Therefore, for every interval module $I_{I} \in D \cup{I}$ , every morphism $I_{I} ( p \leq P ZZ q )$ is a restriction of M ZZ $( p \leq P ZZ q )$ over all pairs $p \leq P_{Z} Z q$ while M ZZ $( p ) = \oplus i_{I} i ( p )$ . It follows that D ′ is a direct decomposition of M ZZ .

We test if D is extendable by checking if the linear maps connecting two vectors (cycle classes) [ z p ] and [ z q ] for all immediate pairs $p,q \in P$ ZZ in every interval module in D are preserved in D ′ . This means that if $p \leq P$ ZZ q , the cycles z p and z q which are homologous in the complex ˆ K q remain homologous in the complex K q . This can be easily determined by checking the annotations of the cycles z p and z q in K q . We need to compute the annotations of representative cycles for the interval modules in D in the complex K q . We have already seen that it costs $O ( tn \omega )$ time in total. Thus, the extendability of D can be checked in $O ( tn \omega )$ time.

Time complexity. Let the graph $G = ( P,E ( p ))$ of the poset P indexing the input filtration F in G EN R ANK have a total of m vertices and edges. Recall that K p denote the complex at point $p \in P$ . Let $e_{p},q = | K p \ K q |$ where ( p,q ) is a directed edge in G , that is, $e_{p}$,q is the number of simplices that are added in the filtration going from p to q . Let

$$
e = \sum_{( p , q ) \in E ( P )} e_{p , q} \text{and} t = \max \{m , e \} .
$$

The quantity t is an upper bound on the size of the input P -filtration and the size of the poset P . Let every complex K p for every point $p \in P$ has at most n simplices. Then, step 1 of G EN R ANK takes at most O ( t ) time to unfold the poset to P ZZ with the procedure described in section 5.1. Producing the zigzag filtration F ZZ takes at most O ( t ) time. Step 2 takes at most O ( n 3 ) time to produce the annotation matrix [ 17 ] for every complex $K p , p \in P$ ZZ , giving a total of O ( tn 3 ) time. Step 3 takes O ( t 2 n ) time to compute the zigzag barcode with representatives by the algorithm in [ 15 ].

Step 4 for testing convertibility and converting of I takes a total of $O ( t \omega n 2 )$ time as discussed before. Testing invertibility of ¯ I for every I involves coning out the filtration F and unfolding ˆ F which cannot take more than O ( t ) time. Computing the zigzag barcode of ˆ F ZZ with representative cycles takes O ( t 2 n ) time by the algorithm in [ 15 ] giving a total time of O ( t 2 n 2 ) for O ( n ) full interval modules like I . The verification of the extendability of the interval decomposition D of the quotient module Q ZZ takes $O ( tn \omega )$ time, giving a total time of $O ( tn \omega +1 )$ over all. Therefore, step 4 takes a total time $O ( t 2 n 2 + tn \omega +1 )$ . Accounting for all terms, we get:

Theorem 6.2. Let F be an input P -filtration of a complex with n simplices where F and P have size at most t . The algorithm G EN R ANK computes rk ( M ) in $O ( tn \omega +1 + t \omega n 2 )$ time where M is the P -module induced by F . With $n = O ( t )$ , the bound becomes $O ( t \omega +2 )$ .

Proof. The correctness of G EN R ANK follows from Theorem 6.1 because step 4 effectively either increases the count $\kappa ( D )$ or determines that $\kappa ( D )$ cannot be increased in which case D is s -complete. The time complexity claim follows from our analysis.

## 7 Special case of degreed homology for d -complexes

In this section, we show that when a P -module M is induced by applying the homology functor in degree $d \geq 0$ on a P -filtration of a d -dimensional simplicial complex, we have a much more efficient algorithm for computing rk ( M ) . The key observation is that, in this case, the representatives for the interval modules in a direct decomposition of M ZZ takes a special form. In particular, the following Proposition holds leading to Theorem 7.1.

Proposition 7.1. Let M be a P -module where $M ( p ) = H d ( K p )$ with K p being a simplicial d -complex. For an interval module I [ p b ,$p_{d}$ ] in a direct decomposition of M ZZ and for any two points p i ,p $j \in [ p b ,p_{d} ]$ , the representative d -cycles z p i and z p j are the same.

Proof. First assume that p i and p j are immediate points in P ZZ and without loss of generality let $p i \to p j$ . According to the definition of representatives, we must have the homology classes [ z p i ] and [ z p j ] homologous in K p j . Since K p j is a d -complex, $[ z p i ] = [ z p j ]$ only if $z p i = z p j$ as chains. It follows by transitivity that this is true even if p i and p j are not immediate.

It follows from the above proposition that a full interval module I in a direct decomposition of M ZZ is foldable. Next proposition says that even the complement I is invertible. Then, applying Theorem 3.1( 3 ), we can claim that Fld s ( I ) is a full interval summand of M .

Proposition 7.2. Let I be any full interval module in a direct decomposition of M ZZ where M ZZ is constructed as in Proposition 7.1. Then, I is foldable and I is invertible.

Proof. I is foldable due to Proposition 7.1. We need to show that I is invertible.

For the full module I , there is a single d -cycle that forms a fixed representative at each point in P ZZ (Proposition 7.1 ). Let z be such a d -cycle and $\sigma$ be any d -simplex in z . Delete $\sigma$ from the complex K p for every $p \in P$ ZZ . Let $M -$ ZZ denote the P ZZ -module induced by the homology functor in degree d on the zigzag filtration $F -$ ZZ obtained from the original filtration F ZZ by deleting the simplex $\sigma$ everywhere. It is easy to verify that $M -$ ZZ is s -foldable because $K p \ \sigma = K p ' \ \sigma$ for every $p,p ' \in P$ ZZ with $s ( p ) = s ( p ' ) . A$ direct decomposition of M ZZ is obtained from a direct decomposition of $M -$ ZZ by adding a full bar with the representative z or equivalently the summand I . It follows that I is invertible.

Theorem 7.1. Let M be a module constructed as in Proposition 7.1 from a P -filtration F of a d -complex where P and F have size at most t . Then, rk ( M ) is the number of full interval modules in any direct decomposition of M ZZ which can be computed in $O ( t \omega )$ time.

Proof. By Proposition 7.2 and Theorem 3.1( 3 ), we get that $\kappa ( D ) = \tau ( D )$ for any direct decomposition D of M ZZ . Then, it follows from Proposition 6.2 that rk $( M ) = \kappa ( D )$ , which is exactly equal to the number of full interval modules in any direct decomposition of M ZZ . Therefore, rk ( M ) can be simply obtained by computing the zigzag barcode of M ZZ (no need of computing the representatives). This can be done in $O ( t \omega )$ time with the fast zigzag algorithm [ 14 ].

If M is induced by a P -filtration of a graph, then rk ( M ) can be computed even faster. Observe that every 1 -cycle that represents a full bar must continue to be present from the initial graph G p 0 at $p 0 \in P$ to the final graph G p m at $p m \in P$ . This suggests the following algorithm. Take G p m ; delete all edges and vertices that are deleted as one moves along P ZZ from p 0 to p m , but do not insert any of the added edges and vertices. In the final graph G ′ p m thus obtained (which may be different from G p m because we ignore the inserted edges and vertices along P ZZ ), we compute the number of independent 1 -cycles. This number can be computed by a depth first search in G ′ p m in linear time. Then, as an immediate corollary we have Theorem 7.2.

Theorem 7.2. If M is induced by degree1 homology of a P -filtration F of a graph with a total of n vertices and edges, then rk ( M ) can be computed in $O ( n + t )$ time where P and F have size at most t .

## 8 Conclusions and discussions

Analyzing a mutliparameter persistence module with the help of one parameter persistence modules is not new. It has been introduced in the context of computing matching distances [ 2 , 10 , 23 ] for 2 -parameter modules. The unfolding/folding technique proposed here offers a different slicing technique. By producing one zigzag path instead of multiple slices, the unfolding preserves the structural maps of the original module M in a lossless manner. We showed how to use them for reconstructing full interval modules and hence for computing the generalized rank. It will be interesting to see what other invariants one can reconstruct using the folding/unfolding of persistenec modules. A natural candidate would be to compute the limit and colimit of the original module from its zigzag straightening. Recent advances in zigzag persistence computations [ 14 , 30 , 32 ] can then be taken advantage of for computing limits and colimits.

A natural question to ask is that if the approach laid out in this paper works even when the original input module is unfolded into another module that is not necessarily a zigzag module. This could lead to a more generalized theory for unfolding. Unfortunately, this question does not have a positive answer. The main bottleneck arises while attempting to extend Proposition 6.1. This proposition says that any full interval module that is a submodule of a zigzag module M ZZ is a sum of the limit modules in a direct decomposiiton of M ZZ . We cannot extend this result to every module M that is not a zigzag module. The proof of Theorem 6.1 uses Proposition 6.2 which depends on this result in a fundamental way. To see the obstacle, consider the module M in Figure 3 (left,top). This module has a full interval module I as a submodule, namely I is given by the vector spaces spanned by the vector v at every point and the internal map as the identity. However, the module M itself is not decomposable and hence has no limit module as a summand. Thus, the full module I has no expression as a sum of limit modules that are summands of M . The situation does not

The special structure of the zigzag modules allows us to express any full submodule as a sum of limit modules that are summands, which in turn lets our algorithm to check if a full module in the current decomposition is convertible or not.

## Acknowledgment.

We thank Iason Papadopoulos of University of Bremen for useful comments on the paper.

