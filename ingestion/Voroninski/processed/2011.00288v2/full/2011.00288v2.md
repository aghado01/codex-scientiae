[Page 1]

# Optimal Sample Complexity of Subgradient Descent for Amplitude Flow via Non-Lipschitz Matrix Concentration

Paul Hand ∗ , Oscar Leong † , and Vladislav Voroninski ‡

August 31, 2021

## Abstract

We consider the problem of recovering a real-valued n -dimensional signal from m phaseless, linear measurements and analyze the amplitude-based non-smooth least squares objective. We establish local convergence of subgradient descent with optimal sample complexity based on the uniform concentration of a random, discontinuous matrix-valued operator arising from the objective’s gradient dynamics. While common techniques to establish uniform concentration of random functions exploit Lipschitz continuity, we prove that the discontinuous matrix-valued operator satisﬁes a uniform matrix concentration inequality when the measurement vectors are Gaussian as soon as m = Ω( n ) with high probability. We then show that satisfaction of this inequality is suﬃcient for subgradient descent with proper initialization to converge linearly to the true solution up to the global sign ambiguity. As a consequence, this guarantees local convergence for Gaussian measurements at optimal sample complexity. The concentration methods in the present work have previously been used to establish recovery guarantees for a variety of inverse problems under generative neural network priors. This paper demonstrates the applicability of these techniques to more traditional inverse problems and serves as a pedagogical introduction to those results.

## 1 Introduction

Consider the problem of recovering a signal x ∗ ∈ R n from m phaseless measurements of the form

$$
y \coloneqq | A x _ { * } | + \eta
$$

where A ∈ R m × n is a measurement matrix, | · | acts entrywise, and η ∈ R m denotes noise. This problem is known as phase retrieval as, in practice, the phase of the signal is lost in the forward measurement process due to the underlying physics of the measurement system. We consider the case when the entries of A are i.i.d. Gaussian, which we will refer to as the generic measurement regime. In this work, we aim to recover x ∗ by solving the following non-smooth least squares problem

$$
\min _ { x \in \mathbb { R } ^ { n } } f ( x ) \colon = \frac { 1 } { 2 } \| | A x | - y \| ^ { 2 } . \\ \intertext { t i c s and College of Computer and Information Science, Northeastern University, Boston , }
$$

∗ MA †

Department of Mathematics and College of Computer and Information Science, Northeastern University, Boston,

Department of Computational and Applied Mathematics, Rice University, Houston, TX ‡

Helm.ai, Menlo Park, CA

[Page 2]

This objective function is known as Amplitude Flow. For generic measurements, previous works have shown that with proper initialization, gradient descent both with [ 12 ] and without [ 29 ] truncated gradients can recover the signal with the optimal sample complexity of m = Ω( n ) .

Existing proof techniques of convergence guarantees for (sub)gradient descent of ( 1 ) follow a two-step process: 1) establish that spectral initialization or some variant thereof guarantees an initializer with relative error bounded by a small absolute constant and then 2) show that the objective satisﬁes a property akin to convexity near the minimizer to guarantee convergence. This latter property is called the local regularity condition RC ( µ,λ,ε ) 1 . Showing that this condition holds is crucial in establishing local convergence for Amplitude Flow [ 29 , 12 ] and its variants [ 23 ].

This proof technique is not unique to Amplitude Flow as it was initially introduced to guarantee convergence for the intensity-based formulation Wirtinger Flow, which aims to solve

$$
\min _ { x \in \mathbb { R } ^ { n } } \frac { 1 } { 2 } \| A x | ^ { 2 } - y ^ { 2 } \| ^ { 2 } . \\ \intertext { a f o r e m e n t i o n e d t w o { t e p s e d u r e } $ e s a b l i s h e d $ e x a c t r e c o r v e r y $ with } \log n ) \ A \text { follow up variant using truncation } [ 5 ] \text { improved } \text { the same }
$$

In the original work [ 3 ], the aforementioned two-step procedure established exact recovery with sample complexity m = Ω( n log n ) . A follow-up variant using truncation [ 5 ] improved the sample complexity to m = Ω( n ) and also employed the RC ( µ,λ,ε ) to show convergence post-initialization. Recently, [ 28 ] established the suﬃciency of a deterministic condition for local convergence when solving ( 2 ) in the lifted domain and a relationship between the condition’s accuracy and convergence rate of gradient descent was shown. This deterministic condition is a uniform matrix concentration inequality that is proven to hold for generic measurements when m = Ω( n log n ) .

Other works include the global landscape analysis in [ 25 ] which showed that ( 2 ) exhibits benign geometry given a suﬃcient number of measurements m = Ω( n log 3 n ) by carefully analyzing the gradient and Hessian in partitioned regions of space. In [ 11 ], the authors considered the robust ℓ 1 loss with intensity-based measurements and established local convergence of a prox-linear algorithm using composite optimization theory. Convex approaches based on lifting [ 4 , 2 , 10 , 15 ] utilize dual certiﬁcates to assert correctness of the minimizers of semideﬁnite programs. Linear programming approaches have also been studied, whose proof techniques range from using tools in statistical learning theory [ 1 ] and geometric probability theory [ 14 ], along with elementary approaches using standard concentration estimates of the singular values of random matrices [ 19 ]. For a more comprehensive overview of prior work for phase retrieval, we refer the reader to [ 13 ].

In this paper, we present a proof technique for solving ( 1 ) with m = Ω( n ) based on uniform concentration of random matrix-valued functions that are discontinuous in space. Consider a subgradient descent algorithm with iterates { x t } t 0 of the form x t +1 = x t − αv x t ,x ∗ where α > 0 , v x t ,x ∗ ∈ ∂f ( x t ) , and ∂f ( x ) is the Clarke subdiﬀerential at x (deﬁned in Section 3 ). Let dist ( x,x ∗ ) := min( x − x ∗ , x + x ∗ ) . We ﬁrst state our main local convergence result in the Gaussian measurement regime.

Theorem 1. There exists positive absolute constants C , c 1 , c 2 , ρ 1 , and ρ 2 such that the following holds. Suppose A ∈ R m × n has i.i.d. N (0 , 1 /m ) entries and the noise is bounded η ρ 1 x ∗ . Assume the initial iterate x 0 satisﬁes dist ( x 0 ,x ∗ ) ρ 2 x ∗ and the step size satisﬁes 0 < α 1 . If m Cn , then with probability at least 1 − 3exp( − c 1 m ) − m exp( − c 2 n ) , we have that for all t 1 , t

$$
d i s t ( x _ { t } , x _ { * } ) & \leqslant \left ( 1 - \frac { \alpha } { 2 } \right ) ^ { t } d i s t ( x _ { 0 } , x _ { * } ) + 4 \| \eta \| . \\ \intertext { c o n v e r g e n c e \ u p \ t o \ t h e \ o i s e \ l e v e l \ w i t h \ o p t i m a l \ s a m }
$$

This result asserts local convergence up to the noise level with optimal sample complexity. In the theorem, note that we require an initializer with relative error less than a sufficiently small constant. There are several schemes to achieve this with m = Ω( n ) Gaussian measurements, even in the presence of noise [5, 29, 12, 11]. While convergence of subgradient descent without truncation for the Amplitude Flow objective is known [29], the method of proof we present here is novel. In particular, we show that Theorem 1 is a consequence of the following two results: 1) a uniform matrix concentration inequality is sufficient to guarantee local convergence with proper initialization and 2) Gaussian matrices satisfy this inequality with high probability when m = Ω( n ) .

1 A function L satisﬁes RC ( µ, λ, ε ) at a stationary point y if for all x ∈ R n such that x − y ε y , ∇L ( x ) , x − y µ 2 x − y 2 + λ 2 ∇L ( x ) 2 .

[Page 3]

We now detail the high level intuition behind the proof ideas and techniques. Let sgn ( z ) := z/ | z | for z = 0 and sgn (0) = 0 act entrywise. For ease of exposition, suppose there is no noise η = 0 . The discontinuous, spatially-varying measurement operator A x := diag ( sgn ( Ax )) A plays a critical role in analyzing subgradient descent as this operator governs the gradient dynamics of f . Speciﬁcally, the gradient almost everywhere is given by ∇ f ( x ) = A T x ( A x x − A x ∗ x ∗ ) . As will be shown in the next section, the gradient in expectation obeys a property equivalent to the RC ( µ,λ,ε ) in neighborhoods of the global minimizers ± x ∗ . Hence if we establish concentration of the quantity A T x A y to its expectation E [ A T x A y ] uniformly in x,y , then this property will also be satisﬁed by the gradient. This will be shown to guarantee local convergence up to the global sign ambiguity with high probability.

/negationslash

The uniform concentration result we establish is the following: when A has i.i.d. N (0 , 1 /m ) entries, then for any parameter 0 < ε < 1 , when m = Ω( n ) we have that with high probability

$$
\| A _ { x } ^ { T } A _ { y } - \Phi _ { x , y } \| \leqslant \varepsilon \, \forall x , y \in \mathbb { R } ^ { n } \quad \\
$$

where Φ x,y := E [ A T x A y ] has an analytic expression. As this result holds uniformly in x,y , we have that for any x,x ∗ ∈ R n , the gradient ∇ f ( x ) ≈ Φ x,x x − Φ x,x ∗ x ∗ . The diﬃculty of establishing ( 3 ) uniformly in x,y is due to the fact that A T x A y is a non-Lipschitz matrix-valued operator. Standard approaches to control these types of quantities exploit Lipschitz continuity by ﬁrst 1) establishing concentration for ﬁxed x,y , then 2) establishing concentration over all points in a net of the sphere by using a union bound, and ﬁnally 3) appealing to Lipschitz continuity to get concentration uniformly in x,y . However, in this case, 3) is not possible as A T x A y is discontinuous with respect to x,y .

Fortunately, this issue can be solved by concentrating Lipschitz continuous approximations of A T x A y with respect to x,y . In particular, one can create continuous matrix-valued functions that are upper and lower bounds of A T x A y with respect to the semideﬁnite ordering. Then, concentration of these continuous approximations can be established by appealing to the standard arguments outlined above. This, in turn, will be shown to establish concentration of the non-Lipschitz quantity of interest by a squeezing argument. Moreover, using novel tools developed in [ 9 ], a more eﬃcient set of coverings of the sphere can be exploited to achieve sample complexity linear in the ambient dimension n . Intuitively, this is achieved by constructing a net of the sphere that does not penalize all directions equally, but instead exploits directions for which the function of interest does not deviate much and penalizes those for which the function exhibits larger change. This intuition is made precise in the proof of Proposition 1 .

Discussion. While the convergence results presented here have been shown in [ 29 ], the contribution of this work lies in the novelty of the analysis. In particular, this work is an illustrative example of using the concentration of non-Lipschitz functions to establish favorable properties of ﬁrst-order algorithms to solve inverse problems. The concentration methods of this paper have been used to establish recovery in compressive sensing [ 20 , 22 ], phase retrieval [ 17 , 18 ], and other problems [ 21 , 24 , 16 , 8 ] under image priors given by generative neural networks. For example, a similar uniform matrix concentration inequality to the one used in this paper was introduced by the present authors in [ 17 , 18 ] to establish recovery in compressive phase retrieval under a generative prior with information-theoretically optimal sample complexity. This paper demonstrates the applicability of

[Page 4]

## 2 Proof Technique

We now establish the suﬃciency of a deterministic condition for local convergence in the form of a uniform matrix concentration inequality and show that Gaussian matrices satisfy this condition with high probability when m = Ω( n ) . A similar condition, known as the Weight Distribution Condition, was ﬁrst introduced in [ 20 ] in the context of compressive sensing under generative neural network priors. The matrix concentration inequality is stated as follows.

Deﬁnition 1. Fix 0 < ε < 1 . We say that A ∈ R m × n satisﬁes the Measurement Distribution Condition (MDC) with constant ε if

where Proof of Theorem 2. Let B ( x ∗ , r ) := { x ∈ R n : ‖ x -x ∗ ‖ /lessorequalslant r } . Suppose x 0 ∈ B ( x ∗ , ε ‖ x ∗ ‖ ) as the proof for the case x 0 ∈ B ( -x ∗ , ε ‖ x ∗ ‖ ) is identical. For t /greaterorequalslant 1 , observe that for any v x t -1 ,x ∗ ∈ ∂f ( x t -1 ) , we have

$$
\| A _ { x } ^ { T } A _ { y } - \Phi _ { x , y } \| \leqslant \varepsilon \ \forall \ x , y \in \mathbb { R } ^ { n }
$$

$$
\Phi _ { x , y } \colon = \begin{cases} \frac { \pi - 2 \theta _ { x , y } } { \pi } I _ { n } + \frac { 2 \sin \theta _ { x , y } } { \pi } M _ { \hat { x } \leftrightarrow \hat { y } } & \text {if } x \neq 0 , y \neq 0 , \\ 0 & \text {otherwise.} \end{cases}
$$

/negationslash

/negationslash

Here θ x,y := ∠ ( x,y ) , ˆ x := x/ x , ˆ y := y/ y , I n is the n × n identity matrix, and M ˆ x ↔ ˆ y 2 is the matrix that sends ˆ x → ˆ y , ˆ y → ˆ x , and z → 0 for any z ∈ span ( { x,y } ) ⊥ . T

Note that for points x,y with small angle, this condition requires A x A y to act like an isometry. In the extreme case when x = y , Φ x,y is the identity. An elementary calculation gives E [ A T x A y ] = Φ x,y for x,y = 0 and A ij ∼ N (0 , 1 /m ) . The ﬁrst result is that the MDC is suﬃcient to guarantee the following: a subgradient descent

/negationslash

algorithm with proper initialization will converge to the true solution up to the global sign ambiguity. To establish this, we ﬁrst show that the MDC is suﬃcient for the objective to satisfy the following regularity condition which states that, within a neighborhood of the true solution, all subgradients point towards the true solution. This result is proven in Section 3.1 .

Lemma 1. Fix 0 < ε 0 . 001 . Suppose A ∈ R m × n satisﬁes the MDC with constant ε . Then for all x ∈ R n such that dist ( x,x ∗ ) ε x ∗ and any v x,x ∗ ∈ ∂f ( x ) , we have that v x,x ∗ − ( x ± x ∗ ) 1 2 x ± x ∗ + 2 η . Here x ± x ∗ := x − x ∗ if x − x ∗ = dist ( x,x ∗ ) and x + x ∗ otherwise.

Note that the conclusion of this lemma in the noiseless setting is in fact equivalent to the RC ( µ,λ,ε ) condition 3 . We now show that satisfaction of the MDC implies local convergence.

Theorem 2 (Deterministic Local Convergence Guarantee) . Fix 0 < ε 0 . 001 . Suppose A ∈ R m × n satisﬁes the MDC with constant ε , η ε 4 x ∗ , and 0 < α 1 . If dist ( x 0 ,x ∗ ) ε x ∗ then for all t 1 , α t

$$
d i s t ( x _ { t } , x _ { * } ) \leqslant \left ( 1 - \frac { \alpha } { 2 } \right ) ^ { t } d i s t ( x _ { 0 } , x _ { * } ) + 4 \| \eta \| . \\ \text {matrix is as follows: consider a rotation matrix R that sends } \hat { x } \mapsto \alpha
$$

2 A formula for this matrix is as follows: consider a rotation matrix R that sends ˆ x → e 1 and ˆ y → cos θ x,y e 1 + sin θ x,y e 2 where θ x,y = ∠ ( x, y ) . Then M ˆ x ↔ ˆ y = R T   cos θ x,y sin θ x,y 0 sin θ x,y − cos θ x,y 0 0 0 0 n − 2   R where 0 n − 2 is the n − 2 × n − 2 matrix of zeros. Note that if θ x,y = 0 or π , M ˆ x ↔ ˆ y = ˆ x ˆ x T or − ˆ x ˆ x T , respectively.

matrix of zeros. Note that if θ x,y = 0 or π , M ˆ x ↔ ˆ y = ˆ x ˆ x or -ˆ x ˆ x , respectively.

Indeed, note that if the conditions of Lemma 1 are satisﬁed and η = 0 , then for all x ∈ R n such that dist ( x, x ∗ ) ε x ∗ and any v x,x ∗ ∈ ∂f ( x ) , v x,x ∗ − ( x ± x ∗ ) 1 2 x ± x ∗ ⇐⇒ v x,x ∗ , x ± x ∗ 3 8 x ± x ∗ 2 + 1 2 v x,x ∗ 2 . Thus the MDC is suﬃcient to guarantee the RC ( µ, λ, ε ) holds with µ = 3 / 4 , λ = 1 , and our choice of ε .

[Page 5]

$$
\| x _ { t } - x _ { * } \| & = \| x _ { t - 1 } - \alpha v _ { x _ { t - 1 } , x _ { * } } + \alpha ( x _ { t - 1 } - x _ { * } ) - \alpha ( x _ { t - 1 } - x _ { * } ) - x _ { * } \| \\ & \leq ( 1 - \alpha ) \| x _ { t - 1 } - x _ { * } \| + \alpha \| v _ { x _ { t - 1 } , x _ { * } } - ( x _ { t - 1 } - x _ { * } ) \| \\ & \leq ( 1 - \alpha ) \| x _ { t - 1 } - x _ { * } \| + \frac { \alpha } { 2 } \| x _ { t - 1 } - x _ { * } \| + 2 \alpha \| \eta \| \\ & = ( 1 - \frac { \alpha } { 2 } ) \, \| x _ { t - 1 } - x _ { * } \| + 2 \alpha \| \eta \| \\ \intertext { where in the third line we used Lemma 1 .  We claim that the iterates must stay within a ball of the }
$$

where in the third line we used Lemma 1 . We claim that the iterates must stay within a ball of the minimizer. Indeed, if x t − 1 ∈ B ( x ∗ ,ε x ∗ ) , we have that by equation ( 5 ) and our bound on the size of the noise η ε 4 x ∗ that

$$
\| x _ { t } - x _ { * } \| & \leqslant \left ( 1 - \frac { \alpha } { 2 } \right ) \| x _ { t - 1 } - x _ { * } \| + 2 \alpha \| n \| \leqslant \left ( 1 - \frac { \alpha } { 2 } \right ) \varepsilon \| x _ { * } \| + \alpha \cdot \frac { \varepsilon } { 2 } \| x _ { * } \| = \varepsilon \| x _ { * } \| \\ s o _ { \ } r _ { t } \in \mathcal { B } ( x _ { \varepsilon } \triangleleft | x | ) \quad T h u s \ w e a n i v o k e l { e m m a l } \ 1 \text { and } \text {equation } ( 5 ) \text { for } \text {each } t \geqslant 1
$$

so x t ∈ B ( x ∗ ,ε x ∗ ) . Thus, we can invoke Lemma 1 and equation ( 5 ) for each t 1 . Letting τ := 1 − α 2 , starting at t = 1 and repeatedly applying ( 5 ), we attain

$$
\| x _ { t } - x _ { * } \| & \leqslant \tau ^ { t } \| x _ { 0 } - x _ { * } \| + 2 \alpha ( \tau ^ { t } + \tau ^ { t - 1 } + \cdots + 1 ) \| \eta \| \leqslant \tau ^ { t } \| x _ { 0 } - x _ { * } \| + \frac { 2 \alpha } { 1 - \tau } \| \eta \| .
$$

Plugging in the deﬁnition of τ yields the desired inequality.

Finally, using recent tools developed in [ 9 ], we show that Gaussian matrices satisfy the MDC with high probability with m = Ω( n ) sample complexity.

Proposition 1. Fix 0 < ε < 1 . Suppose A ∈ R m × n has i.i.d. N (0 , 1 /m ) entries. If m C ε n , then A satisﬁes the MDC with constant ε with probability at least 1 − exp( − cmε 2 / 2) − m exp( − n/ 8) − exp( − m/ 2) . Here C ε = Ω( ε − 2 log( ε − 1 )) and c is a universal constant.

Combining this result with Theorem 2 with ε = 0 . 001 proves Theorem 1 .

Hence this shows that the MDC is suﬃcient for local convergence of subgradient descent with proper initialization. Moreover, the conclusion holds for generic measurements with high probability as soon as m = Ω( n ) . We emphasize that the MDC is a global property concerning the measurement matrix. Hence Proposition 1 implies one has uniform concentration of subgradients to their expectation with optimal sample complexity. Extending this local convergence result to a result about convergence of subgradient descent with generic initialization is an interesting future direction, as shown in recent works [ 6 , 26 ].

# 3 Proofs

In this section, we prove Lemma 1 and Proposition 1 . We ﬁrst introduce some notation used in the proofs. Let [ n ] := { 1 ,... ,n } . Let B ( y,r ) := { x ∈ R n : x − y r } and B := { x ∈ R n : x 1 } . For x ∈ R n \ { 0 } , let ˆ x := x/ x . Let { E } be the indicator function on the event E . For a random variable X , let X | ( E ) be the random variable X conditioned on the event E . Let I n be the n × n identity matrix. Let S n − 1 denote the unit sphere in R n . We write γ = Ω( δ ) when γ Cδ for some positive constant C . Similarly, we write γ = O ( δ ) when γ Cδ for some positive constant C .

[Page 6]

For a locally Lipschitz function f : X → R from a Hilbert space X to R , the Clarke generalized directional derivative [ 7 ] of f at x ∈ X in the direction u is deﬁned by

$$
f ^ { o } ( x ; u ) \colon = \lim _ { y \to x , t \downarrow 0 } \frac { f ( y + t u ) - f ( y ) } { t } .
$$

Then the generalized subdiﬀerential of f at x is deﬁned as

$$
\partial f ( x ) \colon = \{ v \in \mathbb { R } ^ { n } \colon \langle v , u \rangle \leqslant f ^ { o } ( x ; u ) , \ \forall u \in \mathcal { X } \} .
$$

Any v x,x ∗ ∈ ∂f ( x ) is called a subgradient of f at x . When f is diﬀerentiable at x , ∂f ( x ) = {∇ f ( x ) } . In the proofs, we will make use of the following fact concerning the Clarke subdiﬀerential of the objective function f . Since f is piecewise quadratic, Theorem 9.6 from [ 7 ] asserts that for any x ∈ R n , ∂f ( x ) can be written equivalently as

$$
\partial f ( x ) = \text {conv} ( v _ { 1 } , v _ { 2 } , \dots , v _ { s } )
$$

where conv ( · ) denotes the convex hull of v 1 ,... ,v s , s is the number of quadratic functions adjoint to x , and v ℓ is the gradient of the ℓ -th quadratic function of f at x . For each v ℓ , there exists a w ℓ and a suﬃciently small δ ℓ > 0 such that f is diﬀerentiable at x + δ ℓ w ℓ and v ℓ = lim δ ℓ ↓ 0 ∇ f ( x + δ ℓ w ℓ ) .

# 3.1 Convexity property of objective

Here we prove Lemma 1 , the convexity-like property around the minimizer. In essence, it states that when iterates are near the minimizers, all subgradients point towards the true solution.

Proof of Lemma 1 . We consider the case x ∈ B ( x ∗ ,ε x ∗ ) as the case x ∈ B ( − x ∗ ,ε x ∗ ) is similar. Suppose f is diﬀerentiable at x . First, note the MDC implies that A T x A x − I n ε . Moreover, for any x,z ∈ R n , A x z 2 | A T x A x z,z − z 2 | + z 2 (1 + ε ) z 2 . Hence A x 2 for all x ∈ R n when ε < 1 . Thus, we have

$$
\| v _ { x , x _ { * } } - ( x - x _ { * } ) \| & \leqslant \| A _ { x } ^ { T } ( A _ { x } - A _ { x _ { * } } ) x _ { * } \| + \| A _ { x } ^ { T } A _ { x } ( x - x _ { * } ) - ( x - x _ { * } ) \| + \| A _ { x } ^ { T } \eta \| \\ & \leqslant 2 \| ( A _ { x } - A _ { x _ { * } } ) x _ { * } \| + \varepsilon \| x - x _ { * } \| + 2 \| \eta \| . \\
$$

We now show that for suﬃciently small ε , ( A x − A x ∗ ) x ∗ 1 / 8 x − x ∗ . Letting { a i } m i =1 denote the rows of A , observe that

$$
\det \text { denote the rows of A, observe that} \\ & \| ( A _ { x } - A _ { x } . ) x _ { * } \| ^ { 2 } = \sum _ { i = 1 } ^ { m } ( s g n ( \langle a _ { i } , x \rangle ) - s g n ( \langle a _ { i } , x _ { * } \rangle ) ) ^ { 2 } \langle a _ { i } , x _ { * } \rangle ^ { 2 } \\ & \leq \sum _ { i = 1 } ^ { m } ( s g n ( \langle a _ { i } , x \rangle ) - s g n ( \langle a _ { i } , x _ { * } \rangle ) ) ^ { 2 } \langle a _ { i } , ( x - x _ { * } ) \rangle ^ { 2 } \\ & = \| A _ { x } ( x - x _ { * } ) \| ^ { 2 } + \| A _ { x } ( x - x _ { * } ) \| ^ { 2 } - 2 \langle x - x _ { * } , A _ { x } ^ { T } A _ { x } , ( x - x _ { * } ) \rangle . \\ \text {Since } \| A _ { x } ^ { T } A _ { x } - I _ { n } \| \leq \epsilon , \, \text {we have } \| A _ { x } ( x - x _ { * } ) \| ^ { 2 } \leq ( 1 + \epsilon ) \| x - x _ { * } \| ^ { 2 } . \, \text {The same upper bound holds}
$$

Since A T x A x − I n ε , we have A x ( x − x ∗ ) 2 (1 + ε ) x − x ∗ 2 . The same upper bound holds for A x ∗ ( x − x ∗ ) 2 . We now bound 2 x − x ∗ ,A T x A x ∗ ( x − x ∗ ) from below. By the MDC, we have

$$
| \langle x - x _ { * } , ( A _ { x } ^ { \Gamma } A _ { x _ { * } } - \Phi _ { x , x _ { * } } ) ( x - x _ { * } ) \rangle | \leqslant \varepsilon \| x - x _ { * } \| ^ { 2 } .
$$

Since x ∈ B ( x ∗ ,ε x ∗ ) , we have that | θ x,x ∗ | 2 ε . Hence Φ x,x ∗ is approximately an isometry since

$$
\| \Phi _ { x , x _ { * } } - I _ { n } \| \leqslant \frac { 2 | \theta _ { x , x _ { * } } | } { \pi } \| I _ { n } \| + \frac { 2 | \sin \theta _ { x , x _ { * } } | } { \pi } \| M _ { \hat { x } \leftrightarrow \hat { x } _ { * } } \| \leqslant \frac { 8 \varepsilon } { \pi }
$$

[Page 7]

where we used ‖ M z ↔ w ‖ /lessorequalslant 1 for all z, w ∈ S n -1 . Combining this with (8), we have 2 〈 x -x ∗ , A T x A x ∗ ( x -x ∗ ) 〉 /greaterorequalslant ( 2 -16 ε π -2 ε ) ‖ x -x ∗ ‖ 2 . Thus we attain

$$
\| ( A _ { x } - A _ { x } ) x _ { * } \| ^ { 2 } & \leq \| A _ { x } ( x - x _ { * } ) \| ^ { 2 } + \| A _ { x } ( x - x _ { * } ) \| ^ { 2 } - 2 \langle x - x _ { * } , A _ { x } ^ { T } A _ { x } , ( x - x _ { * } ) \rangle \\ & \leq \left ( 2 + 2 \varepsilon - 2 + \frac { 1 6 \varepsilon } { \pi } + 2 \varepsilon \right ) \| x - x _ { * } \| ^ { 2 } \\ & = \left ( 4 \varepsilon + \frac { 1 6 \varepsilon } { \pi } \right ) \| x - x _ { * } \| ^ { 2 } . \\ \intertext { F a l l y ,}$ choosing $ \varepsilon $ so that $ \varepsilon \leq 0 . 0 1 $ , $ we c o n l u c $ }
$$

Finally, choosing ε so that ε 0 . 001 , we conclude

$$
2 \| ( A _ { x } - A _ { x * } ) x _ { * } \| & \leqslant 2 \sqrt { 4 \varepsilon + 1 6 \varepsilon / \pi } \| x - x _ { * } \| \leqslant \frac { 1 } { 4 } \| x - x _ { * } \| . \\ \intertext { t h i s i n e q u a l i t y , } \varepsilon < 1 / 4 , \text { and } ( 7 ) \text { shows } \| v _ { x , x _ { * } } - ( x - x _ { * } ) \| \leqslant 1 / 2 \| x - x _ { * } \| + 2
$$

Combining this inequality, ε < 1 / 4 , and (7) shows ‖ v x,x ∗ -( x -x ∗ ) ‖ /lessorequalslant 1 / 2 ‖ x -x ∗ ‖ +2 ‖ η ‖ .

Finally, for non-differentiable x , recall that by (6) we can write v x,x ∗ = ∑ s /lscript =1 c /lscript v /lscript where c /lscript /greaterorequalslant 0 , ∑ s /lscript =1 c /lscript = 1 , and v /lscript = lim δ /lscript ↓ 0 ∇ f ( x + δ /lscript w /lscript ) for some w /lscript ∈ R n . Then, using ∑ s /lscript =1 c /lscript = 1 and our result for differentiable points, we conclude that for x ∈ B ( x ∗ , ε ‖ x ∗ ‖ ) ,

$$
\| v _ { x , x _ { * } } - ( x - x _ { * } ) \| & \leqslant \sum _ { \ell = 1 } ^ { s } c _ { \ell } \| v _ { \ell } - ( x - x _ { * } ) \| \leqslant \sum _ { \ell = 1 } ^ { s } c _ { \ell } \lim _ { \delta \ell \downarrow 0 } \| \nabla f ( x + \delta _ { \ell } w _ { \ell } ) - ( x + \delta _ { \ell } w _ { \ell } - x _ { * } ) \| \\ & \leqslant \frac { 1 } { 2 } \| x - x _ { * } \| + 2 \| \eta \| .
$$

# 3.2 Gaussian Matrices Satisfy the MDC

To show that A satisﬁes the MDC, we will use novel probabilistic tools developed in [ 9 ], which improved the sample complexity required for Gaussian matrices to satisfy a related concentration result introduced in [ 20 ] known as the Weight Distribution Condition. We ﬁrst write A T x A y in a more convenient form. For v ∈ R n , let diag ( v > 0) denote the diagonal matrix whose i -th entry is 1 if v i > 0 and 0 otherwise. Deﬁne diag ( v < 0) analogously. For x ∈ R n , let A + ,x := diag ( Ax > 0) A and A − ,x := diag ( Ax < 0) A . Since sgn ( b ) = { b> 0 } − { b< 0 } for any b ∈ R , observe that

$$
\begin{array} { r l } { = d i a g ( A x < 0 ) A . \ S i n c e s g n ( b ) = \mathbb { I } _ { \{ b > 0 \} } - \mathbb { I } _ { \{ b < 0 \} } \text { for any } b \in \mathbb { R }
$$

We will establish concentration of each term separately. For the ﬁrst term, [ 9 ] recently showed that concentration is possible when m = Ω( n ) :

Lemma 2 (Theorem 3.2 in [ 9 ]) . Fix ε > 0 . If A ∈ R m × n has i.i.d. N (0 , 1 /m ) entries and m Cε − 2 log( ε − 1 ) n , then with probability at least 1 − exp( − cmε 2 / 2) − m exp( − n/ 8) − exp( − m/ 2) , we have T n

$$
\| A _ { + , x } ^ { T } A _ { + , y } - Q _ { x , y } \| \leqslant \varepsilon \ \forall \ x , y \in \mathbb { R } ^ { n }
$$

where Q x,y := π − θ x,y 2 π I n + sin θ x,y 2 π M ˆ x ↔ ˆ y if x,y = 0 and 0 n × n otherwise. Here C and c are absolute constants.

/negationslash

An elementary calculation shows E [ A T + ,x A + ,y ] = Q x,y . Also by symmetry, E [ A T − ,x A − ,y ] = Q x,y . By applying a nearly identical argument as in [ 9 ], the analogous result for A T − ,x A − ,y holds.

[Page 8]

Lemma 3. Fix ε > 0 . If A ∈ R m × n has i.i.d. N (0 , 1 /m ) entries and m Cε − 2 log( ε − 1 ) n then with probability at least 1 − exp( − cmε 2 / 2) − m exp( − n/ 8) − exp( − m/ 2) , we have

$$
\| A _ { - , x } ^ { T } A _ { - , y } - Q _ { x , y } \| \leqslant \varepsilon \, \forall \, x , y \in \mathbb { R } ^ { n } .
$$

Here C and c are absolute constants.

We now extend the argument in [ 9 ] for A T + ,x A − ,y . Note that a result for A T − x A + ,y would be identical. Observe that

$$
\mathbb { E } [ A _ { + , x } ^ { T } A _ { - , y } ] = H _ { x , y } \colon = \frac { \theta _ { x , y } } { 2 \pi } I _ { n } - \frac { \sin \theta _ { x , y } } { 2 \pi } M _ { \hat { x } \leftrightarrow \hat { y } } .
$$

We will prove the following:

Lemma 4. Fix ε > 0 . If A ∈ R m × n has i.i.d. N (0 , 1 /m ) entries and m Cε − 2 log( ε − 1 ) n then with probability at least 1 − exp( − cmε 2 / 2) − m exp( − n/ 8) − exp( − m/ 2) , we have

$$
\| A _ { + , x } ^ { T } A _ { - , y } - H _ { x , y } \| \leqslant \varepsilon \ \forall \ x , y \in \mathbb { R } ^ { n } .
$$

Here C and c are absolute constants.

Note that this would complete Proposition 1 by observing Φ x,y = 2 Q x,y − 2 H x,y and combining Lemmas 2 , 3 , 4 , and an analogous result for A T − x A + ,y , each satisﬁed with ε/ 4 .

The main probabilistic tool in the proof of Lemma 4 is a result concerning concentration of pseudo-Lipschitz functions. Pseudo-Lipschitzness can be considered as a relaxation of standard Lipschitz continuity but with particular attention towards which sets a function is Lipschitz with respect to. When the sets are balls, then the notion of pseudo-Lipschitzness reduces to standard Lipschitzness. Prior to stating the result, we require the following deﬁnitions.

Deﬁnition 2 ( ( δ,γ ) -wide system) . A set system { B t ⊆ R n : t ∈ Θ } is ( δ,γ ) -wide if B t = − B t , B t is convex, and Vol ( B t ∩ δ B ) γ Vol ( δ B ) ∀ t ∈ Θ .

Deﬁnition 3 (pseudo-Lipschitz function) . Suppose there exists a ( δ,γ ) -wide system { B t ⊆ R n : t ∈ Θ } such that | g t ( x ) − g t ( y ) | ε for any t ∈ Θ and x,y ∈ ( R n ) d with x i − y i ∈ B t for all i ∈ [ d ] . Then we say that { g t } t ∈ Θ is ( ε,δ,γ ) -pseudo-Lipschitz.

Note here that a function is pseudo-Lipschitz with respect to a particular system of sets. The following theorem establishes favorable concentration for pseudo-Lipschitz functions.

Theorem 3 (Theorem 4.4 in [ 9 ]) . Let θ be a random variable taking values in Θ . Let { g t : ( R n ) d → R : t ∈ Θ } be a function family and let h : ( R n ) d → R be a function. Let ε,γ,D > 0 and δ ∈ (0 , 1) . Deﬁne the spherical shell H := (1 + δ/ 2) B \ (1 − δ/ 2) B in R n . Suppose:

- 1. For any ﬁxed x ∈ H d , P θ ( g θ ( x ) h ( x ) + ε ) 1 − p,
- 2. { g t } t ∈ Θ is ( ε,δ,γ ) -pseudo-Lipschitz,
- 3. | h ( x ) − h ( y ) | D whenever x ∈ ( S n − 1 ) d , y ∈ ( R n ) d , and y i − x i δ for all i ∈ [ d ] .

Then

$$
\mathbb { P } _ { \theta } \left ( g _ { \theta } ( x ) \leqslant h ( x ) + 2 \varepsilon + D , \ \forall \ x \in ( \mathcal { S } ^ { n - 1 } ) ^ { d } \right ) \geqslant 1 - \gamma ^ { - 2 d } ( 4 / \delta ) ^ { 2 d n } p .
$$

[Page 9]

# 3.2.1 Proof of Lemma 4

For ease of exposition, assume the entries of A are i.i.d. N (0 , 1) . The main idea is that we will concentrate Lipschitz approximations of A T + ,x A − ,y that are upper and lower bounds with respect to the semideﬁnite ordering. For ε ∈ (0 , 1) , deﬁne the following continuous relaxations of { t> 0 } :

/BD

$$
\varphi _ { - \varepsilon } ^ { + } ( t ) \colon = \begin{cases} 0 & t \leq - \varepsilon \\ 1 + t / \varepsilon & - \varepsilon < t \leq 0 \text { and } \varphi _ { \varepsilon } ^ { + } ( t ) \colon = \begin{cases} 0 & t < 0 \\ t / \varepsilon & 0 \leq t < \varepsilon \, . \end{cases} \\ 1 & t > 0 \end{cases}
$$

  1 t > 0   1 Analogously deﬁne the following continuous relaxations for { t< 0 } :

$$
\text {logosically define the following continuous relaxations for } \mathbb { 1 } _ { \{ t < 0 \} } \colon \\ \varphi _ { - \varepsilon } ( t ) \coloneqq \begin{cases} 1 & t \leqslant - \varepsilon \\ - t / \varepsilon & - \varepsilon < t \leqslant 0 \text { and } \varphi _ { - } ^ { - } ( t ) \coloneqq \begin{cases} 1 & t < 0 \\ 0 & t > 0 \end{cases} \end{cases}
$$

Then we have that for all t ∈ R , ϕ + ε ( t ) { t> 0 } ϕ + − ε ( t ) and ϕ − − ε ( t ) { t< 0 } ϕ − ε ( t ) . For V ∈ R m × n with rows v i for i ∈ [ m ] and x,y ∈ R n , deﬁne m

$$
G _ { V , \text {up} } ( x , y ) \colon = \sum _ { i = 1 } ^ { m } \varphi _ { - \varepsilon } ^ { + } ( \langle v _ { i } , x \rangle ) \varphi _ { \varepsilon } ^ { - } ( \langle v _ { i } , y \rangle ) v _ { i } v _ { i } ^ { \text {T} }
$$

and

$$
G _ { V , \text {low} } ( x , y ) \colon = \sum _ { i = 1 } ^ { m } \varphi _ { \varepsilon } ^ { + } ( \langle v _ { i } , x \rangle ) \varphi _ { - \varepsilon } ^ { - } ( \langle v _ { i } , y \rangle ) v _ { i } v _ { i } ^ { \text {T} } . \\ \intertext { x } r \, y \in \mathbb { R } ^ { n } \ G _ { A } \, _ { x } ( r \, y ) \prec A ^ { \text {T} } \quad A \, _ { x } \prec G _ { A } \, _ { x } ( r \, y ) \, \text { so } \, i t \, \text { suffice}
$$

Note that for any x,y ∈ R n , G A, low ( x,y ) A T + ,x A − ,y G A, up ( x,y ) so it suﬃces to upper bound G A, up ( x,y ) and lower bound G A, low ( x,y ) uniformly. For the upper bound, we will prove the following:

Proposition 2. Fix 0 < ε < 1 . Suppose A ∈ R m × n has i.i.d. N (0 , 1) entries. Then if m Cε − 2 log( ε − 1 ) n , we have that with probability at least 1 − exp( − cmε 2 / 2) − m exp( − n/ 8) − exp( m/ 2) ,

$$
G _ { A , u p } ( x , y ) \preceq m H _ { x , y } + m \varepsilon I _ { n } \ \forall \ x , y \neq 0 .
$$

/negationslash

Here C and c are absolute constants.

The central argument can be broken down into three steps and directly follows [ 9 ]. We ﬁrst show that the function g V ( x,y ) := 1 m u,G V, up ( x,y ) u is ( ε,δ,γ ) -pseudo-Lipschitz for ﬁxed u ∈ S n − 1 for appropriate parameters ε,δ, and γ . Second, we use Theorem 3 to establish, for ﬁxed u , concentration of g A ( x,y ) uniformly in x,y to h ( x,y ) := u,H x,y u . Finally, we use a standard ε -net argument to establish uniform concentration over u , guaranteeing an upper bound on G A, up ( x,y ) . Throughout the proof, we will operate on the set of matrices

$$
\Theta \coloneqq \left \{ V \in \mathbb { R } ^ { m \times n } \colon \| V \| \leqslant 3 \sqrt { m } , \ \max _ { i \in [ m ] } \| v _ { i } \| \leqslant \sqrt { 2 n } \right \} . \\
$$

When A is Gaussian, standard results [ 27 ] show that A ∈ Θ with high probability.

Lemma 5. [ 27 ] Suppose A ∈ R m × n has i.i.d. N (0 , 1) entries. Then with probability at least 1 − exp( − m/ 2) − m exp( − n/ 8) , we have A 3 √ m and max i ∈ [ m ] a i √ 2 n .

[Page 10]

Step 1: Establishing pseudo-Lipschitzness. We ﬁrst establish that { g V } V ∈ Θ is pseudoLipschitz with respect to a particular set system.

Lemma 6. Fix ε > 0 and u ∈ S n − 1 . For V ∈ Θ , deﬁne g V ( x,y ) := 1 m u,G V, up ( x,y ) u . Then { g V } V ∈ Θ is (2 ε,ε 2 / 82 , 1 / 2) -pseudo-Lipschitz with respect to the set system { B M,ε 2 ,u } V ∈ Θ where

$$
B _ { M , \varepsilon ^ { 2 } , u } \colon = \left \{ z \in \mathbb { R } ^ { n } \colon \sum _ { i = 1 } ^ { m } | \langle v _ { i } , z \rangle | \langle v _ { i } , u \rangle ^ { 2 } \leqslant \varepsilon ^ { 2 } m \right \}
$$

Proof. We ﬁrst note that it was shown in Lemma 5.5 of [ 9 ] that the set system { B M,ε 2 ,u } V ∈ Θ is ( ε 2 / 82 , 1 / 2) -wide. We now show that { g V } V ∈ Θ is (2 ε,ε 2 / 82 , 1 / 2) -pseudo-Lipschitz. For x,y, ˜ x, ˜ y ∈ R n , suppose y − ˜ y ∈ B M,ε 2 ,u and x − ˜ x ∈ B M,ε 2 ,u . Then observe that

$$
\mathbb { R } ^ { n } , \text { suppose } y - \tilde { y } \in B _ { M , \varepsilon ^ { 2 } , u } \text { and } x - \tilde { x } \in B _ { M , \varepsilon ^ { 2 } , u } \text { . Then observe that } \\ | g _ { V } ( x , y ) - g _ { V } ( \tilde { x } , \tilde { y } ) | & \leq \frac { 1 } { m } \sum _ { i = 1 } ^ { m } | [ \varphi _ { - } ^ { + } ( \langle v _ { i } , x \rangle ) \varphi _ { \varepsilon } ^ { - } ( \langle v _ { i } , y \rangle ) - \varphi _ { - } ^ { + } ( \langle v _ { i } , \tilde { x } \rangle ) \varphi _ { \varepsilon } ^ { - } ( \langle v _ { i } , y \rangle ) | \\ & + | \varphi _ { - } ^ { + } ( \langle v _ { i } , \tilde { x } \rangle ) \varphi _ { \varepsilon } ^ { - } ( \langle v _ { i } , y \rangle ) - \varphi _ { - } ^ { + } ( \langle v _ { i } , \tilde { x } \rangle ) \varphi _ { \varepsilon } ^ { - } ( \langle v _ { i } , \tilde { y } \rangle ) | \langle v _ { i } , u \rangle ^ { 2 } \\ & \leq \frac { 1 } { m } \sum _ { i = 1 } ^ { m } | [ \varphi _ { - } ^ { + } ( \langle v _ { i } , x \rangle ) - \varphi _ { - } ^ { + } ( \langle v _ { i } , \tilde { x } \rangle ) | \\ & + | \varphi _ { \varepsilon } ^ { - } ( \langle v _ { i } , y \rangle ) - \varphi _ { \varepsilon } ^ { - } ( \langle v _ { i } , \tilde { y } \rangle ) | \langle v _ { i } , u \rangle ^ { 2 } \\ & \leq \frac { 1 } { m \varepsilon } \sum _ { i = 1 } ^ { m } | [ \langle v _ { i } , x - \tilde { x } \rangle | + | \langle v _ { i } , y - \tilde { y } \rangle | \langle v _ { i } , u \rangle ^ { 2 } \\ & \leq 2 \varepsilon . \\ \intertext { In the first inequality, we used the triangle inequality. In the second, we used | \varphi _ { - } ^ { + } ( t ) | , | \varphi _ { \varepsilon } ^ { - } ( t ) | \leq 1 . }
$$

In the ﬁrst inequality, we used the triangle inequality. In the second, we used | ϕ + − ε ( t ) | , | ϕ − ε ( t ) | 1 . In the third, we used the fact that ϕ + − ε and ϕ − ε are both 1 /ε -Lipschitz. In the last inequality, we used the assumptions y − ˜ y ∈ B M,ε 2 ,u and x − ˜ x ∈ B M,ε 2 ,u .

Step 2: Point-wise Concentration. We now show that, for ﬁxed u ∈ S n − 1 , g V ( x,y ) concentrates around h ( x,y ) uniformly in x,y by an application of Theorem 3 .

Lemma 7. Fix ε > 0 and u ∈ S n − 1 . Let A ∈ R m × n have i.i.d. N (0 , 1) entries. Deﬁne θ := A | ( A ∈ Θ) . There exist absolute constants c,K, and ˜ C such that

$$
\mathbb { P } _ { \theta } \left ( g _ { \theta } ( x , y ) \leqslant h ( x , y ) + K _ { \varepsilon } \, \forall \, x , y \neq 0 \right ) \geqslant 1 - ( \tilde { C } / \varepsilon ) ^ { 8 n } \exp ( - c m \varepsilon ^ { 2 } ) .
$$

/negationslash

Proof. We will ﬁrst bound E [ G A, up ( x,y )] . Observe that for any t ∈ R , we have ϕ + − ε ( t ) { t − ε } and ϕ − ε ( t ) { t ε } . This implies that for any t 1 ,t 2 ∈ R , + −

$$
\text {and } \varphi _ { \varepsilon } \left ( t \right ) & \leqslant \mathbb { I } _ { \{ t \leqslant \varepsilon \} } \cdot \text {This implies that for any } t _ { 1 } , t _ { 2 } \in \mathbb { R } , \\ & \quad \varphi _ { - \varepsilon } ^ { + } ( t _ { 1 } ) \varphi _ { \varepsilon } ^ { - } ( t _ { 2 } ) \leqslant \mathbb { I } _ { \{ t _ { 1 } \geqslant - \varepsilon \} } \, \mathbb { I } _ { \{ t _ { 2 } \leqslant \varepsilon \} } \leqslant \mathbb { I } _ { \{ t _ { 1 } > 0 \} } \, \mathbb { I } _ { \{ t _ { 2 } < 0 \} } + \mathbb { I } _ { \{ 0 \leqslant t _ { 2 } \leqslant \varepsilon \} } \, .
$$

Thus

$$
\mathbb { E } [ G _ { A , u p } ( x , y ) ] & \preceq \mathbb { E } \left [ \sum _ { i = 1 } ^ { m } ( \mathbb { 1 } _ { \{ a _ { i } , x \rangle > 0 } ) \, \mathbb { 1 } _ { \{ a _ { i } , y \rangle < 0 } \right ] + \mathbb { 1 } _ { \{ - \varepsilon \leqslant ( a _ { i } , x ) \leqslant 0 \} } + \mathbb { 1 } _ { \{ 0 \leqslant ( a _ { i } , y ) \leqslant \varepsilon \} } ) a _ { i } a _ { i } ^ { T } \right ] \\ & = m H _ { x , y } + m \mathbb { E } [ \mathbb { 1 } _ { - \varepsilon \leqslant \langle a , x \rangle \leqslant 0 } \ a a ^ { T } ] + m \mathbb { E } [ \mathbb { 1 } _ { 0 \leqslant \langle a , y \rangle \leqslant \varepsilon } \ a a ^ { T } ]
$$

[Page 11]

where a ∼ N (0 ,I n ) . It was shown in Lemma 12 of [ 20 ] that E [ {− ε a,x 0 } aa T ] ε 2 x I n ∀ x = 0 . An analogous bound shows E [ { 0 a,y ε } aa T ] ε 2 y I n for all y = 0 . Hence we attain

This completes the upper bound on G A, up . The lower bound on G A, low is identical:

/negationslash

$$
\text {gous bound shows } \mathbb { E } [ \mathbb { I } _ { \{ 0 \leq ( a , y ) \leq \varepsilon \} } \ a a ^ { \mathbb { 1 } } ] \preceq & \frac { \varepsilon } { 2 \| y \| } I _ { n } \text { for all } y \neq 0 . \text { Hence we attain} \\ \mathbb { E } [ G _ { A , \text {up} } ( x , y ) ] \preceq & \ m H _ { x , y } + m \left ( \frac { \varepsilon } { 2 \| x \| } + \frac { \varepsilon } { 2 \| y \| } \right ) I _ { n } \text { } \forall \, x , y \neq 0 . \\ \text {dies } \mathbb { F } [ a _ { \ell } ( x , y ) ] < h ( r _ { \ell } , y ) + 2 \varepsilon \text { for } \text {fixed } x \cdot y \in \mathbb { R } ^ { n } \text { with } \| x \| \, \| y \| > 1 / 2
$$

This implies E [ g A ( x, y )] /lessorequalslant h ( x, y ) + 2 ε for fixed x, y ∈ R n with ‖ x ‖ , ‖ y ‖ /greaterorequalslant 1 / 2 .

x , y 1 / 2 . Observe that g A ( x,y ) = 1 m m i =1 ϕ + − ε ( a i ,x ) ϕ − ε ( a i ,y ) a i ,u 2 is a sum of sub-exponential random variables. Hence by Bernstein’s inequality, we have for some absolute constant c and any β > 0 , P ( g A ( x,y ) − E [ g A ( x,y )] > β ) 2exp( − cm min( β,β 2 )) . Taking β = ε and using E [ g A ( x,y )] h ( x,y )+2 ε , we get P ( g A ( x,y ) > h ( x,y ) + 3 ε ) 2exp( − cmε 2 ) . Since P ( A ∈ Θ) 1 / 2 , conditioning on the event A ∈ Θ at most doubles the failure probability so we attain

$$
\mathbb { P } \left ( g _ { \theta } ( x , y ) \leqslant h ( x , y ) + 3 \varepsilon \right ) \geqslant 1 - 4 \exp ( - c m \varepsilon ^ { 2 } ) .
$$

To establish uniform concentration in x,y , we note that by a simple modiﬁcation to Lemma 27 in [ 17 ], we have that H x,y is L -Lipschitz with respect to x,y ∈ S n − 1 where L = 22 /π . Hence | h ( x,y ) − h (˜ x, ˜ y ) | Lε if x − ˜ x ε and y − ˜ y ε . Thus the result follows by applying Theorem 3 to { g V } V ∈ Θ and θ := A | ( A ∈ Θ) . By Lemma 6 , { g V } V ∈ Θ is (2 ε,ε 2 / 82 , 1 / 2) -pseudo-Lipschitz. We can then take p = exp( − cmε 2 ) by ( 9 ) and D = 2 Lε .

Step 3: Uniform Concentration. The last step is to get a uniform bound over all u ∈ S n − 1 . Augmenting our notation, let g V ( x,y,u ) := 1 m u,G V, up ( x,y ) u and h ( x,y,u ) := u,H x,y u . m × n

Lemma 8. Fix ε > 0 . Suppose A ∈ R has i.i.d. N (0 , 1) entries. There exist absolute constants c and C such that if m Cε − 2 log( ε − 1 ) n , then with probability 1 − exp( − cmε 2 / 2) − m exp( − n/ 8) − exp( − m/ 2) , G A, up ( x,y ) mH x,y + mε ∀ x,y = 0 .

$$
G _ { A , u p } ( x , y ) \preceq & \ m H _ { x , y } + m \varepsilon \ \forall x , y \neq 0 . \\
$$

/negationslash

Proof. We ﬁrst show that g V ( x,y,u ) is 18 -Lipschitz with respect to u ∈ S n − 1 when V ∈ Θ . Fix x,y = 0 . Observe that for u,w ∈ S n − 1 , m m

/negationslash

$$
x , y & \neq 0 . \ \text {Observe that } u , w \in \mathcal { S } ^ { 2 } \quad , \\ | g v ( x , y , u ) - g v ( x , y , w ) | & \leq \frac { 1 } { m } \sum _ { i = 1 } ^ { m } | \langle v _ { i } , u \rangle | ^ { 2 } - \langle v _ { i } , w \rangle ^ { 2 } | \leq \frac { 1 } { m } \sum _ { i = 1 } ^ { m } | \langle v _ { i } , u - w \rangle | | ( v _ { i } , u + w ) | \\ & \leq \frac { 1 } { m } \| V ( u - w ) \| \| V ( u + w ) \| \\ & \leq 1 8 \| u - w \| \\ \text {where we used } \| V \| & \leq 3 \sqrt { m } \text { along with } u , w \in \mathcal { S } ^ { n - 1 } \text { in the last inequality.}
$$

where we used ‖ V ‖ /lessorequalslant 3 √ m along with u, w ∈ S n -1 in the last inequality.

7 and a union bound, it holds with probability at least 1 − (3 /ε ) n ( ˜ C/ε ) 8 n exp( − cmε 2 ) over θ = A | ( A ∈ Θ) that for all x,y ∈ S n − 1 and u ∈ N ε , g θ ( x,y,u ) h ( x,y,u ) + Kε. For any x,y,u ∈ S n − 1 , there exists a w ∈ N ε with u − w ε so using ( 10 ) we get

$$
g _ { \theta } ( x , y , u ) \leqslant g _ { \theta } ( x , y , w ) + 1 8 \| u - w \| \leqslant h ( x , y , w ) + 1 8 \varepsilon .
$$

Since H x,y 2 , we have that for u,w ∈ S n − 1 , | h ( x,y,u ) − h ( x,y,w ) | 4 u − w . This further implies g θ ( x,y,u ) h ( x,y,u )+22 ε . We conclude with probability at least 1 − (3 /ε ) n ( C/ε ) 8 n exp( − cmε 2 ) over θ , the desired inequality holds. Using P ( A ∈ Θ) 1 − m exp( − n/ 8) − exp( − m/ 2) and taking m Cε − 2 log( ε − 1 ) n achieves the ﬁnal result with the desired probability.

[Page 12]

Lemma 9. Fix ε > 0 . Suppose A ∈ R m × n has i.i.d. N (0 , 1) entries. There exist absolute constants c and C such that if m Cε − 2 log( ε − 1 ) n , then with probability 1 − exp( − cmε 2 / 2) − m exp( − n/ 8) − exp( − m/ 2) , G A, low ( x,y ) mH x,y − mε ∀ x,y = 0 .

$$
G _ { A , l o w } ( x , y ) \succeq & \ m H _ { x , y } - m \varepsilon \ \forall x , y \neq 0 . \\
$$

Lemma 4 follows by combining Lemma 8 and Lemma 9 .

# References

- [1] Sohail Bahmani and Justin Romberg. A ﬂexible convex relaxation for phase retrieval. Electronic Journal of Statistics , 11(2):5254–5281, 2017.
- [2] Emmanuel J. Candès and Xiaodong Li. Solving quadratic equations via phaselift when there are about as many equations as unknowns. Foundations of Computational Mathematics , 14.5:1017– 1026, 2014.
- [3] Emmanuel J. Candès, Xiaodong Li, and Mahdi Soltanolkotabi. Phase retrieval via wirtinger ﬂow: Theory and applications. IEEE Transactions on Information Theory , 61(4):195–2007, 2017.
- [4] Emmanuel J. Candès, Thomas Strohmer, and Vladislav Voroninski. Phaselift: Exact and stable signal recovery from magnitude measurements via convex programming. Communications on Pure and Applied Mathematics , 66(8):1241–1274, 2013.
- [5] Yuxin Chen and Emmanuel Candès. Solving random quadratic systems of equations is nearly as easy as solving linear systems. Communications on Pure and Applied Mathematics , 70(5):822– 883, 2017.
- [6] Yuxin Chen, Yuejie Chi, Jianqing Fan, and Cong Ma. Gradient descent with random initialization: Fast global convergence for nonconvex phase retrieval. Mathematical Programming , 176(1-2):5–37, 2019.
- [7] Christian Clason. Nonsmooth analysis and optimization. arXiv preprint arXiv:1708.04180 , 2017.
- [8] Jorio Cocola, Paul Hand, and Vladislav Voroninski. Nonasymptotic guarantees for low-rank matrix recovery with generative priors. Advances in Neural Information Processing Systems (NeurIPS) , 2020.
- [9] Constantinos Daskalakis, Dhruv Rohatgi, and Manolis Zampetakis. Constant-expansion suﬃces for compressed sensing with generative priors. Advances in Neural Information Processing Systems (NeurIPS) , 2020.

[10] Laurent Demanet and Paul Hand. Stable optimizationless recovery from phaseless linear measurements. Journal of Fourier Analysis and Applications , 20(1):199–221, 2014.

[Page 13]

- [11] John Duchi and Feng Ruan. Solving (most) of a set of quadratic equalities: Composite optimization for robust phase retrieval. Information and Inference: a Journal of the IMA , 8(3):471–529, 2019.
- [12] Yonina C. Eldar, Georgios B. Giannakis, and Gang Wang. Solving systems of random quadratic equations via truncated amplitude ﬂow. IEEE Transactions on Information Theory , 64(2):773– 794, 2018.
- [13] Albert Fannjiang and Thomas Strohmer. The numerics of phase retrieval. arXiv preprint arXiv:2004.05788 , 2020.
- [14] Tom Goldstein and Christoph Struder. Phasemax: Convex phase retrieval via basis pursuit. IEEE Transactions on Information Theory , 64(4):2675–2689, 2018.
- [15] Paul Hand. Phaselift is robust to a constant fraction of arbitrary errors. Applied and Computational Harmonic Analysis , 42(3):550–562, 2017.
- [16] Paul Hand and Babhru Joshi. Global guarantees for blind demodulation with generative priors. Advances in Neural Information Processing Systems (NeurIPS) , pages 11535–11545, 2019.
- [17] Paul Hand, Oscar Leong, and Vladislav Voroninski. Phase retrieval under a generative prior. Advances in Neural Information Processing Systems (NeurIPS) , pages 9136–9146, 2018.
- [18] Paul Hand, Oscar Leong, and Vladislav Voroninski. Compressive phase retrieval: Optimal sample complexity with deep generative priors. arXiv preprint arXiv:2008.10579 , 2020.
- [19] Paul Hand and Vladislav Voroninski. An elementary proof of convex phase retrieval in the natural parameter space via the linear program phasemax. Communications in Mathematical Sciences , 16(7):2047–2051, 2018.
- [20] Paul Hand and Vladislav Voroninski. Global guarantees for enforcing deep generative priors by empirical risk. IEEE Transactions on Information Theory , 66(1):401–418, 2019.
- [21] Reinhard Heckel, Wen Huang, Paul Hand, and Vladislav Voroninski. Rate-optimal denoising with deep neural networks. Information and Inference: A Journal of the IMA , 2020.
- [22] Wen Huang, Paul Hand, Reinhard Heckel, and Vladislav Voroninski. A provably convergent scheme for compressive sensing under random generative priors. Journal of Fourier Analysis and Applications , 2021.
- [23] Qi Luo and Hongxia Wang. Phase retrieval via smoothed amplitude ﬂow. arXiv preprint arXiv:1909.10685 , 2019.
- [24] Shuang Qiu, Xiaohan Wei, and Zhuoran Yang. Robust one-bit recovery via relu generative networks: Improved statistical rates and global landscape analysis. International Conference on Machine Learning (ICML) , 2020.
- [25] Ju Sun, Qing Qu, and John Wright. A geometric analysis of phase retrieval. Foundations of Computational Mathematics , 18(5):1131–1198, 2018.
- [26] Yan Shuo Tan and Roman Vershynin. Online stochastic gradient descent with arbitrary initialization solves non-smooth, non-convex phase retrieval. arXiv preprint arXiv:1910.12837 , 2019.

[Page 14]

- [27] Roman Vershynin. Introduction to the non-asymptotic analysis of random matrices. Compressed Sensing: Theory and Applications , Cambridge University Press, 2012.
- [28] Bariscan Yonel and Birsen Yazici. A deterministic theory for exact non-convex phase retrieval. IEEE Transactions on Signal Processing , 68:4612–4626, 2020.
- [29] Huishuai Zhang, Yingbin Liang, and Yuejie Chi. A nonconvex approach for phase retrieval: Reshaped wirtinger ﬂow and incremental algorithms. Journal of Machine Learning Research (JMLR) , 18(141):1–35, 2017.
