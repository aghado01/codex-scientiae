\begin{decodedreasoningpanel}
\decodedpanelhead{GPT-5 Codex}
{[\ldots]} Maybe improvement: set grid to `lg:grid-cols-[320px,minmax(0,2fr),1fr]`? On `xl` align.

Maybe lighten search panel card to drop to top on medium but maintain box. For controllers degrade.

Maybe update summary to collapse? Additional `space-y-6` for bottom.

Also ensure \redhl{watchers}.

Focus on left column: On mediums, maybe ensure 340 width causing horizontal scroll? For smaller than 1024 `lg` not apply, so single column.

Maybe need to add `w-full` to aside.

Maybe lighten to `xl:grid-cols-[360px,minmax(0,2fr),360px]` to maintain.

Add `grid-cols-1` default. {[\ldots]}
\end{decodedreasoningpanel}
