# References

Anderson, E. (1936). The Species Problem in Iris, Annals of the Missouri Botanical Garden 23 (3): 457.

Cuppen, J. (1980). A divide and conquer method for the symmetric tridiagonal eigenproblem, Numerische Mathematik 36 (2): 177–195.

Dryden, I. L. and Mardia, K. V. (1998). Statistical Shape Analysis , Wiley Series in Probability and Statistics, John Wiley & Sons, Chichester ; New York.

Fisher, R. A. (1936). THE USE OF MULTIPLE MEASUREMENTS IN TAXONOMIC PROBLEMS, Annals of Eugenics 7 (2): 179–188.

Joliffe, I. T. (2002). Principal Component Analysis , Springer Series in Statistics, Springer-Verlag, New York.

Kim, D. (2015). Periodic signal analysis as an application of principal component analysis , Master’s thesis, Graduate School, Yonsei University, Seoul, South Korea.

Pan, V., Murphy, B., Rosholt, R., Tang, Y., Wang, X. and Zheng, A. (2008). Eigen-solving via reduction to DPR1 matrices, Computers & Mathematics with Applications 56 (1): 166–171.

Pearson, K. (1901). LIII. On lines and planes of closest fit to systems of points in space , The London, Edinburgh, and Dublin Philosophical Magazine and Journal of Science 2 (11): 559–572.

R Core Team (2022). R: A Language and Environment for Statistical Computing , R Foundation for Statistical Computing, Vienna, Austria.

# Appendix

# Proof of Proposition 1

Since ¯ X = X − 1 n ¯ x ⊤ for ¯ x =   n i =1 x i /n , we have the following equality ¯ X ⊤ ¯ X = X ⊤ X − n ¯ x ¯ x ⊤ . It is well known that the largest eigenvector of X ⊤ X is a maximizer of the Rayleigh-Ritz quotient,

$$
w ^ { * } = \arg \max _ { w \in \mathbb { S } ^ { p - 1 } } w ^ { \top } X ^ { \top } X w ,
$$

for S p − 1 = { x ∈ R p | ∥ x ∥ = 1 } . Using the relationship between X and ¯ X , the quantity from Equation ( 2 ) can be upper bounded as follows:

$$
\max _ { w \in \S p - 1 } \ w ^ { \top } X ^ { \top } X w & = \max _ { w \in \S p - 1 } \ w ^ { \top } \left ( \bar { X } ^ { \top } \bar { X } + n \bar { x } \bar { x } ^ { \top } \right ) w \\ & \leq \underbrace { \max _ { ( A ) } \ w ^ { \top } \bar { X } ^ { \top } \bar { X } \bar { x } } _ { ( A ) } + n \cdot \underbrace { \max _ { w \in \S p - 1 } \ w ^ { \top } \bar { x } \bar { x } ^ { \top } } _ { ( B ) } . \\
$$

According to the Cauchy-Schwarz theorem, (A) is maximized when w = ¯ v 1 while (B) achieves its maximum when w is parallel to ¯ x . Therefore, the choice of w = ¯ v 1 = ¯ x/ ∥ ¯ x ∥ attains the maximum as well as the equality by the Courant-Fischer theorem.

# Proof of Corollary 2

Based on the assumption that ¯ x and ¯ v 1 are parallel, we can rewrite the SVD of X as follows:

$$
\text { then that } & \ x \text { and } \bar { v } _ { 1 } \text { are parallel, we can rewrite the S } \\ & \quad X = \bar { X } + 1 _ { n } \bar { x } ^ { \top } \\ & \quad = \bar { X } + 1 _ { n } \| \bar { x } \| \bar { v } _ { 1 } ^ { \top } \\ & \quad = \sum _ { i = 1 } ^ { p } \bar { \sigma } _ { i } \bar { u } _ { i } \bar { v } _ { i } ^ { \top } + 1 _ { n } \| \bar { x } \| \bar { v } _ { 1 } ^ { \top } \\ & \quad = \sum _ { i = 2 } ^ { p } \bar { \sigma } _ { i } \bar { u } _ { i } \bar { v } _ { i } ^ { \top } + ( \bar { \sigma } _ { 1 } \bar { u } _ { 1 } + 1 _ { n } \| \bar { x } \| ) \, \bar { v } _ { 1 } ^ { \top } \\ \text {such that } & \| S \| ^ { 2 } = \bar { \sigma } _ { 1 } ^ { 2 } + n \| \bar { x } \| ^ { 2 } , \text { then the above equation}
$$

Let S = ¯ σ 1 ¯ u 1 + 1 n ∥ ¯ x ∥ such that ∥ S ∥ 2 = ¯ σ 2 1 + n ∥ ¯ x ∥ 2 , then the above equation continues as

$$
& \| ^ { p } \bar { 1 } ^ { \prime } + \| ^ { p } \| ^ { 2 } ) ^ { 2 } + \| S \| ^ { 2 } - \frac { S } { \| S \| ^ { 2 } } \bar { v } _ { 1 } ^ { \top } \\ & = \sum _ { i = 2 } ^ { p } \bar { \sigma } _ { i } \bar { u } _ { i } \bar { v } _ { i } ^ { \top } + \| S \| \cdot \frac { S } { \| S \| ^ { 2 } } \bar { v } _ { 1 } ^ { \top } \\ & = \sum _ { i = 2 } ^ { p } \bar { \sigma } _ { i } \bar { u } _ { i } \bar { v } _ { i } ^ { \top } + \sqrt { \bar { \sigma } _ { 1 } ^ { 2 } + n \| \bar { x } \| ^ { 2 } } u ^ { * } \bar { v } _ { 1 } ^ { \top } , \\ \intertext { s u r c o t o r } & \, v e c t o r \, o f \, X \text { is } &
$$

where the first left singular vector of X is

$$
u ^ { * } = \frac { S } { \| S \| } = \frac { \bar { \sigma } _ { 1 } \bar { u } _ { 1 } + 1 _ { n } \| \bar { x } \| } { \sqrt { \bar { \sigma } _ { 1 } ^ { 2 } + n \| \bar { x } \| ^ { 2 } } } .
$$

$$
\| X z \| ^ { 2 } = z ^ { \top } X ^ { \top } X z \leq z _ { 0 } ^ { \top } X ^ { \top } X z _ { 0 } = \| X z _ { 0 } \| ^ { 2 } ,
$$

under the condition as the Courant-Fischer theorem completes the proof. Using the relationship

$$
X ^ { \top } X = \bar { X } ^ { \top } \bar { X } + n \bar { x } \bar { x } ^ { \top } = \bar { V } \bar { \Sigma } ^ { 2 } \bar { V } ^ { \top } + n \| \bar { x } \| ^ { 2 } z _ { 0 } z _ { 0 } ^ { \top } ,
$$

we first have

$$
\| X z \| ^ { 2 } & = z ^ { \top } X ^ { \top } X z \\ & = z ^ { \top } \left ( \bar { V } \bar { \Sigma } ^ { 2 } \bar { V } ^ { \top } + n \| \bar { x } \| ^ { 2 } z _ { 0 } z _ { 0 } ^ { \top } \right ) z \\ & = \sum _ { i = 1 } ^ { p } \bar { \sigma } _ { i } ^ { 2 } ( z ^ { \top } \bar { v } _ { i } ) ^ { 2 } + n \| \bar { x } \| ^ { 2 } ( z _ { 0 } ^ { \top } z ) ^ { 2 } .
$$

Similarly, we can derive the following

$$
\| X z _ { 0 } \| ^ { 2 } & = z _ { 0 } ^ { \top } X ^ { \top } X z _ { 0 } \\ & = \sum _ { i = 1 } ^ { p } \bar { \sigma } _ { i } ^ { 2 } ( z _ { 0 } ^ { \top } \bar { v } _ { i } ) ^ { 2 } + n \| \bar { x } \| ^ { 2 } ( z _ { 0 } ^ { \top } z _ { 0 } ) \\ & = \sum _ { i = 1 } ^ { p } \bar { \sigma } _ { i } ^ { 2 } ( z _ { 0 } ^ { \top } \bar { v } _ { i } ) ^ { 2 } + n \| \bar { x } \| ^ { 2 } . \\ \intertext { t h e d f i r e n c e } \| X z _ { 0 } \| ^ { 2 } & = \| X z \| ^ { 2 } \text { is shown to be bounded}
$$

Combining the above, the difference ∥ Xz 0 ∥ 2 − ∥ Xz ∥ 2 is shown to be bounded below

$$
Combining the above, the difference \| X z _ { 0 } \| ^ { 2 } - \| X z \| ^ { 2 } & \text { is shown to be bounded below} \\ \| X z _ { 0 } \| ^ { 2 } - \| X z \| ^ { 2 } & = n \| \bar { x } \| ^ { 2 } + \sum _ { i = 1 } ^ { p } \bar { \sigma } _ { i } ^ { 2 } ( z _ { 0 } ^ { \top } \bar { v } _ { i } ) ^ { 2 } - n \| \bar { x } \| ^ { 2 } ( z _ { 0 } ^ { \top } z ) ^ { 2 } - \sum _ { i = 1 } ^ { p } \bar { \sigma } _ { i } ^ { 2 } ( z ^ { \top } \bar { v } _ { i } ) ^ { 2 } \\ & = n \| \bar { x } \| ^ { 2 } ( 1 - ( z _ { 0 } ^ { \top } z ) ^ { 2 } ) + \sum _ { i = 1 } ^ { p } \bar { \sigma } _ { i } ^ { 2 } ( z _ { 0 } ^ { \top } \bar { v } _ { i } ) ^ { 2 } - \sum _ { i = 1 } ^ { p } \bar { \sigma } _ { i } ^ { 2 } ( z ^ { \top } \bar { v } _ { i } ) ^ { 2 } \\ & \geq n \| \bar { x } \| ^ { 2 } ( 1 - ( z _ { 0 } ^ { \top } z ) ^ { 2 } ) + \bar { \sigma } _ { p } ^ { 2 } \sum _ { i = 1 } ^ { p } ( z _ { 0 } ^ { \top } \bar { v } _ { i } ) ^ { 2 } - \bar { \sigma } _ { 1 } ^ { 2 } \sum _ { i = 1 } ^ { p } ( z ^ { \top } \bar { v } _ { i } ) ^ { 2 } \\ \intertext { a n d s c u $ \{ \bar { v } \} $ s pans } \mathbb { R } ^ { p } \text { by the Pythagorean theorem we have }
$$

and since { ¯ v i } spans R p , by the Pythagorean theorem we have

$$
= n \| \bar { x } \| ^ { 2 } ( 1 - ( z _ { 0 } ^ { \top } z ) ^ { 2 } ) + \bar { \sigma } _ { p } ^ { 2 } - \bar { \sigma } _ { 1 } ^ { 2 } .
$$

Hence, if we bound the last line by 0,

$$
n \| \bar { x } \| ^ { 2 } ( 1 - ( z _ { 0 } ^ { \top } z ) ^ { 2 } ) + \bar { \sigma } _ { p } ^ { 2 } - \bar { \sigma } _ { 1 } ^ { 2 } \geq 0 \iff \| \bar { x } \| ^ { 2 } \geq \frac { \bar { \sigma } _ { 1 } ^ { 2 } - \bar { \sigma } _ { p } ^ { 2 } } { n ( 1 - ( z _ { 0 } ^ { \top } z ) ^ { 2 } ) } ,
$$

we obtain the inscribed condition for which z 0 being the largest eigenvector of X ⊤ X .

$$
\| \bar { x } \| ^ { 2 } \geq \frac { \bar { \sigma } _ { \max } ^ { 2 } - \bar { \sigma } _ { \min } ^ { 2 } } { n \cdot ( 1 - ( 1 - \epsilon ) ^ { 2 } ) } .
$$

This is because the statement ∥ Xz ∥ 2 ≤ ∥ Xz 0 ∥ 2 implies that all candidates for the first singular vector are contained in U ϵ . Fix z / ∈ U ϵ such that | z ⊤ z 0 | < 1 − ϵ , which yields

$$
\frac { \bar { \sigma } _ { \max } ^ { 2 } - \bar { \sigma } _ { \min } ^ { 2 } } { 1 - ( z ^ { \top } z _ { 0 } ) ^ { 2 } } < \frac { \bar { \sigma } _ { \max } ^ { 2 } - \bar { \sigma } _ { \min } ^ { 2 } } { 1 - ( 1 - \epsilon ) ^ { 2 } } .
$$

By Proposition 3 , Equation ( 3 ) holds, which completes the proof.

# Proof of Proposition 5

Similar to before, first observe that the mean direction vector z 0 = ¯ x/ ∥ ¯ x ∥ can be written as a linear combination of right singular vectors,

$$
z _ { 0 } = \sum _ { i = 1 } ^ { p } z _ { i } \bar { v } _ { i } = \bar { V } z ,
$$

for some z i ∈ R , i = 1 ,...,p . Using the above, we can rewrite the unscaled covariance matrix of X as follows:

$$
X ^ { \top } X & = \bar { X } ^ { \top } \bar { X } + n \bar { x } \bar { x } ^ { \top } \\ & = \bar { V } \bar { \Sigma } ^ { 2 } \bar { V } ^ { \top } + n \| \bar { x } \| ^ { 2 } \bar { V } z z ^ { \top } \bar { V } ^ { \top } \\ & = \bar { V } ( \underbrace { ( \bar { \Sigma } ^ { 2 } + n \| \bar { x } \| ^ { 2 } z z ^ { \top } ) \bar { V } ^ { \top } } _ { ( ^ { * } ) } )
$$

It was shown from Corollary 2 that the right singular vectors of X and ¯ X are identical. Therefore, the last line of Equation ( 4 ) implies relationship between two sets of eigenvalues for X ⊤ X and ¯ X ⊤ ¯ X . The quantity ( ∗ ) adds a diagonal matrix and an outer product matrix, which is called the diagonal-plus-rank-one (DPR1) matrix ( Pan et al. ; 2008 ). We employ an established result on spectrum for a given DPR1 matrix.

Theorem 8 (Theorem 2.1 of Cuppen ( 1980 )) . Let D be a diagonal matrix, D = diag( d 1 ,...,d p ) , p ≥ 2 with d 1 > d 2 > ··· > d p and µ ∈ R p a vector consisting of all non-zero elements. For a scalar ρ > 0 , the eigenvalues of the matrix D + ρµµ ⊤ are equal to the p roots λ 1 > λ 2 > ··· > λ p of the rational function

$$
w ( \lambda ) & = 1 + \rho \mu ^ { \top } ( D - \lambda I ) ^ { - 1 } \mu \\ & = 1 + \rho \sum _ { j = 1 } ^ { p } \frac { \mu _ { j } ^ { 2 } } { d _ { j } - \lambda } .
$$

The corresponding eigenvectors x 1 ,...,x p of D + ρµµ ⊤ are given by

$$
x _ { j } = \frac { ( D - \lambda _ { j } I ) ^ { - 1 } \mu } { \| ( D - \lambda _ { j } I ) ^ { - 1 } \mu \| } ,
$$

and the d j strictly separate the eigenvalues λ j as follows:

$$
d _ { p } < \lambda _ { p } < \dots < d _ { 2 } < \lambda _ { 2 } < d _ { 1 } < \lambda _ { 1 } < d _ { 1 } + \rho \mu ^ { \top } \mu .
$$

Denote λ j for the j -th squared singular value of ¯ Σ 2 + n ∥ ¯ x ∥ 2 zz ⊤ . Finally, plugging D = ¯ Σ 2 , ρ = n ∥ ¯ x ∥ 2 and µ = z into Theorem 8 completes the proof.

# Proof of Theorem 6

We use the same reasoning to represent the mean direction z 0 as an element in the space of ¯ V , i.e., z 0 = ¯ V z . The first singular value of an unscaled matrix X can be written as

$$
\| X z _ { 0 } \| ^ { 2 } & = z _ { 0 } X ^ { \top } X z _ { 0 } \\ & = z _ { 0 } ^ { \top } \bar { V } ( \bar { \Sigma } ^ { 2 } + n \| \bar { x } \| ^ { 2 } z z ^ { \top } ) \bar { V } ^ { \top } z _ { 0 } \\ & = z ^ { \top } \bar { V } ^ { \top } \bar { V } ( \bar { 2 } ^ { 2 } + n \| \bar { x } \| ^ { 2 } z z ^ { \top } ) V ^ { \bar { V } ^ { \top } \bar { V } _ { z } } \\ & = z ^ { \top } \bar { \Sigma } ^ { 2 } z + n \| \bar { x } \| ^ { 2 } ( z ^ { \top } z ) ^ { 2 } \\ & = z ^ { \top } \bar { \Sigma } ^ { 2 } z + n \| \bar { x } \| ^ { 2 } ,
$$

using the relationship revealed from Equation ( 4 ) and the facts that ¯ V ⊤ ¯ V = I and z ⊤ z = 1. From Proposition 5 and the above, we get a bound for σ 2 1 as

$$
\max \{ \bar { \sigma } _ { 1 } ^ { 2 } , \bar { \sigma } _ { p } ^ { 2 } + n \| \bar { x } \| ^ { 2 } \} = \bar { \sigma } _ { p } ^ { 2 } + n \| \bar { x } \| ^ { 2 } \leq \sigma _ { 1 } ^ { 2 } \leq \bar { \sigma } _ { 1 } ^ { 2 } + n \| \bar { x } \| ^ { 2 } .
$$

The maximum value from Equation ( 5 ) is obtained from an assumption that the norm of the mean vector is large enough as 2 2 2 2

$$
\| \bar { x } \| ^ { 2 } \geq \frac { 1 } { n } \frac { \bar { \sigma } _ { 1 } ^ { 2 } - \bar { \sigma } _ { p } ^ { 2 } } { 1 - ( 1 - \epsilon ) ^ { 2 } } \geq \frac { \bar { \sigma } _ { 1 } ^ { 2 } - \bar { \sigma } _ { p } ^ { 2 } } { n } .
$$

This leads to compare the partial sums of squared singular values of X and ¯ X as

$$
\sum _ { j = 2 } ^ { k + 1 } \bar { \sigma } _ { j } ^ { 2 } + n \| \bar { x } \| ^ { 2 } < \sum _ { j = 1 } ^ { k + 1 } \sigma _ { j } ^ { 2 } < 2 \bar { \sigma } _ { 1 } ^ { 2 } + \sum _ { j = 2 } ^ { k } \bar { \sigma } _ { j } ^ { 2 } + n \| \bar { x } \| ^ { 2 } .
$$

Therefore, the difference of ∥ X ∥ 2 2 ,k +1 and ∥ ¯ X ∥ 2 2 ,k + n ∥ ¯ x ∥ 2 is bounded as follows;

$$
- \bar { \sigma } _ { 1 } ^ { 2 } + \bar { \sigma } _ { k + 1 } ^ { 2 } < \| X \| _ { 2 , k + 1 } ^ { 2 } - ( \| \bar { X } \| _ { 2 , k } ^ { 2 } + n \| \bar { x } \| ^ { 2 } ) < \bar { \sigma } _ { 1 } ^ { 2 } .
$$

$$
- \bar { \sigma } _ { 1 } ^ { 2 } < \| X \| _ { 2 , k + 1 } ^ { 2 } - ( \| \bar { X } \| _ { 2 , k } ^ { 2 } + n \| \bar { x } \| ^ { 2 } ) < \bar { \sigma } _ { 1 } ^ { 2 } ,
$$

which completes the proof.
