# On the spherical Laplace distribution

Kisung You 1 and Dennis Shung 1

1 Department of Internal Medicine, Yale School of Medicine

# Abstract

The von Mises-Fisher (vMF) distribution has long been a mainstay for inference with data on the unit hypersphere in directional statistics. The performance of statistical inference based on the vMF distribution, however, may suﬀer when there are signiﬁcant outliers and noise in the data. Based on an analogy of the median as a robust measure of central tendency and its relationship to the Laplace distribution, we proposed the spherical Laplace (SL) distribution, a novel probability measure for modelling directional data. We present a sampling scheme and theoretical results on maximum likelihood estimation. We derive eﬃcient numerical routines for parameter estimation in the absence of closed-form formula. An application of model-based clustering is considered under the ﬁnite mixture model framework. Our numerical methods for parameter estimation and clustering are validated using simulated and real data experiments.

# 1 Introduction

A prominent discipline in modern data science is to learn from data equipped with certain geometric constraints. The program often assumes that data reside on Riemannian manifolds Lee (1997) and has attracted much attention over the last decades in both theoretical foundation Pennec (2006); Bhattacharya and Bhattacharya (2012); Patrangenaru and Ellingson (2016) and practical applications across many ﬁelds including computer vision Bissacco et al. (2001); Aggarwal et al. (2004) and medical image analysis Pennec et al. (2020); Fletcher et al. (2009); You and Park (2021). In statistics, the relevant research area has long been known as directional statistics Mardia and Jupp (2000); Ley and Verdebout (2017) whose objects of interests include directions, axes, and rotations. Especially, directions seem to take the largest portion in the ﬁeld for its popularity as a number of applications involve observations on the unit hypersphere, which is a model manifold of constant positive curvature.

An oﬀ-the-shelf choice of probability measure on the unit hypersphere is the von Mises-Fisher (vMF) distribution Fisher (1953); Mardia and Jupp (2000), which is a location-scale family distribution that has higher concentration of mass near a mean direction. Recently, the spherical normal (SN) distribution Hauberg (2018) was proposed as an alternative by adopting the geodesic distance on the unit hypersphere. Both distributions are Gaussian-like laws since they are deﬁned using negated squared distances to their location parameters within an exponential function. For both distributions, it is straightforward to see that the maximum likelihood estimate of a location parameter corresponds to the quantity called Fr´echet mean Kendall (1990), which is a minimizer of sum of squared distances.

While these distributions may play an important role in statistical inference, their performance may deteriorate when there exist outliers or noise of large magnitude. In classical robust statistics Huber (1981), it is well acknowledged that the median is a robust measure of central tendency. This has been translated in the ﬁeld of statistics on manifolds to replace Fr´echet mean with Fr´echet median, which minimizes sum of distances Fletcher et al. (2009); Arnaudon et al. (2013). Hence, it is natural to ask what distribution is related to the concept of Fr´echet median on the unit hypersphere as the vMF and SN distributions to the Fr´echet mean.

This motivates our paper to propose the spherical Laplace (SL) distribution. As its name entails, the SL distribution generalizes the Laplace distribution on the real line onto the unit hypersphere by deﬁning its density function using a scaled distance to a mean location rather than squared distance. A leading contribution of this paper is to establish the SL distribution and derive an explicit, simpliﬁed form of a normalizing constant amicable for numerical approximation. We also consider a rejection-based sampling scheme to draw random samples from the SL distribution.

Another class of our contribution is on parameter estimation in the sense of maximum likelihood, which is one of the fundamental tasks for statistical inference. We show that under mild support conditions the SL distribution admits unique parameter estimates. Since the maximum likelihood estimates are solutions of nonlinear equations, we present algorithms for numerical optimization of the parameters.

We consider model-based clustering Bouveyron et al. (2019) as a major application of the SL distribution. We employ the ﬁnite mixture model McLachlan et al. (2019), which is also a major tool for density estimation, for model-based clustering of data on the unit hypersphere using the SL distribution as its components. Computation for the SL mixture is heavily dependent on a generalized version of the parameter estimation algorithms where observations are given ﬁxed weights. We present computational pipeline in detail for the SL mixture in a similar fashion to its predecessors Banerjee et al. (2005); Hornik and Gr¨un (2014). Computational apparatus for the SL distribution is implemented in an R package called Riemann , which is available on CRAN ( https://CRAN.R-project.org/package=Riemann ) and GitHub ( https://github. com/kisungyou/Riemann ).

The rest of this paper is organized as follows. We start by introducing rudimentary background on geometry of hypersphere and describing how vMF and SN distributions are related in Section 2. Section 3 presents the SL distribution along with a rejection sampler and theoretical results regarding maximum likelihood estimation. We present numerical routines for parameter estimation in Section 4. In Section 5, the SL mixture is presented in technical details. The distribution and proposed algorithms are validated via simulated and real data experiments in Section 6. We conclude by discussing potential issues and a list of topics for future studies in Section 7.

# 2 Background

We start this section by introducing notations throughout the paper. Let S p = { x ∈ R p +1 : x = 1 } be a p -dimensional unit sphere in a ( p + 1)-dimensional Euclidean space R p +1 where · is the standard 2 norm. For a metric space ( X ,δ ), an open ball of radius > 0 centered at x ∈ X is denoted as B ( x , ) = { y ∈ X | δ ( x , y ) < } . For two points x , y ∈ S p , the distance d ( x , y ) is reserved to denote the geodesic or shortest-path distance on a sphere. A superscript ( t ) indicates an object at iteration t . The set of positive

# 2.1 Geometry of hypersphere

The unit hypersphere is a model space for a simply connected manifold of constant positive curvature do Carmo (1992); Lee (1997). We review basic properties of the unit sphere as a Riemannian manifold and explicit forms of computational operations Absil et al. (2008).

For some x ∈ S p , the tangent space is characterized as T x S p = { z ∈ R p +1 | x , z = 0 } with a standard inner product · , · . The Riemannian metric for S p is a canonical metric, i.e., g x ( u , v ) = u , v for all u , v ∈ T x S p . The shortest path connecting any two points x , y ∈ S p is along the great circle in that the geodesic distance is given by d ( x , y ) = arccos( x , y ). Given a pair ( x , u ) ∈ M× T x M for some Riemannian manifold M , there exists a unique geodesic γ : [0 , 1] → M such that γ (0) = x and γ (0) = u . An exponential map is deﬁned by Exp x ( u ) = γ (1), which maps a vector in some tangent space to the manifold itself whose inverse is called a logarithmic map, as shown in Figure1. When M = S p , two maps are endowed with explicit formula. Deﬁne an operator Proj x ( z ) = z − x , z x that projects some vector z ∈ R p +1 onto the tangent space T x S p . For x , y ∈ S p and u ∈ T x S p , we have the following expressions;

$$
( \text {exponential map} ) \quad \exp _ { x } ( u ) = \cos ( \| u \| ) x + \frac { \sin ( \| u \| ) } { \| u \| } u , \quad ( 1 a )
$$

$$
( \log a r i m \, \L a r p ) \quad \L Log _ { x } ( y ) = \frac { d ( x , y ) } { \| \Pr o _ { j _ { x } ( y - x ) } \| ^ { 2 } } \Pr o _ { x } ( y - x ) .
$$

An exponential map, by its construction, is only locally deﬁned. The injectivity radius inj( M ) of a manifold M is a maximal radius of a geodesic ball where the exponential map is well-deﬁned and a diﬀeomorphism, which is π for the unit hypersphere.

![image 1](2208.11929v3_images/imageFile1.png)

Figure 1: Visual representation of exponential ( red ) and logarithmic ( blue ) maps on a Riemannian manifold M for some points x , y ∈ M and a tangent vector u ∈ T x M .

# 2.2 The von Mises-Fisher and spherical normal distributions

On the p -dimensional unit hypersphere, the vMF is one of the most popular distributions as an exponential family distribution that has seen many applications Banerjee et al. (2005); Bhattacharya and Dunson (2012);

Gopal and Yang (2014). Parametrized by two parameters for location µ ∈ S p and scale κ ∈ R + , the vMF distribution has the following density function of the form f vMF ( x | µ ,κ ) ∝ exp( κ x µ ). One observation is that the density is characterized by the square of Euclidean distance,

$$
\exp \left ( - \frac { \kappa } { 2 } \| x - \mu \| ^ { 2 } \right ) = \exp \left ( - \frac { \kappa } { 2 } \left \{ x ^ { \top } x - 2 x ^ { \top } \mu + \mu ^ { \top } \mu \right \} \right ) = C ( \kappa ) \exp ( \kappa x ^ { \top } \mu ) ,
$$

where the constant C ( κ ) does not aﬀect the term inside an exponential since x = µ = 1. Therefore, we may roughly consider the vMF distribution as a direct adaptation of the isotropic Gaussian distribution onto the unit hypersphere.

It is natural to question whether the use of standard Euclidean distribution in deﬁning a probability measure on the constrained domain of hypersphere is appropriate. This led to a recent proposal of the SN distribution to replace the Euclidean norm with the geodesic distance. For an isotropic version of the SN distribution, the density function, parametrized by location µ ∈ S p and concentration λ ∈ R + parameters, is of the following form

$$
f _ { \text {SN} } ( x \, | \, \mu , \lambda ) \, \infty \exp \left ( - \frac { \lambda } { 2 } d ^ { 2 } ( x , \mu ) \right ) .
$$

We close this section by discussing the connection between vMF and SN distributions. As we observed in (2), both laws are parametrized by scaled square of some distances from a location parameter. In the literature of statistics on manifolds, these two formulations correspond to the dichotomy of intrinsic and extrinsic frameworks Bhattacharya and Bhattacharya (2012). Roughly speaking, the intrinsic approach measures dissimilarity of two points on a manifold by the shortest-path geodesic joining the two, while the extrinsic framework uses a standard norm after equivariant embedding of the data on manifold onto the Euclidean space Wasserman (1969). Therefore, vMF and SN distributions may be regarded as extrinsic and intrinsic Gaussian-like probability laws on the sphere, respectively. Hence, this motivates to deﬁne a spherical extension of the Laplace distribution by reducing the order of exponent from the SN density function from 2 to 1.

# 3 The spherical Laplace distribution

# 3.1 Deﬁnition

We propose the spherical Laplace (SL) distribution, which is an isotropic location-scale family distribution on the unit hypersphere S p of p ≥ 1.

Deﬁnition 1. Let µ ∈ S p and σ ∈ R + be location and scale parameters, respectively. Density function of the SL distribution is deﬁned by

$$
f _ { S L } ( x \, | \, \mu , \sigma ) = \frac { 1 } { C _ { p } ( \mu , \sigma ) } \exp \left ( - \frac { d ( x , \mu ) } { \sigma } \right ) ,
$$

for a normalizing constant C p ( µ ,σ ) ,

$$
C _ { p } ( \mu , \sigma ) = \int _ { \mathbb { S } ^ { p } } \exp \left ( - \frac { d ( x , \mu ) } { \sigma } \right ) d x .
$$

The scale parameter quantiﬁes the degree of dispersion as shown in Figure2, where a small σ leads to concentrated mass around µ and a large σ leads to largely dispersed distribution of the mass.

It is trivial to observe that (4) is well deﬁned. The integrand is a smooth, bounded function due to the fact that σ is strictly positive and d ( x , µ ) is bounded in (0 ,π ). Since the domain is compact, the integral is ﬁnite. However, evaluation of (4) involves multidimensional integration over the constrained domain, which makes practical usage of the distribution prohibitive. Although a closed-form formula is not available, we show in the following proposition that the expression for normalizing constant can be transformed into a single-variable integration over the bounded domain.

(a)

![image 2](2208.11929v3_images/imageFile2.png)

(b)

(c)

Figure 2: Densities of the SL distribution on S 2 with diﬀerent values of the scale parameter σ with µ = (1 , 1 , 1) / √ 3. For each plot, regions that are colored in red indicate higher densities compared to the area colored as blue .

Proposition 3.1. The normalizing constant (4) can be written as a univariate integral as

$$
C _ { p } ( \mu , \sigma ) = A _ { p - 1 } \int _ { r = 0 } ^ { \pi } \exp \left ( - \frac { r } { \sigma } \right ) \sin ^ { p - 1 } ( r ) d r , & & ( 5 )
$$

where A p − 1 = 2 π p/ 2 / Γ( p/ 2) is the hypervolume or surface area of S p − 1 and Γ( · ) is the standard Gamma function.

Proof: On a complete Riemannian manifold, one can characterize the geodesic distance in terms of logarithmic maps and Riemannian metric as d 2 ( x , µ ) = g µ (Log µ ( x ) , Log µ ( x )), which is equivalent to Log µ ( x ) Log µ ( x ) on S p with a canonical metric. By a change of variable u = Log µ ( x ), a corresponding Jacobian determinant | J | = (sin u / u ) p − 1 is obtained so that

$$
C _ { p } ( \mu , \sigma ) \ & = \ \int _ { \mathbb { S } ^ { p } } \exp \left ( - \frac { d ( x , \mu ) } { \sigma } \right ) d x = \int _ { \mathbb { S } ^ { p } } \exp \left ( - \frac { \| u \| } { \sigma } \right ) d x \\ & = \ \int _ { \| u \| < \pi } \exp \left ( - \frac { \| u \| } { \sigma } \right ) \left ( \frac { \sin \| u \| } { \| u \| } \right ) ^ { p - 1 } d u .
$$

We can express the above using spherical coordinate system,

$$
\begin{array} { r c l } ( 6 ) & = & \int _ { r = 0 } ^ { \pi } \int _ { \varphi _ { p - 0 } = 0 } ^ { \pi } \cdots \int _ { \varphi _ { p - 2 } = 0 } ^ { \pi } \int _ { \varphi _ { p - 1 } = 0 } ^ { 2 \pi } \exp \left ( - \frac { r } { \sigma } \right ) \left ( \frac { \sin r } { r } \right ) ^ { p - 1 } r ^ { p - 1 } \sin ^ { p - 2 } ( \varphi _ { 1 } ) \cdots \sin ( \varphi _ { p - 2 } ) d r d \varphi _ { 1 } \cdots d \varphi _ { p - 1 } \\ & = & \int _ { \varphi _ { 1 } = 0 } ^ { \pi } \cdots \int _ { \varphi _ { p - 2 } = 0 } ^ { \pi } \int _ { \varphi _ { p - 1 } = 0 } ^ { 2 \pi } \sin ^ { p - 2 } ( \varphi _ { 1 } ) \cdots \sin ( \varphi _ { p - 2 } ) d \varphi _ { 1 } \cdots d \varphi _ { p - 1 } \int _ { r = 0 } ^ { \pi } \exp \left ( - \frac { r } { \sigma } \right ) \sin ^ { p - 1 } ( r ) d r , \quad ( 7 ) \end{array}
$$

# Algorithm 1 Rejection Sampling from the SL distribution

Input:

parameters µ ,σ .

Output:

a random sample x .

Set λ = 1 /σ .

repeat

$$
u \sim \text {Uniform} ( 0 , 1 ) .
$$

$$
y \sim f _ { S N } ( y \ | \ \mu , \lambda ) .
$$

$$
r \leftarrow d ( \mu , y ) .
$$

$$
\tau \leftarrow \exp ( ( r ^ { 2 } - 2 r - \pi ^ { 2 } + 2 \pi ) / 2 \sigma ) .
$$

until u ≤ τ .

Take x ← y .

where the ﬁrst term with nested integrals in (7) reduces to the hypervolume A p − 1 of S p − 1 .

Proposition 3.1 does not only lower computational complexity to evaluate the normalizing constant, but also implies that it does not depend on the choice of µ . In what follows, we will denote the normalizing constant of the SL distribution as C p ( σ ) to emphasize its independence on a choice of location µ .

# 3.2 Sampling

It is often of fundamental interest to draw random samples for a given probability distribution. We start by employing a standard rejection sampling technique for its ease of use and eﬃciency Bishop (2006). We take the SN distribution as a proposal density for the rejection sampler whose sampling strategy was well studied in Hauberg (2018). For a SL distribution with parameters ( µ ,σ ) ∈ S p × R + , setting a SN concentration parameter as λ = 1 /σ with the same location µ reduces to a simple rule of acceptance-rejection, which is summarized in Algorithm 1. Technical details regarding the sampling scheme is described in Appendix.

While the rejection sampler is a convenient choice to sample from the SL distribution, one drawback is that the eﬃciency deteriorates when σ ≈ 0. Recall that the distance is upper bounded by π for any two points on the unit hypersphere. When a draw y from a corresponding SN distribution is given, the acceptance threshold τ is deﬁned as

$$
\tau = \exp \left ( \frac { d ^ { 2 } ( \mu , y ) - 2 d ( \mu , y ) - \pi ^ { 2 } + 2 \pi } { 2 \sigma } \right ) = \exp \left ( \frac { ( d ( \mu , y ) - 1 ) ^ { 2 } - ( \pi - 1 ) ^ { 2 } } { 2 \sigma } \right ) ,
$$

where the numerator within an exponential function is always smaller than or equal to 0. It is trivial to observe that τ → 1 as σ → ∞ , implying that the rejection sampler almost always accepts a new sample for larger values of a scale parameter σ . On the other hand, when σ → 0, the threshold converges to 0 in that the sampler requires a tremendously larger number of repeated sampling from its proposal distribution. Hence, it is suggested to use other Markov chain Monte Carlo methods such as the Metropolis-Hastings (MH) algorithm Metropolis et al. (1953); Hastings (1970) in the regime of small σ values. In our numerical experiments, we used the MH sampler where a sequence of samples is generated from a Gaussian distribution on the tangent space at an iterate.

# 3.3 Maximum likelihood estimation

We now consider the task of maximum likelihood estimation for parameters of the SL distribution. Let x 1 ,..., x N ∈ S p be an i.i.d sample from the SL distribution with two parameters, location µ and scale σ . The maximum likelihood estimates ( ˆ µ MLE , ˆ σ MLE ) ∈ S p × R + are maximizers of the following log-likelihood function,

$$
L ( \mu , \sigma ) & = \sum _ { n = 1 } ^ { N } \log \left \{ \frac { 1 } { C _ { p } ( \sigma ) } \exp \left ( - \frac { d ( x _ { n } , \mu ) } { \sigma } \right ) \right \} \\ & = - \frac { 1 } { \sigma } \sum _ { n = 1 } ^ { N } d ( x _ { n } , \mu ) - N \log C _ { p } ( \sigma ) .
$$

One can observe that (8) does not admit closed-form formulae of its maximizers because of the nonlinear terms, engendering the need to employ computational approach for parameter estimation, which will be discussed in the next section.

A closer look at (8) allows us to easily recognize that the constrained maximization with respect to µ is independent on the scale parameter since

$$
\hat { \mu } _ { M L E } = \arg \max _ { \mu \in \mathbb { S } ^ { p } } - \frac { 1 } { \sigma } \sum _ { n = 1 } ^ { N } d ( x _ { n } , \mu ) = \arg \min _ { \mu \in \mathbb { S } ^ { p } } \sum _ { n = 1 } ^ { N } d ( x _ { n } , \mu ) .
$$

In the literature of manifold-valued statistics, the objective to minimize sum of distances has been known as the Fr´echet or geometric median problem Fletcher et al. (2009); Arnaudon et al. (2013). Once ˆ µ MLE is obtained, solving for σ is merely a univariate minimization problem as follows,

$$
\hat { \sigma } _ { M L E } = \arg \max _ { \sigma \in \mathbb { R } ^ { + } } - \frac { 1 } { \sigma } \sum _ { n = 1 } ^ { N } d ( x _ { n } , \hat { \mu } _ { M L E } ) - N \log C _ { p } ( \sigma ) = \arg \min _ { \sigma \in \mathbb { R } ^ { + } } \ \frac { S } { \sigma } + \log C _ { p } ( \sigma ) ,
$$

for a ﬁxed constant S = N n =1 d ( x n , ˆ µ MLE ) /N . We show existence and uniqueness of maximum likelihood estimates in the following theorem under some conditions.

Theorem 3.2. Let x 1 ,..., x N be an i.i.d sample on a p -dimensional unit hypersphere S p . If the sample is contained in an open geodesic ball B ( x ,π/ 4) for some x ∈ S p and not totally contained in any geodesic, maximum likelihood estimates ( ˆ µ MLE , ˆ σ MLE ) uniquely exist.

Proof: We ﬁrst use the characterization of maximum likelihood estimate for location parameter of the SL distribution as the Fre´chet median. Conditions for existence and uniqueness have been extensively studied on a general Riemannian manifold M Yang (2010); Arnaudon et al. (2013), whose statements are phrased as follows for completeness. Let ¯ B ( x ,ρ ) and ∆ denote a closed geodesic ball of radius ρ centered at x ∈ M and an upper bound of sectional curvatures in ¯ B ( x ,ρ ), respectively. Theorem 3.1 of Yang (2010) states that if a sample is not totally contained in any geodesic and the radius satisﬁes

$$
\rho < \min \left \{ \frac { \pi } { 4 \sqrt { \Delta } } , \frac { \text {inj} ( \bar { B } ( x , \rho ) ) } { 2 } \right \} ,
$$

the Fr´ echet median exists and is unique because the objective function becomes strictly convex under the stated conditions. The unit hypersphere has a constant sectional curvature so that ∆ = 1 and an injectivity radius is π at all points. Therefore, the maximal convexity radius ρ is min { π/ 4 , π/ 2 } = π/ 4 on S p . On top of a support condition of a random sample being not totally contained in any geodesic, this establishes the existence and uniqueness of ˆ µ MLE .

We now turn to examine the scale parameter. Let g ( σ ) = S/σ + log C p ( σ ) with a known constant S ∈ [0 ,π ] as the geodesic distance between any two points on the sphere is bounded above by the injectivity radius π . It needs to be shown that g ( σ ) admits a unique critical point. The ﬁrst-order condition g ( σ ) = 0 is equivalent to whether − SC p ( σ )+ σ 2 C p ( σ ) = 0. Hence, it is suﬃcient to show if G ( S ) = − SC p ( σ )+ σ 2 C p ( σ ) has a unique zero in (0 ,π ) for any choice of σ . This implies a bijection between S and σ , establishing the existence and uniqueness for a critical point of g ( σ ) equivalently.

First, it is trivial that G ( S ) is a continuous function since every term consists of smooth, bounded functions on a ﬁnite interval. Second, we have G (0) > 0 as

$$
\begin{array} { r l } { G ( 0 ) } & { = } & { \sigma ^ { 2 } C _ { p } ^ { \prime } ( \sigma ) } \\ & { = } & { \sigma ^ { 2 } \cdot A _ { p - 1 } \int _ { r = 0 } ^ { \pi } \frac { r } { \sigma ^ { 2 } } \exp \left ( - \frac { r } { \sigma } \right ) \sin ^ { p - 1 } ( r ) d r } \\ & { = } & { A _ { p - 1 } \int _ { r = 0 } ^ { \pi } r \exp \left ( - \frac { r } { \sigma } \right ) \sin ^ { p - 1 } ( r ) d r , } \\ { l _ { r } \colon } & { i a r r e s s a l l } \\ { r } & { = } & { A _ { p - 1 } \int _ { r = 0 } ^ { \pi } r \exp \left ( - \frac { r } { \sigma } \right ) \sin ^ { p - 1 } ( r ) d r , } \end{array}
$$

where all terms in the integrand are positive on (0 ,π ). On the other end, G ( π ) < 0 since

$$
G ( \pi ) \ & = \ - \pi C _ { p } ( \sigma ) + \sigma ^ { 2 } C _ { p } ^ { \prime } ( \sigma ) \\ & = \ A _ { p - 1 } \left \{ - \pi \int _ { r = 0 } ^ { \pi } \exp \left ( - \frac { r } { \sigma } \right ) \sin ^ { p - 1 } ( r ) d r + \sigma ^ { 2 } \int _ { r = 0 } ^ { \pi } \frac { r } { \sigma ^ { 2 } } \exp \left ( - \frac { r } { \sigma } \right ) \sin ^ { p - 1 } ( r ) d r \right \} \\ & = \ A _ { p - 1 } \int _ { r = 0 } ^ { \pi } ( - \pi + r ) \exp \left ( - \frac { r } { \sigma } \right ) \sin ^ { p - 1 } ( r ) d r ,
$$

and − π + r < 0 while the other terms in the integrand are strictly positive except for a measure zero set. Lastly, G ( S ) is a monotonically decreasing function. Take S ∈ (0 ,π ) such that S < S , then

$$
\begin{array} { r l r } { G ( S ^ { \prime } ) - G ( S ) } & { = } & { - S ^ { \prime } C _ { p } ( \sigma ) + \sigma ^ { 2 } C _ { p } ^ { \prime } ( \sigma ) - ( - S C _ { p } ( \sigma ) + \sigma ^ { 2 } C _ { p } ^ { \prime } ( \sigma ) ) } \\ & { = } & { ( S - S ^ { \prime } ) C _ { p } ( \sigma ) < 0 . } \end{array}
$$

By the intermediate value theorem and monotonicity, G ( S ) has a unique zero in (0 ,π ) and so does g ( σ ), which completes the proof.

# 4 Parameter estimation

It was observed in the previous section that maximum likelihood estimation for parameters of the SL distribution can be decomposed into two components in a sequential manner to solve the standard Fr´echet median problem for the location parameter µ ∈ S p and a univariate optimization problem for the scale parameter σ ∈ R + . Throughout this section, we consider a more general scenario where each x n is weighted by a non-negative constant w n > 0 that sums to 1, i.e., N n =1 w n = 1, and present adapted algorithms to the context. This is because it not only reduces to the maximum likelihood estimation when all weights are set to be equal, but also this formulation provides useful apparatus for model-based clustering in the following section.

# 4.1 Estimation of the location

Given a random sample x 1 ,..., x N ∈ S p and corresponding non-negative weights w 1 ,...,w N , the weighted Fr´echet median problem is written as

$$
\min _ { \mu \in \mathbb { S } ^ { p } } F ( \mu ) = \min _ { \mu \in \mathbb { S } ^ { p } } \sum _ { n = 1 } ^ { N } w _ { n } d ( \mathbf x _ { n } , \mu ) ,
$$

where ˆ µ MLE is a solution to the special case of (11) if all weights w n ’s are equal such as w 1 = ··· = w N = 1 /N .

We employ a geometric variant of the Weiszfeld algorithm Fletcher et al. (2009), whose standard version has long been known in the Euclidean regime Weiszfeld (1937). In general, the Weiszfeld algorithm follows a descent path, making it to belong to a class of gradient descent methods. At each iteration, an iterate is updated by varying amount of step size determined by a ﬁxed rule. This removes potentially large computational burden to tune step sizes by line search at each iteration.

Now a geometric Weiszfeld algorithm is described for the weighted Fr´echet median problem, which is summarized in Algorithm 2. Roughly speaking, the gradient grad F | x of a scalar-valued function F on a manifold M is deﬁned as a vector ﬁeld, meaning that it takes a value as a tangent vector in T x M . Once an update is done in the gradient direction, an additional step is necessary to push an iterate onto the manifold itself Absil et al. (2008). Using the variation of energy, it can be readily shown that grad d ( µ , x ) | µ = − Log µ ( x ) /d ( µ , x ), leading to deﬁne a gradient of (11) at iteration t as

$$
\text {grad} \ F | _ { \mu ^ { ( t ) } } = - \sum _ { n = 1 } ^ { N } \frac { w _ { n } } { d ( x _ { n } , \mu ^ { ( t ) } ) } \log _ { \mu ^ { ( t ) } } ( x _ { n } ) = - \sum _ { n = 1 } ^ { N } w _ { n } ^ { ( t ) } \log _ { \mu ^ { ( t ) } } ( x _ { n } ) .
$$

The geometric Weiszfeld algorithm updates an iterate by

$$
\mu ^ { ( t + 1 ) } \ & = \ \exp _ { \mu ^ { ( t ) } } \left ( - \alpha ^ { ( t ) } _ { \ } r a d \ F | _ { \mu ^ { ( t ) } } \right ) \\ & = \ \exp _ { \mu ^ { ( t ) } } \left ( \frac { \sum _ { n = 1 } ^ { N } w _ { n } ^ { ( t ) } \log _ { \mu ^ { ( t ) } } ( x _ { n } ) } { \sum _ { n = 1 } ^ { N } w _ { n } ^ { ( t ) } } \right ) ,
$$

with an automatically-determined step size α ( t ) = 1 / N n =1 w ( t ) n . In our context, exponential and logarithmic maps are computed using the formula introduced in (1a) and (1b).

We close by discussing some technical components of Algorithm 2. At the initial stage, a starting point is taken as µ (0) = N n =1 w n x n / N n =1 w n x n . This quantity is equivalent to an extrinsic mean of a random sample Bhattacharya and Bhattacharya (2012), which is also a maximum likelihood estimate of a location parameter from the vMF distribution. When a random sample is adequately concentrated as stated in Theorem 3.2, an extrinsic mean can be a reasonable choice as it is contained in a convex geodesic ball. Moreover, it only requires elementary arithmetic operations without any need for expensive computational routines to be performed. Second, it is standard to halt iterations when an iterate belongs to a set of given observations. A direct reason is that a scaled weight cannot be evaluated if d ( µ ( t +1) , x n ) = 0 for any n at the next iteration. Ad hoc remedies include to re-initialize an algorithm, remove a matching point from computation in the speciﬁc step, or pad 0 with a very small positive number, all of which we do not consider in this paper. Lastly, the stopping criterion by suﬃciently small incremental geodesic change d ( µ ( t ) , µ ( t +1) ) is based from a fact that the unit hypersphere is a complete Riemannian manifold, which guarantees to measure convergence of a sequence in the sense of Cauchy. One may also choose to use the 2 norm-based

$$
O u t p u t \colon \hat { \mu } = \arg \min _ { n = 1 } F ( \mu ) \text { where } F ( \mu ) = \sum _ { n = 1 } ^ { N } w _ { n } d ( \mathbf x _ { n } , \mu ) \text { for } \mu \in \mathbb { S } ^ { p } .
$$

$$
0 \text { Initialize } \mu ^ { ( 0 ) } = \sum _ { n = 1 } ^ { N } w _ { n } x _ { n } / \| \sum _ { n = 1 } ^ { N } w _ { n } x _ { n } \| .
$$

# repeat

Compute scaled weights w ( t ) n = w n /d ( µ ( t ) , x n ) for n ∈ { 1 ,...,N } . Update an iterate µ ( t +1) by (12).

$$
\text { if } \mu ^ { ( t + 1 ) } \ e q u a l s \ t o \ o n e \ o f \ x _ { 1 } , \dots , x _ { N } \ t h e n
$$

stop the algorithm.

end if

$$
\text {until } d ( \mu ^ { ( t ) } , \mu ^ { ( t + 1 ) } ) \text { or } \| \mu ^ { ( t ) } - \mu ^ { ( t + 1 ) } \| \text { is smaller than } \epsilon . \\ \text {Take } \hat { \mu } = \mu ^ { ( t + 1 ) } .
$$

stopping rule since intrinsic and extrinsic distances converge as two points get suﬃciently close on a complete manifold embedded in some Euclidean space.

# 4.2 Estimation of the scale

Maximum likelihood estimation for the scale parameter σ of the SL distribution is merely a univariate minimization of the following objective function

$$
g ( \sigma ) = \frac { S } { \sigma } + \log C _ { p } ( \sigma ) ,
$$

given a ﬁxed constant S = N n =1 d ( x n , ˆ µ MLE ) /N . According to the Theorem 3.2, the cost function admits a unique critical point. Hence, it can be cast as a root-ﬁnding problem of g ( σ ) = 0.

In order to estimate the scale parameter, we use the Newton-Raphson method Ypma (1995); Burden et al. (2016) for root-ﬁnding. When a univariate function of interest f : R → R has continuous derivatives up to order 0, the Newton-Raphson algorithm solves f ( x ) = 0 by updating an iterate as x ( t +1) = x ( t ) − f ( x ( t ) ) /f ( x ( t ) ) with an initial point x (0) . By setting f = g , we can easily derive the updating rule to solve g ( σ ) = 0 by σ ( t +1) = σ ( t ) − g ( σ ( t ) ) /g ( σ ( t ) ). Deﬁne the quantities

$$
\pi
$$

$$
\begin{array} { r l } { = 0 } & { g ( r ) / g ( r ) \cdot D C n c l \, \Omega \, q a n t r i c { 3 } } \\ & { I _ { 0 } ( \sigma ) } & { = } & { \int _ { r = 0 } ^ { \pi } \exp \left ( - \frac { r } { \sigma } \right ) \sin ^ { p - 1 } ( r ) d r , } \\ & { I _ { 1 } ( \sigma ) } & { = } & { \int _ { r = 0 } ^ { \pi } \frac { r } { \sigma ^ { 2 } } \exp \left ( - \frac { r } { \sigma } \right ) \sin ^ { p - 1 } ( r ) d r , } \\ & { I _ { 2 } ( \sigma ) } & { = } & { \int _ { r = 0 } ^ { \pi } \left ( \frac { r ^ { 2 } } { \sigma ^ { 4 } } - \frac { 2 r } { \sigma ^ { 3 } } \right ) \exp \left ( - \frac { r } { \sigma } \right ) \sin ^ { p - 1 } ( r ) d r , } \\ & { r \, \text {rotated to the normalizing constant and its derivative} \, , \, By \, r \, \Omega \, \text {with} \, r \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \Omega \, \
$$

$$
r = 0
$$

which are integrals related to the normalizing constant and its derivatives. By re-writing derivatives of g with respect to the above integrals, we can simplify the Newton-Raphson update at iteration t as

$$
\sigma ^ { ( t + 1 ) } = \sigma ^ { ( t ) } - \left ( - \frac { S } { ( \sigma ^ { ( t ) } ) ^ { 2 } } + \frac { I _ { 1 } ( \sigma ^ { ( t ) } ) } { I _ { 0 } ( \sigma ^ { ( t ) } ) } \right ) \Big / \left ( \frac { 2 S } { ( \sigma ^ { ( t ) } ) ^ { 3 } } + \frac { I _ { 0 } ( \sigma ^ { ( t ) } ) I _ { 2 } ( \sigma ^ { ( t ) } ) - ( I _ { 1 } ( \sigma ^ { ( t ) } ) ) ^ { 2 } } { ( I _ { 0 } ( \sigma ^ { ( t ) } ) ) ^ { 2 } } \right ) .
$$

When σ is close to zero, evaluation of I 1 ( σ ) and I 2 ( σ ) may suﬀer from numerical instability due to presence of σ in rational terms, while I 0 ( σ ) is more robust to obtain from a computational point of view.

-

Output:

σ

ˆ

= argmin g

g

(

σ

where

g

(

σ

) =

S/σ

- log

C

(

σ

for

σ

∈

.

R

MLE

p

Initialize σ (0) .

# repeat

Update an iterate σ ( t +1) by either (13) or (14).

$$
\text {until } | \sigma ^ { ( t ) } - \sigma ^ { ( t + 1 ) } | < \epsilon .
$$

Take ˆ σ MLE = σ ( t +1) .

As a result, an alternative to (13) is considered by computing derivatives with the centered ﬁnite diﬀerence schemes as follows,

$$
\begin{array} { r l } { g ^ { \prime } ( \sigma ) } & \approx } & { \frac { g ( \sigma + h ) - g ( \sigma - h ) } { 2 h } , } \\ { g ^ { \prime \prime } ( \sigma ) } & \approx } & { \frac { g ( \sigma + h ) - 2 g ( \sigma ) + g ( \sigma - h ) } { h ^ { 2 } } , } \\ { \dot { g } ^ { \prime \prime } ( \sigma ) } & \approx } & { \frac { g ( \sigma + h ) - 2 g ( \sigma ) + g ( \sigma - h ) } { h ^ { 2 } } , } \\ { \dot { g } } & \dot { g } = 0 , 0 , 0 , 0 \dot { g } - 1 , \dot { g } \dot { \cdot } , \dot { g } + 1 , \dot { g } - 1 , \dot { g } \dot { \cdot } , \dot { g } } \end{array}
$$

for a suﬃciently small step-size h > 0. By plugging in the approximate derivatives, we get an updating rule

$$
\sigma ^ { ( t + 1 ) } = \sigma ^ { ( t ) } - \frac { h } { 2 } \cdot \frac { g ( \sigma ^ { ( t ) } + h ) - g ( \sigma ^ { ( t ) } - h ) } { g ( \sigma ^ { ( t ) } + h ) - 2 g ( \sigma ^ { ( t ) } ) + g ( \sigma ^ { ( t ) } - h ) } .
$$

We note that an approximate updating rule (14) has equivalent computational complexity to that of (13) as both integrate one-dimensional smooth functions three times per iteration.

# 5 Application to clustering

One of the direct applications for parameter estimation of the SL distribution is probabilistic clustering of sphere-valued data using ﬁnite mixture models McLachlan et al. (2019). The density of a ﬁnite mixture model of K SL components is

$$
h ( x | \Theta ) = \sum _ { k = 1 } ^ { K } \pi _ { k } f _ { S L } ( x | \mu _ { k } , \sigma _ { k } ) ,
$$

for component weights π k , k ∈ { 1 ,...,K } such that K k =1 π k = 1 and parameters Θ = { π k , µ k ,σ k } K k =1 . We follow a standard approach to maximize log-likelihood given a random sample X = { x 1 ,..., x N } ⊂ S p by introducing latent variables for class membership and applying Expectation-Maximization (EM) algorithm Dempster et al. (1977). We refer interested readers to Bishop (2006) for thorough description of the technique. To brieﬂy introduce, denote a binary matrix of latent class memberships as Z ∈ { 0 , 1 } N × K such that every row of Z contains only one non-zero element and the joint distribution of X and Z as

$$
P ( X , Z \, | \, \Theta ) = \prod _ { n = 1 } ^ { N } \prod _ { k = 1 } ^ { K } \left ( \pi _ { k } f _ { S L } ( x _ { n } \, | \, \mu _ { k } , \sigma _ { k } ) \right ) ^ { z _ { n k } } .
$$

The EM algorithm alternates Eand M-steps. At iteration t , the E-step evaluates posterior distribution of the latent variable P( Z | X , Θ ( t ) ) to compute the complete-data log-likelihood

$$
Q ( \Theta \, | \, \Theta ^ { ( t ) } ) = \sum _ { Z } P ( Z \, | \, X , \Theta ^ { ( t ) } ) \log P ( X , Z \, | \, \Theta ) = E _ { Z | X , \Theta ^ { ( t ) } } [ \log P ( X , Z \, | \, \Theta ) ] ,
$$

and the M-step updates all parameters Θ ( t +1) by maximizing Q ( Θ | Θ ( t ) ).

We now elaborate on explicit expressions for updating parameters in a ﬁnite mixture of SL distributions under the standard EM framework at iteration t . The E-step starts by evaluating posterior of the latent variables, which ﬁrst reduces to evaluating E[ z nk ] as follows,

$$
\gamma _ { n k } \colon = E [ z _ { n k } ] = \frac { \pi _ { k } ^ { ( t ) } f _ { S L } ( x _ { n } \ | \ \mu _ { k } ^ { ( t ) } , \sigma _ { k } ^ { ( t ) } ) } { \sum _ { j = 1 } ^ { K } \pi _ { j } ^ { ( t ) } f _ { S L } ( x _ { n } \ | \ \mu _ { j } ^ { ( t ) } , \sigma _ { j } ^ { ( t ) } ) } ,
$$

for ( n,k ) ∈ { 1 ,...,N } × { 1 ,...,K } . Let Γ := γ nk ∈ [0 , 1] N × K denote a matrix of posterior for the latent variables known as soft clustering or membership matrix. An ( n,k )-th entry of Γ encodes information on how likely an observation x n belongs to the k -th cluster. Given the evaluations, the complete-data log-likelihood can be written as

$$
Q ( \Theta \, | \, \Theta ^ { ( t ) } ) = \sum _ { n = 1 } ^ { N } \sum _ { k = 1 } ^ { K } \gamma _ { n k } \left \{ \log \pi _ { k } + \log f _ { S L } ( x _ { n } \, | \, \mu _ { k } , \sigma _ { k } ) \right \} ,
$$

which is an objective function for maximization in the M-step. For each component k ∈ { 1 ,...,K } , weight and location parameters are updated as follows,

$$
\pi _ { k } ^ { ( t + 1 ) } \ = \ \frac { \sum _ { n = 1 } ^ { N } \gamma _ { n k } } { N } , \\ \mu _ { k } ^ { ( t + 1 ) } \ = \ \arg \min _ { \mu \in \mathbb { S } ^ { p } } \ \sum _ { n = 1 } ^ { N } \gamma _ { n k } \cdot d ( x _ { n } , \mu ) ,
$$

where (16) is a weighted Fr´echet median problem that can be solved using Algorithm 2 with w i = γ ik , i ∈ { 1 ,...,N } . Lastly, scale parameters are solutions of the following problems,

$$
\sigma _ { k } ^ { ( t + 1 ) } = \arg \min _ { \sigma \in \mathbb { R } ^ { + } } \, \left ( \frac { \sum _ { n = 1 } ^ { N } \gamma _ { n k } \cdot d ( \mathbf x _ { n } , \mu _ { k } ^ { ( t + 1 ) } ) } { \sum _ { n = 1 } ^ { N } \gamma _ { n k } } \right ) \cdot \frac { 1 } { \sigma } + \log C _ { p } ( \sigma ) ,
$$

which can be solved by Algorithm 3.

We now turn to discuss some aspects of the model and EM algorithm. First, one way to regularize varying scales across multiple components is to use a common scale parameter rather than component-speciﬁc values, i.e., σ = σ 1 = ··· = σ K . In this setting, the model is called homogeneous and a common scale parameter is updated by

$$
\sigma ^ { ( t + 1 ) } = \arg \min _ { \sigma \in \mathbb { R } ^ { + } } \, \left ( \frac { \sum _ { n = 1 } ^ { N } \sum _ { k = 1 } ^ { K } \gamma _ { n k } \cdot d ( \mathbf x _ { n } , \mu _ { k } ^ { ( t + 1 ) } ) } { \sum _ { n = 1 } ^ { N } \sum _ { k = 1 } ^ { K } \gamma _ { n k } } \right ) \cdot \frac { 1 } { \sigma } + \log C _ { p } ( \sigma ) .
$$

Second, computational cost of the algorithm becomes prohibitive when the sample size N grows. Especially, updating location parameters via (16) is problematic since the membership matrix Γ is highly likely to be dense and a weighted Fr´echet median over a large number of observations needs to be computed. We present two popular heuristics to reduce the sample size hard and stochastic assignments that directly manipulate Γ so that the pertained computation is limited to a smaller subset of the sample. Denote the n -th row of Γ as Γ n and sample ( v z , probability = p z ) is a sampling procedure to randomly draw an element

$$
\text {hard} ( \gamma _ { n k } ) \ = \ \begin{cases} 1 , & \text {if $k$ is maximal index of } \Gamma _ { n } , \\ 0 , & \text {otherwise.} \end{cases}
$$

$$
\text {stochastic} ( \gamma _ { n k } ) \ = \ \begin{cases} 1 , & \text {if $k=sample(1\colon K, \text { probability} = \Gamma_{n})$,} \\ 0 , & \text {otherwise.} \end{cases}
$$

The hard assignment is equivalent to predict discrete-valued label for an observation, while the stochastic assignment performs the same task in a probabilistic manner. Given a large sample, these sparsiﬁcation strategies help to reduce execution time and space complexity in updating both location and scale parameters. We note that the hard assignment is optimal in the sense that a lower bound of the incomplete-data loglikelihood is maximized Banerjee et al. (2005).

Third, we describe some practical details from an implementation side. We initialize the algorithm using partition structure obtained from k -means clustering MacQueen (1967). This is because most computing platforms are equipped with eﬃcient, robust implementations of the algorithm. Besides, a large amount of geometric information is preserved by an equivariant embedding Bhattacharya and Bhattacharya (2012), which is the identity map on S p , in that this approach preserves certain degree of geometric information. Another practical issue is to determine convergence of the algorithm. It is natural to terminate iterations if log-likelihood stops to increase. From a computational perspective, however, the log-likelihood is not an appealing option since computing the quantity requires density evaluation with updated parameters at each iteration while no intermediate quantities may be used. Hence, we suggest to use incremental change in a soft clustering matrix Γ ( t +1) − Γ ( t ) as a quantity to terminate iterations since it implies that clustering results do not evolve and no extra updates are needed.

We close this section by considering a potential variant of our model. The ﬁnite mixture model has long been known to be closely related to the k -means algorithm, which has been also observed in the context of directional statistics Dhillon and Modha (2001); Banerjee et al. (2005); Bishop (2006). Assuming the homogeneous model of equal scale, an entry of membership matrix Γ is written as

$$
\gamma _ { n k } = \frac { \pi _ { k } ^ { ( t ) } f _ { S L } ( x _ { n } \ | \ \mu _ { k } ^ { ( t ) } , \sigma _ { k } ^ { ( t ) } ) } { \sum _ { j = 1 } ^ { K } \pi _ { j } ^ { ( t ) } f _ { S L } ( x _ { n } \ | \ \mu _ { j } ^ { ( t ) } , \sigma _ { j } ^ { ( t ) } ) } = \frac { \pi _ { k } \exp \left ( - d ( x _ { n } , \mu _ { k } ) / \sigma \right ) } { \sum _ { j = 1 } ^ { K } \pi _ { j } \exp \left ( - d ( x _ { n } , \mu _ { j } ) / \sigma \right ) } ,
$$

which quantiﬁes likelihood for the n -th observation to belong to the k -th component or cluster. Let k ∗ be an index for d ( x n , µ k ) being the smallest. When σ → 0, the term d ( x n , µ k ∗ ) decays at the slowest rate in the denominator than others so that all other γ nk ’s approach to zero. This procedure is identical to how the k -medians clustering algorithm updates assignment of each observation. We can validate this reasoning by taking a limit of the complete-data log-likelihood

$$
E _ { Z | X , \Theta } [ \log P ( X , Z | \Theta ) ] \xrightarrow { \sigma \to 0 } - \sum _ { n = 1 } ^ { N } \sum _ { k = 1 } ^ { K } \gamma _ { n k } d ( x _ { n } , \mu _ { k } ) + \text {some constant} .
$$

Therefore, in the limit sense, maximizing the expected complete-data log-likelihood amounts to minimization of cost function in a generic k -medians problem, which has not been popularized in directional statistics up to our knowledge.

# 6 Experiments

We come to assess how the proposed algorithms perform for the tasks of parameter estimation and modelbased clustering. The ﬁrst experiment is composed of evaluating parameter estimation algorithms with simulated data. Then, we investigate eﬀectiveness of model-based clustering with the ﬁnite mixture of SL distributions using simulated and real data examples.

# 6.1 Estimating parameters

We evaluate performance of the proposed algorithms for maximum likelihood estimation of two parameters. We start by presenting the simplest case where two ﬁxed parameters µ 0 ∈ (1 , 0 , 0 , 0 , 0 , 0) ∈ S 5 and σ 0 = 0 . 1 are used to generate random samples of varying size from 25 to 475. Each setting is run 100 times and empirical distributions of two performance measures, run time and accuracy, are reported. For numerical optimization, the stopping criterion of = 10 − 8 was used throughout all computations, which is approximately equal to a square root of the machine epsilon in double precision.

For the location estimation problem, we compared the geometric Weiszfeld method as shown in Algorithm 2 and the standard version of Riemannian gradient descent (RGD) algorithm. The latter relies on line search to determine how much an iterate shall proceed along the descent direction. Given a random sample X = { x 1 ,..., x n } ⊂ S 5 and a maximum likelihood estimate ˆ µ MLE , accuracy is measured by the geodesic distance d ( ˆ µ MLE , µ 0 ) = cos − 1 ( ˆ µ MLE , µ 0 ). Two performance measures under the setting are shown in Figure3. As the sample size grows, it is seen that better estimates are obtained in both methods at almost parallel levels of accuracy. In terms of computational cost, however, the Weiszfeld algorithm consistently outperforms the RGD and the degree of disparity gets larger along the growing sample size. This is an expected phenomenon in the sense that line search requires repeated evaluation of the cost functional while the Weiszfeld algorithm requires a single evaluation at each iteration. Therefore, the RGD may converge in fewer iterations than the Weiszfeld algorithm at the increased cost of overall computation.

![image 3](2208.11929v3_images/imageFile3.png)

(a)

(b)

Figure 3: Empirical distribution of performance measures, (a) accuracy and (b) elapsed time, for the location estimation problem. At each run, a random sample from the SL distribution of parameters µ 0 = (1 , 0 , 0 , 0 , 0 , 0) ,σ 0 = 0 . 1 was drawn and the procedure was repeated 100 times.

Similarly, we compared performance of the proposed Newton’s methods against two univariate optimization routines that do not require derivative information. Our ﬁrst choice is a default optimization routine in R programming language R Core Team (2022) that uses golden-section search with successive parabolic interpolation, which we will denote as Roptim . Another is the diﬀerential evolution algorithm for global optimization using a heuristic approach Storn and Price (1997) and will be denoted as DE . We derived two updating rules for the Newton’s methods using exact integral evaluation and approximation of the derivatives using ﬁnite diﬀerence, which will be called as NewtonE and NewtonA , respectively. We measured accuracy of the estimate ˆ σ MLE by relative error | ˆ σ MLE − σ 0 | /σ 0 . Performance measures for all 4 algorithms under the aforementioned setting are summarized in Figure4. The quality of estimation was comparable across all methods, which is not surprising since all algorithms start from an identical location estimate. When it comes to computational eﬃciency, however, both our proposed algorithms show superior performance to the others.

![image 4](2208.11929v3_images/imageFile4.png)

(a)

(b)

Figure 4: Empirical distribution of performance measures, (a) accuracy and (b) elapsed time, for the scale estimation problem. Each run drew a random sample from the SL distribution parametrized by µ 0 = (1 , 0 , 0 , 0 , 0 , 0) ,σ 0 = 0 . 1. The procedure was repeated 100 times.

Besides the simple case, we performed extensive experiments to compare performance of the proposed algorithms across diﬀerent settings. From the tabularized results that are reported in the Supplementary Materials, we veriﬁed the consistent patterns across heterogeneous experimental conditions regarding the accuracy and elapsed time.

# 6.2 Clustering

First, we use simulated data according to the small-mix example from Banerjee et al. (2005) with minor modiﬁcation as adopted in You and Suh (2022). The data generating model in this example is a mixture of two SN distributions on S 1 ⊂ R 2 . Two equally weighted components are parametrized by ( µ 1 ,λ 1 ) = ([ − 0 . 251 , − 0 . 968] , 10) and ( µ 2 ,λ 2 ) = ([0 . 399 , 0 . 917] , 2) with heterogeneous dispersions. In each run, a total of 200 observations are randomly drawn from the model.

![image 5](2208.11929v3_images/imageFile5.png)

(a)

(b)

(c)

(d)

(e)

(f)

Figure 5: Visualization of the small-mix example data consisting of two distinct classes on S 1 . The true label is shown in (a) and clustering results are given from the algorithms as follows; (b) moSL , (c) kmeans , (d) spkmeans , (e) movMF , and (f) moSN when the number of clusters is set as 2.

Table 1: Average of clustering quality indices from 100 runs for the small-mix experiment. For each column, the cell containing bold-face numeric indicates that the corresponding row is the best performing model given the cluster number K and quality index.

|           | Jaccard |         |        | Rand   |        |        | NMI    |        |        |
| --------- | ------- | ------- | ------ | ------ | ------ | ------ | ------ | ------ | ------ |
|           | K =     | 2 K = 3 | K = 4  | K = 2  | K = 3  | K = 4  | K = 2  | K = 3  | K = 4  |
| moSL-soft | 0.9862  | 0.7521  | 0.6107 | 0.9930 | 0.8756 | 0.8052 | 0.9712 | 0.7972 | 0.7194 |
| moSL-hard | 0.9689  | 0.7430  | 0.5861 | 0.9841 | 0.8708 | 0.7926 | 0.9422 | 0.7848 | 0.7015 |
| kmeans    | 0.9575  | 0.7463  | 0.5588 | 0.9782 | 0.8726 | 0.7789 | 0.9270 | 0.7822 | 0.6930 |
| spkmeans  | 0.9465  | 0.7427  | 0.5997 | 0.9724 | 0.8709 | 0.7996 | 0.9118 | 0.7935 | 0.7104 |
| movMF     | 0.9852  | 0.7927  | 0.6841 | 0.9825 | 0.8963 | 0.8424 | 0.9607 | 0.8193 | 0.7582 |
| moSN      | 0.9853  | 0.7907  | 0.6325 | 0.9830 | 0.8953 | 0.8167 | 0.9627 | 0.8225 | 0.7335 |

We now turn to the real data example using household data, which is a survey data of household-level expenditures on several commodity groups among 40 individuals (20 males, 20 females) Everitt and Hothorn (2010). Following the convention of Hornik and Gru¨n (2014), we extracted expenditures of three categories food, housing, and service. Our interest is on the proportion of each category so that a proﬁle vector is obtained by 1 normalization and projected onto S 2 by square-root transformation, which is a common practice in studying compositional data Marron and Dryden (2021).

Similar to the small-mix example, we compared performance of 6 different algorithms for varying number of clusters with the projected household data. Quality of clustering performance was measured by regarding the gender label as ground-truth cluster label in that the true number of clusters is 2. Results are summarized in Table 2. While the SL and vMF mixtures turned out to be the only models that recovered genderseparated clusters, it is worth to mention that our proposed SL mixture model with standard soft membership showed perfect clustering results when model was correctly specified. Comparative visualization of different clustering outputs is presented in Fig. 6. Contrary to the SL mixture that perfectly recovers the gender- separated classes, other methods show somewhat strange patterns. For example, both k -means and spherical k -means misclassified observations near the north pole as belonging to the same class with those near the equator. This may come from the fact that these algorithms do not acknowledge much for the constrained geometry of the domain. This pattern is also witnessed in two mixture models of vMF and SN components that the distinct classes are seemingly contaminated by farthest points. This may be caused by the fact that data from the male group is much more dispersed than that of the female group. When the SL distribution was fitted separately, the females' expenditure data was distributed with ˆ σ MLE = 0 . 0643, which is much smaller than that of the male group with ˆ σ MLE = 0 . 1426. Since the scale parameter σ amounts to variability within the data, this means that the male group, compared to the female group, is less concentrated and shows higher degree of heterogeneity. In reality, it is nearly impenetrable to distinguish presence of outliers from noise of substantial magnitude. Therefore, specifying a mixture model with robust components can help to deal with the scenario that we observed.

Table 2: Clustering quality indices where the number of clusters varies for the household data projected onto S 2 by square-root transformation of compositional data. For each column, the cells with bold-face numerics indicate that the corresponding row is the best performing algorithm given the cluster number K and quality index.

|           | Jaccard |         |        | Rand   |        |        | NMI    |        |        |
| --------- | ------- | ------- | ------ | ------ | ------ | ------ | ------ | ------ | ------ |
|           | K =     | 2 K = 3 | K = 4  | K = 2  | K = 3  | K = 4  | K = 2  | K = 3  | K = 4  |
| moSL-soft | 1.0000  | 0.7275  | 0.6342 | 1.0000 | 0.8603 | 0.8218 | 1.0000 | 0.7244 | 0.7524 |
| moSL-hard | 0.5920  | 0.7275  | 0.5363 | 0.7385 | 0.8603 | 0.7705 | 0.5105 | 0.7244 | 0.6546 |
| kmeans    | 0.5920  | 0.7789  | 0.5363 | 0.7385 | 0.8923 | 0.7705 | 0.5105 | 0.8331 | 0.6546 |
| spkmeans  | 0.5920  | 0.7789  | 0.5363 | 0.7385 | 0.8923 | 0.7705 | 0.5105 | 0.8331 | 0.6546 |
| movMF     | 0.9025  | 0.7275  | 0.6450 | 0.9500 | 0.8603 | 0.8179 | 0.8558 | 0.7244 | 0.6645 |
| moSN      | 0.5920  | 0.7275  | 0.5363 | 0.7385 | 0.8603 | 0.7705 | 0.5105 | 0.7244 | 0.6546 |

![image 3](2208.11929v3_images/imageFile3.png)

(a)

Figure 6: Visualization of the household data on S 2 . The true label deﬁned by gender is shown in (a) and clustering results are given from the algorithms as follows; (b) kmeans , (c) spkmeans , (d) movMF , (e) moSN , and (f) moSL . All models for comparison are speciﬁed with the number of clusters as 2.

# 7 Conclusions

We close by discussing several topics of interests for future studies. As noted before, the current sampling scheme built upon rejection sampler could be ineﬃcient when a scale parameter is set to be very small. We expect that further investigation along this direction makes the proposed distribution a more appealing component not only for statistical inference but also for probabilistic deep learning such as variational autoencoders. Another line of research is reserved for reﬁning clustering procedures. While our current exploration focuses on the ﬁnite mixture framework, availability of an eﬀective sampler helps to open up opportunities such as a nonparametric Bayesian density estimation Bhattacharya and Dunson (2012) and modelling hierarchical and temporal data on the unit hypersphere Gopal and Yang (2014). The last direction is anisotropic extension of the SL distribution as done in Hauberg (2018) for the SN distribution, which will be beneﬁcial in ﬂexible modelling of directionally dependent data on the unit hypersphere.

# Appendix

We describe technical details on derivation of rejection sampler for the SL distribution. A standard rejection sampler aims at drawing random samples from a target density f ( x ) by samples from a proposal distribution g ( x ), assuming the likelihood ratio f ( x ) /g ( x ) is upper bounded by some constant M ∈ (1 , ∞ ) over the entire domain. A draw from g ( x ) is accepted as a random sample from f ( x ) with probability f ( x ) /Mg ( x ) and the process is repeated until a successful draw. For the SL distribution with parameters ( µ ,σ ) ∈ S p × R + , we consider the SN distribution of parameters ( µ , 1 /σ ) ∈ S p × R + as a proposal density. We note that density functions for SN and SL distributions are explicitly written as

$$
f _ { S N } ( x \ | \ \mu , \lambda ) = \frac { 1 } { Z _ { p } ( \lambda ) } \exp \left ( - \frac { \lambda } { 2 } d ^ { 2 } ( x , \mu ) \right ) \quad \text {and} \quad f _ { S L } ( x \ | \ \mu , \sigma ) = \frac { 1 } { C _ { p } ( \sigma ) } \exp \left ( - \frac { 1 } { \sigma } d ( x , \mu ) \right ) ,
$$

for some normalizing constant Z p for the SN distribution Hauberg (2018). Under the scenario, the likelihood ratio is given by

$$
\frac { f _ { S L } ( x \, | \, \mu , \sigma ) } { f _ { S N } ( x \, | \, \mu , \lambda ) } & = \frac { Z _ { p } ( \lambda ) } { C _ { p } ( \sigma ) } \exp \left ( - \frac { 1 } { \sigma } d ( x , \mu ) + \frac { \lambda } { 2 } d ^ { 2 } ( x , \mu ) \right ) \\ & = \frac { Z _ { p } ( 1 / \sigma ) } { C _ { p } ( \sigma ) } \exp \left ( - \frac { 1 } { \sigma } d ( x , \mu ) + \frac { 1 } { 2 \sigma } d ^ { 2 } ( x , \mu ) \right ) \\ & = \frac { Z _ { p } ( 1 / \sigma ) } { C _ { p } ( \sigma ) } \exp \left ( \frac { ( d ( x , \mu ) - 1 ) ^ { 2 } } { 2 \sigma } \right ) \cdot \exp \left ( - \frac { 1 } { 2 \sigma } \right ) \\ & \leq \frac { Z _ { p } ( 1 / \sigma ) } { C _ { p } ( \sigma ) } \exp \left ( \frac { ( ( \pi - 1 ) ^ { 2 } } { 2 \sigma } \right ) \cdot \exp \left ( - \frac { 1 } { 2 \sigma } \right ) = \colon M , \\ \intertext { c o n t a n t \, M \, i s a n \, u p p e r \, b o u n \, o f \, the \, r a t i o . \, A n \, i
$$

where the constant M is an upper bound of the ratio. An inequality comes from the fact that injectivity radius of a unit hypersphere is π , i.e., any two points on the unit hypersphere have distance of π at most.

Then, the corresponding probabilistic acceptance threshold τ is as follows;

$$
T h e n , t h e r c o n r o p s p o r b a b i l i s c a p e c t a n c t h e r s h o l d \tau \, i s \, a s \, f o l l w s ; \\ \tau = \frac { f _ { S L } ( x \, | \, \mu , \sigma ) } { M f _ { S N } ( x \, | \, \mu , 1 / \sigma ) } \\ = \frac { C _ { p } ( \sigma ) } { Z _ { p } ( 1 / \sigma ) } \exp \left ( - \frac { ( \pi - 1 ) ^ { 2 } } { 2 \sigma } \right ) \cdot \exp \left ( \frac { 1 } { 2 \sigma } \right ) \cdot \frac { 1 } { C _ { p } ( \sigma ) } \exp \left ( - \frac { d ( x , \mu ) } { \sigma } \right ) \cdot Z _ { p } ( 1 / \sigma ) \cdot \exp \left ( \frac { 1 } { 2 \sigma } d ^ { 2 } ( x , \mu ) \right ) \\ = \exp \left ( \frac { 1 } { 2 \sigma } d ^ { 2 } ( x , \mu ) - \frac { 1 } { \sigma } d ( x , \mu ) + \frac { 1 } { 2 \sigma } \right ) \cdot \exp \left ( - \frac { ( \pi - 1 ) ^ { 2 } } { 2 \sigma } \right ) \\ = \exp \left ( \frac { ( d ( x , \mu ) - 1 ) ^ { 2 } - ( \pi - 1 ) ^ { 2 } } { 2 \sigma } \right ) .
$$
