[Page 291]

# Exercise 5.30

# Exercise 5.31

The effect of the regularization term is therefore to pull each weight towards the centre of the j th Gaussian, with a force proportional to the posterior probability of that Gaussian for the given weight. This is precisely the kind of effect that we are seeking.

Derivatives of the error with respect to the centres of the Gaussians are also easily computed to give

$$
\frac { \partial \widetilde { E } } { \partial \mu _ { j } } = \lambda \sum _ { i } \gamma _ { j } ( w _ { i } ) \frac { ( \mu _ { i } - w _ { j } ) } { \sigma _ { j } ^ { 2 } } & & ( 5 . 1 4 2 ) \\ \intertext { l e c k i n t i v i e \, i n t r e t a r t i o n \, f u r c a s e u t i n p h s }
$$

which has a simple intuitive interpretation, because it pushes µ j towards an average of the weight values, weighted by the posterior probabilities that the respective weight parameters were generated by component j . Similarly, the derivatives with respect to the variances are given by

$$
\frac { \partial \widetilde { E } } { \partial \sigma _ { j } } = \lambda \sum _ { i } \gamma _ { j } ( w _ { i } ) \left ( \frac { 1 } { \sigma _ { j } } - \frac { ( w _ { i } - \mu _ { j } ) ^ { 2 } } { \sigma _ { j } ^ { 3 } } \right ) \\ \text {yes} \, \sigma _ { i } \text { towards the weighted average of the squared deviations of the weights}
$$

which drives σ j towards the weighted average of the squared deviations of the weights around the corresponding centre µ j , where the weighting coefﬁcients are again given by the posterior probability that each weight is generated by component j . Note that in a practical implementation, new variables η j deﬁned by

$$
\sigma _ { j } ^ { 2 } = \exp ( \eta _ { j } )
$$

are introduced, and the minimization is performed with respect to the η j . This ensures that the parameters σ j remain positive. It also has the effect of discouraging pathological solutions in which one or more of the σ j goes to zero, corresponding to a Gaussian component collapsing onto one of the weight parameter values. Such solutions are discussed in more detail in the context of Gaussian mixture models in Section 9.2.1.

For the derivatives with respect to the mixing coefﬁcients π j , we need to take account of the constraints

$$
\sum _ { j } \pi _ { j } = 1 , \quad 0 \leqslant \pi _ { i } \leqslant 1 \\ \intertext { s u m t a t i o n s } \left ( \sum _ { j } \pi _ { j } = 1 , \quad 0 \leqslant \pi _ { i } \leqslant 1
$$

which follow from the interpretation of the π j as prior probabilities. This can be done by expressing the mixing coefﬁcients in terms of a set of auxiliary variables { η j } using the softmax function given by

$$
\pi _ { j } = \frac { \exp ( \eta _ { j } ) } { \sum _ { k = 1 } ^ { M } \exp ( \eta _ { k } ) } . \\ \text {regularized error function with respect to the } \{ \eta _ { j } \} \text { then take }
$$

The derivatives of the regularized error function with respect to the { η j } then take the form
