# Manifest: Page 001

## REPAIR_MATH
- RAW: ```
V _ { 0 } \rightarrow V _ { 1 } \rightarrow V _ { 2 } \rightarrow V _ { 3 } \rightarrow \cdots
```
  FIX: ```
$$
V _ { 0 } \rightarrow V _ { 1 } \rightarrow V _ { 2 } \rightarrow V _ { 3 } \rightarrow \cdots
$$
```

- RAW: `where each V i is a vector space over a ﬁeld k , and where each arrow represents a linear transformation between the corresponding vector spaces. Each space V i can be thought of, for instance, as the topology of a point cloud measured at scale i .`
  FIX: `where each $V_i$ is a vector space over a ﬁeld $k$, and where each arrow represents a linear transformation between the corresponding vector spaces. Each space $V_i$ can be thought of, for instance, as the topology of a point cloud measured at scale $i$.`

## REPAIR_PROSE
- RAW: ```
the theory of persistence [11], which is

∗ This work is supported under DARPA grant HR0011-05-10007. The second author is also supported by DARPA grant HR0011-07-1-0002.

Permission to make digital or hard copies of all or part of this work for personal or classroom use is granted without fee provided that copies are not made or distributed for proﬁt or commercial advantage and that copies bear this notice and the full citation on the ﬁrst page. To copy otherwise, to republish, to post on servers or to redistribute to lists, requires prior speciﬁc permission and/or a fee. SCG’09, June 8–10, 2009, Aarhus, Denmark.

the simplest special case.
```
  FIX: ```
the theory of persistence [11], which is the simplest special case.
```

- RAW: `CombinatoricsCounting problems`
  FIX: `Combinatorics; Counting problems`
