# Manifest: Page 008

## REPAIR_PROSE
- RAW: ```
glyph[negationslash]
```
  FIX: ```
```

## REPAIR_MATH
- RAW: ```
c _ { i } ^ { l } = \dim ( ( R _ { i } ^ { l } \cap \ K e r \, f ) / ( R _ { i } ^ { l - 1 } \cap \ K e r \, f ) ) = 1 ,
```
  FIX: ```
\[
c _ { i } ^ { l } = \dim ( ( R _ { i } ^ { l } \cap \ K e r \, f ) / ( R _ { i } ^ { l - 1 } \cap \ K e r \, f ) ) = 1 ,
\]
```

- RAW: ```
Case f i : We compute the representation of the boundary of simplex σ in terms of the cycles Z i , and then reduce the result among the boundaries, obtaining: ∂σ = Z i v = Z i ( B i u + v ).
```
  FIX: ```
Case \( f_i \): We compute the representation of the boundary of simplex \( \sigma \) in terms of the cycles \( Z_i \), and then reduce the result among the boundaries, obtaining: \( \partial\sigma = Z_i v = Z_i ( B_i u + v ) \).
```

- RAW: ```
Death: If v = 0, then let j be the row of the lowest nonzero element in vector v .
```
  FIX: ```
Death: If \( v \neq 0 \), then let \( j \) be the row of the lowest nonzero element in vector \( v \).
```

- RAW: ```
Case g i : There are once again two possibilities:
```
  FIX: ```
Case \( g_i \): There are once again two possibilities:
```
