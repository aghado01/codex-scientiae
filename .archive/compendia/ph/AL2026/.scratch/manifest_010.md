# Manifest: Page 010

## REPAIR_MATH
- RAW: ```
\bigoplus _ { j \in [ n ] } P _ { y _ { j } } \xrightarrow { P ( \alpha ) } \bigoplus _ { i \in [ m ] } P _ { x _ { i } } \to M \to 0 ,
```
  FIX: ```
$$
\bigoplus _ { j \in [ n ] } P _ { y _ { j } } \xrightarrow { P ( \alpha ) } \bigoplus _ { i \in [ m ] } P _ { x _ { i } } \to M \to 0 ,
$$
```
- RAW: ```
d _ { M } ( V _ { I } ) = & \, \frac { P ^ { \prime } ( g _ { 1 } ) ( x ) } { \left [ P ^ { \prime } ( g _ { 3 } ) ( x ) \right ] P ^ { \prime } ( g _ { 2 } ) ( x ) } \, \frac { 0 } { 0 } \, \left | P ^ { \prime } ( s c _ { 1 } ( I ) \oplus s c ( \uparrow I ) ) ( \alpha ) } \, 0 \, \right | \\ & - \, \frac { \, \text {rank} \left [ \frac { P ^ { \prime } ( g _ { 1 } ) ( x ) } { 0 } \right | \, \frac { 0 } { P ^ { \prime } ( g _ { 2 } ) ( x ) } \, \left | P ^ { \prime } ( s c _ { 1 } ( I ) \oplus s c ( \uparrow I ) ) ( \alpha ) } \, 0 } { 0 } \, \frac { \, \uparrow P ^ { \prime } ( s k ( I ) ) ( \alpha ) } { P ^ { \prime } ( s k ( I ) ) ( \alpha ) } \right ] . \, ( 1 . 5 )
```
  FIX: ```
$$
d _ { M } ( V _ { I } ) = & \, \frac { P ^ { \prime } ( g _ { 1 } ) ( x ) } { \left [ P ^ { \prime } ( g _ { 3 } ) ( x ) \right ] P ^ { \prime } ( g _ { 2 } ) ( x ) } \, \frac { 0 } { 0 } \, \left | P ^ { \prime } ( s c _ { 1 } ( I ) \oplus s c ( \uparrow I ) ) ( \alpha ) } \, 0 \, \right | \\ & - \, \frac { \, \text {rank} \left [ \frac { P ^ { \prime } ( g _ { 1 } ) ( x ) } { 0 } \right | \, \frac { 0 } { P ^ { \prime } ( g _ { 2 } ) ( x ) } \, \left | P ^ { \prime } ( s c _ { 1 } ( I ) \oplus s c ( \uparrow I ) ) ( \alpha ) } \, 0 } { 0 } \, \frac { \, \uparrow P ^ { \prime } ( s k ( I ) ) ( \alpha ) } { P ^ { \prime } ( s k ( I ) ) ( \alpha ) } \right ] . \, ( 1 . 5 )
$$
```
- RAW: ```
0 \to M \to \bigoplus _ { i \in [ m ^ { \prime } ] } Q _ { x _ { i } ^ { \prime } } \xrightarrow { \mathbb { Q } ( \alpha ^ { \prime } ) } \bigoplus _ { j \in [ n ^ { \prime } ] } Q _ { y _ { j } ^ { \prime } } ,
```
  FIX: ```
$$
0 \to M \to \bigoplus _ { i \in [ m ^ { \prime } ] } Q _ { x _ { i } ^ { \prime } } \xrightarrow { \mathbb { Q } ( \alpha ^ { \prime } ) } \bigoplus _ { j \in [ n ^ { \prime } ] } Q _ { y _ { j } ^ { \prime } } ,
$$
```
- RAW: ```
d _ { M } ( V _ { I } ) = \text {rank} \left [ \frac { P ( g _ { 1 } ) ( x ^ { \prime } ) } { P ( g _ { 3 } ) ( x ^ { \prime } ) } | P ( g _ { 2 } ) ( x ^ { \prime } ) | \begin{array} { c c } \frac { P ( s c ( I ) ) ( \alpha ^ { \prime } ) } { 0 } & 0 \\ 0 & P ( s k ( \downarrow I ) \oplus s k _ { 1 } ( I ) ) ( \alpha ^ { \prime } ) \end{array} \right ] \\ \quad - \text {rank} \left [ \frac { P ( g _ { 1 } ) ( x ^ { \prime } ) } { 0 } | \frac { 0 } { P ( g _ { 2 } ) ( x ^ { \prime } ) } | \begin{array} { c c } P ( s c ( I ) ) ( \alpha ^ { \prime } ) & 0 \\ 0 & P ( s k ( \downarrow I ) \oplus s k _ { 1 } ( I ) ) ( \alpha ^ { \prime } ) \end{array} \right ] .
```
  FIX: ```
$$
d _ { M } ( V _ { I } ) = \text {rank} \left [ \frac { P ( g _ { 1 } ) ( x ^ { \prime } ) } { P ( g _ { 3 } ) ( x ^ { \prime } ) } | P ( g _ { 2 } ) ( x ^ { \prime } ) | \begin{array} { c c } \frac { P ( s c ( I ) ) ( \alpha ^ { \prime } ) } { 0 } & 0 \\ 0 & P ( s k ( \downarrow I ) \oplus s k _ { 1 } ( I ) ) ( \alpha ^ { \prime } ) \end{array} \right ] \\ \quad - \text {rank} \left [ \frac { P ( g _ { 1 } ) ( x ^ { \prime } ) } { 0 } | \frac { 0 } { P ( g _ { 2 } ) ( x ^ { \prime } ) } | \begin{array} { c c } P ( s c ( I ) ) ( \alpha ^ { \prime } ) & 0 \\ 0 & P ( s k ( \downarrow I ) \oplus s k _ { 1 } ( I ) ) ( \alpha ^ { \prime } ) \end{array} \right ] .
$$
```

## REPAIR_PROSE
- RAW: ```
Main result D (Theorems 5.7 and 5.16 ). Let I be an interval of P . If M ∈ mod k [ P ] has a projective presentation
```
  FIX: ```
Main result D (Theorems 5.7 and 5.16). Let \( I \) be an interval of \( P \). If \( M \in \operatorname{mod} k[P] \) has a projective presentation
```
- RAW: ```
where P ( α ) is called the presentation matrix of M , and α denotes a matrix whose entries are morphisms in k [ P ] and column indices are ( x 1 ,...,x m ) = : x . Then we have the following formula for d M ( V I ) :
```
  FIX: ```
where \( P(\alpha) \) is called the presentation matrix of \( M \), and \( \alpha \) denotes a matrix whose entries are morphisms in \( k[P] \) and column indices are \( (x_1, \dots, x_m) =: x \). Then we have the following formula for \( d_M(V_I) \):
```
- RAW: ```
Here, all block matrices in ( 1.5 ) are defined in Remark 5.8 , and can be fast written down by Proposition 5.11 . Note that for the 2D-grid case, we can replace sc 1 ( I ) with sc ◦ 1 ( I ) in ( 1.5 ) .
```
  FIX: ```
Here, all block matrices in (1.5) are defined in Remark 5.8, and can be fast written down by Proposition 5.11. Note that for the 2D-grid case, we can replace \( sc_1(I) \) with \( sc_1^\circ(I) \) in (1.5).
```
- RAW: ```
Dually, if M has an injective copresentation
```
  FIX: ```
Dually, if \( M \) has an injective copresentation
```
- RAW: ```
where Q ( α ′ ) is called the copresentation matrix of M , and α ′ denotes a matrix whose entries are morphisms in k [ P ] and row indices are ( x ′ 1 ,...,x ′ m ′ ) = : x ′ . Then we have the following formula for d M ( V I ) :
```
  FIX: ```
where \( Q(\alpha') \) is called the copresentation matrix of \( M \), and \( \alpha' \) denotes a matrix whose entries are morphisms in \( k[P] \) and row indices are \( (x'_1, \dots, x'_{m'}) =: x' \). Then we have the following formula for \( d_M(V_I) \):
```
- RAW: ```
Note that for the 2D-grid case, we can replace sk 1 ( I ) with sk ◦ 1 ( I ) .
```
  FIX: ```
Note that for the 2D-grid case, we can replace \( sk_1(I) \) with \( sk_1^\circ(I) \).
```
- RAW: ```
The formulas we present are general and apply to all intervals of a poset. However, as the size of poset increases, the number of its intervals also grows. In practical computations, it is therefore inefficient to iterate over every interval. For this reason, in Section 3.3 we provide a criterion in Lemma 3.46 : given a persistence module M , all of its interval summands are constrained by the tops of the projective modules appearing in a minimal projective resolution of M and the socles of the injective modules appearing in a minimal injective coresolution of M . This means that it suffices
```
  FIX: ```
The formulas we present are general and apply to all intervals of a poset. However, as the size of poset increases, the number of its intervals also grows. In practical computations, it is therefore inefficient to iterate over every interval. For this reason, in Section 3.3 we provide a criterion in Lemma 3.46: given a persistence module \( M \), all of its interval summands are constrained by the tops of the projective modules appearing in a minimal projective resolution of \( M \) and the socles of the injective modules appearing in a minimal injective coresolution of \( M \). This means that it suffices
```
