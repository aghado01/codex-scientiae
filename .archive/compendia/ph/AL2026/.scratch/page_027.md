[Page 27]

that is induced from ( 3.25 ) , where π 11 : = π 11 ( d ) is given by π 11 : = ψ t 11 = δ b, d ( b ′ ) P d ( b ′ ) ,b ′ ( b ′ ,b ) ∈ sk( ⇓ I ) × sk( I ) , and by ( 3.26 ) and ( 3.27 ) , the precise form of π ↓ I 1 : = ( ψ ↓ I 1 ) t is given as follows:

$$
$$
\pi _ { 1 } ^ { \downarrow I } = [ P _ { \sim } b _ { a } ] _ { ( b , b _ { d } ) \in \text {sk} ( I ) \times \text {sk} _ { 1 } ( I ) } ^ { t } = [ P _ { \sim b , b _ { d } } ^ { t } ] _ { ( b _ { a } , b ) \in \text {sk} _ { 1 } ( I ) \times \text {sk} ( I ) } = \colon [ \hat { P } _ { b , b _ { d } } ] _ { ( b _ { a } , b ) \in \text {sk} _ { 1 } ( I ) \times \text {sk} ( I ) } ^ { t } ,
$$
$$

where the entry is given by

$$
$$
\hat { P } _ { b , b _ { d } } \colon = \begin{cases} P _ { b , d } & ( b = \underline { b } ) , \\ - P _ { b , d } & ( b = \overline { b } ) , \\ 0 & ( b \not \in \mathfrak { b } ) , \end{cases}
$$
$$

for all b ∈ sk( I ) and b d ∈ sk 1 ( I ) .

Proof By ( 3.25 ) and ( 3.28 ), DV I has a minimal projective presentation

$$
$$
P _ { 1 } ^ { t } \stackrel { \Psi } { \longrightarrow } P _ { s k ( I ) } ^ { t } \stackrel { \psi _ { 0 } } { \longrightarrow } D V _ { I } \to 0 .
$$
$$

Hence by applying (-) t : = Hom A op (, A op ) to Ψ in ( 3.31 ), we have a minimal projective presentation t t

$$
$$
P _ { s k ( I ) } \stackrel { \Psi ^ { t } } { \longrightarrow } P _ { 1 } \stackrel { \text {coker} \, \Psi ^ { t } } { \longrightarrow } \tau ^ { - 1 } V _ { I } \to 0
$$
$$

of τ − 1 V I = Tr DV I in mod A . Hence the assertion follows.


Note that in the projective presentation ( 3.29 ) of τ − 1 V I ⊕ P 2 , both of the projective terms and the form of the morphism ψ t 1 between them is explicitly given, whereas those in the projective presentation ( 3.32 ), the forms of P 1 and Ψ t are not clear. Therefore, we will use the former presentation ( 3.29 ) in our computation. Fortunately, as seen in ( 3.36 ), the unnecessary P 2 does not disturb it because we can give an explicit form of projective presentation of E ⊕ P 2 as follows.

Proposition 3.24. Let I be an interval of P with sc( I ) = { a 1 ,...,a n } and sk( I ) = { b 1 ,...,b m } ( m ≥ 2) . Choose any choice maps c : sc( ⇑ I ) → sc( I ) and d : sk( ⇓ I ) → sk( I ) , and set ε 1 : = ε 1 ( c ) , π 1 : = π 1 ( d ) . Choose also any i ∈ [ n ] . Then there exists some j ∈ [ m ] such that b j ≥ a i . Let λ : = λ ( b j ,a i ) : = [ λ b,a ] ( b,a ) ∈ sk( I ) × sc( I ) to be the matrix with λ b,a = P b j ,a i if ( b,a ) = ( b j ,a i ) and λ b,a = 0 otherwise. Then the following is a projective presentation of E ⊕ P 2 :

$$
$$
( P _ { s c _ { 1 } ( I ) } \oplus P _ { s c ( \uparrow I ) } ) \oplus P _ { s k ( I ) } \xrightarrow { \mu _ { E } } P _ { s c ( I ) } \oplus ( P _ { s k ( \downarrow I ) } \oplus P _ { s k _ { 1 } ( I ) } ) \xrightarrow { \varepsilon _ { E } } E \oplus P _ { 2 } \to 0 . \ \ ( 3 . 3 3 )
$$
$$

Here µ E is given by

$$
$$
\mu _ { E } \coloneqq \left [ \frac { \varepsilon _ { 1 } \left | \, \lambda \, \right \rfloor } { 0 \left | \pi _ { 1 } \right \rfloor } \right ] ,
$$
$$

where ε 1 and π 1 are given in Propositions 3.18 and 3.23 , respectively.
