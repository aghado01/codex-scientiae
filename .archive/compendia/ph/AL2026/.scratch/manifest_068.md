# Manifest: Page 068

## REPAIR_PROSE
- RAW: ```
=


,
```
  FIX: ```
```
- RAW: ```
(

) =

=


[

]





[

]
```
  FIX: ```
```
- RAW: ```
,

(

) =

=


[

]





]

[
```
  FIX: ```
```

## REPAIR_MATH
- RAW: ```
the (not full) subposet Z of P given by
```
  FIX: ```
the (not full) subposet \( Z \) of \( P \) given by
```
- RAW: ```
together with the usual inclusion map ζ : Z → P , then ζ essentially covers P . By Theorem 4.16 it suffices to compute ¯ d R ( M j ) ( R ( V I )) = d R ( M j ) ( V Z ) for j ∈ { 1 , 2 } . Now, because
```
  FIX: ```
together with the usual inclusion map \( \zeta : Z \to P \), then \( \zeta \) essentially covers \( P \). By Theorem 4.16 it suffices to compute \( \bar{d}_{R(M_j)}(R(V_I)) = d_{R(M_j)}(V_Z) \) for \( j \in \{ 1, 2 \} \). Now, because
```
- RAW: ```
we conclude that d M 1 ( V P ) = 1 , but d M 2 ( V P ) = 0 .
```
  FIX: ```
we conclude that \( d_{M_1}(V_P) = 1 \), but \( d_{M_2}(V_P) = 0 \).
```
- RAW: ```
Consider a poset P having the following Hasse quiver:
```
  FIX: ```
Consider a poset \( P \) having the following Hasse quiver:
```
- RAW: ```
Let M ∈ mod k [ P ] given by
```
  FIX: ```
Let \( M \in \operatorname{mod} \mathbb{k}[P] \) given by
```
- RAW: ```
multiplicity of V P in M . In this case, I = P .
```
  FIX: ```
multiplicity of \( V_P \) in \( M \). In this case, \( I = P \).
```
