# Manifest: Page 032
## REPAIR_PROSE
- RAW: ```
Proposition 3.31. For each a ′ ∈ sc( ⇑ I ) , set c ( a ′ ) : = a ∈ sc( I ) if and only if pr 2 ( a ) = min { pr 2 ( c ) | c ≤ a ′ , c ∈ sc( I ) } . Then the interval module V I has the following minimal projective presentation:
```
  FIX: ```
Proposition 3.31. For each \( a' \in \text{sc}(\Uparrow I) \), set \( c(a') := a \in \text{sc}(I) \) if and only if \( \text{pr}_2(a) = \min \{ \text{pr}_2(c) \mid c \le a', c \in \text{sc}(I) \} \). Then the interval module \( V_I \) has the following minimal projective presentation:
```
- RAW: ```
where ε 0 = ρ V I 1 p p ∈ sc( I ) , ε 11 : = δ a, c ( a ′ ) P a ′ , c ( a ′ ) ( a,a ′ ) ∈ sc( I ) × sc( ⇑ I ) , and ε ↑ I 1 is given in ( 3.38 ) by letting U = ↑ I .
```
  FIX: ```
where \( \varepsilon_0 = \rho_{V_I}^1 p_{p \in \text{sc}(I)} \), \( \varepsilon_{11} := \delta_{a, c(a')} P_{a', c(a')} (a,a') \in \text{sc}(I) \times \text{sc}(\Uparrow I) \), and \( \varepsilon^{\uparrow I}_1 \) is given in ( 3.38 ) by letting \( U = \uparrow I \).
```
- RAW: ```
Assume that V I is injective. Then V I ∼ = Q b 1 : = V ↓ b 1 , and hence I = [ ˆ 0 ,b 1 ] , U = P , and ⇑ I = [ { a ′ 1 ,a ′ 2 } ,ω ] , where delete a ′ 1 or a ′ 2 if it does not exist in P . Here and subsequently, we adopt the following void convention: if the set of sources or sinks of an interval is empty, we set that interval to be an empty set and the associated interval module to be 0 . Moreover, V I / soc V I ∼ = V J is also an interval module for the interval J = [ ˆ 0 , { b 1 − (1 , 0) ,b 1 − (0 , 1) } ] , where delete b 1 − (1 , 0) or b 1 − (0 , 1) if it is not in P . Then the pair ( ↑ J, ⇑ J ) is a unique pair of up-sets in P such that J = ↑ J \⇑ J . These are given by ↑ J = P , and ⇑ J = [ { a ′ 1 ,a ′ 2 ,b 1 } ,ω ] . Hence by Theorem 3.3 , Proposition 3.31 , and Lemma 2.10 , we have the following.
```
  FIX: ```
Assume that \( V_I \) is injective. Then \( V_I \cong Q_{b_1} := V_{\downarrow b_1} \), and hence \( I = [\hat{0}, b_1] \), \( U = P \), and \( \Uparrow I = [\{a'_1, a'_2\}, \omega] \), where delete \( a'_1 \) or \( a'_2 \) if it does not exist in \( P \). Here and subsequently, we adopt the following void convention: if the set of sources or sinks of an interval is empty, we set that interval to be an empty set and the associated interval module to be \( 0 \). Moreover, \( V_I / \text{soc} V_I \cong V_J \) is also an interval module for the interval \( J = [\hat{0}, \{b_1 - (1, 0), b_1 - (0, 1)\}] \), where delete \( b_1 - (1, 0) \) or \( b_1 - (0, 1) \) if it is not in \( P \). Then the pair \( (\uparrow J, \Uparrow J) \) is a unique pair of up-sets in \( P \) such that \( J = \uparrow J \setminus \Uparrow J \). These are given by \( \uparrow J = P \), and \( \Uparrow J = [\{a'_1, a'_2, b_1\}, \omega] \). Hence by Theorem 3.3 , Proposition 3.31 , and Lemma 2.10 , we have the following.
```
- RAW: ```
Theorem 3.32. Assume that V I is injective. Then V I and V I / soc V I have minimal projective presentations of the following forms.
```
  FIX: ```
Theorem 3.32. Assume that \( V_I \) is injective. Then \( V_I \) and \( V_I / \text{soc} V_I \) have minimal projective presentations of the following forms.
```
- RAW: ```
where ε ′ 1 = [ P a ′ 1 , ˆ 0 , P a ′ 2 , ˆ 0 ] , and ε ′′ 1 = [ P a ′ 1 , ˆ 0 , P a ′ 2 , ˆ 0 , P b 1 , ˆ 0 ] . Therefore, we have
```
  FIX: ```
where \( \varepsilon'_1 = [P_{a'_1, \hat{0}}, P_{a'_2, \hat{0}}] \), and \( \varepsilon''_1 = [P_{a'_1, \hat{0}}, P_{a'_2, \hat{0}}, P_{b_1, \hat{0}}] \). Therefore, we have
```
- RAW: ```
Proposition 3.33. For each b ′ ∈ sk( ⇓ I ) , set d ( b ′ ) : = b ∈ sk( I ) if and only if pr 2 ( b ) = min { pr 2 ( d ) | b ′ ≤ d ∈ sk( I ) } . Then the interval module DV I has the following minimal projective presentation:
```
  FIX: ```
Proposition 3.33. For each \( b' \in \text{sk}(\Downarrow I) \), set \( d(b') := b \in \text{sk}(I) \) if and only if \( \text{pr}_2(b) = \min \{ \text{pr}_2(d) \mid b' \le d \in \text{sk}(I) \} \). Then the interval module \( DV_I \) has the following minimal projective presentation:
```

## REPAIR_MATH
- RAW: ```
P _ { s c ^ { \circ } _ { 1 } ( I ) } \oplus P _ { s c ( \uparrow I ) } \xrightarrow { \varepsilon _ { 1 } ; [ \varepsilon _ { 1 } ^ { \dagger } , \varepsilon _ { 1 1 } ] } P _ { s c ( I ) } \xrightarrow { \varepsilon _ { 0 } } V _ { I } \rightarrow 0 ,
```
  FIX: ```
$$
P _ { s c ^ { \circ } _ { 1 } ( I ) } \oplus P _ { s c ( \uparrow I ) } \xrightarrow { \varepsilon _ { 1 } ; [ \varepsilon _ { 1 } ^ { \dagger } , \varepsilon _ { 1 1 } ] } P _ { s c ( I ) } \xrightarrow { \varepsilon _ { 0 } } V _ { I } \rightarrow 0 ,
$$
```
- RAW: ```
P _ { s c ( \uparrow I ) } \stackrel { \varepsilon _ { 1 } ^ { \prime } } { \longrightarrow } P _ { \hat { 0 } } \to V _ { I } \to 0 , \\ P _ { s c ( \uparrow I ) } \oplus P _ { b _ { 1 } } \stackrel { \varepsilon _ { 1 } ^ { \prime \prime } } { \longrightarrow } P _ { 0 } \to V _ { I } / s o c V _ { I } \to 0 ,
```
  FIX: ```
$$
P _ { s c ( \uparrow I ) } \stackrel { \varepsilon _ { 1 } ^ { \prime } } { \longrightarrow } P _ { \hat { 0 } } \to V _ { I } \to 0 , \\ P _ { s c ( \uparrow I ) } \oplus P _ { b _ { 1 } } \stackrel { \varepsilon _ { 1 } ^ { \prime \prime } } { \longrightarrow } P _ { 0 } \to V _ { I } / s o c V _ { I } \to 0 ,
$$
```
- RAW: ```
d _ { M } ( V _ { I } ) = \text {rank} \begin{bmatrix} M _ { a _ { 1 } ^ { \prime } , \hat { 0 } } \\ M _ { a _ { 2 } ^ { \prime } , \hat { 0 } } \\ M _ { b _ { 1 } , \hat { 0 } } \end{bmatrix} - \text {rank} \begin{bmatrix} M _ { a _ { 1 } ^ { \prime } , \hat { 0 } } \\ M _ { a _ { 2 } ^ { \prime } , \hat { 0 } } \end{bmatrix} .
```
  FIX: ```
$$
d _ { M } ( V _ { I } ) = \text {rank} \begin{bmatrix} M _ { a _ { 1 } ^ { \prime } , \hat { 0 } } \\ M _ { a _ { 2 } ^ { \prime } , \hat { 0 } } \\ M _ { b _ { 1 } , \hat { 0 } } \end{bmatrix} - \text {rank} \begin{bmatrix} M _ { a _ { 1 } ^ { \prime } , \hat { 0 } } \\ M _ { a _ { 2 } ^ { \prime } , \hat { 0 } } \end{bmatrix} .
$$
```
- RAW: ```
P _ { s k ( \downarrow I ) } ^ { \prime } \oplus P _ { s k _ { 1 } ^ { \circ } ( I ) } ^ { \prime } \xrightarrow { \psi _ { 1 } ; = [ \psi _ { 1 1 } , \psi _ { 1 } ^ { \downarrow I } ] } P _ { s k ( I ) } ^ { \prime } \xrightarrow { \psi _ { 0 } } D V _ { I } \to 0 ,
```
  FIX: ```
$$
P _ { s k ( \downarrow I ) } ^ { \prime } \oplus P _ { s k _ { 1 } ^ { \circ } ( I ) } ^ { \prime } \xrightarrow { \psi _ { 1 } ; = [ \psi _ { 1 1 } , \psi _ { 1 } ^ { \downarrow I } ] } P _ { s k ( I ) } ^ { \prime } \xrightarrow { \psi _ { 0 } } D V _ { I } \to 0 ,
$$
```

