# Manifest: Page 038

## REPAIR_MATH
- RAW: ```
\sup p p ( s o c \, M ) = \{ x _ { 1 } ^ { \prime } , \dots , x _ { m ^ { \prime } } ^ { \prime } \} .
```
  FIX: ```
$$
\operatorname{supp} ( s o c \, M ) = \{ x _ { 1 } ^ { \prime } , \dots , x _ { m ^ { \prime } } ^ { \prime } \} .
$$
```
- RAW: ```
0 \to \Omega ( X ) \oplus \Omega ( Y ) \xrightarrow { \sigma _ { X } \oplus \sigma _ { Y } } P _ { 0 } ( X ) \oplus P _ { 0 } ( Y ) \xrightarrow { \varepsilon _ { X } \oplus \varepsilon _ { Y } } X \oplus Y \to 0
```
  FIX: ```
$$
0 \to \Omega ( X ) \oplus \Omega ( Y ) \xrightarrow { \sigma _ { X } \oplus \sigma _ { Y } } P _ { 0 } ( X ) \oplus P _ { 0 } ( Y ) \xrightarrow { \varepsilon _ { X } \oplus \varepsilon _ { Y } } X \oplus Y \to 0
$$
```
- RAW: ```
P _ { 1 } ( X ) \oplus P _ { 1 } ( Y ) \xrightarrow { \partial _ { 0 } ^ { X } \oplus \partial _ { 0 } ^ { Y } } P _ { 0 } ( X ) \oplus P _ { 0 } ( Y ) \xrightarrow { \varepsilon _ { X } \oplus \varepsilon _ { Y } } X \oplus Y \to 0
```
  FIX: ```
$$
P _ { 1 } ( X ) \oplus P _ { 1 } ( Y ) \xrightarrow { \partial _ { 0 } ^ { X } \oplus \partial _ { 0 } ^ { Y } } P _ { 0 } ( X ) \oplus P _ { 0 } ( Y ) \xrightarrow { \varepsilon _ { X } \oplus \varepsilon _ { Y } } X \oplus Y \to 0
$$
```
- RAW: ```
P ( X ) \oplus P ( Y ) \coloneqq ( P _ { i } ( X ) \oplus P _ { i } ( Y ) , \partial _ { i } ^ { X } \oplus \partial _ { i } ^ { Y } ) _ { i \geq 0 }
```
  FIX: ```
$$
P ( X ) \oplus P ( Y ) \coloneqq ( P _ { i } ( X ) \oplus P _ { i } ( Y ) , \partial _ { i } ^ { X } \oplus \partial _ { i } ^ { Y } ) _ { i \geq 0 }
$$
```
- RAW: ```
\sup p ( \text {top} P _ { i } ( L ) ) \subseteq \sup p ( \text {top} P _ { i } ( M ) ) .
```
  FIX: ```
$$
\operatorname{supp} ( \text {top} P _ { i } ( L ) ) \subseteq \operatorname{supp} ( \text {top} P _ { i } ( M ) ) .
$$
```
- RAW: ```
\sup p { ( s o c \, Q ^ { i } ( L ) ) \subseteq \sup p { ( s o c \, Q ^ { i } ( M ) ) } } .
```
  FIX: ```
$$
\operatorname{supp} { ( s o c \, Q ^ { i } ( L ) ) \subseteq \operatorname{supp} { ( s o c \, Q ^ { i } ( M ) ) } } .
$$
```
- RAW: ```
c r t _ { i } ( M ) \coloneqq \{ I \in \mathbb { I } \ | \ \forall j \text { with } 0 \leq j \leq i , \text {supp} ( \text {top} \, P _ { j } ( V _ { I } ) ) \subseteq \text {supp} ( \text {top} \, P _ { j } ( M ) ) ,
```
  FIX: ```
$$
\operatorname{crt} _ { i } ( M ) \coloneqq \{ I \in \mathbb { I } \ | \ \forall j \text { with } 0 \leq j \leq i , \text {supp} ( \text {top} \, P _ { j } ( V _ { I } ) ) \subseteq \text {supp} ( \text {top} \, P _ { j } ( M ) ) ,
$$
```
- RAW: ```
Let X,Y ∈ mod A . Then
```
  FIX: ```
Let \( X,Y \in \operatorname{mod} A \). Then
```
- RAW: ```
with ε X ⊕ ε Y a projective cover of X ⊕ Y .
```
  FIX: ```
with \( \varepsilon_X \oplus \varepsilon_Y \) a projective cover of \( X \oplus Y \).
```
- RAW: ```
presentation of X ⊕ Y , and more generally,
```
  FIX: ```
presentation of \( X \oplus Y \), and more generally,
```
- RAW: ```
resolution of X ⊕ Y .
```
  FIX: ```
resolution of \( X \oplus Y \).
```
- RAW: ```
Let L,M ∈ mod A , and assume that L is a direct summand of M . Then for each i ≥ 0 , the following statements hold:
```
  FIX: ```
Let \( L,M \in \operatorname{mod} A \), and assume that \( L \) is a direct summand of \( M \). Then for each \( i \geq 0 \), the following statements hold:
```
- RAW: ```
- (1) P i ( L ) is a direct summand of P i ( M ) . Therefore,
```
  FIX: ```
- (1) \( P_i(L) \) is a direct summand of \( P_i(M) \). Therefore,
```
- RAW: ```
- (2) Dually, Q i ( L ) is a direct summand of Q i ( M ) . Therefore,
```
  FIX: ```
- (2) Dually, \( Q_i(L) \) is a direct summand of \( Q_i(M) \). Therefore,
```
- RAW: ```
generalize crt 0 ( M ) to define the following.
```
  FIX: ```
generalize \( \operatorname{crt}_0(M) \) to define the following.
```
- RAW: ```
Let M ∈ mod A , I ∈ I , and i ≥ 0 . Then we define the i -th critical set of intervals of M to be
```
  FIX: ```
Let \( M \in \operatorname{mod} A \), \( I \in \mathbb{I} \), and \( i \geq 0 \). Then we define the \( i \)-th critical set of intervals of \( M \) to be
```
