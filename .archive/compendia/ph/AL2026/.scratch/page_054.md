[Page 54]

and g 3 in k [ P ] as in Definition 4.5 , and set

$$
g \coloneqq g ( c , d , ( b , a ) ) \coloneqq \left [ \frac { g _ { 1 } | \, 0 } { g _ { 3 } | g _ { 2 } } \right ] .
$$

Then there exists an injective module Q such that V I , τV I ⊕ Q and E I ⊕ Q have the following injective copresentations.

$$
0 \to V _ { I } \to \mathbb { Q } ( \mathbf s k ( I ) ) \xrightarrow { \mathbb { Q } ( g _ { 2 } ) } \mathbb { Q } ( \mathbf s k ( \psi I ) \oplus \mathbf s k _ { 1 } ( I ) ) ,
$$

$$
0 \to \tau V _ { I } \oplus Q \to Q ( s c _ { 1 } ( I ) \oplus s c ( \uparrow I ) ) \xrightarrow { \mathbb { Q } ( g _ { 1 } ) } \mathbb { Q } ( s c ( I ) ) ,
$$

$$
0 \to E _ { I } \oplus Q \to Q ( s c _ { 1 } ( I ) \oplus s c ( \uparrow I ) \oplus s k ( I ) ) \xrightarrow { \mathbb { Q } ( g ) } Q ( s c ( I ) \oplus s k ( \downarrow I ) \oplus s k _ { 1 } ( I ) ) .
$$

Proof By applying D to ( 5.62 ), we have the following almost split sequence of DV I in mod A op :

$$
0 \to D V _ { I } \to D E _ { I } \to \text {Tr} \, V _ { I } \to 0 .
$$

→ I → I → I → Since DV I ∼ = V P op I op , this sequence becomes an almost split sequence starting from V P op I op , and hence we can compute projective presentations of these three terms by Proposition 3.18 and 3.23 and ( 3.33 ). For DV I it is already given in ( 3.25 ). Altogether, these have the following forms:

$$
P ^ { \prime } ( s k ( \downarrow I ) \oplus s k _ { 1 } ( I ) ) \xrightarrow { P ^ { \prime } ( g _ { 2 } ) } P ^ { \prime } ( s k ( I ) ) \to D V _ { I } \to 0 ,
$$

$$
P ^ { \prime } ( s c ( I ) ) \xrightarrow { P ^ { \prime } ( g _ { 1 } ) } P ^ { \prime } ( s c _ { 1 } ( I ) \oplus s c ( \uparrow I ) ) \to \text {Tr} \, V _ { I } \oplus P _ { 2 } ^ { \prime } \to 0 ,
$$

$$
P ^ { \prime } ( s c ( I ) \oplus s k ( \downarrow I ) \oplus s k _ { 1 } ( I ) ) \xrightarrow { P ^ { \prime } ( g ) } P ^ { \prime } ( s c _ { 1 } ( I ) \oplus s c ( \uparrow I ) \oplus s k ( I ) ) \to D E _ { I } \oplus P _ { 2 } ^ { \prime } \to 0 , \quad ( 5 . 7 0 )
$$

where P ′ 2 is a projective module in mod A op . By applying D to these projective presentations, we obtain the assertion. □

By specializing in the 2D-grid case, we have the following. Note that in this case, we can take Q = 0 .

Proposition 5.6 (2D-grid case) . Let P be a 2D-grid, and I an interval of P . Assume that V I is non-projective and let ( 5.62 ) be an almost split sequence ending in V I . Then using the same notations as above, V I , τV I and E I have the following injective copresentations.

$$
0 \to V _ { I } \to \mathbb { Q } ( \text {sk} ( I ) ) \xrightarrow { \mathbb { Q } ( g _ { 2 } ) } \mathbb { Q } ( \text {sk} ( \psi I ) \oplus \text {sk} _ { 1 } ^ { \circ } ( I ) ) ,
$$

$$
0 \to \tau V _ { I } \to \mathcal { Q } ( s c _ { 1 } ^ { \circ } ( I ) \oplus s c ( \uparrow I ) ) \xrightarrow { \mathcal { Q } ( g _ { 1 } ) } \mathcal { Q } ( s c ( I ) ) ,
$$

$$
0 \to E _ { I } \to \mathbb { Q } ( s c _ { 1 } ^ { \circ } ( I ) \oplus s c ( \uparrow I ) \oplus \mathbf s k ( I ) ) \xrightarrow { \mathbb { Q } ( g ) } \mathbb { Q } ( s c ( I ) \oplus \mathbf s k ( \downarrow I ) \oplus \mathbf s k _ { 1 } ^ { \circ } ( I ) ) .
$$

Finally, we have the following formula for d M ( V I ) .
