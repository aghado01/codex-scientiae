# Manifest: Page 009

## REPAIR_MATH
- RAW: ```
d _ { M } ( V _ { I } ) = \bar { d } _ { R ( M ) } ( R ( V _ { I } ) ) ,
```
  FIX: ```
\[
d_{M}(V_{I}) = \bar{d}_{R(M)}(R(V_{I})),
\]
```

## REPAIR_MATH
- RAW: ```
Roughly speaking, the essential cover ζ : Z → P is an order-preserving map, and we say that ζ essentially covers an interval I (resp. relative to compression systems ) if morphisms in P that appear in formula ( 1.2 ) have preimages in Z , subject to some mild technical conditions.
```
  FIX: ```
Roughly speaking, the essential cover \(\zeta \colon Z \to P\) is an order-preserving map, and we say that \(\zeta\) essentially covers an interval \(I\) (resp. relative to compression systems ) if morphisms in \(P\) that appear in formula (1.2) have preimages in \(Z\), subject to some mild technical conditions.
```

## REPAIR_MATH
- RAW: ```
Main result C (Theorem 4.16 ). Let M ∈ mod k [ P ] , and I an interval of P . If ζ : Z → P essentially covers I , then we have
```
  FIX: ```
Main result C (Theorem 4.16). Let \(M \in \operatorname{mod} k[P]\), and \(I\) an interval of \(P\). If \(\zeta \colon Z \to P\) essentially covers \(I\), then we have
```

## REPAIR_MATH
- RAW: ```
where R denotes the restriction functor induced by ζ , and ¯ d N ( L ) denotes the maximal number of copies of L that can be taken as a direct summand of N such that no further copies of L remain in the complement as direct summands.
```
  FIX: ```
where \(R\) denotes the restriction functor induced by \(\zeta\), and \(\bar{d}_N(L)\) denotes the maximal number of copies of \(L\) that can be taken as a direct summand of \(N\) such that no further copies of \(L\) remain in the complement as direct summands.
```

## REPAIR_MATH
- RAW: ```
We remark that in ( 1.4 ), If L is indecomposable, then ¯ d N ( L ) is just the usual multiplicity of L in N . Note that it may happen that R ( V I ) is decomposable. This is why ¯ d is used here instead of d (see Remark 6.11 ).
```
  FIX: ```
We remark that in (1.4), If \(L\) is indecomposable, then \(\bar{d}_N(L)\) is just the usual multiplicity of \(L\) in \(N\). Note that it may happen that \(R(V_I)\) is decomposable. This is why \(\bar{d}\) is used here instead of \(d\) (see Remark 6.11).
```

## REPAIR_MATH
- RAW: ```
For every interval of P , equation ( 1.4 ) provides us with a method to transfer the computation of the interval multiplicity to the computation of corresponding multiplicity over another “essential” poset Z . When P is a special type of poset, the “essential” poset Z can be taken as either a single zigzag poset or a directed tree formed by connecting several zigzag posets.
```
  FIX: ```
For every interval of \(P\), equation (1.4) provides us with a method to transfer the computation of the interval multiplicity to the computation of corresponding multiplicity over another “essential” poset \(Z\). When \(P\) is a special type of poset, the “essential” poset \(Z\) can be taken as either a single zigzag poset or a directed tree formed by connecting several zigzag posets.
```

## REPAIR_PROSE
- RAW: ```
2-parameter persistent cohomology. • op
```
  FIX: ```
2-parameter persistent cohomology.
```

## REPAIR_MATH
- RAW: ```
Let us denote by P the extension of the Yoneda embedding Y · : k [ P op ] → prj k [ P ] , x ↦→ P x := k [ P ]( x, -) , where P x denotes the projective indecomposable k [ P ] -module at x . Similarly, by P ′ we denote the extension of the Yoneda embedding Y · : k [ P ] → prj( k [ P op ]) , x ↦→ P ′ x := k [ P ](-, x ) .
```
  FIX: ```
Let us denote by \(P\) the extension of the Yoneda embedding \(Y^\bullet \colon k[P^{\operatorname{op}}] \to \operatorname{prj} k[P]\), \(x \mapsto P_x := k[P](x, -)\), where \(P_x\) denotes the projective indecomposable \(k[P]\)-module at \(x\). Similarly, by \(P'\) we denote the extension of the Yoneda embedding \(Y^\bullet \colon k[P] \to \operatorname{prj}(k[P^{\operatorname{op}}])\), \(x \mapsto P'_x := k[P](-, x)\).
```
