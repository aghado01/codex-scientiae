# Manifest: Page 011

## REPAIR_MATH
- RAW: ```
\Delta _ { q } ^ { L , K } = \partial _ { q + 1 } ^ { L , K } ( \partial _ { q + 1 } ^ { L , K } ) ^ { * } + ( \partial _ { q } ^ { K } ) ^ { * } \partial _ { q } ^ { K } .
```
  FIX: ```
$$
\Delta _ { q } ^ { L , K } = \partial _ { q + 1 } ^ { L , K } ( \partial _ { q + 1 } ^ { L , K } ) ^ { * } + ( \partial _ { q } ^ { K } ) ^ { * } \partial _ { q } ^ { K } .
$$
```
- RAW: ```
\mathcal { S } _ { \rho \preceq \tau } = \mathcal { S } _ { \sigma \preceq \tau } \mathcal { S } _ { \rho \preceq \sigma }
```
  FIX: ```
$$
\mathcal { S } _ { \rho \preceq \tau } = \mathcal { S } _ { \sigma \preceq \tau } \mathcal { S } _ { \rho \preceq \sigma }
$$
```
- RAW: ```
d ^ { q } | _ { \mathcal { S } ( \sigma ) } = \sum _ { \sigma \preceq \tau } [ \sigma \colon \tau ] \mathcal { S } _ { \sigma \preceq \tau } .
```
  FIX: ```
$$
d ^ { q } | _ { \mathcal { S } ( \sigma ) } = \sum _ { \sigma \preceq \tau } [ \sigma \colon \tau ] \mathcal { S } _ { \sigma \preceq \tau } .
$$
```

