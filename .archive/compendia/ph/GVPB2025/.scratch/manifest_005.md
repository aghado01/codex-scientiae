# Manifest: Page 005

## REPAIR_MATH
- RAW: ```
P _ { \text {ers} _ { p } } ( \Phi ) = \left \{ [ b , d ] \ | \ b , d \in \{ 0 , \dots , 2 ( N _ { \text {layers} } - 1 ) \} \right \} .
```
  FIX: ```
$$
P _ { \text {ers} _ { p } } ( \Phi ) = \left \{ [ b , d ] \ | \ b , d \in \{ 0 , \dots , 2 ( N _ { \text {layers} } - 1 ) \} \right \} .
$$
```
- RAW: ```
\widehat { P I } _ { p } ( b / 2 , d / 2 ) = & P I _ { p } ( b , d ) + P I _ { p } ( b - 1 , d ) \\ & + P I _ { p } ( b , d - 1 ) + P I _ { p } ( b - 1 , d - 1 ) ,
```
  FIX: ```
$$
\widehat { P I } _ { p } ( b / 2 , d / 2 ) = & P I _ { p } ( b , d ) + P I _ { p } ( b - 1 , d ) \\ & + P I _ { p } ( b , d - 1 ) + P I _ { p } ( b - 1 , d - 1 ) ,
$$
```

## REPAIR_PROSE
- RAW: ```
choice of a k NN filtration
```
  FIX: ```
choice of a \(k\)-NN filtration
```
- RAW: ```
[b , d] 4 ,
```
  FIX: ```
\([b, d]\)^4 ,
```
- RAW: ```
{ 0 , 1 , 2 ,..., 2( N layers − 1) }
```
  FIX: ```
\(\{ 0, 1, 2, \dots, 2(N_{\text{layers}} - 1) \}\)
```
- RAW: ```
p -dimensional
```
  FIX: ```
\(p\)-dimensional
```
- RAW: ```
0 -dimensional
```
  FIX: ```
0-dimensional
```
- RAW: ```
{ x 5 ,x 6 ,x 7 }
```
  FIX: ```
\(\{ x_5, x_6, x_7 \}\)
```
- RAW: ```
{ x 6 ,x 7 }
```
  FIX: ```
\(\{ x_6, x_7 \}\)
```
- RAW: ```
changes in the k NN graph.
```
  FIX: ```
changes in the \(k\)-NN graph.
```
- RAW: ```
generates Pers p (Φ) is
```
  FIX: ```
generates \(\text{Pers}_p(\Phi)\) is
```
- RAW: ```
size (2 N layers − 1) × (2 N layers − 1) , for each homology dimension p .
```
  FIX: ```
size \((2N_{\text{layers}} - 1) \times (2N_{\text{layers}} - 1)\) , for each homology dimension \(p\) .
```
- RAW: ```
b,d > 0 ,
```
  FIX: ```
\(b, d > 0\) ,
```
- RAW: ```
where PI p is the effective
```
  FIX: ```
where \(PI_p\) is the effective
```
- RAW: ```
holes and b,d are model layers indexed by even numbers. 5
```
  FIX: ```
holes and \(b, d\) are model layers indexed by even numbers.^5
```
- RAW: ```
The collection of PI p s taken over all p contains
```
  FIX: ```
The collection of \(PI_p\)s taken over all \(p\) contains
```
- RAW: ```
4 The repetition of a pair [b , d] indicates that multiple holes in dimension p have been created and destroyed in correspondence of the same layers. 5

Note that this operation does not modify the information about the model layers contained in the original Pers p (Φ) ,
```
  FIX: ```
^4 The repetition of a pair \([b, d]\) indicates that multiple holes in dimension \(p\) have been created and destroyed in correspondence of the same layers.

^5 Note that this operation does not modify the information about the model layers contained in the original \(\text{Pers}_p(\Phi)\) ,
```
