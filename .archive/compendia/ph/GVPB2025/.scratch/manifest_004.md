# Manifest: Page 004

## REPAIR_PROSE
- RAW: ```
Layer

Layer


Layer =

Layer

Intersection

Intersection>

Layer _
```
  FIX: ```
```

## REPAIR_MATH
- RAW: ```
$$
K _ { \ell _ { i } } = \bigcup _ { S \subseteq V _ { \ell _ { i } } } \{ S \ | \ \forall x _ { s } , x _ { l } \in S , \ ( x _ { s } , x _ { l } ) \in E _ { \ell _ { i } } \\ & \quad \text {and } | S | \leq m + 1 \} .
$$
```
  FIX: ```
\[
K _ { \ell _ { i } } = \bigcup _ { S \subseteq V _ { \ell _ { i } } } \{ S \ | \ \forall x _ { s } , x _ { l } \in S , \ ( x _ { s } , x _ { l } ) \in E _ { \ell _ { i } } \\ & \quad \text {and } | S | \leq m + 1 \} .
\]
```
- RAW: ```
$$
(2) n Ke Kez Kez Kez-1 Kez nKez
$$
```
  FIX: ```
\[
(2) n Ke Kez Kez Kez-1 Kez nKez
\]
```
- RAW: ```
p -dimensional
```
  FIX: ```
\( p \)-dimensional
```
- RAW: ```
layer ℓ i .
```
  FIX: ```
layer \( \ell_i \).
```
- RAW: ```
a k-Nearest
```
  FIX: ```
a \( k \)-Nearest
```
- RAW: ```
graph G ℓ i = ( V ℓ i ,E ℓ i )
```
  FIX: ```
graph \( G_{\ell_i} = (V_{\ell_i}, E_{\ell_i}) \)
```
- RAW: ```
layer ℓ i ,
```
  FIX: ```
layer \( \ell_i \),
```
- RAW: ```
number k NN of
```
  FIX: ```
number \( k_{\text{NN}} \) of
```
- RAW: ```
a k NN -based
```
  FIX: ```
a \( k_{\text{NN}} \)-based
```
- RAW: ```
dimension m .
```
  FIX: ```
dimension \( m \).
```
- RAW: ```
complex K ℓ i defined
```
  FIX: ```
complex \( K_{\ell_i} \) defined
```
- RAW: ```
define L ≡ N layers
```
  FIX: ```
define \( L \equiv N \) layers
```
- RAW: ```
denoted by Φ .
```
  FIX: ```
denoted by \( \Phi \).
```
- RAW: ```
p = 0 ,...,m − 1 ,
```
  FIX: ```
\( p = 0, \dots, m - 1 \),
```
- RAW: ```
being m the
```
  FIX: ```
being \( m \) the
```
- RAW: ```
choose m = 4 ,
```
  FIX: ```
choose \( m = 4 \),
```
- RAW: ```
dimension p = 3 .
```
  FIX: ```
dimension \( p = 3 \).
```
