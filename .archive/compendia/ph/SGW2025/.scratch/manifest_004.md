# Manifest: Page 004

## REPAIR_MATH
- RAW: ```
0 \leftarrow M \leftarrow \bigoplus _ { j \in \mathbb { Z } } S ( - j ) ^ { \beta _ { 0 , j } ( M ) } \leftarrow \bigoplus _ { j \in \mathbb { Z } } S ( - j ) ^ { \beta _ { 1 , j } ( M ) } \leftarrow \cdots \leftarrow \bigoplus _ { j \in \mathbb { Z } } S ( - j ) ^ { \beta _ { i , j } ( M ) } \leftarrow \cdots \leftarrow 0 .
```
  FIX: ```
$$
0 \leftarrow M \leftarrow \bigoplus _ { j \in \mathbb { Z } } S ( - j ) ^ { \beta _ { 0 , j } ( M ) } \leftarrow \bigoplus _ { j \in \mathbb { Z } } S ( - j ) ^ { \beta _ { 1 , j } ( M ) } \leftarrow \cdots \leftarrow \bigoplus _ { j \in \mathbb { Z } } S ( - j ) ^ { \beta _ { i , j } ( M ) } \leftarrow \cdots \leftarrow 0 .
$$
```
- RAW: ```
\beta _ { i , j } ( M ) = \dim _ { k } ( T o r _ { i } ^ { S } ( M , k ) _ { j } ) .
```
  FIX: ```
$$
\beta _ { i , j } ( M ) = \dim _ { k } ( T o r _ { i } ^ { S } ( M , k ) _ { j } ) .
$$
```
- RAW: ```
\beta _ { i } ( M ) \colon = \sum _ { j \in \mathbb { Z } } \beta _ { i , j } ( M ) ,
```
  FIX: ```
$$
\beta _ { i } ( M ) \colon = \sum _ { j \in \mathbb { Z } } \beta _ { i , j } ( M ) ,
$$
```
- RAW: ```
\beta _ { i , \alpha } ( M ) \colon = \dim _ { k } ( T o r _ { i } ^ { S } ( M , k ) _ { \alpha } ) \quad ( \alpha \in \mathbb { Z } ^ { n } ) .
```
  FIX: ```
$$
\beta _ { i , \alpha } ( M ) \colon = \dim _ { k } ( T o r _ { i } ^ { S } ( M , k ) _ { \alpha } ) \quad ( \alpha \in \mathbb { Z } ^ { n } ) .
$$
```
- RAW: ```
\beta _ { i , j } ( M ) = \sum _ { | \alpha | = j } \beta _ { i , \alpha } ( M ) .
```
  FIX: ```
$$
\beta _ { i , j } ( M ) = \sum _ { | \alpha | = j } \beta _ { i , \alpha } ( M ) .
$$
```
- RAW: ```
0 \to N \to M \to Q \to 0 ,
```
  FIX: ```
$$
0 \to N \to M \to Q \to 0 ,
$$
```
- RAW: ```
\cdots \longrightarrow \text {Tor} _ { i } ^ { S } ( N , k ) _ { j } \longrightarrow \text {Tor} _ { i } ^ { S } ( M , k ) _ { j } \longrightarrow \text {Tor} _ { i } ^ { S } ( Q , k ) _ { j } \longrightarrow \text {Tor} _ { i - 1 } ^ { S } ( N , k ) _ { j } \longrightarrow \cdots
```
  FIX: ```
$$
\cdots \longrightarrow \text {Tor} _ { i } ^ { S } ( N , k ) _ { j } \longrightarrow \text {Tor} _ { i } ^ { S } ( M , k ) _ { j } \longrightarrow \text {Tor} _ { i } ^ { S } ( Q , k ) _ { j } \longrightarrow \text {Tor} _ { i - 1 } ^ { S } ( N , k ) _ { j } \longrightarrow \cdots
$$
```

