# Manifest: Page 019

## REPAIR_MATH
- RAW: ```
\overrightarrow { D _ { l , p } ^ { k - 1 } } \overrightarrow { C ^ { k } ( K _ { l } ) } \overrightarrow { \leftarrow } \overrightarrow { C ^ { k + 1 } ( K _ { l } ) } \\ \overrightarrow { K } \overrightarrow { D _ { l , p } ^ { k - 1 } } \overrightarrow { C ^ { k } ( K _ { l + p } ) } \overrightarrow { C ^ { k + 1 } ( K _ { l } ) } \\ C ^ { k - 1 } ( K _ { l + p } ) \overrightarrow { \leftarrow } \overrightarrow { C ^ { k } ( K _ { l + p } ) } \\
```
  FIX: ```
$$
\overrightarrow { D _ { l , p } ^ { k - 1 } } \overrightarrow { C ^ { k } ( K _ { l } ) } \overrightarrow { \leftarrow } \overrightarrow { C ^ { k + 1 } ( K _ { l } ) } \\ \overrightarrow { K } \overrightarrow { D _ { l , p } ^ { k - 1 } } \overrightarrow { C ^ { k } ( K _ { l + p } ) } \overrightarrow { C ^ { k + 1 } ( K _ { l } ) } \\ C ^ { k - 1 } ( K _ { l + p } ) \overrightarrow { \leftarrow } \overrightarrow { C ^ { k } ( K _ { l + p } ) } \\
$$
```
- RAW: ```
L _ { l , p } ^ { k } = D _ { l , p } ^ { k - 1 } \delta _ { l , p } ^ { k } + \delta _ { l } ^ { k + 1 } D _ { l } ^ { k } ,
```
  FIX: ```
$$
L _ { l , p } ^ { k } = D _ { l , p } ^ { k - 1 } \delta _ { l , p } ^ { k } + \delta _ { l } ^ { k + 1 } D _ { l } ^ { k } ,
$$
```
- RAW: ```
L _ { l , p } ^ { k } = D _ { l , p } ^ { k - 1 } ( D _ { l , p } ^ { k - 1 } ) ^ { T } + ( D _ { l } ^ { k } ) ^ { T } D _ { l } ^ { k } .
```
  FIX: ```
$$
L _ { l , p } ^ { k } = D _ { l , p } ^ { k - 1 } ( D _ { l , p } ^ { k - 1 } ) ^ { T } + ( D _ { l } ^ { k } ) ^ { T } D _ { l } ^ { k } .
$$
```

