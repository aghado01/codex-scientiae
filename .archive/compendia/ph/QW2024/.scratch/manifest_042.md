# Manifest: Page 042

## REPAIR_MATH
- RAW: ```
\Delta _ { q } ^ { \mathcal { F } , \mathcal { G } } = ( d _ { \mathcal { F } , \mathcal { G } } ^ { q } ) ^ { \dagger } d _ { \mathcal { F } , \mathcal { G } } ^ { q } + d _ { \mathcal { F } } ^ { q - 1 } ( d _ { \mathcal { F } } ^ { q - 1 } ) ^ { \dagger } .
```
  FIX: ```
$$
\Delta _ { q } ^ { \mathcal { F } , \mathcal { G } } = ( d _ { \mathcal { F } , \mathcal { G } } ^ { q } ) ^ { \dagger } d _ { \mathcal { F } , \mathcal { G } } ^ { q } + d _ { \mathcal { F } } ^ { q - 1 } ( d _ { \mathcal { F } } ^ { q - 1 } ) ^ { \dagger } .
$$
```
- RAW: ```
\ker \Delta _ { q } ^ { \mathcal { F } , \mathcal { G } } \cong \pi ^ { \bullet } ( H ^ { q } ( Y ; \mathcal { G } ) ) .
```
  FIX: ```
$$
\ker \Delta _ { q } ^ { \mathcal { F } , \mathcal { G } } \cong \pi ^ { \bullet } ( H ^ { q } ( Y ; \mathcal { G } ) ) .
$$
```
- RAW: ```
\cdots \longrightarrow V ^ { q - 1 } \xrightarrow { d } V ^ { q } \xrightarrow { d } V ^ { q + 1 } \longrightarrow \cdots ,
```
  FIX: ```
$$
\cdots \longrightarrow V ^ { q - 1 } \xrightarrow { d } V ^ { q } \xrightarrow { d } V ^ { q + 1 } \longrightarrow \cdots ,
$$
```
- RAW: ```
\cdots \longleftrightarrow V ^ { q - 1 } \stackrel { d ^ { \dagger } } { \longleftrightarrow } V ^ { q } \stackrel { d ^ { \dagger } } { \longrightarrow } V ^ { q + 1 } \stackrel { } { \longrightarrow } \cdots .
```
  FIX: ```
$$
\cdots \longleftrightarrow V ^ { q - 1 } \stackrel { d ^ { \dagger } } { \longleftrightarrow } V ^ { q } \stackrel { d ^ { \dagger } } { \longrightarrow } V ^ { q + 1 } \stackrel { } { \longrightarrow } \cdots .
$$
```
- RAW: ```
\cdots \longrightarrow ( V ^ { q - 1 } ) ^ { * } \xrightarrow { ( d ^ { \dagger } ) ^ { * } } ( V ^ { q } ) ^ { * } \xrightarrow { ( d ^ { \dagger } ) ^ { * } } ( V ^ { q + 1 } ) ^ { * } \longrightarrow \cdots ,
```
  FIX: ```
$$
\cdots \longrightarrow ( V ^ { q - 1 } ) ^ { * } \xrightarrow { ( d ^ { \dagger } ) ^ { * } } ( V ^ { q } ) ^ { * } \xrightarrow { ( d ^ { \dagger } ) ^ { * } } ( V ^ { q + 1 } ) ^ { * } \longrightarrow \cdots ,
$$
```

