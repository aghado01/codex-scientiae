# Manifest: Page 049

## REPAIR_MATH
- RAW: ```
\cdots \longrightarrow C _ { q + 1 } ( X ) \xrightarrow { \partial _ { q + 1 } ^ { X } } C _ { q } ( X ) \xrightarrow { \partial _ { q } ^ { X } } C _ { q - 1 } ( X ) \longrightarrow \cdots \\ \cdots \longrightarrow C _ { q + 1 } ( Y ) \xrightarrow { \partial _ { q + 1 } ^ { Y } } C _ { q } ( Y ) \xrightarrow { \partial _ { q } ^ { Y } } C _ { q - 1 } ( Y ) \longrightarrow \cdots
```
  FIX: ```
$$
\cdots \longrightarrow C _ { q + 1 } ( X ) \xrightarrow { \partial _ { q + 1 } ^ { X } } C _ { q } ( X ) \xrightarrow { \partial _ { q } ^ { X } } C _ { q - 1 } ( X ) \longrightarrow \cdots \\ \cdots \longrightarrow C _ { q + 1 } ( Y ) \xrightarrow { \partial _ { q + 1 } ^ { Y } } C _ { q } ( Y ) \xrightarrow { \partial _ { q } ^ { Y } } C _ { q - 1 } ( Y ) \longrightarrow \cdots
$$
```
- RAW: ```
C _ { q + 1 } ( Y ) \supset \Theta _ { q + 1 } ^ { Y \leftarrow X } = \{ c \in C _ { q + 1 } ( Y ) \, | \, \partial _ { q + 1 } ^ { Y } ( c ) \in f _ { q } ( \ker \partial _ { q } ^ { X } ) \}
```
  FIX: ```
$$
C _ { q + 1 } ( Y ) \supset \Theta _ { q + 1 } ^ { Y \leftarrow X } = \{ c \in C _ { q + 1 } ( Y ) \, | \, \partial _ { q + 1 } ^ { Y } ( c ) \in f _ { q } ( \ker \partial _ { q } ^ { X } ) \}
$$
```
- RAW: ```
C _ { q - 1 } ( X ) \supset \Theta _ { q - 1 } ^ { X \to Y } = \{ c \in C _ { q - 1 } ( X ) \, | \, ( \partial _ { q } ^ { X } ) ^ { * } ( c ) \in ( \ker f _ { q } ) ^ { \perp } \} ,
```
  FIX: ```
$$
C _ { q - 1 } ( X ) \supset \Theta _ { q - 1 } ^ { X \to Y } = \{ c \in C _ { q - 1 } ( X ) \, | \, ( \partial _ { q } ^ { X } ) ^ { * } ( c ) \in ( \ker f _ { q } ) ^ { \perp } \} ,
$$
```

