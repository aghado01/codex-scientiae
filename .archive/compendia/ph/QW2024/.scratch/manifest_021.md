# Manifest: Page 021

## REPAIR_MATH
- RAW: ```
\cdots \xrightarrow { d _ { q + 2 } ^ { V } } V _ { q + 1 } \xrightarrow { d _ { q + 1 } ^ { V } } V _ { q } \xrightarrow { d _ { q } ^ { V } } V _ { q - 1 } \xrightarrow { d _ { q - 1 } ^ { V } } \cdots
```
  FIX: ```
$$
\cdots \xrightarrow { d _ { q + 2 } ^ { V } } V _ { q + 1 } \xrightarrow { d _ { q + 1 } ^ { V } } V _ { q } \xrightarrow { d _ { q } ^ { V } } V _ { q - 1 } \xrightarrow { d _ { q - 1 } ^ { V } } \cdots
$$
```
- RAW: ```
\iota ^ { \bullet } ( H _ { q } ( V ) ) \cong \frac { \ker d _ { q } ^ { V } } { \ker d _ { q } ^ { V } \cap \text {im} \, d _ { q + 1 } ^ { W } } .
```
  FIX: ```
$$
\iota ^ { \bullet } ( H _ { q } ( V ) ) \cong \frac { \ker d _ { q } ^ { V } } { \ker d _ { q } ^ { V } \cap \text {im} \, d _ { q + 1 } ^ { W } } .
$$
```

