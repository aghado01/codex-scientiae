# Manifest: Page 016

## REPAIR_MATH
- RAW: ```
1 . \ G _ { \epsilon } \circ F = \varphi _ { M } ^ { 2 \epsilon } ,
```
  FIX: ```
$$
1 . \ G _ { \epsilon } \circ F = \varphi _ { M } ^ { 2 \epsilon } ,
$$
```
- RAW: ```
2 . \ F _ { \epsilon } \circ G = \varphi _ { N } ^ { 2 \epsilon } ,
```
  FIX: ```
$$
2 . \ F _ { \epsilon } \circ G = \varphi _ { N } ^ { 2 \epsilon } ,
$$
```
- RAW: ```
d _ { \mathcal { I } } ( M , N ) \coloneqq \inf \{ \epsilon \geq 0 \colon M \text { and } N \text { are } \epsilon \text {-interleaved} \}
```
  FIX: ```
$$
d _ { \mathcal { I } } ( M , N ) \coloneqq \inf \{ \epsilon \geq 0 \colon M \text { and } N \text { are } \epsilon \text {-interleaved} \}
$$
```
- RAW: ```
L a n _ { F } ( M ) ( q ) \coloneqq \text {colm} M | _ { P [ F \leq q ] } ,
```
  FIX: ```
$$
L a n _ { F } ( M ) ( q ) \coloneqq \text {colm} M | _ { P [ F \leq q ] } ,
$$
```
- RAW: ```
E \coloneqq ( - ) | _ { U } \circ L a n _ { i } ( \circ ) .
```
  FIX: ```
$$
E \coloneqq ( - ) | _ { U } \circ L a n _ { i } ( \circ ) .
$$
```
- RAW: ```
d _ { \mathcal { I } } ( M , N ) \coloneqq d _ { \mathcal { I } } ( E ( M ) , E ( N ) ) .
```
  FIX: ```
$$
d _ { \mathcal { I } } ( M , N ) \coloneqq d _ { \mathcal { I } } ( E ( M ) , E ( N ) ) .
$$
```
- RAW: ```
d _ { \mathcal { I } } ( M , N ) \coloneqq d _ { \mathcal { I } } ( \tilde { E } ( M ) , \tilde { E } ( N ) ) .
```
  FIX: ```
$$
d _ { \mathcal { I } } ( M , N ) \coloneqq d _ { \mathcal { I } } ( \tilde { E } ( M ) , \tilde { E } ( N ) ) .
$$
```

## REPAIR_PROSE
- RAW: `Z Z -G RIL`
  FIX: `$\mathbb{Z}\mathbb{Z}$-GRIL`
- RAW: `for additional details. n n`
  FIX: `for additional details.`
- RAW: `for all x ∈ R n . n`
  FIX: `for all \(x \in \mathbb{R}^n\).`
- RAW: `I I Lemma A.8.`
  FIX: `Lemma A.8.`

## REPAIR_MATH
- RAW: `R op × R -indexed`
  FIX: `\(\mathbb{R}^{\mathrm{op}} \times \mathbb{R}\)-indexed`
- RAW: `R n -indexed`
  FIX: `\(\mathbb{R}^n\)-indexed`
- RAW: `( u -shift functor)`
  FIX: `(\(u\)-shift functor)`
- RAW: `The u -shift`
  FIX: `The \(u\)-shift`
- RAW: `( − ) u : vec R → vec R`
  FIX: `\(( - )_u \colon \mathrm{vec}^{\mathbb{R}} \to \mathrm{vec}^{\mathbb{R}}\)`
- RAW: `for u ∈ R n ,`
  FIX: `for \(u \in \mathbb{R}^n\),`
- RAW: `For M ∈ vec R n ,`
  FIX: `For \(M \in \mathrm{vec}^{\mathbb{R}^n}\),`
- RAW: `M u is defined as`
  FIX: `\(M_u\) is defined as`
- RAW: `M u ( x ) = M ( x + u )`
  FIX: `\(M_u(x) = M(x+u)\)`
- RAW: `for all x ∈ R n`
  FIX: `for all \(x \in \mathbb{R}^n\)`
- RAW: `M u ( x 1 ≤ x 2 ) = M ( x 1 + u ≤ x 2 + u )`
  FIX: `\(M_u(x_1 \leq x_2) = M(x_1+u \leq x_2+u)\)`
- RAW: `for all x 1 ≤ x 2 ∈ R n ,`
  FIX: `for all \(x_1 \leq x_2 \in \mathbb{R}^n\),`
- RAW: `Let M,N ∈ vec R n .`
  FIX: `Let \(M,N \in \mathrm{vec}^{\mathbb{R}^n}\).`
- RAW: `Let F : M → N be`
  FIX: `Let \(F \colon M \to N\) be`
- RAW: `F u : M u → N u`
  FIX: `\(F_u \colon M_u \to N_u\)`
- RAW: `F u ( x ) = F ( x + u ): M u ( x ) → N u ( x )`
  FIX: `\(F_u(x) = F(x+u) \colon M_u(x) \to N_u(x)\)`
- RAW: `( ϵ -interleaving)`
  FIX: `(\(\epsilon\)-interleaving)`
- RAW: `Let ϵ ∈ [0 , ∞ )`
  FIX: `Let \(\epsilon \in [0, \infty)\)`
- RAW: `( − ) ϵ`
  FIX: `\(( - )_\epsilon\)`
- RAW: `ϵ = ϵ (1 , 1 ,..., 1)`
  FIX: `\(\epsilon = \epsilon(1, 1, \dots, 1)\)`
- RAW: `M and N are ϵ -interleaved`
  FIX: `\(M\) and \(N\) are \(\epsilon\)-interleaved`
- RAW: `F : M → N ϵ and G : N → M ϵ`
  FIX: `\(F \colon M \to N_\epsilon\) and \(G \colon N \to M_\epsilon\)`
- RAW: `φ u M : M → M u`
  FIX: `\(\varphi^M_u \colon M \to M_u\)`
- RAW: `M ( x ) is`
  FIX: `\(M(x)\) is`
- RAW: `M ( x ≤ x + u )`
  FIX: `\(M(x \leq x+u)\)`
- RAW: `Let M,N ∈ vec R .`
  FIX: `Let \(M,N \in \mathrm{vec}^{\mathbb{R}}\).`
- RAW: `d I ( M,N )`
  FIX: `\(d_{\mathcal{I}}(M,N)\)`
- RAW: `between M and N is defined`
  FIX: `between \(M\) and \(N\) is defined`
- RAW: `d I ( M,N ) = ∞`
  FIX: `\(d_{\mathcal{I}}(M,N) = \infty\)`
- RAW: `Let P and Q be`
  FIX: `Let \(P\) and \(Q\) be`
- RAW: `Let F : P → Q be`
  FIX: `Let \(F \colon P \to Q\) be`
- RAW: `Let P [ F ≤ q ] denote`
  FIX: `Let \(P[F \leq q]\) denote`
- RAW: `P [ F ≤ q ] : = { p ∈ P : F ( p ) ≤ q }`
  FIX: `\(P[F \leq q] := \{ p \in P \mid F(p) \leq q \}\)`
- RAW: `M : P → vec`
  FIX: `\(M \colon P \to \mathrm{vec}\)`
- RAW: `of M along F is`
  FIX: `of \(M\) along \(F\) is`
- RAW: `Lan F ( M ): Q → vec`
  FIX: `\(\mathrm{Lan}_F(M) \colon Q \to \mathrm{vec}\)`
- RAW: `U ⊂ R op × R`
  FIX: `\(U \subset \mathbb{R}^{\mathrm{op}} \times \mathbb{R}\)`
- RAW: `U : = { ( a,b ): a ≤ b }`
  FIX: `\(U := \{ (a,b) \mid a \leq b \}\)`
- RAW: `i : ZZ → R op × R`
  FIX: `\(i \colon \mathbb{Z}\mathbb{Z} \to \mathbb{R}^{\mathrm{op}} \times \mathbb{R}\)`
- RAW: `( − ) | U : vec R op × R → vec U`
  FIX: `\(( - )|_U \colon \mathrm{vec}^{\mathbb{R}^{\mathrm{op}} \times \mathbb{R}} \to \mathrm{vec}^U\)`
- RAW: `E : vec ZZ → vec U`
  FIX: `\(E \colon \mathrm{vec}^{\mathbb{Z}\mathbb{Z}} \to \mathrm{vec}^U\)`
- RAW: `Let M,N be`
  FIX: `Let \(M,N\) be`
- RAW: `UU ⊂ R op × R × R`
  FIX: `\(UU \subset \mathbb{R}^{\mathrm{op}} \times \mathbb{R} \times \mathbb{R}\)`
- RAW: `analog of U ,`
  FIX: `analog of \(U\),`
- RAW: `UU : = { ( a,b,c ): a ≤ b }`
  FIX: `\(UU := \{ (a,b,c) \mid a \leq b \}\)`
- RAW: `˜ i : ZZ × Z → R op × R × R`
  FIX: `\(\tilde{i} \colon \mathbb{Z}\mathbb{Z} \times \mathbb{Z} \to \mathbb{R}^{\mathrm{op}} \times \mathbb{R} \times \mathbb{R}\)`
- RAW: `˜ E : = ( − ) | UU ◦ Lan ˜ i ( ◦ )`
  FIX: `\(\tilde{E} := ( - )|_{UU} \circ \mathrm{Lan}_{\tilde{i}}(\circ)\)`
- RAW: `between M and N is given`
  FIX: `between \(M\) and \(N\) is given`
- RAW: `˜ E sends`
  FIX: `\(\tilde{E}\) sends`
- RAW: `ZZ × Z`
  FIX: `\(\mathbb{Z}\mathbb{Z} \times \mathbb{Z}\)`
- RAW: `Let M be`
  FIX: `Let \(M\) be`
- RAW: `M ∼ = k ∈ K I k`
  FIX: `\(M \cong \bigoplus_{k \in K} I_k\)`
- RAW: `˜ E ( M ) ∼ = k ∈ K ˜ E ( I k )`
  FIX: `\(\tilde{E}(M) \cong \bigoplus_{k \in K} \tilde{E}(I_k)\)`
- RAW: `where K is`
  FIX: `where \(K\) is`
