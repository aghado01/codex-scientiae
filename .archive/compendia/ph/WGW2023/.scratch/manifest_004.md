# Manifest: Page 004

## REPAIR_MATH
- RAW: ```
\partial e _ { i _ { 0 } \dots i _ { p } } = \sum _ { q = 0 } ^ { p } ( - 1 ) ^ { q } e _ { i _ { 0 } \dots \hat { i } _ { q } \dots i _ { p } } ,
```
  FIX: ```
$$
\partial e _ { i _ { 0 } \dots i _ { p } } = \sum _ { q = 0 } ^ { p } ( - 1 ) ^ { q } e _ { i _ { 0 } \dots \hat { i } _ { q } \dots i _ { p } } ,
$$
```
- RAW: ```
\cdots \Lambda _ { p } \stackrel { \partial } { \longrightarrow } \Lambda _ { p - 1 } \stackrel { \partial } { \longrightarrow } \cdots \stackrel { \partial } { \longrightarrow } \Lambda _ { 0 } \stackrel { \partial } { \longrightarrow } \mathbb { K } \stackrel { \partial } { \longrightarrow } 0 .
```
  FIX: ```
$$
\cdots \Lambda _ { p } \stackrel { \partial } { \longrightarrow } \Lambda _ { p - 1 } \stackrel { \partial } { \longrightarrow } \cdots \stackrel { \partial } { \longrightarrow } \Lambda _ { 0 } \stackrel { \partial } { \longrightarrow } \mathbb { K } \stackrel { \partial } { \longrightarrow } 0 .
$$
```
- RAW: ```
\bar { \partial } \colon \mathcal { R } _ { p } \rightarrow \mathcal { R } _ { p - 1 } ,
```
  FIX: ```
$$
\bar { \partial } \colon \mathcal { R } _ { p } \rightarrow \mathcal { R } _ { p - 1 } ,
$$
```
- RAW: ```
\dots \mathcal { R } _ { p } \stackrel { \bar { \partial } } { \longrightarrow } \mathcal { R } _ { p - 1 } \stackrel { \bar { \delta } } { \longrightarrow } \dots \stackrel { \bar { \delta } } { \longrightarrow } \mathcal { R } _ { 0 } \stackrel { \bar { \delta } } { \longrightarrow } \mathbb { K } \stackrel { \bar { \delta } } { \longrightarrow } 0 .
```
  FIX: ```
$$
\dots \mathcal { R } _ { p } \stackrel { \bar { \partial } } { \longrightarrow } \mathcal { R } _ { p - 1 } \stackrel { \bar { \delta } } { \longrightarrow } \dots \stackrel { \bar { \delta } } { \longrightarrow } \mathcal { R } _ { 0 } \stackrel { \bar { \delta } } { \longrightarrow } \mathbb { K } \stackrel { \bar { \delta } } { \longrightarrow } 0 .
$$
```
- RAW: ```
\text {if } i _ { 0 } \dots i _ { n } \in P , \text { then } i _ { 0 } \dots i _ { n - 1 } \in P , \text { and } i _ { 1 } \dots i _ { n } \in P .
```
  FIX: ```
$$
\text {if } i _ { 0 } \dots i _ { n } \in P , \text { then } i _ { 0 } \dots i _ { n - 1 } \in P , \text { and } i _ { 1 } \dots i _ { n } \in P .
$$
```
- RAW: ```
\text {if } \sigma \in \mathcal { K } , \text { then any subset of } \sigma \text { is also in } \mathcal { K } .
```
  FIX: ```
$$
\text {if } \sigma \in \mathcal { K } , \text { then any subset of } \sigma \text { is also in } \mathcal { K } .
$$
```

