# Manifest: Page 005

## REPAIR_MATH
- RAW: ```
\mathcal { A } _ { n } = \mathcal { A } _ { n } ( P ) = \text {span} \{ e _ { i _ { 0 } \dots i _ { n } } \colon i _ { 0 } \dots i _ { n } \in P _ { n } \} .
```
  FIX: ```
$$
\mathcal { A } _ { n } = \mathcal { A } _ { n } ( P ) = \text {span} \{ e _ { i _ { 0 } \dots i _ { n } } \colon i _ { 0 } \dots i _ { n } \in P _ { n } \} .
$$
```
- RAW: ```
\cdots \mathcal { A } _ { n } \stackrel { \partial } { \longrightarrow } \mathcal { A } _ { n - 1 } \stackrel { \partial } { \longrightarrow } \cdots \stackrel { \partial } { \longrightarrow } \mathcal { A } _ { 0 } \stackrel { \partial } { \longrightarrow } \mathbb { K } \stackrel { \partial } { \longrightarrow } 0 .
```
  FIX: ```
$$
\cdots \mathcal { A } _ { n } \stackrel { \partial } { \longrightarrow } \mathcal { A } _ { n - 1 } \stackrel { \partial } { \longrightarrow } \cdots \stackrel { \partial } { \longrightarrow } \mathcal { A } _ { 0 } \stackrel { \partial } { \longrightarrow } \mathbb { K } \stackrel { \partial } { \longrightarrow } 0 .
$$
```
- RAW: ```
\Omega _ { n } = \Omega _ { n } ( P ) = \{ v \in \mathcal { A } _ { n } \colon \partial v \in \mathcal { A } _ { n - 1 } \} .
```
  FIX: ```
$$
\Omega _ { n } = \Omega _ { n } ( P ) = \{ v \in \mathcal { A } _ { n } \colon \partial v \in \mathcal { A } _ { n - 1 } \} .
$$
```
- RAW: ```
\cdots \Omega _ { n } \stackrel { \partial } { \longrightarrow } \Omega _ { n - 1 } \stackrel { \partial } { \longrightarrow } \cdots \stackrel { \partial } { \longrightarrow } \Omega _ { 0 } \stackrel { \partial } { \longrightarrow } \mathbb { K } \stackrel { \partial } { \longrightarrow } 0 ,
```
  FIX: ```
$$
\cdots \Omega _ { n } \stackrel { \partial } { \longrightarrow } \Omega _ { n - 1 } \stackrel { \partial } { \longrightarrow } \cdots \stackrel { \partial } { \longrightarrow } \Omega _ { 0 } \stackrel { \partial } { \longrightarrow } \mathbb { K } \stackrel { \partial } { \longrightarrow } 0 ,
$$
```
- RAW: ```
\cdots \Omega _ { n } \xrightarrow { \partial } \Omega _ { n - 1 } \xrightarrow { \partial } \cdots \xrightarrow { \partial } \Omega _ { 0 } \xrightarrow { \partial } 0 ,
```
  FIX: ```
$$
\cdots \Omega _ { n } \xrightarrow { \partial } \Omega _ { n - 1 } \xrightarrow { \partial } \cdots \xrightarrow { \partial } \Omega _ { 0 } \xrightarrow { \partial } 0 ,
$$
```
- RAW: ```
\Omega _ { n } = \Omega _ { n } ( G ) = \{ v \in \mathcal { A } _ { n } \colon \partial v \in \mathcal { A } _ { n - 1 } \} ,
```
  FIX: ```
$$
\Omega _ { n } = \Omega _ { n } ( G ) = \{ v \in \mathcal { A } _ { n } \colon \partial v \in \mathcal { A } _ { n - 1 } \} ,
$$
```
- RAW: ```
\cdots \stackrel { \partial } { \longrightarrow } \Omega _ { 3 } \stackrel { \partial } { \longrightarrow } \Omega _ { 2 } \stackrel { \partial } { \longrightarrow } \Omega _ { 1 } \stackrel { \partial } { \longrightarrow } \Omega _ { 0 } \stackrel { \partial } { \longrightarrow } \mathbb { K } \stackrel { \partial } { \longrightarrow } 0 ,
```
  FIX: ```
$$
\cdots \stackrel { \partial } { \longrightarrow } \Omega _ { 3 } \stackrel { \partial } { \longrightarrow } \Omega _ { 2 } \stackrel { \partial } { \longrightarrow } \Omega _ { 1 } \stackrel { \partial } { \longrightarrow } \Omega _ { 0 } \stackrel { \partial } { \longrightarrow } \mathbb { K } \stackrel { \partial } { \longrightarrow } 0 ,
$$
```
- RAW: ```
H _ { n } ( G ) = H _ { n } ( V , E ; \mathbb { K } ) \coloneqq \ker ( \partial | _ { \Omega _ { n } } ) / \text {im} ( \partial | _ { \Omega _ { n + 1 } } ) .
```
  FIX: ```
$$
H _ { n } ( G ) = H _ { n } ( V , E ; \mathbb { K } ) \coloneqq \ker ( \partial | _ { \Omega _ { n } } ) / \text {im} ( \partial | _ { \Omega _ { n + 1 } } ) .
$$
```

