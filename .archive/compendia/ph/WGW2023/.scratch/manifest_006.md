# Manifest: Page 006

## REPAIR_MATH
- RAW: ```
\cdots \Omega _ { n + 1 } \stackrel { \partial _ { n + 1 } } { \longrightarrow } \Omega _ { n } \stackrel { \partial _ { n } } { \longrightarrow } \Omega _ { n - 1 } \stackrel { \partial _ { n - 1 } } { \longrightarrow } \Omega _ { n - 2 } \cdots .
```
  FIX: ```
$$
\cdots \Omega _ { n + 1 } \stackrel { \partial _ { n + 1 } } { \longrightarrow } \Omega _ { n } \stackrel { \partial _ { n } } { \longrightarrow } \Omega _ { n - 1 } \stackrel { \partial _ { n - 1 } } { \longrightarrow } \Omega _ { n - 2 } \cdots .
$$
```
- RAW: ```
H ^ { n } ( G ) = H ^ { n } ( V , E ; \mathbb { K } ) \coloneqq \ker ( d _ { n + 1 } ) / \text { im} ( d _ { n } ) ,
```
  FIX: ```
$$
H ^ { n } ( G ) = H ^ { n } ( V , E ; \mathbb { K } ) \coloneqq \ker ( d _ { n + 1 } ) / \text { im} ( d _ { n } ) ,
$$
```
- RAW: ```
( f _ { * * } ) _ { n } ( e _ { i _ { 0 } \dots i _ { n } } ) = e _ { f ( i _ { 0 } ) \dots f ( i _ { n } ) } .
```
  FIX: ```
$$
( f _ { * * } ) _ { n } ( e _ { i _ { 0 } \dots i _ { n } } ) = e _ { f ( i _ { 0 } ) \dots f ( i _ { n } ) } .
$$
```
- RAW: ```
( ( f _ { * * } ) _ { n - 1 } \circ \partial ) ( e _ { i _ { 0 } \dots i _ { n } } ) = \sum _ { q = 0 } ^ { n } ( - 1 ) ^ { q } ( f _ { * * } ) _ { n - 1 } ( e _ { i _ { 0 } \dots \hat { i } _ { q } \dots i _ { n } } )
```
  FIX: ```
$$
( ( f _ { * * } ) _ { n - 1 } \circ \partial ) ( e _ { i _ { 0 } \dots i _ { n } } ) = \sum _ { q = 0 } ^ { n } ( - 1 ) ^ { q } ( f _ { * * } ) _ { n - 1 } ( e _ { i _ { 0 } \dots \hat { i } _ { q } \dots i _ { n } } )
$$
```
- RAW: ```
= \sum _ { q = 0 } ^ { n } ( - 1 ) ^ { q } ( e _ { f ( i _ { 0 } ) \dots \hat { f } ( i _ { q } ) \dots f ( i _ { n } ) } )
```
  FIX: ```
$$
= \sum _ { q = 0 } ^ { n } ( - 1 ) ^ { q } ( e _ { f ( i _ { 0 } ) \dots \hat { f } ( i _ { q } ) \dots f ( i _ { n } ) } )
$$
```
- RAW: ```
= ( \partial ^ { \prime } \circ ( f _ { * } ) _ { n } ) ( e _ { i _ { 0 } \dots i _ { n } } ) .
```
  FIX: ```
$$
= ( \partial ^ { \prime } \circ ( f _ { * } ) _ { n } ) ( e _ { i _ { 0 } \dots i _ { n } } ) .
$$
```
- RAW: ```
( \tilde { f } _ { * * } ) _ { n } \colon \Lambda _ { n } ( V ) / \mathcal { N } _ { n } ( V ) \to \Lambda _ { n } ( V ^ { \prime } ) / \mathcal { N } _ { n } ( V ^ { \prime } ) .
```
  FIX: ```
$$
( \tilde { f } _ { * * } ) _ { n } \colon \Lambda _ { n } ( V ) / \mathcal { N } _ { n } ( V ) \to \Lambda _ { n } ( V ^ { \prime } ) / \mathcal { N } _ { n } ( V ^ { \prime } ) .
$$
```
- RAW: ```
( f _ { * } ) _ { n } \colon \mathcal { R } _ { n } ( V ) \to \mathcal { R } _ { n } ( V ^ { \prime } ) .
```
  FIX: ```
$$
( f _ { * } ) _ { n } \colon \mathcal { R } _ { n } ( V ) \to \mathcal { R } _ { n } ( V ^ { \prime } ) .
$$
```
- RAW: ```
( f _ { * } ) _ { n } ( e _ { i _ { 0 } \dots i _ { n } } ) \coloneqq \begin{cases} e _ { f ( i _ { 0 } ) \dots f ( i _ { n } ) } & \text {if } e _ { f ( i _ { 0 } ) \dots f ( i _ { n } ) } \text { is regular,} \\ 0 & \text {otherwise.} \end{cases}
```
  FIX: ```
$$
( f _ { * } ) _ { n } ( e _ { i _ { 0 } \dots i _ { n } } ) \coloneqq \begin{cases} e _ { f ( i _ { 0 } ) \dots f ( i _ { n } ) } & \text {if } e _ { f ( i _ { 0 } ) \dots f ( i _ { n } ) } \text { is regular,} \\ 0 & \text {otherwise.} \end{cases}
$$
```
- RAW: ```
( f _ { * } ) _ { n } \colon \Omega _ { n } ( G ; \mathbb { K } ) \to \Omega _ { n } ( G ^ { \prime } ; \mathbb { K } ) & & ( 2 0 )
```
  FIX: ```
$$
( f _ { * } ) _ { n } \colon \Omega _ { n } ( G ; \mathbb { K } ) \to \Omega _ { n } ( G ^ { \prime } ; \mathbb { K } ) & & ( 2 0 )
$$
```
- RAW: ```
( f _ { * } ) _ { n } \colon H _ { n } ( G ; \mathbb { K } ) \to H _ { n } ( G ^ { \prime } ; \mathbb { K } ) , \ \ n \geq 0 .
```
  FIX: ```
$$
( f _ { * } ) _ { n } \colon H _ { n } ( G ; \mathbb { K } ) \to H _ { n } ( G ^ { \prime } ; \mathbb { K } ) , \ \ n \geq 0 .
$$
```

