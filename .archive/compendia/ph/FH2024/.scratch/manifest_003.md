# Manifest: Page 003

## REPAIR_PROSE
- RAW: ```
[Page 3]

# 2 Preliminaries
```
  FIX: ```
# 2 Preliminaries
```

## REPAIR_MATH
- RAW: ```
a filtration of a simplicial complex K 0 ⊂ K 1 ⊂ ... ⊂ K n (most frequently the Vietoris-Rips complex [10]) is constructed from point cloud data and the p -th simplicial homology functor H p ( − ) with coefficients in a field (usually 2 ) is applied to the filtration.
```
  FIX: ```
a filtration of a simplicial complex \( K_0 \subset K_1 \subset \dots \subset K_n \) (most frequently the Vietoris-Rips complex [10]) is constructed from point cloud data and the \( p \)-th simplicial homology functor \( H_p(-) \) with coefficients in a field (usually \( \mathbb{F}_2 \)) is applied to the filtration.
```
- RAW: ```
This results in a sequence of vector spaces M 0 ,...,M n and associated linear maps M ( a ≤ b ) : M a → M b for all 0 ≤ a ≤ b ≤ n , the so-called (oneparameter) persistence module.
```
  FIX: ```
This results in a sequence of vector spaces \( M_0, \dots, M_n \) and associated linear maps \( M(a \leq b) : M_a \to M_b \) for all \( 0 \leq a \leq b \leq n \), the so-called (oneparameter) persistence module.
```
- RAW: ```
The maps M ( a ≤ b ) are also referred to as structure maps .
```
  FIX: ```
The maps \( M(a \leq b) \) are also referred to as structure maps.
```
- RAW: ```
In the case where the poset is or , or a subset thereof, we call it a oneparameter persistence module.
```
  FIX: ```
In the case where the poset is \( \mathbb{R} \) or \( \mathbb{Z} \), or a subset thereof, we call it a oneparameter persistence module.
```
- RAW: ```
When the indexing poset is n we call it a multiparameter persistence module.
```
  FIX: ```
When the indexing poset is \( \mathbb{R}^n \) we call it a multiparameter persistence module.
```
- RAW: ```
When the target category is the category of finite dimensional vector spaces, the persistence module is called pointwise finite dimensional . By Vec we mean the category of all vector spaces over a fixed field and by vec the full subcategory of finite dimensional vector spaces.
```
  FIX: ```
When the target category is the category of finite dimensional vector spaces, the persistence module is called pointwise finite dimensional. By \( \mathbf{Vec} \) we mean the category of all vector spaces over a fixed field and by \( \mathbf{vec} \) the full subcategory of finite dimensional vector spaces.
```
- RAW: ```
Definition 2.1 Let b ≤ d . An interval module I [ b,d ] with birth time b and death time d is defined as
```
  FIX: ```
Definition 2.1 Let \( b \leq d \). An interval module \( I_{[b,d]} \) with birth time \( b \) and death time \( d \) is defined as
```
- RAW: ```
I _ { i } = \begin{cases} \mathbb { F } & \text {for } b \leq i \leq d , \\ 0 & \text {otherwise} , \end{cases}
```
  FIX: ```
$$
I _ { i } = \begin{cases} \mathbb { F } & \text {for } b \leq i \leq d , \\ 0 & \text {otherwise} , \end{cases}
$$
```
- RAW: ```
where all the maps between I i and I i +1 are identity maps if both I i and I i +1 are and zero maps otherwise.
```
  FIX: ```
where all the maps between \( I_i \) and \( I_{i+1} \) are identity maps if both \( I_i \) and \( I_{i+1} \) are \( \mathbb{F} \) and zero maps otherwise.
```
- RAW: ```
The rank invariant sends a pair of indices a ≤ b to the rank of the linear map M ( a ≤ b ) .
```
  FIX: ```
The rank invariant sends a pair of indices \( a \leq b \) to the rank of the linear map \( M(a \leq b) \).
```
