# Manifest: Page 024

## REPAIR_MATH
- RAW: ```
\dot { x } = - x + a y + x ^ { 2 } y , \quad \dot { y } = b - a y - x ^ { 2 } y .
```
  FIX: ```
$$
\dot { x } = - x + a y + x ^ { 2 } y , \quad \dot { y } = b - a y - x ^ { 2 } y .
$$
```

- RAW: `parameter a = 0 . 1 and varying b we obtain a limit cycle for 0 . 4 ≤ b ≤ 0 . 8`
  FIX: `parameter \( a = 0.1 \) and varying \( b \) we obtain a limit cycle for \( 0.4 \leq b \leq 0.8 \)`

- RAW: `taking only the x -coordinates`
  FIX: `taking only the \( x \)-coordinates`

- RAW: `embedding dimension d = 2 (see`
  FIX: `embedding dimension \( d = 2 \) (see`

- RAW: `For each value of b , we cut`
  FIX: `For each value of \( b \), we cut`

- RAW: `from b = 0 . 35 to b = 0 . 9 in steps of 0 . 05 .`
  FIX: `from \( b = 0.35 \) to \( b = 0.9 \) in steps of \( 0.05 \).`

- RAW: `dimension one from b = 0 . 45 , which corresponds to zigzag index 5 .`
  FIX: `dimension one from \( b = 0.45 \), which corresponds to zigzag index 5.`

- RAW: `value for ε such`
  FIX: `value for \( \varepsilon \) such`

- RAW: `indices 12 and 13 and ε = 10 .`
  FIX: `indices 12 and 13 and \( \varepsilon = 10 \).`

- RAW: `ranging from b = 0 . 5 to b = 0 . 75 , which`
  FIX: `ranging from \( b = 0.5 \) to \( b = 0.75 \), which`

- RAW: `of the first ( n = 1 ) time series`
  FIX: `of the first \( (n = 1) \) time series`

## REPAIR_PROSE
- RAW: `with a SNR of 30 .`
  FIX: `with a SNR of 30.`

- RAW: `ground truth image , on the right`
  FIX: `ground truth image, on the right`

- RAW: `zigzag indices 7 to 18 , meaning`
  FIX: `zigzag indices 7 to 18, meaning`

- RAW: `zigzag indices 6 to 17 .`
  FIX: `zigzag indices 6 to 17.`

- RAW: `still captures the relevant behavior`
  FIX: `still captures the relevant behavior.`
