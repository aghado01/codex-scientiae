# Manifest: Page 014

## REPAIR_PROSE
- RAW: ```
 With the notion
```
  FIX: ```
With the notion
```
- RAW: ```
V -compatible
```
  FIX: ```
\( V \)-compatible
```
- RAW: ```
V -compatibility
```
  FIX: ```
\( V \)-compatibility
```

## REPAIR_MATH
- RAW: ```
\rho ( t ) \colon = \begin{cases} x ; & t = 0 , \\ y ; & t = 1 , \\ z ; & t = 2 . \end{cases}
```
  FIX: ```
$$
\rho ( t ) \colon = \begin{cases} x ; & t = 0 , \\ y ; & t = 1 , \\ z ; & t = 2 . \end{cases}
$$
```
- RAW: ```
A ⊂ X
```
  FIX: ```
\( A \subset X \)
```
- RAW: ```
Inv V ( A ) : = { x ∈ A | eSol V ( x,A ) ̸ = ∅} =   { im φ | eSol V ( A ) }
```
  FIX: ```
\( \mathrm{Inv}_V(A) := \{ x \in A \mid \mathrm{eSol}_V(x, A) \neq \emptyset \} = \{ \mathrm{im}\, \varphi \mid \varphi \in \mathrm{eSol}_V(A) \} \)
```
- RAW: ```
S ⊂ X
```
  FIX: ```
\( S \subset X \)
```
- RAW: ```
Inv V S = S
```
  FIX: ```
\( \mathrm{Inv}_V S = S \)
```
- RAW: ```
N ⊂ X
```
  FIX: ```
\( N \subset X \)
```
- RAW: ```
path x · y · z
```
  FIX: ```
path \( x \cdot y \cdot z \)
```
- RAW: ```
x,z ∈ N implies y ∈ N
```
  FIX: ```
\( x,z \in N \) implies \( y \in N \)
```
- RAW: ```
set S is called
```
  FIX: ```
set \( S \) is called
```
- RAW: ```
block N such that
```
  FIX: ```
block \( N \) such that
```
- RAW: ```
S = Inv V N
```
  FIX: ```
\( S = \mathrm{Inv}_V N \)
```
- RAW: ```
set N is
```
  FIX: ```
set \( N \) is
```
- RAW: ```
if N is
```
  FIX: ```
if \( N \) is
```
- RAW: ```
that N is
```
  FIX: ```
that \( N \) is
```
- RAW: ```
any x ∈ N , [ x ] V is a subset of N .
```
  FIX: ```
any \( x \in N \), \( [x]_V \) is a subset of \( N \).
```
- RAW: ```
fix x ∈ N and y ∈ [ x ] V and notice that y ∈ F V ( x ) and x ∈ F V ( y ).
```
  FIX: ```
fix \( x \in N \) and \( y \in [x]_V \) and notice that \( y \in F_V(x) \) and \( x \in F_V(y) \).
```
- RAW: ```
path x · y · x implies that y ∈ N .
```
  FIX: ```
path \( x \cdot y \cdot x \) implies that \( y \in N \).
```
- RAW: ```
Thus, [ x ] V ⊂ N ,
```
  FIX: ```
Thus, \( [x]_V \subset N \),
```
- RAW: ```
let x,z ∈ N and y ∈ X such that x > y > z .
```
  FIX: ```
let \( x,z \in N \) and \( y \in X \) such that \( x > y > z \).
```
- RAW: ```
y ∈ F V ( x ) and z ∈ F V ( y ).
```
  FIX: ```
\( y \in F_V(x) \) and \( z \in F_V(y) \).
```
- RAW: ```
y ∈ N by definition
```
  FIX: ```
\( y \in N \) by definition
```
- RAW: ```
x,z ∈ N and y ̸∈ N .
```
  FIX: ```
\( x,z \in N \) and \( y \notin N \).
```
- RAW: ```
[ x ] V ̸ = [ y ] V ̸ = [ z ] V ,
```
  FIX: ```
\( [x]_V \neq [y]_V \neq [z]_V \),
```
- RAW: ```
of N .
```
  FIX: ```
of \( N \).
```
- RAW: ```
y ∈ cl( x ), z ∈ cl( y ) and y ∈ mo N .
```
  FIX: ```
\( y \in \mathrm{cl}(x) \), \( z \in \mathrm{cl}(y) \) and \( y \in \mathrm{mo}\, N \).
```
- RAW: ```
since N is
```
  FIX: ```
since \( N \) is
```
- RAW: ```
mo N is closed and z ∈ mo N ,
```
  FIX: ```
\( \mathrm{mo}\, N \) is closed and \( z \in \mathrm{mo}\, N \),
```
- RAW: ```
```
  FIX: ```
\(\square\)
```
- RAW: ```
block for S .
```
  FIX: ```
block for \( S \).
```
- RAW: ```
B 1 , B 2 and B 3 ,
```
  FIX: ```
\( B_1 \), \( B_2 \) and \( B_3 \),
```
- RAW: ```
M 1 , M 2 , and M 3 ,
```
  FIX: ```
\( M_1 \), \( M_2 \), and \( M_3 \),
```
- RAW: ```
```
  FIX: ```
\(\lozenge\)
```
