# Manifest: Page 041

## REPLACE_TABLES
None

## REPAIR_PROSE
- RAW: `in Example 6.4 . Then,`
  FIX: `in Example 6.4. Then,`
- RAW: `since the 70s [ 28 ].`
  FIX: `since the 70s [28].`
- RAW: `introduced in [ 11 ], together`
  FIX: `introduced in [11], together`
- RAW: `Section 8 .`
  FIX: `Section 8.`
- RAW: `persistence module . Before`
  FIX: `persistence module. Before`
- RAW: `theorem (Theorem 5.12 ) guarantees`
  FIX: `theorem (Theorem 5.12) guarantees`
- RAW: `(see Section 5.2 ),`
  FIX: `(see Section 5.2),`

## REPAIR_MATH
- RAW: `interval module I [2 , 4] is`
  FIX: `interval module \( I[2, 4] \) is`
- RAW: ```
$$
0 \leftarrow \frac { 0 } { k } \ k \leftarrow \frac { \text {Id} } { k } \ k \xrightarrow { \text {Id} } k \longleftarrow \longrightarrow k \longrightarrow 0 . \\ \diamond
$$
```
  FIX: ```
\[
0 \leftarrow \frac { 0 } { k } \ k \leftarrow \frac { \text {Id} } { k } \ k \xrightarrow { \text {Id} } k \longleftarrow \longrightarrow k \longrightarrow 0 . \\ \diamond
\]
```
- RAW: `zigzag module M ,`
  FIX: `zigzag module \( M \),`
- RAW: `multiset S (`
  FIX: `multiset \( S \) (`
- RAW: `M ∼ = I ∈ S I I .`
  FIX: `\( M \cong \bigoplus_{I \in S} I \).`
- RAW: `The multiset S is denoted as the barcode of M .`
  FIX: `The multiset \( S \) is denoted as the barcode of \( M \).`
- RAW: `Let TD be a transition diagram for a zigzag filtration of block decompositions B .`
  FIX: `Let \( TD \) be a transition diagram for a zigzag filtration of block decompositions \( B \).`
- RAW: `we denote by P from now on.`
  FIX: `we denote by \( P \) from now on.`
- RAW: `functor to TD , we obtain a persistence module M over P .`
  FIX: `functor to \( TD \), we obtain a persistence module \( M \) over \( P \).`
- RAW: `properties of TD and M .`
  FIX: `properties of \( TD \) and \( M \).`
- RAW: `Lemma 7.1. Let TD be a transition diagram.`
  FIX: `Lemma 7.1. Let \( TD \) be a transition diagram.`
- RAW: `induced poset P representing an inclusion ( P p,λ ,E p,λ ) → ( P q,λ ,E q,λ ) then`
  FIX: `induced poset \( P \) representing an inclusion \( (P_{p,\lambda}, E_{p,\lambda}) \to (P_{q,\lambda}, E_{q,\lambda}) \) then`
- RAW: `homomorphism in homology is 0.`
  FIX: `homomorphism in homology is \( 0 \).`
- RAW: `linear map equals 0. □`
  FIX: `linear map equals \( 0 \). \(\square\)`
- RAW: `Remark 7.2 . Since all vertical arrows in TD come`
  FIX: `Remark 7.2. Since all vertical arrows in \( TD \) come`
- RAW: `Hasse diagram of P . In addition,`
  FIX: `Hasse diagram of \( P \). In addition,`
- RAW: `Consider the poset P induced by TD , and the quiver Q formed by the Hasse diagram of P . Consider the ideal I generated`
  FIX: `Consider the poset \( P \) induced by \( TD \), and the quiver \( Q \) formed by the Hasse diagram of \( P \). Consider the ideal \( I \) generated`
