# Manifest: Page 005

## REPLACE_TABLES
*(No tables found on this page)*

## REPAIR_PROSE
- RAW: `Poincare´`
  FIX: `Poincaré`
- RAW: `ﬁbration`
  FIX: `fibration`
- RAW: `suﬃces`
  FIX: `suffices`
- RAW: `suﬃciently`
  FIX: `sufficiently`
- RAW: `classiﬁed`
  FIX: `classified`
- RAW: `classiﬁes`
  FIX: `classifies`
- RAW: `ﬁrst`
  FIX: `first`
- RAW: `Lu¨c24`
  FIX: `Lück24`
- RAW: ```
/negationslash
```
  FIX: ```
```

## REPAIR_MATH
- RAW: ```
E \stackrel { \pi } { \rightarrow } M \stackrel { x f } { \longrightarrow } \{ - d \} \times B g l _ { 1 } ( \mathbb { S } ) \subseteq P i c ( \mathbb { S } ) ,
```
  FIX: ```
$$
E \stackrel { \pi } { \rightarrow } M \stackrel { x f } { \longrightarrow } \{ - d \} \times B g l _ { 1 } ( \mathbb { S } ) \subseteq P i c ( \mathbb { S } ) ,
$$
```
- RAW: ```
E \stackrel { \pi } { \rightarrow } M \stackrel { f } { \rightarrow } M ( \mathbb { Z } / 3 \mathbb { Z } , 4 ) \stackrel { x } { \rightarrow } B g l _ { 1 } ( \mathbb { S } )
```
  FIX: ```
$$
E \stackrel { \pi } { \rightarrow } M \stackrel { f } { \rightarrow } M ( \mathbb { Z } / 3 \mathbb { Z } , 4 ) \stackrel { x } { \rightarrow } B g l _ { 1 } ( \mathbb { S } )
$$
```
- RAW: ```
\Omega ^ { d \overline { \mathcal { S } } ^ { a l g } } ( E ) \rightarrow \Omega ^ { d - 1 } [ E \otimes L ^ { q } ( \mathbb { Z } ) ]
```
  FIX: ```
$$
\Omega ^ { d \overline { \mathcal { S } } ^ { a l g } } ( E ) \rightarrow \Omega ^ { d - 1 } [ E \otimes L ^ { q } ( \mathbb { Z } ) ]
$$
```
- RAW: `degree ≤ 6 is`
  FIX: `degree \( \leq 6 \) is`
- RAW: `complex X , ro( X ) = 0 implies that ro( X ) vanishes`
  FIX: `complex \( X \), \( ro(X) = 0 \) implies that \( ro(X) \) vanishes`
- RAW: `If tso( X ) = 0, does it follow that tso( X ) vanishes`
  FIX: `If \( tso(X) = 0 \), does it follow that \( tso(X) \) vanishes`
- RAW: `splitting of L q ( Z ) does not induce`
  FIX: `splitting of \( L^q(\mathbb{Z}) \) does not induce`
- RAW: `splitting of S alg ( X ) into S alg ( X ) and a second`
  FIX: `splitting of \( \mathcal{S}^{alg}(X) \) into \( \mathcal{S}^{alg}(X) \) and a second`
- RAW: `X is d -dimensional`
  FIX: `\( X \) is \( d \)-dimensional`
- RAW: `and π 1 ( X ) is a`
  FIX: `and \( \pi_1(X) \) is a`
- RAW: `Then S alg ( X ) = 0 but S alg ( X ) (2) ≃ [ X ⊗ τ ≤ 0 L q ( Z )] (2) = 0.`
  FIX: `Then \( \mathcal{S}^{alg}(X) = 0 \) but \( \mathcal{S}^{alg}(X)_{(2)} \simeq [X \otimes \tau_{\leq 0} L^q(\mathbb{Z})]_{(2)} = 0 \).`
- RAW: `one has tso( X ) = 0 whenever X is aspherical`
  FIX: `one has \( tso(X) = 0 \) whenever \( X \) is aspherical`
- RAW: `complex E with tso( E ) = 0 whose`
  FIX: `complex \( E \) with \( tso(E) = 0 \) whose`
- RAW: `Let ¯ x ∈ π ℓ (Bgl 1 ( S )) be an element of positive degree ℓ and order n .`
  FIX: `Let \( \bar{x} \in \pi_\ell(Bgl_1(\mathbb{S})) \) be an element of positive degree \( \ell \) and order \( n \).`
- RAW: `Let M ( Z /n Z ,ℓ ) be the corresponding mod n Moore space in dimension ℓ .`
  FIX: `Let \( M(\mathbb{Z}/n\mathbb{Z}, \ell) \) be the corresponding mod \( n \) Moore space in dimension \( \ell \).`
- RAW: `map x : M ( Z /n Z ,ℓ ) → Bgl 1 ( S ) whose composite with the tautological map S ℓ → M ( Z /n Z ,ℓ ) is ¯ x .`
  FIX: `map \( x : M(\mathbb{Z}/n\mathbb{Z}, \ell) \to Bgl_1(\mathbb{S}) \) whose composite with the tautological map \( S^\ell \to M(\mathbb{Z}/n\mathbb{Z}, \ell) \) is \( \bar{x} \).`
- RAW: `map f : M → M ( Z /n Z ,ℓ ) inducing an isomorphism on π ℓ with M a stably framed (smooth) m -manifold for any m > 2 ℓ ;`
  FIX: `map \( f: M \to M(\mathbb{Z}/n\mathbb{Z}, \ell) \) inducing an isomorphism on \( \pi_\ell \) with \( M \) a stably framed (smooth) \( m \)-manifold for any \( m > 2\ell \);`
- RAW: `embedded ℓ -sphere in S ℓ × S m − ℓ .`
  FIX: `embedded \( \ell \)-sphere in \( S^\ell \times S^{m-\ell} \).`
- RAW: `Let k ≥ ℓ +1 be`
  FIX: `Let \( k \geq \ell + 1 \) be`
- RAW: `, so that − xf : M → Bgl 1 ( S ) lifts along Baut ∗ ( S k ) → Bgl 1 ( S ) and let π : E → M be the associated`
  FIX: `, so that \( -xf : M \to Bgl_1(\mathbb{S}) \) lifts along \( Baut^*(S^k) \to Bgl_1(\mathbb{S}) \) and let \( \pi: E \to M \) be the associated`
- RAW: `map E → M ( Z /n Z ,ℓ ) is then again an isomorphism on π ℓ . Moreover, E is a`
  FIX: `map \( E \to M(\mathbb{Z}/n\mathbb{Z}, \ell) \) is then again an isomorphism on \( \pi_\ell \). Moreover, \( E \) is a`
- RAW: `where d = m + k is the dimension of E .`
  FIX: `where \( d = m + k \) is the dimension of \( E \).`
- RAW: `Let ¯ x ∈ π 4 (Bgl 1 ( S )) ∼ = Z / 24 Z be a generator of the 3-torsion and consider the`
  FIX: `Let \( \bar{x} \in \pi_4(Bgl_1(\mathbb{S})) \cong \mathbb{Z}/24\mathbb{Z} \) be a generator of the 3-torsion and consider the`
- RAW: `complex E associated to ¯ x as constructed above. Then, the composite`
  FIX: `complex \( E \) associated to \( \bar{x} \) as constructed above. Then, the composite`
- RAW: `of E but does not lift along BTop → Bgl 1 ( S ). Indeed, on π 4 it induces an inclusion Z / 3 Z ⊆ Z / 24 Z , which cannot factor through Z ⊕ Z / 2 ∼ = π 4 (BTop)`
  FIX: `of \( E \) but does not lift along \( BTop \to Bgl_1(\mathbb{S}) \). Indeed, on \( \pi_4 \) it induces an inclusion \( \mathbb{Z}/3\mathbb{Z} \subseteq \mathbb{Z}/24\mathbb{Z} \), which cannot factor through \( \mathbb{Z} \oplus \mathbb{Z}/2 \cong \pi_4(BTop) \)`
- RAW: `tso( E ) = 0. To that end, note`
  FIX: `\( tso(E) = 0 \). To that end, note`
- RAW: `that E is simply connected.`
  FIX: `that \( E \) is simply connected.`
- RAW: `of the π π -theorem,`
  FIX: `of the \( \pi-\pi \)-theorem,`
- RAW: `tso( E ) = 0, it`
  FIX: `\( tso(E) = 0 \), it`
- RAW: `ro( E ) = 0.`
  FIX: `\( ro(E) = 0 \).`
- RAW: `composite E → M ( Z / 3 Z , 4) → {− d } × Bgl 1 ( S ) is the`
  FIX: `composite \( E \to M(\mathbb{Z}/3\mathbb{Z}, 4) \to \{-d\} \times Bgl_1(\mathbb{S}) \) is the`
- RAW: ` D E of E . In particular, we obtain a map Th( D E ) → Ω d Th( x ), where Th( − ) is the Thom spectrum functor.`
  FIX: ` \( D_E \) of \( E \). In particular, we obtain a map \( Th(D_E) \to \Omega^d Th(x) \), where \( Th(-) \) is the Thom spectrum functor.`
