# Manifest: Page 014

## REPAIR_MATH
- RAW: ```
E | E [ X _ { k } ^ { 0 } | X _ { 0 } , Y _ { 1 } , \dots , Y _ { k } , \{ X _ { 1 } ^ { v } , \dots , X _ { k } ^ { v } \colon | v | > m \} ] | \geq \frac { 1 } { 4 }
```
  FIX: ```
$$
E | E [ X _ { k } ^ { 0 } | X _ { 0 } , Y _ { 1 } , \dots , Y _ { k } , \{ X _ { 1 } ^ { v } , \dots , X _ { k } ^ { v } \colon | v | > m \} ] | \geq \frac { 1 } { 4 }
$$
```
- RAW: ```
P \left [ \Sigma ^ { x } ( \{ z \colon z ^ { 0 } = x ^ { 0 } \} ) \geq \frac { 3 } { 4 } \right ] \geq \frac { 1 } { 2 }
```
  FIX: ```
$$
P \left [ \Sigma ^ { x } ( \{ z \colon z ^ { 0 } = x ^ { 0 } \} ) \geq \frac { 3 } { 4 } \right ] \geq \frac { 1 } { 2 }
$$
```
- RAW: ```
P \left [ | E [ X _ { k } ^ { 0 } | X _ { 0 } , Y _ { 1 } , \dots , Y _ { k } , \{ X _ { 1 } ^ { v } , \dots , X _ { k } ^ { v } \colon | v | > m \} ] | \geq \frac { 1 } { 2 } \left | X _ { 0 } , \dots , X _ { k } \right ] \geq \frac { 1 } { 2 } ,
```
  FIX: ```
$$
P \left [ | E [ X _ { k } ^ { 0 } | X _ { 0 } , Y _ { 1 } , \dots , Y _ { k } , \{ X _ { 1 } ^ { v } , \dots , X _ { k } ^ { v } \colon | v | > m \} ] | \geq \frac { 1 } { 2 } \left | X _ { 0 } , \dots , X _ { k } \right ] \geq \frac { 1 } { 2 } ,
$$
```
- RAW: ```
\mathfrak { E } J ^ { \prime } \coloneqq \{ \{ q , r \} \colon q \in J ^ { \prime } , \ r \in ( J \wedge J ^ { \prime } ) \cup \partial J , \ \| q - r \| = 1 \} .
```
  FIX: ```
$$
\mathfrak { E } J ^ { \prime } \coloneqq \{ \{ q , r \} \colon q \in J ^ { \prime } , \ r \in ( J \wedge J ^ { \prime } ) \cup \partial J , \ \| q - r \| = 1 \} .
$$
```
- RAW: ```
\Sigma ^ { x } ( \{ z \colon z ^ { 0 } = - x ^ { 0 } \} ) \leq \Sigma ^ { x } ( \{ z \colon \exists J ^ { \prime } \in \mathfrak { C } _ { z , x } , \ 0 \in J ^ { \prime } \} ) \leq \sum _ { J ^ { \prime } \ni 0 } \Sigma ^ { x } ( \{ z \colon J ^ { \prime } \in \mathfrak { C } _ { z , x } \} ) .
```
  FIX: ```
$$
\Sigma ^ { x } ( \{ z \colon z ^ { 0 } = - x ^ { 0 } \} ) \leq \Sigma ^ { x } ( \{ z \colon \exists J ^ { \prime } \in \mathfrak { C } _ { z , x } , \ 0 \in J ^ { \prime } \} ) \leq \sum _ { J ^ { \prime } \ni 0 } \Sigma ^ { x } ( \{ z \colon J ^ { \prime } \in \mathfrak { C } _ { z , x } \} ) .
$$
```
- RAW: ```
\Sigma ^ { x } ( \{ z \colon J ^ { \prime } \in \mathfrak { C } _ { z , x } \} ) \leq \exp \left ( - \, 2 \beta \sum _ { \{ q , r \} \in \mathfrak { C } _ { J ^ { \prime } } } \xi ^ { q r } \right ) .
```
  FIX: ```
$$
\Sigma ^ { x } ( \{ z \colon J ^ { \prime } \in \mathfrak { C } _ { z , x } \} ) \leq \exp \left ( - \, 2 \beta \sum _ { \{ q , r \} \in \mathfrak { C } _ { J ^ { \prime } } } \xi ^ { q r } \right ) .
$$
```
- RAW: ```
A & = \{ z \colon z ^ { q } = - x ^ { q } \text { and } z ^ { r } = x ^ { r } \text { for } \{ q , r \} \in \mathfrak { E } J ^ { \prime } , \ q \in J ^ { \prime } \} , \\ B & = \{ z \colon z ^ { q } = x ^ { q } \text { and } z ^ { r } = x ^ { r } \text { for } \{ q , r \} \in \mathfrak { E } J ^ { \prime } , \ q \in J ^ { \prime } \} .
```
  FIX: ```
$$
A & = \{ z \colon z ^ { q } = - x ^ { q } \text { and } z ^ { r } = x ^ { r } \text { for } \{ q , r \} \in \mathfrak { E } J ^ { \prime } , \ q \in J ^ { \prime } \} , \\ B & = \{ z \colon z ^ { q } = x ^ { q } \text { and } z ^ { r } = x ^ { r } \text { for } \{ q , r \} \in \mathfrak { E } J ^ { \prime } , \ q \in J ^ { \prime } \} .
$$
```
- RAW: `Proposition 3.6. There exists an absolute constant 0 < p < 1 / 2 such that`
  FIX: `Proposition 3.6. There exists an absolute constant \( 0 < p_0 < 1 / 2 \) such that`
- RAW: `for every k,m ≥ 1 whenever 0 < p < p .`
  FIX: `for every \( k, m \geq 1 \) whenever \( 0 < p < p_0 \).`
- RAW: `Proof. Let us ﬁx k,m ≥ 1 throughout the proof, and deﬁne 0 := ( k, 0) ∈ J . We will prove below the following claim: there exists an absolute constant 0 < p < 1 / 2 such that`
  FIX: `Proof. Let us ﬁx \( k, m \geq 1 \) throughout the proof, and deﬁne \( 0 \coloneqq (k, 0) \in J \). We will prove below the following claim: there exists an absolute constant \( 0 < p_0 < 1 / 2 \) such that`
- RAW: `whenever 0 < p < p   : that is, when the noise is suﬃciently small, the conditional distribution P [ X 0 k ∈ ·| X 0 ,Y 1 ,...,Y k , { X v 1 ,...,X v k : | v | > m } ] assigns a large probability to the actually realized value of X 0 k at least half of the time (recall Lemma 3.4). Let us complete the proof assuming this claim. Note that Σ x ( { z : z 0 = x 0 } ) ≥ 3 / 4 implies | Σ x ( { z : z 0 = 1 } ) − Σ x ( { z : z 0 = − 1 } ) | ≥ 1 / 2. Thus the above claim implies that`
  FIX: `whenever \( 0 < p < p_0 \): that is, when the noise is suﬃciently small, the conditional distribution \( \mathbf{P} [ X_k^0 \in \cdot \mid X_0, Y_1, \dots, Y_k, \{ X_1^v, \dots, X_k^v \colon |v| > m \} ] \) assigns a large probability to the actually realized value of \( X_k^0 \) at least half of the time (recall Lemma 3.4). Let us complete the proof assuming this claim. Note that \( \Sigma^x (\{ z \colon z^0 = x^0 \}) \geq 3 / 4 \) implies \( | \Sigma^x (\{ z \colon z^0 = 1 \}) - \Sigma^x (\{ z \colon z^0 = - 1 \}) | \geq 1 / 2 \). Thus the above claim implies that`
- RAW: `  where we have used Lemma 3.4 and the fact that { X q } and { ξ qr } are independent. The proof is now completed by a straightforward estimate.`
  FIX: `  where we have used Lemma 3.4 and the fact that \( \{ X^q \} \) and \( \{ \xi^{qr} \} \) are independent. The proof is now completed by a straightforward estimate.`
- RAW: `It remains to prove the claim. To this end, we use a Peierls argument. Fix for the time being conﬁgurations x,z ∈ {− 1 , 1 } J . For any J ⊆ J , deﬁne the boundary edges`
  FIX: `It remains to prove the claim. To this end, we use a Peierls argument. Fix for the time being conﬁgurations \( x, z \in \{ -1, 1 \}^J \). For any \( J^\prime \subseteq J \), deﬁne the boundary edges`
- RAW: `A subset J ⊆ J is called a contour if it is simply connected, z q = − x q for all { q,r } ∈ E J with q ∈ J , and z r = x r if in addition r ∈ J \ J . We will denote the set of contours as C z,x (note that the deﬁnition of a contour depends on the given conﬁgurations z and x ). If z 0 = − x 0 , then there must exist a contour J ∈ C z,x such that 0 ∈ J : indeed, construct J by choosing the maximal connected subset of J such that 0 ∈ J and z q = − x q for all q ∈ J , and then ‘ﬁll in the holes’ to make J simply connected. Thus`
  FIX: `A subset \( J^\prime \subseteq J \) is called a contour if it is simply connected, \( z^q = -x^q \) for all \( \{ q, r \} \in \mathfrak{E}J^\prime \) with \( q \in J^\prime \), and \( z^r = x^r \) if in addition \( r \in J \setminus J^\prime \). We will denote the set of contours as \( \mathfrak{C}_{z,x} \) (note that the deﬁnition of a contour depends on the given conﬁgurations \( z \) and \( x \)). If \( z^0 = -x^0 \), then there must exist a contour \( J^\prime \in \mathfrak{C}_{z,x} \) such that \( 0 \in J^\prime \): indeed, construct \( J^\prime \) by choosing the maximal connected subset of \( J \) such that \( 0 \in J^\prime \) and \( z^q = -x^q \) for all \( q \in J^\prime \), and then ‘ﬁll in the holes’ to make \( J^\prime \) simply connected. Thus`
- RAW: `Now note that, by the deﬁnition of a contour, x q z q = − 1 whenever { q,r } ∈ E J with q ∈ J , and x q x r z q z r = − 1 if in addition r ∈ J \ J . Thus the existence of a contour implies the presence of many such edges. The basic idea of the proof is that the probability that this occurs is small under Σ x due to Lemma 3.4. Let us make this precise.`
  FIX: `Now note that, by the deﬁnition of a contour, \( x^q z^q = -1 \) whenever \( \{ q, r \} \in \mathfrak{E}J^\prime \) with \( q \in J^\prime \), and \( x^q x^r z^q z^r = -1 \) if in addition \( r \in J \setminus J^\prime \). Thus the existence of a contour implies the presence of many such edges. The basic idea of the proof is that the probability that this occurs is small under \( \Sigma^x \) due to Lemma 3.4. Let us make this precise.`
- RAW: `Lemma 3.7. For any J ⊆ J , we have`
  FIX: `Lemma 3.7. For any \( J^\prime \subseteq J \), we have`
- RAW: `Proof. Assume without loss of generality that J is simply connected. Let us use for simplicity the convention that z r = x r for r ∈ ∂J . Deﬁne the events`
  FIX: `Proof. Assume without loss of generality that \( J^\prime \) is simply connected. Let us use for simplicity the convention that \( z^r = x^r \) for \( r \in \partial J^\prime \). Deﬁne the events`
