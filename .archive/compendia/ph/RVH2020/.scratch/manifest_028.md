# Manifest: Page 028

## REPAIR_PROSE
- RAW: ```
ca`dl`g
```
  FIX: ```
càdlàg
```

## REPAIR_MATH
- RAW: ```
d Y _ { t } ^ { v } = X _ { t } ^ { v } \, d t + \sigma \, d W _ { t } ^ { v } , \quad Y _ { 0 } ^ { v } = 0 , \quad v \in \mathbb { Z } ,
```
  FIX: ```
\[
d Y _ { t } ^ { v } = X _ { t } ^ { v } \, d t + \sigma \, d W _ { t } ^ { v } , \quad Y _ { 0 } ^ { v } = 0 , \quad v \in \mathbb { Z } ,
\]
```
- RAW: ```
( X _ { t } ^ { v } , Y _ { t } ^ { v } ) _ { t \in \mathbb { R } , v \in \mathbb { Z } } \stackrel { \text {law} } { = } ( X _ { t + s } ^ { v + w } , Y _ { t + s } ^ { v + w } - Y _ { s } ^ { v + w } ) _ { t \in \mathbb { R } , v \in \mathbb { Z } } \text { for all } s \in \mathbb { R } , \ w \in \mathbb { Z }
```
  FIX: ```
\[
( X _ { t } ^ { v } , Y _ { t } ^ { v } ) _ { t \in \mathbb { R } , v \in \mathbb { Z } } \stackrel { \text {law} } { = } ( X _ { t + s } ^ { v + w } , Y _ { t + s } ^ { v + w } - Y _ { s } ^ { v + w } ) _ { t \in \mathbb { R } , v \in \mathbb { Z } } \text { for all } s \in \mathbb { R } , \ w \in \mathbb { Z }
\]
```
- RAW: ```
| \mathbf P [ X _ { t } \in A | X _ { 0 } , \{ Y _ { s } \} _ { 0 \leq s \leq t } ] - \mathbf P [ X _ { t } \in A | \{ Y _ { s } \} _ { 0 \leq s \leq t } ] | \xrightarrow { t \to \infty } 0 \ \ i n \ L ^ { 1 }
```
  FIX: ```
\[
| \mathbf P [ X _ { t } \in A | X _ { 0 } , \{ Y _ { s } \} _ { 0 \leq s \leq t } ] - \mathbf P [ X _ { t } \in A | \{ Y _ { s } \} _ { 0 \leq s \leq t } ] | \xrightarrow { t \to \infty } 0 \ \ i n \ L ^ { 1 }
\]
```
- RAW: ```
process X = ( X t ) t ∈ R with
```
  FIX: ```
process \( X = (X_t)_{t \in \mathbb{R}} \) with
```
- RAW: ```
in {− 1 , 1 } Z . We
```
  FIX: ```
in \( \{-1, 1\}^{\mathbb{Z}} \). We
```
- RAW: ```
that X satisﬁes
```
  FIX: ```
that \( X \) satisﬁes
```
- RAW: ```
that x  → E x [ f ( X t )] is
```
  FIX: ```
that \( x \mapsto \mathbb{E}_x [f(X_t)] \) is
```
- RAW: ```
every t and bounded quasilocal function f (a
```
  FIX: ```
every \( t \) and bounded quasilocal function \( f \) (a
```
- RAW: ```
observations ( Y t ) t ∈ R ,
```
  FIX: ```
observations \( (Y_t)_{t \in \mathbb{R}} \),
```
- RAW: ```
where ( W v t ) t ∈ R are
```
  FIX: ```
where \( (W_t^v)_{t \in \mathbb{R}} \) are
```
- RAW: ```
independent of X ,
```
  FIX: ```
independent of \( X \),
```
- RAW: ```
and σ > 0 denotes
```
  FIX: ```
and \( \sigma > 0 \) denotes
```
- RAW: ```
process ( X t ,Y t ) t ∈ R is
```
  FIX: ```
process \( (X_t, Y_t)_{t \in \mathbb{R}} \) is
```
- RAW: ```
on Z d with
```
  FIX: ```
on \( \mathbb{Z}^d \) with
```
- RAW: ```
increments of Y that are translation invariant and not Y itself).
```
  FIX: ```
increments of \( Y \) that are translation invariant and not \( Y \) itself).
```
- RAW: ```
set A .
```
  FIX: ```
set \( A \).
```
- RAW: ```
stability of X is
```
  FIX: ```
stability of \( X \) is
```
- RAW: ```
on {− 1 , 1 } Z is
```
  FIX: ```
on \( \{-1, 1\}^{\mathbb{Z}} \) is
```
