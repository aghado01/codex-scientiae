# Manifest: Page 006

## REPAIR_MATH
- RAW: ```
\sup _ { A } | P [ X _ { k } \in A | X _ { 0 } ] - \lambda ( A ) | \stackrel { k \to \infty } { \longrightarrow } 0 \ \ i n \ L ^ { 1 } .
```
  FIX: ```
$$
\sup _ { A } | P [ X _ { k } \in A | X _ { 0 } ] - \lambda ( A ) | \stackrel { k \to \infty } { \longrightarrow } 0 \ \ i n \ L ^ { 1 } .
$$
```
- RAW: ```
\Phi ( x ^ { \prime } , x , d y ) = g ( x ^ { \prime } , x , y ) \, \varphi ( d y ) , \quad g ( x ^ { \prime } , x , y ) > 0 \ \text {for all } x , x ^ { \prime } , y .
```
  FIX: ```
$$
\Phi ( x ^ { \prime } , x , d y ) = g ( x ^ { \prime } , x , y ) \, \varphi ( d y ) , \quad g ( x ^ { \prime } , x , y ) > 0 \ \text {for all } x , x ^ { \prime } , y .
$$
```
- RAW: ```
\sup _ { A } | P [ X _ { k } \in A | X _ { 0 } , Y _ { 1 } , \dots , Y _ { k } ] - P [ X _ { k } \in A | Y _ { 1 } , \dots , Y _ { k } ] | \stackrel { k \to \infty } { \longrightarrow } 0 \ \ i n \ L ^ { 1 } .
```
  FIX: ```
$$
\sup _ { A } | P [ X _ { k } \in A | X _ { 0 } , Y _ { 1 } , \dots , Y _ { k } ] - P [ X _ { k } \in A | Y _ { 1 } , \dots , Y _ { k } ] | \stackrel { k \to \infty } { \longrightarrow } 0 \ \ i n \ L ^ { 1 } .
$$
```
- RAW: ```
P ^ { \mu } [ X _ { k } \in \cdot ] \xrightarrow { k \to \infty } \lambda \ \text { for every } \mu
```
  FIX: ```
$$
P ^ { \mu } [ X _ { k } \in \cdot ] \xrightarrow { k \to \infty } \lambda \ \text { for every } \mu
$$
```
- RAW: ```
| \pi _ { k } ^ { \mu } ( f ) - \pi _ { k } ^ { \nu } ( f ) | \stackrel { k \to \infty } { \longrightarrow } 0 \ \text { in } L ^ { 1 } ( P ^ { \gamma } )
```
  FIX: ```
$$
| \pi _ { k } ^ { \mu } ( f ) - \pi _ { k } ^ { \nu } ( f ) | \stackrel { k \to \infty } { \longrightarrow } 0 \ \text { in } L ^ { 1 } ( P ^ { \gamma } )
$$
```
- RAW: `condition X 0 under`
  FIX: `condition \( X _ { 0 } \) under`
- RAW: `Denote by P µ the`
  FIX: `Denote by \( P ^ { \mu } \) the`
- RAW: `process ( X k ,Y k ) k ≥ 0 with`
  FIX: `process \( ( X _ { k } , Y _ { k } ) _ { k \ge 0 } \) with`
- RAW: `distribution X 0 ∼ µ . A`
  FIX: `distribution \( X _ { 0 } \sim \mu \). A`
- RAW: `started at µ as π µ k := P µ [ X k ∈ ·| Y 1 ,...,Y k ], we`
  FIX: `started at \( \mu \) as \( \pi _ { k } ^ { \mu } := P ^ { \mu } [ X _ { k } \in \cdot | Y _ { 1 } , \dots , Y _ { k } ] \), we`
- RAW: `measures µ,ν,γ and functions f . The`
  FIX: `measures \( \mu , \nu , \gamma \) and functions \( f \). The`
- RAW: `case ν = λ and µ = γ = δ x for x outside a λ -null set.`
  FIX: `case \( \nu = \lambda \) and \( \mu = \gamma = \delta _ { x } \) for \( x \) outside a \( \lambda \)-null set.`
- RAW: `measures µ,ν,γ , cf.`
  FIX: `measures \( \mu , \nu , \gamma \), cf.`
- RAW: `process ( π k ) k ≥ 0 is itself`
  FIX: `process \( ( \pi _ { k } ) _ { k \ge 0 } \) is itself`
- RAW: `chain ( X k ) k ≥ 0 ‘lift’ to ergodic properties of the Markov chain ( π k ) k ≥ 0 . For`
  FIX: `chain \( ( X _ { k } ) _ { k \ge 0 } \) ‘lift’ to ergodic properties of the Markov chain \( ( \pi _ { k } ) _ { k \ge 0 } \). For`
- RAW: `example, if ( X k ) k ≥ 0 admits a unique stationary measure, does ( π k ) k ≥ 0 admit a unique stationary measure also? Similarly, if ( X k ) k ≥ 0 converges to its stationary measure starting from any initial condition, does the same property hold for ( π k ) k ≥ 0 ? Remarkably`
  FIX: `example, if \( ( X _ { k } ) _ { k \ge 0 } \) admits a unique stationary measure, does \( ( \pi _ { k } ) _ { k \ge 0 } \) admit a unique stationary measure also? Similarly, if \( ( X _ { k } ) _ { k \ge 0 } \) converges to its stationary measure starting from any initial condition, does the same property hold for \( ( \pi _ { k } ) _ { k \ge 0 } \)? Remarkably`

## REPAIR_PROSE
- RAW: `ﬁ`
  FIX: `fi`
- RAW: `ﬀ`
  FIX: `ff`
- RAW: `Theorem 2.2 ([50]) . Suppose`
  FIX: `Theorem 2.2 ([50]). Suppose`
