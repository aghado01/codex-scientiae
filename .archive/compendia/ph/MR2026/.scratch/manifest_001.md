# Manifest: Page 001

## REPAIR_MATH
- RAW: `for each system size N and control parameter λ , observational data define a random persistence diagram D N ( λ ) in a standard diagram metric space.`
  FIX: `for each system size \( N \) and control parameter \( \lambda \), observational data define a random persistence diagram \( D_N(\lambda) \) in a standard diagram metric space.`

- RAW: `Assuming (i) convergence in probability of persistence diagrams to deterministic limits on either side of a critical value λ c , and (ii) a macroscopic feature separation condition`
  FIX: `Assuming (i) convergence in probability of persistence diagrams to deterministic limits on either side of a critical value \( \lambda_c \), and (ii) a macroscopic feature separation condition`

## REPLACE_TABLES
None

## REPAIR_PROSE
- RAW: `# Abstract`
  FIX: `## Abstract`

- RAW: `# 1 Introduction`
  FIX: `## 1 Introduction`

- RAW: `provably separates two phases. We formulate a probabilistic framework in which, for each system size N and control parameter λ , observational data define a random persistence diagram D N ( λ ) in a standard diagram metric space. Assuming (i) convergence in probability of persistence diagrams to deterministic limits on either side of a critical value λ c , and (ii) a macroscopic feature separation condition—namely the presence of at least one persistent bar with lifetime bounded away from zero in one phase and only vanishingly short bars in the other—we show that persistent entropy exhibits an asymptotically non-vanishing gap across phases. The result relies only on continuity of PE along the convergent diagram sequence (or under mild regularization or lifetime truncation), and is therefore broadly applicable across data modalities, filtrations, and homological degrees.`
  FIX: `provably separates two phases. We formulate a probabilistic framework in which, for each system size \( N \) and control parameter \( \lambda \), observational data define a random persistence diagram \( D_N(\lambda) \) in a standard diagram metric space. Assuming (i) convergence in probability of persistence diagrams to deterministic limits on either side of a critical value \( \lambda_c \), and (ii) a macroscopic feature separation condition—namely the presence of at least one persistent bar with lifetime bounded away from zero in one phase and only vanishingly short bars in the other—we show that persistent entropy exhibits an asymptotically non-vanishing gap across phases. The result relies only on continuity of PE along the convergent diagram sequence (or under mild regularization or lifetime truncation), and is therefore broadly applicable across data modalities, filtrations, and homological degrees.`

- RAW: `suﬀicient conditions under which persistent entropy provably separates two phases.`
  FIX: `sufficient conditions under which persistent entropy provably separates two phases.`

- RAW: `a topological transition time defined via the stabilization of a chosen topological statistic on a sliding window, and a probability-based estimator of critical parameters within a finite observation horizon.`
  FIX: `a topological transition time defined via the stabilization of a chosen topological statistic on a sliding window, and a probability-based estimator of critical parameters within a finite observation horizon.`

- RAW: `suﬀicient conditions under which persistent entropy reliably detects phase transitions has remained limited`
  FIX: `sufficient conditions under which persistent entropy reliably detects phase transitions has remained limited`

- RAW: `diﬀicult to define, or strongly model-dependent.`
  FIX: `difficult to define, or strongly model-dependent.`

- RAW: `a general theoretical understanding of when and why persistent entropy reliably detects phase transitions has remained limited, particularly in settings where data and learned representations are inherently stochastic.`
  FIX: `a general theoretical understanding of when and why persistent entropy reliably detects phase transitions has remained limited, particularly in settings where data and learned representations are inherently stochastic.`

- RAW: `we establish a general, model-independent theorem providing suﬀicient conditions under which persistent entropy provably separates two phases.`
  FIX: `we establish a general, model-independent theorem providing sufficient conditions under which persistent entropy provably separates two phases.`

- RAW: `suitable order parameters may be unknown, diﬀicult to define, or strongly model-dependent.`
  FIX: `suitable order parameters may be unknown, difficult to define, or strongly model-dependent.`
