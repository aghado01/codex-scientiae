# Manifest: Page 015

## REPAIR_MATH
- RAW: ```
f _ { \theta } \colon \mathcal { X } \rightarrow \mathcal { Y } ,
```
  FIX: ```
$$
f _ { \theta } \colon \mathcal { X } \rightarrow \mathcal { Y } ,
$$
```
- RAW: ```
\mathcal { L } ( \theta ) & = \frac { 1 } { | \mathcal { D } | } \sum _ { ( x , y ) \in \mathcal { D } } \ell ( f _ { \theta } ( x ) , y ) , \\
```
  FIX: ```
$$
\mathcal { L } ( \theta ) & = \frac { 1 } { | \mathcal { D } | } \sum _ { ( x , y ) \in \mathcal { D } } \ell ( f _ { \theta } ( x ) , y ) , \\
$$
```
- RAW: ```
with parameters θ ∈ R P optimized
```
  FIX: ```
with parameters \( \theta \in \mathbb{R}^P \) optimized
```
- RAW: ```
parameter values { θ ( t ) } t ≥ 0 , where t denotes
```
  FIX: ```
parameter values \( \{ \theta(t) \}_{t \ge 0} \), where \( t \) denotes
```

## REPAIR_PROSE
- RAW: ```
[Page 15]

# 5.4 Neural network training dynamics
```
  FIX: ```
# 5.4 Neural network training dynamics
```

## REPLACE_TABLES
None
