# Manifest: Page 016

## REPAIR_PROSE
- RAW: `we repeated each experiment over R = 5 independent runs`
  FIX: `we repeated each experiment over \( R = 5 \) independent runs`

- RAW: `Persistent homology was then computed in homological degree H 1 , and the corresponding persistent entropy PE( H 1 ) was evaluated`
  FIX: `Persistent homology was then computed in homological degree \( H_1 \), and the corresponding persistent entropy \( PE(H_1) \) was evaluated`

- RAW: `# 5.4.2 Neural networks: experimental output analysis`
  FIX: `### 5.4.2 Neural networks: experimental output analysis`

## REPLACE_TABLES
None

## REPAIR_MATH
None
