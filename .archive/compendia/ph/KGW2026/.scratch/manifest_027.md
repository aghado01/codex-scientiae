# Manifest: Page 027

## REPAIR_PROSE
- RAW: ```
where c is the number connected components of digraph.
```
  FIX: ```
where \(c\) is the number of connected components of the digraph.
```

## REPAIR_MATH
- RAW: ```
C = \{ C _ { e } \colon e \in G \ \ T \}
```
  FIX: ```
$$
C = \{ C _ { e } \colon e \in G \setminus T \}
$$
```
- RAW: ```
c _ { e } = \sum y _ { i } v _ { i } \in \ker ( \partial _ { 1 } )
```
  FIX: ```
$$
c _ { e } = \sum y _ { i } v _ { i } \in \ker ( \partial _ { 1 } )
$$
```
- RAW: ```
C _ { n } = \{ C _ { e _ { 1 } } , \dots , C _ { e _ { t } } \}
```
  FIX: ```
$$
C _ { n } = \{ C _ { e _ { 1 } } , \dots , C _ { e _ { t } } \}
$$
```
- RAW: ```
kernel of ∂ 1 for N = 2
```
  FIX: ```
kernel of \(\partial_1\) for \(N = 2\)
```
- RAW: ```
to the case N ≥ 3
```
  FIX: ```
to the case \(N \ge 3\)
```
- RAW: ```
Theorem 3.10. Let G be a digraph and G u its underlying undirected graph. Then the kernel of ∂ 1 : Ω N 1 → Ω N 0 for N ≥ 3 is generated by:
```
  FIX: ```
Theorem 3.10. Let \(G\) be a digraph and \(G_u\) its underlying undirected graph. Then the kernel of \(\partial_1 : \Omega_1^N \to \Omega_0^N\) for \(N \ge 3\) is generated by:
```
- RAW: ```
Proof. Let v ∈ ker( ∂ 1 ) and let T be a spanning tree of G u . Each non-tree edge e ∈ G \ T determines a fundamental cycle C e . Let
```
  FIX: ```
Proof. Let \(v \in \ker(\partial_1)\) and let \(T\) be a spanning tree of \(G_u\). Each non-tree edge \(e \in G \setminus T\) determines a fundamental cycle \(C_e\). Let
```
- RAW: ```
Partition C into admissible cycles C a and non-admissible cycles C n . For each admissible cycle C e ∈ C a , by Proposition 3.2, there exists a weighted cycle
```
  FIX: ```
Partition \(C\) into admissible cycles \(C_a\) and non-admissible cycles \(C_n\). For each admissible cycle \(C_e \in C_a\), by Proposition 3.2, there exists a weighted cycle
```
- RAW: ```
supported on C e .
```
  FIX: ```
supported on \(C_e\).
```
- RAW: ```
If v contains the edge e , define v ′ = v − c e therefore v ′ ∈ ker ( ∂ 1 ). Repeating this process, we obtain a vector v ′ in the kernel that is free from admissible cycles.
```
  FIX: ```
If \(v\) contains the edge \(e\), define \(v' = v - c_e\) therefore \(v' \in \ker(\partial_1)\). Repeating this process, we obtain a vector \(v'\) in the kernel that is free from admissible cycles.
```
- RAW: ```
If v ′ is supported only on edges of the spanning tree T , then v ′ cannot lie in the kernel
```
  FIX: ```
If \(v'\) is supported only on edges of the spanning tree \(T\), then \(v'\) cannot lie in the kernel
```
- RAW: ```
Hence v ′ must involve edges corresponding to non-admissible cycles.
```
  FIX: ```
Hence \(v'\) must involve edges corresponding to non-admissible cycles.
```
- RAW: ```
be the non-admissible cycles appearing in v ′ .
```
  FIX: ```
be the non-admissible cycles appearing in \(v'\).
```
- RAW: ```
For any two cycles I,J ∈ C n with nonempty intersection (i.e., sharing at least one vertex), consider their merge I ∪ J . By Proposition 3.3, there exists a nonzero weighted element supported on I ∪ J that lies in ker( ∂ 1 ). Using such elements, we can express v ′ as a linear combination of kernel elements supported on merges of non-admissible cycles.
```
  FIX: ```
For any two cycles \(I, J \in C_n\) with nonempty intersection (i.e., sharing at least one vertex), consider their merge \(I \cup J\). By Proposition 3.3, there exists a nonzero weighted element supported on \(I \cup J\) that lies in \(\ker(\partial_1)\). Using such elements, we can express \(v'\) as a linear combination of kernel elements supported on merges of non-admissible cycles.
```
- RAW: ```
Each fundamental cycle C e contains a unique non-tree edge e .
```
  FIX: ```
Each fundamental cycle \(C_e\) contains a unique non-tree edge \(e\).
```
- RAW: ```
We conclude that ker( ∂ 1 ) is generated by admissible cycles and kernel elements supported on merges of non-admissible cycles.
```
  FIX: ```
We conclude that \(\ker(\partial_1)\) is generated by admissible cycles and kernel elements supported on merges of non-admissible cycles.
```
- RAW: ```
homology group H N,q 1 for N ≥ 3 differs fundamentally from the classical case N = 2.
```
  FIX: ```
homology group \(H_{N,q}^1\) for \(N \ge 3\) differs fundamentally from the classical case \(N = 2\).
```
- RAW: ```
lie in ker( ∂ 1 ), and additional
```
  FIX: ```
lie in \(\ker(\partial_1)\), and additional
```
- RAW: ```
dimension of Z N,q 1 is bounded above by | E | − | V | + c
```
  FIX: ```
dimension of \(Z_{N,q}^1\) is bounded above by \(|E| - |V| + c\)
```
