# Manifest: Page 004

## REPAIR_MATH
- RAW: ```
\det \text {of vectors} \, ^ { f ^ { \prime } } = \left ( \sqrt { g } \left ( t _ { 1 } \right ) , f ^ { ( g ) } ( t _ { 2 } ) , \dots , f ^ { ( g ) } ( t _ { p } ) \right ) \text {for MC/CM iterates} \, g = 1 , \dots , G , \text { each}
```
  FIX: ```
$$
\det \text {of vectors} \, ^ { f ^ { \prime } } = \left ( \sqrt { g } \left ( t _ { 1 } \right ) , f ^ { ( g ) } ( t _ { 2 } ) , \dots , f ^ { ( g ) } ( t _ { p } ) \right ) \text {for MC/CM iterates} \, g = 1 , \dots , G , \text { each}
$$
```
- RAW: ```
P ( t _ { 1 } , \dots , t _ { n } )
```
  FIX: ```
$$
P ( t _ { 1 } , \dots , t _ { n } )
$$
```
- RAW: ```
P ( t _ { 1 } , \dots
```
  FIX: ```
$$
P ( t _ { 1 } , \dots
$$
```
- RAW: ```
produce a draw ϕ ( g ) from the posterior distribution of any characteristic ϕ = ϕ ( f ) (such as the value at which the maximum of f ( t ) occurs).
```
  FIX: ```
produce a draw \( \phi^{(g)} \) from the posterior distribution of any characteristic \( \phi = \phi(f) \) (such as the value at which the maximum of \( f(t) \) occurs).
```
- RAW: ```
interval [ A, B ]. The user may sample from the posterior distribution of any functional ϕ simply by evaluating
```
  FIX: ```
interval \( [A, B] \). The user may sample from the posterior distribution of any functional \( \phi \) simply by evaluating
```
- RAW: ```
maximum of f ( t ) is obtained by finding the location of the maximum of for each g .
```
  FIX: ```
maximum of \( f(t) \) is obtained by finding the location of the maximum of for each \( g \).
```
- RAW: ```
intensity function λ = λ( t ) based on a sequence of event times t 1 , t 2 , . . . , t n in an interval (0, T ] is
```
  FIX: ```
intensity function \( \lambda = \lambda(t) \) based on a sequence of event times \( t_1, t_2, \dots, t_n \) in an interval \( (0, T] \) is
```
- RAW: ```
Here the number of events N is a Poisson random variable with expectation . Conditionally on the number of events N = n the probability density becomes
```
  FIX: ```
Here the number of events \( N \) is a Poisson random variable with expectation . Conditionally on the number of events \( N = n \) the probability density becomes
```

