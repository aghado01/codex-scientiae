Original 120: ⟨ ⟩ where 𝑥 𝑐 is the center of 𝑋 calculated by Eq. ( 2 ). Let denote the set of points corresponding 𝑋 𝑖 to each node 𝑣 𝑖 in 𝐺 . In Eq. ( 8 ) and ( 10 ) , we substitute 𝑓 $⟂$ for 𝑓 and 𝑋 𝑖 for 𝑋 to compute the number of intervals 𝑆 𝑖 that 𝑋 𝑖 can partition in the orthogonal direction. We denote the set of nodes awaiting splitting as $=$
Result 120:   where $x_c$ is the center of $X$ calculated by Eq. ( 2 ). Let $X_i$ denote the set of points corresponding to each node $v_i$ in $G$. In Eq. ( 8 ) and ( 10 ) , we substitute $f^\perp$ for $f$ and $X_i$ for $X$ to compute the number of intervals $S_i$ that $X_i$ can partition in the orthogonal direction. We denote the set of nodes awaiting splitting as $=$
