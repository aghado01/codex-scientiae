# Batch executor — completed work ledger

Completed roadmap-grain work for the shared batch executor. The ahead-only queue is
[roadmap.md](roadmap.md); current architecture is [decisions.md](decisions.md); design evidence lives under
[../discussions/](../discussions/). This is not a commit changelog: entries record closed capabilities,
contracts, tests, and migration evidence.

| item | closed | contracts | witness |
|---|---|---|---|
| Initial execution kernel — greedy finite queue over a runspace pool; direct runspace and child-PowerShell modes; auto/explicit worker budget; ISS presets and per-runspace initialization; structured item results with stable ordering and failure continuation | 2026-08-04 | D1, D3, D5 | `src/shared/batch-executor.ps1`; executor Pester suite; commit `080bac6` |
| Child-process lifecycle hardening — headless/window/profile/environment/priority/timeout policy; concurrent live-child registry; parent process-tree kill on token cancellation, timeout, and final teardown; child diagnostics and logger correlation | 2026-08-04 | D5, D6 | process-mode tests in `tests/shared/batch-executor.Tests.ps1`; commit `080bac6` |
| Job and plan model — heterogeneous per-job modes under one budget; named/positional entrypoints; runtime dependencies; per-job process specifications; cost-biased dispatch with original-order results; duplicate ID, runtime-profile, process-policy, and declared write-collision validation | 2026-08-04 | D1, D4, D8–D11 | `tests/shared/batch-plan.Tests.ps1` (8 tests); commit `080bac6` |
| Initial batch-executor architecture canon — retired the Colonel/Gauntlet two-tier framing; fixed phase-not-mode decomposition, one-module/two-layer package, payload-source boundary, four-command export surface, mechanical-first sequencing, and teardown-before-move gate | 2026-08-04 | D1–D16 | [independent proposal](../discussions/opus-batch-executor-independent-proposal-20260804.md); [proposal and review](../discussions/sol-batch-executor-module-proposal-20260804.md); this planning set |
| BEX-101–BEX-105 — teardown safety gate: real child/grandchild fixture; token cancellation with queued-launch suppression; per-child and total-batch timeouts; hosting-pipeline stop/unwind; bounded survivor cleanup in the harness. The first host-stop witness exposed an indefinite CLR wait, replaced by 200 ms interruption checkpoints so PowerShell stop reaches parent-owned `finally` teardown promptly | 2026-08-04 | D6, D7 | `tests/shared/batch-executor.Tests.ps1` (20 tests); focused suite repeated green; complete shared suite 121 green; `src/shared/batch-executor.ps1` wait loop |
| BEX-201–BEX-206 — deterministic module package; payload-source boundary; mechanical public/private host split; four-command canonical surface; private worker-budget coverage; `Compile-BatchPlan` compatibility facade; capability contract | 2026-08-05 | D12–D16 | `src/shared/batch-executor/batch-executor.psd1`; module README; 20 executor + 8 plan + 8 module-surface tests; complete shared suite 129 green |
| BEX-207 — restored the repository topology gate after the membrane/PDF-converter eviction without weakening missing-input detection; removed orphaned publisher and membrane fixtures, retired the stale MCP registration, moved the governance prototype to the graveyard, corrected live paths and descriptions, and made MCP validation fail on absent/malformed server sections, commands, args, or script targets | 2026-08-05 | Phase 2 exit gate | `tests/infrastructure/path-topology.Tests.ps1` 3 green; corpus health 6 green; reader MCP 9 green; Markdown postprocess 12 green; complete shared suite 129 green |
| Phase 3 entry safety closure — made dispatcher unwind the final kill owner for every child it starts, replaced its long CLR child wait with interruption checkpoints, and changed parent unwind to submit all pipeline stop requests before awaiting any one stop. Together these close pre-registration and post-sweep registration races without letting queued work run in serial waves; a queued multi-item hosting-pipeline-stop witness covers the boundary | 2026-08-05 | D6, D7 | `src/shared/batch-executor/private/process-lifecycle.ps1`; `src/shared/batch-executor/payloads/process-dispatcher.payload.ps1`; process-tree host-stop tests in `tests/shared/batch-executor.Tests.ps1` |
| BEX-301 — froze type-tagged private contracts for resolved process specs, dispatch-ready items, write-once execution preparation, the single mutable lifecycle owner, invocation terminal override, wait outcome, and legal lifecycle transitions; explicitly froze the public Phase 3 projection | 2026-08-05 | D17, D18 | `src/shared/batch-executor/private/execution-state.ps1`; 7 state-contract tests; complete shared suite 137 green |
| BEX-302 — extracted pre-dispatch, execution-resource-free preparation: script/mode/ID/spec validation, string-normalized environment and policy snapshots, shared budget, session configuration, per-item direct copies, encoded child command, and every process payload now complete before the pool opens or any `BeginInvoke`; preparation failure is all-or-nothing, plan compilation remains separate, mixed-mode process dependency semantics are unchanged, and `DispatchMs` is submission-only | 2026-08-05 | D1, D2, D10, D17, D18 | `src/shared/batch-executor/private/executor-preparation.ps1`; 5 preparation-contract tests; 21 executor tests; complete shared suite 142 green |
| BEX-303 — bound the one mutable lifecycle owner before the exported outer `try` and extracted dispatch. The pool is owner-visible immediately after construction, a pending-pipeline slot covers construction through `BeginInvoke`, invocation records are registered incrementally after successful submission, launch failures remain item-local results, execution modes share the same pool/registry/budget, legal phase transitions are live, and the exact public projection excludes every private handle | 2026-08-05 | D1, D2, D17, D18 | `src/shared/batch-executor/private/executor-dispatch.ps1`; partial-pool-open and pending-pipeline disposal witnesses; exact public-projection witness; 4 lifecycle tests; 21 executor tests; complete shared suite 146 green |
| BEX-304 — extracted interruptible await/cancel as one lifecycle phase using only frozen preparation policy. The phase records one typed outcome, applies 200 ms host-interruption checkpoints to both the main wait and bounded process drain, and retains child kill -> direct stop -> one batch-wide process drain -> final batched stop ordering; an already-cancelled process batch proves queued supervisors start no children | 2026-08-05 | D2, D6, D7, D17 | `src/shared/batch-executor/private/executor-await.ps1`; 2 await-contract tests; 21 executor/adversarial tests; complete shared suite 148 green |
| BEX-305 — extracted result collection into the lifecycle owner. Every invocation materializes into its original index, pipeline and envelope diagnostics retain their existing merge rules, the public result shape and original caller `Input` identity are unchanged, `EndInvoke` and post-invoke normalization failures cannot suppress siblings, all result slots are required before `Collected`, and resource disposal remains teardown-owned | 2026-08-05 | D2, D5, D17, D18 | `src/shared/batch-executor/private/executor-collection.ps1`; 3 collection-contract tests; 21 executor/adversarial tests; complete shared suite 151 green |
| BEX-306 — extracted teardown and final execution-record assembly while retaining one lexical outer `try/finally` in the exported function. Teardown preserves child -> supervisor -> pool order, clears handles only after confirmed disposal, and cannot claim `Closed` when a handle or child registry entry remains; final projection requires that closed lifecycle and exposes no private state. Lifecycle transitions enforce dispatch accounting, typed wait outcome, complete result materialization, and completed handle-free teardown | 2026-08-05 | D2, D6, D7, D17, D18 | `src/shared/batch-executor/private/executor-teardown.ps1`; 3 teardown/assembly tests; 8 state-contract tests; 21 executor/adversarial tests; complete shared suite 155 green |
| BEX-307 — closed Phase 3 after re-running process-tree cancellation/timeout, queued host-stop, failure-containment, stable-order, mixed-mode, concurrency-pressure, module-surface, and topology gates. Final ownership review moved pool publication inside its construction helper and made failed dispatch-side disposal retain the pending slot; 6 lifecycle witnesses cover both exceptional windows. D2, D17, and D18 are implemented; the ahead-only roadmap now begins with thin domain adapters, and an AST witness freezes the exported function as one lexical `try/finally` phase orchestrator | 2026-08-05 | D2, D6, D7, D17, D18 | 21 executor/adversarial tests; 8 job/plan tests; 9 module/structure tests; 6 infrastructure topology/dependency tests; complete shared suite 158 green |
| BEX-401 — added `Get-TestBatchJob` without expanding the executor surface; it now lives with sibling planners in the shared `adapters` module. It discovers one isolated `PowerShellProcess` job per Pester file, pins the exact Pester 5-or-newer manifest and child PowerShell, derives stable identity from repository-relative source plus normalized filters, and routes its sole declared write (native XML) through the D19 address resolver. Planning neither allocates a run nor creates artifacts; generic execution results remain in memory. The repository runner now throws on empty or failed runs so failures propagate through both direct and nested workers without suppressing siblings | 2026-08-05 | D5, D8, D11, D19, D20 | `src/adapters/public/Get-TestBatchJob.ps1`; 7 adapter tests; 21 executor/adversarial tests; 6 infrastructure topology/dependency tests; complete shared suite 158 green; complete repository suite 466 green |
| BEX-402 — added `Get-LatexBatchJob` without freezing the provisional parent-inventory schema or expanding either executor or latex-ingest surfaces; it shares the `adapters` module rather than owning a unitary package. A caller-selected manifest projection maps each source-ready document to one isolated process job with stable source/config identity, exact latex-ingest dependency hash, explicit child policy, preserved environment transport, cost hint, and its original inventory row. One D19 resolver assigns application-run, lane-output, and optional bundle roots beneath the existing caller run; these are complete declared writes and planning creates none. The worker invokes only the manifest-backed production entrypoint, never source initialization or compatibility, and generic results stay in memory | 2026-08-05 | D5, D8, D11, D19, D21; latex-ingest D15, D17 | `src/adapters/public/Get-LatexBatchJob.ps1`; 8 adapter tests including sibling failure containment and one live source-deposit/latex-ingest child; 6 infrastructure topology/dependency tests; complete shared suite 158 green; complete repository suite 474 green |
| Pester testing-overhaul scoping — rejected adapter-first automatic fact slicing over tests that were not authored as independent jobs; recorded the pre-BEX-403 42-file/approximately-451-`It` structural census; selected one fresh-process Pester file/container as the default atomic boundary; separated test-authoring, runner, adapter, executor, and thin-shell ownership; imported only exact discovery/invocation and failure-isolation lessons from ThermoMapper; and created the ahead-only BEX-501–BEX-507 queue. This closes planning only: no test, runner, adapter, or runtime implementation changed | 2026-08-05 | D19, D20; BEX-403 entry gate | [testing-overhaul brief](../briefs/sol-pester-batch-testing-overhaul-20260805.md); [testing-overhaul workplan](testing-overhaul-workplan.md); [ahead-only roadmap](roadmap.md) |
| BEX-403 — closed Phase 4 by freezing adapter thinness as a cross-adapter structural and runtime gate. Production adapter source calls the executor only through `New-BatchJob`; public planners expose no execution-owner inputs; all adapter source is barred from private schedulers, PowerShell hosts, process lifecycle, cancellation, and parallel pipelines; and planner hosts are barred from run allocation, retry, logger lifecycle, generic-result persistence, and indirect invocation. Existing single-resolver witnesses plus strengthened planning/integration checks prove each entire caller run stays untouched during planning, worker destinations equal declared writes beneath that run, and every produced Pester or LaTeX file is covered by its job writes. No production runtime changed, and D19 remains accepted pending shared run-context infrastructure | 2026-08-06 | D8, D11, D19 current adapter realization, D20, D21 | `tests/adapters/adapter-thinness.Tests.ps1` (2 tests); 17 focused adapter tests; 6 infrastructure tests; complete shared suite 158 green; complete repository suite 474 passed plus 2 dependency-gated skips (476 total) |
| BEX-208 — retired the flat compatibility facade after a tracked-repository and active sibling-code inventory found zero production consumers; the only executable consumer was its own equivalence test. Deleted `src/shared/batch-executor.ps1` and the `Compile-BatchPlan` alias, replaced equivalence coverage with a tombstone requiring both legacy path and command to remain absent, and made the manifest the sole supported load path. Historical ledger and discussion references remain as provenance; the canonical four-command surface and runtime behavior did not change | 2026-08-06 | D14, D15 superseded, D22 | 8 module-surface, 8 batch-plan, 17 adapter, and 6 infrastructure tests; complete shared suite 158 green; complete repository suite 474 passed plus 2 dependency-gated skips (476 total) |

## Baseline at canon establishment

- Focused batch suites: 26 passing tests (18 executor, 8 plan).
- Complete shared suite: 119 passing tests.
- Production source consumers: none; the two batch test files are the only current loaders.
- Public loading form: flat script dot-source, pending BEX-201–BEX-205.
- Known lifecycle-test gap: current tests verify direct children but not descendant process trees or
  hosting-pipeline exceptional unwind; BEX-101–BEX-105 are the next gate.
