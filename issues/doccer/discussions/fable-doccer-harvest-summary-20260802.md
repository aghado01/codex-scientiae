Re-examining with the axis pivoted — the seed read was organized by _specimen_ (file); this question asks for the view organized by _capability_, which is exactly the evaluation act the catalog was waiting for. The headline: every hand-rolled interval-algebra solution in the four files decomposes cleanly into latent-capability + store-data + residual-policy. Exactly one mechanism hole exists (pairing), and exactly one family has desire outrunning the engine (the byte/edit plane). Here's the full decomposition:

| latent engine capability (exists today)                                                                                                                                                                                      | public utility desired                                                                                                | domain adapter residue                                                                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Scoped collection under suppression** — rule scope ∩ caller scope, matches can't bridge exclusions (D3/D6/D16). Bespoke twins: `Get-MaskedText` blank-to-space, md-repair's fence toggle, mdnav's line-scoped noise scan   | `collect` — rule pack + scope in, claims (or counts) out                                                              | the two **rule packs** (LaTeX registers; markdown constructs + noise species) as store data; thresholds (`≤2` prose words, `≥4` text-prose) stay policy                          |
| **Span-set algebra** — `SpanSet.{Union, Intersect, Subtract, Complement}` complete. Twins: the entire masks.ps1 op table, `TexProse/TexExpandable` compositions, mdnav's `mergeSpans`/`coveredWithin`                        | the span-algebra verb — sets in, set or measure out                                                                   | composition **recipes** as store entries (`prose = ¬(math ∪ code ∪ comments)`, `alignment-fault = bareAmp − env`) — the missing-example bin, zero engine work                    |
| **Slice/rebase** — D19 `ToParent`/`ToChild` + weaving. Twin: `Move-Mask`/`Limit-Mask`, the pincer substrate                                                                                                                  | rides the first two as scope/rebase options; the macro-expansion demo is its showcase                                 | which window (line/chunk/environment) = domain choice                                                                                                                            |
| **Line grain** — D15 content extents, D21 total `ByLine` with membership policies. Twins: `Split-AtLevel` (with its CR bug), both line tables                                                                                | an output mode on collect/project, not a separate verb                                                                | — (the _byte_-offset variant is not latent; see F3 below)                                                                                                                        |
| **Runs + named measures** — `EmitRuns` break-keys, D23 gap cadence. Twins: `constructRuns`, `cadence()`, `Get-MaskCoverage`, `compositionOf`                                                                                 | later: composition / coverage / partition-size stats as individually named D8 measures — **catalog, not yet species** | meaning thresholds (`cv<0.6`, `≥4` occurrences) stay consumer, per D23's own precedent                                                                                           |
| **Containment lookup** — D24 `FindContaining`/`ClaimOrder`. Twins: mdnav's `anchorAt` binary search, `marks`' containing-heading                                                                                             | an annotate-with-containing-unit option on verbs                                                                      | basis choice                                                                                                                                                                     |
| **— (missing mechanism)** pairing: stack-discipline open/close matching, composition-refusal argued                                                                                                                          | `pair`, after its engine contract lands                                                                               | open/close token rules = store entries (`\begin/\end` name-aware, fences char/len/close, `\left/\right` family); fault _remedies_ = orchestration; the residue itself = evidence |
| **— (missing family)** byte plane + edit application: F3 (encoding map) + F1 (edit plans, contract drafted). Live twins: `Get-MdLineIndex`, `Set-MdSpan` + back-to-front batches, mdnav's per-match `byteLength` conversions | an edit-apply capability, eventually — the one place desired utilities outrun the engine today                        | fix _content_ (what to write) = domain; safe _application_ (where, without drift, staleness-guarded) = engine-family business                                                    |

The per-file rewrite futures fall straight out: **masks.ps1** is a ~100% mechanical twin — reborn as the thin veneer roadmap item 3 already promises. **latex.ps1** becomes a rule pack plus roughly ten recipe lines plus a small policy table (fix maps, thresholds, the dangling-operator suffix logic). **md-repair.ps1** splits down its own middle — the inventory half collapses to `collect`, the verdict cascade stays PS forever by design, and the splice half waits on F1/F3. **mdnav** gets no rewrite at all — succession via F6 with its fixtures as the oracle, per standing doctrine.

What we actually _learned_, distilled: the adapters' residue is remarkably thin — patterns-as-data, a handful of thresholds, verdict and fix tables — meaning these files are mostly mechanical twin and the D10 boundary was drawn in the right place years before the engine existed. The two verbs awaiting your ratification would give every row above its reach-for except the last two, which are contract work, not verb work. Appending this capability-view as a dated addendum to the harvest doc:

Addendum committed as `bbae557` — the harvest doc now carries both views: the specimen tables from the read, and the capability decomposition on top. The catalog's state of play is unchanged but sharper: two verbs sit ready for your ratification with every latent row waiting on them, `pair` waits on its engine contract (composition-refusal already argued), the F1/F3 pair carries live witnesses whenever you judge that family ripe, and everything else stays pinned in the drawer as specimens — cataloged, unconcluded, exactly as the discipline demands.
