Current local state is intentionally not clean. I found these existing changes and will preserve them:

- Modified: `AGENTS.md`, `TODO.md`, `brewery/README.md`
- Modified dependency file under `tools/tikz-render/node_modules/`
- Untracked: the thread export itself
- `main` is 32 commits ahead of `origin/main`

The checked-in template now points readers to `codex-reader`; the existing `2408.16741v2` bundle is stale and still contains the old fictional `view_file` guidance and old schema. I understand that old bundles and historical runs are residues to be regenerated or removed, not migrated as authoritative assets.

No files were changed and I did not run the suite.
