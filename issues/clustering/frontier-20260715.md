# Figure-lane frontier — 2026-07-15

**Status: FRONTIER BRIEF — supersedes [frontier-20260713.md](frontier-20260713.md).** Re-mint
occasion: external review of the 0713 plan ([sol-pdfdig-feedback.md](../sol-pdfdig-feedback.md)),
verified claim-by-claim against the repo on 2026-07-15 — every checkable claim held. The plan
survives with **four amendments and two scope splits, all accepted**: C′ closing-record checklist,
A3-0 upgraded to a four-layer localizing probe, D-0 given a real alignment oracle + negative
controls, the A3-vs-D decision rule predeclared, E split E1/E2/E3, F split F1/F2. Nothing was
re-measured since 0713; §0 carries forward. Historical log:
[tier3-engineering-plan.md](tier3-engineering-plan.md).

## §0 — Instrument state (unchanged from 0713)

**Gate (both corpora, `Compare-FigureCounts` two-population):**

| | PRIMARY (captioned vs floats) | SECONDARY (uncaptioned vs inline) |
|---|---|---|
| ph-zigzag (10 papers) | **0.7** (7/10 exact, 0 over) | 5.6 recorded → **5.3 with C′ on** |
| voroninski (23 papers) | **0.35** (18/23 exact, 0 over) | 11.74 recorded → **9.96 with C′ on** |

Clustering PRIMARY is DONE on both corpora — every residual under is attributed to a non-clustering
cause. Zero over-detections anywhere.

**Knob states (figure_regions):** everything landed-and-on except one:
- `banded_metric` — **OFF, parked by verdict** (B-4: targets immune, adverse SECONDARY, λ re-sweep
  closed against it twice).
- `stray_eject` — **ON (default) since 2026-07-15** — crop eyeball passed 13/13, C′ CLOSED (§2).

**Corpus/infra state:** de-hyphenation landed + accepted 4/4. 1109 + 2008 + 2403 have fresh pig
runs; the other 30 papers' newest runs predate de-hyphenation (text-only difference — geometry
identical; refresh on natural cadence).

**Gauntlet migration (2026-07-15, post-re-mint):** the test beds now live at
`ingestion/gauntlet/{voroninski, ph-zigzag, mapper}` (charter: ingestion/gauntlet/README.md;
mapper enters as the declared TRANSPORT corpus). Pointer sweep applied across src/scratch/tests;
post-move gate reproduced PRIMARY row-exact on both calibration corpora. **Instrument nuance made
explicit by the move:** the LIVE gate on on-disk runs reads 0.7/5.4 · 0.35/11.57, while the
recorded baselines in the table above (5.6/11.74) are the offline harness's re-cluster with
current code — both true, different instruments (most on-disk runs predate recent landings); they
converge on full-corpus regen, and knob decisions always compare offline-vs-offline.

**Standard-battery declaration (`9dc0faa`, same day):** the gauntlet is the STANDARD TEST BATTERY
for converter dev work — charter + battery table in `ingestion/gauntlet/README.md`, pointer in
CLAUDE.md. ph-zigzag + voroninski (calibration) gate every increment; mapper (transport) joins
milestone runs once gate-capable — its 0/10 pig-run gap is the battery's first standing task
(§2.5 item 6). **The battery accumulates:** the gauntlet is the standing intake for new
testing/stressing corpora; each enters transport-by-default and graduates to calibration only by
explicit frontier decision (README charter carries the rule).

## §1 — The landed record (compressed; details in tier3-engineering-plan.md + probe headers)

Unchanged from 0713 §1 — foundations (Tier-2 arc, PRIMARY 3.7 → 0.7/0.35), the PRIMARY-invariant
veto ladder (design law: **structural priors VETO figure-hood reliably and never assert it**), A1/A2
complete, B parked with standing artifacts (the banded metric inert behind its knob, the prose-band
sidecar = D's substrate, `banded-ablation.ps1` = the offline gate harness), C′ landed + gate-PASSED
(spine-elbow eject, post-caption region placement).

**Record hygiene (this re-mint):** the stale SECONDARY baseline comment in `banded-ablation.ps1`
(5.4/11.57 → true recorded 5.6/11.74) is corrected; `opus-clustering-next-steps.md` carries an
explicit SUPERSEDED banner so its provenance-first ordering can't be accidentally resurrected (its
lineage/cophenetic abstraction remains conceptually live for the m2 provenance view and the
ThermoMapper feedback loop — the *ordering* is what died).

## §2 — The frontier, re-evaluated

### Immediate: C′ close-out and default-on — ✅ CLOSED 2026-07-15 (this commit)

`stray_eject.enabled` is now **true** (the store default). The checklist ran in full; the detailed
record is tier3-engineering-plan.md **C′-4**. The closing record:
1. Knob-on run stamps `20260715_062456`–`20260715_064034` — one fresh pig run per manifest paper
   (1608.02165v1 + 1506.01437v2, 1109.4499v1, 1701.08493v2 ×2 rows, 1705.07576v3, 1807.04261v1,
   2210.00916, 1109.0573v2, 1509.05064v1, 1812.04176v1).
2. 13/13 verdicts PASS, zero figure-ink loss. Census re-run first reproduced C′-0 exactly (71
   clusters / 173 members @ ln-gap 1.0, sentinels byte-identical, zero dissolutions). Of the 11 FP
   rows, **8 crops are pixel-identical** (the region-level post-caption placement voids those
   cluster-level census rows — their strays live in the other split product) and **3 tighten with
   nothing lost** (1506 p31 crop 331→163pt tall, 1506 p32 369×215→264×174, 1509 p26 367×215→263×173
   — the "colorbar-risk" rows were bbox inflation over dead whitespace; no satellites existed).
3. 1608 p8 crop 399→152pt (running head + §3 heading + paragraph + §3.1 heading all leave; 4 panels
   + colorbars intact), p9 171→71pt (5 body lines leave; both panels + legend markers intact) —
   the gate-recorded capH 398→151 / 169→69 within rounding.
4. Config flip + this frontier update in the same commit. Live gate post-regen: PRIMARY row-exact
   both corpora (0.35 / 0.7, 0 over); SECONDARY 10.52 voroninski (converging on the offline 9.96 as
   knob-on runs replace stale ones). Suite 75/75 — the knob-off B-4 regression test and the
   banded-ablation harness now PIN their knobs explicitly (both sides of every off/on contrast are
   store-default-independent); `pig-run.json` now carries `stray_ejects`/`stray_eject_clusters`.
5. ~~The stale 5.4/11.57 baseline comment in `banded-ablation.ps1`~~ — fixed at this re-mint.

**Named observation (not a blocker):** the tight crops EXPOSE a pre-existing letters-lane gap —
axis titles and y-labels are wide letter blocks outside the path-ink union (V_letters attaches only
≤4em blocks), so 1608 p9 and the three tightened FP rows clip them at the new edges; they only ever
appeared "inside" the old crops by riding the stray-inflated bbox. Candidate fix: crop-bbox letter
padding, E1/`visible_bbox` family (§ E below).

### The fork: A3 vs D (probes first; rule predeclared below)

**A3-0 — the four-layer probe (targets: 2210 −2, 2302 −1, the last PRIMARY unders).**
The 0713 binary question ("do the caption's words exist?") collapses several distinct failure
locations with very different fixes. Upgraded: probe each missing float through all four lanes,
grounded by **figure identity and PDF coordinates** (never merely nearby text):

| layer | lane | failure it isolates |
|---|---|---|
| 1 | caption glyphs in `letters.jsonl` | extraction failure |
| 2 | caption tokens in `words.jsonl` | word-assembly failure |
| 3 | line/block assembly in `blocks.jsonl` | XYCut/block failure vs block-text fragmentation |
| 4 | typed node/cue visibility in `nodes.jsonl` + attachment pass | attachment failure |

**Acceptance is explicit:** recover the three floats with target-number assertions, zero new caption
claims, zero PRIMARY overs, both corpora. If letters/words survive (layers 1–2), the fix is a
**bounded caption rescue** — the lenient-cue idiom the attachment scan already uses, reusing the
existing style/geometry/separator/in-text-reference guards — NOT DLA surgery. If nothing survives
layer 2, it is IR-engine work, costed separately before anything is built.

**D-0 — glyph-cluster candidates (2112 −10), now with a real alignment oracle.**
The largest defect in the 0713 plan (review catch, verified): the 2112 diagrams sidecar has 17
records but its schema is only `n, kind, status, image, source` — **no PDF page, no bbox** — so it
cannot directly label candidate glyph clusters. Before calibrating any arrow/grid discriminator,
construct object-level truth. Three routes, in preference order:

1. **Text-anchor alignment from the `source` field** (extends the review's options): the sidecar
   carries each diagram's full tikzpicture source, and 2112's diagrams are built from math-text
   nodes (`K_0 ↔ … ↔ K_n` chains) whose glyphs — arrows included — land in the letters/words lanes
   (the very reason the path lane is empty for tikz-cd). Match distinctive token sequences from the
   source into `letters.jsonl`/`words.jsonl` → page + bbox per diagram. Transports to future papers;
   manual labeling doesn't.
2. Scale-tolerant template matching of the 17 oracle PNGs against page rasters.
3. Manual labeling of the 17 records in PDF page coordinates — cheapest for one paper, the fallback.

**Negative controls are mandatory:** multiline display equations inside 2112; several
`oracle_inline=0` voroninski papers; arrow-heavy equations (these stress the arrow-glyph
discriminator directly — the proposed primary signal). **Score object-level precision/recall, not
the aggregate −10**: missing diagrams and false equation crops can cancel exactly as stale PRIMARY
counts once did. Expect the calibration to reshape the rule — it has, all seven times.

### The predeclared decision rule (review amendment, adopted verbatim)

"Let measurements pick" was underdetermined: A3 and D optimize different objectives, and
measurements establish *feasibility*, never *valuation*. The valuation is fixed now, consistent
with the post-render objective (a missing captioned float is the loudest defect a reader sees):

1. A3-0 reveals surviving letters/words + a bounded rescue path → **A3 first**.
2. A3 requires global DLA work AND D-0 shows clean target/negative separation → **D first**.
3. A3 is engine-deep AND D lacks clean alignment or equation separation → **take F (or E1)**;
   force neither implementation.

### E — IR enrichment, split (substrate; independent, interleaves)

The 0713 "clip-group-id + color-bucket + marked-content-id" increment bundled three signals with
very different accessibility (review catch — path records today stop at paint booleans,
`pdfdig-ir.ps1` lane 4):

- **E1 — path paint color (start here, opportunistic):** PdfPig exposes fill/stroke colors and the
  crop payoff is already diagnosed (white-fill de-inflation, the 1701 id49 class, folding into
  `visible_bbox`). The opinion-free IR emits **canonical raw `fill_color`/`stroke_color` —
  colorspace + components as PdfPig reports them**; RGB normalization is already a small opinion
  (ICC conversion is approximate), and page-background/color *buckets* are derived downstream. A
  bucket in the IR would violate the measurement-vs-opinion separation.
- **E2 — clip-group identity:** separate feasibility probe. The existing `is_clipping` boolean does
  not imply group identity is recoverable.
- **E3 — marked-content-to-path association:** separate feasibility probe.

E2/E3 are deferred until their signals prove reachable. Downstream unlocks unchanged from 0713:
white-fill de-inflation, the m2 Jaccard/IDF provenance view entering `Join-FigureViews` as
graded-strength evidence, the SymmetrizationRule port, V_caption as SIGNED evidence.

### F — thrust-5 remainder, split as two asymmetric gaps (deliverable width)

Verified asymmetry: `finalize.ps1` weaves pig crops into `{slug}-membrane/`, but `publish.ps1`
resolves only the raw docling `{slug}/` source dir and only *surfaces* body-omitted images rather
than weaving them. So:

- **F1 — docling image placement/weave** into the finalized body (today the weave no-ops without a
  pig run).
- **F2 — publish source abstraction:** recognize both `{slug}/` and `{slug}-membrane/`, rewriting
  either to the canonical `images/{slug}/`. The destination contract is already correct.

Fixtures: one docling-only and one pig-only dry-run/publish fixture. Low design risk, high reader
visibility — good second-half-of-a-session work.

### Standing out-of-sample debt

The mapper gate, unchanged — and now the battery's first standing task (`9dc0faa`): once
`gauntlet/mapper` has pig runs + oracle sidecars (9/10 papers can — 2504.09042 is PDF-only), run
the gate there untouched. The transport test the em-normalized knob philosophy predicts we pass; a
failure is a frontier event worth its own brief. Mapper is the first transport corpus, not the
last — the gauntlet accumulates stress corpora transport-first (§0 battery declaration), so this
section is a rolling ledger: each new corpus adds a gate-capability debt (pig runs + oracle
sidecars) and then a standing milestone run.

### Parked

B's banded metric — revisit only if a future defect class is genuinely gap-shaped. The V_stream
re-weld watch-item stays on the gate-audit checklist.

## §2.5 — Recommended order (review order, adopted)

1. ~~Close and default-enable C′~~ — ✅ DONE 2026-07-15 (§2 record).
2. Run refined A3-0 and aligned D-0 — both probes, an afternoon.
3. Implement per the predeclared rule: A3 if bounded; otherwise D only if its negative controls
   are convincing.
4. F as the low-risk second-half task.
5. E1 color opportunistically; defer E2/E3 until their signals prove reachable.
6. Run the mapper transport gate after its 0/10 pig-run gap is filled.

## §3 — Protocol (extended; the original seven-for-seven clauses stand)

Calibrate before implementing (probe first; expect the premise to move). Gate BOTH corpora on every
increment (baseline variant must reproduce recorded numbers before the knob variant means anything).
PRIMARY invariance non-negotiable — prefer structural (pipeline placement) over empirical. Read the
render; eyeball crops before default-on. Commit probes under `scratch/` (force-add past the ignore).
New thresholds only as relative, em-normalized statistics of proven species.

**New clauses (this re-mint):**
- **Probes must localize, not merely detect** — a binary existence probe collapses failure
  locations with different fixes; layer the probe along the lane pipeline (A3-0 form).
- **Assertion thrusts are accepted on object-level precision/recall against object-level truth**,
  never on aggregate count deltas — aggregates can cancel.
- **When two thrusts optimize different objectives, predeclare the decision rule before probing** —
  measurements establish feasibility; valuation is a policy choice made in advance.

## §4 — Standing instruments (unchanged)

- `src/pdf-converter/Compare-FigureCounts.ps1` — the gate.
- `scratch/banded-ablation.ps1` — offline re-cluster + gate + sentinels (baseline / banded-on /
  eject-on / banded-eject-on); baseline self-check comment now carries the true recorded numbers.
- `scratch/band-weld-calib.ps1`, `scratch/persistence-band-calib.ps1`, `scratch/stray-eject-calib.ps1`
  — the B/C calibration probes; their headers carry the iteration records.
- The sentinel set: targets 1608 p8/p9 + 2112 p8; guards 2006 p11, 2008 p8/p12, 2204 p12, 2501 p12,
  2603 p8/p11.

## §5 — Lessons register (0713's five stand; this re-mint adds three)

1–5. Unchanged from 0713 (premises are hypotheses; pipeline order beats geometry; clamp at contact
scale; PowerShell array/`$PID`/gitignore traps; selection semantics ≥ metrics).

6. **A binary probe collapses failure locations.** "Do the words exist?" cannot distinguish
   extraction, assembly, block, fragmentation, and attachment failures — and each has a different
   fix. Layer probes along the pipeline so the answer localizes.
7. **Aggregate acceptance invites cancellation.** A −10 that improves to −2 can hide missing
   diagrams offset by false equation crops — the same failure two-population scoring was built to
   kill at PRIMARY. Assertion work needs object-level truth.
8. **Cross-objective forks need predeclared valuation.** Feasibility is measurable; the worth of a
   missing captioned float vs a SECONDARY reduction is not — decide it before the probes run, or
   the probes decide it for you post-hoc.

## §6 — Adjacent fronts (pointers, out of scope here)

src-reorg (issues/src-reorg/), library/publish two-plane redesign (issues/publish-lane/design.md),
membrane F-siblings (docling-lane weave overlaps F1 above), benchmark-harvest dispatch wiring,
mapper corpus intake.
