Not carried forward:
Random browser User-Agent rotation. The Python adapter uses an honest application/contact identity; UA rotation did not create legitimate extra quota.
Semantic Scholar’s automatic 350 ms interval merely because a key exists. A key is still used, but faster grants should become explicit configuration rather than inferred.
Sci-Hub mirror/last-good rotation, because that adapter has not been implemented.
The old process-local arXiv worker queue; background job lifecycle remains a separate milestone.
A future rate-policy model should expose interval, concurrency ceiling, backoff, bounded cooldown, endpoint failover, and any explicitly granted credential rate. It should not disguise the client or rotate identities to evade provider controls.
